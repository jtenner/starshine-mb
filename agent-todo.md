# Agent Tasks

## Scope
- Keep only active unreleased work or explicitly deferred future work.
- Group work by release target.
- Use explicit slice ids so future agents can execute in dependency order.
- Move completed work and historical evidence to the relevant docs/wiki page, release notes, or git history.

## Current Parity Focus
- Keep the Binaryen no-DWARF default optimize path as the v0.1.0 parity target.
- Canonical order and nested-shape notes live in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md` and `docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`.
- Prefer direct-pass Binaryen behavior parity first, then ordered-neighborhood replay, then preset scheduling.
- Behavior parity is not byte-for-byte wasm/text parity: exact helper shape, local numbering, and raw canonical output may differ when semantic evidence proves the same observable behavior. It is also not satisfied by a green fuzz lane if source/docs still list broad unimplemented Binaryen transform families.
- Do not widen public `optimize` / `shrink` slots until the surrounding Binaryen neighborhood is representable and oracle-proven.

## v0.1.0 Active Slices

### O4z debug startup recovery

- [O4Z-STARTUP]001 - Reduced Startup Map Init Trap
  - Status: smoke-green on 2026-06-02; keep only follow-up hardening here until a full-suite signoff or smaller artifact-generation fixture replaces the integration-shaped guard.
  - Goal: keep the repaired debug-WASI artifact/reduced fixture and conservative pass guards from regressing while broader self-optimized spec coverage is scheduled.
  - Why: the original stale artifact trap was repaired by regenerating the committed debug-WASI artifact and reduced fixture from the current correct `$moonbit.malloc` shape. Subsequent full self/debug `-O4z` prefix probes exposed optimizer owners that are now guarded conservatively: `ssa-nomerge` nested-control liveness, `optimize-instructions` commutative canonicalization, `simplify-locals-nostructure` local.tee/load sinking, `coalesce-locals` loop/structured-local.tee coalescing, and `vacuum` branchy structured write cleanup.
  - Durable evidence: `docs/wiki/raw/research/0693-2026-06-01-o4z-debug-startup-func3750.md`.
  - Remaining follow-ups:
    - [ ] Keep `tests/repros/o4z-debug-startup-map-init-repro.wasm` as the focused repro until a smaller source/build-artifact fixture replaces it.
    - [ ] Recover optimization precision one pass at a time only with focused tests and semantic evidence: nested SSA liveness, safe commutative operand ordering, local.tee-aware simplify-locals sinking, remaining path-sensitive coalesce-locals slot drift, and branchy structured vacuum cleanup.
  - Suggested tests: `moon fmt`, `moon test src/passes`, `moon build --target native --release src/cmd`, `bun test scripts/lib/o4z-debug-startup-map.test.ts`, `wasm-tools validate --features all tests/repros/o4z-debug-startup-map-init-repro.wasm`, `wasm-tools validate --features all .tmp/o4z-bench/starshine-o4z-candidate.wasm`, and the self-optimized spec smoke commands listed in the research note.

### json-as debug artifact triage

- [JSON-AS]002 - `vacuum` after repeated `remove-unused-names` corrupts json-as debug runtime state
  - Status: active correctness blocker found after removing all `remove-unused-brs` slots from the same 2026-06-05 `json-as` preset replay.
  - Goal: identify whether `vacuum`, `remove-unused-names`, or HOT lowering/splice cleanup corrupts stack/object state when `vacuum` cleans the post-`remove-unused-names` artifact.
  - Why: with all `remove-unused-brs` slots removed, `medium.bench.incremental.naive.debug.wasm` still failed. The first failing prefix was `memory-packing -> once-reduction -> global-refining -> global-struct-inference -> ssa-nomerge -> dead-code-elimination -> remove-unused-names -> remove-unused-names -> vacuum`. Prefix 8 ran repeatedly; prefix 9 serialized once and then failed during GC with `abort: Index out of range in ~lib/rt.ts:21`, followed by an `unreachable` in `~lib/rt/__typeinfo`, reached from `~lib/rt/itcms/Object#get:isPointerfree`, `Object#makeGray`, `__visit`, and `Array<RecentActivity>#__visit`.
  - Finding: the printed prefix-8-to-prefix-9 diff mostly shows removal of `const; drop` debris, but several array constructor / setter / assertion functions also show stack/local traffic moving around stores. The module validates, so the actionable issue is a runtime semantic corruption in cleanup or lowering, not malformed wasm. 2026-06-05 follow-up found the vacuum mutation itself was small, but HOT lowering after vacuum reassociated root stack values across `local.set` / call sequences in array constructors and setters; vacuum now conservatively skips root regions that mix root `local.set` with stack-effecting calls or memory copies/fills. The no-RUB prefix 9 and normal prefix 10 now validate and complete the medium-naive Node smoke.
  - Suggested tests: focused `src/passes/vacuum*_test.mbt` or pass-manager prefix fixture, `moon test src/passes`, `moon build --target native --release src/cmd`, and cloned `json-as` prefix replay without `remove-unused-brs` slots.

- [JSON-AS]003 - Optimize preset misses Binaryen O4 function/type cleanup on json-as debug artifacts
  - Status: active optimization-parity gap; do not widen the preset until `[JSON-AS]001` and `[JSON-AS]002` are fixed or safely gated.
  - Goal: close the largest size gaps against standard Binaryen `wasm-opt -O4` for `json-as` debug artifacts after correctness is restored.
  - Why: on `medium.bench.incremental.naive.debug.wasm`, Starshine reduced data and globals but left many functions/types alive. Size and section evidence from the 2026-06-05 replay: debug `222187` bytes / `276` functions / `29` types / `128` globals; fixed Starshine `--optimize -O4` `168807` bytes / `276` functions / `29` types / `38` globals; Binaryen O4 `110359` bytes / `85` functions / `17` types / `36` globals. Starshine matched Binaryen's data-section payload (`25312` bytes) but missed most function/type cleanup; the remaining size includes both live code and separate `StripDebug` custom-section bytes tracked by `[JSON-AS]008`.
  - Finding: 2026-06-05 investigation under `.tmp/json-as/build/starshine-vs-binaryen/investigation-o4-live/` shows `remove-unused-module-elements` is not the primary blocker: rerunning it after the fixed Starshine O4 output leaves medium-naive at `276` functions, and the visible roots are only five function exports plus a two-function active element segment and no start section. `duplicate-function-elimination` is a safe missing preset contributor (`optimize + duplicate-function-elimination + remove-unused-module-elements` passed Node smoke and originally reduced functions to `221` / `224` / `263` for medium-naive / medium-simd / large-swar) but does not reduce types. A 2026-06-05 DFE fixed-point widening, guarded by `duplicate-function-elimination reaches fixed point when callee dedup unlocks more duplicates`, improves that same lane to `211` / `214` / `261` functions with Node smoke and validation, reaching a modest additional code-section win while preserving the larger remaining Binaryen gap; direct `duplicate-function-elimination` 10000-case compare on 2026-06-06 is normalized-green (`6768` compared, `6768` normalized, `0` mismatches, `20` Binaryen/tool command failures). Plain `inlining + remove-unused-module-elements` removes most functions and passed Node smoke, but is size-losing on these artifacts. `inlining-optimizing` and `inlining-optimizing + dae-optimizing + duplicate-function-elimination + remove-unused-module-elements` originally got close to Binaryen function/type counts (`85` / `86` / `80` functions and `19` types in the three-artifact run) but validate-only outputs trapped under the Node runner with memory-access OOB. A 2026-06-05 correctness guard now skips the optimizing nested-cleanup suffix and final local cleanup on large modules (`>80` defined functions), so direct `inlining-optimizing --remove-unused-module-elements` and `--optimize --inlining-optimizing --remove-unused-module-elements` validate and complete Node smoke on medium-naive, medium-simd, and large-swar. This is a conservative correctness recovery, not a full inlining-derived size signoff: medium-naive / medium-simd / large-swar `opt+inlining-optimizing+rume` sizes are `174958` / `177455` / `396110`, with functions `93` / `94` / `85`, and the direct 1000-case compare still stops at the mismatch cap (`52` compared, `26` normalized matches, `26` mismatches, `1` Binaryen rec-group-zero command failure). Sampled mismatches are agent-classified as semantic-safe/size-winning Starshine cleanup of generated `const; drop` and empty-else/nop debris that Binaryen leaves, but they need a focused normalization/classification follow-up before closing `[O4Z-AUDIT-INL]`. The direct `dae-optimizing` medium-naive invalid-local validation failure was fixed on 2026-06-05 by making the selected Func237 raw-debris cleanup use the current function signature when trimming locals; direct `dae-optimizing` now validates and passes Node smoke on medium-naive, medium-simd, and large-swar, with 10000-case compare normalized-green (`6766` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). Do not schedule inlining-derived lanes for size until their direct compare mismatches and skipped large-module cleanup owners are separately closed.
  - Follow-up evidence: 2026-06-05 post-DFE probes found two unscheduled direct hot passes with small but safe-looking json-as size wins; the refreshed 2026-06-06 analysis, commands, section table, and scheduling recommendation are preserved in `docs/wiki/raw/research/0713-2026-06-06-jsonas-dfe-cf-rse-preset-analysis.md`. Summary: `--code-folding --redundant-set-elimination --remove-unused-module-elements` after `optimize + duplicate-function-elimination + remove-unused-module-elements` validated and passed Node smoke on all three artifacts, reducing medium-naive / medium-simd / large-swar sizes to `152271` / `157841` / `357830` and code payloads to `126181` / `132972` / `315404`; 1000-case compare probes for each pass were normalized-green (`998` compared, `0` mismatches, `2` Binaryen rec-group command failures); direct `code-folding` 10000-case compare on 2026-06-06 is also normalized-green (`6761` compared, `6761` normalized, `0` mismatches, `20` Binaryen/tool command failures), and direct `redundant-set-elimination` 10000-case compare is normalized-green after the 2026-06-06 loop-entry default reset fix (`6771` compared, `6771` normalized, `0` mismatches, `20` Binaryen/tool command failures). The fix keeps raw RSE from treating default writes inside loops as redundant when the loop-entry value may differ; it repaired the restored `medium.bench.incremental.simd.wasm` `opt+dfe+rume+rse+rume` and `opt+dfe+rume+cf+rse+rume` Node smoke. A follow-up restored-example lane after the fix validated and Node-smoked `opt+dfe+rume+cf+rse+rume` outputs for `medium.bench.incremental.naive.wasm`, `medium.bench.incremental.simd.wasm`, and `large.lazy.bench.incremental.swar.wasm`; those checked-in example inputs are already optimized, so this evidence is runtime-correctness only, not size-win evidence. `merge-locals` originally validated but trapped under Node on medium-naive in this same suffix position; a 2026-06-05 fix now clears raw local-copy aliases across structured control so copies before loops/blocks cannot retarget later source reads after nested destination writes. The `opt+dfe+rume+merge-locals+rume` lane now validates and completes Node smoke on medium-naive / medium-simd / large-swar (`152693` / `158266` / `358874` bytes; `211` / `214` / `261` functions), and direct `merge-locals` 10000-case compare is normalized-green (`6769` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). `simplify-locals-notee-nostructure` originally validated but trapped under Node on medium-naive in the same suffix position; a 2026-06-05 large-module guard (`>100` defined functions) now skips that direct pass on artifact-sized modules until its precise large-function hazard is reduced. The `opt+dfe+rume+simplify-locals-notee-nostructure+rume` lane now validates and completes Node smoke on medium-naive / medium-simd / large-swar (`152693` / `158266` / `358874` bytes; `211` / `214` / `261` functions), and direct `simplify-locals-notee-nostructure` 10000-case compare is normalized-green (`6764` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). Binaryen no-pass roundtripping of the Starshine `opt+dfe+rume+cf+rse` medium-naive output drops size `152271 -> 150331` and code `126181 -> 124241` without changing function/type counts, indicating a remaining encoder/canonical-structure/codegen-size gap separate from semantic optimization. A traced medium-naive `--optimize` run showed pass-local performance dominated by `simplify-locals` (`356569us` total, mostly `scan-root-region`) plus repeated hot lifting (`283080us`, `2956` lifts), while local Binaryen `wasm-opt --all-features -O4` wall time was faster in a 3-run median (`0.668s` vs Starshine optimize `0.863s`; Starshine optimize plus DFE/CF/RSE `1.165s`). Treat this as evidence for both cautious preset candidates and a hot-pass lifting/simplify-locals performance audit.
  - Current recommendation: `DFE + RUME + CF + RSE + RUME` is runtime-green and direct-pass-green as an incremental suffix candidate, but it should land only in a dedicated preset-widening slice with exact ordering tests, full gate evidence, and a decision about whether DFE's incidental name-section stripping is acceptable before explicit `[JSON-AS]008` `strip-debug`.
  - Deliverables:
    - [ ] Add preset placement for `duplicate-function-elimination`, `code-folding`, and `redundant-set-elimination` with exact-order tests, fresh `json-as` artifact validation/Node smoke, and the section metrics from research note `0713`.
    - [ ] Measure incremental contributions of `inlining`, `inlining-optimizing`, `dae-optimizing`, type cleanup / `remove-unused-types`, and function reordering/removal equivalents against the same `json-as` debug artifacts.
    - [ ] Add preset-order tests for any widened optimize/shrink slots; do not collapse repeated cleanup slots without Binaryen-neighborhood evidence.
    - [ ] Record size deltas by section (`types`, `functions`, `globals`, `code`, `data`) for medium-naive, medium-simd, and large-swar debug artifacts.
  - Suggested tests: `moon test src/passes`, `moon test src/cmd` for preset changes, `moon build --target native --release src/cmd`, direct pass compare for every newly scheduled pass, and cloned `json-as` Starshine-vs-Binaryen artifact size replay.

- [JSON-AS]008 - Schedule and measure `strip-debug` for debug custom-section stripping
  - Status: direct module pass implemented and scheduled as the final public `optimize` / `shrink` preset pass; active artifact measurement follow-up remains. `src/passes/strip_debug.mbt` and `src/passes/strip_debug_test.mbt` register and test `strip-debug`; `bun scripts/pass-fuzz-compare.ts --list-passes` lists it. The old "not implemented yet" wording is superseded by the 2026-06-07 behavior inventory. 2026-06-25 direct evidence in `docs/wiki/raw/research/0908-2026-06-25-strip-debug-custom-section-preservation.md` narrows Starshine to Binaryen-shaped name-section stripping while preserving non-name custom sections. 2026-06-25 preset evidence in `docs/wiki/raw/research/0909-2026-06-25-strip-debug-preset-placement.md` schedules `strip-debug` after `directize` in both presets; remaining work is json-as artifact measurement.
  - Goal: decide whether and where to schedule explicit `strip-debug` / `StripDebug` in size/optimize paths after direct-pass and artifact evidence.
  - Why: 2026-06-05 `json-as` size metrics showed Starshine O4 still carrying non-semantic `name` custom-section bytes in fixed standard artifacts while Binaryen O4 carried no custom sections. Current evidence: `medium.bench.incremental.naive.starshine-o4.wasm` has `14549` total custom-section bytes and `large.bench.incremental.swar.starshine-o4.wasm` has `15743`, all from the `name` custom section; `medium.bench.incremental.simd.starshine-o4.wasm` currently has `0`. This is a deterministic size gap independent of the deeper live-function/type cleanup gap.
  - Deliverables:
    - [ ] Re-measure `json-as` custom-section deltas after scheduling; expected immediate wins are about `14.5KB` on medium-naive and `15.7KB` on large-swar before addressing remaining function/type cleanup.
  - Suggested tests: focused `src/passes/strip_debug*_test.mbt`, `moon test src/passes`, `moon test src/cmd` if CLI/help changes, `moon build --target native --release src/cmd`, direct `--strip-debug` compare evidence, `wasm-tools validate --features all` on rewritten `json-as` artifacts, and Node runtime smoke for the three fixed `json-as` cases.

- [JSON-AS]004 - Keep a cloned json-as benchmark replay for optimizer artifact signoff
  - Status: active artifact-signoff follow-up; 2026-06-06 full `as-test` suite replay is Starshine-green for the pinned clone but not yet scripted as a durable repo task.
  - Goal: make the `json-as` debug-artifact comparison repeatable without relying on temporary restored wasm files under `examples/`.
  - Why: the restored example wasm files were already heavily optimized and made Starshine look size-regressive. A fresh git clone of `json-as` at `f707d68d5ce5136ecfd0c576140421286c9e93a8`, local `bun install`, `assemblyscript@0.28.17`, and `bun run build:transform` produced debug artifacts where Binaryen O4 runs successfully and Starshine currently validates but traps. The clone path used during triage was `.tmp/json-as`, which is intentionally not durable. On 2026-06-06, the stronger suite replay built all 105 configured `as-test` wasm artifacts (`35` specs across `naive`, `swar`, and `simd`) with a Node WASI runner, optimized them with current Starshine `--traps-never-happen --closed-world --optimize -O4`, validated every output with `wasm-tools validate --features all`, and ran the full suite successfully: `35` files, `1284` suites, `10656` tests, `3` modes, `0` failures. The comparable Binaryen `wasm-opt --all-features --traps-never-happen --closed-world -O4` suite replay also passed. Starshine suite outputs total `57,167,519` bytes versus Binaryen `44,705,784` bytes (`+27.87%`), so correctness is ahead of size parity.
  - Runtime measurement note: without `d8`/`v8` available locally, the 2026-06-06 direct performance pass used the Node WASI test artifact runner as a proxy, not the official `json-as` benchmark harness. Two cold process runs per optimized suite artifact gave summed medians of Starshine `161,962.7ms` vs Binaryen `153,542.5ms` (`+5.48%` Starshine slower overall): `naive` `+17.73%`, `simd` `-0.48%`, `swar` `-0.69%`. Treat these as local artifact-runner timing evidence only; stable benchmark claims still require the real benchmark runtime.
  - Deliverables:
    - [ ] Add a documented, opt-in replay command or task that clones `json-as` into `.tmp/`, pins AssemblyScript `0.28.17`, builds `large` SWAR, `medium` NAIVE, and `medium` SIMD debug artifacts, and emits Starshine/Binaryen outputs without touching committed examples.
    - [ ] Prefer the repo's `d8` runner when available; otherwise use a checked-in Node runner equivalent for local smoke comparisons.
  - Suggested tests: opt-in script/task dry run, `wasm-tools validate --features all` on all generated outputs, and runtime smoke under Node or `d8` for debug, Binaryen O4, and Starshine outputs.

### Whole-command wall-time budget

- [WALL]001 - Cross-Pass Runtime Budget And Attribution
  - Goal: own whole-command Starshine-vs-Binaryen wall-time measurement outside individual pass parity slices.
  - Why: direct pass slices may be signed off while aggregate preset or no-op command paths still hide parse/emit, validation, HOT lift/lower, cache, or buffering costs.
  - Current known target: direct `--ssa-nomerge` whole-command replay on `tests/node/dist/starshine-debug-wasi.wasm`; 2026-06-14 attribution shows the wall spike is raw SSANM work, not the parsed `pass:*` timer.
  - Current evidence: `.tmp/self-ssa-nomerge-debug-wasi-walltime-investigation-final-20260614/result.json` recorded Starshine `4564.108ms` wall time, `0.199ms` pass-local time, `1590.243ms` `raw:ssa-nomerge:*` time, `466.093ms` other traced time, and `2507.573ms` untraced overhead vs Binaryen `918.897ms` wall / `431.565ms` pass time. Command-level traced phases were read `5.919ms`, decode `153.825ms`, final validate `154.813ms`, reuse check `1.302ms`, encode `137.987ms`, and output write `5.719ms`. Top raw functions are now small and spread out, led by `Func 499` `_M0FP37jtenner9starshine6passes41dae__instr__is__nontrapping__pure__binary` (`26.630ms`), `Func 3781` `_M0FP37jtenner9starshine6binary32decode__instruction__with__depth` (`9.515ms`), `Func 498` `_M0FP37jtenner9starshine6passes40dae__instr__is__nontrapping__pure__unary` (`8.576ms`), and `Func 3490` `_M0MP37jtenner9starshine4wast12KeywordTable3new` (`8.542ms`). Earlier stale-binary attribution in `.tmp/self-ssa-nomerge-debug-wasi-walltime-investigation-func-fixed-20260614` showed a 59s raw cliff; the current rebuilt binary supersedes it. 2026-06-30 vacuum slot23 trace-batching reduced the direct `--vacuum` trace to `42` lines, but the timing-only repeats still measured Starshine whole-command `623.617ms` / `634.730ms` vs Binaryen `540.657ms` / `565.452ms`; Starshine pass-local stayed faster (`1.916ms` / `1.887ms` vs `145.407ms` / `147.442ms`), leaving residual wall primarily in generic decode/final-validate/encode plus the remaining `guard:vacuum-writeback-batch`, not trace volume.
    - Separate pass-local runtime, raw fast-path runtime, harness/tool startup, parse/emit, validation, HOT lift/lower, analysis cache, and artifact representation costs.
    - Maintain a prioritized list of cross-cutting runtime fixes without blocking pass correctness signoff on aggregate wall time.
    - Evaluate remaining non-pass candidates such as encoder size/backpatch and code-section buffering reduction.
  - Suggested tests: focused timing traces, `moon info`, `moon fmt`, `moon test`, and targeted self-compare commands for any changed pass/tool path.

- [WALL]002 - Self-optimization O4z wall time is dominated by `inlining-optimizing`
  - Status: root cause fixed 2026-06-15; keep active for follow-up sub-hotspot optimization and full self-opt signoff.
  - Goal: reduce or explicitly gate the `inlining-optimizing` whole-module runtime on the Starshine debug CLI artifact before treating self-optimization as practical for routine use.
  - Why: the successful traced run `.tmp/self-opt-after-live-label-fix.stderr` spans about `13m18s` by file timestamps (`03:53:42` to `04:07:00`). Trace timer attribution shows a single `pass:inlining-optimizing` instance taking `676.916s` (`11m16.9s`), about `85%` of the observed wall time. The next largest pass timer is `dae-optimizing` at `1.914s`; cumulative HOT `lift` across `35,631` function-pass lifts is `3.017s`, `merge-blocks` totals `0.424s` across `12,994` calls, and module verification/final validation timers are sub-second per pass. Current inlining trace only reports nested cleanup touched counts (`1932`, `465`, `70`, `6`, `1`) and has no internal phase timers, so the bottleneck is localized to `src/passes/inlining.mbt` / `inlining_run_module_pass` but not yet subdivided between graph analysis, function-info building, call rewriting, helper removal, nested cleanup, and iteration/fixed-point control.
  - Suggested tests: `moon fmt`, focused `src/passes/inlining*_test.mbt`, `moon test`, `moon build --target native --release src/cmd`, timed `bun self-opt optimize`, and any direct `inlining-optimizing` compare lane needed after behavior changes.

### O4z Per-Pass Deep Audits

Release gate: complete these before the v0.1.0 release so `-O4z` pass coverage is more comprehensive and pass-local runtime owners are known before publishing.

Use this checklist for every `[O4Z-AUDIT-*]` slice below:
- Start from the pass wiki page and owner source/test files; update docs if findings become durable.
- Treat the target as Binaryen behavior parity, not output parity. A direct compare lane with `0` mismatches is necessary evidence, but it does not close an audit while docs still say the pass is a narrow subset or list broad missing Binaryen behavior.
- Run or refresh direct pass oracle evidence by building `src/cmd` once (`moon build --target native --release src/cmd`) and using `bun scripts/pass-fuzz-compare.ts --pass <name> --count 1000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` first, then scale to 10000 only when changing behavior or closing the slice.
- Inspect tests for missing positive/negative shapes, add focused test-first fixtures for any bug or missed optimization, and keep validation failures separate from representation drift.
- Capture pass-local timing where available; file whole-command issues under `[WALL]001` unless the pass is clearly the owner.
- Replay the pass's `-O4z` slot/neighborhood when it has saved artifacts or documented generated-audit evidence.
- Close with an agent-classified findings note: bugs found/fixed, missing behavior added, representation-only drift, tool/oracle failures, performance owners, deferred risks, exact commands, counts, and artifact paths.
- Do not remove an audit from this backlog unless every remaining Binaryen behavior difference is narrow, evidence-backed, tool-blocked or explicitly accepted as a non-goal, and linked to reopening criteria.

Behavior inventory source: `docs/wiki/raw/research/0714-2026-06-07-o4z-behavior-parity-inventory.md`.

Reopened from prior removed audits because committed docs still list behavior gaps:

- [O4Z-PRESET-BEHAVIOR] - Reconcile documented Binaryen no-DWARF order with Starshine public presets
  - Status: active behavior-inventory slice; do not widen presets until direct pass audits and ordered neighborhoods are green.
  - Current finding: after the DFE audit and the 2026-07-12 public `reorder-locals` scheduling update, public `optimize` / `shrink` include Binaryen-shaped early and late DFE neighborhoods, the public three-slot `reorder-locals` cleanup story, `dae-optimizing`, `inlining-optimizing`, `duplicate-import-elimination`, and one early `remove-unused-module-elements` slot. Remaining documented count/order gaps are now outside DFE and RL scheduling: the second pre-pass `remove-unused-module-elements`, `code-folding`, `redundant-set-elimination`, and the known extra Starshine `remove-unused-brs` slot. Local `wasm-opt --version` now reports `version_130`, so the exact path needs a fresh `version_130` reread before final non-DFE scheduling claims.
  - Deliverables: refresh the no-DWARF path against the current local `version_130` oracle, add exact preset-order tests for any changes, and keep repeated cleanup slots intentional unless an ordered-neighborhood proof justifies divergence.

### Coalesce-locals direct-pass parity

- [COALESCE-LOCALS]001 - Random-all `heap2local-struct` local declaration / renumbering debris
  - Status: active direct-pass parity investigation for `coalesce-locals` against current Binaryen direct `--coalesce-locals`; do not mark closed until a future slice either accepts the normalization boundary with durable evidence or implements safe raw-parity local packing.
  - Shared random-all evidence: direct `--coalesce-locals` compare against Binaryen over the `random-all-profiles` GenValid lane requested/compared `1000/1000`, normalized matches `837`, raw mismatches `163`, validation failures `0`, generator failures `0`, command failures `0`. Mismatch selected-profile breakdown: `heap2local-struct=38`, `ssa-nomerge-smoke=125`, newly added `coalesce-locals-*` profiles `0`. All `652` mismatch artifacts validate with `wasm-tools validate --features all` (`binaryen.raw.wasm`, `starshine.raw.wasm`, `binaryen.wasm`, `starshine.wasm` across all `163` mismatch dirs). Downstream convergence check `wasm-opt -O -all binaryen.raw.wasm -o b.wasm; wasm-opt -O -all starshine.raw.wasm -o s.wasm; cmp b.wasm s.wasm` made all `163` optimized pairs byte-identical; optimized size delta after downstream `-O` is `0` for both mismatch families.
  - Correctness constraints for any fix: use exact local type equality only, never subtype-based coalescing; preserve parameter semantics; preserve implicit zero-initialized local semantics; preserve `local.tee` stack semantics; be conservative around branch/loop/control-flow carriers unless proven safe; do not classify a mismatch as a Starshine win merely because both outputs validate or downstream cleanup erases it.
  - Classification: cleanup-normalized local-declaration / local-renumbering debris. This is not a Starshine win and is not a semantic mismatch based on current validation and downstream convergence evidence. Starshine raw output is larger in all `38` cases (total `+471` bytes, mean `+12.39`); canonical wasm size is equal in all `38`; normalized WAT size is equal in all `38`; the existing `local-cleanup-debris` normalizer accounts for all `38`; downstream `wasm-opt -O` makes each pair byte-identical.
  - Representative shape: input has locals like `(local i32 (ref null 0) (ref null 4))` with the used nullable struct ref local originally at index `2`. Binaryen maps the used nullable struct ref local to an earlier local index. Starshine preserves unused leading declaration debris and leaves the used ref local at a later index. Treat this as local declaration packing / logical-local renumbering, not same-type-slot coalescing.
  - Reduced repro candidate:

    ```wat
    (module
      (type $F (func))
      (type $S (struct (field (mut i32)) (field eqref)))
      (func (export "main")
        (local i32)
        (local (ref null $F))
        (local (ref null $S))

        (local.set 2
          (struct.new $S
            (i32.const 10)
            (ref.null none)
          )
        )

        (struct.set $S 0
          (local.get 2)
          (i32.const 30)
        )

        (drop
          (struct.get $S 0
            (local.get 2)
          )
        )
      )
    )
    ```

  - Suggested action: accept current `local-cleanup-debris` normalization as the narrow boundary unless exact raw parity is required. If exact raw parity is required, implement a safe final local-packing / renumbering step after coalescing: keep params fixed; build final coalesced logical-local groups; mark groups used if referenced by `local.get`, `local.set`, or `local.tee`; assign new non-param indices to used groups first, matching Binaryen's observed ordering if possible; append unused local declarations later; rewrite every local index use consistently; preserve each logical local's exact type; validate after rewriting. This must not be implemented as unsafe same-type slot reuse. Binaryen appears to be renumbering logical locals, not coalescing an `i32` slot with a nullable ref slot.
  - Risk areas: params are not implicit-zero locals; nullable refs are defaultable but nondefaultable refs require preserving set-before-get proof; exact logical-local type must move with the local index.
  - Suggested reduced tests: the GC ref local declaration packing case above; exact-type-only ref test using a base struct and subtype struct to prevent subtype-based coalescing; param/implicit-local-initialization test ensuring a non-param local does not inherit param semantics.
  - Replication: use the existing pass compare harness in direct-pass form, e.g. conceptual lane `bun scripts/pass-fuzz-compare.ts --pass coalesce-locals --gen-valid-profile random-all-profiles ...`, or the equivalent direct `--coalesce-locals` Starshine-vs-Binaryen compare. Expected high-level result from this investigation: `1000` compared, `837` normalized matches, `163` raw mismatches, `38` `heap2local-struct`, `125` `ssa-nomerge-smoke`, `0` validation/generator/command failures. Validate mismatch artifacts with `wasm-tools validate --features all binaryen.raw.wasm`, `wasm-tools validate --features all starshine.raw.wasm`, `wasm-tools validate --features all binaryen.wasm`, and `wasm-tools validate --features all starshine.wasm`. Check downstream convergence with `wasm-opt -O -all binaryen.raw.wasm -o b.wasm; wasm-opt -O -all starshine.raw.wasm -o s.wasm; cmp b.wasm s.wasm`.

- [COALESCE-LOCALS]002 - Random-all `ssa-nomerge-smoke` downstream-equivalent local cleanup drift
  - Status: active direct-pass parity investigation for `coalesce-locals` against current Binaryen direct `--coalesce-locals`; do not mark closed until a future slice either implements a narrow cleanup fix or records a separately named normalization boundary.
  - Shared random-all evidence: same direct `--coalesce-locals` random-all lane as `[COALESCE-LOCALS]001`: requested/compared `1000/1000`, normalized `837`, raw mismatches `163`, failures `0`, selected-profile breakdown `heap2local-struct=38`, `ssa-nomerge-smoke=125`, newly added `coalesce-locals-*` profiles `0`; all raw/canonical mismatch artifacts validate, and downstream `wasm-opt -O` makes all `163` pairs byte-identical with optimized size delta `0` for both families.
  - Correctness constraints for any fix: exact local type equality only; preserve params, implicit-zero non-param locals, `local.tee` stack values, and control-flow carrier semantics; be conservative around branches, loops, block exits, EH/control constructs if present, params, and nondefaultable refs; do not claim a Starshine win from validation alone or downstream convergence alone.
  - Classification: downstream-`-O` equivalent local-cleanup drift; raw-size-losing direct-pass parity gap. This is not currently a Starshine win and is not a semantic mismatch based on current validation and downstream convergence evidence. The existing `local-cleanup-debris` normalizer does not account for these `125` mismatches. Starshine raw output is larger in all `125` cases (total `+1687` bytes, mean `+13.5`); canonical wasm sizes are equal in all `125`; Starshine normalized WAT is consistently larger by `+23` chars per case; downstream `wasm-opt -O` makes all `125` pairs byte-identical.
  - Aggregate opcode/count observation across all `125` cases, computed as Starshine WAT minus Binaryen WAT:

    ```text
    local.set: -500
    local.get: +125
    drop:      +625
    nop:       +125
    ```

  - Interpretation: fewer `local.set`s alone is not enough evidence for a Starshine win. The extra `drop`, `nop`, and `local.get` debris plus consistent raw-size loss means this remains a direct-pass parity gap unless stronger performance evidence is collected. Likely shapes include dead store converted to `drop` in one output but not the other; branch/return path dead local cleanup differences; block/`br_if` carrier liveness differences; final `local.set tmp; nop; drop(local.get tmp)` forwarding missed by Starshine; different cleanup order around coalescing, SSA no-merge stress, and local DCE.
  - Reduced repro candidates:

    ```wat
    ;; 1. Terminal return dead store.
    (module
      (func (export "main") (param $p i32)
        (local $x i32)

        (if (local.get $p)
          (then
            (local.set $x (i32.const 7))
            return
          )
        )

        (local.set $x (i32.const 9))
        (drop (local.get $x))
      )
    )
    ```

    ```wat
    ;; 2. Block branch dead store where local is not read after the block.
    (module
      (func (export "main") (param $p i32)
        (local $x i32)

        (block $exit
          (if (local.get $p)
            (then
              (local.set $x (i32.const 7))
              (br $exit)
            )
          )

          (local.set $x (i32.const 9))
        )

        (drop (i32.const 0))
      )
    )
    ```

    ```wat
    ;; 3. Negative branch carrier case where the local is live after the block.
    (module
      (func (export "main") (param $p i32)
        (local $x i32)

        (block $exit
          (if (local.get $p)
            (then
              (local.set $x (i32.const 7))
              (br $exit)
            )
          )

          (local.set $x (i32.const 9))
        )

        (drop (local.get $x))
      )
    )
    ```

    ```wat
    ;; 4. br_if carrier negative case.
    (module
      (func (export "main") (param $p i32)
        (local $x i32)

        (local.set $x (i32.const 7))

        (block $b
          (br_if $b (local.get $p))
          (local.set $x (i32.const 9))
        )

        (drop (local.get $x))
      )
    )
    ```

    ```wat
    ;; 5. Final forwarding debris shape.
    (module
      (func (export "main")
        (local $tmp i32)

        (local.set $tmp (i32.const 122))
        nop
        (drop (local.get $tmp))
      )
    )
    ```

    ```wat
    ;; 6. local.tee stack semantics guard.
    (module
      (func (export "main")
        (local $x i32)

        (drop
          (i32.add
            (local.tee $x (i32.const 121))
            (i32.const 1)
          )
        )

        (drop (local.get $x))
      )
    )
    ```

  - Suggested action: prefer a narrow Starshine cleanup fix if direct `coalesce-locals` raw parity matters. Candidate cleanup rules: dead `local.set` to `drop` only when the assigned value is not read on any path before overwrite or function exit; `local.tee` write removal only when the local write is dead while preserving the stack value; `local.set tmp; nop*; drop(local.get tmp)` to `drop(expr)` only when the get is the only use of the set value and there are no intervening effects/control-flow hazards. Validate transformed functions. If not fixing now, add/use a narrow explicitly named normalizer such as `dead-local-cleanup-debris`, but keep it reported separately from true direct normalized matches.
  - Evidence required before calling either `[COALESCE-LOCALS]001` or `[COALESCE-LOCALS]002` a Starshine win: raw/code-body size improvement, not regression; or canonical/downstream optimized size improvement; or measured runtime improvement with confidence intervals; or measured engine compile-time or generated-code-size improvement; or correctness/validation advantage over Binaryen. For `ssa-nomerge-smoke`, fewer `local.set`s alone is insufficient because Starshine currently has more `drop`, `nop`, and `local.get` debris and larger raw output. Any win claim must state whether it applies before or after downstream `-O`; after downstream `-O`, all current mismatch pairs converge byte-identically, so there is no remaining post-`-O` win in the current evidence.
  - Replication: use the existing pass compare harness in direct-pass form, e.g. conceptual lane `bun scripts/pass-fuzz-compare.ts --pass coalesce-locals --gen-valid-profile random-all-profiles ...`, or the equivalent direct `--coalesce-locals` Starshine-vs-Binaryen compare. Expected high-level result from this investigation: `1000` compared, `837` normalized matches, `163` raw mismatches, `38` `heap2local-struct`, `125` `ssa-nomerge-smoke`, `0` validation/generator/command failures. Validate mismatch artifacts with `wasm-tools validate --features all binaryen.raw.wasm`, `wasm-tools validate --features all starshine.raw.wasm`, `wasm-tools validate --features all binaryen.wasm`, and `wasm-tools validate --features all starshine.wasm`. Check downstream convergence with `wasm-opt -O -all binaryen.raw.wasm -o b.wasm; wasm-opt -O -all starshine.raw.wasm -o s.wasm; cmp b.wasm s.wasm`.

- [O4Z-AUDIT-SSA-FULL] - Deep audit public full `ssa` sibling
  - Status: active sibling backlog split out of `[O4Z-AUDIT-SSA]` by `[SSANM-007c]` on 2026-06-13. Keep these slices about Binaryen `SSAify(true)` full merge-local behavior; do not count them as `ssa-nomerge` closeout prerequisites except where a contrast test documents the sibling boundary.
  - Scope: direct public `--pass ssa` behavior, including non-merge freshening/default materialization already active in Starshine, plus still-open merge-local materialization, incoming `local.tee`/entry-prepend handling, full-SSA loop/branch/EH/typed-control boundaries, and direct-pass signoff.
  - Current finding: `src/passes/ssa.mbt` registers an active partial full-`ssa` hot pass. It builds an analysis-only merge plan, routes non-merge families through the shared no-merge rewrite, and deliberately leaves merge families fail-closed until the remaining `[SSA-FULL-*]` slices materialize Binaryen-style merge locals.
  - Active `SSA-FULL` work slices:
    - [ ] [SSA-FULL-002D] - Handle entry/default merge inputs and prepend ordering
      - Goal: complete full-SSA merge materialization for parameter-entry and default-entry sources, including function-entry prepends for parameter inputs and default materialization without illegal nondefaultable refs.
      - Deliverables: red-first tests for parameter-entry one-arm merges, default-entry one-arm merges, exact nullable ref defaults, skipped nondefaultable default entries, and deterministic local/prepend ordering.
      - Suggested evidence: focused `src/passes/ssa_test.mbt`, validation of lowered modules, and direct `--pass ssa` compare/fuzz when available.
    - [ ] [SSA-FULL-002E] - Classify loop, branch, EH, and typed-control full-SSA boundaries
      - Goal: decide which loop/backedge, branch-exit, `br_table`, EH, and typed-control families can safely use full-SSA merge materialization and which need fail-closed or dedicated typed/EH helpers.
      - Deliverables: positive or deliberately fail-closed fixtures, source-backed ownership notes, and docs updates that keep full `ssa` behavior separate from `SSANM` no-merge boundaries.
      - Suggested evidence: focused `src/passes/ssa_test.mbt`, direct compare for admitted mutation families, and wiki/backlog updates.
    - [ ] [SSA-FULL-003] - Direct full `ssa` closeout signoff
      - Goal: close the direct public `ssa` pass only after merge-local materialization, entry/default handling, and boundary decisions are implemented or explicitly deferred.
      - Deliverables: final parity/status note in `docs/wiki/binaryen/passes/ssa/`, exact test/fuzz commands and results, remaining accepted boundaries with reopening criteria, and backlog cleanup.
      - Suggested evidence: `moon info`, `moon fmt`, focused `ssa_test.mbt`, `moon test src/passes`, full `moon test`, native build, and a direct `--pass ssa` compare lane if supported.

- [O4Z-AUDIT-DAE] - Deep audit `dae-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit. 2026-07-04/2026-07-05 profile work added deterministic body templates for all ten direct DAE leaves plus `dae-optimizing`; plain `dead-argument-elimination` and `dae` are active module-pass registry/dispatcher entries with focused pipeline tests. Red-first slices fixed the owned-direct `struct.new_default` GC arg/result exact-reference refinement gap, focused static `local.get` result-refinement, typed-block operand argument-refinement, zero-param, parametric, and multi-result typeidx block/if/loop operand argument-refinement, typed-select operand argument-refinement including typed-call result operands, branch-free effectful call, and bounded branchful non-escaping `if` conditions, nullable `ref.null` select LUB repair, and nullable typed-select common-LUB, typed-block body result-refinement, two-caller direct-call argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, multi-result typed-select operand plus block-condition, pure-condition, call-condition, call-post-op-condition, memory-condition, and branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, zero-param typeidx block body result-refinement, zero-param typeidx terminal `if` returned-value refinement, fallthrough typed/typeidx loop single-result returned-value refinement, zero-param multi-result fallthrough loop returned-value refinement, simple multi-result `if`-arm returned-value refinement, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm returned-value refinement, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands and bounded typed-select operands in multi-result bodies including the non-tail-lane select-first/scalar-second shape, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets, direct owned `return_call` unused-parameter repair, generated single-result DAE-F coverage, a `dae-optimizing` post-cleanup convergence gap where exact non-null `ref.is_null` folded to `0` but DAE was not rerun to delete the newly dead parameter, and a second post-cleanup rerun gap where dropped-result cleanup reduced a helper to `nop` but DAE did not rerun to delete its newly dead parameters. The direct dedicated DAE profile is now scaled to 10000 in `.tmp/pass-fuzz-dae-direct-closeout-10000-single-return`: requested/compared `10000/10000`, normalized `3125`, cleanup-normalized `0`, mismatches `6875`, validation/generator/property/command failures `0`, Binaryen cache `10000/0`; failures are only the six already-classified cleanup leaves, with total Starshine deltas `-31250` canonical / `-24375` raw bytes in the original lane. A 2026-07-06 red-first type-reuse fix for the convergence raw-positive caveat added simple function-signature reuse before appending DAE replacement types; mismatch replays over the saved direct dedicated-profile and direct random-all failure dirs remain raw-red but remove the convergence raw-size regression (`dae-convergence` now `-2500` raw/canonical in the dedicated replay and `-240` raw/canonical in the random-all replay). This is still raw-red direct DAE evidence, not a green lane. `dae-optimizing` dedicated profile is green at 10000 in `.tmp/pass-fuzz-dae-optimizing-closeout-10000-single-return` under `drop-consts` and `unreachable-control-debris`; a post-fix 1000 rerun stays green, and regular GenValid `.tmp/pass-fuzz-dae-optimizing-genvalid-100000-20260705` normalized `100000/100000` with zero failures. Explicit wasm-smith is zero-mismatch after a red-first imported-tag type-liveness repair: `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-post-import-tag-20260705` requested `10000`, compared `9956`, normalized `9955`, cleanup-normalized `1`, mismatches `0`, validation/generator/property failures `0`, command failures `44` all in Binaryen/oracle classes, cache `wasm-smith 10000/0`, `Binaryen 9955/1`, `Binaryen failures 44/0`. Random-all was open through the early partial attempts: timed-out partial attempts reached `408` and `398` compared cases; replay of the 25 partial mismatches after the exact-ref-is-null fix normalized the four arg-refinement cases, and replay after the unused-return/nop-body rerun fix normalized `6/25`, leaving `19` mismatches. Those residuals are triaged as `coverage-forced-portable=11` broad pure/unreachable cleanup drift plus known `dae-localization=4` and `dae-effectful-args=4` cleanup families, not closeout evidence. A later full random-all retry `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705` timed out without `result.json` after `1037` records (`1035` compared, `985` normalized, `50` known cleanup/scheduling mismatches, `2` Starshine tail-call result command failures); red-first tail-call result-preservation fixtures are green, including an unreachable-root suffix fixture; native CLI replays of `case-000433` and `case-000434` now succeed, and compare-harness command-failure replays now have zero command failures. The replayed mismatches are classified as sampled `coverage-forced-portable` pure/unreachable cleanup with deletion-only canonical WAT diffs and validating artifacts. A bounded completed post-tail random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-500-post-tail2-20260705` requested/compared `500/500`, normalized `474`, mismatched `26`, and had zero validation/generator/property/command failures; a higher `.tmp/pass-fuzz-dae-optimizing-random-all-1000-post-tail2-20260705` lane requested/compared `1000/1000`, normalized `952`, cleanup-normalized `0`, mismatched `48`, and had zero validation/generator/property/command failures with Binaryen cache `1000/0`. Selected-profile mismatches stayed in the known cleanup/scheduling classes (`coverage-forced-portable=30`, `dae-effectful-args=9`, `dae-localization=9`) with validating artifacts and aggregate Starshine deltas `-17057` raw / `-99010` canonical / `-679016` WAT bytes across mismatch dirs. A completed 1282-case lane `.tmp/pass-fuzz-dae-optimizing-random-all-1282-post-tail2-20260705` requested/compared `1282/1282`, normalized `1227`, cleanup-normalized `0`, mismatched `55`, and had zero validation/generator/property/command failures with Binaryen cache `1282/0`; mismatches stayed in the same classes (`coverage-forced-portable=32`, `dae-effectful-args=14`, `dae-localization=9`) with aggregate deltas `-16097` raw / `-104538` canonical / `-717104` WAT bytes and all `275` input/raw/canonical failure artifacts validating. A 2000-case retry `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-20260705` timed out after 7200s without `result.json` after `1287` case records and no command failures, matching the completed 1282-case classification but remaining non-closeout. A harness follow-up added `--no-reduce-mismatches` to disable expensive GenValid byte-slice reductions for closeout counting; the 2000-case confirmation `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-no-reduce-20260705` completed `2000/2000` with `1919` normalized, `81` known cleanup/scheduling mismatches, zero failures, and all `405` input/raw/canonical artifacts validating. The required full random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` completed requested/compared `10000/10000`, normalized `9538`, cleanup-normalized `0`, mismatches `462`, validation/generator/property/command failures `0`, Binaryen cache `5766/4234`, `reduceMismatches=false`; mismatches stayed fully classified in known cleanup/scheduling families (`coverage-forced-portable=259`, `dae-effectful-args=136`, `dae-localization=67`) with aggregate Starshine deltas `-133065` raw / `-835247` canonical / `-5729443` WAT bytes and all `2310` input/raw/canonical artifacts validating. Paired probes now include 10000-case optimizing-profile ordered lanes: direct DAE + `vacuum` still only has the older 1000-case raw-red evidence with the same direct residuals; `dae-optimizing + vacuum` is green through 10000; `dae-optimizing + inlining-optimizing` alone is raw-red through 10000 because Starshine retains a private unreachable helper/type that Binaryen deletes (`+100000` raw / `+100000` canonical / `+1100000` WAT bytes, all `50000` mismatch artifacts validating); adding `remove-unused-module-elements` is green through 10000, classifying the inlining-only delta as a size-losing scheduling-neighborhood cleanup requirement rather than a DAE core bug or Starshine win; and the broader no-DWARF post-pass suffix `dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize` is green through 10000 on the optimizing profile (`.tmp/pass-fuzz-dae-optimizing-full-postpass-context-10000-single-return-no-reduce-20260706`, `10000/10000`, zero failures). The audit remains open on broader arbitrary returned-value/control/candidate type-refinement beyond the focused allocation, static `local.get` result, typed-block operand, zero-param, parametric, and multi-result typeidx block/if/loop operands, typed-select operands including typed-call result operands, nullable `ref.null` select LUB and nullable common-LUB, typed-block result, two-caller argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB including direct-call plus zero-/ten-param-probed stack-sliced call_ref/call_indirect operands, multi-result typed-select operand plus block-condition, pure-condition, call-condition, call-post-op-condition, memory-condition, and branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, zero-param typeidx block body result-refinement, zero-param typeidx terminal `if` returned-value refinement, fallthrough typed/typeidx loop single-result returned-value refinement, zero-param multi-result fallthrough loop returned-value refinement, simple multi-result `if`-arm returned-value refinement, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm returned-value refinement, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands and bounded typed-select operands in multi-result bodies including the non-tail-lane select-first/scalar-second shape, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets, direct raw-red dedicated/random-all mismatches, ordered scheduling/integration, and final closeout lanes; direct regular GenValid is green at closeout scale (`.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705` normalized `100000/100000` with zero failures). The previous direct wasm-smith 10000 size-losing `case-009332` unreachable-control-debris mismatch is fixed by a red-first raw dropped-unreachable cleanup slice: replay `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-case-009332-replay-after-debris-cleanup-20260705` normalized `1/1`, and post-fix `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705` requested `10000`, compared `9956`, normalized `9956`, mismatches `0`, with `44` Binaryen/oracle command failures. Direct random-all remains raw-red: the pre-fix 500 lane had `443` normalized / `57` mismatches, the post-fix 500 rerun timed out without `result.json`, the completed post-fix 100-case smoke normalized `87/100` with `13` known direct/coverage cleanup mismatches, the 200/300/400-case post-fix lanes normalized `176/200`, `262/300`, and `350/400` with zero failures and validating artifacts, and the full no-reduction lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` requested/compared `10000/10000`, normalized `9004`, cleanup-normalized `0`, mismatches `996`, validation/generator/property/command failures `0`, Binaryen cache `4950/5050`, `reduceMismatches=false`; residuals stayed in known direct/coverage cleanup families (`coverage-forced-portable=259`, `dae-unused-params=214`, `dae-constant-args=197`, `dae-effectful-args=136`, `dae-localization=67`, `dae-tail-call-boundary=63`, `dae-convergence=60`) with aggregate Starshine deltas `-592240` raw / `-1294602` canonical / `-8890446` WAT bytes and all `4980` input/raw/canonical artifacts validating. A 2026-07-06 stratified replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706` reran one saved raw-red case from each residual class with `--runtime-execution node`: import-free cases `000007`, `000020`, `000031`, `000046`, `000050`, and `000104` all had equal Node results, `coverage-forced-portable` case `000018` was runtime-blocked by imports, and all seven replays had zero validation/generator/property/command failures while staying raw mismatches. A follow-up red-first simple function-signature reuse test failed before DAE reused an existing equivalent simple function type instead of appending a duplicate replacement type; focused `dae_optimizing_test.mbt` passed `199/199`, `moon test src/passes` passed `4232/4232`, and native CLI rebuild passed with pre-existing warnings. A later red-first typed-block operand refinement fixture brought `dae_optimizing_test.mbt` to `200/200` and `moon test src/passes` to `4233/4233`; replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-20260706` compared `996/996`, left all saved direct random-all cases raw mismatches, and had zero validation/generator/property/command failures. A follow-up red-first typeidx-block operand fixture covers zero-param `(block (type $carrier))` actuals whose type result is a non-null leaf; it passes after DAE resolves `TypeIdxBlockType` actual candidate result types, with no compare-pass rerun and no change to direct raw-red status. A follow-up red-first typed-block result-refinement fixture now brings `dae_optimizing_test.mbt` to `201/201` and `moon test src/passes` to `4234/4234`; replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-20260706` again compared `996/996`, left all saved direct random-all cases raw mismatches, and had zero validation/generator/property/command failures. A later red-first argument-LUB fixture covers a Binaryen v130 two-caller common-supertype shape and now passes focused `*common direct-call LUB*` after DAE accumulates nearest common supertypes for non-null direct-call GC actual candidates. A subsequent red-first returned-value LUB fixture covers a Binaryen v130 terminal-`if` common-supertype result shape and now passes after DAE computes the returned-arm LUB and rewrites the terminal result instruction type. A later red-first explicit-return LUB fixture covers the same common-supertype result shape when both terminal void-`if` arms perform `struct.new_default; return` and leave a trailing `unreachable`; it now passes after DAE recognizes explicit return operands in terminal void `if` arms. A subsequent red-first loop-carried explicit-return fixture covers a Binaryen v130 shape where a terminal void `loop` body performs `struct.new_default; return` and leaves trailing `unreachable`; it now passes after DAE recognizes void loop bodies with explicit-return result evidence. A later red-first typed-control explicit-return slice covers Binaryen v130 typed terminal `if`, `block`, and `loop` shapes where all paths explicitly return allocation values; it now passes after DAE accepts `ValTypeBlockType` explicit-return evidence and rebuilds those terminal controls as void while tightening the helper result signature. Focused `*GC results*` passed `7/7`, full `dae_optimizing_test.mbt` passed `207/207`, and `moon test src/passes` passed `4240/4240` after the typed-control explicit-return slice. The later flat, droppable-prefix, simple block-carrier/nested simple block-carrier/nested explicit-return block-carrier/simple loop-carrier multi-result, zero-param typeidx block body, simple multi-result `if`-arm, zero-param typeidx terminal `if`, fallthrough typed/typeidx loop, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm, simple mixed single-result return/fallthrough, simple mixed multi-result return/fallthrough, and single/multiple flat-suffix single-result `if` operand result-refinement slices bring focused `*multi-result*` to `22/22`, focused `*single-result if operands*` to `5/5`, focused `*multi-result if arms*` to `7/7`, focused `*typeidx block bodies*` to `2/2`, focused `*typeidx if*` to `2/2`, focused `*fallthrough loop*` to `2/2`, focused `*GC results*` to `38/38` after the typed-select returned-value slice, focused `*select*` to `49/49` after the pure-condition typed-select follow-up, focused `*typeidx block operands*` to `1/1` after failing red before the typeidx-block operand slice, focused `*typeidx if operands*` and `*typeidx loop operands*` to `1/1` each after the typeidx if/loop operand slice (the if case was observed red before implementation), focused `*parameters*` to `7/7`, full `dae_optimizing_test.mbt` to `247/247`; this slice added red-first typed-select argument-operand coverage, so focused `*typed select operands*` passes `1/1`, focused `*parameters*` passes `8/8`, full `dae_optimizing_test.mbt` passes `248/248`, and `moon test src/passes` passes `4281/4281`; a follow-up red-first nullable typed-select argument slice covers `ref.null` plus a leaf allocation feeding `select (result (ref null $leaf))`, failed while Starshine kept `(ref null $base)`, and now passes after DAE treats null select operands as nullable LUB refinements and rewrites `ref.null` actual operands to the tightened select type. Focused `*nullable select LUB*` passes `1/1` after the red failure, focused `*nullable typed-select actuals*` passes `1/1` after red failure, and a later parametric typeidx block/if/loop argument slice failed red for all three controls while Starshine kept `(ref null $base)`, then passed after DAE accepted single-result typeidx control result candidates with params and final `block`/`loop` controls at the end of multi-instruction argument slices. Focused `*parametric typeidx*` passes `3/3`; a follow-up multi-result typeidx block/if/loop argument slice failed red for all three controls while Starshine kept `(ref null $base)`, then passed after DAE accepted the last non-null concrete GC result lane of final `block`/`if`/`loop` actuals. Focused `*multi-result typeidx*` passes `3/3`; a follow-up effectful typed-select condition slice failed red while Starshine kept `(ref null $base)`, then passed after DAE accepted branch-free select-condition slices with function-type-backed stack effects. Focused `*effectful typed-select conditions*` passes `1/1`, focused `*branchful typed-select*` passes `1/1` for the Binaryen-positive argument-side branchful condition coverage, focused `*typed-call select*` passes `1/1` after failing red for direct-call result operands, focused `*typed-select*` passes `2/2`, focused `*parameters*` passes `18/18`; a follow-up call-condition typed-select returned-value slice failed red on `*call-condition select GC results*` and now passes (`1/1`); a follow-up Binaryen-positive call-post-op condition coverage slice now passes `*call-postop select GC results*` (`1/1`) without a new implementation change; the previous slice added Binaryen-positive branch-free potentially trapping memory-load condition coverage and focused `*memory-condition select GC results*` passes (`1/1`) through the same stack-effect model; this slice added Binaryen-positive non-tail-lane multi-result typed-select returned-value coverage and focused `*non-tail-lane multi-result select*` passes (`1/1`) through the existing bounded flat multi-result select recognizer. A subsequent red-first slice added Binaryen-positive non-tail-lane drop-only parametric typeidx block/if/loop returned-value refinement; focused `*non-tail-lane parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed (`3/3`) after DAE recognized drop-only consumed-param terminal controls and rewrote their typeidx results. A follow-up red-first non-drop parametric typeidx returned-value slice showed Binaryen also narrows when terminal typeidx controls consume an `i32` through a stack-safe nontrapping pure `i32.eqz; drop` prefix before the flat GC/scalar results; focused `*non-drop parametric typeidx*` failed for all three controls, then passed (`3/3`) after DAE generalized consumed-param prefix recognition from direct drops to stack-safe nontrapping pure prefixes. Focused `*select GC results*` passes `4/4` before the follow-up coverage test, focused `*select*` passes `60/60` after the typed-call select-operand argument slice, focused `*branchful select-condition*` failed red then passes `1/1`, focused `*typeidx*` passes `25/25` after the trapping-load parametric typeidx returned-value slice, focused `*effectful parametric typeidx*` failed red for block/if/loop before implementation and now passes `3/3`, full `dae_optimizing_test.mbt` passes `277/277`, full `moon test` passes `7749/7749`, and `moon test src/passes` passes `4310/4310`; `moon info` passes with pre-existing warnings, native CLI build passes with pre-existing warnings, and direct DAE remains raw-red overall. A direct residual closeout replay with `--normalize drop-consts --normalize unreachable-control-debris` over those saved mismatches, `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-cleanup-normalized-20260706`, still compared `996/996`, normalized `0`, cleanup-normalized `0`, mismatched `996`, had zero validation/generator/property/command failures, used Binaryen cache `996/0`, and all `4980` input/raw/canonical artifacts validated; the measured aggregate deltas are all Starshine-smaller (`-593155` raw / `-1294602` canonical / `-8900446` WAT total), with per-profile deltas recorded in the DAE fuzzing page. A direct `dead-argument-elimination + remove-unused-module-elements` replay over the same saved residual set, `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`, also stayed raw-red (`996/996` compared, `0` normalized, `0` cleanup-normalized, `996` mismatches, zero failures, Binaryen cache `724/272`, all `4980` input/raw/canonical artifacts validating); aggregate deltas remained Starshine-smaller for canonical/WAT (`-1166714` canonical / `-8016600` WAT) and total raw (`-505487` raw), but `coverage-forced-portable` had `59` raw-positive cases in this context, so RUME does not close direct DAE residuals. A 2026-07-07 selected-profile audit saved in `.tmp/dae-random-all-residual-profile-size-summary-20260707.json` maps that RUME replay to `coverage-forced-portable=259` (`-503105` raw / `-1164131` canonical / `-7987532` WAT, `59` raw-positive and `0` canonical-positive cases), `dae-unused-params=214` (`-428` raw/canonical), `dae-constant-args=197` (`-1182` raw/canonical), `dae-effectful-args=136` (`-272` raw/canonical), `dae-localization=67` (`-134` raw / `-335` canonical), `dae-tail-call-boundary=63` (`-126` raw/canonical), and `dae-convergence=60` (`-240` raw/canonical), isolating the RUME raw-positive risk to coverage-generated unreachable-tail samples while keeping direct DAE raw-red. A 2026-07-07 red-first coverage unreachable-tail cleanup fix then reduced a raw-positive sample to `.tmp/dae-coverage-raw-positive-minisample-20260707.wat`, added `dead-argument-elimination trims unreachable tail debris from coverage-shaped functions`, and made DAE recursively canonicalize block-wrapped unreachable roots plus truncate same-region tails after them. Validation passed after the red failure: focused test `1/1`, `moon fmt`, `moon test src/passes` `4332/4332`, full `moon test` `7771/7771`, `moon info` with pre-existing warnings, and native `src/cmd` build with pre-existing warnings. The post-fix direct DAE+RUME residual replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-after-unreachable-tail-trim-20260707` still stays raw-red (`996/996` compared, `0` normalized, `0` cleanup-normalized, `996` mismatches, zero failures, Binaryen cache `996/0`, `reduceMismatches=false`), but the selected-profile size audit `.tmp/dae-rume-residual-profile-size-summary-after-unreachable-tail-trim-20260707.json` removes the previous coverage raw-positive risk: `coverage-forced-portable=259` is now `-1173747` raw / `-1175646` canonical / `-8042558` WAT with `0` raw/canonical/WAT-positive cases, and all other residual profiles remain non-positive. Direct DAE remains raw-red overall. 2026-07-07 follow-ups now fully classify three saved direct residual families for both the direct random-all lane and the post-tail-trim direct DAE+RUME replay without declaring direct DAE green: `.tmp/dae-tail-call-boundary-residual-classification-20260707.json` covers all `63` `dae-tail-call-boundary` cases, where Binaryen removes the same unused first helper parameter and pure `return_call` operands but retains one unused replacement local while Starshine omits it (`-126` raw / `-126` canonical / `-1071` WAT bytes in each lane, `0` positive raw/canonical/WAT cases, representative case `104` equal under Node); `.tmp/dae-unused-params-residual-classification-20260707.json` covers all `214` `dae-unused-params` cases, where Binaryen removes the same unused params/call operands but retains two unused replacement locals while Starshine omits them (`-428` raw / `-428` canonical / `-7276` WAT bytes in each lane, `0` positive raw/canonical/WAT cases, representative case `20` equal under Node); `.tmp/dae-constant-args-residual-classification-20260707.json` covers all `197` `dae-constant-args` cases, where Binaryen materializes the uniform removed-parameter `i32.const 77` through a fresh helper local while Starshine substitutes the same pure constant directly (`-1182` raw / `-1182` canonical / `-10835` WAT bytes in each lane, `0` positive cases, representative case `7` equal under Node). These are family-specific measured Starshine-win cleanup classifications, not direct DAE green evidence. A targeted follow-up on the largest raw-positive coverage sample (`case-007094`) replayed direct DAE+RUME with cleanup normalizers and zero command/validation/generator/property failures, remained a mismatch, was runtime-blocked by imports under Node, validated five artifacts, and measured Starshine `+1006` raw but `-4123` canonical / `-28236` WAT bytes; a follow-up raw-positive subset scan under `.tmp/dae-raw-positive-analysis-20260706` quantified all `59` raw-positive coverage cases as raw-positive (`+30761` total, `+348..+1006` per case) but all canonical/WAT-negative (`-186785` canonical / `-1280032` WAT total), with no scanned pre-terminal side-effecting keywords in input/Binaryen/Starshine raw WAT. A follow-up source review ties the first terminal to `gen_valid_append_core_control_ops()` in the coverage prelude before later effectful templates, and a bounded `wasm-tools shrink` attempt on `case-007094` accepted the raw-positive/canonical-negative original but found no smaller module in 200 attempts (`41526` bytes, `0.00%` reduction). This supports but does not close the pure-prefix/unreachable-tail cleanup classification for that subset. Replays over the saved direct random-all mismatches (`.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-type-reuse-20260706`, `996/996` still mismatched, zero failures, Node checked `737` with `0` semantic mismatches and `259` import-blocked) and saved direct dedicated-profile mismatches (`.tmp/pass-fuzz-dae-direct-closeout-mismatch-replay-after-type-reuse-20260706`, `6875/6875` still mismatched, zero failures, Node all-equal) remove the convergence raw-positive caveat: direct random-all `dae-convergence=60` is now `-240` raw/canonical / `-2040` WAT with no positive raw/canonical cases, and dedicated `dae-convergence=625` is now `-2500` raw/canonical / `-21250` WAT with no positive raw/canonical cases. This strengthens but does not close the measured-Starshine-win cleanup classification; direct DAE remains raw-red overall, not green.
  - Scope: dead argument/result removal, nested cleanup scheduler, selected-shape genericization, raw cleanup policy, type liveness, pass-local runtime, and pass-owned GenValid coverage.
  - Deliverables: apply the common checklist; coordinate with `[AUDIT003-*]`; refresh DAE-normalized direct compare and late `DAE` slot evidence; keep accepted raw-cleanup drift separate from semantic mismatches.
  - Strategy consolidation work:
    - [ ] `[O4Z-AUDIT-DAE-CONSOLIDATE]` Replace accumulated scanner-specific DAE recognizers with Binaryen-shaped reusable evidence layers.
      - Goal: move `dead-argument-elimination` / `dae-optimizing` toward one coherent module-boundary core with reusable ownership facts, value/stack-slice evidence, GC type LUB/refinement, effect-preserving repair, convergence, and explicit plain-vs-optimizing scheduling.
      - Current inventory: `docs/wiki/binaryen/passes/dead-argument-elimination/starshine-strategy.md` now groups current scanner surfaces into ownership/call-boundary analysis, value/stack-slice recovery, GC type evidence/LUB, operand/result repair, localization/effect preservation, convergence/fixpoint, and cleanup scheduling.
      - First consolidation completed 2026-07-07: `DaeGcSelectValueEvidence` / `dae_gc_select_value_slices_evidence(...)` now centralizes typed-select GC operand candidate, LUB/refinement, and rewrite-hook evidence for both `dae_direct_gc_result_select_candidate_body(...)` and `dae_try_refine_multi_result_select_operand_body(...)`; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Second consolidation completed 2026-07-07: `dae_gc_select_actual_value_slices_evidence(...)` reuses the same evidence carrier for argument-side typed-select actuals, centralizing nullable `ref.null` candidate/LUB/refinement and rewrite hooks for candidate collection plus effect-preserving callsite repair; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Third consolidation completed 2026-07-07: `DaeIndirectGcResultSliceEvidence` / `dae_indirect_call_gc_result_slice_evidence(...)` centralizes shared `call_ref` / `call_indirect` returned-value operand evidence, including signature candidate extraction, param-count checks, stack-local argument/table validation, and effect/branch rejection; red-first whitebox coverage failed on the missing helper before passing after extraction, and the old per-opcode functions now only verify opcode/target shape before delegating.
      - Fourth consolidation completed 2026-07-07: `DaeDirectGcCallResultEvidence` plus `dae_direct_call_gc_result_signature_evidence(...)` / `dae_direct_call_gc_result_refinement_evidence(...)` centralizes direct-call result signature extraction and old-result refinement for both argument-side call-result candidates and returned-value refinement; red-first whitebox coverage failed on the missing helpers before passing after extraction.
      - Fifth consolidation completed 2026-07-07: `DaeGcTypeEvidence` / `dae_gc_result_slot_evidence(...)` centralizes single-slot GC candidate, optional refinement, and rewrite evidence for multi-result single-`if` operand refinement, parametric typeidx arm result candidates, and the flat multi-result carrier tail in `dae_try_refine_direct_gc_types(...)`; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Sixth consolidation completed 2026-07-07: `DaeValueSlice` / `DaeSelectValueSlices` / `dae_find_select_value_slices(...)` centralize typed-select left/right value-slice recovery, stack-local condition bounds, optional left-start and droppable-prefix constraints, and effect/branch disposition for `dae_direct_gc_result_select_candidate_body(...)` and `dae_try_refine_multi_result_select_operand_body(...)`; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Seventh consolidation completed 2026-07-07: `DaeControlResultSlice` / `dae_find_multi_result_control_slice(...)` centralize flat, block-carried, and terminal-return multi-result arm/control bound recovery, including result start/end, lane count, carrier kind, flattening state, terminal-return state, and effect/branch disposition for `dae_multi_result_if_arm_candidate_body(...)` and `dae_multi_result_if_arm_return_candidate_body(...)`; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Eighth consolidation completed 2026-07-07: `DaeControlResultGcEvidence` / `dae_control_result_slice_gc_evidence(...)` combine recovered `DaeControlResultSlice` bounds with `dae_gc_result_slot_evidence(...)` to collect per-lane candidates, refinements, rewrite hooks, and body-change state. The parametric typeidx arm result path and the flat/block/loop multi-result carrier tail in `dae_try_refine_direct_gc_types(...)` now delegate their per-lane slot scans to the helper; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Ninth consolidation completed 2026-07-07: `DaeControlResultPairGcEvidence` / `dae_control_result_pair_gc_evidence(...)` consume paired `DaeControlResultGcEvidence` carriers, compute per-lane terminal-arm GC LUB/refinement, and selectively apply arm rewrites only for lanes whose paired LUB refined the signature. The two direct terminal-`if` LUB/rewrite loops in `dae_try_refine_direct_gc_types(...)` now delegate through `DaeControlResultSlice` + per-arm GC evidence + the paired helper instead of open-coding duplicate then/else candidate loops; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Tenth consolidation completed 2026-07-07: `dae_parametric_typeidx_multi_result_arm_gc_evidence(...)` wraps parametric typeidx arm prefix-consumption, recovered `DaeControlResultSlice`, and per-lane `DaeControlResultGcEvidence` into one arm carrier. `dae_try_refine_parametric_typeidx_multi_result_terminal_control_body(...)` now uses that carrier for block/loop/single-arm refinement and delegates paired `if` then/else LUB/refinement/rewrite handling through `dae_control_result_pair_gc_evidence(...)` instead of keeping a separate parametric candidate loop; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Eleventh consolidation completed 2026-07-07 (superseded by the one hundred forty-fourth shape/type phase split): the original `DaeGcActualShapeEvidence` / `dae_gc_actual_shape_evidence(...)` centralized argument-side actual-shape candidate/refinement/repair evidence for direct single-instruction actuals, final typed/typeidx `block` / `loop` / `if` actuals, and nullable typed-select actual rewrites. Candidate collection and refined-actual rewriting delegated candidate/refinement/repair extraction to the shared carrier; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Twelfth consolidation completed 2026-07-07: `DaeOperandDisposition`, `dae_operand_disposition_from_range(...)`, and `dae_operand_disposition_from_call_shape(...)` centralized droppable/effect/branch range classification plus removed-operand replay/drop emission; its initial append wrapper was later superseded by the thirty-eighth `DaeOperandRemovalPlan` slice. `dae_slice_is_trivially_droppable(...)` and `dae_rewrite_callsite_at(...)` share the carrier; red-first whitebox coverage failed on the missing helper before passing after extraction. This is still behavior-preserving and does not add localization/retry or optimizing cleanup to plain DAE.
      - Thirteenth consolidation completed 2026-07-07: `DaeValueSlice` now carries `DaeOperandDisposition`, and `dae_value_slice_from_operand_disposition(...)` / `dae_value_slice_from_operand_disposition_with_purity(...)` centralize select/control value-slice construction over the same repair carrier while preserving the old distinction between effect-free computed ranges and trivially droppable call-shape slices. The redundant branch/effect range wrappers are gone; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Fourteenth consolidation completed 2026-07-07: `dae_collect_callsite_value_slices(...)` now builds callsite argument carriers as `DaeValueSlice`s; its original combined `dae_gc_actual_value_slice_evidence(...)` entrypoint was superseded by the one hundred forty-fourth `dae_gc_actual_value_slice_shape_evidence(...)` plus current-target evaluation split, while `dae_rewrite_refined_gc_actual_slice(...)` and `dae_direct_gc_actual_candidate_type_for_value_slice(...)` let callsite removal, refined-actual repair, and direct GC actual collection share those value-slice/disposition facts. `dae_rewrite_callsite_at(...)`, `dae_rewrite_refined_gc_actuals_instrs(...)`, and `dae_collect_direct_struct_default_actuals_instrs(...)` now consume the shared value-slice carrier; red-first whitebox coverage failed on the missing callsite value-slice helper before passing after extraction.
      - Fifteenth consolidation completed 2026-07-07: `dae_value_slice_single_instruction_index(...)` centralizes single-instruction actual evidence on the `DaeValueSlice` carrier, and the remaining callsite shape-wrapper users in uniform-constant actual collection plus the selected Func313/Func323 global-suffix repair now consume value slices directly. The old private `dae_collect_callsite_shapes(...)` wrapper is removed; red-first whitebox coverage failed on the missing single-instruction helper before passing after extraction.
      - Sixteenth consolidation completed 2026-07-08: `DaeCallBoundaryFacts` plus `dae_make_call_boundary_facts(...)`, `dae_call_boundary_allows_forwarding(...)`, and `dae_call_boundary_direct_callers(...)` centralize escape/tail/direct-caller forwarding gates for recursive uniform-constant actual collection. `dae_collect_uniform_const_actuals_with_facts(...)`, `dae_collect_uniform_const_actuals_visited(...)`, `dae_collect_uniform_const_actuals_instrs(...)`, `dae_resolve_uniform_const_candidate(...)`, and `dae_resolve_forwarded_uniform_const_local_get(...)` now pass the carrier instead of separate boundary arrays; red-first whitebox coverage failed on the missing boundary helpers before passing after extraction.
      - Seventeenth consolidation completed 2026-07-08: `dae_record_uniform_const_candidate(...)` centralizes the `seen` / `invalid` / prior-candidate merge policy for recursive uniform-constant actual collection. Both direct materializable constants and local-set-carried constants now record through the helper instead of open-coding duplicate merge/invalidation loops; red-first whitebox coverage failed on the missing candidate recorder before passing after extraction.
      - Eighteenth consolidation completed 2026-07-08: `DaeCallBoundaryFacts` now also carries direct-call and dropped-call counts, `dae_call_boundary_facts_from_current(...)` builds the boundary carrier from `DaeCurrentCallFacts`, and `DaeCallBoundaryCandidate` / `dae_call_boundary_candidate(...)` centralize owned-callee gating plus count/caller evidence. The main DAE core loop and reverse exact-literal loop now consume the boundary candidate for owned-callee checks, direct-call counts, dropped-call counts, and caller lists instead of immediately re-indexing separate call-fact arrays; red-first whitebox coverage failed on the missing candidate helper before passing after extraction.
      - Nineteenth consolidation completed 2026-07-08: `DaeCallBoundaryCandidate` now also carries self-direct-caller and nonself-direct-rewrite eligibility facts. The selected unread-parameter and selected exact-literal rewrite lanes consume the boundary candidate for owned-callee, positive direct-call, no-self-recursion gating and for rewrite count/caller evidence instead of open-coding escape/tail/count/self-caller array checks; red-first whitebox coverage failed on the missing candidate fields before passing after extraction.
      - Twentieth consolidation completed 2026-07-08: `dae_collect_uniform_const_actuals_from_boundary_facts(...)` is now the boundary-carrier entrypoint for uniform-constant actual collection. The main core uniform-constant lanes, reverse exact-literal lane, selected exact-literal lane, and selected forwarded-wrapper lane pass `DaeCallBoundaryFacts` directly instead of rebuilding or threading separate escape/tail/direct-caller arrays at those entrypoints; red-first whitebox coverage failed on the missing helper before passing after extraction.
      - Twenty-first consolidation completed 2026-07-08: `DaeModuleBoundarySnapshot` / `dae_collect_module_boundary_snapshot(...)` now centralize imported/defined/total counts, type indices, current call facts, and `DaeCallBoundaryFacts` for the main core, reverse exact-literal core, selected Func323 global-suffix repair, selected Func867 default-argument specialization, selected single-`i32` suffix default-argument specialization, and selected Func3737 wrapper-suffix removal; red-first whitebox coverage failed on the missing snapshot helper before passing after extraction.
      - Twenty-second consolidation completed 2026-07-08: `DaeOriginalBoundarySnapshot` / `dae_collect_original_boundary_snapshot(...)` now centralize original imported/defined/total counts, original escapes, original param counts, and the original call summary for `dae_run_core(...)` setup; `dae_collect_original_call_summary_with_context(...)` reuses the already-collected boundary context instead of rebuilding imported/defined/escape facts internally. Red-first whitebox coverage failed on the missing original-boundary snapshot helper before passing after extraction.
      - Twenty-third consolidation completed 2026-07-08: `dae_rewrite_dropped_result_calls_with_policy_instrs(...)` centralizes the duplicate dropped-result call/drop repair traversal behind an explicit stop-at-unreachable-root policy. The top-level dropped-result rewrite and anywhere cleanup wrappers now share one recursive block/loop/try/if scanner instead of maintaining separate near-identical loops; red-first whitebox coverage failed on the missing policy helper before passing after extraction.
      - Twenty-fourth consolidation completed 2026-07-08: `DaeDroppedResultScope`, `dae_dropped_result_scope_from_optional(...)`, and scope-consuming dropped-result helpers centralize the selected-vs-all caller scope used by undropped-call legality checks, call/drop rewriting, dropped-call cleanup, and no-param dead-suffix repair in `dae_try_remove_dropped_results(...)`. Red-first whitebox coverage failed on the missing scope helpers before passing after extraction. This is behavior-preserving and does not change dropped-result candidate legality, result-removal policy, localization/retry, convergence scheduling, or optimizing cleanup.
      - Twenty-fifth consolidation completed 2026-07-08: `dae_repair_dead_suffix_calls_in_scope_except(...)` and the existing `DaeDroppedResultScope` helpers now cover the remaining full-module dropped-result cleanup paths. Unobserved-unreachable/no-param result-removal helpers build one full scope and share scoped undropped-call legality, dead-suffix repair, no-param repair, and call/drop cleanup instead of keeping separate all-functions loops; red-first whitebox coverage failed on the missing full-scope repair helper before passing after extraction. This is behavior-preserving and does not change dropped-result candidate legality, result-removal policy, localization/retry, convergence scheduling, or optimizing cleanup.
      - Twenty-sixth consolidation completed 2026-07-08: `DaeOriginalResultPreservationEvidence` and `dae_original_result_preservation_any(...)` (now built through `dae_original_result_preservation_evidence_from_row(...)`) centralize the common original-vs-current result-preservation guards for the main core zero-param/result-removal lane, the main core current-param lane, and the reverse exact-literal lane. Red-first whitebox coverage failed on the missing preservation helpers before passing after extraction. This is behavior-preserving and does not change dropped-result candidate legality, result-removal policy, localization/retry, convergence scheduling, or optimizing cleanup.
      - Twenty-seventh consolidation completed 2026-07-08: `DaeDroppedResultCandidate` and `dae_dropped_result_candidate_from_current_call_facts(...)` centralize current direct-call count, dropped-call count, direct-caller list, escape/tail owned status, all-direct-calls-dropped status, and owned dropped-result candidate eligibility for the fact-selected dropped-result candidate collector and the per-def dropped-result removal attempt path. Red-first whitebox coverage failed on the missing candidate helper before passing after extraction. This is behavior-preserving and does not change result-removal policy, selected/full repair scope, localization/retry, convergence scheduling, or optimizing cleanup.
      - Twenty-eighth consolidation completed 2026-07-08: `dae_dropped_result_candidate_from_boundary_facts(...)` makes dropped-result candidate eligibility reuse `DaeCallBoundaryFacts` / `DaeCallBoundaryCandidate`, and the current-call helper now delegates through that boundary carrier. The fact-selected dropped-result collector builds one boundary carrier for owned-callee, direct-call count, dropped-call count, and direct-caller evidence instead of keeping a parallel current-call indexing path. Red-first whitebox coverage failed on the missing boundary-candidate helper before passing. This is behavior-preserving and does not change result-removal policy, selected/full repair scope, localization/retry, convergence scheduling, or optimizing cleanup.
      - Twenty-ninth consolidation completed 2026-07-08: `dae_original_result_preservation_blocks_argument_side_result_repair(...)` centralizes the shared argument-side result-repair / dropped-result blocking predicate over `DaeOriginalResultPreservationEvidence` for the main current-param lane and the reverse exact-literal lane. The helper preserves the existing scheduler difference where the main lane treats the parameterized self-side-chain guard as blocking and the reverse exact-literal lane does not. Red-first whitebox coverage failed on the missing repair-gate helper before passing. This is behavior-preserving and does not change result-removal policy, selected/full repair scope, localization/retry, convergence scheduling, or optimizing cleanup.
      - Thirtieth consolidation completed 2026-07-08: `DaeDroppedResultRemovalLegalityEvidence`, `dae_dropped_result_removal_legality_evidence(...)`, and `dae_dropped_result_removal_legality_blocks(...)` centralize result-count-aware large-result cleanup skips, live tail-call blockers, and scoped undropped direct-call blockers across the selected dropped-result removal path plus the full-module unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup paths. Red-first whitebox coverage failed on the missing result-removal legality helper before passing. This is behavior-preserving and does not change candidate selection, selected/full repair scope, type signature updates, localization/retry, convergence scheduling, or optimizing cleanup.
      - Thirty-first consolidation completed 2026-07-08 (superseded by the one hundred forty-fifth four-outcome rewrite disposition): `DaeOperandRemovalAction` plus helper predicates made `DaeOperandDisposition` carry an explicit drop-without-repair / replay-for-removal / reject-branching-removal decision. The thirty-eighth slice then consumed that action through `DaeOperandRemovalPlan` for callsite append/drop repair until the one hundred forty-fifth slice moved callsite planning, trivial-droppable checks, and value-slice consistency to the replacement rewrite disposition. Red-first whitebox coverage failed on the missing action helpers before passing after extraction. This is behavior-preserving and does not add true localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Thirty-second consolidation completed 2026-07-08: `DaeDroppedResultRepairPlan`, `DaeDroppedResultDeadSuffixRepairKind`, `dae_dropped_result_repair_plan(...)`, and `dae_apply_dropped_result_repair_plan(...)` centralize the all-caller vs no-param-only dead-suffix repair mode plus optional dropped-call-drop cleanup across selected dropped-result removal and the full-module unobserved-unreachable, dead-self-suffix, uncalled-result, and unreachable cleanup helpers. Red-first whitebox coverage failed on the missing repair-plan helpers before passing after extraction. This is behavior-preserving and does not change result-removal legality, candidate selection, type updates, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.
      - Thirty-third consolidation completed 2026-07-08: `DaeCallsiteValueSliceEvidence`, `dae_callsite_value_slice_evidence(...)`, and `dae_callsite_value_slice_evidence_is_consistent(...)` centralize callsite-wide argument recovery facts: call index, argument count, entry-stack context, recovered `DaeValueSlice`s, cursor, and per-slice repair dispositions. `dae_rewrite_callsite_at(...)`, refined GC actual rewriting, uniform-constant actual collection, direct GC actual collection, and the selected Func313/Func323 global-suffix repair path now consume the carrier instead of pairing raw `(slices, cursor)` tuples by hand. Red-first whitebox coverage failed on the missing callsite evidence helper before passing after extraction. This is behavior-preserving and does not add localization/retry, arbitrary repair edits, non-final typeidx/control operand reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Thirty-fourth consolidation completed 2026-07-08: `DaeCallsiteSingleInstructionActualEvidence`, `dae_callsite_single_instruction_actual(...)`, and `dae_callsite_single_instruction_actual_is_consistent(...)` centralize single-instruction actual lookup over `DaeCallsiteValueSliceEvidence`. Uniform-constant actual collection and the selected Func313/Func323 global-suffix repair path now consume that record instead of each indexing into value slices and rechecking single-instruction bounds by hand. Red-first whitebox coverage failed on the missing single-instruction actual helper before passing after extraction. This is behavior-preserving and does not add localization/retry, arbitrary repair edits, non-final typeidx/control operand reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Thirty-fifth consolidation completed 2026-07-09: `DaeDroppedResultModuleContext` and `dae_dropped_result_module_context(...)` centralize imported count, defined-function index, type indices, copied function/type sections, full dropped-result scope, and result-removal legality for full-module dropped-result cleanup paths. Unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup now consume that carrier instead of rebuilding the same section/scope/legality facts by hand. Red-first whitebox coverage failed on the missing module-context helper before passing after extraction. This is behavior-preserving and does not change result-removal policy, selected/full repair semantics, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.
      - Thirty-sixth consolidation completed 2026-07-09: `DaeDroppedResultModuleContext` now carries params/results and optional selected-lane perf-timer-compatible setup, and `dae_try_remove_dropped_results(...)` consumes that carrier for selected dropped-result removal instead of rebuilding imported/defined/type-index, function/type-section, selected scope, and result-removal legality facts by hand. Red-first whitebox coverage failed on missing `context.params` / `context.results` fields before passing after extraction. This is behavior-preserving and does not change result-removal policy, repair-plan semantics, type updates, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.
      - Thirty-seventh consolidation completed 2026-07-09: `DaeOriginalResultPreservationContext`, `dae_original_result_preservation_context(...)`, and `dae_original_result_preservation_evidence_from_context(...)` centralize the long original result-preservation input array list. The main core zero-param/result-removal lane, main core current-param lane, and reverse exact-literal lane now build one carrier and reuse it for preservation evidence instead of repeating the same original summary arrays at each check. Red-first whitebox coverage failed on the missing context helpers before passing after extraction. This is behavior-preserving and does not change preservation predicates, result-removal policy, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.
      - Thirty-eighth consolidation completed 2026-07-09: `DaeOperandRemovalPlan`, `dae_operand_removal_plan(...)`, `dae_callsite_operand_removal_plan(...)`, and `dae_append_operand_with_removal_plan(...)` centralize the callsite keep/replay/drop repair plan over the existing disposition and callsite evidence carriers. `dae_rewrite_callsite_at(...)` now asks `DaeCallsiteValueSliceEvidence` for a per-operand removal plan, and `dae_slice_is_trivially_droppable(...)` uses the same plan predicate instead of interpreting disposition action booleans independently. Red-first whitebox coverage failed on the missing callsite plan helper before passing after extraction. This is behavior-preserving and does not add true localization/retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Thirty-ninth consolidation completed 2026-07-09: `DaeControlEntryStackEvidence` and `dae_control_entry_stack_evidence(...)` centralize nested control entry/result stack context. Callsite removal, refined GC actual rewriting, uniform-constant actual collection, and direct GC actual collection now derive child block/loop/`try_table`/`if` entry-stack values through that carrier instead of calling the block-type stack-effect helper directly in each recursive loop. Red-first whitebox coverage failed on the missing control-entry helper before passing after extraction. This is behavior-preserving and does not add non-final typeidx/control reconstruction, localization/retry, convergence changes, or optimizing cleanup to plain DAE.
      - Fortieth consolidation completed 2026-07-09: `dae_stack_bool_has_true_operand(...)`, `dae_stack_bool_apply_effect(...)`, `dae_stack_int_has_operand_value(...)`, `dae_stack_int_has_known_operand(...)`, and `dae_stack_int_apply_effect(...)` centralize stack operand-source lookup plus stack-effect pop/push updates for bool/int provenance scanners. Original dead-suffix/result-preservation scans in `dae_mark_dead_suffix_escaped_result_operand_targets(...)`, `dae_mark_dead_suffix_same_result_operand_targets(...)`, `dae_dead_suffix_call_to_has_same_result_operand(...)`, and `dae_instrs_have_undropped_call_to_with_param_operand(...)` now consume those helpers instead of open-coding parallel source-stack loops. Red-first whitebox coverage failed on the missing helper names before passing after extraction. This is behavior-preserving and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-first consolidation completed 2026-07-09: `DaeDeadSuffixEvidence` and `dae_dead_suffix_evidence(...)` centralize unreachable-root plus suffix-tail recovery for dead-suffix repair, direct-call target/count scanners, dropped/undropped escaped-result scanners, self-call param-operand preservation, self-recursive side-call checks, and private-result side-call checks. The old loops now consume the shared carrier instead of each copying or walking the post-unreachable tail by hand. Red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and does not change unreachable-root policy, localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-second consolidation completed 2026-07-09: `DaeStackOperandWindowEvidence`, `dae_stack_operand_window_evidence(...)`, and `dae_stack_operand_window_evidence_is_consistent(...)` centralize top-of-stack operand-window bounds for bool/int provenance scanners. `dae_stack_bool_has_true_operand(...)`, `dae_stack_int_has_operand_value(...)`, and `dae_stack_int_has_known_operand(...)` now consume the shared window carrier instead of each recomputing `stack.length() - arg_count + p` and underfull-stack bounds by hand. Red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-third consolidation completed 2026-07-09: `DaeStackEffectEvidence`, `dae_stack_effect_evidence(...)`, `dae_stack_bool_apply_instr_effect(...)`, and `dae_stack_int_apply_instr_effect(...)` centralize instruction stack-effect facts before bool/int provenance push/pop updates. `dae_mark_dead_suffix_escaped_result_operand_targets(...)`, `dae_mark_dead_suffix_same_result_operand_targets(...)`, `dae_dead_suffix_call_to_has_same_result_operand(...)`, and `dae_instrs_have_undropped_call_to_with_param_operand(...)` now consume the shared helper instead of matching `dae_instr_stack_effect(...)` and applying inputs/outputs by hand. Red-first whitebox coverage failed on the missing helper names before passing after extraction. This is behavior-preserving and preserves unknown-stack-effect permissiveness; it does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-fourth consolidation completed 2026-07-09: `dae_control_entry_stack_values(...)` centralizes child entry-stack extraction over `DaeControlEntryStackEvidence`. Recursive callsite removal, refined-GC actual rewriting, uniform-constant actual collection, and direct GC actual collection now use the helper for block/loop/`try_table`/`if` entry-stack values instead of each matching the evidence carrier and projecting `entry_stack_values` by hand. Red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and does not add non-final typeidx/control reconstruction, localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-fifth consolidation completed 2026-07-09: `DaeControlBlockTypeSignatureEvidence` / `dae_control_block_type_signature_evidence(...)` centralizes block-type params/results/input/output/typeidx facts for `void`, `valtype`, and type-indexed control. `dae_instr_stack_effect_from_block_type(...)`, `dae_control_entry_stack_evidence(...)`, and `dae_try_refine_parametric_typeidx_multi_result_terminal_control_body(...)` now consume the same signature carrier instead of resolving stack effects and typeidx function signatures separately. Red-first whitebox coverage failed on the missing signature helper before passing after extraction. This is behavior-preserving and does not add non-final typeidx/control reconstruction, localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-sixth consolidation completed 2026-07-09: `DaeSingleBlockResultCarrierEvidence` / `dae_single_block_result_carrier_evidence(...)` centralizes the proof for a single branch-free/effect-free zero-input block carrying the expected multi-result arity. `dae_find_multi_result_control_slice(...)` now consumes that carrier for non-return flattenable blocks, block-plus-`return` carriers, and nested carrier unwrapping instead of resolving block effects and effect scans in three local shape scanners. Red-first whitebox coverage failed on the missing carrier helper before passing after extraction. This is behavior-preserving and does not add non-final typeidx/control reconstruction, localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-seventh consolidation completed 2026-07-09: `DaeNestedControlEntryEvidence` / `dae_nested_control_entry_evidence(...)` centralizes instruction-level block/loop/`try_table`/`if` entry-stack facts on top of `DaeControlEntryStackEvidence`. Refined-GC actual rewriting, callsite removal, uniform-constant actual collection, and direct-GC actual collection now consume that carrier instead of each resolving child entry-stack context directly from block types; red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and does not add non-final typeidx/control reconstruction, localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-eighth consolidation completed 2026-07-09: `DaeTailCallBoundaryEvidence` / `dae_tail_call_boundary_evidence(...)` centralizes direct-vs-indirect tail-call boundary facts. Direct-call parameter/constant rewrite legality consumes the carrier's indirect-tail blocker, and dropped-result removal legality consumes the same carrier's any-tail result-removal blocker, instead of maintaining separate recursive direct-tail and indirect-tail scanner loops; red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and preserves the existing difference between indirect-tail callsite-rewrite blocking and any-tail dropped-result blocking; it does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Forty-ninth consolidation completed 2026-07-09: `DaeDirectCallTargetEvidence` / `dae_direct_call_target_evidence(...)` centralizes target-specific direct-call count, dropped direct-call, undropped direct-call, and live-prefix undropped-call facts. Direct-call containment, anywhere direct-call counting, undropped direct-call detection, and live-undropped direct-call detection now consume the shared recursive carrier instead of maintaining separate block/loop/`try_table`/`if` scanners; red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving and keeps `return_call` as undropped-use evidence without counting it as an ordinary direct `call`, keeps calls after root `unreachable` visible to anywhere counts while excluding them from live-prefix blockers, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fiftieth consolidation completed 2026-07-09: `DaeDeadSuffixDirectCallEvidence` / `dae_dead_suffix_direct_call_evidence(...)` centralizes dead-suffix dropped/undropped target facts. The dropped-suffix and undropped-suffix predicates now share one carrier, and full-module unobserved-unreachable plus dead-self-suffix dropped-result cleanup gates consume it directly instead of asking separate target predicates; red-first whitebox coverage failed on the missing helper before passing after extraction. This is behavior-preserving, keeps the existing top-level `call`+`drop` dropped-suffix policy, reuses recursive direct-call target evidence for undropped uses, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-first consolidation completed 2026-07-09: `DaeDroppedResultScopeDirectCallEvidence` / `dae_dropped_result_scope_direct_call_evidence(...)` centralizes selected-vs-all caller direct-call count, dropped-call, undropped-use, and live-prefix blocker facts for dropped-result removal legality. The result-removal legality helper now consumes the scope carrier instead of open-coding a caller loop over a separate live-undropped predicate, and selected/full-scope whitebox coverage now reads the same carrier; red-first coverage failed on the missing helper before passing after extraction. This is behavior-preserving, keeps invalid-caller skipping, `skip_def` exclusion, live-prefix result blocking, tail-call blocking, large-result cleanup blocking, and plain-vs-optimizing scheduling unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-second consolidation completed 2026-07-09: `DaeOriginalNoParamDroppedCallerGuardEvidence` / `dae_original_no_param_dropped_caller_guard_evidence(...)` centralizes safe indexed original-boundary facts for the no-param dropped-caller preservation guard. `dae_should_preserve_undropped_self_result_from_no_param_dropped_caller(...)` now consumes the carrier via `dae_should_preserve_undropped_self_result_from_no_param_dropped_caller_evidence(...)`, so both original result-preservation evidence and the param-operand self-result side-callee count collector share the same param/call/nonself/self/dead-suffix/mutual facts instead of rechecking the long original summary array list directly; red-first coverage failed on the missing helper names before passing after extraction. This is behavior-preserving, keeps out-of-range facts false/zero, active/nonself/parameterized count gates, self-result/param-operand/result-other requirements, mutual-call rejection, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-third consolidation completed 2026-07-09: `DaeOriginalParameterizedUndroppedSelfSideChainEvidence` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_context(...)` centralizes safe indexed original-boundary facts for the parameterized undropped self-side-chain preservation guard. The unobserved/current core checks consume the existing preservation-input context instead of repeating the same original summary array list; red-first coverage failed on the missing helper names before passing after extraction. The later sixty-fifth row slice makes this context helper project through `DaeOriginalResultPreservationRow`. This is behavior-preserving, keeps out-of-range facts false/zero, parameterized nonself/self-call/result-other/escaped-dead-suffix gates, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-fourth consolidation completed 2026-07-09: `DaeDroppedResultCalleeRewriteEvidence` / `dae_dropped_result_callee_rewrite_evidence(...)` centralizes dropped-result callee rewrite facts for full-module cleanup gates. Unobserved-unreachable, dead-self-suffix, and uncalled-result removal now share param/result counts, original-unreachable status, simplified-unreachable status, dropped/undropped dead-suffix call facts, missing-suffix predicates, and result-removal blocker status instead of pairing dead-suffix, simplification, and legality checks locally; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps the required dropped-vs-undropped suffix policy, simplification policy, result-removal blocker policy, repair-plan behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-fifth consolidation completed 2026-07-09: `DaeEscapedResultDirectCallEvidence` / `dae_escaped_result_direct_call_evidence(...)` centralizes dropped and undropped escaped-result direct-call facts, with `dae_dead_suffix_escaped_result_direct_call_evidence(...)` projecting those facts onto dead suffixes. The original-boundary dead-suffix preservation collectors now share one recursive block/loop/`try_table`/`if` evidence carrier instead of maintaining parallel dropped-vs-undropped escaped-result scanners; red-first coverage failed on the missing helper names before passing after extraction. This is behavior-preserving, keeps escaped-result/result-count, dropped-vs-undropped, dead-suffix, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-sixth consolidation completed 2026-07-09: `DaeDeadSuffixDirectTargetEvidence` / `dae_dead_suffix_direct_target_evidence(...)` centralizes direct, dropped-direct, and undropped-direct dead-suffix target lists, with `dae_collect_dead_suffix_direct_target_lists(...)` keeping the existing top-level `call`+`drop` dropped-target policy explicit. The original-boundary direct-target, dropped-target, and undropped-target collectors now share one dead-suffix projection instead of each recovering the suffix and applying its own target-list policy; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps direct `call` vs `return_call`, recursive undropped-target, top-level dropped-target, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-seventh consolidation completed 2026-07-09: `dae_stack_single_value_producer_evidence(...)` centralizes zero-input/one-output instruction stack-effect proof and moves additional raw stack-effect tuple users onto `DaeStackEffectEvidence`. Indirect-call GC result-slice operand validation, typed-select condition stack-local checks, multi-result `if`/typed-select suffix consumers, `dae_find_value_slice(...)`, parametric typeidx prefix stack accounting, and self-call operand-origin propagation now consume shared stack-effect evidence instead of matching `(inputs, outputs)` locally; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps original unknown-stack-effect rejection/permissiveness, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-eighth consolidation completed 2026-07-09: `dae_control_single_result_block_type_evidence(...)` centralizes zero-input/one-output block-type proof over `DaeControlBlockTypeSignatureEvidence`. Direct GC result `block`, `loop`, and `if` candidate paths plus a multi-result suffix consumer now consume shared block-type evidence instead of matching `@lib.ValTypeBlockType` or raw `Some((0, 1))` stack-effect tuples locally; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps typeidx lookup rejection, void/multi-result bailouts, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Fifty-ninth consolidation completed 2026-07-09: `DaeNestedControlChildrenEvidence` / `dae_nested_control_children_evidence(...)` centralizes nested-control primary/secondary child body projection over the existing entry-stack carrier. Recursive uniform-constant actual collection and direct `struct.new_default` actual collection now share the same block/loop/`try_table`/`if` child-body and entry-stack evidence instead of maintaining separate arm walkers and local entry-stack projections; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps nested-control evidence rejection, branch/arm traversal order, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.
      - Sixtieth consolidation completed 2026-07-09: `dae_rebuild_nested_control_children(...)` centralizes reconstruction over `DaeNestedControlChildrenEvidence`. Refined-GC actual rewriting and callsite operand removal now share nested-control child-body evidence plus one rebuild helper for block/loop/`try_table`/`if` primary/secondary bodies instead of maintaining separate reconstruction arms with local entry-stack projections; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, preserves `try_table` catches and if-with-else shape checks, keeps nested-control evidence rejection, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Sixty-first consolidation completed 2026-07-09: `DaeMultiResultSuffixBoundsEvidence` / `dae_multi_result_suffix_bounds_evidence(...)` centralizes non-terminal/terminal-return suffix bounds for multi-result refinement scans. The single-`if` operand refinement path and typed-select operand refinement path now share minimum-length checks, terminal-return mode filtering, operand-end calculation, suffix-start iteration, and trivially-dropped-prefix filtering before their shape-specific candidate loops; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps existing scan order, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Sixty-second consolidation completed 2026-07-09: `DaeStackSliceSimulationEvidence` / `dae_stack_slice_simulation_evidence(...)` centralizes bounded stack-slice simulation with per-caller instruction-policy hooks. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks now share underflow rejection, final-stack tracking, max-stack tracking, and effect/branch observation instead of each applying instruction stack effects in a local loop; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps existing condition/prefix instruction policies, single-instruction typed-select behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Sixty-third consolidation completed 2026-07-09: `DaeEscapingBranchEvidence`, `dae_escaping_branch_evidence(...)`, and `dae_no_escaping_branch_evidence(...)` centralize depth-based branch-escape facts for localization/control gates. The existing escaping-branch predicate now derives from the carrier, and three Func237 carrier-localization gates consume the no-escape evidence directly instead of treating branch-safety as a local boolean; red-first coverage failed on the missing helper names before passing after extraction. This is behavior-preserving, keeps the exact branch-depth policy, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add broader localize-then-retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Sixty-fourth consolidation completed 2026-07-09: `dae_multi_result_control_carrier_chain_slice(...)` centralizes multi-result carrier-chain recovery for fallthrough, terminal-return, terminal block-body, and block-plus-`return` paths. `dae_find_multi_result_control_slice(...)` now delegates those formerly separate unwrapping loops to one helper over `DaeSingleBlockResultCarrierEvidence`; red-first coverage failed on the missing helper name before passing after extraction. This is behavior-preserving, keeps carrier kind/flattening/appended-return/terminal-return policy, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add broader localize-then-retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Sixty-fifth consolidation completed 2026-07-09: `DaeOriginalResultPreservationRow` plus `dae_original_result_preservation_row_from_context(...)`, `dae_original_no_param_dropped_caller_guard_evidence_from_row(...)`, `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)`, and `dae_original_result_preservation_evidence_from_row(...)` centralize per-definition original-boundary result-preservation guard fact projection. Result-preservation context consumers and the parameterized self-side-chain helper now share the row instead of maintaining separate indexed wrappers; red-first coverage failed on the missing helper names before passing. Validation for this slice passed focused preservation-row evidence (`1/1`), full DAE whitebox (`116/116`), `moon test src/passes` (`4383/4383`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7822/7822`), and `git diff --check`. This is behavior-preserving, keeps zero/false out-of-range defaults, result-removal policy, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged.
      - Sixty-sixth consolidation completed 2026-07-09: `DaeOriginalResultRemovalPruneRow` plus `dae_original_result_removal_prune_row(...)`, `dae_should_prune_later_mutual_dropped_result_from_row(...)`, and `dae_should_prune_dropped_self_result_with_void_side_callers_from_row(...)` centralize per-definition original-boundary result-removal prune facts. The unobserved cleanup lane and zero-param current core lane now build one row and consume it for both later-mutual dropped-result pruning and dropped-self-result-with-void-side-callers pruning instead of re-indexing overlapping original summary arrays; red-first coverage failed on the missing helper names before passing. Validation for this slice passed focused prune-row evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7823/7823`). This is behavior-preserving, keeps zero/false out-of-range defaults, later-mutual/void-side-caller prune predicates, result-removal policy, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged.
      - Sixty-seventh consolidation completed 2026-07-09: `DaeOriginalResultRemovalPruneRow` now also carries private-result, result-other, no-param-result-caller, and mixed dropped/undropped caller facts, and row-derived `dae_should_prune_mixed_dropped_undropped_caller_from_row(...)`, `dae_should_prune_dropped_only_private_result_side_caller_from_row(...)`, and `dae_should_prune_dropped_only_no_param_result_side_caller_from_row(...)` centralize the remaining mixed/dropped-only result-side prune predicates in the unobserved cleanup lane and zero-param current core lane. Red-first coverage failed on the widened row arity, missing row field, and missing helper names before passing after extraction. Validation passed focused prune-row evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps zero/false out-of-range defaults, mixed/dropped-only/later-mutual/void-side-caller prune predicates, result-removal policy, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged.
      - Sixty-eighth consolidation completed 2026-07-09: `DaeOriginalResultRemovalPruneRow` now also carries block-undropped-self and same-self-result-operand facts, and row-derived `dae_should_preserve_mixed_self_suffix_from_row(...)`, `dae_should_preserve_same_self_result_operand_from_row(...)`, and `dae_should_preserve_escaped_block_self_result_from_row(...)` centralize mixed-self suffix, same-self result-operand, and escaped-block self-result preservation guards. The unobserved cleanup lane and zero-direct-call current core lane now consume the row for these guards instead of re-indexing the same original summary arrays; red-first coverage failed on the widened row arity and missing helper names before passing. Validation passed focused prune-row evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps zero/false out-of-range defaults, existing result-removal prune/preserve predicates, result-removal policy, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged.
      - Sixty-ninth consolidation completed 2026-07-09: `DaeStackSliceTransitionEvidence` / `dae_stack_slice_transition_evidence(...)` centralizes expected-final-stack acceptance over the bounded stack-slice simulator. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks now consume the transition carrier instead of each matching `dae_stack_slice_simulation_evidence(...)` and comparing `final_stack_values` locally; red-first coverage failed on the missing helper name before passing. Validation passed focused stack-slice simulation coverage (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps existing condition/prefix instruction policies, single-instruction typed-select behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add localization/retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventieth consolidation completed 2026-07-09: `DaeEscapingBranchEvidence` now carries `start` / `end` range bounds, and `dae_escaping_branch_range_evidence(...)` / `dae_no_escaping_branch_range_evidence(...)` centralize depth-based branch-escape facts for full arrays and subranges. The full-array helpers delegate through the range carrier, and two Func237 localization gates now prove no-escape over a prefix/range before copying accepted prefixes; red-first coverage failed on missing evidence fields and helper names before passing. Validation passed focused escaping-branch evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps the exact branch-depth policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-first consolidation completed 2026-07-09: `dae_no_escaping_branch_prefix_evidence(...)` and `dae_no_escaping_branch_suffix_evidence(...)` now project prefix/suffix no-escape facts through the range-aware escaping-branch carrier. Several Func237 and Func256 localization/control cleanup gates now prove accepted prefixes/suffixes before copying temporary instruction arrays; red-first coverage failed on the missing helper names before passing. Validation passed focused escaping-branch evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps the exact branch-depth policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-second consolidation completed 2026-07-09: `DaeDroppedOperandPairSkipEvidence` / `dae_dropped_operand_pair_skip_evidence(...)` now centralizes dropped trivial-operand pair skipping for multi-result suffix scanners. The single-`if` and typed-select multi-result refinement loops consume one bounds-checked start/end/next/skipped-pairs carrier instead of open-coding duplicate pure-operand-plus-`drop` loops; red-first coverage failed on the missing helper name before passing. Validation passed focused stack-slice simulation evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, and `moon test src/passes` (`4384/4384`). This is behavior-preserving, keeps suffix bounds, candidate selection, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-third consolidation completed 2026-07-09: `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)` now centralizes paired fallthrough/terminal-return if GC refinement evidence. The value-producing terminal-`if` path and the `if`-plus-`unreachable` terminal-return path in `dae_try_refine_direct_gc_types(...)` now share then/else slice recovery, per-arm GC evidence, paired LUB/refinement, and if-body-change facts instead of open-coding duplicate slice/evidence/pair loops; red-first coverage failed on the missing helper name before passing. Validation passed focused control-result pair GC evidence (`1/1`), full DAE whitebox (`117/117`), `moon fmt`, `moon test src/passes` (`4384/4384`), `moon info` with pre-existing non-DAE warnings, full `moon test` (`7823/7823`), and `git diff --check`. This is behavior-preserving, keeps the existing fallthrough block-type rewrite vs terminal-return body-change difference, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-fourth consolidation completed 2026-07-09: `DaeControlResultPairRefinementApplicationEvidence` / `dae_apply_control_result_pair_refinements(...)` now centralizes paired GC refined-result vector application. Parametric typeidx terminal-control refinement and both direct multi-result `if` refinement paths apply paired LUB/refined result vectors through the helper instead of open-coding result-index loops over `next_results` and the optional local refined-result vector; red-first coverage failed on the missing helper name before passing. Validation passed focused control-result pair GC evidence (`1/1`) and full DAE whitebox (`117/117`) so far. This is behavior-preserving, keeps fallthrough type-section rewrite, terminal-return body-change policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-fifth consolidation completed 2026-07-09: `DaeControlResultUndroppedDirectCallEvidence` / `dae_control_result_undropped_direct_call_evidence(...)` now centralizes control-result undropped direct-call blocker facts for dead-suffix result-preservation scans. The block-undropped self-call scanner now uses one evidence helper for block, loop, `try_table`, if-with-else, and single-arm if result arity plus undropped-direct-call checks instead of open-coding per-shape loops; red-first coverage failed on the missing helper name before passing. Validation passed focused control-result undropped direct-call evidence (`1/1`) so far. This is behavior-preserving, keeps conservative unknown typeidx result handling, void-result nonblocking behavior, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-sixth consolidation completed 2026-07-09: `DaeOriginalDeadSuffixSideCallEvidence` / `dae_original_dead_suffix_side_call_evidence(...)` now centralizes original-boundary dead-suffix side-call cleanup gate facts for the zero-direct-call current core. The dead-suffix-only parameter cleanup gate and later uncalled-result skip gate both consume the carrier for void-side cleanup preservation, recursive result-side chain preservation, and same-self result-operand preservation instead of open-coding the same predicates twice; red-first coverage failed on the missing helper name before passing. Validation passed focused dead-suffix side-call evidence (`1/1`), full DAE whitebox (`119/119`), and `moon test src/passes` (`4386/4386`) so far. This is behavior-preserving, keeps existing side-call/self-recursion predicates, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-seventh consolidation completed 2026-07-09: `DaeOriginalResultPreservationEvidence` now carries `result_side_chain_cleanup_preservation`, a shared summary over no-param dropped-caller preservation, dropped-escaped-result-under-self-caller preservation, and parameterized undropped self-side-chain preservation. The unobserved-unreachable cleanup gate, dead-suffix-only parameter cleanup gate, and later uncalled-result skip gate now consume the result-preservation carrier for that common side-chain cleanup decision instead of rechecking the same original-boundary predicates locally; red-first coverage failed on the missing evidence field before passing. Validation passed focused result-preservation evidence (`1/1`), full DAE whitebox (`119/119`), and `moon test src/passes` (`4386/4386`) so far. This is behavior-preserving, keeps escaped-block and same-self result-operand gate differences, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-eighth consolidation completed 2026-07-09: `DaeOriginalResultRemovalPruneEvidence` / `dae_original_result_removal_prune_evidence_from_row(...)` now aggregates original-boundary result-removal prune decisions from `DaeOriginalResultRemovalPruneRow`, with helper predicates for params-absent dropped-result cleanup and uncalled-after-param-cleanup blockers. The zero-direct-call current core and zero-param current core now consume those aggregate blockers instead of locally rechecking later-mutual, mixed dropped/undropped, dropped-only private/no-param, and dropped-self-with-void-side-caller predicates; red-first coverage failed on the missing helper name before passing. Validation passed focused prune-row evidence (`1/1`) and full DAE whitebox (`119/119`) so far. This is behavior-preserving, keeps the private-vs-no-param dropped-only distinction, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Seventy-ninth consolidation completed 2026-07-09: `DaeNoEscapeSliceEvidence` / `dae_no_escaping_branch_copied_range_evidence(...)` now centralizes accepted no-escaping branch ranges plus the copied instruction slice. `dae_func237_outer_loop_then_split_next_carrier(...)` and `dae_try_wrap_func237_outer_loop_carrier_instr(...)` consume that carrier instead of separately proving no-escape and copying the same accepted prefix; red-first coverage failed on the missing helper name before passing. Validation passed focused escaping-branch evidence (`1/1`), full DAE whitebox (`119/119`), `moon test src/passes` (`4386/4386`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7825/7825`). This is behavior-preserving, keeps the same branch-depth policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eightieth consolidation completed 2026-07-09: `DaeNoEscapeSliceEvidence` now has `dae_no_escape_slice_evidence_from_branch(...)`, `dae_no_escaping_branch_copied_prefix_evidence(...)`, and `dae_no_escaping_branch_copied_suffix_evidence(...)` so prefix/suffix consumers can carry branch-safe evidence and the copied accepted slice together. Dropped-result branch-carrier folding, unreachable-after-if-else folding, and leading terminal-`br 0` block splicing consume those copied no-escape carriers instead of proving branch safety and copying slices separately; red-first coverage failed on the missing helper names before passing. Validation passed focused escaping-branch evidence (`1/1`), full DAE whitebox (`119/119`), `moon test src/passes` (`4386/4386`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7825/7825`). This is behavior-preserving, keeps the same branch-depth/prefix/suffix policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-first consolidation completed 2026-07-09: `DaeTerminalIfNoEscapeCarrierEvidence`, `dae_no_escaping_branch_copied_evidence(...)`, and `dae_terminal_if_no_escape_carrier_evidence(...)` now centralize terminal zero-result `if` localization/control carrier facts. Func237 inner-iteration wrapping and Func256 void-loop carrier wrapping consume one branch-safe copied condition-prefix / zero-result-if / then-body / optional copied no-escape else-body carrier instead of each open-coding prefix no-escape proof, prefix copying, `if` matching, and else-body branch checks; red-first coverage failed on the missing helper name before passing. Validation passed focused escaping-branch evidence (`1/1`), focused Func237 inner-iteration loop coverage (`3/3`), full DAE whitebox (`119/119`), and `moon test src/passes` (`4386/4386`) so far. This is behavior-preserving, keeps the same branch-depth policy, Func237 required-else gate, Func256 no-else allowance, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-second consolidation completed 2026-07-09: `DaeMixedMultiResultReturnArmGcEvidence` / `dae_mixed_multi_result_return_arm_gc_evidence(...)` now centralize mixed return/fallthrough multi-result `if` arm GC facts. The mixed fallthrough/refinement path consumes the same helper for then and else arms instead of open-coding return-arm candidate extraction, candidate/current LUB, rejection, and effect-free fallthrough acceptance separately for each arm; red-first coverage failed on the missing helper name before passing. Validation passed focused mixed multi-result return-arm evidence (`1/1`), focused control-result pair GC evidence (`1/1`), focused parametric typeidx arm evidence (`1/1`), focused mixed multi-result coverage (`1/1`), full DAE whitebox (`120/120`), `moon fmt`, `moon test src/passes` (`4387/4387`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7826/7826`). This is behavior-preserving, keeps mixed return/fallthrough result-refinement policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-third consolidation completed 2026-07-09: `DaeSingleResultControlPairGcEvidence` / `dae_single_result_control_pair_gc_evidence(...)` now centralize single-result `if` arm GC LUB/rewrite facts. The implicit returned-value `if` path and explicit-return zero-result `if` path both consume the helper for then/else candidate LUB and rewritten-arm body selection instead of open-coding that logic twice; red-first coverage failed on the missing helper name before passing. Validation passed focused single-result control-pair evidence (`1/1`) and full DAE whitebox (`121/121`) so far. This is behavior-preserving, keeps implicit-vs-explicit-return block-type rewrite policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-fourth consolidation completed 2026-07-09: `DaeMixedMultiResultReturnArmRewriteEvidence` / `dae_apply_mixed_multi_result_return_arm_refinements(...)` now centralize refined-slot rewrite application for mixed return/fallthrough multi-result `if` arms. The then-return and else-return application paths consume one helper after lane refinement selection instead of separately copying arm bodies, replaying candidate scans, applying optional rewrites, and tracking flattened-body changes; red-first coverage failed on the missing helper name before passing. Validation passed focused mixed return-arm rewrite evidence (`1/1`) and focused mixed whitebox coverage (`2/2`) so far. This is behavior-preserving, keeps mixed return/fallthrough candidate policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-fifth consolidation completed 2026-07-09: `DaeControlResultSingleRefinementApplicationEvidence` / `dae_apply_control_result_single_refinements(...)` now centralize single-arm refined-result application over `DaeControlResultGcEvidence`. The parametric typeidx terminal-control single-arm path and the flat multi-result direct-GC body refinement path consume one helper to apply `refined_candidates` into `next_results` plus optional local refined-result vectors instead of open-coding result-index loops in each path; red-first coverage failed on the missing helper name before passing. Validation passed focused single refinement application (`1/1`), focused refinement whitebox coverage (`4/4`), full DAE whitebox (`123/123`), `moon fmt`, `moon test src/passes` (`4390/4390`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7829/7829`). This is behavior-preserving, keeps candidate policy, body rewrite selection, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-sixth consolidation completed 2026-07-09: `DaeTerminalNoEscapePrefixEvidence` / `dae_terminal_no_escape_prefix_evidence(...)` now centralize terminal-instruction plus copied no-escape prefix evidence. The unreachable-after-if-else branch folder and leading terminal-`br 0` block splicer consume one helper instead of each recovering the final instruction, proving no-escape for the prefix, and copying that prefix locally; red-first coverage failed on the missing helper name before passing. Validation passed focused escaping-branch evidence (`1/1`), focused Func237 cleanup coverage (`27/27`), full DAE whitebox (`123/123`), `moon fmt`, `moon test src/passes` (`4390/4390`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7829/7829`). This is behavior-preserving, keeps branch-depth policy, terminal checks, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-seventh consolidation completed 2026-07-09: `DaeMixedSingleResultReturnArmGcEvidence` / `dae_mixed_single_result_return_arm_gc_evidence(...)` now centralize mixed return/fallthrough single-result `if` arm evidence. The then/else arms in `dae_direct_gc_result_mixed_if_fallthrough_candidate_body(...)` consume one helper for return-arm candidate extraction, candidate/current LUB rejection, optional returned-arm rewrite propagation, and effect-free fallthrough acceptance instead of open-coding the same policy twice; red-first coverage failed on the missing helper name before passing. Validation passed focused mixed single-result fallthrough evidence (`1/1`), focused mixed whitebox coverage (`3/3`), full DAE whitebox (`124/124`), `moon fmt`, `moon test src/passes` (`4391/4391`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7830/7830`). This is behavior-preserving, keeps simple mixed return/fallthrough policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-eighth consolidation completed 2026-07-09: `DaeStackSliceLocalizationEvidence` / `dae_stack_slice_localization_evidence(...)` now centralize expected-final stack-slice transition, no-escaping-branch range, operand-disposition, and removal-plan facts for stack-local slices. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks consume that combined carrier instead of separately relying on stack-transition evidence while branch/localization and repair facts stayed detached; red-first coverage failed on the missing helper name before passing. Validation passed focused stack-slice localization evidence (`1/1`), focused stack-slice coverage (`2/2`), full DAE whitebox (`125/125`), `moon fmt`, `moon test src/passes` (`4392/4392`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7831/7831`). This is behavior-preserving, keeps condition/prefix instruction policies, single-instruction typed-select behavior, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Eighty-ninth consolidation completed 2026-07-09: `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` now centralize local-copy gap range policy for two Func237 cleanup-localization subflows. Immediate single-use temp-copy folding and used-temp copy retargeting consume the mode-tagged helper for temp/target local interference, branch/throw/return rejection, call-with-root-`try_table` preservation, and the used-temp recursive nested-control policy instead of maintaining separate range loops; red-first coverage failed on the missing helper name before passing. Validation passed focused local-copy gap evidence (`1/1`), focused Func237 copy coverage (`9/9`), full DAE whitebox (`126/126`), `moon fmt`, `moon test src/passes` (`4393/4393`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7832/7832`). This is behavior-preserving optimizing-cleanup evidence consolidation, keeps immediate-vs-used-temp policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninetieth consolidation completed 2026-07-09: `DaeTargetCallsiteEvidence` / `dae_target_callsite_evidence(...)` now centralize target-callee callsite matching for direct-only and direct-or-`return_call` policies. Self-forwarded-parameter local-use suppression, refined-GC actual rewriting, parameter-removal callsite rewriting, recursive uniform-constant actual collection, and direct `struct.new_default` GC actual collection consume the shared helper instead of open-coding callee-index checks; red-first coverage failed on the missing helper name before passing. Validation passed focused target-callsite evidence (`1/1`) and focused callsite whitebox coverage (`6/6`) so far. This is behavior-preserving ownership/call-boundary and callsite-repair evidence consolidation, keeps direct-only vs tail-aware policies, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-first consolidation completed 2026-07-09: `DaeDroppedPureLeafCleanupEvidence` plus `dae_dropped_pure_leaf_cleanup_evidence_from_stack(...)` / `dae_dropped_pure_leaf_cleanup_evidence_from_leaf(...)` now centralize raw-cleanup preservation gates for dropped pure leaves. Stack-suffix truncation after `dae_find_droppable_pure_stack_expr_start(...)` and adjacent pure-leaf-plus-`drop` stripping consume one evidence carrier for range eligibility, integer-constant debris preservation, local-get-after-same-set preservation, and local-get-after-effect preservation instead of open-coding the same policy in two cleanup paths; red-first coverage failed on the missing helper names before passing. Validation passed focused dropped pure leaf cleanup evidence (`1/1`), focused raw-cleanup coverage (`21/21`), full DAE whitebox (`128/128`), `moon fmt`, `moon test src/passes` (`4395/4395`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7834/7834`). This is behavior-preserving localization/effect-preservation and cleanup-policy evidence consolidation, keeps raw cleanup modes, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-second consolidation completed 2026-07-09: `DaeSingleResultControlGcCandidateEvidence` plus `dae_single_result_control_gc_candidate_evidence(...)` / `dae_apply_single_result_control_gc_candidate_evidence(...)` now centralize single-result control GC candidate body recovery and rewrite wrapping. The direct GC result `block` and `loop` arms consume one helper pair for block-type proof, body candidate recovery, optional rewritten-body selection, and valtype wrapper reconstruction instead of open-coding parallel block/loop scanners; red-first coverage failed on the missing helper names before passing. Validation passed focused single-result control GC evidence (`1/1`), full DAE whitebox (`129/129`), `moon test src/passes` (`4396/4396`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7835/7835`). This is behavior-preserving GC type/refinement and result-repair evidence consolidation, keeps block/loop candidate policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-third consolidation completed 2026-07-09: `DaeResultRewriteApplicationEvidence` plus `dae_apply_result_rewrites_to_body(...)` now centralize masked result-slot repair application for flat control-result GC evidence, paired then/else control-result GC LUB evidence, mixed return/fallthrough return-arm repair, and mixed fallthrough terminal-result repair. Those paths now share one bounds-checked rewrite-window helper instead of open-coding parallel rewritten-body copies, active-lane masks, optional rewrite application, and body-changed counters; red-first coverage failed on the missing helper name before passing. Validation passed focused result-rewrite application evidence (`1/1`), full DAE whitebox (`130/130`), `moon test src/passes` (`4397/4397`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7836/7836`). This is behavior-preserving operand/result repair consolidation, keeps active-lane rewrite policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-fourth consolidation completed 2026-07-09: `DaeSelectValueRewriteApplicationEvidence` plus `dae_apply_select_value_rewrites_to_body(...)` now centralize typed-select left/right/select repair application for single-result returned-value select repair and flat multi-result typed-select operand repair. Those paths now share one bounds-checked select repair helper instead of open-coding parallel operand-rewrite and select-rewrite blocks; red-first coverage failed on the missing helper name before passing. Validation passed focused select value rewrite application evidence (`1/1`), full DAE whitebox (`131/131`), `moon test src/passes` (`4398/4398`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7837/7837`). This is behavior-preserving operand/result repair consolidation, keeps typed-select candidate policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-fifth consolidation completed 2026-07-09: `DaeSingleResultControlPairApplicationEvidence` plus `dae_apply_single_result_control_pair_gc_evidence(...)` now centralize single-result `if` rewrite application for implicit returned-value and explicit-return zero-result refinement paths. Those paths now share value-result vs void-result block-type selection, rewritten then/else body insertion, and application evidence instead of open-coding parallel `if` reconstruction blocks; red-first coverage failed on the missing helper name before passing. Validation passed focused single-result control pair application evidence (`1/1`), full DAE whitebox (`132/132`), `moon test src/passes` (`4399/4399`), `moon fmt`, `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7838/7838`). This is behavior-preserving operand/result repair consolidation, keeps single-result arm candidate/LUB policy, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-sixth consolidation completed 2026-07-09: `DaeStackSliceLocalizationRequest` plus `dae_stack_slice_localization_evidence_from_request(...)` now centralize the bounded stack-slice localization request for typed-select condition recovery and parametric typeidx prefix-consumption. Both callers now build one request for body/range, initial/final stack expectations, branch depth, instruction policy, and keep/remove repair intent before deriving transition, no-escape, disposition, and removal-plan evidence; red-first coverage failed on the missing request/helper names before passing. Validation passed focused stack-slice localization request coverage (`1/1`), full DAE whitebox (`132/132`), `moon fmt`, `moon test src/passes` (`4399/4399`), and `git diff --check`. This is behavior-preserving value/stack-slice localization consolidation, keeps true localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-seventh consolidation completed 2026-07-09: `DaeSingleResultControlPairRepairEvidence` plus `dae_single_result_control_pair_repair_evidence(...)` now centralize single-result `if` arm candidate gathering, then/else LUB selection, optional arm rewrite selection, and value-result vs explicit-return zero-result `if` rewrite application. The implicit returned-value `if` GC refinement path and explicit-return zero-result `if` refinement path now consume one repair carrier instead of nesting parallel arm candidate extraction around the lower-level pair/application helpers; red-first coverage failed on the missing helper name before passing. Validation passed focused single-result control pair repair evidence (`1/1`), full DAE whitebox (`133/133`), `moon fmt`, `moon test src/passes` (`4400/4400`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7839/7839`). This is behavior-preserving operand/result repair and GC LUB/refinement consolidation, keeps the existing implicit-vs-explicit arm candidate policies, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-eighth consolidation completed 2026-07-09: `DaeIndexedInstructionRewriteApplicationEvidence` plus `dae_apply_indexed_instruction_rewrites_to_body(...)` now centralize sparse indexed body-edit application beneath masked result-slot repair and typed-select left/right/select repair. Those helpers now share duplicate-target rejection, out-of-range rejection, optional rewrite application, unchanged-body preservation, and rewrite-count/body-changed evidence instead of maintaining separate body-copy/counting loops; red-first coverage failed on the missing helper name before passing. Validation passed focused indexed instruction rewrite evidence (`1/1`), focused rewrite-application evidence (`2/2`), full DAE whitebox (`134/134`), `moon fmt`, `moon test src/passes` (`4401/4401`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7840/7840`). This is behavior-preserving operand/result repair application consolidation, keeps result-slot mask policy, typed-select ordering/select-slot validation, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - Ninety-ninth consolidation completed 2026-07-09: `DaeMultiResultOperandSlotEvidence` plus `dae_multi_result_operand_slot_prefix_evidence(...)` and `dae_multi_result_operand_slot_single_producer_evidence(...)` now centralize multi-result suffix slot prefix/fallback evidence beneath the single-`if` and flat typed-select operand scanners. Those paths now share dropped-pair skipping, exhausted-suffix rejection, operand-start projection, and zero-input/one-output single-producer fallback evidence instead of each pairing dropped-pair evidence with its own local fallback producer check; red-first coverage failed on the missing helper names before passing. Validation passed focused operand slot evidence (`1/1`), focused multi-result whitebox coverage (`3/3`), full DAE whitebox (`135/135`), `moon fmt`, `moon test src/passes` (`4402/4402`), and `git diff --check`. This is behavior-preserving value/stack-slice recovery consolidation, keeps single-`if` vs typed-select candidate policies, localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - One hundredth consolidation completed 2026-07-09: `DaeStackSliceLocalizationPolicy`, `dae_select_condition_localization_policy(...)`, `dae_parametric_typeidx_prefix_localization_policy(...)`, and `dae_stack_slice_localization_evidence_from_policy(...)` now centralize policy-backed localization request projection for multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption. Both scanners now share initial/final stack expectations, branch depth, instruction admissibility, and keep-operand repair intent through the policy entrypoint instead of open-coding request construction; red-first coverage failed on the missing policy helper names before passing. Validation passed focused localization evidence (`1/1`), focused stack-slice coverage (`2/2`), full DAE whitebox (`135/135`), `moon fmt`, `moon test src/passes` (`4402/4402`), and `git diff --check`. This is behavior-preserving localization/effect-preservation and value/stack-slice repair-policy consolidation, keeps true localization/retry behavior, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - One hundred first consolidation completed 2026-07-09: `dae_apply_first_instruction_rewrite_to_body(...)` now centralizes first-matching sparse body repair scans above `DaeIndexedInstructionRewriteApplicationEvidence`. Func237 outer-loop carrier wrapping, nested branch-result flattening, and recursive dropped-result branch-carrier folding now share the helper instead of each open-coding a scan/copy/single-instruction rewrite loop; red-first coverage failed on the missing helper name before passing. Validation passed focused first-instruction rewrite coverage (`1/1`), focused rewrite coverage (`17/17`), full DAE whitebox (`136/136`), `moon fmt`, `moon test src/passes` (`4403/4403`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7842/7842`). This is behavior-preserving operand/result repair-application consolidation, keeps existing Func237 shape policies, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - One hundred second consolidation completed 2026-07-09: `dae_try_rewrite_first_control_child_body(...)` now centralizes recursive control-child body repair reconstruction for block, loop, `if`, and `try_table` instructions. Func237 dropped-result branch-carrier folding and unreachable-after-if-else folding now share the helper instead of each open-coding child traversal/rebuild cases; the unreachable-after-if-else recursive body scan also delegates first-matching instruction replacement through `dae_apply_first_instruction_rewrite_to_body(...)`. Red-first coverage failed on the missing helper name before passing. Validation passed focused control-child rewrite coverage (`1/1`), focused rewrite coverage (`18/18`), full DAE whitebox (`137/137`), `moon fmt`, `moon test src/passes` (`4404/4404`), `git diff --check`, `moon info` with pre-existing non-DAE warnings, and full `moon test` (`7843/7843`). This is behavior-preserving operand/result repair-application consolidation, keeps existing Func237 shape policies, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - One hundred third consolidation completed 2026-07-09: `dae_apply_first_current_or_control_child_body_rewrite(...)` now centralizes the current-body-then-recursive-control-child body rewrite application pattern. Func237 dropped-result branch-carrier folding, unreachable-after-if-else folding, immediate local-copy folding, and used-temp-copy retargeting now share the helper instead of mixing current-body scans with separate child scan/copy loops; red-first coverage failed on the missing helper name before passing. Validation passed focused recursive-body rewrite coverage (`1/1`), focused rewrite coverage (`19/19`), full DAE whitebox (`138/138`), `moon fmt`, `moon test src/passes` (`4405/4405`), and `git diff --check`. This is behavior-preserving operand/result repair-application consolidation, keeps existing Func237 shape policies, root-before-child order, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, or optimizing cleanup to plain DAE.
      - One hundred fourth consolidation completed 2026-07-09: `DaeRecursiveBodyRewriteApplicationEvidence` / `dae_apply_first_recursive_body_rewrite(...)` now centralizes current-body versus first recursive-control-child body-rewrite application evidence over the sparse indexed edit helper. The existing `dae_apply_first_current_or_control_child_body_rewrite(...)` wrapper now delegates through the evidence carrier, and Func237 inner-iteration loop wrapping plus i32-param-loop spill repair share the same recursive application planner instead of open-coding recursive scan/copy/rebuild loops; red-first coverage failed on the missing helper name before passing. Validation passed so far: focused recursive-body application evidence (`1/1`), focused recursive-body rewrite coverage (`2/2`), and focused rewrite coverage (`20/20`). This is behavior-preserving repair-application consolidation, preserves root-before-child and first-child traversal order, keeps existing Func237/loop-spill shape policies, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, localization/retry, or optimizing cleanup to plain DAE.
      - One hundred fifth consolidation completed 2026-07-09: `DaeBodyRewriteFixpointEvidence` / `dae_apply_body_rewrite_until_stable(...)` now centralizes bounded body-rewrite convergence evidence with max-iteration, iteration-count, limit, and body-change facts. Func237 inner-iteration loop wrapping and selected zero-tee/set splitting now consume the same fixpoint helper instead of maintaining separate bounded rewrite loops; red-first coverage failed on the missing helper name before passing. Validation passed focused fixpoint evidence (`1/1`), focused rewrite coverage (`21/21`), full DAE whitebox (`140/140`), `moon fmt`, `moon test src/passes` (`4407/4407`), and `git diff --check`. This is behavior-preserving convergence/fixpoint consolidation, keeps existing loop bounds, cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, localization/retry behavior, and optimizing cleanup boundaries unchanged.
      - One hundred sixth consolidation completed 2026-07-09: `dae_apply_first_recursive_instruction_body_rewrite(...)` now centralizes first-matching recursive instruction rewrite evidence, and `dae_apply_first_recursive_control_child_body_rewrite(...)` is a projection over that helper. Control-child recursion, Func237 leading-retain hoisting, and Func237 terminal-`br 0` block flattening now share the evidence helper instead of keeping duplicate scan/copy loops; red-first coverage failed on the missing control-child helper name before passing. Validation passed focused recursive-body application evidence (`1/1`), focused rewrite coverage (`21/21`), `moon fmt`, `moon test src/passes` (`4407/4407`), and `git diff --check`. This is behavior-preserving repair-application consolidation, preserves prior instruction-level traversal semantics, cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, localization/retry behavior, and optimizing cleanup boundaries unchanged.
      - One hundred seventh consolidation completed 2026-07-09: `dae_apply_first_recursive_instruction_body_rewrite_to_body(...)` now exposes the evidence-backed array-return projection over the recursive instruction rewrite helper. Func237 nested branch-result flattening and zero-branch flattening now share that projection with leading-retain hoisting and terminal-`br 0` flattening instead of keeping separate first-match scan/copy loops or ad hoc evidence unpacking; red-first coverage failed on the missing projection helper name before passing. Validation passed focused recursive-body application evidence (`1/1`) and focused rewrite coverage (`21/21`) so far. This is behavior-preserving repair-application consolidation, preserves prior instruction-level traversal semantics, Func237 shape policies, cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, localization/retry behavior, and optimizing cleanup boundaries unchanged.
      - One hundred eighth consolidation completed 2026-07-09: `DaeStackSliceLocalizationEvidence` now carries copied no-escape slice evidence and `dae_stack_slice_localization_policy_accepts(...)` centralizes the evidence-to-boolean policy projection. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption now share the policy-acceptance helper and copied accepted-slice facts instead of each open-coding match/unpack boilerplate; red-first coverage failed on the missing field/helper names before passing. Validation passed `moon fmt`, focused stack-slice localization evidence (`1/1`), focused localization coverage (`2/2`), `moon test src/passes` (`4407/4407`), and `git diff --check`. This is behavior-preserving localization/effect-preservation consolidation, preserves true localization/retry gaps, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred ninth consolidation completed 2026-07-09: `dae_control_block_type_signature_evidence_from_type_sec(...)`, `dae_control_block_type_stack_shape_evidence(...)`, `dae_control_block_type_stack_shape_accepts(...)`, and `dae_control_block_type_exact_signature_accepts(...)` centralize type-section-backed control block signature, stack-shape, and exact-signature proof. `dae_control_block_type_signature_evidence(...)`, `dae_control_single_result_block_type_evidence(...)`, zero-param raw cleanup block-type predicates, and the Func237 single-`i32`-param zero-result gate now share the same signature carrier instead of resolving typeidx function signatures locally in each scanner; red-first coverage failed on the missing helper names before passing. Validation passed focused control-entry stack evidence (`1/1`) so far. This is behavior-preserving control-type evidence and cleanup-policy consolidation, preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred tenth consolidation completed 2026-07-09: `DaeControlBlockTypeGcResultEvidence`, `dae_control_block_type_non_null_concrete_gc_result_evidence(...)`, `dae_control_block_type_single_non_null_concrete_gc_result_evidence(...)`, and `dae_control_block_type_last_non_null_concrete_gc_result_evidence(...)` centralize non-null concrete GC result-slot proof over the shared control block signature carrier. Direct GC actual candidate recovery and result-shape GC actual recovery now share the same type-section-backed slot evidence instead of maintaining separate `ValTypeBlockType` / `TypeIdxBlockType` scanners for single-result and trailing-result controls; red-first coverage failed on the missing helper names before passing. Validation passed focused control-block GC result evidence (`1/1`) so far. This is behavior-preserving GC type evidence and control-type consolidation, preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred eleventh consolidation completed 2026-07-09: `dae_control_block_type_zero_input_evidence(...)` now centralizes zero-input control block proof over the shared type-section-backed control block signature carrier. Single-block result-carrier recovery and direct GC explicit-return block/loop/`if` candidate checks consume the helper instead of each maintaining separate `input_count == 0`, `VoidBlockType`/`ValTypeBlockType`, or `Some((0, _))` stack-effect tuple checks; red-first coverage failed on the missing helper name before passing. Validation passed `moon fmt`, focused control-block input evidence (`1/1`), focused control-block GC result evidence (`1/1`), focused control-entry stack evidence (`1/1`), `moon test src/passes` (`4409/4409`), and `git diff --check`. This is behavior-preserving control-type evidence consolidation, preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred twelfth consolidation completed 2026-07-09: `dae_control_block_type_zero_input_output_count_evidence(...)` now centralizes zero-input plus exact-output-count control block proof over the same type-section-backed signature carrier. Single-block result-carrier recovery, mixed single- and multi-result if/fallthrough GC candidates, terminal-return block carriers, and multi-result `if`/block/loop carrier gates in direct GC type refinement consume the helper instead of unpacking `Some((0, 0))` or `Some((0, outputs)) if outputs == results.length()` stack-effect tuples locally; red-first coverage failed on the missing helper name before passing. Validation passed `moon fmt`, focused control-block input evidence (`1/1`), focused multi-result coverage (`3/3`), `moon test src/passes` (`4409/4409`), and `git diff --check`. This is behavior-preserving control-type/value-slice evidence consolidation, preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred thirteenth consolidation completed 2026-07-09: `DaeControlBlockTypeGcResultSlot`, `dae_control_block_type_non_null_concrete_gc_result_from_signature_evidence(...)`, and `dae_control_block_type_non_null_concrete_gc_result_slot_evidence(...)` now centralize single-result and trailing-result non-null concrete GC control-block slot selection over the shared type-section-backed signature carrier. The existing single-result and last-result wrappers share the same slot selector and signature-to-result projection instead of each maintaining result-count/result-index/candidate checks; red-first whitebox coverage failed on the missing slot helper before passing. Validation passed focused control-block GC result evidence (`1/1`) so far. This is behavior-preserving GC type/control-type evidence consolidation, preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred fourteenth consolidation completed 2026-07-09: `DaeEmptyZeroResultControlCleanupEvidence`, `dae_empty_zero_result_control_cleanup_evidence(...)`, and `dae_rebuild_cleaned_single_body_control_or_drop(...)` now centralize raw-cleanup zero-result control body cleanup over the shared type-section-backed zero-input/output-count carrier. The block, loop, and `try_table` arms in `dae_strip_nops_and_dropped_pure_leaf_instrs(...)` share one child-cleanup/rebuild helper and one empty-control drop decision instead of each maintaining the same zero-result-empty-body predicate; red-first whitebox coverage failed on the missing cleanup helper before passing. Validation passed focused empty zero-result control cleanup evidence (`1/1`) so far. This is behavior-preserving cleanup-evidence consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.
      - One hundred fifteenth consolidation completed 2026-07-09: `dae_single_result_control_gc_candidate_instr_evidence(...)` now centralizes instruction-level block/loop direct-GC result candidate dispatch over the existing `DaeSingleResultControlGcCandidateEvidence` body recovery and rewrite wrapper. The `@lib.Block` and `@lib.Loop` arms in `dae_direct_gc_result_candidate_instr(...)` share one helper instead of each maintaining the same candidate/apply sequence; red-first whitebox coverage failed on the missing helper before passing. Validation passed `moon fmt`, focused single-result control GC evidence (`1/1`), `moon test src/passes` (`4410/4410`), and `git diff --check`. This is behavior-preserving GC type/control repair consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, localization/retry behavior, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred sixteenth consolidation completed 2026-07-09: `dae_no_escaping_branch_copied_slice_evidence(...)` now centralizes copied no-escape slice recovery over range-aware branch evidence. Copied range, copied prefix, and copied whole-body evidence helpers share the accepted-slice projection instead of each matching branch evidence and copying accepted instructions separately; red-first whitebox coverage failed on the missing helper before passing. Validation passed `moon fmt`, focused escaping-branch evidence (`1/1`), `moon test src/passes` (`4410/4410`), and `git diff --check`. This is behavior-preserving localization/value-slice evidence consolidation, preserves branch-depth policy, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred seventeenth consolidation completed 2026-07-09: `dae_trivially_dropped_prefix_evidence(...)` now centralizes full-prefix proof over `DaeDroppedOperandPairSkipEvidence`. Typed-select value-slice recovery, flat multi-result and multi-return result-start recovery, multi-result suffix-bound enumeration, and direct-GC flat result fallback consume one dropped-prefix carrier instead of a separate boolean-only even-pair scan; red-first whitebox coverage failed on the missing helper before passing. Validation passed `moon fmt`, focused multi-result operand-slot evidence (`1/1`), `moon test src/passes` (`4410/4410`), and `git diff --check`. This is behavior-preserving value/stack-slice evidence consolidation, preserves dropped-pair policy, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred eighteenth consolidation completed 2026-07-09: `DaeLocalUsageEvidence`, `dae_local_usage_evidence(...)`, range evidence, and the single-get-write projection now centralize recursive local get/write facts, with `try_table` presence carried for future local preservation consumers. Func237 immediate-copy and used-temp cleanup-localization, local-constant inlining, selected Func311 terminal-call argument rewriting, and high-local trace reporting consume one local-usage carrier instead of paired get/write scanners; red-first whitebox coverage failed on the missing helper before passing. Validation passed `moon fmt`, focused local-usage evidence (`1/1`), focused Func237 copy coverage (`9/9`), `moon test src/passes` (`4411/4411`), and `git diff --check`. This is behavior-preserving localization/cleanup evidence consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred nineteenth consolidation completed 2026-07-09: `DaeFunc237LocalCopyCandidateEvidence` and `dae_func237_local_copy_candidate_evidence(...)` now centralize Func237 local-copy candidate eligibility over `DaeLocalUsageEvidence`. Immediate-copy and used-temp cleanup-localization share parameter-local rejection, mode-specific temp get/write gates, and root `try_table` preservation from the local-usage `has_try_table` fact instead of maintaining separate temp-usage predicates plus a recursive try-table-only scanner; red-first whitebox coverage failed on the missing candidate helper before passing. Validation passed `moon fmt`, focused candidate evidence (`1/1`), focused Func237 copy coverage (`10/10`), `moon test src/passes` (`4412/4412`), and `git diff --check`. This is behavior-preserving local-preservation evidence consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twentieth consolidation completed 2026-07-09: `DaeLocalCopyTargetEvidence`, `dae_local_copy_target_evidence(...)`, and `dae_local_copy_target_evidence_instr_independent(...)` now centralize protected temp/target distinctness, parameter-local rejection, and preserved-operand temp/target independence for local-copy cleanup. Func237 immediate-copy, used-temp copy, and store-carrier copy cleanup-localization share the protected target proof instead of each keeping local slot/self-copy predicates inline; red-first whitebox coverage failed on the missing target helper before passing. Validation passed `moon fmt`, focused local-copy target evidence (`1/1`), focused Func237 copy coverage (`10/10`), `moon test src/passes` (`4413/4413`), and `git diff --check`. This is behavior-preserving local-preservation evidence consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-first consolidation completed 2026-07-09: `DaeDroppablePureStackEvidence`, `dae_droppable_pure_stack_evidence(...)`, and `dae_droppable_pure_stack_evidence_allows_live_local_copy_cleanup(...)` now centralize trailing pure-stack bounds plus local-get/local-free policy for raw cleanup live-local-copy folding. The five-instruction, three-instruction, two-instruction set/get, and tee/set copy-chain branches share one pure-stack evidence carrier instead of repeatedly using a boolean-only helper; red-first whitebox coverage failed on missing helper names before passing. Validation passed focused droppable pure-stack evidence (`1/1`), focused dropped pure-leaf cleanup evidence (`1/1`), focused raw cleanup local-copy folding (`1/1`), `moon fmt`, and `moon test src/passes` (`4414/4414`). This is behavior-preserving cleanup/localization evidence consolidation, preserves Binaryen's local-get-sourced-copy preservation rule, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-second consolidation completed 2026-07-09: `DaeDroppedPureLeafSubjectEvidence`, `dae_dropped_pure_leaf_subject_evidence_from_stack(...)`, and `dae_dropped_pure_leaf_subject_evidence_from_leaf(...)` now centralize raw dropped pure-leaf cleanup subjects across stack-suffix truncation and adjacent leaf-plus-`drop` stripping. Both cleanup constructors now consume one carrier for dropped instruction, prefix bounds, singleton status, prefix-observable-effect fact, integer-constant debris preservation, local-get-after-same-set preservation, and local-get-after-effect preservation instead of separate stack-vs-leaf predicates; red-first whitebox coverage failed on missing helper names before passing. Validation passed focused dropped pure-leaf subject evidence (`1/1`), focused dropped pure-leaf cleanup evidence (`1/1`), focused raw integer debris cleanup (`2/2`), and focused raw cleanup local-copy folding (`1/1`) so far. This is behavior-preserving cleanup-preservation evidence consolidation, preserves plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-third consolidation completed 2026-07-09: `DaeFlatMultiResultSuffixEvidence` and `dae_flat_multi_result_suffix_evidence(...)` now centralize flat and terminal-`return` result-tail proof for `dae_multi_result_control_carrier_chain_slice(...)`. The carrier-chain helper consumes one start/end/result-count/terminal-return evidence record instead of switching between separate flat-start and multi-return-start scanners; red-first whitebox coverage failed on missing helper names before passing. Validation passed `moon fmt`, focused control-result slice coverage (`1/1`), `moon test src/passes` (`4415/4415`), and `git diff --check`. This is behavior-preserving value/stack-slice bound recovery consolidation; it preserves dropped-prefix policy, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-fourth consolidation completed 2026-07-09: `DaeLocalReferenceExtentEvidence` and `dae_local_reference_extent_evidence(...)` now centralize recursive local-reference extent proof. High-local cleanup guards and max-local projection now share one carrier, and the selected Func311 high-local trace path consumes that carrier directly instead of rescanning for the maximum after proving a high-local reference exists; red-first whitebox coverage failed on the missing helper name before passing. Validation passed `moon fmt`, focused local-usage evidence (`1/1`), `moon test src/passes` (`4415/4415`), and `git diff --check`. This is behavior-preserving localization/cleanup-preservation evidence consolidation; it preserves local trimming policy, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-fifth consolidation completed 2026-07-09: `DaeLiveLocalCopyCleanupEvidence` and `dae_live_local_copy_cleanup_evidence(...)` now centralize raw cleanup live-local-copy replacement plans over the existing droppable pure-stack carrier. The five-instruction triple-copy, three-instruction double-copy, and two-instruction set/get plus tee/set branches now ask one helper for replacement instructions, next index, and copy count instead of open-coding separate copy-chain recognizers; red-first whitebox coverage failed on the missing helper before passing. Validation passed `moon fmt`, focused droppable pure-stack evidence (`1/1`), `moon test src/passes` (`4415/4415`), and `git diff --check`. This is behavior-preserving cleanup/localization evidence consolidation; it preserves Binaryen's local-get-sourced-copy preservation rule, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-sixth consolidation completed 2026-07-09: `DaeEmptyZeroResultControlCleanupEvidence` now carries primary/secondary body lengths and body count, and `dae_empty_zero_result_if_cleanup_evidence(...)` centralizes empty zero-result `if` condition-strip cleanup over the same carrier used by block/loop/`try_table` empty-body cleanup. Raw cleanup no longer keeps an inline zero-result/empty-then/empty-else predicate in the `if` arm; red-first whitebox coverage failed on missing fields and the missing helper before passing. This is behavior-preserving cleanup-scheduling evidence consolidation; it preserves condition-stripping behavior, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-seventh consolidation completed 2026-07-09: `DaeEffectFreeFallthroughEvidence` and `dae_effect_free_fallthrough_evidence(...)` centralize mixed-arm fallthrough side-effect proof for the single-result and multi-result return/fallthrough arm GC evidence paths. Both scanners now consume one helper for accepting fallthrough arms with no observable effect or branch instead of open-coding separate `dae_instrs_have_observable_effect_or_branch(...)` rejection branches; red-first whitebox coverage failed on the missing helper names before passing. Validation passed `moon fmt`, focused effect-free fallthrough evidence (`1/1`), focused mixed fallthrough arm evidence (`1/1`), `moon test src/passes` (`4416/4416`), and `git diff --check`. This is behavior-preserving localization/effect-preservation evidence consolidation; it preserves GC LUB behavior, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-eighth consolidation completed 2026-07-09: `DaeEffectFreeBodyEvidence` and `dae_effect_free_body_evidence(...)` centralize side-effect-free body proof for single-block result-carrier recovery, nested block-carrier unwrapping in direct GC refinement, and mixed return/fallthrough fallthrough evidence. These paths now share one accepted body carrier instead of calling `dae_instrs_have_observable_effect_or_branch(...)` directly at each body-acceptance site; red-first whitebox coverage failed on the missing single-block carrier field and missing effect-free body helper names before passing. Validation passed `moon fmt`, focused effect-free body evidence (`1/1`), focused control-result slices (`1/1`), focused mixed fallthrough arm evidence (`1/1`), `moon test src/passes` (`4416/4416`), and `git diff --check`. This is behavior-preserving localization/effect-preservation evidence consolidation; it preserves GC LUB behavior, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred twenty-ninth consolidation completed 2026-07-09: `DaeTerminalReturnBodyEvidence` and `dae_terminal_return_body_evidence(...)` centralize terminal-`return` body proof for terminal-return block-carrier recovery and nested block-carrier unwrapping in direct GC refinement. These paths now share one accepted terminal-return body carrier instead of using the old boolean-only scanner in one path and a local effect-free-or-terminal-return guard in the other; red-first whitebox coverage failed on the missing terminal-return evidence helper names before passing. Validation passed `moon fmt`, focused terminal-return body evidence (`1/1`), focused control-result slices (`1/1`), focused effect-free body evidence (`1/1`), `moon test src/passes` (`4417/4417`), and `git diff --check`. This is behavior-preserving localization/effect-preservation and value/control carrier evidence consolidation; it preserves GC LUB behavior, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirtieth consolidation completed 2026-07-09: `dae_control_block_type_exact_signature_evidence(...)`, `DaeI32ParamZeroResultLoopEvidence`, and `dae_i32_param_zero_result_loop_evidence(...)` centralize type-section-backed single-`i32`-param/zero-result loop proof for the explicit-carrier and iteration-local typed-loop spill paths. Both paths now share exact signature evidence, loop-body length evidence, and optional first-`local.tee` iteration-local evidence instead of independently matching loop block types and first-instruction carrier locals; red-first whitebox coverage failed on the missing helper names before passing. Validation passed `moon fmt`, focused i32-param loop evidence (`1/1`), focused explicit-carrier typed-loop spill (`1/1`), focused nested iteration-local typed-loop spill (`1/1`), `moon test src/passes` (`4418/4418`), and `git diff --check`. This is behavior-preserving control-block-shape/localization evidence consolidation; it preserves the narrow typed-loop spill policy, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-first consolidation completed 2026-07-09: `DaeDroppablePureControlValueEvidence` and `dae_droppable_pure_control_value_evidence(...)` centralize raw-cleanup pure-control value proof for zero-input/single-result block/loop/`try_table` bodies and pure value `if` arms. Dropped pure-value arity and pure-`if` condition-stripping now share the same type-section-backed evidence carrier for condition arity, body lengths, catch count, and pure-body acceptance instead of keeping separate non-`if` and `if` pure-control scanners; red-first whitebox coverage failed on the missing helper names before passing. Validation passed `moon fmt`, focused droppable pure-control value evidence (`1/1`), focused dropped-pure coverage (`6/6`), `moon test src/passes` (`4419/4419`), and `git diff --check`. This is behavior-preserving raw-cleanup/control-value evidence consolidation; it preserves catch-free `try_table` policy, pure-body requirements, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-second consolidation completed 2026-07-09: `DaeLocalGetRangeEvidence` and `dae_local_get_range_evidence(...)` centralize suffix local-get/local-free proof for raw cleanup preservation. Droppable pure-stack cleanup evidence and dropped pure-leaf stack-subject evidence now share one start/end/singleton/local-free/range-allows-cleanup carrier instead of independently scanning for local gets and deriving cleanup eligibility; red-first whitebox coverage failed on the missing helper names before passing. Validation passed `moon fmt`, focused local-get range evidence (`1/1`), focused droppable pure-stack evidence (`1/1`), focused dropped pure-leaf subject evidence (`1/1`), `moon test src/passes` (`4420/4420`), and `git diff --check`. This is behavior-preserving raw-cleanup/local-preservation evidence consolidation; it preserves local-get-sourced copy-chain retention, dropped pure-leaf singleton cleanup behavior, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-third consolidation completed 2026-07-09: `DaeRedundantAccessCleanupEvidence` and `dae_redundant_access_cleanup_evidence(...)` centralize adjacent local/global access-pair cleanup rewrites. The raw cleanup loop now asks one carrier for local-get/local-set removal, local-get/local-tee skipping, global-get/global-set removal, and local-tee/local-set replacement instead of open-coding four neighboring pair recognizers; red-first whitebox coverage failed on the missing helper names before passing. Validation passed focused redundant access cleanup evidence (`1/1`), focused raw cleanup (`21/21`), `moon fmt`, focused redundant access cleanup evidence (`1/1`), `moon test src/passes` (`4421/4421`), and `git diff --check`. This is behavior-preserving raw-cleanup/localization evidence consolidation; it preserves existing pair rewrites, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-fourth consolidation completed 2026-07-09: `DaeDroppedLocalTeeSuffixCleanupEvidence` and `dae_dropped_local_tee_suffix_cleanup_evidence(...)` centralize raw-cleanup local-tee suffix rewrites. The raw cleanup loop now asks one carrier for direct local-tee/drop, local-tee plus non-trapping unary-drop, single non-trapping binary-suffix drop, and paired non-trapping binary-suffix drop rewrites instead of open-coding separate neighboring suffix scanners; red-first whitebox coverage failed on the missing helper names before passing. Validation passed focused dropped local-tee suffix cleanup evidence (`1/1`), focused raw cleanup (`21/21`), `moon fmt`, repeated focused dropped local-tee suffix cleanup evidence (`1/1`), `moon test src/passes` (`4422/4422`), and `git diff --check`. This is behavior-preserving raw-cleanup/localization evidence consolidation; it preserves existing `local.set` replacements, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-fifth consolidation completed 2026-07-09: `DaeLiveLocalCopyCleanupEvidence` now carries `requires_local_free_stack`, and `dae_live_local_copy_cleanup_evidence(...)` owns the unconditional redundant same-target `local.set`/`local.get`/`local.set` collapse as well as the existing local-free-stack copy-chain repairs. The raw cleanup loop no longer keeps a separate three-instruction same-target scanner, while distinct-target stackification still requires the shared droppable pure-stack proof; red-first whitebox coverage first failed on the missing policy field before passing after implementation. Validation passed focused droppable pure-stack evidence (`1/1`), focused raw cleanup (`21/21`), `moon fmt`, repeated focused coverage, `moon test src/passes` (`4422/4422`), and `git diff --check`. This is behavior-preserving raw-cleanup/localization evidence consolidation; it preserves same-target `local.set` output, local-get-sourced copy retention, touched-function cleanup gating, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-sixth consolidation completed 2026-07-09: `DaeDroppedPureExpressionCleanupEvidence` and `dae_dropped_pure_expression_cleanup_evidence(...)` centralize adjacent dropped pure-expression repair plans. The raw cleanup loop now asks one carrier for pure leaf-plus-`drop`, leaf/unary-plus-`drop`, and two-leaf/binary-plus-`drop` deletion ranges instead of open-coding three sequential scanners; the carrier records bounds, next cursor, operand/operator counts, cleanup family, and whether dropped-leaf preservation policy applies. Red-first whitebox coverage first failed on the missing helper names before passing. Validation passed `moon info`, `moon fmt`, focused dropped pure-expression evidence (`1/1`), focused raw cleanup (`21/21`), `moon test src/passes` (`4423/4423`), and `git diff --check`. This is behavior-preserving raw-cleanup/localization evidence consolidation; it preserves effect-adjacent integer leaf debris, existing unary/binary cleanup, the local-get-left binary bailout, plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries unchanged.
      - One hundred thirty-seventh consolidation completed 2026-07-09: `DaeSelectConditionLocalizationEvidence` and `dae_select_condition_localization_evidence(...)` centralize typed-select condition acceptance as reusable evidence instead of a boolean gate. Singleton ranges record trivial-droppability plus optional zero-input/single-result producer evidence; multi-instruction ranges retained the existing policy-backed stack transition, copied no-escape slice, disposition, and then-current keep-operand repair plan; the one hundred forty-fifth slice replaces that removal-plan field with the shared four-outcome rewrite disposition. Select value-slice bound recovery plus argument-side and returned-value GC select evidence now share the carrier, and the old singleton/boolean wrappers are removed. Red-first whitebox coverage failed on the missing helper names before implementation. Validation passed `moon fmt`, focused select-condition localization evidence (`1/1`), focused stack-slice coverage (`3/3`), focused select pass coverage (`82/82`), `moon test src/passes` (`4424/4424`), and `git diff --check`. This is behavior-preserving value/stack-slice and localization evidence consolidation; it does not widen condition acceptance, add localize-then-retry, alter GC refinement, change plain-vs-optimizing scheduling, rerun compare-pass, or make direct DAE green.
      - One hundred thirty-eighth consolidation completed 2026-07-09: `DaeSelectValueSlices` now retains its proven `DaeSelectConditionLocalizationEvidence`, and `dae_gc_select_value_slices_evidence(...)` consumes the full carrier for single-result and multi-result typed-select GC refinement. The multi-result path replaces four parallel left/right/condition/select index arrays with one `Array[DaeSelectValueSlices?]`, keeping operand, condition, and select evidence coherent through candidate discovery and repair. Red-first whitebox coverage failed because the carrier lacked `condition` and the plural evidence entrypoint was unbound, then passed after implementation for singleton and multi-instruction conditions. Validation passed `moon info`, `moon fmt`, focused select-value coverage (`3/3`), focused select-condition localization evidence (`1/1`), focused multi-result coverage (`3/3`), focused select pass coverage (`82/82`), `moon test src/passes` (`4424/4424`), and `git diff --check`. This is behavior-preserving value/stack-slice and GC-evidence consolidation; accepted shapes, LUB/refinement, repair output, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred thirty-ninth consolidation completed 2026-07-09: `DaeGcSelectValueSlicesEvidence` and `dae_gc_select_value_slices_candidate_evidence(...)` now pair recovered select slices with their computed candidate/LUB/refinement and rewrite evidence. Single-result returned-value select refinement and multi-result typed-select operand discovery share the combined entrypoint, and the multi-result path stores one complete per-result carrier so rewrite application no longer recomputes GC evidence against the original body. Red-first whitebox coverage failed on the missing helper, then passed with a multi-instruction condition while asserting recovered bounds and GC refinement. Validation passed `moon info` with pre-existing warnings, `moon fmt`, focused GC select value evidence (`1/1`), focused select-value coverage (`3/3`), focused multi-result coverage (`3/3`), focused public select coverage (`82/82`), `moon test src/passes` (`4424/4424`), and `git diff --check`. This is behavior-preserving value/stack-slice and GC-evidence threading; accepted select shapes, LUB/refinement, indexed rewrite ordering, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred fortieth consolidation completed 2026-07-09: `dae_gc_select_actual_value_slices_evidence(...)` replaces argument-side typed-select raw index reconstruction with the existing condition-carrying `DaeSelectValueSlices` and returns `DaeGcSelectValueSlicesEvidence`. The then-combined `dae_gc_actual_shape_evidence(...)` recovered that carrier, strictly preserved singleton left/right actual acceptance, and routed candidate collection plus refined-actual repair through the same shape abstraction while keeping target-dependent old/rewrite type planning in each traversal. Red-first whitebox coverage failed on the missing helper, then passed with a multi-instruction stack-local condition and nullable `ref.null` rewrite evidence. Validation passed `moon info` with pre-existing warnings, `moon fmt`, focused GC select actual evidence (`1/1`), focused GC actual shape evidence (`1/1`), focused public typed-select coverage (`3/3`), `moon test src/passes` (`4424/4424`), and `git diff --check`. This is behavior-preserving value/stack-slice and GC-evidence consolidation; accepted operand shapes, LUB/refinement, repair output, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred forty-first consolidation completed 2026-07-09: the then-combined `DaeGcActualShapeEvidence` retained its optional complete `DaeGcSelectValueSlicesEvidence` branch instead of flattening argument-side select rewrites into raw left/right fields. `dae_rewrite_refined_gc_actual_slice(...)` consumes that branch through `dae_apply_select_value_rewrites_to_body(...)`, making refined-actual repair the third production consumer of the indexed left/right/select repair path and removing direct `slice.start`, `slice.start + 1`, and `slice.end - 1` select edits. Candidate and rewrite traversals still recompute target-dependent evidence independently, avoiding stale body-indexed evidence across phases. Red-first whitebox coverage failed because the shape carrier lacked `select`, then passed with singleton operands and a three-instruction stack-local condition. Validation passed `moon info` with pre-existing warnings, `moon fmt`, focused GC actual-shape evidence (`1/1`), focused select-rewrite evidence (`1/1`), focused public typed-select coverage (`3/3`), `moon test src/passes` (`4424/4424`), and `git diff --check`. This is behavior-preserving value/stack-slice and repair-application consolidation; accepted shapes, LUB/refinement, effects, convergence, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred forty-second consolidation completed 2026-07-09: `DaeAdjacentParamForwardEvidence` and `dae_adjacent_param_forward_evidence(...)` centralize the exact adjacent `local.get 0 .. local.get n-1` operand range ending at a call index. Self-forwarded local-use suppression, self-call origin handling, and dead-suffix dropped-self-call full-forward detection now share that target-independent proof instead of keeping three copies of the prefix scanner. Target-callee matching stays in callers, dead-suffix detection still enforces the unreachable-root floor, zero-parameter behavior remains caller-specific, and ignored-local marking covers exactly the recovered range. Red-first whitebox coverage first failed because the helper was unbound, then passed for valid, wrong/reordered, insufficient-prefix, empty-prefix, exact ignored-range, and dead-suffix-floor cases. Validation passed `moon info` with pre-existing warnings, `moon fmt`, focused adjacent forwarding evidence (`1/1`), `moon test src/passes` (`4425/4425`), and `git diff --check`. This is behavior-preserving ownership/call-boundary and value-range consolidation; accepted forwarding shapes, convergence, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred forty-third consolidation completed 2026-07-09: `DaeSingleBodyControlShapeEvidence` and `DaeSingleBodyControlRewriteEvidence` centralize block/loop control identity, original versus rewritten body selection, replacement block type, and reconstruction. Explicit-return void repair and refined single-result GC control repair now share the same carrier/application path; `DaeSingleResultControlGcCandidateEvidence` retains candidate-specific facts, paired-arm `if` repair stays separate, and the explicit-return consumer preserves the old no-rewrite result for already-void unchanged controls. Red-first whitebox coverage first failed because the new helpers were unbound, then passed for block/loop identity, unchanged/rewritten body selection, refined-valtype/void replacements, unsupported-control rejection, and both production consumers. Validation passed `moon info` with pre-existing warnings, `moon fmt`, focused single-body reconstruction (`1/1`), focused single-result GC control evidence (`1/1`), `moon test src/passes` (`4426/4426`), and `git diff --check`. This is behavior-preserving GC/result repair reconstruction consolidation; zero-input and candidate/refinement gates, effects, localization/retry, convergence, cleanup scheduling, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred forty-fourth consolidation completed 2026-07-09: `DaeGcActualShapeKind` / `DaeGcActualShapeDiscoveryEvidence` now separate target-independent direct/control/select shape discovery from `DaeGcActualTypeEvidence` target-specific candidate/refinement/LUB/rewrite planning. Candidate collection and refined-actual rewriting each call `dae_gc_actual_value_slice_shape_evidence(...)` and then `dae_gc_actual_type_evidence(...)` against their current body and target type, so body-indexed select evidence is not retained across mutation phases. The type evaluator re-reads current direct/control terminals, validates retained select bounds, and rejects changed-terminal or wrong-target evidence. Red-first whitebox coverage first failed because the new helpers and constructors were unbound, then passed for direct/block/select distinction, candidate-only evidence, `require_refined` rejection, nullable select rewrite planning, and stale-body/wrong-target-type avoidance. Validation passed `moon fmt`, focused shape-discovery coverage (`1/1`), focused GC actual-shape coverage (`2/2`), focused GC select-actual coverage (`1/1`), `moon test src/passes` (`4427/4427`), `moon info` with pre-existing warnings, and full `moon test` (`7866/7866`). This is behavior-preserving value/stack-slice and GC-evidence phase separation; accepted shapes, effects, localization/retry, convergence, cleanup scheduling, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.
      - One hundred forty-fifth consolidation completed 2026-07-09: `DaeOperandRewriteAction` / `DaeOperandRewriteDisposition` supersede the removal-only three-way action with four explicit outcomes: direct drop, effect-preserving replay/drop, localize-and-retry eligibility, and escaping-branch rejection. `dae_callsite_operand_removal_plan(...)` derives and consistency-checks the shared carrier before callsite replay/drop repair, while `dae_stack_slice_localization_evidence_from_request(...)` carries it after transition and no-escape proof for typed-select condition and parametric typeidx prefix consumers. Localization evidence no longer carries a keep-operand removal plan, and `DaeOperandRemovalPlan.requires_localization_retry` keeps localize-and-retry distinct from direct rejection. Red-first whitebox coverage first failed because the new disposition and consistency helpers were unbound, then passed for all four outcomes, repair-plan consistency, and both production paths. Validation passed `moon fmt`, focused operand/localization/select/callsite tests, `moon test src/passes` (`4428/4428`), `moon info` with 9 pre-existing warnings, and full `moon test` (`7867/7867`). This is behavior-preserving disposition/repair evidence consolidation; no scratch local is allocated, no retry is applied, accepted shapes and output are unchanged, plain-vs-optimizing scheduling is unchanged, no compare lane was rerun, and direct DAE remains raw-red.
      - One hundred forty-sixth consolidation completed 2026-07-09: `dae_apply_body_rewrite_until_stable_or_original(...)` now projects unchanged-body behavior from `DaeBodyRewriteFixpointEvidence` / `dae_apply_body_rewrite_until_stable(...)`. Func237 immediate-local-copy, used-temp-copy, and store-carrier-copy cleanup use the shared driver with their existing `32`, `64`, and `8` limits; Func256 mapped-loop-condition repair uses the evidence-returning helper with its existing limit of `4`. This removes four scanner-local convergence loops while preserving one-rewrite-at-a-time recognizers, ordering, bounds, and no-change contracts. Red-first whitebox coverage failed because the projection helper was unbound, then passed for changed, unchanged, and iteration-limited behavior. Focused fixpoint/Func237/Func256 tests passed, `moon test src/passes` passed `4429/4429`, `moon info` passed with 9 pre-existing warnings, and full `moon test` passed `7868/7868`. No compare lane was rerun; module-core scheduling, localization behavior, cleanup selection, plain-vs-optimizing scheduling, and direct raw-red status are unchanged.
      - Next target: inspect saved `dae-localization` residuals together with whole-call argument stack transitions and caller local-type reconstruction to determine whether a generic localize-all-arguments-then-reload repair can safely serve direct and `return_call` callsites without relying on Func237 artifact-specific machinery. Require explicit evaluation-order, effect/trap, ambient-stack, local-type, and escaping-branch proof plus boundary rescheduling. If two honest production consumers still do not emerge, consolidate non-final argument-side typeidx/control reconstruction or broader original-vs-current result-removal legality instead of adding another localization wrapper. Keep optimizing-only nested cleanup out of plain DAE.
      - Acceptance: write or update focused tests first for any behavior widening; run `moon test src/passes` for behavior changes; keep plain `dead-argument-elimination` / `dae` scheduler behavior distinct from `dae-optimizing`; do not report direct DAE green unless raw-red direct lanes are fixed or every residual family has measured Starshine-win proof.
      - Remaining risk: current direct DAE is raw-red (`9004/10000` normalized and `996` raw mismatches in the full random-all no-reduction lane); only `dae-tail-call-boundary`, `dae-unused-params`, and `dae-constant-args` saved residual families have full measured Starshine-win classifications so far. Exact adjacent full-parameter forwarding now shares one range proof, but broader ownership/origin evidence and non-adjacent forwarding remain scanner-heavy. Single-body block/loop reconstruction is now shared across explicit-return void repair and refined single-result GC repair, but paired-arm `if`, non-final argument-side control reconstruction, and broader repair planning remain separate. Argument-side typed-select actual evidence now separates direct/control/select shape discovery from current-target candidate/refinement/LUB/rewrite evaluation. Candidate collection and refined-actual repair deliberately rediscover per traversal, and target evaluation re-reads current terminals or validates retained select bounds, so target-dependent facts are not retained across phases; broader argument-side typeidx/control reconstruction and localization/retry planning remain open. Broader typeidx/control GC evidence beyond final actuals, the terminal-arm carrier, the mixed return/fallthrough multi-result arm evidence carrier, the mixed return/fallthrough single-result arm evidence carrier, the effect-free body and fallthrough evidence helpers, the single-result control-pair evidence carrier, the mixed return-arm rewrite application carrier, masked result-slot rewrite application carrier, typed-select rewrite application carrier, indexed instruction rewrite application carrier, recursive control-child rewrite helper, current-body-then-control-child recursive application helper, recursive body-rewrite application evidence helper, bounded body-rewrite fixpoint evidence plus unchanged-body projection now shared by inner-loop wrapping, zero-tee/set splitting, three Func237 local-copy cleanup families, and Func256 mapped-loop-condition repair, recursive instruction rewrite evidence helper, the single-result control-pair if-application and arm-candidate/LUB repair carriers, the single-arm refined-result application carrier, the block-type signature carrier, zero-input control evidence helper, zero-input/output-count control evidence helper, single-result block-type helper, nested-control children/rebuild carrier, multi-result suffix-bound carrier, bounded stack-slice simulation, expected-final transition, combined stack-slice localization/repair carrier, select-condition singleton/multi-instruction localization carrier, request-backed and policy-backed stack-slice localization carriers plus copied-slice policy-acceptance projection, Func237 local-copy gap, candidate, and target carriers, target-callee callsite carrier, dropped pure-expression cleanup repair-plan carrier, dropped pure-leaf cleanup preservation/subject carriers, droppable pure-stack, local-get range, redundant access cleanup, dropped local-tee suffix cleanup, and widened live-local-copy cleanup carriers, zero-result control empty-body/if cleanup carrier, i32-param zero-result typed-loop evidence, droppable pure-control value evidence, terminal-return body and effect-free body/fallthrough carriers, single-body block/loop reconstruction carrier plus single-result control GC candidate carrier and instruction-level control-GC dispatch helper, copied no-escape slice projection helper, dropped-prefix helper, dropped-pair skip carrier, and multi-result operand-slot fallback carrier, escaping-branch evidence plus prefix/suffix/copied-range/prefix/suffix, terminal-if no-escape, and terminal-prefix no-escape helpers, the single-block result-carrier helper, the flat/terminal result-suffix helper, local-reference extent evidence, boundary candidate legality beyond forwarding gates plus boundary-carried uniform-constant entry collection, owned-callee count/caller/self-recursion eligibility, the first module boundary snapshot, the first original-boundary snapshot plus shared result-preservation guards, no-param dropped-caller guard, parameterized self-side-chain guard, preservation-input context, and argument-side repair gate, result-removal prune row mixed-self/same-operand/escaped-block and dead-suffix side-call predicates plus result-preservation side-chain cleanup summary evidence and aggregate result-removal prune cleanup/skip blockers, repair application beyond the shared four-outcome operand rewrite disposition and consistency-checked callsite removal plan, especially scratch-local allocation, value-slice rewriting, and retry rescheduling, plus paired fallthrough/terminal-return GC refinement plus mixed return/fallthrough multi-result arm evidence plus mixed return/fallthrough single-result arm evidence plus single-result control-pair evidence plus mixed return-arm rewrite application plus paired-result application plus single-arm refinement application/control-result undropped direct-call blocker/final actual-shape/single-instruction/control-entry/projection/nested-control entry/nested-control children/rebuild/multi-result suffix-bound/bounded stack-slice simulation/expected-final transition/combined stack-slice localization-repair/select-condition localization/Func237 local-copy gap/target-callee callsite/dropped pure-leaf cleanup preservation/single-result control GC candidate body-rewrite/masked result-slot rewrite application/typed-select rewrite application/indexed instruction rewrite application/dropped-pair skip/multi-result operand-slot fallback/escaping-branch/prefix-suffix/copied-range-prefix-suffix/terminal-if no-escape/terminal-prefix no-escape/block-type signature/single-block result-carrier/stack operand-window/instruction-effect/single-value-producer/block-type-single-result/provenance subsets, the now-shared dropped-result call/drop traversal policy, the first dead-suffix unreachable-tail evidence carrier, the dead-suffix direct-call target carrier, the dead-suffix direct target-list carrier, the tail-call boundary blocker carrier, the direct-call target evidence carrier, the scoped dropped-result direct-call legality carrier, the no-param dropped-caller and parameterized self-side-chain original-boundary guard carriers, selected/full dropped-result repair scope plus the all/no-param repair-plan and selected/full-module context carriers, the dropped-result callee rewrite carrier, escaped-result direct-call carrier, dropped-result legality beyond the first boundary-based candidate, result-removal blocker, tail-call blocker, repair-plan, callee rewrite, and selected/full-module context carriers, remaining dropped-result result-removal policy, true localization/retry, module-core convergence scheduling beyond the shared local body-rewrite driver, and cleanup scheduling remain scanner-heavy.
  - Open profile work:
    - [x] `[O4Z-AUDIT-DAE-PROFILE-A]` Add DAE profile names, aliases, aggregate membership, config helper, API snapshot, and registry tests. Added `dead-argument-elimination` plus aliases `dae`, `dae-all`, `dae-closeout`, `dead-argument-elimination-all`, `dead-argument-elimination-closeout`, `pass-dae-closeout`; leaf profiles `dae-unused-params`, `dae-effectful-args`, `dae-constant-args`, `dae-unused-returns`, `dae-arg-type-refinement`, `dae-return-type-refinement`, `dae-unseen-call-boundary`, `dae-tail-call-boundary`, `dae-localization`, `dae-convergence`; and `dae-optimizing`/`dae-optimizing-closeout`.
    - [ ] `[O4Z-AUDIT-DAE-PROFILE-B]` Add DAE-specific body templates and feature facts proving each leaf emits the intended direct-call optimization or bailout shape, rather than relying only on a small generic envelope. `dae-unused-params` now has a deterministic three-function private-helper/direct-caller template; `dae-effectful-args` now has a deterministic mutable-global side-effect producer whose result is passed as an unused helper actual; `dae-constant-args` now has a deterministic same-second-constant direct-caller template; `dae-unused-returns` now has a deterministic private single-result helper with all direct callers dropping the helper result (the superseded two-result template is retained only as sampled multi-result tuple/local-spill classification evidence); `dae-tail-call-boundary` now has a deterministic private helper reached only by `return_call`; `dae-unseen-call-boundary` now has a deterministic private helper escaped through an active element `ref.func` and called indirectly through a `funcref` table; `dae-convergence` now has a deterministic wrapper/leaf helper chain where pruning the leaf's unused parameter unlocks wrapper parameter pruning on a later iteration; `dae-localization` now has a deterministic hard effectful actual wrapped in a value-producing block before a private helper call; `dae-arg-type-refinement` now has a deterministic GC base/leaf-subtype module whose owned callers pass narrower leaf values to a broad helper parameter; `dae-return-type-refinement` now has a deterministic GC base/leaf-subtype module whose broad-result helper always returns the narrower leaf value; `dae-optimizing` now has a deterministic five-function helper/caller template where removing an unused helper param leaves identity-add debris for the nested cleanup suffix. All direct leaf templates now exist and focused DAE GenValid tests pass, including red-first generated single-result DAE-F coverage for `dae-unused-returns`; the current selected direct-smoke residual families are now classified through the 1000-case lane, but direct DAE compare evidence remains raw-mismatch-heavy and not closeout.
    - [ ] `[O4Z-AUDIT-DAE-PROFILE-C]` Focused pass/validate tests and scaled dedicated-profile compare evidence now exist, but this is not closeout. Green validation includes `moon test --package jtenner/starshine/validate --file gen_valid_tests.mbt --filter '*dae*'` (`11/11`) from the previous slice, `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*unused-return cleanup exposes dead params*'` (`1/1`, failed red first), full `dae_optimizing_test.mbt` (`284/284` after the three-parameter call_ref/call_indirect select returned-value follow-up; 286/286 after the four-parameter follow-up; 288/288 after the five-parameter follow-up; 290/290 after the six-parameter follow-up; 292/292 after the seven-parameter follow-up; 294/294 after the eight-parameter follow-up; 296/296 after the nine-parameter follow-up; 298/298 after the ten-parameter stack-slice follow-up), focused `*typed-call select*` (`1/1`, failed red first), focused `*call_ref select returned-value*` (`11/11`, failed red for zero-arg, single-parameter, two-parameter, three-parameter, four-parameter, five-parameter, six-parameter, seven-parameter, eight-parameter, nine-parameter, and ten-parameter slices), focused `*call_indirect select returned-value*` (`10/10`, failed red for one-, two-, three-, four-, five-, six-, seven-, eight-, nine-, and ten-argument slices), focused `*branchful typed-select*` (`1/1`), focused `*trapping-load parametric typeidx*` (`3/3`, failed red first), focused `*typeidx*` (`25/25`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*imported tag type references*'` (`1/1`, failed red first), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*tag*'` (`4/4`), red-first `moon test src/passes` failed on the reduced dropped-unreachable cleanup fixture before that fix and later failed on the simple type-reuse whitebox expectation before the signature-reuse fix; after the ten-parameter stack-slice slice, `moon fmt`, focused `*call_ref select returned-value*` (`11/11`), focused `*call_indirect select returned-value*` (`10/10`), focused `*select*` (`82/82`), full `dae_optimizing_test.mbt` (`298/298`), `moon test src/passes` (`4331/4331`), `moon info` with pre-existing warnings, full `moon test` (`7770/7770`), native `src/cmd` build with pre-existing warnings, and `git diff --check` pass, plus earlier focused/full pass validation recorded in the DAE wiki. Direct compare remains raw-red: refreshed 100-case aggregate `.tmp/pass-fuzz-dae-direct-smoke-100-single-return` requested/compared `100/100`, normalized `32`, mismatches `68`; 1000-case aggregate `.tmp/pass-fuzz-dae-direct-smoke-1000-single-return` requested/compared `1000/1000`, normalized `312`, mismatches `688`; closeout-scale dedicated profile `.tmp/pass-fuzz-dae-direct-closeout-10000-single-return` requested/compared `10000/10000`, normalized `3125`, cleanup-normalized `0`, mismatches `6875`, failures `0`, Binaryen cache `10000/0`. At 10000, selected counts are `constant-args=1875`, `unused-params=1875`, `effectful-args=1250`, `unused-returns=1250`, and `localization`/`convergence`/`arg-refinement`/`unseen`/`return-refinement`/`tail-call=625` each; failure dirs remain only `unused-params=1875`, `constant-args=1875`, `effectful-args=1250`, `localization=625`, `convergence=625`, and `tail-call=625`, with original aggregate deltas `-31250` canonical / `-24375` raw bytes; the post-type-reuse mismatch replay improves that to `-31250` canonical / `-27500` raw bytes while preserving the raw-red mismatch count. `dae-unused-returns`, unseen, arg-refinement, and return-refinement have no failure dirs in the refreshed aggregates. `dae-optimizing` dedicated profile is green at 100, 1000, and 10000 (`.tmp/pass-fuzz-dae-optimizing-closeout-10000-single-return` normalized `10000/10000`, zero failures, Binaryen cache `10000/0`), regular GenValid is green at 100000, and explicit wasm-smith `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-post-import-tag-20260705` has zero mismatches / zero Starshine failures (`9956` compared, `9955` normalized, `1` cleanup-normalized, `44` Binaryen/oracle command failures). Paired context smokes now include 10000-case optimizing-profile ordered lanes: direct DAE + `vacuum` remains raw-red at the older 1000-case scale (`312` normalized / `688` mismatches), `dae-optimizing + vacuum` is green at 10000, `dae-optimizing + inlining-optimizing` mismatches all 10000 because Starshine retains a private unreachable helper/type that Binaryen deletes, and adding `remove-unused-module-elements` is green at 10000. Latest random-all partial replay after the unused-return/nop-body rerun fix normalized `6/25` and left `19` triaged non-closeout cleanup mismatches (`coverage-forced-portable=11`, `dae-localization=4`, `dae-effectful-args=4`). The later `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705` retry timed out without `result.json` after `1037` records: `1035` compared, `985` normalized, `0` cleanup-normalized, `50` mismatches in known cleanup/scheduling classes, and `2` Starshine command failures in `coverage-forced-portable`. Red-first tail-call result fixtures passed after conservative syntactic tail-call result-preservation guards; `case-000433` and `case-000434` now replay without command failures, and command-failure replay converts both saved cases into ordinary mismatches. Post-tail random-all smoke `.tmp/pass-fuzz-dae-optimizing-random-all-smoke100-post-tail2-20260705` requested/compared `100/100`, normalized `94`, mismatched `6`, and had zero failures; a longer prefix attempt timed out after `269` compared records with `258` normalized, `11` mismatches, and zero command failures. Completed lanes now include the required full random-all run for `dae-optimizing` and the full raw-red direct random-all run using `--no-reduce-mismatches`; the earlier 2000-case ordinary retry remains partial evidence only because it timed out during reduction. Latest non-profile ordered-integration follow-up: regular GenValid full-suffix `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-regular-genvalid-10000-20260706` requested `10000`, compared `2015` before max-failure cutoff, normalized `0`, mismatched `2015`, had zero validation/generator/property/command failures, selected `binaryen-oracle-portable=2015`, and used Binaryen cache `0/2015`; the no-DAE suffix control `.tmp/pass-fuzz-postpass-context-regular-genvalid-no-dae-control-1000-20260706` also mismatched `1000/1000` with zero failures. Treat this as broader non-profile post-pass/portable-oracle output-shape raw-red, not DAE-owned green evidence or a DAE-owned Starshine-win classification. Next: continue fixing or fully proving direct dedicated/random-all raw mismatch families as measured Starshine wins, keep direct DAE explicitly raw-red until then, extend ordered-neighborhood/scheduling beyond the now-scaled optimizing-profile vacuum/inlining/RUME, direct DAE+RUME residual replay, and full post-pass suffix probes when DAE-owned non-profile integration evidence is required, and cover broader arbitrary returned-value/control/candidate type refinement beyond the focused allocation, static `local.get` result, typed-block operand, zero-param, parametric, and multi-result typeidx block/if/loop operands, typed-select operands including typed-call result operands, nullable `ref.null` select LUB and nullable common-LUB, typed-block result, two-caller argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB, multi-result typed-select operand plus block-condition, pure-condition, call-condition, call-post-op-condition, memory-condition, and branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, zero-param typeidx block body result-refinement, zero-param typeidx terminal `if` returned-value refinement, fallthrough typed/typeidx loop single-result returned-value refinement, zero-param multi-result fallthrough loop returned-value refinement, simple multi-result `if`-arm returned-value refinement, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm returned-value refinement, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands and bounded typed-select operands in multi-result bodies including the non-tail-lane select-first/scalar-second shape, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets; do not declare direct DAE green from the dedicated-profile, broad GenValid, wasm-smith, raw-red random-all lanes, the 2026-07-06 stratified runtime replay, the typed-block focused replays, the direct RUME replay, or the non-profile full-suffix/control raw-red probes.
    - [x] `[O4Z-AUDIT-DAE-TYPE-REFINE]` Focused direct-DAE tests for live GC argument and return refinement were compiled and observed red on 2026-07-05: both kept `(ref null $base)` instead of narrowing to `(ref $leaf)`. Minimal implementation added a narrow owned-direct `struct.new_default` actual/result refinement path, type signature repair via existing `dae_update_defined_func_type`, and `StructNewDefault` stack-effect support. A follow-up red-first exactness expectation then failed while Starshine emitted inexact `(ref $leaf)` where Binaryen emits `(ref (exact $leaf))`; `dae_refined_struct_default_ref_type(...)` now emits exact allocation-derived reference signatures, focused exact tests pass, and the 100-case direct smoke has no arg/return refinement failure dirs. A later red-first typed-block operand fixture broadened argument refinement to non-null typed block-result actuals; it passes, but saved direct random-all mismatch replay stayed `996/996` raw-red. A follow-up red-first typeidx-block operand fixture broadened the same argument-refinement path to zero-param type-indexed block actuals whose function type returns a non-null leaf. A later typeidx if/loop operand slice covers Binaryen-positive zero-param `(if (type $carrier) ...)` and `(loop (type $carrier) ...)` actuals; it passes after DAE accepts candidate types from the value-producing control instruction at the end of an argument slice. A follow-up typed-select operand slice covers a Binaryen-positive owned direct caller where two exact leaf allocations feed `select (result (ref $leaf))`; it failed red while Starshine kept `(ref null $base)`, then passed after DAE computes the operand LUB and rewrites the select result type at direct callsites when tightening the helper parameter to exact non-null leaf. A later nullable typed-select operand slice covers a Binaryen-positive `ref.null` plus exact leaf allocation feeding `select (result (ref null $leaf))`; it failed red while Starshine stayed broad, then passed after DAE treats null select operands as nullable LUB refinements and rewrites the `ref.null` actual to the tightened select type. A follow-up nullable typed-select common-LUB fixture covers two callers whose nullable typed-select actuals use distinct leaves under `$mid`; it failed red while Starshine stayed broad and now passes after nullable actual LUBs preserve nullability. No compare-pass lane was rerun for these focused slices, and direct DAE remains raw-red. A follow-up red-first typed-block body fixture broadened result refinement to a helper body shaped as `(block (result (ref $leaf)) (struct.new_default $leaf))`; it passes after DAE rewrites the wrapper to the exact allocation expression while tightening the result signature, and the saved direct random-all mismatch replay still stayed `996/996` raw-red. A later red-first argument-LUB fixture broadened argument refinement to two owned direct callers that pass distinct leaf allocations with a common non-final supertype; it passes after DAE accumulates the nearest common supertype. Broader arbitrary returned-value/control/candidate LUB refinement remains open under the main DAE audit, not this focused slice; single-result parametric typeidx control actuals, last-lane multi-result typeidx control actuals, branch-free effectful call/call-post-op/memory-load select conditions, non-tail-lane multi-result typed-select returned values, and non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned values are now covered across the relevant argument/returned-value refinement surfaces, while broader non-direct-call parametric control reconstruction and broader branchful/control-local select conditions stay open. The focused static `local.get`, terminal-`if`, typed-select, multi-result typed-select operand plus block-condition, pure-condition, call-condition, call-post-op-condition, and memory-condition typed-select, non-tail-lane multi-result typed-select, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-control explicit-return, zero-param typeidx block body, zero-param typeidx terminal `if`, fallthrough typed/typeidx loop, zero-param multi-result fallthrough loop, simple multi-result `if`-arm returned-value refinement, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm returned-value refinement, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands and bounded typed-select operands in multi-result bodies including the non-tail-lane select-first/scalar-second shape, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets are now covered separately by the latest red-first fixtures.
    - [x] `[O4Z-AUDIT-DAE-TAILCALL]` Focused direct-DAE test for unused-parameter removal through owned direct `return_call` sites was observed red on 2026-07-05: the helper stayed at two params because `ReturnCall` targets were treated as blanket tail-call barriers. Minimal implementation counts direct `ReturnCall` in call facts/callers and routes matching `ReturnCall` through the existing callsite operand slicer, so removed operands are repaired just like `call`. Focused test now passes and after-tailcall compare shows residual sampled tail-leaf drift is only Starshine's smaller omission of Binaryen's unused old-parameter local; dropped-result and indirect/ref tail-call surfaces remain open under the main DAE audit.
    - [x] `[O4Z-AUDIT-DAE-METADATA-TAG]` Focused metadata repair for imported tag type liveness. The first explicit `dae-optimizing` wasm-smith 10000 lane exposed one Starshine command failure on a module whose only live type reference was an imported tag. Red-first `dead-argument-elimination preserves imported tag type references while pruning types` failed with final validation `No type exists for TypeIdx`, then passed after DAE marked imported `TagExternType` indices live and rewrote imported tag type indices during simple function-type pruning. The original wasm-smith case now replays for both direct DAE and `dae-optimizing`, and the post-fix wasm-smith rerun has zero Starshine failures.
    - [x] `[O4Z-AUDIT-DAE-TAIL-RESULT]` Closed the random-all tail-call result validation blocker. The 2026-07-05 random-all retry `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705` found two Starshine `tail call result mismatch` command failures in `coverage-forced-portable`. Red-first `dae-optimizing preserves results on functions that tail-call result callees` failed on a reduced direct-tail dropped-result shape, then passed after DAE treats direct `return_call` uses as undropped result uses and conservatively skips result-removal paths when live tail calls are present. `case-000433` and `case-000434` now replay after a follow-up red-first unreachable-root suffix fixture and conservative syntactic tail-call result guard. The full no-reduction random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` now completes without command failures; remaining cleanup/scheduling mismatches are classified, so this blocker is closed while broader direct-DAE raw-red and ordered-neighborhood audit work remains open.

- [O4Z-AUDIT-INL] - Deep audit `inlining-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit. Direct compare is green with documented cleanup normalizers as of 2026-06-05: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass inlining-optimizing --normalize drop-consts --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-inlining-optimizing-local-cleanup-norm1-10000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` produced 6772 compared, 3376 normalized matches, 3396 cleanup-normalized matches, 0 mismatches, and 20 Binaryen/tool command failures (17 rec-group-zero, 1 bad-section-size, 1 table-index-out-of-range, 1 invalid-tag-index). Agent classification: cleanup-normalized cases are semantic-safe Starshine cleanup of closed dropped constant-expression trees, unused local declarations/local renumbering, and standalone `nop` debris; these are no-effect transforms by the WebAssembly execution model, not evidence inferred from size alone.
  - Scope: direct inlining heuristics, optimizing cleanup scheduler, no-inline policy interaction, helper compaction, tail-call/multivalue surfaces, and local-declaration drift.
  - Deliverables: apply the common checklist; refresh `INL` slot evidence; file partial-inlining/name repair only if new evidence warrants reopening deferred slices.
  - 2026-07-05 performance correction: closeout was reopened because the user requires direct pass-local Starshine time `<=` Binaryen time, not merely `<=2x`. The timing fixture/method is `.tmp/io-perf-20260705/measure_io_perf.py`, generating helper-chain WAT under `.tmp/io-perf-20260705/measurements/` and comparing Starshine `--tracing pass --inlining-optimizing` medians against `BINARYEN_PASS_DEBUG=1 wasm-opt --all-features --inlining-optimizing`. Pre-fix quick evidence showed a nested-cleanup cliff (`20` helpers ~`82ms` Starshine vs ~`0.93ms` Binaryen; `50` helpers timed out after `>20s`). Repairs added linear OI local-write marking, a raw simplify-locals pure i32 accumulator-chain fold, and an early IO touched-function fast path. Current medians meet the 1x bar for `1`, `5`, `10`, `20`, `50`, and `100` helpers: `0.286ms` vs `0.645170ms`, `0.450ms` vs `0.612569ms`, `0.549ms` vs `0.688842ms`, `0.791ms` vs `0.815382ms`, `1.671ms` vs `3.123630ms`, and `3.135ms` vs `12.754000ms`. Keep `[O4Z-AUDIT-INL]` active only for future evidence/release-gate work; reopen performance if those fixture medians regress above `1x`, the exact accumulator fast path overmatches an effectful/non-i32 shape, pass-specific compare finds a semantic mismatch, or a broader artifact exposes a new nested cleanup scaling cliff.
  - 2026-07-05 audit note: `docs/wiki/raw/research/1551-2026-07-05-inlining-optimizing-o4z-audit.md` reviewed docs/source/tests and kept `[INL]005` / `[INL]006` deferred. Evidence tooling has improved: `inlining-optimizing-all` now exists as a focused GenValid aggregate over direct-wrapper, param-spill, return-call, and cleanup-payoff leaves that guarantee inline-then-cleanup opportunities and manifest selected-profile/profile-case labels. Emit-batch smoke generated 8 wasm-tools-valid inputs, compare lanes `.tmp/pass-fuzz-inlining-optimizing-all-focused-20` and `.tmp/pass-fuzz-inlining-optimizing-all-focused-1000` reached `20/20` and `1000/1000` normalized matches, and the required pass-specific lane `.tmp/pass-fuzz-inlining-optimizing-all-focused-10000` reached `10000/10000` normalized matches with zero failures/mismatches. Selected-profile counts in the 10000 lane: direct-wrapper `3019`, param-spill `2993`, return-call `1998`, cleanup-payoff `1990`; profile-case counts matched. A naive aggregate over existing broad inlining/precompute/cleanup profiles remains rejected as a profile-design/tooling failure. Follow-up in the same audit narrowed `pass-inlining` GenValid to scalar direct-call smoke inputs by disabling stringref const-expression and GC-ref surfaces; seed `0x1e11e` now emits 8 wasm-tools-valid `pass-inlining` inputs. The remaining direct seed-`0x1e11e` single-case mismatch is classified as Starshine-win / accepted cleanup drift after runtime equal-trap replay, validation, size evidence, and downstream `-Oz` convergence to identical 59-byte wasm. A 2026-07-05 ordinary 1000-case attempt at `.tmp/pass-fuzz-inlining-optimizing-ordinary-1000-20260705` timed out after `900s` without a summary, so it is not evidence; the follow-up `.tmp/pass-fuzz-inlining-optimizing-ordinary-20-20260705-triage` completed `20/20` with `20` raw mismatches from no-call `binaryen-oracle-portable` cleanup-only inputs, and later `.tmp/pass-fuzz-inlining-optimizing-ordinary-1000-noreduce-allnorm-20260705` completed `1000/1000` with `1000` raw no-call/no-side-effect trap/control cleanup mismatches and zero command/generator/validation/property failures. Agent classification for that sampled ordinary family: Starshine-win / accepted cleanup drift, supported by no-call/no-side-effect facts, validation, case-1 runtime equal-trap replay, sampled size wins, and downstream `-Oz --strip-debug` convergence for cases 1-5. The explicit wasm-smith lane `.tmp/pass-fuzz-inlining-optimizing-wasm-smith-10000` requested `10000` and compared `9956`: `9953` normalized matches, `3` raw mismatches, no generator/validation/property failures, and `44` Binaryen/oracle command failures; the three raw mismatches are classified as Starshine-win / accepted cleanup drift after validation, no-call/no-effect fact review, size/diff inspection, and downstream `-Oz --strip-debug --all-features` convergence to identical 8-byte wasm. The supplemental normalized wasm-smith replay classified two of the three as cleanup-normalized and left only a local-set/get const-return cleanup form raw. Random-all remains open, but triage made forward progress: the first completed no-reduce/all-normalizer 1000-case random-all lane exposed four `coverage-forced-portable` Starshine command aborts in final `reorder-locals` cleanup; `reorder-locals` now fails closed on stale/out-of-range local accesses so inlining's validate-or-fallback path can discard invalid cleanup candidates, and all four saved cases replay with exit `0` and validating output. The refreshed `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-1000-noreduce-allnorm-fixed1-20260705` lane completed `1000/1000` with `7` normalized matches, `105` cleanup-normalized matches, `888` raw mismatches, and zero generator/validation/property/command failures. The remaining raw mismatches are broad other-profile cleanup/representation families, not yet accepted closeout evidence. A follow-up 2026-07-05 representative sample inspected the first raw mismatch from each still-raw selected profile (`coverage-forced-portable`, `ssa-nomerge-parity`, `pass-fuzz-stress`, `binaryen-oracle-portable`, `local-subtyping-straight-line`, `heap2local-struct`, `ssa-nomerge-smoke`, `coalesce-locals-structured`, `coalesce-locals-straight-line`, `local-subtyping-structured`, and `coalesce-locals-loop-copy-through`). All 11 raw/canonical output pairs validate, visible diffs are no-effect cleanup/representation debris, Starshine is smaller in every sampled raw/canonical pair, and shared downstream `wasm-opt --all-features -Oz --strip-debug` converges all 11 pairs to identical wasm. Runtime replay is partial only: `case-000002` and `case-000003` are all-equal, `case-000001` is unsupported by the Node adapter, and `case-000004` timed out before evidence. Agent classification for the sample set is Starshine-win / accepted cleanup drift, but this is sampled evidence and does not close random-all. A later 2026-07-05 normalizer slice added narrow pure-float/signed-conversion dropped-debris and empty-const-if cleanup coverage: `.tmp/pass-fuzz-inlining-optimizing-ordinary-10000-allnorm-purefloat-20260705` completed `10000/10000` with `10000` cleanup-normalized matches, `0` mismatches, and zero failures, while `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-1000-allnorm-purefloat-20260705` improved random-all triage to `7` normalized, `473` cleanup-normalized, `520` raw mismatches, and zero failures; the remaining raw profiles are local-allocation/renumbering families (`ssa-nomerge-*`, `coalesce-locals-*`, `local-subtyping-*`, and part of `heap2local-struct`). This does not close `[O4Z-AUDIT-INL]`: the pass-specific and wasm-smith lanes now have evidence, and current regular GenValid `.tmp/pass-fuzz-inlining-optimizing-ordinary-100000-allnorm-tailguard-20260705` completed `100000/100000` with `100000` cleanup-normalized matches, `0` mismatches, and zero failures. The first full-count random-all attempt found a true tail-call cleanup validation failure (`coverage-forced-portable case-004369`, expected `f32`, found `i64`); Starshine now skips nested/final local cleanup for tail-call modules, and `.tmp/pass-fuzz-inlining-optimizing-random-all-case-004369-validation-replay-tailguard-20260705` replays green. Current random-all `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-10000-allnorm-tailguard-20260705` completed `10000/10000` with `1250` normalized, `3490` cleanup-normalized, `5260` raw local-allocation/renumbering mismatches, and zero failures. Follow-up after the tail guard reran wasm-smith: `.tmp/pass-fuzz-inlining-optimizing-wasm-smith-10000-tailguard-20260705` compared `9956/10000` with `9953` normalized, `3` raw cleanup-drift mismatches, zero Starshine/generator/validation/property failures, and `44` cached Binaryen/oracle command failures; `.tmp/io-wasm-smith-tailguard-downstream-oz-20260705` reconverged the three raw mismatches to identical 8-byte wasm. Random-all sample `.tmp/io-random-all-tailguard-downstream-oz-sample50-20260705` checked the first `50` raw mismatches from each remaining raw profile (`400` total) and all `400` converged under shared `wasm-opt --all-features -Oz --strip-debug`; local-subtyping `case-000229` also replayed under Node as `all-equal`. Exhaustive downstream follow-up `.tmp/io-random-all-tailguard-downstream-oz-all-20260705` checked all `5260` current raw random-all mismatches and all `5260/5260` converged to identical downstream wasm: `ssa-nomerge-parity 1250/1250`, `ssa-nomerge-smoke 1250/1250`, `local-subtyping-straight-line 689/689`, `coalesce-locals-straight-line 545/545`, `heap2local-struct 455/455`, `local-subtyping-structured 366/366`, `coalesce-locals-structured 355/355`, and `coalesce-locals-loop-copy-through 350/350`. Downstream sizes were stable (`35` bytes for `ssa-nomerge-*`, `34` bytes for the other profiles), but `207` local-subtyping cases are raw-size-losing before downstream cleanup; six size-losing local-subtyping runtime replays (`000085`, `000101`, `000205`, `000229`, `000237`, `000253`) were all `all-equal`. Agent classification is accepted cleanup/representation drift, not a blanket size win; reopen on validation failure, downstream-convergence failure, runtime semantic mismatch, unexplained size loss without downstream convergence/measured win, new side effects in the residual family, or a reduced inlining/touched-cleanup transform gap. Use `_build/native/release/build/cmd/cmd.exe` for refreshed native compare lanes.

- [O4Z-AUDIT-SGO] - Deep audit `simplify-globals-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: global value propagation, nested cleanup runtime, late-tail scheduling, closed/open-world boundaries, string/GC/refinalization breadth, and accepted default-local drift.
  - Deliverables: apply the common checklist; refresh direct compare and `SGO` tail evidence; coordinate with `[SGO]003` through `[SGO]005`; keep accepted v0.1.0 signoff separate from new improvement findings.

- [O4Z-AUDIT-SG] - Deep audit `string-gathering`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: string.const gathering, canonical global reuse, data/name/export interactions, late-tail ordering, and string-heavy module code-size impact.
  - Deliverables: apply the common checklist; add missing stringref/global fixtures; refresh direct compare and `SG` tail evidence; document any Binaryen canonical-string reuse gap.

- [O4Z-AUDIT-RG] - Deep audit `reorder-globals`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: dependency-preserving global order, global.get/set remaps, exports/imports/start/elem/data interactions, string global late tail, and names/metadata.
  - Deliverables: apply the common checklist; add missing dependency/remap fixtures; refresh direct compare and `RG` tail evidence; record any order-only representation drift separately from correctness.

- [O4Z-AUDIT-DIR] - Deep audit `directize`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: immutable table facts, holes/traps, type matching/subtyping, select lowering, imported/exported/mutated table negatives, tail calls, and late-tail code-size impact.
  - Deliverables: apply the common checklist; coordinate tests with `[AUDIT004-D]` through `[AUDIT004-F]`; refresh direct compare and final `DIR` tail evidence; document any broader directization opportunities or safety bailouts.

## Deferred Until MoonBit Threading Support

- [HOT]002 - Native Parallel Hot-Batch Queue
  - Status: deferred; MoonBit does not currently support the threading model Starshine needs for a safe native worker queue.
  - Resume when: MoonBit native threading is stable enough to run per-function optimizer workers with deterministic, byte-stable output and explicit runtime gating.
  - Deliverables: add a native-only worker queue over eligible defined functions for the hot batch payload (`ssa-nomerge -> dead-code-elimination -> vacuum -> optimize-instructions -> simplify-locals`); keep final output byte-stable and deterministic; gate behind an explicit native-only option.
  - Dependencies: [HOT]001 replay hardening must stay green.

## v0.1.1 Backlog

### Optimizer Audit Follow-Ups

- [AUDIT]001 - Hot Pass Descriptor Metadata Truthfulness
  - Status: active follow-up from the 2026-05-31 optimizer audit. The former `global-refining` thin-coverage item is retired because the 2026-06-18 direct Binaryen behavior audit closed GR-001..GR-006 and published focused coverage plus 100000-case compare evidence.
  - Goal: make hot pass `requires` metadata describe every analysis a pass may request through `pass_require_*`, so the registry remains a truthful pass-author contract and future scheduling/perf tooling can trust descriptors.
  - Why: the audit found passes that lazily request analyses without listing them in `requires`; tests currently pass because the helper layer builds analyses on demand, but the metadata contract is stale.
  - Suggested tests: `moon test src/passes`, registry/descriptor focused tests, and one small trace/perf smoke proving analysis timers still appear under the expected pass.
  - Exit criteria: every hot descriptor is either mechanically covered by tests or explicitly documented as intentionally lazy; no pass silently depends on undeclared analysis metadata.

- [AUDIT]002 - Pass-Manager Raw Skip And Gate Boundary Coverage
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: protect large-function raw precleaner / skip heuristics from silent coverage holes and performance cliffs.
  - Why: `src/passes/pass_manager.mbt` contains many threshold-shaped gates for `remove-unused-brs`, `simplify-locals`, and related raw adapters. They are performance-motivated, but nearby shapes can miss cleanup or unexpectedly fall back to expensive HOT lowering. The 2026-06-04 `[O4Z-AUDIT-SL]` closeout retired the simplify-locals semantic/direct and generated late-neighborhood audit gate, but intentionally left small structured call-mesh and giant/no-structure raw threshold boundary coverage here under `[AUDIT002-F]` / `[AUDIT002-G]`.
  - Deliverables:
    - [ ] `[AUDIT002-A]` Inventory every `can_skip`, `unchecked`, `giant`, `large`, and threshold-based raw gate in `pass_manager.mbt`, grouping them by owning pass and intended artifact/function family.
    - [ ] `[AUDIT002-B]` Add white-box tests for `remove-unused-brs` large-result `br_table` dispatch ladder gates: exact match, locals/instruction lower-bound miss, block-depth miss, and HOT-only-candidate miss.
    - [ ] `[AUDIT002-C]` Add white-box tests for `remove-unused-brs` large value-if branch ladder gates: locals range, instruction range, call range, return/block range, and drop/select/br_if negatives.
    - [ ] `[AUDIT002-D]` Add white-box tests for `remove-unused-brs` drop-heavy / typed-`br_table` / void-if-return ladder gates, one predicate family per test so boundary failures identify the exact gate.
    - [ ] `[AUDIT002-E]` Add white-box tests for `remove-unused-brs` moderate and unchecked call-mesh gates, including ±1 thresholds around locals, instruction count, if count, and call count.
    - [ ] `[AUDIT002-F]` Add white-box tests for `simplify-locals` small structured call mesh gates: exact match, instruction lower/upper bounds, local-get/call ranges, and exact-tail negative.
    - [ ] `[AUDIT002-G]` Add white-box tests for `simplify-locals` giant validator and no-structure gates, with one fixture per threshold family rather than one mega-fixture.
    - [ ] `[AUDIT002-H]` For each broad skip family touched by `[AUDIT002-B]` through `[AUDIT002-G]`, add one public-pipeline fixture proving the intended cleanup either still runs or intentionally stays skipped with a trace reason.
    - [ ] `[AUDIT002-I]` Add trace strings for currently silent broad skip decisions where a future agent cannot tell whether a function was skipped for safety, performance, or shape mismatch.
    - [ ] `[AUDIT002-J]` Run a focused artifact timing replay for the debug artifact after adding trace/tests; document any gate that exists only to stay within `Starshine <= 2x Binaryen`.
  - Suggested tests: `moon test src/passes`, focused `pass_manager_wbtest`, and a timing-only `self-optimize-compare` replay only after asking if it will be long.
  - Exit criteria: every raw skip/gate family has boundary tests and a documented purpose; no threshold is an unexplained magic number.

- [AUDIT]003 - DAE Selected-Shape De-Artifacting And Genericization
  - Status: active v0.1.1 follow-up; not a v0.1.0 blocker because current DAE direct-pass surfaces are accepted.
  - Goal: convert remaining selected `FuncXXX` artifact lanes into generic, named, semantics-first shape recognizers where practical, while keeping accepted artifact evidence intact.
  - Current finding: notes `1564-2026-07-12-dae-generic-folded-multivalue-suffix-defaults.md`, `1565-2026-07-13-dae-generic-constant-condition-inter-argument-defaults.md`, and `1566-2026-07-13-dae-generic-immutable-global-inter-argument-defaults.md` replace the bespoke Func3737/Func3765 forwarded-default helpers, add guarded direct-literal-condition inter-argument recovery, and genericize the Func323 immutable-global default family. The former selected Func323 8/9-parameter rewrite and Func313-specific caller matcher are removed. Only the already-seven-parameter dropped folded-block residual cleanup remains selected and artifact-local pending a low-definition carrier repro.
  - Why: the audit found many selected-function helpers in `dead_argument_elimination.mbt`. They are covered and historically justified, but they are hard to maintain and weak as a general optimizer surface.
  - Deliverables:
    - [ ] `[AUDIT003-A]` Inventory selected-function helpers and tests into a markdown table with columns: helper, selected def(s), fixture/test, transform family, safety guard, artifact reason, and current generic candidate.
    - [ ] `[AUDIT003-B]` Classify Func237 carrier/loop/local-map helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-C]` Classify Func256/Func298 loop-carrier helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-D]` Classify Func287/Func288 setup/switch-carrier helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-E]` Classify Func299 inverted-if, Func311/Func313 terminal-call-arg, and Func408/Func505/Func521 literal/control helpers, one family at a time.
    - [ ] `[AUDIT003-F]` Pick the smallest safe classified family and add a reduced non-artifact fixture that fails without a generic recognizer and does not depend on absolute debug-artifact function indices.
    - [ ] `[AUDIT003-G]` Implement only that one family behind semantic guards, keep the selected artifact fixture as a regression, and remove only the corresponding selected-index gate if artifact/timing/fuzz evidence stays green.
    - [ ] `[AUDIT003-H]` Repeat `[AUDIT003-F]`/`[AUDIT003-G]` for the next smallest family; every repeat gets its own focused test-first commit-sized slice, not a broad DAE rewrite.
    - [ ] `[AUDIT003-I]` For families that remain selected-only, add comments in source and wiki explaining why they are intentionally artifact-local and what evidence would reopen them.
    - [ ] `[AUDIT003-J]` Refresh direct `--dae-optimizing` compare with DAE normalizers and classify any mismatch families by agent judgment, keeping accepted raw-cleanup drift separate from true semantic mismatches.
  - Suggested tests: focused DAE fixtures first, `moon test src/passes`, direct `bun scripts/pass-fuzz-compare.ts --pass dae-optimizing --normalize drop-consts --normalize unreachable-control-debris --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe`, and artifact timing when selected lanes move.
  - Exit criteria: selected-function lanes are either replaced by generic recognizers or explicitly documented as intentionally artifact-local with current evidence.

- [AUDIT]004 - Thin Module-Pass Shape Coverage Expansion
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: add small, public-pipeline tests for module passes whose current direct coverage is thin relative to their implementation breadth.
  - Why: the all-pass fuzz smoke found no validation failures, but several module passes have only a small number of focused tests and are likely places for missing shapes to hide.
  - Deliverables:
    - [ ] `[AUDIT004-D]` `directize` table visibility negatives: add separate imported-table and exported-table tests proving indirect calls remain indirect.
    - [ ] `[AUDIT004-E]` `directize` element coverage: add passive element, declarative element, active nonconstant offset, active out-of-range hole, and type-mismatch-trap tests.
    - [ ] `[AUDIT004-F]` `directize` multi-table and tail-call coverage: add one multi-table partial optimization fixture, one `return_call_indirect` fixture, and one unsupported `call_ref`/reference-call non-interference fixture if WAT support exists.
    - [ ] `[AUDIT004-G]` `duplicate-import-elimination`: add separate tests for same module/name duplicate function imports, different module/name same-signature duplicates, nonfunction-import negatives, and different-signature negatives.
    - [ ] `[AUDIT004-H]` `duplicate-import-elimination` remapping: add separate call, `ref.func`, table element, export/start, and name/custom-section preservation tests.
    - [ ] `[AUDIT004-I]` `merge-locals`: add separate tests for multi-value functions, GC/ref locals with equal types, GC/ref locals with unequal types, dead-tail local traffic, write-interrupted copy chains, effect-interrupted copy chains, and local-name cleanup.
    - [ ] `[AUDIT004-L]` `once-reduction`: add separate tests for table escape, global escape, `ref.func` escape, export escape, start-function behavior, imported function negative, exported function negative, and multi-use once-positive.
    - [ ] `[AUDIT004-N]` After each pass-family test batch, run the pass-targeted compare smoke with `--count 1000` before moving to the next family; do not batch all behavior changes behind one signoff.
  - Suggested tests: adjacent `*_test.mbt` public-pipeline fixtures, `moon test src/passes`, and pass-targeted compare smoke for each touched pass.
  - Exit criteria: each listed module pass has positive and negative coverage for its most important module shapes, with no new validation failures or unexplained Binaryen mismatches.

- [AUDIT]005 - Standalone No-Inline Pass Contract Tests
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: give `no-inline`, `no-full-inline`, and `no-partial-inline` their own focused module-pass tests instead of relying only on inlining integration tests.
  - Why: `src/passes/no_inline.mbt` has meaningful standalone behavior but no adjacent `no_inline_test.mbt` file.
  - Deliverables:
    - [ ] `[AUDIT005-A]` Add `src/passes/no_inline_test.mbt` with a helper that runs a no-inline module pass and extracts function annotations.
    - [ ] `[AUDIT005-B]` Add wildcard positive coverage for a defined WAT function name.
    - [ ] `[AUDIT005-C]` Add no-match negative coverage proving no annotation section is created.
    - [ ] `[AUDIT005-D]` Add imported function name matching coverage.
    - [ ] `[AUDIT005-E]` Add WAT identifier lowering coverage for defined functions.
    - [ ] `[AUDIT005-F]` Add repeated marker deduplication coverage.
    - [ ] `[AUDIT005-G]` Add metadata-code-inline non-interaction coverage.
    - [ ] `[AUDIT005-H]` Add `no-full-inline` only marker coverage.
    - [ ] `[AUDIT005-I]` Add `no-partial-inline` only marker coverage.
    - [ ] `[AUDIT005-J]` Add combined policy coverage and retain one narrow inlining integration smoke as the end-to-end guard.
  - Suggested tests: `moon test src/passes`, plus a narrow inlining integration smoke after standalone tests are green.
  - Exit criteria: no-inline policy marker semantics are protected independently from the inliner.

- [AUDIT]006 - Function TypeIdx/RecIdx Invariant Documentation
  - Status: active documentation follow-up from user clarification on 2026-05-31.
  - Goal: document clearly that `func_sec` function type references are absolutely invariant as global `TypeIdx` entries, not local recursive `RecIdx` entries; optimizer code may treat `RecIdx` in function-section type-index positions as impossible after valid decode/validation.
  - Why: the audit initially flagged `RecIdx` aborts in locals passes as potential user-input risk. User clarified this shape is a 100% invariant. The issue is documentation clarity, not behavior change.
  - Deliverables:
    - [ ] `[AUDIT006-A]` Add or update a wiki page covering the function-section type-index invariant: module-level function declarations use global `TypeIdx`; `RecIdx` is only meaningful inside a rec-group-local type context.
    - [ ] `[AUDIT006-B]` Cite concrete source anchors in that page: `src/lib/types.mbt`, validation/type-section handling, and at least one optimizer module-pass function-signature cache.
    - [ ] `[AUDIT006-C]` Add inline comment near the `merge-locals` `RecIdx` abort explaining it is an unreachable invariant assertion for function-section type references.
    - [ ] `[AUDIT006-F]` Add a pass/helper test or validation test, if practical, demonstrating that valid module-level function declarations resolve through global `TypeIdx`; if no practical test exists, document why the source/validation references are sufficient.
    - [ ] `[AUDIT006-G]` Update `docs/wiki/ir2/pass-porting-checklist.md` or a related architecture page with guidance: do not cargo-cult broad `RecIdx` support into module-pass function-signature caches unless the source is actually inside a rec-group-local type context.
    - [ ] `[AUDIT006-H]` Refresh `docs/wiki/index.md` and `docs/wiki/log.md` for the new/updated invariant documentation.
  - Suggested tests: docs-only changes should still run `moon test src/passes`; if a validation/helper test is added, run the specific package plus `moon test` when practical.
  - Exit criteria: future agents can see that function-section `RecIdx` handling is an invariant assertion and do not spend time trying to support an impossible module shape.

- [AUDIT]007 - Audit Compare Refresh And Mismatch Classification Hygiene
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: turn the ad hoc audit smoke into durable, reproducible evidence without treating accepted representation drift as a correctness failure.
  - Why: the 50-case all-pass smoke and 1000-case DAE refresh found no validation failures, but the evidence currently lives only in `.tmp/deep-audit-20260531` and this thread.
  - Deliverables:
    - [ ] `[AUDIT007-A]` File a numbered research note summarizing the exact 2026-05-31 all-pass smoke commands and environment, including `--starshine-bin`, `--jobs auto`, seed, and out dirs.
    - [ ] `[AUDIT007-B]` Add the per-pass count table to that note: compared count, normalized matches, compare-normalized matches, validation failures, command failures, and mismatch count.
    - [ ] `[AUDIT007-C]` Add a lightweight docs recipe for running an all-pass smoke compare with `--jobs auto` and a prebuilt `--starshine-bin`, explicitly not part of normal `moon test`.
    - [ ] `[AUDIT007-D]` Add or update script tests only if the docs recipe requires script behavior changes; keep recipe-only changes docs-only.
    - [ ] `[AUDIT007-E]` Refresh direct `--pass inlining` mismatch classification with a small replay set, explicitly separating local-declaration representation drift from semantic mismatch.
    - [ ] `[AUDIT007-G]` Refresh direct `--pass dae-optimizing` classification with documented normalizers and keep generated dropped-constant cleanup drift separate from validation or semantic failures.
    - [ ] `[AUDIT007-H]` Link the research note from the relevant pass wiki pages or the audit/log page so future agents do not rediscover the same smoke results.
  - Suggested tests: no implementation tests unless tooling changes; if tooling/docs recipe changes, run script tests and a small compare replay.
  - Exit criteria: the audit evidence is durable and indexed, with exact commands and agent-classified mismatch families.

### SGO - Follow-Up Improvements

- [SGO]003 - Optional Binaryen `SimplifyGlobals.cpp` Breadth
  - Status: deferred to v0.1.1; not a v0.1.0 blocker.
  - Resume when: a new semantic mismatch, wasm validation failure, targeted artifact/code-size need, or string/GC/refinalization requirement points at SGO breadth.
  - Candidate families: side-effecting-but-safe `read-only-to-write` value-flow positives, broader same-as-init expression matching beyond direct literal / `ref.null` / `ref.func`, broader runtime linear-trace propagation, and additional GC/refinalization-safe replacement surfaces.
  - Deliverables when resumed: add focused shape tests first, rerun direct `--pass simplify-globals-optimizing` oracle fuzz for the new family, and update the SGO wiki pages with the exact accepted subset.

- [SGO]004 - Nested Cleanup Runtime And Exact-Scheduler Experiment
  - Status: deferred to v0.1.1.
  - Resume when: a measured SGO-specific wall-time owner appears, the nested `vacuum` wrapper overhead becomes a concrete runtime target, or an artifact/code-size case demonstrates value from omitted nested default-function slots.
  - Candidate work: reduce nested `vacuum` / lift-lower wrapper cost, add cheaper function-filtered adapters for currently module-shaped cleanup passes, or run a controlled exact-scheduler experiment with artifact and fuzz evidence.
  - Deliverables when resumed: trace before/after nested timers, preserve validation and direct 10k SGO fuzz parity, and document whether the accepted artifact-tuned lane changes.

- [SGO]005 - Default-Local Compare Normalization
  - Status: deferred to v0.1.1 tooling/cosmetic follow-up.
  - Goal: eliminate or normalize the accepted direct SGO artifact diff where Binaryen preserves an explicit default local initialization and Starshine relies on WebAssembly default local zero-initialization.
  - Resume when: exact artifact diffs need to become quieter for release QA or compare-harness work.
  - Deliverables when resumed: either teach the compare helper to ignore explicit default local initialization when semantically equivalent, or add a deliberate Starshine emission/canonicalization option if preserving the explicit set proves more useful.

## v0.2.0 Backlog

### SET - Shared-Everything Threads Proposal Surface

Release stance: proposal-tracking backlog, not a v0.1.0 release gate. Starshine currently has the focused `struct.atomic.get`, `struct.atomic.get_s`, and `struct.atomic.get_u` surface plus conservative optimizer handling. The remaining shared-everything threads surface is intentionally sliced below so future agents can add core/type/binary/WAST/validation/fuzzer/pass support in dependency order instead of treating proposal examples as already-supported syntax.

Common rules for every `[SET-*]` slice:
- Start from `docs/wiki/raw/wasm/2026-06-04-struct-atomic-get-sources.md`, `docs/wiki/wast/gc-aggregate-instruction-authoring.md`, `docs/wiki/wast/atomic-memory-instruction-authoring.md`, and the current shared-everything threads proposal text. Refresh the source snapshot first if the proposal changed.
- Add tests first for the exact surface being widened; do not add parser keywords, binary opcodes, or optimizer rewrites without validator/effect coverage.
- Keep linear-memory atomics (`MemArg` instructions) separate from shared-GC aggregate atomics and proposal shared resource/thread-local features.
- Treat `struct.atomic.get*` as the only already-supported shared-GC aggregate atomic family until a slice below lands.
- Preserve generic optimizer conservatism for any atomic read/write/RMW/cmpxchg/wait operation unless a pass-specific proof documents why rewriting is safe.
- Update `docs/wiki/wast/gc-aggregate-instruction-authoring.md`, `docs/wiki/wast/atomic-memory-instruction-authoring.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, and pass-specific wiki pages when behavior changes.

- [SET-SRC]001 - Refresh shared-everything source manifest and local gap matrix
  - Status: active planning slice; run before implementing any remaining proposal family.
  - Goal: replace the current struct-atomic-get-only source snapshot with a broader proposal tracking manifest and a local implementation matrix.
  - Why: the proposal is moving and the current durable source capture intentionally scoped itself to `struct.atomic.get*`; future implementers need exact opcodes, text spellings, immediates, feature flags, validation rules, and local unsupported surfaces before coding.
  - Deliverables:
    - [ ] Capture the current proposal instruction/type/resource list with date, source URLs, and caveats in `docs/wiki/raw/wasm/` or a numbered research note.
    - [ ] Build a matrix with rows for shared types/resources, thread-local globals, `pause`, `ref.i31_shared`, global atomics, table atomics, struct atomic set/RMW/cmpxchg, array atomic get/set/RMW/cmpxchg, waitqueue operations, aggregate waits, proposal memory waits, and release/acquire ordering.
    - [ ] For each row, record local status across `src/lib`, `src/binary`, `src/wast`, `src/validate`, `src/ir`, `src/passes`, `src/validate/gen_valid`, and docs.
    - [ ] Decide exact local spellings for order operands, including whether compact Binaryen spellings like `seqcst` should remain unsupported or become accepted aliases.
    - [ ] Update the WAST aggregate/atomic pages so they link the new matrix and continue warning that only `struct.atomic.get*` is implemented today.
  - Suggested tests: docs-only unless a source-refresh helper changes; run `bun validate readme-api-sync` if docs references affect README/API sync.
  - Exit criteria: future agents can pick any `[SET-*]` slice without rediscovering proposal scope or conflating shared-GC atomics with linear-memory atomics.

- [SET-TYPE]001 - Sharedness and thread-local resource type model
  - Status: blocked on `[SET-SRC]001` exact rules.
  - Goal: add the type/resource metadata needed by the rest of the proposal before instruction families depend on it.
  - Why: aggregate atomics and shared resource validation require knowing whether heaps, arrays, structs, tables, globals, functions, elements, data, and memories are shared/thread-local where the proposal requires it.
  - Deliverables:
    - [ ] Add focused type/model tests for shared annotations, thread-local globals, and any proposal `bot-share` / shared-heap distinctions selected by `[SET-SRC]001`.
    - [ ] Extend `src/lib/types.mbt` and related constructors only as far as the proposal-backed matrix requires.
    - [ ] Extend binary encode/decode and pretty/show helpers for new type/resource metadata.
    - [ ] Extend WAST parser/printer/lowering for shared/thread-local annotations with round-trip tests.
    - [ ] Extend validation/typecheck with positive and negative sharedness/thread-local tests.
    - [ ] Update arbitrary/module generators only after validation rejects unsupported combinations deterministically.
  - Suggested tests: package-local type/binary/WAST/validate tests, `moon test src/lib`, `moon test src/binary`, `moon test src/wast`, `moon test src/validate`, then `moon test` when the model becomes public.
  - Exit criteria: sharedness/thread-local metadata round-trips and validates independently of the later atomic instruction families.

- [SET-CORE]001 - `pause` and `ref.i31_shared` scalar proposal instructions
  - Status: blocked on `[SET-SRC]001`; independent from aggregate atomic families once exact encodings are confirmed.
  - Goal: implement the smallest non-aggregate proposal instructions as a low-risk proving ground for proposal feature plumbing.
  - Deliverables:
    - [ ] Add core enum/constructor/show/equality/arbitrary coverage for `pause` and `ref.i31_shared` if `[SET-SRC]001` confirms they remain in scope.
    - [ ] Add binary encode/decode tests for exact opcodes and immediates.
    - [ ] Add WAST keyword/parser/printer/lowering tests for text spellings.
    - [ ] Add validation stack/effect tests, including any sharedness restrictions for `ref.i31_shared`.
    - [ ] Lift/lower HOT and model effects conservatively; ensure cleanup passes preserve the instructions unless proved pure/nontrapping by the spec.
  - Suggested tests: `moon test src/lib src/binary src/wast src/validate src/ir src/passes`, then `moon test` after public surface changes.
  - Exit criteria: scalar proposal instructions can parse, encode, validate, lift/lower, print, and survive optimizer passes.

- [SET-GLOBAL]001 - Global atomic get/set/RMW/cmpxchg
  - Status: blocked on `[SET-SRC]001` and any `[SET-TYPE]001` global sharedness model needed.
  - Goal: implement proposal global atomic operations as effectful global/resource operations.
  - Deliverables:
    - [ ] Add instruction enum variants and helpers for `global.atomic.get`, `global.atomic.set`, and `global.atomic.rmw.{add,sub,and,or,xor,xchg,cmpxchg}` or the exact current proposal spellings.
    - [ ] Add binary encode/decode, WAST parse/print/lower, and validation tests for index kind, value type, mutability/sharedness, order operands, stack behavior, and invalid cases.
    - [ ] Model effects as global/resource reads/writes plus trap/order barriers as applicable; HOT lift/lower must not assume `MemArg`.
    - [ ] Update module remapping/liveness passes so global indices and referenced types remain valid after reordering/removal/dedup passes.
    - [ ] Add generic pass guard tests proving optimization passes do not delete, merge, or move global atomic operations unsafely.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass tests, `moon test src/passes`, and a small all-pass fuzz smoke once generation can emit the family.
  - Exit criteria: global atomic operations are first-class, valid, remapped correctly, and optimizer-conservative.

- [SET-TABLE]001 - Table atomic get/set/RMW/cmpxchg
  - Status: blocked on `[SET-SRC]001` and any `[SET-TYPE]001` table sharedness model needed.
  - Goal: implement proposal table atomic operations as effectful table/resource operations.
  - Deliverables:
    - [ ] Add instruction enum variants/helpers for `table.atomic.get`, `table.atomic.set`, and `table.atomic.rmw.{xchg,cmpxchg}` or exact current proposal spellings.
    - [ ] Add binary/WAST/validation tests for table indices, element reference types, stack behavior, sharedness, order operands, and invalid type/index cases.
    - [ ] Extend HOT/effects without reusing linear-memory `MemArg` assumptions.
    - [ ] Update table-index remapping users in directize, duplicate-import/module-element removal, reorder-like paths, and any table-liveness helpers.
    - [ ] Add pass guard tests proving table atomic operations are barriers for CSE, local sinking, DCE, and instruction folding.
  - Suggested tests: `moon test src/lib src/binary src/wast src/validate src/ir src/passes`, then targeted directize/RUME/DIE tests if remapping changes.
  - Exit criteria: table atomic operations preserve table indices/types through all module passes and remain effect barriers.

- [SET-STRUCT]001 - Struct atomic set/RMW/cmpxchg aggregate families
  - Status: blocked on `[SET-SRC]001`; likely depends on `[SET-TYPE]001` shared-struct metadata.
  - Goal: extend the current `StructAtomicGet*` surface to proposal struct atomic writes and read-modify-write operations.
  - Deliverables:
    - [ ] Add core enum variants/helpers for `struct.atomic.set`, `struct.atomic.rmw.*`, and `struct.atomic.rmw.cmpxchg` or exact current proposal spellings.
    - [ ] Add binary encode/decode tests for opcodes, order operands, type index, field index, and value immediates/operands.
    - [ ] Add WAST parse/print/lower tests using canonical Starshine order spellings and explicit compatibility-alias decisions from `[SET-SRC]001`.
    - [ ] Add validator tests for field mutability, packed-field signedness/width, sharedness, stack behavior, null traps, and invalid field/type indices.
    - [ ] Extend HOT lift/lower/effects as aggregate atomic write/RMW/cmpxchg barriers, not linear-memory atomics.
    - [ ] Update duplicate-function/module-element/dead-argument/type-liveness/remap passes for any referenced type indices.
    - [ ] Add generic pass guard tests proving dropped writes/RMW/cmpxchg are preserved and loads/stores are not moved across them.
    - [ ] Revisit GSI/heap2local/global-type-optimization docs after implementation: writes/RMW/cmpxchg keep fields live/mutable and should block immutable-field assumptions unless the proof explicitly excludes them.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass tests, `moon test src/passes`, direct `--pass global-struct-inference` compare after GSI liveness assumptions are touched.
  - Exit criteria: struct aggregate atomic writes/RMW/cmpxchg are first-class and all optimizer passes treat them as effectful field traffic.

- [SET-ARRAY]001 - Array atomic get/set/RMW/cmpxchg aggregate families
  - Status: blocked on `[SET-SRC]001`; likely depends on broader array WAST/core surface and `[SET-TYPE]001` shared-array metadata.
  - Goal: implement proposal array aggregate atomic operations without confusing them with linear-memory atomics.
  - Deliverables:
    - [ ] Audit current array instruction support and add any missing ordinary array core/WAST prerequisites needed for atomic fixtures.
    - [ ] Add core enum variants/helpers for `array.atomic.get*`, `array.atomic.set`, `array.atomic.rmw.*`, and `array.atomic.rmw.cmpxchg` or exact current proposal spellings.
    - [ ] Add binary/WAST/validation tests for array type index, element index operand, packed signedness, order operands, sharedness, null traps, bounds traps, and invalid element types.
    - [ ] Extend HOT/effects as aggregate atomic operations that may trap and synchronize.
    - [ ] Update type-index/remap/liveness passes for referenced array type indices.
    - [ ] Add pass guard tests for optimization passes and any array-specific passes once they exist.
  - Suggested tests: focused array package tests plus `moon test src/passes`; add generator coverage only after validation is stable.
  - Exit criteria: array atomic families are implemented as shared-GC aggregate operations with correct traps, signedness, and optimizer barriers.

- [SET-WAIT]001 - Waitqueue and aggregate wait operations
  - Status: blocked on `[SET-SRC]001`, `[SET-TYPE]001`, and likely core shared resource decisions.
  - Goal: implement proposal waitqueue operations and aggregate waits as a separate synchronization surface from existing linear-memory atomics.
  - Deliverables:
    - [ ] Add core resource/type representation for waitqueues if still present in the proposal.
    - [ ] Add instructions such as `waitqueue.new`, `waitqueue.notify`, `struct.wait`, `array.wait`, `global.wait`, `table.wait`, and proposal `memory.wait32` / `memory.wait64` only after `[SET-SRC]001` confirms exact spelling and behavior.
    - [ ] Add binary/WAST/validation tests for waitqueue indices/resources, waited locations, timeout/value operands, sharedness, trap/blocking semantics, and invalid cases.
    - [ ] Model effects as synchronization/blocking barriers; HOT/pass logic must preserve exact order and never precompute/delete waits or notifies.
    - [ ] Update generators cautiously: deterministic validation fixtures first, randomized emission later with clear feature gates.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass guard tests; avoid runtime execution assumptions unless the harness has deterministic wait behavior.
  - Exit criteria: waitqueue/wait operations validate and round-trip, and optimizers treat them as hard synchronization barriers.

- [SET-MEMORD]001 - Proposal release/acquire orderings for memory atomics and fence
  - Status: blocked on `[SET-SRC]001`; separate from existing stable linear-memory atomic opcode support.
  - Goal: widen existing `MemArg`-based atomic memory instructions and `atomic.fence` only where the proposal adds explicit order operands or altered validation.
  - Deliverables:
    - [ ] Compare current `AtomicOrder` modeling against proposal ordering requirements for linear-memory atomics, aggregate atomics, and fences.
    - [ ] Add binary/WAST/validation tests for any new order immediates, default order behavior, aliases, and invalid encodings.
    - [ ] Keep current linear-memory atomic authoring docs accurate: Starshine core/binary/validator/generator support does not imply WAST text support unless keywords/parser/lowering are added.
    - [ ] Extend HOT/effects and pass guard tests only if order operands change instruction identity or barrier strength.
  - Suggested tests: `moon test src/binary src/wast src/validate src/ir src/passes` plus existing atomic-memory fixture tests.
  - Exit criteria: memory-order semantics are represented consistently across linear-memory and shared-GC atomics without regressing existing atomic tests.

- [SET-GEN]001 - Generator, invalid-fuzzer, and compare harness coverage for shared-everything features
  - Status: blocked until at least one `[SET-CORE]`, `[SET-GLOBAL]`, `[SET-TABLE]`, `[SET-STRUCT]`, `[SET-ARRAY]`, or `[SET-WAIT]` implementation slice lands.
  - Goal: move implemented shared-everything features from hand-written fixtures into systematic valid/invalid generation and pass-compare coverage.
  - Deliverables:
    - [ ] Add GenValid feature-gated generation for each implemented family with a ledger row or update to the existing atomics/GC aggregate rows.
    - [ ] Add invalid-AST strategies for sharedness mismatches, order operand mistakes, index-kind errors, packed signedness mistakes, mutability violations, and waitqueue/resource misuse.
    - [ ] Add binary-invalid tests for malformed encodings that cannot be represented in the AST.
    - [ ] Update compare-pass generation profiles only after Binaryen and Starshine agree on the same proposal surface or the harness can gate proposal features explicitly.
    - [ ] File durable mismatch classifications for any Binaryen proposal-syntax drift or unsupported oracle behavior.
  - Suggested tests: `moon test src/validate src/fuzz src/binary`, targeted `bun fuzz run` smokes, and small `bun scripts/pass-fuzz-compare.ts --count 1000 --jobs auto --starshine-bin ...` lanes once generation is enabled.
  - Exit criteria: implemented proposal features are exercised by valid and invalid fuzzing without destabilizing existing pass parity lanes.

- [SET-PASS]001 - Optimizer-wide shared-everything proposal audit after each new family
  - Status: repeat after each `[SET-*]` instruction-family slice.
  - Goal: prevent new shared-everything instructions from bypassing pass invariants, remapping, liveness, effect barriers, or Binaryen parity docs.
  - Deliverables:
    - [ ] For the newly implemented family, grep every active pass for adjacent ordinary/global/table/struct/array/memory handling and add explicit preserve/remap/barrier coverage where needed.
    - [ ] Update `docs/wiki/raw/research/0708-2026-06-04-struct-atomic-get-pass-opportunity-audit.md` or create a successor numbered audit if the scope outgrows struct atomic gets.
    - [ ] Add public-pipeline guard fixtures for optimization and module-remapping passes touched by the family.
    - [ ] Re-run direct pass compare smokes for any pass whose behavior changes; scale to 10000 only for behavior-changing optimizer rewrites.
    - [ ] Keep GSI/heap2local/global-type-optimization claims conservative unless a family-specific semantic proof supports optimization.
  - Suggested tests: family-specific pass tests, `moon test src/passes`, targeted compare-pass smokes, and docs/wiki log updates.
  - Exit criteria: no newly implemented shared-everything instruction is treated as pure/deletable/remappable-by-accident, and all durable pass decisions are documented.

### INL - Deferred Inliner Breadth

- [INL]005 - Partial Inlining Splitter
  - Status: deferred to v0.2.0; not a v0.1.0 blocker.
  - Resume only if a reduced Pattern A/B fixture demonstrates one of: semantic or validation correctness, a clear pass-local performance win, a downstream size/optimization win that offsets code growth, or a user-facing need for `no-partial-inline` splitter behavior.
  - Deliverables when resumed: implement or explicitly reject/scope out Pattern A and Pattern B splitting, add reduced splitter and helper-cleanup fixtures, define `no-partial-inline` behavior against the splitter, and compare direct `--pass inlining` / `--pass inlining-optimizing` on split-family repros.

- [INL]006 - Residual Name/Annotation Repair
  - Status: deferred to v0.2.0.
  - Resume only with a concrete user-facing debug-name requirement, annotation-collision requirement, semantic mismatch, wasm validation failure, or proven pass-local performance/code-size issue.
  - Deliverables when resumed: add focused repair tests first, then either implement deterministic Binaryen-like local/label name reconstruction and broader annotation collision repair or explicitly document the unsupported surface as rejected.

### Tooling And Runtime Follow-Up

- [TOOL]001 - Self-Optimize Compare Normalization Symmetry
  - Goal: make the exact artifact helper stop reporting harmless raw/debug-only outer-block drift as a pass blocker.
  - Deliverables: either canonicalize Binaryen through the same strip-debug path before canonical-function comparison while preserving `binaryen.raw.wasm`, or teach the canonical-function fallback to ignore transparent unused-label void block wrappers.
  - Acceptance: the exact-slot artifact command no longer reports `defined=200 abs=217` solely because Binaryen raw `--debug` kept an outer block that symmetric normalization removes.

- [HOT]003 - Node-Package Worker Queue Port
  - Status: deferred behind [HOT]002 and future Node package rebuild work.
  - Deliverables: reuse the native hot-batch queue contract in the shipped Node package with `worker_threads`, one WasmGC module per worker, worker-local heap state, and stable-order serialized merge.
  - Dependencies: [HOT]002 native queue contract, worker-local function serialization hooks, and future Node package rebuild work.

### FUZ - Fuzzer Hardening and GenValid Widening

#### Tiny FUZ work-slice board

Use this board as the tracking view for fuzzer work. Each slice should be small enough for one focused TDD loop: add/adjust focused tests, implement one narrow behavior, update docs/wiki counters, and run the targeted fuzz smoke plus `moon test src/fuzz` or the relevant package test. Prefer finishing p1 GenValid/metamorphic slices before p2 infrastructure unless a bug or release need says otherwise.

p1 next-up / active:
- None currently tracked. Prefer adding a new tiny GenValid/metamorphic slice before broad p2 infrastructure work if a release need appears.

p2 invalid/binary/text tiny slices:
- Census note: `[FUZ]1020D1` audited the checked-in invalid-AST registry and wiki history. The registry currently has 265 deterministic AST-invalid strategy specs across all 15 `ValidationIssueFamily` buckets after `[FUZ]1020F3` added packed-field plain-accessor coverage. Already-covered breadth includes the closed `[FUZ]1020A`/`B`/`C`/`D3`/`D4`/`D5`/`E1`/`E2`/`E3`/`E4`/`E5`/`F1`/`F2`/`F3` families: start out-of-range/wrong-kind plus imported-start signature variants, cross-kind duplicate export names, datacount absent/too-small/too-large plus mixed active/passive cardinality mismatch, element function-index/typed-expression/table-index basics plus active element/table ref-type mismatch, table/data/memory bulk operand/index families, atomics non-shared, natural-alignment, and stack/index families, SIMD lane/operand/index families, GC aggregate/input/variance/descriptor basics plus final-supertype policy, descriptor `describes` previous-definition boundary, and packed-field accessor-shape failures, ref/branch payloads including `br_on_null`, `br_on_non_null`, `br_on_cast`, multi-value `br_table` arity, `call_indirect` table element compatibility, `return_call_indirect` table element compatibility, ordinary `try_table` catch payload mismatch, and `try_table` `catch_ref` payload-plus-exnref mismatch, name-map out-of-range indices, and code-section cardinality/imported-body families. Name-section indirect-map ordering/count invalidity was classified by `[FUZ]1020G1` as binary/codec-invalid rather than AST-invalid: `NameMap` and `IndirectNameMap` arrays can be constructed in memory, but `src/binary/encode.mbt` rejects non-increasing indices with `InvalidNameMapOrder`, `src/binary/decode.mbt` rejects malformed ordered/count payloads before validation, and existing binary invalid tests cover the decode/validate split. Do not add an AST-invalid strategy for ordering/count unless the in-memory model gains a representation that bypasses codec ordering invariants and reaches `validate_name_sec(...)`.

p2 oracle/reporting/infrastructure tiny slices:
- None currently tracked. Add a new tiny slice before broad oracle/reporting/infrastructure work.

## OI-L follow-up blocker (2026-07-03)

Goal: finish OI-L after partial local-receiver implementation.

Current state:
- Aggregate atomic RMW/cmpxchg binaries decode/encode/validate and native probe smoke passes.
- `optimize-instructions` rewrites local-receiver unshared struct RMW/cmpxchg and identity cmpxchg/RMW in the focused OI-L test.
- Array aggregate atomics are intentionally unchanged.

Remaining blocker:
- Global-receiver/native Binaryen-shaped struct probes still expose a HOT lift/lower result-root sentinel issue: mutating them can lower to `unreachable`. The pass is fail-closed to unchanged unless the struct receiver is `local.get`.

Exit criteria:
- Fix HOT lift/root result handling for aggregate atomic result bodies, or add an equivalently safe raw rewrite.
- Widen the receiver gate to at least `global.get` after red-first coverage.
- Confirm struct probes rewrite in native WAT, array probes preserve array atomics, all outputs validate, then update the OI-L matrix row from partial/blocker to the final classification.
