# Agent Tasks

## Scope
- Keep only active unreleased work or explicitly deferred future work.
- Group work by release target.
- Use explicit slice ids so future agents can execute in dependency order.
- Move completed work and historical evidence to the relevant docs/wiki page, release notes, or git history.

## Current Parity Focus
- Keep the Binaryen no-DWARF default optimize path as the v0.1.0 parity target.
- Canonical order and nested-shape notes live in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md` and `docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`.
- Prefer direct-pass Binaryen behavior parity first, then ordered-neighborhood replay, then preset scheduling.
- Behavior parity is not byte-for-byte wasm/text parity: exact helper shape, local numbering, and raw canonical output may differ when semantic evidence proves the same observable behavior. It is also not satisfied by a green fuzz lane if source/docs still list broad unimplemented Binaryen transform families.
- Do not widen public `optimize` / `shrink` slots until the surrounding Binaryen neighborhood is representable and oracle-proven.

## v0.1.0 Active Slices

### O4z debug startup recovery

- [O4Z-STARTUP]001 - Reduced Startup Map Init Trap
  - Status: smoke-green on 2026-06-02; keep only follow-up hardening here until a full-suite signoff or smaller artifact-generation fixture replaces the integration-shaped guard.
  - Goal: keep the repaired debug-WASI artifact/reduced fixture and conservative pass guards from regressing while broader self-optimized spec coverage is scheduled.
  - Why: the original stale artifact trap was repaired by regenerating the committed debug-WASI artifact and reduced fixture from the current correct `$moonbit.malloc` shape. Subsequent full self/debug `-O4z` prefix probes exposed optimizer owners that are now guarded conservatively: `ssa-nomerge` nested-control liveness, `optimize-instructions` commutative canonicalization, `simplify-locals-nostructure` local.tee/load sinking, `coalesce-locals` loop/structured-local.tee coalescing, and `vacuum` branchy structured write cleanup.
  - Durable evidence: `docs/wiki/raw/research/0693-2026-06-01-o4z-debug-startup-func3750.md`.
  - Remaining follow-ups:
    - [ ] Keep `tests/repros/o4z-debug-startup-map-init-repro.wasm` as the focused repro until a smaller source/build-artifact fixture replaces it.
    - [ ] Recover optimization precision one pass at a time only with focused tests and semantic evidence: nested SSA liveness, safe commutative operand ordering, local.tee-aware simplify-locals sinking, path-sensitive coalesce-locals, and branchy structured vacuum cleanup.
  - Suggested tests: `moon fmt`, `moon test src/passes`, `moon build --target native --release src/cmd`, `bun test scripts/lib/o4z-debug-startup-map.test.ts`, `wasm-tools validate --features all tests/repros/o4z-debug-startup-map-init-repro.wasm`, `wasm-tools validate --features all .tmp/o4z-bench/starshine-o4z-candidate.wasm`, and the self-optimized spec smoke commands listed in the research note.

### json-as debug artifact triage

- [JSON-AS]001 - `remove-unused-brs` corrupts AssemblyScript incremental-GC loop exits
  - Status: fixed for the 2026-06-05/2026-06-06 runtime and direct-compare evidence; keep only artifact/preset follow-ups elsewhere.
  - Goal: make `remove-unused-brs` preserve normal loop fallthrough exits when stripping tail branches/returns from nested regions.
  - Why: a fresh git clone of `JairusSW/json-as` at `f707d68d5ce5136ecfd0c576140421286c9e93a8`, built with AssemblyScript `0.28.17`, produced a valid Starshine output that traps at runtime. Prefix bisection of `medium.bench.incremental.naive.debug.wasm` found the first failing optimize prefix at slot 8: `memory-packing -> once-reduction -> global-refining -> global-struct-inference -> ssa-nomerge -> dead-code-elimination -> remove-unused-names -> remove-unused-brs`. Prefix 7 ran successfully; prefix 8 trapped in `~lib/rt/itcms/step` / `~lib/rt/itcms/interrupt` / `~lib/rt/itcms/__new` during benchmark startup.
  - Finding: in `~lib/rt/itcms/step`, `remove-unused-brs` rewrote a loop whose `obj == toSpace` arm normally falls out of the loop into a shape where that fallthrough path reaches `unreachable`. This is a true semantic mismatch, not representation drift or a validation issue. 2026-06-05 follow-up added reduced guards for this loop-fallthrough rotation and for HOT stack/local-set writeback hazards found in the same prefix: stack-style tail returns/branches and result/root regions mixing `local.set` with calls or memory copies/fills can be semantically re-associated by HOT lowering after any RUB mutation, so RUB now conservatively skips those shapes. A later follow-up widened the RUB stack/local-set guard to void root regions after `dumpToFile` showed the same HOT lowering reassociation corrupting the file-name string; medium-naive prefixes 8, 9, and 11 now validate and complete the Node runtime smoke.
  - Deliverables:
    - [x] Add a focused regression fixture for the `itcms.step` loop shape: conditional loop body where one arm updates state and branches back, while the other arm falls through normally.
    - [x] Guard `remove_unused_brs_try_strip_tail_if_exits` / `remove_unused_brs_strip_tail_control` so branch-to-loop-tail rewrites do not turn legitimate loop exits into `unreachable`.
    - [x] Replay the `json-as` medium-naive prefix 8 artifact and require it to run under the Node benchmark runner before re-enabling this shape. 2026-06-05 latest replay validates and completes both serialize and deserialize under Node; prefix 9 (`+ remove-unused-names`) and prefix 11 (`+ vacuum + remove-unused-brs`) also completed after the vacuum guard in `[JSON-AS]002`.
    - [x] Refresh direct `--remove-unused-brs` compare evidence and classify any remaining mismatches separately from this artifact trap. 2026-06-06 command `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass remove-unused-brs --normalize drop-consts --normalize unreachable-control-debris --out-dir .tmp/pass-fuzz-remove-unused-brs-branch-heavy-fix2-10000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` compared `6768/10000`: `3847` normalized matches, `2921` cleanup-normalized matches, `0` mismatches, `20` Binaryen/tool command failures. Agent classification: residual cleanup-normalized branch/unreachable wrappers are semantic-safe/size-winning for Starshine, not the json-as loop-exit corruption family.
  - Suggested tests: focused `src/passes/remove_unused_brs*_test.mbt`, `moon test src/passes`, `moon build --target native --release src/cmd`, direct `bun scripts/pass-fuzz-compare.ts --pass remove-unused-brs --count 1000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe`, and a cloned `json-as` replay of the prefix listed above.

- [JSON-AS]002 - `vacuum` after repeated `remove-unused-names` corrupts json-as debug runtime state
  - Status: active correctness blocker found after removing all `remove-unused-brs` slots from the same 2026-06-05 `json-as` preset replay.
  - Goal: identify whether `vacuum`, `remove-unused-names`, or HOT lowering/splice cleanup corrupts stack/object state when `vacuum` cleans the post-`remove-unused-names` artifact.
  - Why: with all `remove-unused-brs` slots removed, `medium.bench.incremental.naive.debug.wasm` still failed. The first failing prefix was `memory-packing -> once-reduction -> global-refining -> global-struct-inference -> ssa-nomerge -> dead-code-elimination -> remove-unused-names -> remove-unused-names -> vacuum`. Prefix 8 ran repeatedly; prefix 9 serialized once and then failed during GC with `abort: Index out of range in ~lib/rt.ts:21`, followed by an `unreachable` in `~lib/rt/__typeinfo`, reached from `~lib/rt/itcms/Object#get:isPointerfree`, `Object#makeGray`, `__visit`, and `Array<RecentActivity>#__visit`.
  - Finding: the printed prefix-8-to-prefix-9 diff mostly shows removal of `const; drop` debris, but several array constructor / setter / assertion functions also show stack/local traffic moving around stores. The module validates, so the actionable issue is a runtime semantic corruption in cleanup or lowering, not malformed wasm. 2026-06-05 follow-up found the vacuum mutation itself was small, but HOT lowering after vacuum reassociated root stack values across `local.set` / call sequences in array constructors and setters; vacuum now conservatively skips root regions that mix root `local.set` with stack-effecting calls or memory copies/fills. The no-RUB prefix 9 and normal prefix 10 now validate and complete the medium-naive Node smoke.
  - Deliverables:
    - [x] Minimize the no-`remove-unused-brs` prefix-9 failure into a smaller WAT or wasm repro that still reaches the `~lib/rt/__typeinfo` abort. The committed regression is a focused root stack/local-set call-hazard skip; the artifact diff family was array constructor / setter stack reassociation after trivial cleanup.
    - [x] Audit `vacuum` region splicing and dead-root deletion around stack-producing locals and stores; confirm whether the suspicious array constructor / `#__set` reorderings are semantic changes or printer/lowering artifacts. The suspicious `local.set` movement was a HOT lowering semantic change triggered by vacuum mutation, not a validation issue.
    - [ ] Add a focused regression before changing behavior; prefer a fixture that exercises `const; drop` cleanup adjacent to array allocation, write barriers, and GC visits. 2026-06-05 direct `vacuum` refresh attempts (`--count 1000` then `--count 100`, seed `0x5eed`, out dirs `.tmp/pass-fuzz-vacuum-refresh-1000` and `.tmp/pass-fuzz-vacuum-refresh-100`) timed out before summary emission; the shorter run wrote `16` mismatch dirs before a 300s timeout. Sampled case `case-000002-gen-valid` is agent-classified as semantic-safe but size/representation-losing constant-if cleanup (`if (i32.const 1/0)` preserved by Starshine vs Binaryen's simplified block/empty form), not the json-as stack/local-set call-hazard family. The timeout itself remains a harness/pass-performance follow-up.
    - [x] Replay the no-`remove-unused-brs` prefix after the fix and require the medium-naive benchmark to complete. 2026-06-05 replay command validated and completed serialize plus deserialize under Node.
  - Suggested tests: focused `src/passes/vacuum*_test.mbt` or pass-manager prefix fixture, `moon test src/passes`, `moon build --target native --release src/cmd`, and cloned `json-as` prefix replay without `remove-unused-brs` slots.

- [JSON-AS]005 - `simplify-locals-nostructure` corrupts json-as medium-naive after prefix 16
  - Status: fixed for the 2026-06-05 medium-naive prefix-17 replay and covered by the later full O4 smokes.
  - Goal: make the post-`tuple-optimization` `simplify-locals-nostructure` slot preserve string/object state before resuming full O4 smoke.
  - Why: after RUB prefix 8/11 and vacuum prefix 10 were smoke-green, the first failing medium-naive prefix became slot 17: `... -> code-pushing -> tuple-optimization -> simplify-locals-nostructure`. Prefix 16 completed under Node; prefix 17 validated but trapped before benchmark output with `RuntimeError: memory access out of bounds` in `~lib/rt/common/OBJECT#get:rtSize`, called from `~lib/string/String.UTF8.byteLength`, `assembly/__benches__/lib/bench/utf8ByteLength`, and `start:assembly/__benches__/medium.bench`.
  - Finding: the first HOT guard left raw/lowered `simplify-locals-nostructure` cleanup active. That cleanup removed stack-root `local.tee` / local traffic in functions that also contained calls or memory-copy/fill hazards, including the huge benchmark start function. A pass-manager raw/lowered no-structure guard now skips those stack-effect/local-write cleanup families; `prefix-17-fixed1.wasm` validates and completes the Node serialize+deserialize smoke.
  - Deliverables:
    - [x] Minimize the prefix-17 failure to raw/lowered no-structure cleanup preserving stack-root call hazards.
    - [x] Add a focused regression before changing behavior (`pass_manager_wbtest.mbt` lowered cleanup stack-root call hazard).
    - [x] Replay medium-naive prefix 17 under Node after the fix. 2026-06-05 replay completed serialize and deserialize.
    - [x] Resume full O4 smoke for medium-naive, medium-simd, and large-swar after the next `simplify-locals` prefix blocker is fixed. 2026-06-05 rebuilt standard `*.starshine-o4.wasm` artifacts with `--traps-never-happen --closed-world --optimize -O4`; all three validated and completed Node serialize/deserialize smoke.
  - Suggested tests: focused pass-manager/simplify-locals tests, `moon test src/passes`, `moon build --target native --release src/cmd`, and cloned `json-as` prefix/full O4 runtime smokes.

- [JSON-AS]006 - `simplify-locals` corrupts json-as medium-naive after the late local cleanup prefix
  - Status: fixed for the no-HSO prefix-26 replay on 2026-06-05 and covered by the later full O4 smokes.
  - Goal: make the later full `simplify-locals` slot preserve string/object state after the preceding late-local cleanup cluster so full O4 runtime smoke can proceed.
  - Why: after prefix 17 was fixed, medium-naive prefixes through prefix 25 completed under Node. The next failing no-HSO ordered prefix was slot 26, adding full `simplify-locals` after the preceding local-cleanup passes. The first validating output trapped with `RuntimeError: memory access out of bounds` in `~lib/rt/common/OBJECT#get:rtSize`, called from `assembly/index/JSON.__serialize<~lib/string/String>`, `UserPreferences#__SERIALIZE`, `serializeStruct<UserPreferences>`, and the benchmark start function. After the global-delta guard, prefix 26 reached benchmark execution and then trapped during allocation in `~lib/rt/itcms/Object#get:nextWithColor` from `~lib/rt/itcms/__new`.
  - Finding: `simplify-locals` was moving global-derived delta reads across realloc-style calls; a focused regression now keeps `global.get offset` before the call and the pass guards large raw `simplify-locals` functions that mix local writes, stack effects, and global state. A second allocator subfamily came from large local-tee/memory-write functions such as TLSF `removeBlock`: dropping the stack-root tee before a later local read left allocator bitmap state stale. Large `simplify-locals` functions that combine local tees with memory writes now skip the hazardous rewrite family; `prefix-26-fixed4.wasm` validates and completes medium-naive serialize plus deserialize under Node.
  - Deliverables:
    - [x] Diff the initial prefix-25/26 artifacts for stack functions `100`, `105`, `218`, `219`, `220`, and `264`, and isolate the global-delta/realloc ordering issue.
    - [x] Add a focused regression before changing `simplify-locals` behavior (`simplify-locals keeps global delta reads before realloc calls`).
    - [x] Diff the remaining prefix-25/fixed-prefix-26 allocator changes, starting with TLSF/RT functions `12`, `13`, `14`, `19`, `20`, `21`, `22`, `92`, `96`, and `97`.
    - [x] Replay prefix 26 under Node after the allocator subfamily fix, then continue ordered-prefix bisection. 2026-06-05 replay of `prefix-26-fixed4.wasm` completed serialize and deserialize.
  - Suggested tests: focused `src/passes/simplify_locals*_test.mbt` or pass-manager whitebox coverage, `moon test src/passes`, `moon build --target native --release src/cmd`, and cloned `json-as` prefix replay under Node.

- [JSON-AS]007 - `merge-blocks` corrupts json-as medium-naive after prefix 26
  - Status: fixed for the no-HSO prefix-27 replay and full medium/large O4 smokes on 2026-06-05.
  - Goal: make the next no-HSO ordered prefix, `... -> simplify-locals -> merge-blocks`, preserve string serialization state before continuing O4 bisection.
  - Why: `prefix-26-fixed4.wasm` validates and completes the Node benchmark, but adding the next no-HSO pass (`merge-blocks`) produces a validating module that fails the medium benchmark assertion. Serialized strings gain an extra leading quote (for example `"jairus` instead of `jairus`) and then trap at `Expectation#toBe<~lib/string/String>`.
  - Finding: prefix-26/fixed-prefix-27 diffs showed `merge-blocks` flattening the block wrapper around the string serializer loop in `JSON.__serialize<~lib/string/String>` plus similar buffer growth / `heap.realloc` wrappers in serializer helpers. Removing that loop wrapper changed the lowered stack/control shape enough to copy the opening quote into string payload output. `merge-blocks` now conservatively preserves block roots whose body contains a loop; `prefix-27-fixed1.wasm` and all later no-HSO prefixes through directize validate and complete Node smoke.
  - Deliverables:
    - [x] Diff and reduce the prefix-26/fixed-prefix-27 `merge-blocks` changes, starting with functions `101`, `214`, `219`, `223`, and `226`.
    - [x] Add a focused `merge-blocks` regression before changing behavior (`merge-blocks preserves loop wrapper blocks with continue targets`).
    - [x] Replay no-HSO prefix 27 under Node after the fix, then continue ordered-prefix bisection. Prefixes 27 through 37 completed under Node.
  - Suggested tests: focused `src/passes/merge_blocks*_test.mbt`, `moon test src/passes`, `moon build --target native --release src/cmd`, and cloned `json-as` prefix replay under Node.

- [JSON-AS]003 - Optimize preset misses Binaryen O4 function/type cleanup on json-as debug artifacts
  - Status: active optimization-parity gap; do not widen the preset until `[JSON-AS]001` and `[JSON-AS]002` are fixed or safely gated.
  - Goal: close the largest size gaps against standard Binaryen `wasm-opt -O4` for `json-as` debug artifacts after correctness is restored.
  - Why: on `medium.bench.incremental.naive.debug.wasm`, Starshine reduced data and globals but left many functions/types alive. Size and section evidence from the 2026-06-05 replay: debug `222187` bytes / `276` functions / `29` types / `128` globals; fixed Starshine `--optimize -O4` `168807` bytes / `276` functions / `29` types / `38` globals; Binaryen O4 `110359` bytes / `85` functions / `17` types / `36` globals. Starshine matched Binaryen's data-section payload (`25312` bytes) but missed most function/type cleanup; the remaining size includes both live code and separate `StripDebug` custom-section bytes tracked by `[JSON-AS]008`.
  - Finding: 2026-06-05 investigation under `.tmp/json-as/build/starshine-vs-binaryen/investigation-o4-live/` shows `remove-unused-module-elements` is not the primary blocker: rerunning it after the fixed Starshine O4 output leaves medium-naive at `276` functions, and the visible roots are only five function exports plus a two-function active element segment and no start section. `duplicate-function-elimination` is a safe missing preset contributor (`optimize + duplicate-function-elimination + remove-unused-module-elements` passed Node smoke and originally reduced functions to `221` / `224` / `263` for medium-naive / medium-simd / large-swar) but does not reduce types. A 2026-06-05 DFE fixed-point widening, guarded by `duplicate-function-elimination reaches fixed point when callee dedup unlocks more duplicates`, improves that same lane to `211` / `214` / `261` functions with Node smoke and validation, reaching a modest additional code-section win while preserving the larger remaining Binaryen gap; direct `duplicate-function-elimination` 10000-case compare on 2026-06-06 is normalized-green (`6768` compared, `6768` normalized, `0` mismatches, `20` Binaryen/tool command failures). Plain `inlining + remove-unused-module-elements` removes most functions and passed Node smoke, but is size-losing on these artifacts. `inlining-optimizing` and `inlining-optimizing + dae-optimizing + duplicate-function-elimination + remove-unused-module-elements` originally got close to Binaryen function/type counts (`85` / `86` / `80` functions and `19` types in the three-artifact run) but validate-only outputs trapped under the Node runner with memory-access OOB. A 2026-06-05 correctness guard now skips the optimizing nested-cleanup suffix and final local cleanup on large modules (`>80` defined functions), so direct `inlining-optimizing --remove-unused-module-elements` and `--optimize --inlining-optimizing --remove-unused-module-elements` validate and complete Node smoke on medium-naive, medium-simd, and large-swar. This is a conservative correctness recovery, not a full inlining-derived size signoff: medium-naive / medium-simd / large-swar `opt+inlining-optimizing+rume` sizes are `174958` / `177455` / `396110`, with functions `93` / `94` / `85`, and the direct 1000-case compare still stops at the mismatch cap (`52` compared, `26` normalized matches, `26` mismatches, `1` Binaryen rec-group-zero command failure). Sampled mismatches are agent-classified as semantic-safe/size-winning Starshine cleanup of generated `const; drop` and empty-else/nop debris that Binaryen leaves, but they need a focused normalization/classification follow-up before closing `[O4Z-AUDIT-INL]`. The direct `dae-optimizing` medium-naive invalid-local validation failure was fixed on 2026-06-05 by making the selected Func237 raw-debris cleanup use the current function signature when trimming locals; direct `dae-optimizing` now validates and passes Node smoke on medium-naive, medium-simd, and large-swar, with 10000-case compare normalized-green (`6766` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). Do not schedule inlining-derived lanes for size until their direct compare mismatches and skipped large-module cleanup owners are separately closed.
  - Follow-up evidence: 2026-06-05 post-DFE probes found two unscheduled direct hot passes with small but safe-looking json-as size wins; the refreshed 2026-06-06 analysis, commands, section table, and scheduling recommendation are preserved in `docs/wiki/raw/research/0713-2026-06-06-jsonas-dfe-cf-rse-preset-analysis.md`. Summary: `--code-folding --redundant-set-elimination --remove-unused-module-elements` after `optimize + duplicate-function-elimination + remove-unused-module-elements` validated and passed Node smoke on all three artifacts, reducing medium-naive / medium-simd / large-swar sizes to `152271` / `157841` / `357830` and code payloads to `126181` / `132972` / `315404`; 1000-case compare probes for each pass were normalized-green (`998` compared, `0` mismatches, `2` Binaryen rec-group command failures); direct `code-folding` 10000-case compare on 2026-06-06 is also normalized-green (`6761` compared, `6761` normalized, `0` mismatches, `20` Binaryen/tool command failures), and direct `redundant-set-elimination` 10000-case compare is normalized-green after the 2026-06-06 loop-entry default reset fix (`6771` compared, `6771` normalized, `0` mismatches, `20` Binaryen/tool command failures). The fix keeps raw RSE from treating default writes inside loops as redundant when the loop-entry value may differ; it repaired the restored `medium.bench.incremental.simd.wasm` `opt+dfe+rume+rse+rume` and `opt+dfe+rume+cf+rse+rume` Node smoke. A follow-up restored-example lane after the fix validated and Node-smoked `opt+dfe+rume+cf+rse+rume` outputs for `medium.bench.incremental.naive.wasm`, `medium.bench.incremental.simd.wasm`, and `large.lazy.bench.incremental.swar.wasm`; those checked-in example inputs are already optimized, so this evidence is runtime-correctness only, not size-win evidence. `merge-locals` originally validated but trapped under Node on medium-naive in this same suffix position; a 2026-06-05 fix now clears raw local-copy aliases across structured control so copies before loops/blocks cannot retarget later source reads after nested destination writes. The `opt+dfe+rume+merge-locals+rume` lane now validates and completes Node smoke on medium-naive / medium-simd / large-swar (`152693` / `158266` / `358874` bytes; `211` / `214` / `261` functions), and direct `merge-locals` 10000-case compare is normalized-green (`6769` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). `simplify-locals-notee-nostructure` originally validated but trapped under Node on medium-naive in the same suffix position; a 2026-06-05 large-module guard (`>100` defined functions) now skips that direct pass on artifact-sized modules until its precise large-function hazard is reduced. The `opt+dfe+rume+simplify-locals-notee-nostructure+rume` lane now validates and completes Node smoke on medium-naive / medium-simd / large-swar (`152693` / `158266` / `358874` bytes; `211` / `214` / `261` functions), and direct `simplify-locals-notee-nostructure` 10000-case compare is normalized-green (`6764` compared, `0` mismatches, `20` Binaryen/tool command failures: rec-group-zero/bad-section-size/table-index/tag-index). Binaryen no-pass roundtripping of the Starshine `opt+dfe+rume+cf+rse` medium-naive output drops size `152271 -> 150331` and code `126181 -> 124241` without changing function/type counts, indicating a remaining encoder/canonical-structure/codegen-size gap separate from semantic optimization. A traced medium-naive `--optimize` run showed pass-local performance dominated by `simplify-locals` (`356569us` total, mostly `scan-root-region`) plus repeated hot lifting (`283080us`, `2956` lifts), while local Binaryen `wasm-opt --all-features -O4` wall time was faster in a 3-run median (`0.668s` vs Starshine optimize `0.863s`; Starshine optimize plus DFE/CF/RSE `1.165s`). Treat this as evidence for both cautious preset candidates and a hot-pass lifting/simplify-locals performance audit.
  - Current recommendation: `DFE + RUME + CF + RSE + RUME` is runtime-green and direct-pass-green as an incremental suffix candidate, but it should land only in a dedicated preset-widening slice with exact ordering tests, full gate evidence, and a decision about whether DFE's incidental name-section stripping is acceptable before explicit `[JSON-AS]008` `strip-debug`.
  - Deliverables:
    - [ ] Add preset placement for `duplicate-function-elimination`, `code-folding`, and `redundant-set-elimination` with exact-order tests, fresh `json-as` artifact validation/Node smoke, and the section metrics from research note `0713`.
    - [ ] Measure incremental contributions of `inlining`, `inlining-optimizing`, `dae-optimizing`, type cleanup / `remove-unused-types`, and function reordering/removal equivalents against the same `json-as` debug artifacts.
    - [ ] Add preset-order tests for any widened optimize/shrink slots; do not collapse repeated cleanup slots without Binaryen-neighborhood evidence.
    - [ ] Record size deltas by section (`types`, `functions`, `globals`, `code`, `data`) for medium-naive, medium-simd, and large-swar debug artifacts.
  - Suggested tests: `moon test src/passes`, `moon test src/cmd` for preset changes, `moon build --target native --release src/cmd`, direct pass compare for every newly scheduled pass, and cloned `json-as` Starshine-vs-Binaryen artifact size replay.

- [JSON-AS]008 - Schedule and measure `strip-debug` for debug custom-section stripping
  - Status: direct module pass implemented; active preset/artifact follow-up remains. `src/passes/strip_debug.mbt` and `src/passes/strip_debug_test.mbt` register and test `strip-debug`; `bun scripts/pass-fuzz-compare.ts --list-passes` lists it. The old "not implemented yet" wording is superseded by the 2026-06-07 behavior inventory.
  - Goal: decide whether and where to schedule explicit `strip-debug` / `StripDebug` in size/optimize paths after direct-pass and artifact evidence.
  - Why: 2026-06-05 `json-as` size metrics showed Starshine O4 still carrying non-semantic `name` custom-section bytes in fixed standard artifacts while Binaryen O4 carried no custom sections. Current evidence: `medium.bench.incremental.naive.starshine-o4.wasm` has `14549` total custom-section bytes and `large.bench.incremental.swar.starshine-o4.wasm` has `15743`, all from the `name` custom section; `medium.bench.incremental.simd.starshine-o4.wasm` currently has `0`. This is a deterministic size gap independent of the deeper live-function/type cleanup gap.
  - Deliverables:
    - [x] Add a TDD fixture that proves `strip-debug` removes structured `name_sec`, `raw_name_sec_payload`, and non-semantic debug custom sections while preserving semantic sections and validation.
    - [x] Register the pass as a module pass, wire CLI/pass-manager dispatch, and expose the canonical pass spelling expected by Starshine/Binaryen compare tooling.
    - [ ] Refresh direct `--strip-debug` compare evidence with the current native binary and classify any custom-section/name-section drift separately from semantic sections.
    - [ ] Decide preset placement after direct pass evidence; likely a late `optimize` / `shrink` slot after passes that may use names for diagnostics or extract helpers.
    - [ ] Re-measure `json-as` custom-section deltas after scheduling; expected immediate wins are about `14.5KB` on medium-naive and `15.7KB` on large-swar before addressing remaining function/type cleanup.
  - Suggested tests: focused `src/passes/strip_debug*_test.mbt`, `moon test src/passes`, `moon test src/cmd` if CLI/help changes, `moon build --target native --release src/cmd`, direct `--strip-debug` compare evidence, `wasm-tools validate --features all` on rewritten `json-as` artifacts, and Node runtime smoke for the three fixed `json-as` cases.

- [JSON-AS]004 - Keep a cloned json-as benchmark replay for optimizer artifact signoff
  - Status: active artifact-signoff follow-up; 2026-06-06 full `as-test` suite replay is Starshine-green for the pinned clone but not yet scripted as a durable repo task.
  - Goal: make the `json-as` debug-artifact comparison repeatable without relying on temporary restored wasm files under `examples/`.
  - Why: the restored example wasm files were already heavily optimized and made Starshine look size-regressive. A fresh git clone of `json-as` at `f707d68d5ce5136ecfd0c576140421286c9e93a8`, local `bun install`, `assemblyscript@0.28.17`, and `bun run build:transform` produced debug artifacts where Binaryen O4 runs successfully and Starshine currently validates but traps. The clone path used during triage was `.tmp/json-as`, which is intentionally not durable. On 2026-06-06, the stronger suite replay built all 105 configured `as-test` wasm artifacts (`35` specs across `naive`, `swar`, and `simd`) with a Node WASI runner, optimized them with current Starshine `--traps-never-happen --closed-world --optimize -O4`, validated every output with `wasm-tools validate --features all`, and ran the full suite successfully: `35` files, `1284` suites, `10656` tests, `3` modes, `0` failures. The comparable Binaryen `wasm-opt --all-features --traps-never-happen --closed-world -O4` suite replay also passed. Starshine suite outputs total `57,167,519` bytes versus Binaryen `44,705,784` bytes (`+27.87%`), so correctness is ahead of size parity.
  - Runtime measurement note: without `d8`/`v8` available locally, the 2026-06-06 direct performance pass used the Node WASI test artifact runner as a proxy, not the official `json-as` benchmark harness. Two cold process runs per optimized suite artifact gave summed medians of Starshine `161,962.7ms` vs Binaryen `153,542.5ms` (`+5.48%` Starshine slower overall): `naive` `+17.73%`, `simd` `-0.48%`, `swar` `-0.69%`. Treat these as local artifact-runner timing evidence only; stable benchmark claims still require the real benchmark runtime.
  - Deliverables:
    - [ ] Add a documented, opt-in replay command or task that clones `json-as` into `.tmp/`, pins AssemblyScript `0.28.17`, builds `large` SWAR, `medium` NAIVE, and `medium` SIMD debug artifacts, and emits Starshine/Binaryen outputs without touching committed examples.
    - [x] Keep runtime execution separate from validation: validation alone missed earlier Starshine corruption. 2026-06-06 full-suite replay validated each optimized wasm and then executed the suite under the Node WASI runner.
    - [ ] Prefer the repo's `d8` runner when available; otherwise use a checked-in Node runner equivalent for local smoke comparisons.
    - [x] Capture Binaryen baseline runtime numbers as reference only, not as deterministic CI thresholds, unless a stable benchmark environment is added. 2026-06-06 Node WASI proxy timings are recorded in `.tmp/jsonas-suite-test/wasi-artifact-perf-summary.json` and summarized above.
  - Suggested tests: opt-in script/task dry run, `wasm-tools validate --features all` on all generated outputs, and runtime smoke under Node or `d8` for debug, Binaryen O4, and Starshine outputs.

### Whole-command wall-time budget

- [WALL]001 - Cross-Pass Runtime Budget And Attribution
  - Goal: own whole-command Starshine-vs-Binaryen wall-time measurement outside individual pass parity slices.
  - Why: direct pass slices may be signed off while aggregate preset or no-op command paths still hide parse/emit, validation, HOT lift/lower, cache, or buffering costs.
  - Current known target: direct `--ssa-nomerge` whole-command replay on `tests/node/dist/starshine-debug-wasi.wasm`; 2026-06-14 attribution shows the wall spike is raw SSANM work, not the parsed `pass:*` timer.
  - Current evidence: `.tmp/self-ssa-nomerge-debug-wasi-walltime-investigation-final-20260614/result.json` recorded Starshine `4564.108ms` wall time, `0.199ms` pass-local time, `1590.243ms` `raw:ssa-nomerge:*` time, `466.093ms` other traced time, and `2507.573ms` untraced overhead vs Binaryen `918.897ms` wall / `431.565ms` pass time. Command-level traced phases were read `5.919ms`, decode `153.825ms`, final validate `154.813ms`, reuse check `1.302ms`, encode `137.987ms`, and output write `5.719ms`. Top raw functions are now small and spread out, led by `Func 499` `_M0FP37jtenner9starshine6passes41dae__instr__is__nontrapping__pure__binary` (`26.630ms`), `Func 3781` `_M0FP37jtenner9starshine6binary32decode__instruction__with__depth` (`9.515ms`), `Func 498` `_M0FP37jtenner9starshine6passes40dae__instr__is__nontrapping__pure__unary` (`8.576ms`), and `Func 3490` `_M0MP37jtenner9starshine4wast12KeywordTable3new` (`8.542ms`). Earlier stale-binary attribution in `.tmp/self-ssa-nomerge-debug-wasi-walltime-investigation-func-fixed-20260614` showed a 59s raw cliff; the current rebuilt binary supersedes it.
  - Deliverables:
    - Separate pass-local runtime, raw fast-path runtime, harness/tool startup, parse/emit, validation, HOT lift/lower, analysis cache, and artifact representation costs.
    - Maintain a prioritized list of cross-cutting runtime fixes without blocking pass correctness signoff on aggregate wall time.
    - Evaluate remaining non-pass candidates such as encoder size/backpatch and code-section buffering reduction.
  - Suggested tests: focused timing traces, `moon info`, `moon fmt`, `moon test`, and targeted self-compare commands for any changed pass/tool path.

- [WALL]002 - Self-optimization O4z wall time is dominated by `inlining-optimizing`
  - Status: root cause fixed 2026-06-15; keep active for follow-up sub-hotspot optimization and full self-opt signoff.
  - Goal: reduce or explicitly gate the `inlining-optimizing` whole-module runtime on the Starshine debug CLI artifact before treating self-optimization as practical for routine use.
  - Why: the successful traced run `.tmp/self-opt-after-live-label-fix.stderr` spans about `13m18s` by file timestamps (`03:53:42` to `04:07:00`). Trace timer attribution shows a single `pass:inlining-optimizing` instance taking `676.916s` (`11m16.9s`), about `85%` of the observed wall time. The next largest pass timer is `dae-optimizing` at `1.914s`; cumulative HOT `lift` across `35,631` function-pass lifts is `3.017s`, `merge-blocks` totals `0.424s` across `12,994` calls, and module verification/final validation timers are sub-second per pass. Current inlining trace only reports nested cleanup touched counts (`1932`, `465`, `70`, `6`, `1`) and has no internal phase timers, so the bottleneck is localized to `src/passes/inlining.mbt` / `inlining_run_module_pass` but not yet subdivided between graph analysis, function-info building, call rewriting, helper removal, nested cleanup, and iteration/fixed-point control.
  - Deliverables:
    - [x] Inventory current Starshine behavior against Binaryen v130 before performance edits. 2026-06-15 note `docs/wiki/raw/research/0723-2026-06-15-inlining-optimizing-behavior-inventory.md` finds the basic direct-call full-inlining thresholds mostly match Binaryen defaults; the likely performance-risk surfaces are Starshine-only graph/prediction/trim scaffolding, repeated full-module scans, rewrite walks, and iteration control rather than changing Binaryen-compatible heuristics first.
    - [x] Add fine-grained perf timers inside `inl_run_module_pass_once` and `inlining_run_module_pass` for `inl_original_reachable_private_cycle_funcs`, `inl_prepare_multivalue_block_types`, `inl_build_function_infos`, `inl_rewrite_all_calls`, `inl_remove_dead_inlined_helpers`, `inl_run_nested_cleanup`, dead/unreachable cleanup, finalization, iteration counts, and touched-function counts.
    - [x] Re-run the self-opt debug artifact replay with the added timers and record the top inlining subphase(s), iteration count, and touched counts. 2026-06-15 note `docs/wiki/raw/research/0724-2026-06-15-inlining-optimizing-wall-time-root-cause.md` records `.tmp/self-opt-inlining-detail.stderr`: `pass:inlining-optimizing` `690.716s`, with `219.543s` initial plus `449.151s` iterative time in `inl_original_reachable_private_cycle_funcs(...)`; nested cleanup was skipped by the large-module guard. Iterations touched `1933`, `465`, `70`, `6`, `1`, then no-change.
    - [x] Optimize the hot subphase. `inl_original_reachable_private_cycle_funcs(...)` now builds an iteration-local transitive reachability table instead of repeatedly running recursive `inl_graph_reaches(...)` with fresh `seen` arrays. The same artifact replay in `.tmp/self-opt-inlining-reach.stderr` reduced `pass:inlining-optimizing` to `41.091s`; original-graph time fell to `3.321s` initial plus `14.592s` iterative (`~16.8x` pass-local speedup).
    - [x] Follow up remaining post-fix sub-hotspots: `detail:inlining:once:preserve-root-cycle-check` `12.274s` and `detail:inlining:once:collapse-unreachable-roots` `8.835s` on the same replay. 2026-06-15 note `docs/wiki/raw/research/0725-2026-06-15-inlining-optimizing-cache-followup.md` records cached same-signature/dead-suffix preservation facts, lazy original-reachability prediction after no-change detection, and per-helper signature-key caches. The same artifact replay in `.tmp/self-opt-inlining-final-perf.stderr` reduced `pass:inlining-optimizing` to `3.528s`; remaining traced buckets are sub-second (`once:original-reachable-private-cycle-funcs` `0.867s`, initial original reachability `0.285s`, collapse roots `0.271s`, preserve-root-cycle check `0.159s`).
    - [x] Preserve correctness evidence while changing performance behavior: reachability fix evidence remains in `.tmp/pass-fuzz-inlining-optimizing-reachability-10000`; follow-up evidence in `.tmp/pass-fuzz-inlining-optimizing-perf-cache-10000` compared `7607/10000`, with `0` mismatches, `0` validation failures, `0` property failures, and `20` Binaryen/tool command failures. `moon info`, `moon fmt`, focused `inlining_wbtest.mbt` / `inlining_test.mbt`, full `moon test` (`5784/5784`), native cmd build, and artifact replay passed on 2026-06-15.
  - Suggested tests: `moon fmt`, focused `src/passes/inlining*_test.mbt`, `moon test`, `moon build --target native --release src/cmd`, timed `bun self-opt optimize`, and any direct `inlining-optimizing` compare lane needed after behavior changes.

### O4z Per-Pass Deep Audits

Release gate: complete these before the v0.1.0 release so `-O4z` pass coverage is more comprehensive and pass-local runtime owners are known before publishing.

Use this checklist for every `[O4Z-AUDIT-*]` slice below:
- Start from the pass wiki page and owner source/test files; update docs if findings become durable.
- Treat the target as Binaryen behavior parity, not output parity. A direct compare lane with `0` mismatches is necessary evidence, but it does not close an audit while docs still say the pass is a narrow subset or list broad missing Binaryen behavior.
- Run or refresh direct pass oracle evidence by building `src/cmd` once (`moon build --target native --release src/cmd`) and using `bun scripts/pass-fuzz-compare.ts --pass <name> --count 1000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` first, then scale to 10000 only when changing behavior or closing the slice.
- Inspect tests for missing positive/negative shapes, add focused test-first fixtures for any bug or missed optimization, and keep validation failures separate from representation drift.
- Capture pass-local timing where available; file whole-command issues under `[WALL]001` unless the pass is clearly the owner.
- Replay the pass's `-O4z` slot/neighborhood when it has saved artifacts or documented generated-audit evidence.
- Close with an agent-classified findings note: bugs found/fixed, missing behavior added, representation-only drift, tool/oracle failures, performance owners, deferred risks, exact commands, counts, and artifact paths.
- Do not remove an audit from this backlog unless every remaining Binaryen behavior difference is narrow, evidence-backed, tool-blocked or explicitly accepted as a non-goal, and linked to reopening criteria.

Behavior inventory source: `docs/wiki/raw/research/0714-2026-06-07-o4z-behavior-parity-inventory.md`.

Reopened from prior removed audits because committed docs still list behavior gaps:


- [O4Z-AUDIT-GR] - Confirm or fix remaining `global-refining` behavior watchpoints
  - Status: reopened focused v0.1.0 audit. The main boundary behavior is green, but the parity page still calls out a missing explicit GC feature gate and unproven descriptor/stringref/public-type initializer surfaces.
  - Scope: Binaryen's GC-enabled top-level gate, descriptor-bearing public type bodies, broader stringref and exact-ref initializer expressions, open/closed-world exported-global rules, and any future local cached `global.get` retagging/refinalization equivalent.
  - Deliverables: add focused fixtures or source-backed no-op proof for each watchpoint; close only when remaining differences are proven representation-only/local-IR-only or explicitly accepted.

- [O4Z-AUDIT-GSI] - Widen `global-struct-inference` beyond subset signoff
  - Status: completed for ordinary `global-struct-inference` / `--gsi` v0.1.0 parity. Evidence lives in `docs/wiki/raw/research/0718-2026-06-08-global-struct-inference-behavior-gap-inventory.md`: focused GSI tests passed `58/58`, `moon test src/passes` passed `2003/2003`, full `moon test` passed `5195/5195`, native `src/cmd` build passed with pre-existing warnings, direct 10000-case compare `.tmp/pass-fuzz-global-struct-inference-debris-cleanup-10000` compared `7609/10000` with `0` mismatches, and strict post-cleanup 100000-case compare `.tmp/pass-fuzz-global-struct-inference-post-debris-100000` compared `99751/100000` with `99751` normalized matches, `0` mismatches, `0` validation/property/generator failures, and `249` Binaryen/tool command failures. The exact Binaryen `version_130` source/lit surface was refreshed; no additional ordinary-GSI positive surface was found beyond the covered direct-read, closed-world, descriptor-read, un-nesting, packed-field, nullable-trap, and refinalization families. `moon info` still reproduces the known Moon tool panic.
  - Scope closed: open-world direct immutable-global reads, closed-world candidate-global reasoning over locals and params, subtype propagation, one/two unique-value selection, non-constant un-nesting, packed fields, atomic gets, `ref.get_desc`, aggregate array un-nesting, carrier breadth proven for ordinary `--gsi`, larger decision trees, Binaryen-style refinalization triggers, and Binaryen-style dropped-unreachable debris cleanup before hard `unreachable`.
  - Explicit non-scope: sibling `global-struct-inference-desc-cast` / `gsi-desc-cast` remains a separate pass audit; do not claim `ref.cast_desc_eq` parity under ordinary `--gsi`.

Preset behavior inventory:

- [O4Z-PRESET-BEHAVIOR] - Reconcile documented Binaryen no-DWARF order with Starshine public presets
  - Status: active behavior-inventory slice; do not widen presets until direct pass audits and ordered neighborhoods are green.
  - Current finding: after the DFE audit, public `optimize` / `shrink` include Binaryen-shaped early and late DFE neighborhoods plus `dae-optimizing`, `inlining-optimizing`, `duplicate-import-elimination`, and one early `remove-unused-module-elements` slot. Remaining documented count/order gaps are now outside DFE: the second pre-pass `remove-unused-module-elements`, repeated `reorder-locals` / `coalesce-locals` details, `code-folding`, `redundant-set-elimination`, and the known extra Starshine `remove-unused-brs` slot. Local `wasm-opt --version` now reports `version_130`, so the exact path needs a fresh `version_130` reread before final non-DFE scheduling claims.
  - Deliverables: refresh the no-DWARF path against the current local `version_130` oracle, add exact preset-order tests for any changes, and keep repeated cleanup slots intentional unless an ordered-neighborhood proof justifies divergence.

- [O4Z-AUDIT-SSA] - Deep audit `ssa-nomerge`
  - Status: active v0.1.0 release-gating audit. The previous `SSA-LG`, `SSA-RAW`, `SSA-SHADOW`, `SSA-TYPED-LOOP`, `SSA-HUGE`, `SSA-PRESET`, `SSA-GEN`, and `SSA-CLOSEOUT` checklist is superseded by the `SSANM-*` backlog below. Completed LocalGraph analysis and full-`ssa` staging evidence is preserved in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md` and related wiki pages; do not re-add completed analysis-only work here.
  - Scope: match Binaryen `SSAify(false)` behavior for `ssa-nomerge`: LocalGraph-backed per-write freshening, single-source get retargeting, default-entry materialization, canonical preservation of merge regions, explicit EH/typed-control boundaries, huge-function scalability, artifact replay, and preset scheduling. Full `ssa` merge-local materialization is a sibling pass effort and should not be tracked as `SSANM` work.
  - Current finding: Starshine now has the LocalGraph facts needed for Binaryen-style no-merge policy and consumes them for completed straight-line, default, canonical-merge, mixed normal-control, ordinary block/if, and narrow decorated loop-backedge families. The retained lifted HOT `ssa_destroy_into_hot(...)` bridge and raw branch/table/typed/EH copy-like helpers remain source-reachable, but they are documented as fallback/boundary or sibling full-`ssa` surfaces rather than ordinary no-merge behavior. Binaryen `ssa-nomerge` does not lower overlay phis through predecessor copies; it allocates fresh locals only for explicit writes whose influenced gets remain single-source, rewrites those single-source gets, materializes legal default-entry reads, and leaves multi-source merge gets on the original canonical local.
  - Current external blockers and replay anchors: direct fuzz has strong green evidence, but closeout remains blocked by the checked-in debug-WASI direct artifact first-diff chain after removing the huge `ssa-nomerge` size guard. The `defined=2 abs=29` stale-read chain found in `.tmp/self-ssa-nomerge-no-huge-limit-20260615` is fixed; `.tmp/self-ssa-nomerge-artifact-refresh-20260615` is the current fast full self-compare anchor and reports `defined=2087 abs=2114` because the diagnostic pretty fallback is intentionally capped on very large WAT functions. Targeted extraction reclassifies the stale `.tmp/self-ssa-nomerge-loop-carrier-fix2-20260615` `defined=32 abs=59` tuple as semantic-safe but local-count-losing/no-size-regression representation drift, and classifies the current `abs=2114` tuple as a semantic-safe Starshine local-count win / no-regression representation difference, not a currently actionable semantic gap. Remaining blockers are continued artifact-diff classification past `defined=6078 abs=6105`, any O4z scheduling regressions exposed by removing broad no-op guards, and final-closeout evidence.
  - Active `SSANM` work slices, grouped by identifier:
    - [x] [SSANM-001] - Refresh Binaryen no-merge source and fixture baseline
      - Goal: recheck the current local Binaryen `version_130` source/test surface for `SSAify.cpp`, `LocalGraph`, `ReFinalize`, pass registration, `ssa-nomerge_enable-simd`, `ssa.wast`, and `local-graph.cpp` before replacing Starshine mutation policy.
      - Status: completed 2026-06-13 through `[SSANM-001a]` and `[SSANM-001b]`; source refresh and fixture planning are durable in the SSA no-merge wiki pages.
      - Deliverables: concise wiki/parity note confirming whether the existing `version_129` dossier still applies; focused fixture list for the implementation slices below; explicit source-backed statement for any Binaryen drift.
      - Evidence: local `wasm-opt --version` reported `wasm-opt version 130 (version_130)`; official `version_129` and `version_130` owner/test surfaces had no no-merge behavior-contract drift; the fixture baseline matrix maps the refreshed sources to `[SSANM-002a]` through `[SSANM-007b]`. No executable tests/fuzz were run for the epic marker because it only records completed docs/source-review children.
    - [x] [SSANM-001a] - Refresh Binaryen no-merge source surface
      - Status: completed 2026-06-13; source note lives in `docs/wiki/raw/binaryen/2026-06-13-ssa-nomerge-version-130-source-refresh.md` and the parity summary lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: compare the current local Binaryen `version_130` implementation/test files against the checked-in `version_129` dossier before mutation slices consume the LocalGraph plan.
      - Deliverables: source-backed wiki/parity note for `SSAify.cpp`, `LocalGraph`, `ReFinalize`, pass registration, `ssa-nomerge_enable-simd`, `ssa.wast`, and `local-graph.cpp`; explicit drift/no-drift statements; source paths or reviewed URLs.
      - Evidence: local `wasm-opt --version` reported `wasm-opt version 130 (version_130)`. Downloaded `version_129` and `version_130` official sources under `.tmp/ssanm001a` showed byte-identical `SSAify.cpp`, `local-graph.h`, `LocalGraph.cpp`, `ssa-nomerge_enable-simd.wast`, `ssa-nomerge_enable-simd.txt`, `ssa.wast`, and `local-graph.cpp` gtests. `ReFinalize.cpp` drift is limited to wide-int finalizers; `pass.cpp` / `passes.h` drift is unrelated registrations/options spelling with unchanged `ssa-nomerge` registration and early scheduling. Tests were not run because this was docs/source review only.
    - [x] [SSANM-001b] - Refresh fixture baseline from current Binaryen source/tests
      - Status: completed 2026-06-13; fixture baseline matrix lives in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`, with signoff summary in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: turn the refreshed source surface into a focused fixture matrix for `[SSANM-002a]` through `[SSANM-007b]`.
      - Deliverables: matrix of straight-line, default-entry, branch/loop merge, tee, EH, typed-control, and huge-function fixture owners; explicit notes for any Binaryen source/test shape not represented by an existing or planned Starshine fixture.
      - Evidence: docs/source review only. Local `wasm-opt --version` still reported `wasm-opt version 130 (version_130)`. Reviewed `.tmp/ssanm001a/version_130` anchors for `SSAify.cpp` no-merge allocation/get-rewrite flow, the five dedicated no-merge fixture functions (`$basics`, `$if`, `$if2`, `$nomerge`, `$simd-zero`), and the eight LocalGraph gtests. No executable tests/fuzz were run because no behavior or generated contracts changed.
    - [x] [SSANM-002a] - Build a LocalGraph no-merge rewrite planner
      - Status: completed 2026-06-13; `src/passes/ssa_nomerge.mbt` now exposes an analysis-only LocalGraph rewrite plan.
      - Goal: add a no-mutation `ssa-nomerge` plan that consumes `HotLocalGraph` and classifies every relevant write/get as Binaryen would.
      - Deliverables: planned fresh-local allocation per explicit `local.set` / `local.tee`, canonical keep decisions for already-SSA locals and merge-feeding writes, get retarget/default/keep decisions, and focused planner tests for straight-line, branch, loop-carried, param-entry, body-default, tee, dead-write, and no-read shapes.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed on missing `SsaNoMergeRewritePlan` / `ssa_nomerge_build_rewrite_plan` identifiers, then passed `379/379`. `moon info` passed with the three pre-existing GenValid warnings; `moon fmt` passed; `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `379/379`; `moon test src/passes` passed `2409/2409`; full `moon test` passed `5714/5714`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings. Direct `ssa-nomerge` compare/fuzz was not run because the new planner is analysis-only and does not change pass mutation or public CLI behavior.
      - Invariants: no merge locals, no predecessor copies, no entry prepends, no pass mutation.
    - [x] [SSANM-002b] - Shadow the LocalGraph plan against current `ssa-nomerge`
      - Status: completed 2026-06-13; test-only shadow helpers compare the LocalGraph plan with current observable `ssa-nomerge` output on reduced fixtures.
      - Goal: compare planned keep/freshen/default/merge decisions with the current HOT/raw behavior before changing mutation.
      - Deliverables: temporary or test-only comparison helpers, a mismatch table grouped by Binaryen behavior family, and updated backlog notes separating true parity gaps from legacy artifact-specific local-allocation hacks.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed on missing `ssa_nomerge_test_shadow_summary_from_wat`, then passed `381/381`. Shadow fixtures cover already-SSA straight-line canonical traffic, overwritten `local.tee` freshening/retargeting, body-default materialization through the raw default path, and branch-local already-SSA alignment. The current reduced shadow table found those families aligned with the planner; the remaining direct profile mismatches are still the known Binaryen local-allocation behavior gap from generated coverage, not a new shadow-helper mutation. Final validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `381/381`; `moon test src/passes` passed `2411/2411`; full `moon test` passed `5716/5716`; native `moon build --target native --release src/cmd` completed with no work to do; `git diff --check` passed; short direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm002b-shadow-1000` requested `1000`, compared `998`, had `998` normalized matches, `0` mismatches, and `2` Binaryen/tool command failures with cache hits. The helper is test-only and does not change pass mutation or public CLI behavior.
    - [x] [SSANM-003a] - Implement LocalGraph straight-line no-merge writes
      - Status: completed 2026-06-13; `src/passes/pass_manager.mbt` now builds and consumes `SsaNoMergeRewritePlan` for raw straight-line `ssa-nomerge` functions without `local.tee`.
      - Goal: replace straight-line heuristic freshening with the LocalGraph plan while preserving existing safety guards during the transition.
      - Deliverables: mutation for scalar straight-line `local.set` freshening, repeated gets after one set, dead parameter overwrites, already-SSA canonical locals, no-read/dead-write boundaries, and public-pipeline WAT tests.
      - Evidence: red-first focused test initially failed because the trace still reported the legacy `straight-line-local-writes` path; after implementation, public-pipeline fixtures prove the LocalGraph-planned raw path for live parameter overwrite canonical preservation and overwritten body-local `local.set` freshening/retargeting. `local.tee` remains on the legacy heuristic for `[SSANM-003b]`. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `383/383`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2413/2413`; full `moon test` passed `5718/5718`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm003a-localgraph-set-10000` requested `10000`, compared `7605`, had `7605` normalized matches, `0` mismatches, and `20` Binaryen/tool command failures with cache hits.
    - [x] [SSANM-003b] - Implement LocalGraph `local.tee` no-merge writes
      - Status: completed 2026-06-13; the raw straight-line `ssa-nomerge` path now consumes `SsaNoMergeRewritePlan` for `local.tee` as well as `local.set`.
      - Goal: make `local.tee` use the same per-write no-merge eligibility as `local.set` while preserving stack-result semantics.
      - Deliverables: tee freshening, tee get-retargeting, dead tee overwrite through the existing planner/shadow coverage, folded child-expression tee where WAT/HOT lowering emits a flat raw `local.tee`, and effect/trap non-duplication proof by preserving a folded call operand as one call plus one retargeted tee. Structured-control `local.tee` still belongs to `[SSANM-006a]` / boundary slices.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because a new tee fixture still traced the legacy `straight-line-local-writes` path; after implementation, focused public-pipeline fixtures prove LocalGraph-planned trace/output for overwritten root `local.tee` freshening, stack-result preservation, final get retargeting, and folded effectful tee operands without duplicating the call. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `385/385`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2415/2415`; full `moon test` passed `5720/5720`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm003b-localgraph-tee-10000` requested `10000`, compared `7604`, had `7604` normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits.
    - [x] [SSANM-004a] - Implement LocalGraph default-entry get materialization
      - Status: completed 2026-06-13; the raw `ssa-nomerge` path now consumes `SsaNoMergeRewritePlan` for default-entry get materialization in straight-line functions and in a narrow normal structured-control subset with body-local reads that have no explicit writes.
      - Goal: make default replacement a general LocalGraph get rewrite, not only a no-local-write raw special case.
      - Deliverables: scalar numeric, `v128`, nullable/exact ref, and branchy-control body-default fixtures; parent type repair/validation proof; nondefaultable entry reads stay unchanged.
      - Invariants: parameter-entry reads remain parameter reads; explicit writes are not treated as defaults; default replacement must validate after lowering.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because structured default-with-write fixtures still traced the legacy `structured-local-writes` path; after implementation, focused public-pipeline fixtures prove LocalGraph-planned default materialization for numeric and `v128` straight-line reads with writes, branchy structured body-default reads with other writes, exact nullable ref defaults under a parent result block, and parameter-entry canonical preservation. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `389/389`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2419/2419`; full `moon test` passed `5724/5724`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm004a-default-localgraph-10000` requested `10000`, compared `7609`, had `7609` normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits.
    - [x] [SSANM-004b] - Retire or narrow legacy raw default/no-write special cases
      - Status: completed 2026-06-13; no-local-write default reads now reuse the LocalGraph `SsaNoMergeRewritePlan` materializer and the legacy recursive default-only rewriter is removed.
      - Goal: after `SSANM-004a`, remove duplicated default-local rewrite policy or prove the raw path is still needed only as a fast path.
      - Deliverables: code simplification or documented fast-path boundary, direct compare evidence, and no regression in dropped-unreachable-debris cleanup.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because the branchy no-write default fixture still traced `default-local-reads`; after implementation, no-write default reads trace `default-local-reads-localgraph-plan` and dropped-unreachable-debris cleanup remains a separate `no-local-writes-unreachable-debris` fast path. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `390/390`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2420/2420`; full `moon test` passed `5725/5725`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm004b-default-fastpath-10000` requested `10000`, compared `7604`, had `7604` normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits.
    - [x] [SSANM-005a] - Preserve canonical one-arm default and parameter merges
      - Status: completed 2026-06-13; simple missing-else one-arm entry/default merges now take a LocalGraph-planned canonical-preservation fast path instead of falling into the legacy structured local-write rewriter.
      - Goal: match Binaryen no-merge behavior when a post-branch get may see either an explicit write or the entry/default source.
      - Deliverables: one-arm body-local default merge, one-arm parameter-entry merge, missing-else branch fixtures, and assertions that no fresh merge local/predecessor copy is introduced.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because both new one-arm merge fixtures still traced the legacy `structured-local-writes` path; after implementation, they trace `structured-one-arm-merge-localgraph-plan`, keep the canonical body local or parameter slot, and introduce no fresh merge/predecessor-copy local. Final validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `392/392`; `moon test src/passes` passed `2422/2422`; full `moon test` passed `5727/5727`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; `git diff --check` passed; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm005a-one-arm-merge-10000` requested `10000`, compared `7602`, had `7602` normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits.
    - [x] [SSANM-005b] - Preserve canonical both-arm and multi-source merges
      - Status: completed 2026-06-13 through `[SSANM-005b1]` through `[SSANM-005b3]`; remaining broader branch/table mutation expansion is tracked by `[SSANM-006a3]`, and typed-control branch operands remain in `[SSANM-007b*]`.
      - Goal: keep multi-source merge reads and their merge-feeding writes on the original canonical local.
      - Deliverables: both-arm write merges, loop/backedge merges, `br` / `br_if` / `br_table` merge reads where represented, and direct contrast tests against full `ssa` merge-local behavior.
    - [x] [SSANM-005b1] - Preserve simple both-arm and `br_if` multi-source merges
      - Status: completed 2026-06-13; simple normal structured-control multi-source merge plans now bypass the legacy structured local-write rewriter when the LocalGraph plan is canonical-only.
      - Goal: prevent fresh merge locals or predecessor copies for both-arm `if` merges and block/`br_if` early-exit merges that Binaryen no-merge keeps on the canonical slot.
      - Deliverables: public-pipeline tests for both-arm body-local writes and block/`br_if` multi-source reads, canonical set/get assertions, no temp-local writes, and direct compare evidence.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because both new fixtures still traced `structured-local-writes`; after implementation, they trace `structured-multisource-merge-localgraph-plan`, keep only the original body local, keep both merge-feeding writes on local `1`, and introduce no temp/predecessor-copy local. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `394/394`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2424/2424`; full `moon test` passed `5729/5729`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; `git diff --check` passed; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm005b1-multisource-merge-10000` requested `10000`, compared `7607`, had `7607` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits (`wasm-smith` `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`).
      - Remaining boundaries: plain `br`, `br_table`, nested loop/control backedges, typed-control branch operands, EH, and mixed fresh/canonical per-write regions remain in `[SSANM-005b3]`, `[SSANM-005c]`, `[SSANM-006a]`, and `[SSANM-007*]`.
    - [x] [SSANM-005b2] - Classify and preserve loop/backedge canonical merges
      - Status: completed 2026-06-13; simple void-loop direct `br_if 0` backedge merges now bypass the legacy HOT defer when the LocalGraph plan is canonical-only.
      - Goal: decide which loop-carried and backedge merge reads can safely take the LocalGraph canonical-only path without bypassing existing typed-loop/value-backedge scratch lowerings.
      - Deliverables: reduced loop/backedge fixtures, source-backed boundary classification for existing HOT/defer helpers, validation and direct compare evidence for any admitted no-mutation family.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because the loop-carried fixture still added scratch locals through the legacy defer/HOT path; after implementation, the fixture traces `structured-loop-backedge-merge-localgraph-plan`, keeps the entry and backedge writes on canonical local `0`, keeps both reads on local `0`, and introduces no temp/predecessor-copy local. Typed-loop/value backedge scratch lowerings remain covered by existing typed-loop tests because this path only admits void loops with direct `br_if 0`, no nested blocks/loops, no plain `br`, no `br_table`, no `br_on_*`, no cast branches, and no `try_table`. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `394/394`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2424/2424`; full `moon test` passed `5729/5729`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; `git diff --check` passed; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm005b2-loop-backedge-10000` requested `10000`, compared `7607`, had `7607` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`), and cache hits (`wasm-smith` `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`).
    - [x] [SSANM-005b3] - Classify `br`, `br_table`, and full-`ssa` contrast for multi-source merges
      - Status: completed 2026-06-13; plain `br` and `br_table` multi-source merge regions are now explicitly fail-closed from LocalGraph canonical-only paths, and full-`ssa` contrast remains documented as sibling planner/materialization work rather than `ssa-nomerge` behavior.
      - Goal: separate plain branch and table-branch canonical no-merge cases from existing branch-alias copy/scratch families, and add focused contrast against full `ssa` merge-local behavior where practical.
      - Deliverables: `br` / `br_table` fixtures or documented fail-closed boundaries, direct contrast tests or source-backed explanation, and docs/backlog updates before closing `[SSANM-005b]`.
      - Evidence: added focused public-pipeline fail-closed coverage in `src/passes/ssa_nomerge_test.mbt`: `ssa-nomerge keeps plain br multi-source merges off LocalGraph path` and `ssa-nomerge keeps br_table multi-source merges off LocalGraph path` prove branch/table multi-source regions validate, preserve the original `br` / `br_table`, and do not claim `structured-multisource-merge-localgraph-plan` or `structured-mixed-localgraph-plan`. Existing full-`ssa` planner/public tests in `src/passes/ssa_test.mbt` remain the sibling contrast: full `ssa` has merge-planner coverage, but public merge materialization is still fail-closed and must not be counted as `ssa-nomerge` work. Red-first was not practical because this slice locks/documented existing boundary behavior rather than changing pass mutation. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `404/404`.
    - [x] [SSANM-005c] - Freshen overwritten writes inside broader merge regions
      - Status: completed 2026-06-13 through `[SSANM-005c1]`, `[SSANM-005c2]`, and `[SSANM-005c3a]` through `[SSANM-005c3c]`; remaining broader branch/table/loop/EH/typed-control expansion is tracked by `[SSANM-006a]` and `[SSANM-007*]` rather than this mixed normal-control slice.
      - Goal: implement Binaryen’s per-write, not per-local or per-arm, no-merge rule.
      - Deliverables: fixtures where one original local has early freshenable writes, later canonical merge-feeding writes, overwritten dead writes inside merge arms, and freshenable writes after a merge region.
    - [x] [SSANM-005c1] - Classify mixed fresh/canonical merge-region fixtures
      - Status: completed 2026-06-13; focused planner coverage now pins the mixed per-write decision table before broader mutation.
      - Goal: pin the LocalGraph decision table for mixed regions before mutation: early writes that are single-source should freshen, merge-feeding writes should stay canonical, and later single-source writes should become freshenable again.
      - Deliverables: planner/shadow or public-pipeline red-first fixtures for a single local with early freshenable writes, both-arm canonical merge-feeding writes, and post-merge freshenable writes; classification of any legacy structured raw helper that must remain active.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed on missing `ssa_nomerge_test_mixed_plan_summary_from_wat`; after adding the test helper and fixture, the mixed plan classifies two `Freshen` writes, two canonical merge-feeding writes, two retargeted gets, and one canonical merge get for the same original body local.
    - [x] [SSANM-005c2] - Apply planned freshening for `local.set` in mixed merge regions
      - Status: completed 2026-06-13; a narrow normal structured-control `local.set` path now recursively applies the LocalGraph plan for mixed fresh/canonical regions.
      - Goal: admit a narrow normal structured-control family where LocalGraph can freshen only the eligible `local.set` writes while preserving merge-feeding writes and merge reads on the canonical slot.
      - Deliverables: public-pipeline tests proving canonical merge writes stay on the original local, single-source gets retarget to planned fresh locals, no predecessor-copy merge local is introduced, and direct compare evidence stays green.
      - Evidence: red-first public-pipeline fixture initially kept only the original body local; after implementation, it traces `structured-mixed-localgraph-plan`, freshens the early and post-merge `local.set`s to planned locals `2` and `3`, keeps both-arm merge-feeding writes and the merge read on canonical local `1`, and introduces no predecessor-copy merge local. Final validation: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `396/396`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2426/2426`; full `moon test` passed `5731/5731`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm005c2-mixed-10000` requested `10000`, compared `7605`, had `7605` normalized matches, `0` cleanup-normalized matches, `0` mismatches, and `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`) with cache hits (`wasm-smith` `3813/0`, Binaryen `7605/0`, Binaryen failures `20/0`).
    - [x] [SSANM-005c3] - Extend mixed-region classification to `local.tee`, branch exits, and nested control
      - Status: completed 2026-06-13 through `[SSANM-005c3a]` / `[SSANM-005c3b]` / `[SSANM-005c3c]`.
      - Goal: decide whether tee writes, early exits, branch tables, or nested structured regions can reuse the mixed per-write mutation path or must remain under `[SSANM-006a]` / `[SSANM-007*]` boundaries.
      - Deliverables: focused positive/fail-closed fixtures, docs/backlog updates, and direct compare evidence for any newly admitted mutation family.
    - [x] [SSANM-005c3a] - Admit `local.tee` in narrow mixed regions
      - Status: completed 2026-06-13; supported normal-control mixed regions with small `local.tee` write sets now use the existing structured LocalGraph rewrite cursor.
      - Goal: let supported normal-control mixed fresh/canonical regions rewrite planned `local.tee` writes through the existing structured LocalGraph cursor while preserving tee stack results.
      - Deliverables: red-first public-pipeline tee fixture, output validation, direct compare evidence, and docs/backlog updates that keep branch exits and nested boundary work separate.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because the mixed tee fixture stayed on the legacy path and kept only the original body local. After implementation, it traces `structured-mixed-localgraph-plan`, retargets the freshenable tee writes to planned locals `2` and `3`, keeps both merge-feeding `local.set`s on canonical local `1`, retargets single-source gets, and preserves the tee stack result. A narrow guard keeps tee-admission to at most four local writes so larger artifact-specific scratch/copy families remain on existing helpers. Final validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `397/397`; `moon test src/passes` passed `2427/2427`; full `moon test` passed `5732/5732`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm005c3a-tee-10000` requested `10000`, compared `7601`, had `7601` normalized matches, `0` cleanup-normalized matches, `0` mismatches, and `20` Binaryen/tool command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`) with cache hits (`wasm-smith` `3811/0`, Binaryen `7601/0`, Binaryen failures `20/0`).
    - [x] [SSANM-005c3b] - Classify branch exits and `br_table` in mixed regions
      - Status: completed 2026-06-13; mixed `br_if` early exits are covered as supported normal-control LocalGraph rewrites, while plain `br` and `br_table` mixed regions remain fail-closed for branch-alias/table helper owners.
      - Goal: decide which plain `br`, `br_if`, and `br_table` mixed-region shapes can reuse the planned mixed rewrite without bypassing branch-alias or typed-control copy helpers.
      - Deliverables: positive or fail-closed branch/table fixtures, raw-helper ownership notes, direct compare evidence for any admitted mutation family, and docs/backlog updates.
      - Evidence: added focused public-pipeline coverage in `src/passes/ssa_nomerge_test.mbt`: `ssa-nomerge LocalGraph freshens mixed br_if early-exit writes` proves a mixed region with an early `br_if 0` freshens only the pre-merge write to local `2`, keeps branch-merge writes on canonical local `1`, preserves `br_if`, and traces `structured-mixed-localgraph-plan`; `ssa-nomerge keeps mixed plain br regions off LocalGraph path` and `ssa-nomerge keeps mixed br_table regions off LocalGraph path` prove those shapes validate but do not use `structured-mixed-localgraph-plan`, keeping branch/table helper ownership explicit. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `402/402`. Red-first was not practical because the current narrow gate already admitted the `br_if` subset and rejected plain `br` / `br_table`; no pass behavior changed, so direct compare was not required for this classification slice.
    - [x] [SSANM-005c3c] - Classify nested supported mixed regions
      - Status: completed 2026-06-13; nested normal `block` / `if` mixed regions are covered as supported LocalGraph rewrites, while loop-containing mixed regions remain fail-closed for later structured-control / typed-loop owners.
      - Goal: separate nested normal `block`/`if` regions that can use the planned mixed rewrite from loops, typed-control targets, EH, and branch-operand boundaries owned by `[SSANM-006a]` / `[SSANM-007*]`.
      - Deliverables: positive/fail-closed nested-control fixtures, docs/backlog updates, and direct compare evidence for any newly admitted mutation family.
      - Evidence: added focused public-pipeline coverage in `src/passes/ssa_nomerge_test.mbt`: `ssa-nomerge LocalGraph freshens nested mixed block if writes` proves a nested normal `block` / `if` / `block` region freshens only planned non-merge writes to locals `2` and `3`, keeps nested merge-feeding writes on canonical local `1`, and traces `structured-mixed-localgraph-plan`; `ssa-nomerge keeps mixed loop regions off LocalGraph path` proves loop-containing mixed regions validate but do not use that path. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `402/402`. Red-first was not practical because the current scanner already recursively supported normal `block` / `if` and rejected loops; no pass behavior changed, so direct compare was not required for this classification slice.
    - [x] [SSANM-006a] - Drive normal structured control from LocalGraph policy
      - Status: completed 2026-06-14 through `[SSANM-006a1]` through `[SSANM-006a3]`; ordinary normal structured LocalGraph policy is now implemented for the supported `block` / `if` / mixed subsets and explicitly fail-closed for branch/table/nested target boundaries that still need typed-control, EH, huge-function, or later branch-helper owners.
      - Goal: replace branch-family raw heuristics with LocalGraph-planned rewrites for ordinary normal-flow `block`, `if`, `loop`, `br`, `br_if`, and `br_table` cases.
      - Deliverables: structured mutation that retargets only planned single-source gets, keeps merge regions canonical, validates after lowering, and records any raw helper family that can be deleted.
      - Evidence: child slices `[SSANM-006a1]` through `[SSANM-006a3d]` classify supported structured defaults, one-arm/both-arm/simple-loop canonical merges, narrow mixed normal control, ordinary block/if `local.set` freshening, merge-adjacent freshening, branch/table helper ownership, plain branch/table fail-closed routes, and nested branch-target fail-closed routes. Latest focused `ssa_nomerge_test.mbt` passed `416/416`; behavior-changing children earlier ran direct `--pass ssa-nomerge` compare lanes, while the final branch/table/nested classification children admitted no new mutation family.
    - [x] [SSANM-006a1] - Classify normal structured-control LocalGraph boundaries
      - Status: completed 2026-06-13; supported normal-flow families and fail-closed branch-operand/table boundaries are now explicitly mapped before `[SSANM-006a2]` changes ordinary structured mutation.
      - Goal: pin which ordinary `block`, `if`, loop, branch-exit, and branch-table shapes can safely reuse the LocalGraph rewrite plan after the merge-region slices, and which must remain in typed-control/EH fail-closed owners.
      - Deliverables: focused planner/shadow or public-pipeline fixtures for supported normal-flow families, fail-closed fixtures for unsupported branch operands or nested targets, and a table mapping legacy raw helper families to keep/delete/replace decisions.
      - Evidence: added focused fail-closed public-pipeline coverage in `src/passes/ssa_nomerge_test.mbt` for value-carrying `br` and `br_table` branch operands. The tests validate those normal structured-control boundary shapes, preserve the branch/table opcode, and assert they do not claim `structured-multisource-merge-localgraph-plan` or `structured-mixed-localgraph-plan`. Existing `[SSANM-005a]` through `[SSANM-005c3c]` tests remain the supported-family evidence for missing-else merges, both-arm/block-`br_if` merges, simple void-loop `br_if 0` backedge merges, mixed `br_if` early exits, nested normal `block`/`if`, and small `local.tee`. Source/docs review records the raw helper ownership table in the SSA no-merge wiki. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `406/406`; direct compare was not rerun because this classification added fail-closed boundary tests and docs without changing pass mutation behavior.
    - [x] [SSANM-006a2] - Apply planned structured `local.set` rewrites for ordinary control
      - Status: completed 2026-06-13 through `[SSANM-006a2a]` through `[SSANM-006a2c]`; ordinary `block` / `if` single-source and merge-adjacent `local.set` traffic now uses named LocalGraph-planned paths, and remaining legacy structured-helper ownership is narrowed to boundary families.
      - Goal: admit a narrow ordinary structured-control mutation path that recursively applies planned `local.set` freshening and single-source get retargeting while preserving canonical merge writes/reads.
      - Deliverables: public-pipeline tests for block/if local.set freshening around canonical merges, validation proof, no predecessor-copy merge locals, and direct compare evidence.
    - [x] [SSANM-006a2a] - Apply planned structured `local.set` freshening for no-merge block/if regions
      - Status: completed 2026-06-13; supported ordinary `block` / `if` regions with single-source `local.set` traffic now use `structured-localgraph-plan` instead of the legacy structured local-write helper.
      - Goal: route supported ordinary `block` / `if` regions with only single-source `local.set` traffic through the LocalGraph rewrite plan instead of the legacy structured local-write helper.
      - Deliverables: red-first public-pipeline tests proving nested normal block/if writes freshen to planned locals, single-source gets retarget, output validates, and no predecessor-copy or merge local appears.
      - Evidence: red-first focused `ssa_nomerge_test.mbt` failed because ordinary structured set fixtures kept only the original body local; after implementation, `src/passes/pass_manager.mbt` uses a stricter ordinary set gate for normal `block` / `if` shapes, rejects branches, returns, loops, EH, typed-control, `local.tee`, and traps, and applies the existing planned structured rewrite when the LocalGraph plan has freshened writes plus retargeted gets. Focused tests prove ordinary `if` and nested `block` set writes freshen to planned locals `2` and `3`, and an older body-local structured fixture now expects the Binaryen-style fresh locals instead of canonical reuse. Final validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `408/408`; `moon test src/passes` passed `2438/2438`; full `moon test` passed `5743/5743`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm006a2a-ordinary-10000` requested `10000`, compared `7605`, had `7605` normalized matches, `0` mismatches, `20` Binaryen/tool command failures, and cache hits.
    - [x] [SSANM-006a2b] - Preserve canonical merge writes while freshening adjacent ordinary `local.set`s
      - Status: completed 2026-06-13; explicit merge-adjacent ordinary `local.set` coverage now proves the existing mixed LocalGraph path keeps merge-feeding writes and merge reads canonical while freshening adjacent non-merge writes.
      - Goal: extend the structured planned path only for ordinary local-source regions where freshenable writes appear before or after canonical merge-feeding writes, without routing branch operands or table exits through the path.
      - Deliverables: public-pipeline tests for block/if freshening around canonical merges, explicit negative checks for value branches and table exits, validation proof, and direct compare evidence.
      - Evidence: added `ssa-nomerge LocalGraph preserves merge sets between ordinary fresh sets` in `src/passes/ssa_nomerge_test.mbt`. Red-first was not practical because `[SSANM-005c2]` had already implemented the underlying mixed path; this slice locks the explicit post-`[SSANM-006a2a]` ownership. The fixture proves planned fresh locals `2` / `3` around a canonical both-arm merge on local `1`, no temp/predecessor-copy merge local, and `structured-mixed-localgraph-plan`. Existing `[SSANM-006a1]` / `[SSANM-005c3b]` negative fixtures remain the value-branch/table-exit boundary proof. Validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `409/409`; `moon test src/passes` passed `2439/2439`; full `moon test` passed `5744/5744`; native `src/cmd` build passed; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm006a2b-merge-adjacent-10000` requested `10000`, compared `7605`, had `7605` normalized matches, `0` mismatches, `20` Binaryen/tool command failures, and cache hits.
    - [x] [SSANM-006a2c] - Narrow legacy structured local-write helper ownership after planned rewrites
      - Status: completed 2026-06-13; the legacy `structured-local-writes` / `structured-local-writes-mutated` summaries are now documented as boundary-helper owners rather than ordinary LocalGraph no-merge behavior.
      - Goal: after supported ordinary local-source rewrites use LocalGraph, document and narrow the remaining `structured-local-writes` / `structured-local-writes-mutated` families to branch-alias, typed-control, EH, huge-function, or explicit fail-closed owners.
      - Deliverables: source/test ownership table, trace summary updates if needed, docs/backlog cleanup, and direct compare evidence when behavior changes.
      - Evidence: added the `[SSANM-006a2c]` ownership table to `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`, tying ordinary completed families to named LocalGraph reasons and retaining legacy helper ownership only for branch/table, typed-control, EH, huge-function, and artifact/debug boundary work. No code behavior changed for this slice, so the `[SSANM-006a2b]` test/fuzz signoff covers the only executable change in the same work unit.
    - [x] [SSANM-006a3] - Extend ordinary structured control to branch exits, `br_table`, and nested supported regions
      - Status: completed 2026-06-14 through `[SSANM-006a3a]` through `[SSANM-006a3d]`; branch/table/nested target ownership is now explicit, with no new branch/table mutation admitted beyond the already-supported mixed `br_if` and simple loop-backedge subsets.
      - Goal: decide which plain `br`, `br_if`, `br_table`, and nested normal-flow regions can use the structured LocalGraph mutation path without bypassing branch-alias, typed-control, or EH safety helpers.
      - Deliverables: positive/fail-closed fixtures, docs/backlog updates, direct compare evidence for any newly admitted mutation family, and a delete/narrowing plan for superseded raw helpers.
      - Evidence: `[SSANM-006a3a]` inventories branch-exit helper ownership; `[SSANM-006a3b]` locks ordinary plain `br` and non-mixed/non-backedge `br_if`; `[SSANM-006a3c]` locks ordinary no-value `br_table`; `[SSANM-006a3d]` locks unsupported nested outer-target and loop-exit `br_if` shapes. Focused `ssa_nomerge_test.mbt` passed `416/416`; direct compare was not required for these fail-closed classification slices because they changed no mutation behavior.
    - [x] [SSANM-006a3a] - Inventory ordinary branch-exit helper ownership
      - Status: completed 2026-06-14; branch-exit helper ownership is now recorded in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` before any new branch mutation is admitted.
      - Goal: map current plain `br`, `br_if`, nested-target, and branch-value helper families before admitting any new LocalGraph structured mutation.
      - Deliverables: source/test table for existing branch-alias, structured-mixed, canonical-merge, and fail-closed paths; explicit boundary between ordinary local-source rewrites and typed-control branch-operand lowerings.
      - Evidence: source/test review only. The inventory records the already-supported mixed `br_if` early-exit and simple void-loop direct `br_if 0` backedge families, and keeps ordinary `br` / extra `br_if`, `br_table`, value branch operands, typed ref/cast branches, EH, and broader nested/loop targets assigned to `[SSANM-006a3b]`, `[SSANM-006a3c]`, `[SSANM-006a3d]`, `[SSANM-007a*]`, or `[SSANM-007b*]`. Tests/fuzz were not run because this slice added ownership inventory only and changed no executable behavior.
    - [x] [SSANM-006a3b] - Classify plain `br` and `br_if` ordinary local-source regions
      - Status: completed 2026-06-14; ordinary plain `br` and non-mixed/non-backedge `br_if` local-source regions are now locked as fail-closed from the planned structured LocalGraph paths.
      - Goal: decide which void/plain branch-exit regions with only local-source traffic can use planned structured LocalGraph rewrites, while preserving branch-alias helpers for value operands and typed targets.
      - Deliverables: positive or fail-closed public-pipeline fixtures, trace assertions, validation proof, and direct compare evidence for any newly admitted mutation family.
      - Evidence: added focused public-pipeline tests `ssa-nomerge keeps ordinary plain br local-source regions off planned structured path` and `ssa-nomerge keeps ordinary br_if local-source regions off planned structured path` in `src/passes/ssa_nomerge_test.mbt`. They validate the branch fixtures, preserve `br` / `br_if`, and assert the ordinary `structured-localgraph-plan`, mixed `structured-mixed-localgraph-plan`, and loop-backedge reason where applicable are not claimed. Final validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `411/411`; `moon test src/passes` passed `2441/2441`; direct compare was not run because this slice only locks existing fail-closed routing and admits no new mutation family.
    - [x] [SSANM-006a3c] - Classify ordinary `br_table` local-source regions
      - Status: completed 2026-06-14; ordinary no-value `br_table` local-source regions are now locked as fail-closed from planned structured LocalGraph paths.
      - Goal: decide whether table exits without typed/value operands can use planned structured LocalGraph policy, or should remain fail-closed until typed branch-table lowering is narrowed.
      - Deliverables: table-exit positive/fail-closed fixtures, branch-table helper ownership docs, validation proof, and direct compare evidence for any newly admitted mutation family.
      - Evidence: added focused public-pipeline test `ssa-nomerge keeps ordinary br_table local-source regions off planned structured path` in `src/passes/ssa_nomerge_test.mbt`. It validates the table-exit fixture, preserves `br_table`, and asserts the ordinary structured, mixed, multi-source, and loop-backedge LocalGraph reasons are not claimed. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `412/412`. Direct compare was not run because this slice only locks existing fail-closed routing and admits no new mutation family.
    - [x] [SSANM-006a3d] - Expand nested normal branch-target regions safely
      - Status: completed 2026-06-14 through `[SSANM-006a3d1]`, `[SSANM-006a3d2]`, and `[SSANM-006a3d3]`; nested normal branch-target combinations now have an explicit supported/fail-closed ownership matrix.
      - Goal: classify nested `block` / `if` / loop-target combinations that are not covered by `[SSANM-005c3c]` or `[SSANM-006a2]`, without routing typed loops, EH, or branch operands through ordinary LocalGraph mutation.
      - Deliverables: nested-target fixture matrix, docs/backlog updates, and a narrowing/deletion plan for any superseded raw structured helpers.
      - Evidence: added the `[SSANM-006a3d]` nested-target ownership table to `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` and focused fail-closed tests `ssa-nomerge keeps nested br_if to outer block off planned structured path` plus `ssa-nomerge keeps nested loop-exit br_if off planned structured path` in `src/passes/ssa_nomerge_test.mbt`. The tests validate, preserve `br_if`, and assert that ordinary structured, mixed, multi-source, and loop-backedge LocalGraph reasons are not claimed for unsupported nested targets. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `416/416`. Direct compare was not run because this slice only locks existing fail-closed routing and admits no new mutation family.
    - [x] [SSANM-006a3d1] - Inventory nested normal branch-target ownership
      - Status: completed 2026-06-14.
      - Goal: map nested `block` / `if` branch-target combinations against the already-supported mixed/nested LocalGraph path, ordinary `block` / `if` local.set path, and fail-closed branch/table/typed/EH boundaries.
      - Deliverables: source/test ownership table in the SSA no-merge implementation docs, with explicit candidate families for executable locks or later mutation.
      - Evidence: `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` now separates supported nested normal `block` / `if` no-branch and mixed-path rewrites from nested outer-target `br_if`, loop-containing exits, typed branch operands, EH, and `br_table` boundaries.
    - [x] [SSANM-006a3d2] - Lock unsupported nested target shapes off planned LocalGraph mutation
      - Status: completed 2026-06-14.
      - Goal: prove nested branch targets that are not already covered by `[SSANM-005c3c]`, `[SSANM-006a2]`, or the simple loop-backedge subset validate without claiming ordinary planned LocalGraph reasons.
      - Deliverables: focused fail-closed public-pipeline fixtures, trace assertions, validation proof, and direct compare evidence only if a new mutation family is admitted.
      - Evidence: `src/passes/ssa_nomerge_test.mbt` now locks nested outer-block `br_if` and nested loop-exit `br_if` shapes off `structured-localgraph-plan`, `structured-mixed-localgraph-plan`, `structured-multisource-merge-localgraph-plan`, and `structured-loop-backedge-merge-localgraph-plan`; focused `ssa_nomerge_test.mbt` passed `416/416`.
    - [x] [SSANM-006a3d3] - Close nested-target helper narrowing docs
      - Status: completed 2026-06-14.
      - Goal: summarize the supported and fail-closed nested-target boundary after `[SSANM-006a3d1]` / `[SSANM-006a3d2]`, including which raw structured helpers remain necessary.
      - Deliverables: docs/backlog closeout, wiki log entry, and comparison note explaining why fuzz was or was not required.
      - Evidence: refreshed the SSA no-merge implementation, parity, index, and wiki log pages. Remaining nested/loop-target mutation, if desired, should start from red-first positive fixtures and direct `--pass ssa-nomerge` compare evidence.
    - [x] [SSANM-006b] - Retire predecessor-copy behavior from no-merge paths
      - Status: completed 2026-06-14; `[SSANM-006b1]`, `[SSANM-006b2a]` through `[SSANM-006b2d]`, and `[SSANM-006b3]` now inventory, reroute, classify, and narrow ordinary predecessor-copy behavior away from public `ssa-nomerge` claims.
      - Goal: ensure `ssa-nomerge` no longer relies on HOT SSA destruction that externalizes overlay phis through predecessor copies for ordinary no-merge work.
      - Deliverables: summary string/docs update, tests proving merge-copy shapes belong outside `ssa-nomerge`, and a clear route for any remaining HOT SSA destruction users to full `ssa` or a separate helper.
      - Evidence: ordinary completed straight-line/default/structured no-merge families now use named LocalGraph/no-write/no-op boundary reasons instead of lifted HOT SSA destruction; retained branch/table/typed/EH copy-like helpers are documented as explicit boundary owners; public summary/help now says `Freshen single-source locals and materialize defaults while preserving merge traffic.`; red-first `registry_test.mbt` failed on the old predecessor-copy summary and passed after the summary update; focused `ssa_nomerge_test.mbt` stayed green (`425/425`). Direct compare was not rerun for the final closeout because `[SSANM-006b3]` changed summary/help text and docs only, not pass mutation or wasm output.
    - [x] [SSANM-006b1] - Inventory predecessor-copy-producing no-merge paths
      - Status: completed 2026-06-13; the inventory is durable in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`.
      - Goal: identify every remaining `ssa-nomerge` path that can still create predecessor copies, merge locals, or proxy/scratch copies for ordinary LocalGraph-supported local-source work.
      - Deliverables: source/test map of HOT SSA destruction entry points, raw helper families that still intentionally allocate branch/typed-control scratch locals, and public-pipeline fixtures that distinguish ordinary no-merge predecessor-copy drift from accepted typed-control/EH boundaries.
      - Evidence: source/test review only. Remaining copy-producing risk is the fallback HOT `ssa_nomerge_run` / `@ir.ssa_destroy_into_hot(...)` path and raw branch/typed-control scratch/proxy helpers in `src/passes/pass_manager.mbt`; ordinary LocalGraph-supported straight-line, default, canonical-merge, mixed normal-control, and `[SSANM-006a2a]` block/if set families are now explicitly mapped away from predecessor-copy behavior. No extra tests were required beyond the `[SSANM-006a2a]` validation because this slice recorded source ownership rather than changing additional pass behavior.
    - [x] [SSANM-006b2] - Reroute ordinary no-merge families away from predecessor copies
      - Status: completed 2026-06-14; straight-line/default families are locked off HOT fallback, branch/table/typed/EH scratch helpers are classified as boundary ownership, and `[SSANM-006b2c]` closed the remaining structured fallback reroute / retained-boundary work.
      - Goal: make ordinary LocalGraph-supported no-merge traffic use planned canonical/fresh/default rewrites instead of HOT SSA destruction or predecessor-copy merge materialization.
      - Deliverables: red-first fixtures for any ordinary family still entering copy-producing paths, implementation that preserves canonical merge reads/writes without merge locals, and direct compare evidence.
    - [x] [SSANM-006b2a] - Census fallback HOT SSA-destruction survivors
      - Status: completed 2026-06-14; source/test census is recorded in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`.
      - Goal: find reduced ordinary `ssa-nomerge` fixtures that still return `None` from the raw dispatcher and fall into `ssa_destroy_into_hot`, separating true ordinary gaps from branch/typed/EH/huge boundaries.
      - Deliverables: source/test inventory table, focused telemetry or whitebox assertions only where they guard routing behavior, and explicit candidate list for `[SSANM-006b2b]` / `[SSANM-006b2c]`.
      - Evidence: source review of `src/passes/pass_manager.mbt` shows completed no-write, straight-line, default, canonical merge, mixed normal-control, and ordinary block/if families return named raw LocalGraph reasons instead of falling through to HOT SSA destruction. Remaining true fallback-risk candidates are non-simple loop-carried `br_if` shapes that deliberately defer to HOT and legacy structured helper rejection/invalid-escape-carrier paths; branch-table, typed-control, EH, and huge-function cases remain boundary owners rather than ordinary predecessor-copy work. No executable tests were added because this slice changed docs/source inventory only; the adjacent `[SSANM-006a3c]` focused test guards the newly classified table boundary.
    - [x] [SSANM-006b2b] - Lock straight-line and default families off HOT SSA destruction
      - Status: completed 2026-06-14; straight-line, no-write, default-entry, and unreachable-debris families now have focused trace-lock fixtures proving they return named raw LocalGraph/no-write exits instead of falling through to lifted HOT SSA destruction.
      - Goal: prove completed straight-line `local.set` / `local.tee`, default-entry, and no-local-write families cannot regress into predecessor-copy fallback.
      - Deliverables: focused public-pipeline or dispatcher tests with trace assertions for named LocalGraph reasons, plus docs/backlog updates; behavior mutation only if the census finds a real fallback.
      - Evidence: added `ssa-nomerge locks straight-line writes off HOT fallback` and `ssa-nomerge locks no-write default families off HOT fallback` in `src/passes/ssa_nomerge_test.mbt`. The tests assert the named raw/no-write trace reasons `straight-line-local-writes-localgraph-plan`, `no-local-writes`, `default-local-reads-localgraph-plan`, and `no-local-writes-unreachable-debris`, plus no `pass[ssa-nomerge]:mutated` or `skip-invalid-lower` trace. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `414/414`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2444/2444`. Direct compare was not run because this is a regression-lock test/docs slice with no behavior mutation.
    - [x] [SSANM-006b2c] - Reroute structured ordinary merge/freshening fallback gaps
      - Status: completed 2026-06-14; `[SSANM-006b2c1]` and `[SSANM-006b2c2]` admit branch-free decorated void-loop `br_if 0` backedges, while `[SSANM-006b2c3]` / `[SSANM-006b2c4]` lock and document retained branch/table and invalid-carrier boundaries.
      - Goal: move any remaining ordinary `block` / `if` / supported `br_if` / loop-backedge families found by the census onto the planned LocalGraph paths without introducing merge locals.
      - Deliverables: red-first fixtures for each admitted family, implementation, validation proof, and direct `--pass ssa-nomerge` compare evidence.
    - [x] [SSANM-006b2c1] - Reduce ordinary structured fallback survivors
      - Status: completed 2026-06-14; reduced the non-simple loop-carried candidate to a void-loop `br_if 0` canonical merge with branch-free block decoration before the backedge, and classified it as ordinary LocalGraph-supported no-merge work.
      - Goal: turn the `[SSANM-006b2a]` census candidates into minimal fixtures and decide which are ordinary LocalGraph-supported no-merge work versus branch/table/typed/EH/huge boundaries.
      - Deliverables: focused trace or whitebox fixtures for non-simple loop-carried `br_if`, legacy structured helper rejection, and invalid-escape-carrier examples; no mutation unless a reduced case proves ordinary local-source ownership.
      - Evidence: new red-first public-pipeline fixture `ssa-nomerge LocalGraph preserves decorated loop-carried br_if merge canonical` failed before implementation with three i32 locals instead of the single canonical local, proving the old path fell through to predecessor-copy-producing HOT SSA destruction. Legacy structured helper rejection / invalid-escape-carrier candidates remain for `[SSANM-006b2c3]` because this reduced positive did not cover them.
    - [x] [SSANM-006b2c2] - Admit planned LocalGraph reroutes for ordinary survivors
      - Status: completed 2026-06-14 for branch-free decorated void-loop backedge merges.
      - Goal: route any `[SSANM-006b2c1]` ordinary positive family through planned no-merge rewrites without predecessor-copy materialization.
      - Deliverables: red-first public-pipeline fixtures, implementation, validation proof, and direct `--pass ssa-nomerge` compare evidence for each admitted mutation family.
      - Evidence: `src/passes/pass_manager.mbt` now lets the loop-backedge canonical-only gate tolerate branch-free block decoration inside void-loop bodies while still rejecting nested branches, nested blocks, nested loops, `br_table`, typed ref/cast branches, and `try_table`. The focused fixture now returns `structured-loop-backedge-merge-localgraph-plan`, keeps only the original local, preserves all reads/writes on local 0, and rejects `pass[ssa-nomerge]:mutated`. Validation: red-first focused test failed before implementation; final `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `423/423`; `moon test src/passes` passed `2453/2453`; full `moon test` passed `5758/5758`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-006b2c-10000` requested 10000 cases with `--jobs auto` and the prebuilt native Starshine binary, compared 9977 cases, had 9977 normalized matches, 0 mismatches, 0 validation/property/generator failures, and 23 Binaryen command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1) with cache hits (`wasm-smith` 5000/0, Binaryen 9977/0, Binaryen failures 23/0).
    - [x] [SSANM-006b2c3] - Lock retained fallback boundaries fail-closed
      - Status: completed 2026-06-14; `[SSANM-006b2c3a]` added branch/table decorated loop-backedge boundary locks, `[SSANM-006b2c3b]` recorded the invalid-carrier lowering-safety boundary, and `[SSANM-006b2c3c]` closed the retained-boundary notes.
      - Goal: keep reduced survivor families that are not ordinary no-merge work out of lifted HOT SSA destruction and out of misleading LocalGraph success reasons.
      - Deliverables: fail-closed fixtures or source-backed notes for branch/table/typed/EH/huge or invalid-carrier survivors, with trace assertions and direct compare only if behavior changes.
    - [x] [SSANM-006b2c3a] - Lock structured-helper rejection boundaries
      - Status: completed 2026-06-14.
      - Goal: prove branch/table/typed/EH survivors rejected by the ordinary structured reroute gates preserve their boundary routing and do not report misleading ordinary LocalGraph success reasons.
      - Deliverables: focused fail-closed trace fixtures or source-backed notes for representative legacy structured-helper rejection shapes, with validation and no direct compare unless behavior changes.
      - Evidence: added red-first public-pipeline fixtures `ssa-nomerge rejects branchy decorated loop-backedge boundary reasons` and `ssa-nomerge rejects table decorated loop-backedge boundary reasons`. Both initially failed because they fell through to lifted HOT SSA destruction and reported `pass[ssa-nomerge]:mutated`; `src/passes/pass_manager.mbt` now returns unchanged with `structured-loop-backedge-boundary-noop` for loop-carried branch/table decoration boundaries while still avoiding ordinary planned LocalGraph reasons. Final validation: `moon fmt`; `moon info` passed with three pre-existing GenValid warnings; focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `425/425`; `moon test src/passes` passed `2455/2455`; full `moon test` passed `5760/5760`; native `moon build --target native --release src/cmd` passed with pre-existing warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-006b2c-boundary-final-10000` requested 10000, compared 9977, had 9977 normalized matches, 0 mismatches, and 23 Binaryen/tool command failures.
    - [x] [SSANM-006b2c3b] - Lock invalid-escape-carrier fallback boundary
      - Status: completed 2026-06-14.
      - Goal: document or test the invalid lowered-carrier path that can still skip raw rewriting so it is treated as a lowering safety boundary rather than ordinary no-merge predecessor-copy work.
      - Deliverables: reduced trace lock or source-backed no-op rationale covering `skip-invalid-lower` / invalid escape carriers, with direct compare only if behavior changes.
      - Evidence: source review in `src/passes/pass_manager.mbt` shows invalid lowered carriers are detected after raw structured rewriting and again in the generic `ssa-nomerge` writeback skip guard. The no-default-materialization invalid-carrier path remains a lowering/writeback safety boundary rather than ordinary LocalGraph local-source work; no reduced source-WAT fixture was added because the detector matches lowered block/carrier structure rather than a stable source syntax family.
    - [x] [SSANM-006b2c3c] - Close retained fallback boundary notes
      - Status: completed 2026-06-14.
      - Goal: summarize the fail-closed structured-helper and invalid-carrier boundary decisions before `[SSANM-006b2c4]` closes the broader structured reroute docs.
      - Deliverables: backlog/docs evidence, direct-compare explanation, and reopening criteria for any retained fallback-risk boundary.
      - Evidence: `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` now records the admitted decorated-loop reroute, branch/table decorated boundary no-op reason, invalid-carrier source boundary, reopening criteria, and the validation/direct-compare requirement for any future boundary admission.
    - [x] [SSANM-006b2c4] - Close structured fallback reroute docs
      - Status: completed 2026-06-14; `[SSANM-006b3]` is now unblocked to inventory and narrow remaining predecessor-copy wording/helper surfaces.
      - Goal: summarize admitted reroutes and retained boundaries, update parity/index/wiki-log notes, and decide whether `[SSANM-006b3]` can retire or narrow legacy predecessor-copy wording.
      - Deliverables: docs/backlog closeout, direct-compare explanation, and reopening criteria for any retained ordinary fallback risk.
      - Evidence: updated the ssa-nomerge implementation map, parity page, index, wiki log, and backlog to close the structured fallback reroute subtrack. Final validation and direct compare evidence is recorded on `[SSANM-006b2c3a]`.
    - [x] [SSANM-006b2d] - Prove branch/table/typed/EH scratch paths are not ordinary predecessor-copy work
      - Status: completed 2026-06-14; branch/table/typed/EH copy-like raw helpers are now documented as explicit boundary owners rather than ordinary no-merge predecessor-copy materialization.
      - Goal: keep branch-alias, `br_table`, typed-control, and EH scratch/proxy lowerings classified as explicit boundary helpers or fail-closed paths rather than ordinary no-merge predecessor-copy materialization.
      - Deliverables: boundary regression fixtures or source-backed table, cross-links to `[SSANM-006a3*]`, `[SSANM-007a*]`, and `[SSANM-007b*]`, and direct compare evidence only if behavior changes.
      - Evidence: added the `[SSANM-006b2d]` source/test classification table to `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`. It maps branch-alias copies, `br_table` alias/scratch/proxy helpers, typed loop param/result stores, ref/null/cast branch spills, and EH/`try_table` boundary helpers to `[SSANM-006a3*]`, `[SSANM-007a*]`, and `[SSANM-007b*]` instead of ordinary `[SSANM-006b2c]` rerouting. Existing focused tests already guard the listed helper families; focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `414/414`. Direct compare was not run because this slice changed docs/source classification only and admitted no new mutation family.
    - [x] [SSANM-006b3] - Retire or narrow legacy predecessor-copy helpers
      - Status: completed 2026-06-14; summary/help now describes no-merge freshening/default/canonical-merge behavior, and retained predecessor-copy/copy-like helper surfaces are documented as fallback, boundary, or sibling full-`ssa` ownership.
      - Goal: after ordinary no-merge families are rerouted, delete or explicitly narrow leftover no-merge predecessor-copy helpers and update pass summaries/docs so predecessor copies are no longer described as normal `ssa-nomerge` behavior.
      - Deliverables: summary/help/docs updates, tests proving retained scratch/copy lowerings are typed-control/EH/full-SSA or explicitly fail-closed boundaries, and backlog cleanup before closing `[SSANM-006b]`.
      - Evidence: `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` now has the `[SSANM-006b3]` reachability table for summary/help text, retained lifted HOT fallback, shared `ssa_destroy` predecessor-copy helpers, raw branch/table/typed/EH copy-like helpers, and stale HOT-IR strategy wording. Red-first focused `registry_test.mbt` failed against the old summary, then passed after `ssa_nomerge_summary()` changed; focused `ssa_nomerge_test.mbt` passed `425/425`. Direct compare was not run because no mutation, trace routing, or wasm output changed.
    - [x] [SSANM-006b3a] - Inventory legacy predecessor-copy wording and helper reachability
      - Status: completed 2026-06-14.
      - Goal: after `[SSANM-006b2c]`, map any remaining `ssa-nomerge` docs, summaries, public traces, and helper paths that still imply ordinary predecessor-copy materialization.
      - Deliverables: source/docs table distinguishing dead/superseded helper wording from retained typed-control/EH/full-SSA boundary helper ownership.
      - Evidence: inventory table in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` classifies public summary/help as stale ordinary wording, `ssa_nomerge_run(...)` / `ssa_destroy_into_hot(...)` as retained fallback risk, `src/ir/ssa_destroy.mbt` predecessor-copy helpers as shared SSA/full-`ssa` infrastructure, and raw branch/table/typed/EH copy-like helpers as explicit ABI boundaries.
    - [x] [SSANM-006b3b] - Narrow or delete ordinary no-merge predecessor-copy surfaces
      - Status: completed 2026-06-14.
      - Goal: remove dead ordinary predecessor-copy helper routes, or narrow names/comments/guards so retained copy-like helpers are not advertised as normal `ssa-nomerge` behavior.
      - Deliverables: red-first or regression-lock tests for every changed public trace/helper route, implementation/docs updates, and direct compare evidence if behavior changes.
      - Evidence: added red-first public registry coverage for the intended summary/help wording; `ssa_nomerge_summary()` now returns `Freshen single-source locals and materialize defaults while preserving merge traffic.` and the registry entry carries the same text. Raw trace/helper names were not changed because retained `structured-local-writes-mutated` and scratch/proxy helpers still classify branch/table/typed/EH ABI repairs, not ordinary no-merge predecessor copies.
    - [x] [SSANM-006b3c] - Refresh public summaries and close predecessor-copy retirement docs
      - Status: completed 2026-06-14.
      - Goal: align pass summaries, wiki pages, and backlog wording with the final retained-helper boundary after `[SSANM-006b3a]` / `[SSANM-006b3b]`.
      - Deliverables: docs/wiki/log closeout, reopening criteria for retained helper families, and final validation notes before closing `[SSANM-006b]`.
      - Evidence: updated the SSA no-merge implementation map, HOT-IR strategy page, parity page, index, wiki log, and this backlog. Reopen only if an ordinary LocalGraph-supported family again reaches lifted HOT SSA destruction, public docs/help reintroduce predecessor copies as normal no-merge behavior, or a retained boundary helper is broadened without red-first fixtures and direct compare evidence.
    - [x] [SSANM-007a] - Keep exceptional-edge boundaries explicit
      - Status: completed 2026-06-14 through `[SSANM-007a1]` through `[SSANM-007a3]`; real throwing EH is locked fail-closed, while existing no-throw `try_table` subsets are classified as ordinary normal-flow local-write or typed-control helper work.
      - Goal: preserve safety for `try`, `try_table`, `throw`, `throw_ref`, `rethrow`, `delegate`, catches, calls inside no-throw `try_table` bodies, and any path where LocalGraph does not model exceptional successors.
      - Deliverables: fail-closed tests for real exceptional-flow families, positive tests only for source-backed no-exception normal-flow subsets, and a Binaryen-source note explaining whether the boundary is Starshine tooling debt or an intentional no-mutation subset.
      - Evidence: `[SSANM-007a2]` locks direct/conditional throwing `try_table`, `throw_ref`, legacy catch/rethrow, and delegate boundaries as no-mutation/fail-closed. `[SSANM-007a3]` keeps no-throw `try_table` local writes admitted only when `run_hot_pipeline_raw_try_table_body_has_exceptional_edge(...)` sees no throw/call-bearing edge, adds a call-bearing fail-closed contrast, and documents no-throw typed-loop/proxy/store rewrites as typed-control helper ownership rather than real EH SSA support. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `420/420`. Direct compare was not run for the closeout because the final slice added regression locks/docs only and admitted no new mutation family.
    - [x] [SSANM-007a1] - Refresh EH source and local boundary inventory
      - Status: completed 2026-06-14; source/test inventory lives in `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`.
      - Goal: pin the Binaryen no-merge source/test evidence for exceptional edges against Starshine's current normal-flow-only LocalGraph boundary.
      - Deliverables: source-backed note for `try`, `try_table`, `throw`, `throw_ref`, `rethrow`, `delegate`, catches, and no-throw bodies; map each current `ssa_nomerge_test.mbt` EH guard to fail-closed, legacy scratch/copy, or candidate no-throw subset.
      - Evidence: source/test review mapped `ssa_nomerge_has_exceptional_flow(...)`, `run_hot_pipeline_raw_try_table_body_has_exceptional_edge(...)`, no-throw `try_table` raw helpers, and current EH tests into fail-closed throwing flows, no-throw normal-flow candidate/rewrite subsets, and legacy exceptional terminator HOT no-mutation guards. Focused `ssa_nomerge_test.mbt` passed `418/418` after the adjacent `[SSANM-007a2a]` locks.
    - [x] [SSANM-007a2] - Lock throwing exceptional-flow fail-closed behavior
      - Status: completed 2026-06-14 through `[SSANM-007a2a]` through `[SSANM-007a2c]`; throwing `try_table`, `throw_ref`, legacy catch/rethrow, and delegate boundaries are locked fail-closed, with no new EH mutation admitted.
      - Goal: make real exceptional exits stay outside LocalGraph no-merge mutation until EH successors are modeled or an explicit repair helper exists.
      - Deliverables: focused fail-closed public-pipeline fixtures for throwing `try_table` / catch / throw-ref-style families, trace assertions that the planned structured LocalGraph path is not used, and docs/backlog updates.
      - Evidence: `[SSANM-007a2a]` added public-pipeline throwing `try_table` catch/catch-all locks. `[SSANM-007a2b]` added a direct HOT `throw_ref` no-revision fixture and strengthened the legacy catch/rethrow public-pipeline trace lock, while the direct HOT delegate fixture remains the delegate boundary proof. `[SSANM-007a2c]` closes the docs around locked throwing EH families, no-throw `try_table` candidates, and reopening criteria. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `419/419`. Direct compare was not run because this fail-closed regression-lock/docs slice admitted no new mutation family.
    - [x] [SSANM-007a2a] - Lock throwing `try_table` catch-target exits
      - Status: completed 2026-06-14.
      - Goal: prove `try_table` bodies that can throw to an enclosing catch target remain fail-closed from ordinary planned LocalGraph mutation while preserving the local writes observed at the catch continuation.
      - Deliverables: public-pipeline fixtures for direct and conditional `throw` to `catch` / `catch_all` targets, trace assertions against ordinary structured, mixed, multi-source, and loop-backedge LocalGraph reasons, validation proof, and docs/backlog updates.
      - Evidence: added focused public-pipeline tests `ssa-nomerge keeps throwing try_table catch exits off planned structured path` and `ssa-nomerge keeps conditional catch_all throws off planned structured path`. They validate, preserve `local.set` plus `throw`, reject `pass[ssa-nomerge]:mutated`, and assert the throwing EH fixtures do not claim ordinary structured, mixed, multi-source, or loop-backedge LocalGraph reasons. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `418/418`. Direct compare was not run because this slice only locks existing fail-closed routing and admits no new mutation family.
    - [x] [SSANM-007a2b] - Lock legacy exceptional terminators off HOT mutation
      - Status: completed 2026-06-14 through `[SSANM-007a2b1]` through `[SSANM-007a2b3]`.
      - Goal: keep `throw_ref`, legacy `try` / `catch` / `rethrow`, and `delegate` shapes outside HOT SSA-destruction mutation until exceptional successors are represented in the local SSA contract.
      - Deliverables: focused HOT or public-pipeline fixtures where WAT lowering supports them, local-traffic preservation assertions, no-revision/no-mutation assertions, and docs/backlog updates.
      - Evidence: focused `ssa_nomerge_test.mbt` now has `ssa-nomerge preserves throw_ref shapes without hot local corruption`, a strengthened `ssa-nomerge preserves legacy rethrow shapes without hot local corruption` trace/no-mutation lock, and the existing direct HOT delegate no-revision fixture. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `419/419`.
    - [x] [SSANM-007a2b1] - Lock `throw_ref` off HOT mutation
      - Status: completed 2026-06-14.
      - Goal: prove `throw_ref` roots preserve nearby local writes and do not advance the HOT revision under `ssa-nomerge`.
      - Deliverables: direct HOT fixture with an `exnref` operand, local-write preservation assertion, no-revision/no-mutation assertion, and docs/backlog updates.
      - Evidence: `ssa-nomerge preserves throw_ref shapes without hot local corruption` builds a direct HOT `throw_ref` root with an `exnref` operand, preserves a neighboring `local.set`, and asserts the `ssa-nomerge` HOT revision is unchanged.
    - [x] [SSANM-007a2b2] - Lock legacy `try` / `catch` / `rethrow` off HOT mutation
      - Status: completed 2026-06-14.
      - Goal: prove legacy catch/rethrow flow remains a no-mutation boundary in both HOT-only and public-pipeline routing.
      - Deliverables: public-pipeline trace fixture where WAT lowering supports it, direct HOT no-revision assertion, local-traffic preservation assertion, and docs/backlog updates.
      - Evidence: `ssa-nomerge preserves legacy rethrow shapes without hot local corruption` preserves local traffic through the HOT-only path and then verifies the public pipeline reports neither ordinary planned structured LocalGraph reasons nor `pass[ssa-nomerge]:mutated`.
    - [x] [SSANM-007a2b3] - Lock `delegate` off HOT mutation
      - Status: completed 2026-06-14.
      - Goal: prove delegate terminators remain direct HOT no-mutation boundaries until exceptional delegation targets participate in the SSA local contract.
      - Deliverables: direct HOT delegate fixture, local-write preservation assertion, no-revision/no-mutation assertion, and docs/backlog updates.
      - Evidence: `ssa-nomerge preserves delegate shapes without hot local corruption` builds the delegate HOT shape directly because nested legacy delegate WAT does not survive current lift/lower, then asserts local traffic and unchanged HOT revision.
    - [x] [SSANM-007a2c] - Close throwing EH fail-closed docs
      - Status: completed 2026-06-14.
      - Goal: summarize which throwing EH families are locked fail-closed, which no-throw EH-body subsets remain candidates for `[SSANM-007a3]`, and which APIs would be required to reopen real exceptional-flow mutation.
      - Deliverables: SSA no-merge implementation/parity/index/wiki-log updates, backlog cleanup for `[SSANM-007a2]`, and a comparison note explaining why direct fuzz was or was not required.
      - Evidence: docs summarize direct/conditional throwing `try_table`, `throw_ref`, legacy catch/rethrow, and delegate boundaries as fail-closed; no-throw `try_table` normal-flow candidates were left for `[SSANM-007a3]`, which later classified them; real EH mutation requires exceptional successor/reaching-set support or an EH-aware repair helper. Direct compare was not run because no behavior mutation was admitted.
    - [x] [SSANM-007a3] - Classify no-throw `try_table` and EH-body normal-flow subsets
      - Status: completed 2026-06-14 through `[SSANM-007a3a]` through `[SSANM-007a3c]`.
      - Goal: decide whether no-throw EH bodies can reuse normal-flow LocalGraph rewrites, or whether all EH containers should remain fail-closed for v0.1.0.
      - Deliverables: positive or deliberately fail-closed no-throw `try_table` fixtures, direct compare evidence for any admitted mutation family, and reopening criteria for remaining EH boundaries.
      - Evidence: source/test inventory now keeps three categories explicit: ordinary no-throw `try_table` local writes through the existing structured local-write raw path; call/throw-bearing `try_table` bodies fail-closed because the helper treats calls/throws as exceptional-edge risks; and no-throw typed-loop/proxy/store rewrites through typed-control helpers. Focused tests strengthen the ordinary no-throw local-write fixture with a trace assertion, add `ssa-nomerge keeps call-bearing try_table bodies off no-throw local rewrite`, and strengthen the no-throw scalar proxy backedge fixture with trace/validation. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `420/420`. Direct compare was not run because no pass behavior changed and no new mutation family was admitted.
    - [x] [SSANM-007a3a] - Inventory no-throw `try_table` normal-flow owners
      - Status: completed 2026-06-14.
      - Goal: classify current no-throw `try_table` support into ordinary local-source rewrites, typed-loop helper rewrites, and fail-closed call/throw-bearing boundaries before changing behavior.
      - Deliverables: source/test table for `run_hot_pipeline_raw_try_table_body_has_exceptional_edge(...)`, ordinary no-throw local writes, typed-loop/proxy/store helpers, and call/throw rejection; no mutation unless the inventory finds an unowned gap.
      - Evidence: `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` now maps the no-throw body owner surface to `run_hot_pipeline_raw_try_table_body_has_exceptional_edge(...)`, `structured-local-writes`, typed-loop helper paths, and call/throw fail-closed boundaries.
    - [x] [SSANM-007a3b] - Lock ordinary no-throw `try_table` local-write admission
      - Status: completed 2026-06-14.
      - Goal: keep the no-throw `try_table` ordinary local-write subset as normal-flow work only when the body has no exceptional/call-bearing edge.
      - Deliverables: focused public-pipeline trace/output fixtures, explicit contrast with throwing/call-bearing bodies, validation proof, and direct compare only if behavior changes.
      - Evidence: `ssa-nomerge rewrites no-throw try_table local writes` now asserts the existing `structured-local-writes` trace/output shape, while `ssa-nomerge keeps call-bearing try_table bodies off no-throw local rewrite` proves call-bearing bodies do not take that rewrite and preserve call/local traffic without mutation.
    - [x] [SSANM-007a3c] - Lock no-throw typed-loop helper ownership and close out EH-body subsets
      - Status: completed 2026-06-14.
      - Goal: document and test that no-throw `try_table` typed-loop/proxy/store rewrites are typed-control helper work, not proof of real exceptional-edge SSA support.
      - Deliverables: focused trace/output fixtures or source-backed table for scalar proxy, multi-param/result, catch relabeling, and store-model helpers; closeout docs/backlog updates for `[SSANM-007a3]` and parent `[SSANM-007a]` if no EH work remains.
      - Evidence: the no-throw scalar proxy backedge fixture now records trace/validation while still asserting the loop-param scalar proxy helper shape; existing multi-param/result, catch-relabeling, and store-model fixtures remain the typed-control helper proof. Docs now close `[SSANM-007a3]` and parent `[SSANM-007a]` with real EH reopening criteria.
    - [x] [SSANM-007b] - Classify typed-control and loop-param/result boundaries
      - Status: completed 2026-06-14 through `[SSANM-007b1]` through `[SSANM-007b3]`; typed loop/result, branch operand, table, null/non-null, and cast-branch boundary locks are now covered and documented.
      - Goal: separate true local-source no-merge decisions from stack/control-value ABI rewrites around typed loop params/results and branch operands.
      - Deliverables: classification fixtures for scalar/reference loop params, single-result and multi-result loops, branch operands, `br_table`, `br_on_*`, and nested-loop targets; mark each as LocalGraph local rewrite, typed-control lowering work, or deliberate fail-closed boundary.
      - Evidence: `[SSANM-007b1]` inventories typed-control helper owners; `[SSANM-007b2]` locks scalar/reference loop-param proxy, no-copy typed-loop, multi-param/single-result store-model, typed-loop `br_table`, and nested target boundaries; `[SSANM-007b3]` locks value branch/table operands, null/non-null exits, and cast/fail exits with trace/validation helpers that reject ordinary planned structured LocalGraph reasons. Reopen typed-control mutation only with red-first behavior fixtures and direct `--pass ssa-nomerge` compare evidence if a later slice admits a new typed-control mutation family.
    - [x] [SSANM-007b1] - Refresh typed-control source and fixture inventory
      - Status: completed 2026-06-14 through `[SSANM-007b1a]` through `[SSANM-007b1c]`; typed-control loop/result helper owners, typed branch/cast owners, and next boundary-lock handoff are now durable in the SSA no-merge implementation docs.
      - Goal: pin Binaryen no-merge source/test evidence against Starshine's typed loop/result and branch-operand lowering boundaries before broadening LocalGraph mutation.
      - Deliverables: source-backed inventory for scalar/reference loop params, value-producing loops/blocks, branch operands, `br_table`, `br_on_*`, cast branches, and nested loop targets; map each existing typed-control `ssa_nomerge_test.mbt` guard to LocalGraph-local, scratch/copy lowering, or fail-closed ownership.
      - Evidence: `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md` now records typed loop-param/result proxy/store/no-copy owners, `br_table` selector/scratch helpers, nested loop target gates, `br_on_null` / `br_on_non_null` branch-spill helpers, prefix/tested reference producer handling, `br_on_cast` / `br_on_cast_fail` inverse-cast lowerings, and no-throw `try_table` typed-control wrappers as ABI repair or fail-closed surfaces rather than ordinary LocalGraph local-source no-merge mutation. Focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `420/420`. Direct compare was not run because this was a source/test inventory with no pass behavior mutation and no newly admitted mutation family.
    - [x] [SSANM-007b1a] - Inventory typed loop param/result helper owners
      - Status: completed 2026-06-14.
      - Goal: map scalar/reference loop params, value-producing blocks/loops, single-result and multi-result loop helpers, store-model rewrites, and nested-loop target guards to their current `ssa_nomerge_test.mbt` proof surface and raw helper owners.
      - Deliverables: source/test inventory table in the SSA no-merge implementation docs, with each loop/result family classified as ordinary LocalGraph local-source work, typed-control ABI repair, or fail-closed boundary; no mutation unless the inventory finds an unowned ordinary gap.
      - Evidence: the implementation docs now map scalar proxies, copy-needing scalar/reference loop params, multi-param no-result store models, single-result/multi-param single-result loops, `br_table` loop/result helpers, nested loop target gates, and no-copy typed-loop-param contrast helpers to `run_hot_pipeline_raw_loop_param_*`, table helper, and branch-local copy owners.
    - [x] [SSANM-007b1b] - Inventory typed branch operand and cast/null exit owners
      - Status: completed 2026-06-14.
      - Goal: map value-carrying `br`, `br_table`, `br_on_null`, `br_on_non_null`, `br_on_cast`, and `br_on_cast_fail` families to current branch-alias, cast/null scratch, or fail-closed owners before `[SSANM-007b3]` adds executable locks.
      - Deliverables: source/test inventory table that cross-links existing branch/table/cast/null tests, separates ordinary local-source no-merge decisions from stack/control-value ABI lowerings, and records any missing fixture candidates.
      - Evidence: the implementation docs now map value `br` / `br_if`, value `br_table`, `br_on_null` / `br_on_non_null`, tested-producer spills, `br_on_cast` / `br_on_cast_fail`, and no-throw `try_table` wrappers to typed branch/table/cast helper owners and to `[SSANM-007b3]` for executable boundary locks.
    - [x] [SSANM-007b1c] - Close typed-control inventory and hand off boundary locks
      - Status: completed 2026-06-14.
      - Goal: summarize the typed-control source and fixture inventory, update parity/index/log notes, and leave exact candidate lists for `[SSANM-007b2]` loop/result boundary locks and `[SSANM-007b3]` branch/cast boundary locks.
      - Deliverables: docs/backlog closeout for `[SSANM-007b1]`, explicit comparison note explaining why tests/fuzz were or were not required, and no behavior mutation.
      - Evidence: parity, index, and wiki log pages now record `[SSANM-007b1]` as docs/source inventory only. `[SSANM-007b2]` should add loop-param/result trace/output locks; `[SSANM-007b3]` should add typed branch operand and cast/null exit locks. Direct compare was not required because no mutation behavior changed.
    - [x] [SSANM-007b2] - Lock typed loop param/result ABI boundaries
      - Status: completed 2026-06-14 through `[SSANM-007b2a]` through `[SSANM-007b2d]`; scalar/reference, no-copy, store-model, table, and nested-target locks are now covered and documented.
      - Goal: keep loop-param/result rewrites that require stack/control-value ABI repair on explicit scratch/copy helpers instead of silently routing them through ordinary LocalGraph local mutation.
      - Deliverables: focused public-pipeline fixtures for scalar/reference loop params, single-result and multi-result loops, no-copy typed-loop cases, and nested-loop target boundaries, with trace/output assertions showing whether each family mutates through an intentional typed-control helper or remains fail-closed.
      - Evidence: `[SSANM-007b2a]` / `[SSANM-007b2b]` added scalar/reference proxy, no-copy, and store-model trace/output locks. `[SSANM-007b2c]` extends the same `structured-local-writes-mutated` trace lock to all-current, mixed-target, copy-needing, single-result, non-current/nested, reference, no-throw `try_table`, and nested-loop target `br_table` / typed-loop boundary fixtures. `[SSANM-007b2d]` closes the docs/backlog track and leaves branch/cast/null operand work to `[SSANM-007b3]`. Direct compare was not run because these were regression/classification locks over existing behavior with no new mutation family.
    - [x] [SSANM-007b2a] - Lock scalar/reference typed loop-param proxy and no-copy boundaries
      - Status: completed 2026-06-14.
      - Goal: prove scalar and reference loop-param backedges use explicit typed-control proxy/copy helpers only when an overwrite or edge-copy is needed, while no-copy typed-loop cases remain no-mutation boundaries.
      - Deliverables: strengthened trace/output fixtures for direct scalar `br`, scalar/reference `br_if`, copy-needing contrast, and no-copy typed-loop-param cases; direct compare only if behavior changes.
      - Evidence: `ssa_test_expect_scalar_loop_param_branch_proxy(...)` now captures trace, validates output, requires `structured-local-writes-mutated`, and is used by direct scalar `br`, scalar `br_if`, numeric, nested, copy-needing scalar, nullable reference, non-null reference, and copy-needing reference typed loop-param fixtures. `ssa_test_no_copy_typed_loop_param_module_no_mutation(...)` now also rejects legacy structured local-write raw reasons for no-copy typed-loop-param boundaries. Validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `420/420`; `moon test src/passes` passed `2450/2450`. Direct compare was not run because this was a regression/classification lock over existing behavior with no new mutation family.
    - [x] [SSANM-007b2b] - Lock multi-param and loop-result typed-loop store-model boundaries
      - Status: completed 2026-06-14.
      - Goal: keep multi-param no-result loops, single-result loops, and multi-param single-result loops on typed-control store/result ABI helpers instead of ordinary LocalGraph local-source mutation.
      - Deliverables: focused trace/output fixtures for multi-param `br` / `br_if`, single-result loops, multi-param single-result loops, and no-throw `try_table` wrappers where they exercise the same typed-control store/result helper.
      - Evidence: `ssa_test_expect_loop_param_store_model(...)` now captures trace, validates output, requires `structured-local-writes-mutated`, and asserts the multi-param/store-model backedge shape for multi-param direct `br`, multi-param `br_if`, single-result direct `br`, single-result `br_if`, and multi-param single-result `br_table` fixtures. Validation: `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; focused `ssa_nomerge_test.mbt` passed `420/420`; `moon test src/passes` passed `2450/2450`. Direct compare was not run because this was a regression/classification lock over existing behavior with no new mutation family. No-throw `try_table` wrappers remain covered by `[SSANM-007a3]` and can be rechecked during `[SSANM-007b2d]` closeout if needed.
    - [x] [SSANM-007b2c] - Lock typed-loop `br_table` and nested-target boundaries
      - Status: completed 2026-06-14.
      - Goal: classify all-current, mixed current/enclosing, non-current/nested typed-loop table targets and nested loop-target gates as typed-control/table helper territory or explicit fail-closed boundaries.
      - Deliverables: trace/output fixtures for current-loop, mixed-target, copy-needing, single-result, and nested-loop typed `br_table` / nested target shapes; direct compare only if a new mutation family is admitted.
      - Evidence: `ssa_test_expect_typed_loop_boundary_output(...)`, `ssa_test_expect_typed_loop_module_boundary_output(...)`, and `ssa_test_expect_loop_param_br_table_proxy(...)` now capture trace, validate output, require `structured-local-writes-mutated`, and are used by all-current scalar/copy-needing/multi-param typed-loop `br_table`, mixed-target table rewrites, non-current/nested table targets, no-throw `try_table` table variants, reference table params, and nested loop-target fixtures. Focused `ssa_nomerge_test.mbt` passed `420/420`. Direct compare was not run because this only strengthened existing table/nested boundary locks.
    - [x] [SSANM-007b2d] - Close typed loop/result boundary docs
      - Status: completed 2026-06-14.
      - Goal: summarize `[SSANM-007b2a]` through `[SSANM-007b2c]`, update parity/index/wiki-log notes, and leave any branch-operand/cast/null work to `[SSANM-007b3]`.
      - Deliverables: docs/backlog closeout, explicit comparison note explaining why direct fuzz was or was not required, and reopening criteria for any retained typed-control fail-closed boundary.
      - Evidence: parity, index, implementation-structure, and wiki-log pages now close `[SSANM-007b2]` as typed loop/result executable boundary locks. Reopen typed loop/result mutation only with a red-first behavior fixture and direct `--pass ssa-nomerge` compare evidence if a later slice admits a new table/nested target mutation family; branch/cast/null operands remain open in `[SSANM-007b3]`.
    - [x] [SSANM-007b3] - Classify typed branch operands and cast/null branch exits
      - Status: completed 2026-06-14 through `[SSANM-007b3a]` through `[SSANM-007b3d]`; value branch/table operands, null/non-null exits, and cast/fail exits are now locked as typed-control or branch/table ABI boundaries.
      - Goal: decide which branch-operand, `br_table`, `br_on_null`, `br_on_non_null`, `br_on_cast`, and `br_on_cast_fail` families are local-source no-merge decisions versus typed-control lowering work.
      - Deliverables: positive or fail-closed fixtures for branch operands and typed exits, direct compare evidence for any newly admitted mutation family, and docs/backlog updates that keep branch-alias/cast scratch ownership explicit.
      - Evidence: `[SSANM-007b3a]` / `[SSANM-007b3b]` validate value `br`, `br_if`, and `br_table` operands through a shared boundary helper; `[SSANM-007b3c]` validates representative `br_on_null` / `br_on_non_null` no-copy, prefix/copy-needed, raw-path, and tested-producer shapes; `[SSANM-007b3d]` validates representative `br_on_cast` / `br_on_cast_fail` no-copy, divergent-alias, multi-result, multi-param, no-copy loop-target, and copy-needing loop-target shapes. Direct compare was not run for these regression/classification locks because no pass behavior changed and no new mutation family was admitted.
    - [x] [SSANM-007b3a] - Lock value-carrying `br` and `br_if` operand boundaries
      - Status: completed 2026-06-14.
      - Goal: prove scalar/reference branch operands that require stack-value preservation remain on explicit branch-alias or scratch lowering helpers, while ordinary local-source writes remain owned by LocalGraph no-merge decisions.
      - Deliverables: focused trace/output fixtures for direct and conditional value branches, including copy-needed and no-copy contrasts; direct compare only if a new mutation family is admitted.
      - Evidence: `ssa_test_expect_value_branch_boundary_output(...)` validates value-branch output and rejects all ordinary planned structured LocalGraph reasons. `ssa-nomerge keeps value br operands off LocalGraph path` now consumes that helper, and `ssa-nomerge keeps value br_if operands off LocalGraph path` adds the conditional value-operand lock. Focused `ssa_nomerge_test.mbt` passed `422/422`. Direct compare was not run because this only strengthens fail-closed boundary/classification coverage and admits no new mutation family.
    - [x] [SSANM-007b3b] - Lock typed `br_table` branch-operand boundaries
      - Status: completed 2026-06-14.
      - Goal: separate table selector/value operand ABI repair from ordinary no-merge local-source mutation, including mixed branch targets and branch-local copy ownership.
      - Deliverables: trace/output fixtures for value-carrying `br_table` shapes that are not already closed by `[SSANM-007b2c]`, with explicit selector/scratch ownership and any fail-closed targets recorded.
      - Evidence: the value-branch boundary helper now also covers value-carrying `br_table` operands, and the new mixed-target value `br_table` fixture keeps selector/result target ABI repair off ordinary planned structured LocalGraph reasons while preserving the table opcode. Focused `ssa_nomerge_test.mbt` passed `422/422`. Direct compare was not run because this only strengthens fail-closed boundary/classification coverage and admits no new mutation family.
    - [x] [SSANM-007b3c] - Lock `br_on_null` and `br_on_non_null` typed-exit boundaries
      - Status: completed 2026-06-14 through `[SSANM-007b3c1]` through `[SSANM-007b3c4]`; cast branch closeout remains open in `[SSANM-007b3d]`.
      - Goal: keep null/non-null branch spills and tested reference producer handling classified as typed-control lowering or explicit fail-closed work rather than generic LocalGraph local mutation.
      - Deliverables: positive or fail-closed fixtures for nullable and non-null reference exits, tested producer spills, prefix locals, and no-copy contrasts; direct compare only if behavior changes.
      - Evidence: `ssa_test_expect_null_branch_boundary_module_output(...)` now captures trace, validates output, rejects ordinary planned structured LocalGraph reasons, and returns the requested function body. No-copy `br_on_null`, copy-needing/prefix `br_on_null`, no-copy raw-path `br_on_non_null`, and local-tee tested-producer null/non-null fixtures consume that helper or equivalent trace/validation assertions before checking proxy/store/temp/opcode shapes. Focused `ssa_nomerge_test.mbt` passed `422/422`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2452/2452`; full `moon test` passed `5757/5757`. Direct compare was not run because this only strengthens regression/classification coverage and admits no new mutation family.
    - [x] [SSANM-007b3c1] - Lock no-copy null/non-null typed-exit contrasts
      - Status: completed 2026-06-14.
      - Goal: prove nullable and non-null reference exits that do not need scratch repair preserve their `br_on_null` / `br_on_non_null` opcode shape and stay off ordinary planned structured LocalGraph reasons.
      - Deliverables: focused public-pipeline trace/output fixtures for minimal no-copy null and non-null branch exits, with validation and direct compare only if behavior changes.
      - Evidence: no-copy `br_on_null` now uses the shared null-branch boundary helper and preserves the null proxy plus opcode shape; no-copy raw-path `br_on_non_null` now validates, rejects ordinary planned structured reasons, and preserves the non-null branch opcode while checking temp-local repair.
    - [x] [SSANM-007b3c2] - Lock prefix-local null/non-null branch spills
      - Status: completed 2026-06-14.
      - Goal: prove prefix-local and branch-spill null/non-null exits remain typed-control lowering work rather than generic LocalGraph local-source freshening.
      - Deliverables: focused trace/output fixtures over existing prefix-local or scratch-producing shapes, with explicit local-count/opcode assertions and direct compare only if behavior changes.
      - Evidence: the copy-needing/prefix `br_on_null` fixture now validates through the shared helper, rejects planned structured LocalGraph reasons, and keeps the null-taken copy plus `ref.as_non_null` repair shape.
    - [x] [SSANM-007b3c3] - Lock tested-producer null/non-null exits
      - Status: completed 2026-06-14.
      - Goal: keep ref-null, ref-cast, conversion, i31, ref.func, struct, array, and subtype-producing null/non-null branch tests classified as typed branch-producer handling.
      - Deliverables: shared helper coverage or strengthened assertions for tested producer fixtures, including validation and ordinary LocalGraph reason rejection; direct compare only if behavior changes.
      - Evidence: local-tee tested-producer `br_on_null` and `br_on_non_null` fixtures now use the shared helper before asserting the multi-param store-backedge output; the broader tested-producer matrix remains owned by the same typed branch spill helpers and reopening requires red-first behavior plus direct compare.
    - [x] [SSANM-007b3c4] - Close null/non-null typed-exit docs
      - Status: completed 2026-06-14.
      - Goal: summarize `[SSANM-007b3c1]` through `[SSANM-007b3c3]`, update parity/index/wiki-log notes, and hand remaining cast ownership to `[SSANM-007b3d]`.
      - Deliverables: docs/backlog closeout, direct-compare explanation, and reopening criteria for retained null/non-null branch boundaries.
      - Evidence: implementation-structure, parity, index, and wiki-log pages now classify null/non-null branch exits as typed-control ABI boundary locks. Reopen only with a red-first behavior fixture and direct `--pass ssa-nomerge` compare evidence if a later slice admits a new `br_on_*` mutation family.
    - [x] [SSANM-007b3d] - Lock cast-branch exits and close typed-branch docs
      - Status: completed 2026-06-14 through `[SSANM-007b3d1]` through `[SSANM-007b3d4]`; parent `[SSANM-007b3]` is closed.
      - Goal: classify `br_on_cast` and `br_on_cast_fail` inverse-cast/scratch ownership, then summarize `[SSANM-007b3a]` through `[SSANM-007b3c]` in parity/index/wiki-log notes.
      - Deliverables: cast branch fixtures or source-backed fail-closed notes, docs/backlog closeout for `[SSANM-007b3]`, direct-compare explanation, and reopening criteria for retained typed branch/cast boundaries.
      - Evidence: `ssa_test_expect_cast_branch_boundary_module_output(...)` captures trace, validates output, rejects ordinary planned structured LocalGraph reasons, rejects `hot-lift-error`, and returns output plus trace. No-copy raw-path, divergent-alias, multi-result, multi-param typed-loop, no-copy loop-target, and copy-needing loop-target `br_on_cast` / `br_on_cast_fail` fixtures now use it before checking opcode, temp-local, store-model, proxy, mutation, or inverse-cast copy-block shapes. Focused `ssa_nomerge_test.mbt` passed `422/422`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2452/2452`; full `moon test` passed `5757/5757`. Direct compare was not run because this only strengthens regression/classification coverage and admits no new mutation family.
    - [x] [SSANM-007b3d1] - Lock no-copy cast-branch typed-exit contrasts
      - Status: completed 2026-06-14.
      - Goal: prove `br_on_cast` and `br_on_cast_fail` exits that do not need scratch repair preserve their opcode shape, validate, and stay off ordinary planned structured LocalGraph reasons.
      - Deliverables: strengthened no-copy cast/fail fixtures or shared helper coverage with trace assertions and direct compare only if behavior changes.
      - Evidence: no-copy raw-path `br_on_cast` and `br_on_cast_fail` fixtures now validate through the shared cast-branch helper, reject ordinary planned structured reasons, reject `hot-lift-error`, and preserve the cast/fail opcode plus existing temp-local repair shape.
    - [x] [SSANM-007b3d2] - Lock divergent-alias cast-branch scratch copies
      - Status: completed 2026-06-14.
      - Goal: keep taken-edge cast/fail copies after divergent aliases and multi-result cast exits classified as typed-control branch ABI repair rather than ordinary no-merge predecessor-copy work.
      - Deliverables: focused trace/output fixtures for copy-needed and multi-result `br_on_cast` / `br_on_cast_fail` shapes, plus validation and ordinary LocalGraph reason rejection.
      - Evidence: divergent-alias and multi-result `br_on_cast` / `br_on_cast_fail` fixtures now validate through the shared helper, require mutation, reject ordinary planned structured reasons, and keep the inverse-cast branch-copy block shape.
    - [x] [SSANM-007b3d3] - Lock typed-loop cast-branch backedge repairs
      - Status: completed 2026-06-14.
      - Goal: classify typed loop-param cast/fail backedges, including no-copy and copy-needing loop-target variants, as typed-control loop ABI repair surfaces.
      - Deliverables: strengthened loop-param cast/fail fixtures with validation, opcode/proxy/store assertions, and direct compare only if a new mutation family is admitted.
      - Evidence: multi-param typed-loop cast/fail fixtures now validate through the shared helper before asserting store-backedge and copy-block shapes; no-copy loop-target cast/fail fixtures require no pass mutation and keep the loop-param cast proxy; copy-needing loop-target fixtures require mutation and keep the branch-copy block shape.
    - [x] [SSANM-007b3d4] - Close cast-branch and typed-branch docs
      - Status: completed 2026-06-14.
      - Goal: summarize `[SSANM-007b3d1]` through `[SSANM-007b3d3]`, close parent `[SSANM-007b3]` if no typed branch work remains, and update parity/index/wiki-log notes.
      - Deliverables: docs/backlog closeout, direct-compare explanation, and reopening criteria for retained cast branch typed-control boundaries.
      - Evidence: implementation-structure, parity, index, and wiki-log pages now classify cast branch exits as typed-control ABI boundary locks and close parent `[SSANM-007b3]` / `[SSANM-007b]`. Reopen only with a red-first behavior fixture and direct `--pass ssa-nomerge` compare evidence if a later slice admits a new cast-branch mutation family.
    - [x] [SSANM-007c] - Split full `ssa` work out of the no-merge backlog
      - Status: completed 2026-06-13; full `ssa` work now lives under sibling `[O4Z-AUDIT-SSA-FULL]` / `[SSA-FULL-*]` slices rather than the `SSANM` no-merge backlog.
      - Goal: keep full `ssa` merge-local materialization from being mistaken for `ssa-nomerge` behavior.
      - Deliverables: move or recreate full-`ssa` planner/materialization tasks under a sibling backlog section if still needed; keep `SSANM` tasks restricted to `SSAify(false)` behavior.
      - Evidence: docs/backlog-only split. Existing `src/passes/ssa.mbt` / `src/passes/ssa_test.mbt` coverage already tracks the active partial full-`ssa` pass: completed registry/planner/non-merge slices are recorded under `[O4Z-AUDIT-SSA-FULL]`, while remaining merge-local materialization and direct closeout stay out of `SSANM`.
    - [ ] [SSANM-008] - Revisit huge-function guardrails with LocalGraph policy
      - Goal: classify remaining large structured functions using the new no-merge plan instead of blind raw thresholds.
      - Status: epic only; execute through child slices after the LocalGraph no-merge planner/mutation work is available.
      - Deliverables: extracted/replayed evidence for the six current huge guards (`Func 3536`, `Func 3781`, `Func 3885`, `Func 4119`, `Func 5417`, and `Func 5419`) plus drift classification for old anchors `Func 265`, `Func 3518`, and `Func 4522`; pass-local timing; output size; validation; decision to admit, narrow, or keep fail-closed.
      - Suggested evidence: direct artifact/function replay before and after any threshold or policy change.
    - [x] [SSANM-008a] - Refresh huge-function replay anchors
      - Status: completed 2026-06-14; the huge-function anchor drifted from nine functions to six current huge skips.
      - Goal: confirm the current first-diff and large structured-local-write function list still matches the checked-in debug-WASI artifact before changing huge-function policy.
      - Deliverables: table for `Func 265`, `Func 3518`, `Func 3536`, `Func 3781`, `Func 3885`, `Func 4119`, `Func 4522`, `Func 5417`, and `Func 5419` with current status, extraction/replay path, local/write/control shape summary, and whether the function still reaches a huge/raw guard.
      - Evidence: native `src/cmd` build passed with the pre-existing pass-manager unused-function warnings. Direct traced replay `.tmp/ssa-nomerge-huge-anchor-refresh-20260614` exited `0`, validated, had `0` `skip-invalid-lower`, and now reports six `large-structured-local-writes` skips: `Func 3536` (`16195` instrs / `2871` locals), `Func 3781` (`39283` / `6634`), `Func 3885` (`26350` / `3879`), `Func 4119` (`9458` / `1332`), `Func 5417` (`19507` / `3524`), and `Func 5419` (`15300` / `2313`). Extracted-function replays live under `.tmp/ssanm008a-huge-functions-20260614`: the six current huge functions still replay the huge guard after extraction and validate; old anchors `Func 265`, `Func 3518`, and `Func 4522` now validate through non-huge boundary/no-op reasons (`multiparam-value-if-branch-carrier-noop` or `branch-heavy-void-loop-exit-mesh-noop`). Self-compare `.tmp/self-ssa-nomerge-huge-anchor-refresh-20260614` validated both outputs and now first-diffs at `defined=108 abs=135`; Starshine output remains smaller (`3155453` vs Binaryen `3155990`) and pass-local time is reported as `0.225ms` versus Binaryen `375.105ms`, while whole-command Starshine runtime remains slower. No pass behavior was mutated, so direct 10000-case compare was not rerun.
    - [x] [SSANM-008b] - Classify huge-function LocalGraph plan decisions
      - Status: completed 2026-06-14; no-mutation planner counts are recorded for the six current huge guards plus the three drifted non-huge old anchors.
      - Goal: run the no-mutation LocalGraph planner over the six current huge structured-local-write functions plus the three drifted non-huge old anchors, then separate plan-supported ordinary local rewrites from unsupported typed-control/EH/large-CFG boundaries.
      - Deliverables: per-function counts for freshenable writes, merge-feeding canonical writes, default/parameter-entry reads, tee writes, and fail-closed boundary reasons; update the parity page with the classification table.
      - Evidence: temporary native debug instrumentation appended LocalGraph plan counts to extracted-function trace reasons, then was removed before commit. Native `src/cmd` build passed with the pre-existing pass-manager unused-function warnings. Replays under `.tmp/ssanm008b-plan-classification-20260614` validated all nine extracted outputs. The six current huge guards remain size-threshold boundaries with no merge-feeding canonical writes and no merge reads: `Func 3536` planned `3080` writes / `2786` freshenable / `1652` tee writes; `Func 3781` `7341` / `7337` / `3211`; `Func 3885` `4054` / `4052` / `2352`; `Func 4119` `1436` / `1288` / `439`; `Func 5417` `3663` / `2163` / `1270`; and `Func 5419` `2742` / `2742` / `1038`. Drifted old anchors are not huge-threshold candidates: `Func 265` is `multiparam-value-if-branch-carrier-noop`, while `Func 3518` and `Func 4522` are `branch-heavy-void-loop-exit-mesh-noop`. Direct compare was not rerun because no committed pass behavior, trace policy, or wasm output changed.
    - [x] [SSANM-008c] - Admit huge-function mutation safely
      - Status: completed 2026-06-15 after explicit user direction to remove Starshine-only huge `ssa-nomerge` limits unless Binaryen has equivalent limits.
      - Goal: after `[SSANM-008b]`, admit the six current huge structured functions through the existing no-merge path instead of keeping a broad raw-size guard.
      - Evidence: Binaryen `SSAify(false)` source review found no equivalent huge-function guard. Red-first focused `ssa_nomerge_test.mbt` failed under the old guard for larger no-branch and branch structured fixtures with `large-structured-local-writes`, then passed `444/444` after removing the size/local threshold check from the `ssa-nomerge` raw dispatcher. Native `src/cmd` build passed with pre-existing unused-function warnings; `moon fmt`, `moon test src/passes` (`2478/2478`), `moon info` (three pre-existing GenValid warnings), and full `moon test` (`5783/5783`) passed. Direct `ssa-nomerge` compare `.tmp/pass-fuzz-ssa-nomerge-no-huge-limit-10000` compared `7601/10000`, had `7601` normalized matches, `0` mismatches/validation/property/generator failures, and `20` cached Binaryen command failures. Debug-WASI self-compare `.tmp/self-ssa-nomerge-no-huge-limit-20260615` completed; both Starshine raw/canonical outputs validate; trace has `0` `large-structured-local-writes`; Starshine command runtime was `4198.989ms`, raw timed region `1503.887ms`, Binaryen command runtime `725.270ms`, Binaryen pass runtime `392.564ms`, and whole compare wrapper real time `10.793s`. Canonical Starshine output was smaller (`3149564` bytes vs Binaryen `3156337`) but still differed at `defined=2 abs=29`. Follow-up classification found and fixed two true stale-read semantic issues in that non-huge `tlsf/insertBlock` function; `.tmp/self-ssa-nomerge-loop-carrier-fix2-20260615` now validates and advances the first diff to `defined=32 abs=59`, a still-open suspected loop-carried-local semantic issue.
    - [ ] [SSANM-008d] - Measure huge-function pass-local budget
      - Goal: prove any admitted huge-function policy stays inside the pass-local performance floor or keep the family fail-closed with documented timing evidence.
      - Deliverables: pass-local Starshine/Binaryen timings, HOT lift/lower attribution if relevant, output size/validation summary, and reopening criteria for any retained performance guard.
      - Suggested evidence: timing-only artifact/function replay and direct compare counts after policy changes.
    - [ ] [SSANM-009] - Replay and classify checked-in debug-WASI artifact parity
      - Goal: use artifact replay as evidence after the LocalGraph rewrite, not as the primary implementation strategy.
      - Status: epic only; execute through child slices after the LocalGraph no-merge planner/mutation path is available.
      - Deliverables: resume from the current `.tmp/self-ssa-nomerge-debug-wasi-anchor-refresh-20260614` anchor at `defined=108 abs=135`, classify remaining first diffs as matched Binaryen behavior, measured Starshine win, parity gap, or explicit boundary, and delete/reclassify obsolete artifact-specific hacks where the LocalGraph path supersedes them.
    - [x] [SSANM-009a] - Refresh debug-WASI replay anchor
      - Status: completed 2026-06-14; the old `defined=215 abs=242` anchor is stale and superseded by `defined=108 abs=135`.
      - Goal: refresh the checked-in debug-WASI direct `ssa-nomerge` replay anchor and explicitly supersede the old `defined=215 abs=242` tuple before using the artifact as post-LocalGraph evidence.
      - Deliverables: exact replay command/out-dir, current first-diff tuple, validation status, pass-local timing when available, and a note when the prior `.tmp/self-ssa-nomerge-debug-wasi-brtable-callspill-20260613` anchor has drifted.
      - Evidence: native `src/cmd` build passed with the pre-existing pass-manager unused-function warnings. `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --out-dir .tmp/self-ssa-nomerge-debug-wasi-anchor-refresh-20260614 --starshine-bin target/native/release/build/cmd/cmd.exe --ssa-nomerge` validated both outputs and remained non-equal; current first diff is `defined=108 abs=135`. Starshine size stayed smaller than Binaryen in the prior same-anchor replay (`3155453` vs `3155990`), and this refresh reported Starshine pass-local time `0.220ms` vs Binaryen `380.411ms`. Whole-command Starshine runtime remains much slower (`61193.468ms` vs `810.879ms`) and stays under `[WALL]001` unless later evidence proves an SSANM-local owner. No mutation was made.
    - [ ] [SSANM-009b] - Classify post-LocalGraph debug-WASI first diffs
      - Status: child-sliced 2026-06-14; `[SSANM-009b1]` fixed the former `defined=108 abs=135` first-diff family, `[SSANM-009b2]` fixed the `defined=215 abs=242` unwritten fresh-local carrier family, `[SSANM-009b3]` fixed the `defined=363 abs=390` dropped canonical branch-carrier write family, `[SSANM-009b4]` fixed/classified the `defined=501 abs=528` loop-carried plain-branch boundary, `[SSANM-009b5]` fixed/classified the `defined=538 abs=565` medium scanner variant, `[SSANM-009b6]` fixed/classified the `defined=651 abs=678` typed result-loop carrier boundary, `[SSANM-009b7]` fixed/classified the `defined=987 abs=1014` typed loop-param plain-branch boundary, `[SSANM-009b8]` fixed the `defined=1163 abs=1190` branch result-carrier defaulting gap, `[SSANM-009b9]` fixed/classified the `defined=1286 abs=1313` nested typed loop-param boundary, `[SSANM-009b10]` fixed the refreshed `defined=3 abs=30` parameter-tee branch-carrier gap, `[SSANM-009b11]` fixed the `defined=154 abs=181` loop-header alias-copy parity gap, `[SSANM-009b12]` fixed the `defined=300 abs=327` branch-exit store-suffix carrier gap, and `[SSANM-009b13]` classified the `defined=1413 abs=1440` candidate as part of a repeated multi-param typed result-loop store-model representation/parity gap now split to `[SSANM-009b13a]`.
      - Goal: after the LocalGraph no-merge rewrite is active, inspect each remaining artifact first-diff family before adding artifact-specific code.
      - Deliverables: per-diff classification as matched Binaryen behavior, behavior-parity gap, measured Starshine win, huge-function boundary, typed-control/EH boundary, or tool/representation issue; reduce actionable parity gaps into focused tests.
      - Suggested evidence: targeted replay dirs, canonical diff snippets or summaries, focused reduced fixtures for actionable families, and direct `--pass ssa-nomerge` compare evidence after mutations.
    - [x] [SSANM-009b1] - Fix straight-line stack-carried tee/default-read first diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=108 abs=135` to `defined=215 abs=242`.
      - Goal: reduce and fix the first post-`[SSANM-009a]` artifact diff where a void call sits between a stack-carried producer and `local.tee`, and a later read of that tee target was incorrectly materialized as the body-local default.
      - Deliverables: red-first reduced fixture, straight-line raw rewrite fix, artifact replay showing the first-diff movement, direct `--pass ssa-nomerge` compare evidence, and docs/wiki update.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `425/426`, showing `i32.const 0` before the decref-like call where the reduced fixture required `local.get 4`. The fix keeps a straight-line fresh-alias table while applying the LocalGraph rewrite plan so a later `MaterializeDefault` decision for a just-freshened stack-carried write is rewritten to the fresh local. Final evidence: focused `ssa_nomerge_test.mbt` passed `426/426`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2456/2456`; full `moon test` passed `5761/5761`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b-20260614` validated both outputs and moved the first diff to `defined=215 abs=242`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b-stack-tee-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1) with persistent-cache hits.
    - [x] [SSANM-009b2] - Fix current debug-WASI `defined=215 abs=242` unwritten fresh-local carrier
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=215 abs=242` to `defined=363 abs=390`.
      - Goal: inspect and fix the post-`[SSANM-009b1]` first-diff family before adding artifact-specific code.
      - Classification: true behavior-parity/safety gap in a large structured branch/value-carrier function, not a huge-function guard. The legacy raw structured path could produce appended fresh locals that were read but never written after branch-carrier alias freshening. The fix treats such rewrites as unsafe and falls back instead of committing a malformed alias plan.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `426/427`, reporting `local.get 79` of an appended local with no corresponding write in the reduced standalone `defined=215 abs=242` fixture. Final evidence: focused `ssa_nomerge_test.mbt` passed `427/427`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2457/2457`; full `moon test` passed `5762/5762`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b2-unwritten-fresh-20260614` validated both outputs and moved the first diff to `defined=363 abs=390`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b2-unwritten-fresh-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b3] - Fix current debug-WASI `defined=363 abs=390` dropped canonical branch-carrier write
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=363 abs=390` to `defined=501 abs=528`.
      - Goal: inspect and fix the post-`[SSANM-009b2]` first-diff family before adding artifact-specific code.
      - Classification: true behavior-parity/safety gap in `dae_instr_has_escaping_branch`, not a huge-function guard. The legacy raw structured rewrite could freshen branch-arm writes to appended locals while a later outer branch-target check still read the original canonical label local, so tag arms could observe the default label value instead of the branch-carried label.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `427/428`, reporting no canonical write to local `11` in the reduced standalone `defined=363 abs=390` fixture. The fix rejects raw structured rewrites that drop all writes to a pre-existing local while retaining reads of that local in plain-branch-containing functions, so the pass falls closed instead of committing a malformed branch-carrier alias plan. Final evidence: focused `ssa_nomerge_test.mbt` passed `428/428`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2458/2458`; full `moon test` passed `5763/5763`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b3-branch-carrier-20260614` validated both outputs and moved the first diff to `defined=501 abs=528`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b3-branch-carrier-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b4] - Fix/classify current debug-WASI `defined=501 abs=528` loop-carried plain-branch diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=501 abs=528` to `defined=538 abs=565`.
      - Goal: inspect the post-`[SSANM-009b3]` first-diff family before adding artifact-specific code.
      - Classification: size-losing / parity-risky legacy structured copy boundary in `dae_rewrite_dropped_result_calls_instrs`, not a proven Starshine win. The old raw structured helper freshened a loop-carried index/local through appended locals and synthesized branch/else copies around plain `br` exits. The fix keeps large structured functions (`base_local_count > 128`) with void loops containing body-local reads, body-local writes, and plain `br` traffic fail-closed with `loop-carried-plain-br-boundary-noop` until a Binaryen-shaped LocalGraph rewrite exists.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `428/429`, reporting the old `structured-local-writes` route instead of `loop-carried-plain-br-boundary-noop`. Final evidence: focused `ssa_nomerge_test.mbt` passed `429/429`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2459/2459`; full `moon test` passed `5764/5764`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b4-loop-plain-br-20260614b` validated both outputs and moved the first diff to `defined=538 abs=565`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b4-loop-plain-br-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b5] - Fix/classify current debug-WASI `defined=538 abs=565` medium loop-carried scanner diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=538 abs=565` to `defined=651 abs=678`.
      - Goal: inspect the post-`[SSANM-009b4]` first-diff family before adding artifact-specific code.
      - Classification: size-losing / parity-risky medium loop-carried plain-branch boundary in `dae_scan_local_use_ignoring_instrs`, not a proven Starshine win. The old raw structured helper freshened a loop-carried scanner local through appended locals and synthesized branch/else copies around nested plain `br` exits. The fix lowers the existing fail-closed `loop-carried-plain-br-boundary-noop` admission threshold to source-lowered scanner-sized functions (`base_local_count > 64`) while retaining small loop scratch fixtures on the legacy helper path until a Binaryen-shaped LocalGraph rewrite exists.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `429/430`, reporting the old `structured-local-writes` route instead of `loop-carried-plain-br-boundary-noop`. Final evidence: focused `ssa_nomerge_test.mbt` passed `430/430`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2460/2460`; full `moon test` passed `5765/5765`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b5-medium-loop-plain-br-20260614b` validated both outputs and moved the first diff to `defined=651 abs=678`; an earlier self-compare attempt to `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b5-medium-loop-plain-br-20260614` timed out after writing partial artifacts; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b5-medium-loop-plain-br-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b6] - Fix/classify current debug-WASI `defined=651 abs=678` typed result-loop carrier diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=651 abs=678` to `defined=987 abs=1014`.
      - Goal: inspect the post-`[SSANM-009b5]` first-diff family before adding artifact-specific code.
      - Classification: size-losing / parity-risky typed result-loop carrier boundary in `dae_instr_is_unreachable_root`, not a proven Starshine win. The raw input has a source-lowered single-param/single-result typed loop carrier that Binaryen leaves canonical; Starshine's legacy structured helper lowered it through a helper scratch plus a void-loop/result-block shape and appended six helper locals. The fix keeps single-param/result typed loops that start by storing the loop param into a body local and only use plain `br` traffic fail-closed with `result-loop-plain-br-boundary-noop`, while preserving the existing typed-control helper families for `br_if`, `br_table`, and reference/cast branches.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` first failed `430/431` while the new boundary reason was absent; during narrowing, broader result-loop predicates also broke existing typed-control helper tests and were tightened to the single-param/result plain-`br` source-lowered shape. Final evidence: focused `ssa_nomerge_test.mbt` passed `431/431`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2461/2461`; full `moon test` passed `5766/5766`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b6-result-loop-plain-br-20260614g` validated both outputs and moved the first diff to `defined=987 abs=1014`; earlier self-compare attempts `...20260614`, `...20260614b`, and broad result-loop probes through `...20260614f` timed out or retained the same first diff while narrowing the predicate; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b6-result-loop-plain-br-final-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b7] - Fix/classify current debug-WASI `defined=987 abs=1014` typed loop-param plain-branch diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=987 abs=1014` to `defined=1163 abs=1190`.
      - Goal: inspect the post-`[SSANM-009b6]` first-diff family before adding artifact-specific code.
      - Classification: behavior/parity-risky typed loop-param local.set/plain-`br` carrier boundary in `gr_collect_func_global_sets`, not a proven Starshine win. The raw input has a source-lowered single-param/no-result typed loop carrier that Binaryen leaves canonical; Starshine's legacy scalar typed-loop proxy helper could introduce a helper scratch and later local freshening could strand the updated backedge value on a different local from the next-iteration read. The fix keeps only the narrow single-param/no-result typed-loop shape whose body starts with body-local `local.set`, uses plain `br`, and has no nested block/loop/try_table or non-plain typed-control traffic fail-closed with `loop-param-plain-br-boundary-noop`, while preserving the existing local.tee, br_if, br_table, nested, try_table, and reference/cast typed-control helper families.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `431/432`, reporting `structured-local-writes-mutated` instead of `loop-param-plain-br-boundary-noop`; during narrowing, broader loop-param predicates broke existing try_table and nested typed-loop helper tests and were tightened to the plain top-level source-lowered shape. Final evidence: focused `ssa_nomerge_test.mbt` passed `432/432`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2462/2462`; full `moon test` passed `5767/5767`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b7-loop-param-plain-br-20260614c` validated both outputs and moved the first diff to `defined=1163 abs=1190`; earlier self-compare attempts `...20260614` and `...20260614b` timed out after writing partial artifacts; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b7-loop-param-plain-br-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b8] - Fix current debug-WASI `defined=1163 abs=1190` branch result-carrier defaulting diff
      - Status: completed 2026-06-14; the current debug-WASI first diff advanced from `defined=1163 abs=1190` to `defined=1286 abs=1313`.
      - Goal: inspect the post-`[SSANM-009b7]` first-diff family before adding artifact-specific code.
      - Classification: true behavior-parity/safety gap in `ar_rewrite_load_reinterpret_pair`, not a proven Starshine win. Branch arms store a selected memarg in canonical locals and branch to constructor-specific continuations; the raw helper and planned structured/default paths could treat those continuation reads as body-local entry defaults and call the load constructors with `i32.const 0` instead of the branch-carried memarg.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `432/433`, showing `i32.const 0` before constructor calls in the reduced standalone `defined=1163 abs=1190` fixture. The fix only materializes defaults for locals that are never written in the raw function; written branch-carrier locals stay canonical in the raw structured helper, planned structured rewrite, and standalone default materializer. Final evidence: focused `ssa_nomerge_test.mbt` passed `433/433`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; `moon test src/passes` passed `2463/2463`; full `moon test` passed `5768/5768`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b8-branch-result-carriers-20260614c` validated both outputs and moved the first diff to `defined=1286 abs=1313`; earlier self-compare attempts `...20260614` and `...20260614b` timed out after writing partial artifacts, and timing-only `...branch-result-carriers-timing-20260614` confirmed the repaired `abs=1190` constructor calls use `local.get` operands; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b8-branch-result-carriers-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b9] - Fix/classify current debug-WASI `defined=1286 abs=1313` nested typed loop-param diff
      - Status: completed 2026-06-14; the normal full self-compare did not complete within local timeouts after the fix, so `[SSANM-009b10]` owns refreshing the next first-diff tuple.
      - Goal: inspect the post-`[SSANM-009b8]` first-diff family before adding artifact-specific code.
      - Classification: behavior/parity-risky nested typed loop-param plain-`br` boundary in `run_hot_pipeline_raw_simplify_locals_take_value_suffix`, not a proven arbitrary representation drift. The raw input nests a source-lowered single-param/no-result typed loop inside an outer void loop. Binaryen leaves the loop carrier coherent/canonical; the legacy structured helper lowered it through a void-loop/block-result carrier mesh and, in the artifact, could strand the next-iteration read and backedge update on different helper locals.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `433/434`, reporting `structured-local-writes` instead of `loop-param-plain-br-boundary-noop` for the reduced nested typed-loop fixture. The fix lets the existing typed loop-param plain-`br` boundary scanner recurse through enclosing void loops/blocks while still rejecting typed/result loops and `try_table` recursion. Final evidence: focused `ssa_nomerge_test.mbt` passed `434/434`; `moon test src/passes` passed `2464/2464`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; full `moon test` passed `5769/5769`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; timing-only self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b9-nested-loop-param-timing-20260614` validated both outputs, reported Starshine size `3153215` vs Binaryen `3155990`, and pass-local timing `0.402ms` vs Binaryen `387.222ms` while whole-command runtime remains `[WALL]001`; local function extraction from that timing replay shows `defined=1286 abs=1313` now keeps a coherent single carrier (`local 59`) in the formerly divergent nested loop, with remaining differences limited to fewer Starshine temp locals; full normal self-compare attempts `...nested-loop-param-20260614` and `...20260614b` timed out after writing partial artifacts; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b9-nested-loop-param-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b10] - Fix refreshed debug-WASI `defined=3 abs=30` parameter-tee branch-carrier diff
      - Status: completed 2026-06-14; a lightweight alpha comparison over the post-fix normal self-compare artifacts advances the next candidate to `defined=154 abs=181`, owned by `[SSANM-009b11]`.
      - Goal: run or otherwise derive a post-`[SSANM-009b9]` artifact comparison, classify the next actionable family, and avoid adding artifact-specific code before the first current diff is understood.
      - Classification: true behavior-parity/safety gap in `tlsf/addMemory`, not representation drift. The condition writes an aligned pointer back to parameter local `1` with `local.tee`, while the taken branch may overwrite that same parameter before later reads. Binaryen keeps the parameter tee canonical; Starshine's structured scratch freshener moved it to an appended body local, so the not-taken path could observe the stale original parameter.
      - Evidence: red-first focused `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` failed `434/435`, showing seven i32 locals where the reduced fixture expected the preserved-parameter shape. The fix preserves parameter `local.tee` writes when the root structured function has at most one call, a preceding parameter tee is followed by an `if` arm that writes the same parameter, and a later read occurs before the next write; body-local allocator scratch freshening remains enabled. Final evidence: focused `ssa_nomerge_test.mbt` passed `435/435`; `moon test src/passes` passed `2465/2465`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; full `moon test` passed `5770/5770`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; normal self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b10-param-tee-20260614` timed out after writing artifacts; timing-only self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b10-param-tee-timing-20260614` validated both outputs, reported Starshine size `3153215` vs Binaryen `3155990`, and pass-local timing `0.392ms` vs Binaryen `374.836ms` while whole-command runtime remains `[WALL]001`; local extraction confirms `defined=3 abs=30` now preserves `local.tee 1`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b10-param-tee-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures with persistent-cache hits.
    - [x] [SSANM-009b11] - Fix current debug-WASI `defined=154 abs=181` loop-header alias-copy diff
      - Status: completed 2026-06-14; a lightweight alpha scan over the post-fix normal self-compare artifacts advances the next candidate to `defined=300 abs=327`, owned by `[SSANM-009b12]`.
      - Goal: turn the lightweight alpha candidate from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b10-param-tee-20260614` into a stronger completed first-diff classification before adding more artifact-specific code.
      - Classification: size-losing / behavior-parity local-allocation gap in `parse__olevel__text`, not a proven Starshine win. The legacy structured local-write path freshened locals written before a loop even though the loop header reads them before any loop-local write, then repaired plain loop backedges with extra canonical alias copies that Binaryen does not emit.
      - Evidence: targeted `--print-func 181` extraction from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b10-param-tee-20260614` and the saved alpha diff showed the first actionable difference was a dead `local.set` before `br $label`; the reduced fixture `ssa-nomerge avoids loop-backedge alias copies for header-read locals` was red first with eight i32 locals instead of the expected six. The fix makes later-read/later-write scans descend through `loop` and `try_table` containers, so root writes whose later reads live in loop headers stay canonical. Final evidence: focused `ssa_nomerge_test.mbt` passed `436/436`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; normal self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b11-loop-header-20260614` timed out after writing artifacts; timing-only self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b11-loop-header-timing-20260614` validated both outputs, reported Starshine size `3152912` vs Binaryen `3155990`, pass-local timing `0.401ms` vs Binaryen `386.187ms`, and whole-command runtime remains `[WALL]001`; local alpha comparison over the post-fix artifacts advanced the next candidate to `defined=300 abs=327`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b11-loop-header-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1) with persistent-cache hits.
    - [x] [SSANM-009b12] - Fix current debug-WASI `defined=300 abs=327` branch-exit store-suffix carrier diff
      - Status: completed 2026-06-14; the post-fix lightweight alpha scan advances the next candidate to `defined=1413 abs=1440`, owned by `[SSANM-009b13]`.
      - Goal: classify the next lightweight alpha candidate from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b11-loop-header-20260614` before adding more artifact-specific code.
      - Classification: behavior-parity / size-losing local-allocation gap in `_M0FP37jtenner9starshine6passes55dae__try__unwrap__func313__suffix__staging__block__once`. Branch arms push multiple branch-carrier values before adjacent `local.set`s and a plain branch to an outer merge; Binaryen keeps the post-merge carriers canonical without Starshine's extra store-suffix alias locals. Starshine previously only recognized an immediately following `br`, so earlier stores in a stack-to-local suffix were freshened and then repaired with extra alias copies.
      - Evidence: targeted `--print-func 327` extraction from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b11-loop-header-20260614` showed Starshine had 261 i32 locals versus Binaryen's 259 and extra store-suffix alias locals/copies in the first branch-carrier mesh. The reduced fixture `ssa-nomerge keeps branch-exit store suffix carriers canonical` was red first with four body locals instead of the expected two. The fix makes the branch-exit canonical-alias scanner look through adjacent `local.set` suffixes before a plain `br`, stopping if the same local is overwritten. Final evidence: focused `ssa_nomerge_test.mbt` passed `437/437`; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager unused-function warnings; normal self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b12-branch-suffix-20260614` timed out after writing artifacts, but targeted extraction shows `abs=327` now has 259 i32 locals on both Starshine and Binaryen; timing-only self-compare `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b12-branch-suffix-timing-20260614` validated both outputs, reported Starshine size `3152718` vs Binaryen `3155990`, pass-local timing `0.367ms` vs Binaryen `376.258ms`, and whole-command runtime remains `[WALL]001`; local alpha comparison over the post-fix artifacts advanced the next candidate to `defined=1413 abs=1440`; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b12-branch-suffix-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1) with persistent-cache hits.
    - [x] [SSANM-009b13] - Refresh/classify current debug-WASI `defined=1413 abs=1440` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a repeated multi-param typed result-loop store-model representation/parity gap, not a fresh semantic-safety bug.
      - Goal: classify the next lightweight alpha candidate from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b12-branch-suffix-20260614` before adding more artifact-specific code.
      - Classification: size-losing / behavior-parity output-shape gap in `_M0FP37jtenner9starshine6passes51optimize__instructions__is__boolean__constant__tree`. The source has a multi-param single-result typed loop whose recursive branch supplies several loop-carrier values. Starshine lowers the result loop through a void-loop/store model and spills stack-carried branch operands into extra helper locals before storing loop-param scratches; Binaryen uses a tuple/block-shaped carrier and needs fewer i32 locals in the extracted artifact. This is not a validation failure and direct compare remains green, but it is not a proven Starshine win.
      - Evidence: `--print-func 1440` over `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b12-branch-suffix-20260614/{starshine,binaryen}.wasm` confirms both validate and differ in the typed result-loop carrier shape: Starshine has `38` body locals (`35` i32 / `3` i64), Binaryen has `37` (`34` i32 / `3` i64), while the input had `20` (`18` i32 / `2` i64). A second alpha skip found the same family at `defined=1431 abs=1458`, where Starshine has `53` body locals and Binaryen `51`; skipping both advances the local lightweight alpha candidate to `defined=1451 abs=1478`. Direct compare was not rerun because this slice made no behavior, trace, or generated-wasm change; the preceding `[SSANM-009b12]` direct lane remained normalized-green with `0` mismatches.
    - [x] [SSANM-009b13a] - Align multi-param typed result-loop store-model carriers
      - Status: completed 2026-06-14; the naive tuple/block carrier path is disabled until it has canonical local-count proof, and the current source-shaped result-loop boundary is classified as a measured Starshine win for the repeated `abs=1440` / `abs=1458` family.
      - Goal: reduce and fix or explicitly defer the `[SSANM-009b13]` repeated multi-param single-result typed loop family so Starshine no longer needs extra helper locals for stack-carried recursive branch operands unless a measured Starshine win is proven.
      - Deliverables: reduced red-first fixture for the `abs=1440` / `abs=1458` carrier shape, Binaryen/Starshine local and size deltas, a store-model or documented boundary policy, direct artifact/function replay after any change, and direct `--pass ssa-nomerge` compare evidence if behavior changes.
      - Evidence: initial standalone reduction attempts under `.tmp/ssanm009b13-inspect` did not reproduce the artifact local-count gap until the Starshine raw output was also canonicalized through `wasm-opt --strip-debug`; that canonicalization reproduced the `abs=1440` one-local Starshine loss. The first red/green sub-fix preserved a non-void raw result loop for GC-free multi-param single-result typed loops with only plain-`br` current-loop backedges and no `try_table`, protected by `ssa-nomerge preserves result loop carrier for multi-param direct br backedges`. A later tuple/block carrier experiment was red-reclassified by `ssa-nomerge leaves tuple carrier disabled without canonical local proof`: direct tuple carrier blocks in Starshine raw output caused the shared strip-debug canonicalizer to add more helper locals. The final policy keeps that tuple carrier disabled and relies on the source-shaped result-loop boundary for this subset. Current targeted self artifacts `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` timed out after writing artifacts, but targeted extraction validates both outputs and shows Starshine final canonical locals are now lower than Binaryen: `abs=1440` Starshine `29` body locals (`27` i32 / `2` i64) versus Binaryen `37` (`34` i32 / `3` i64), and `abs=1458` Starshine `39` (`37` i32 / `2` i64) versus Binaryen `51` (`48` i32 / `3` i64). Skipping both repeats advances the local alpha candidate to `defined=1451 abs=1478`; the current full self artifact first-diffs earlier at `defined=540 abs=567`, now owned by `[SSANM-009b14]`. Validation for the final state: red-first focused `ssa_nomerge_test.mbt` failed `438/439` with the tuple carrier still enabled, then passed `439/439`; `moon test src/passes` passed `2470/2470`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; full `moon test` passed `5775/5775`; native `src/cmd` build passed with pre-existing pass-manager warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b13a-disable-tuple-20260614` requested 10000 cases, compared 9977, and had 9977 normalized matches, 0 mismatches, and 23 cached Binaryen/tool command failures.
    - [x] [SSANM-009b14] - Classify current debug-WASI `defined=540 abs=567` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the first diff as a semantic-safe Starshine local-count win / no-regression representation difference, not an implementation gap.
      - Goal: classify the current full self artifact first diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` before adding more SSANM artifact code.
      - Classification: local-count-winning representation difference in the branch/table-heavy function `abs=567` / `defined=540`. The Starshine and Binaryen canonical `body_raw` token streams have identical length and differ only in 17 one-shot local indexes. Binaryen allocates fresh dead one-use temps `50..66` for `local.tee` values and branch-local `local.set` carriers, while Starshine reuses otherwise-dead existing slots such as `7`, `8`, `13`, `14`, `16`, `17`, `18`, `26`, `27`, `30`, `31`, `32`, `33`, `34`, `35`, and `38`. No branch/table opcode, call, load/store, constant, label, or control shape differs.
      - Evidence: `wasm-tools validate --features all` passed for both `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614/{starshine,binaryen}.wasm`. Targeted `--print-func 567` extraction under `.tmp/ssanm009b13a-next540` shows Starshine has `45` body i32 locals versus Binaryen `62`; both function bodies encode to the same `775` byte code body (`772` instruction bytes plus `3` local-declaration bytes), so this is local-count reducing and size-neutral for the function body, not a size-losing drift. Whole canonical artifact sizes remain Starshine `3149379` bytes versus Binaryen `3155990` bytes, but that whole-module delta is not attributed solely to `abs=567`. Direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` requested 10000 cases, compared 9977, and had 9977 normalized matches, 0 mismatches, and 23 cached Binaryen/tool command failures. A preliminary `--print-func 568` probe under `.tmp/ssanm009b14-next568` shows the next exact-print diff is a similar one-shot temp-local family (`92` Starshine body i32 locals versus `112` Binaryen) and is now owned by `[SSANM-009b15]`.
    - [x] [SSANM-009b15] - Classify current debug-WASI `defined=541 abs=568` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe Starshine local-count win / no-regression representation difference, not an implementation gap.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b14]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in the branch/table-heavy function `abs=568` / `defined=541`. Starshine and Binaryen have identical canonical `body_raw` token lengths and differ only in 20 local-index choices: Binaryen allocates fresh dead one-use `local.tee` temps `95..114`, while Starshine reuses otherwise-dead existing slots `67`, `40`, `61`, `70`, `43`, `60`, `74`, `73`, `46`, `59`, `77`, `51`, `37`, `38`, `30`, `31`, `23`, `24`, `14`, and `16`. No branch/table opcode, call, load/store, constant, label, or structured-control node differs.
      - Evidence: `wasm-tools validate --features all` passed for both current artifacts. Targeted `--print-func 568` extraction under `.tmp/ssanm009b14-next568` plus local-use analysis shows each differing Starshine local and each Binaryen fresh local appears exactly once as a dead `local.tee` stack-preserving temp. Starshine has `92` body i32 locals versus Binaryen `112`; both function bodies encode to the same `1253` byte code body (`1250` instruction bytes plus `3` local-declaration bytes). No executable code changed, so direct compare was not rerun; the current native direct lane `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` remains the latest evidence with 9977 normalized matches, 0 mismatches, and 23 cached Binaryen/tool command failures.
    - [x] [SSANM-009b16] - Classify current debug-WASI `defined=547 abs=574` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the next raw-body print diff as the same one-shot temp-local representation family.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b15]` before adding more SSANM artifact code.
      - Classification: semantic-safe Starshine local-count win / no-regression representation difference in `abs=574` / `defined=547`. Starshine and Binaryen canonical `body_raw` token streams have equal length and differ only in 9 one-use `local.tee` target indexes: `5 -> 25`, `9 -> 26`, `8 -> 27`, `7 -> 28`, `12 -> 29`, `16 -> 30`, `22 -> 31`, `18 -> 32`, and `21 -> 33`. No control, branch/table, call, memory, label, or constant token differs.
      - Evidence: targeted `--print-func 574` extraction under `.tmp/ssanm009b15-next574` shows every differing local appears exactly once as a dead `local.tee` temp on both sides. Starshine has `23` body i32 locals versus Binaryen `32`; both code bodies encode to the same `371` bytes (`368` instruction bytes plus `3` local-declaration bytes). No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b17] - Classify current debug-WASI `defined=548 abs=575` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the next raw-body print diff as a semantic-safe neutral local-slot permutation, not an implementation gap and not a measured Starshine win.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b16]` before adding more SSANM artifact code.
      - Classification: neutral representation-only local permutation in `abs=575` / `defined=548`. Starshine and Binaryen both have `7` body i32 locals plus two params and equal canonical `body_raw` token lengths. The only token differences are local indexes in the same small branch/result-carrier skeleton: the offset-8 load is teed only for the compare (`local 6` in Starshine, `local 7` in Binaryen); the offset-12 load is stored and immediately reread through one branch-local temp (`local 8` in Starshine, `local 6` in Binaryen); and the final stack-preserving tee before canonical `local.set 2` uses a dead temp (`local 7` in Starshine, `local 8` in Binaryen). No branch/table opcode, call, load/store memarg, constant, label, result type, or structured-control node differs.
      - Evidence: targeted `--print-func 575` extraction under `.tmp/ssanm009b16-next575` plus local-use analysis shows the differing locals are branch-local temporaries whose dataflow stays self-contained before the shared later `local.get 2` / `call 576`. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `548` encodes to the same `86` byte code body in both artifacts (`83` instruction bytes plus `3` local-declaration bytes), with the same single local declaration run of seven i32 locals. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b18] - Classify current debug-WASI `defined=549 abs=576` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe Starshine local-count win / no-regression representation difference, not an implementation gap.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b17]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=576` / `defined=549`. Starshine and Binaryen canonical `body_raw` token streams have equal length and differ only in 15 dead `local.tee` target indexes: Starshine reuses `14`, `15`, `31`, `56`, `10`, `11`, `12`, `27`, `29`, `30`, `40`, `24`, `25`, `37`, and `22`, while Binaryen allocates fresh locals `61..75`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 576` extraction under `.tmp/ssanm009b17-next576` plus local-use analysis shows every differing local appears exactly once as a dead stack-preserving `local.tee` temp on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `549` encodes to the same `714` byte code body in both artifacts (`711` instruction bytes plus `3` local-declaration bytes), but Starshine has `59` body i32 locals versus Binaryen `74`, so this is local-count reducing and function-body-size neutral. The Starshine raw trace for abs `576` reports `call-heavy-memory-structured-noop`, so this diff belongs to current artifact representation classification rather than a new SSANM mutation. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b19] - Classify current debug-WASI `defined=552 abs=579` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference with typed local groups shifted by Binaryen-only i32 one-shot temps.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b18]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local plus typed-local group renumbering in `abs=579` / `defined=552`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked. Binaryen inserts seven one-use i32 `local.tee` temps `61..67` where Starshine reuses existing one-use i32 locals `45..51`; because those Binaryen temps are declared before the f64/f32/i64 local groups, the live typed carrier groups are shifted by seven local indexes (`f64` Starshine `61..67` versus Binaryen `68..74`, `f32` Starshine `68..74` versus Binaryen `75..81`, and `i64` Starshine `75..81` versus Binaryen `82..88`). No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 579` extraction under `.tmp/ssanm009b18-next579` plus local-use/diff-token analysis shows the seven direct i32 differences are one-use `local.tee` targets and the remaining local-token differences are a type-preserving group renumbering of the same live carriers. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `552` encodes to the same `832` byte code body in both artifacts (`823` instruction bytes plus `9` local-declaration bytes), but Starshine has local declarations `(59 i32, 7 f64, 7 f32, 7 i64)` versus Binaryen `(66 i32, 7 f64, 7 f32, 7 i64)`. The Starshine raw trace for abs `579` reports `call-heavy-memory-structured-noop`, so this remains artifact representation classification rather than a new SSANM mutation. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b20] - Classify current debug-WASI `defined=553 abs=580` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe one-shot temp-local local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b19]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=580` / `defined=553`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked. The only local-token differences are four dead stack-preserving `local.tee` target choices: Starshine reuses one-use locals `19`, `14`, `24`, and `25`, while Binaryen allocates fresh one-use locals `40`, `41`, `42`, and `43`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 580` extraction under `.tmp/ssanm009b19-next580` plus local-use analysis shows all four differing locals appear exactly once as `local.tee` targets. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `553` encodes to the same `476` byte code body in both artifacts (`473` instruction bytes plus `3` local-declaration bytes), but Starshine has `34` body i32 locals versus Binaryen `38`. The Starshine raw trace for abs `580` reports `call-heavy-memory-structured-noop`, so this is not a new SSANM mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b21] - Classify current debug-WASI `defined=554 abs=581` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe one-shot temp-local local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b20]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=581` / `defined=554`. Starshine and Binaryen canonical `body_raw` strings are identical once local indexes are masked, with the same `175` local operations (`119` `local.get`, `31` `local.set`, and `25` `local.tee`). The only local-token differences are 15 dead stack-preserving `local.tee` target choices: Starshine reuses one-use locals `16`, `12`, `13`, `17`, `18`, `26`, `24`, `19`, `29`, `28`, `30`, `31`, `38`, `41`, and `42`, while Binaryen allocates fresh one-use locals `61..75`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 581` extraction under `.tmp/ssanm009b20-next581` plus local-use analysis shows every differing local appears exactly once as a `local.tee` target on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `554` encodes to the same `756` byte code body in both artifacts (`753` instruction bytes plus `3` local-declaration bytes), but Starshine has `56` body i32 locals versus Binaryen `71`. The Starshine raw trace for abs `581` reports `call-heavy-memory-structured-noop`, so this is not a new SSANM mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b22] - Classify current debug-WASI `defined=557 abs=584` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation, not an implementation gap and not a measured Starshine win.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b21]` before adding more SSANM artifact code.
      - Classification: neutral local-slot permutation in `abs=584` / `defined=557`. Starshine and Binaryen have identical masked `body_raw` strings, the same 14 local operations (`8` `local.get`, `2` `local.set`, and `4` `local.tee`), and the same local declaration count. The only differences are a type-preserving permutation of branch-local i32 temps: Starshine uses `7`/`8` as dead `local.tee` targets and `9` for the loaded value's set/get pair, while Binaryen uses `8`/`9` as dead `local.tee` targets and `7` for that set/get pair. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 584` extraction under `.tmp/ssanm009b21-next584` plus local-use analysis shows the differing slots are a self-contained same-type permutation. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `557` encodes to the same `65` byte code body in both artifacts (`62` instruction bytes plus `3` local-declaration bytes), with the same eight body i32 locals. The Starshine raw trace for abs `584` reports `structured-local-writes`, so this remains representation classification rather than a new behavior mutation. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b23] - Classify current debug-WASI `defined=558 abs=585` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b22]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=585` / `defined=558`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `378` local operations (`259` `local.get`, `77` `local.set`, and `42` `local.tee`). The only local-token differences are 20 dead stack-preserving `local.tee` target choices: Starshine reuses one-use locals `6`, `7`, `46`, `47`, `49`, `50`, `52`, `86`, `83`, `37`, `100`, `71`, `62`, `58`, `32`, `67`, `91`, `73`, `75`, and `79`, while Binaryen allocates fresh one-use locals `121..140`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 585` extraction under `.tmp/ssanm009b22-next585` plus local-use analysis shows every differing local appears exactly once as a `local.tee` target on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `558` encodes to a smaller Starshine code body: `1626` bytes (`1623` instruction bytes plus `3` local-declaration bytes) versus Binaryen `1640` bytes (`1636` instruction bytes plus `4` local-declaration bytes), with Starshine `119` body i32 locals versus Binaryen `139`. The Starshine raw trace for abs `585` reports `call-heavy-memory-structured-noop`, so this is not a new SSANM mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b24] - Classify current debug-WASI `defined=560 abs=587` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe local-count win with type-group renumbering and no encoded-body-size regression.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b23]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local plus i64 group renumbering in `abs=587` / `defined=560`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `135` local operations (`90` `local.get`, `21` `local.set`, and `24` `local.tee`). Binaryen inserts 14 extra one-use i32 `local.tee` temps plus one extra one-use i64 `local.tee` temp before/inside the live i64 carrier group, shifting the i64 carrier locals from Starshine `46..47` to Binaryen `60` and `62`; the remaining differing locals are one-use tee temps. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 587` extraction under `.tmp/ssanm009b23-next587` plus local-use/diff-token analysis shows the local-token differences are one-use `local.tee` temps plus type-preserving i64 carrier renumbering. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `560` encodes to the same `677` byte code body in both artifacts (`672` instruction bytes plus `5` local-declaration bytes), but Starshine declares `(42 i32, 2 i64)` body locals versus Binaryen `(56 i32, 3 i64)`. The Starshine raw trace for abs `587` reports `call-heavy-memory-structured-noop`, so this remains artifact representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b25] - Classify current debug-WASI `defined=562 abs=589` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b24]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=589` / `defined=562`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `371` local operations (`211` `local.get`, `70` `local.set`, and `90` `local.tee`). The only local-token differences are 37 dead stack-preserving `local.tee` target choices: Starshine reuses one-use locals `14`, `15`, `97`, `9`, `11`, `96`, `24`, `25`, `99`, `98`, `34`, `35`, `101`, `100`, `47`, `48`, `103`, `44`, `102`, `75`, `76`, `109`, `72`, `107`, `106`, `105`, `104`, `108`, `123`, `93`, `94`, `95`, `114`, `90`, `112`, `111`, and `113`, while Binaryen allocates fresh one-use locals `153..189`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 589` extraction under `.tmp/ssanm009b24-next589` plus local-use/diff-token analysis shows every differing local appears exactly once as a `local.tee` target on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `562` encodes to a smaller Starshine code body: `1803` bytes (`1799` instruction bytes plus `4` local-declaration bytes) versus Binaryen `1840` bytes (`1836` instruction bytes plus `4` local-declaration bytes), with Starshine `151` body i32 locals versus Binaryen `188`. The Starshine raw trace for abs `589` reports `call-heavy-memory-structured-noop`, so this is not a new SSANM mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b26] - Classify current debug-WASI `defined=566 abs=593` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b25]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=593` / `defined=566` after `defined=563..565 abs=590..592` matched in the whole-print scan. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `99` local operations (`68` `local.get`, `19` `local.set`, and `12` `local.tee`). The only local-token differences are seven dead stack-preserving `local.tee` target choices: Starshine reuses one-use locals `32`, `13`, `14`, `17`, `26`, `24`, and `29`, while Binaryen allocates fresh one-use locals `38..44`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 593` extraction under `.tmp/ssanm009b25-next593` plus local-use/diff-token analysis shows every differing local appears exactly once as a `local.tee` target on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `566` encodes to the same `428` byte code body in both artifacts (`425` instruction bytes plus `3` local-declaration bytes), but Starshine has `31` body i32 locals versus Binaryen `38`. The Starshine raw trace for abs `593` reports `call-heavy-memory-structured-noop`, so this remains artifact representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b27] - Classify current debug-WASI `defined=567 abs=594` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference with i64 group renumbering.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b26]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local plus i64 group renumbering in `abs=594` / `defined=567`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `496` local operations (`342` `local.get`, `95` `local.set`, and `59` `local.tee`). The only local-token differences are 40 one-use write-target choices: 33 stack-preserving `local.tee` targets plus seven dead `local.set` targets. Starshine reuses i32 one-use locals `48`, `128`, `29`, `56`, `117`, `116`, `118`, `121`, `122`, `125`, `74`, `82`, `79`, `83`, `102`, `99`, `87`, `88`, `95`, `104`, `103`, `105`, `108`, `41`, `137`, `43`, `113`, `25`, `63`, `62`, `64`, `19`, `20`, `66`, `67`, `70`, and `131` plus i64 one-use set locals `161`, `160`, and `159`; Binaryen allocates corresponding fresh one-use i32 locals `159..195` (including four i32 set-only slots), plus fresh one-use i64 set locals `199..201`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 594` extraction under `.tmp/ssanm009b26-next594` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` or `local.set` target on its side. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `567` encodes to a smaller Starshine code body: `2439` bytes (`2433` instruction bytes plus `6` local-declaration bytes) versus Binaryen `2473` bytes (`2467` instruction bytes plus `6` local-declaration bytes), with Starshine body locals `(151 i32, 3 i64)` versus Binaryen `(188 i32, 6 i64)`. The Starshine raw trace for abs `594` reports `call-heavy-memory-structured-noop`, so this is not a new SSANM mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b28] - Classify current debug-WASI `defined=568 abs=595` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as another semantic-safe one-shot temp-local local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b27]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local representation difference in `abs=595` / `defined=568`. Starshine and Binaryen canonical `body_raw` token streams have equal length and are identical once local indexes are masked, with the same `43` local operations (`31` `local.get`, `10` `local.set`, and `2` `local.tee`). The only local-token difference is one dead stack-preserving `local.tee` target: Starshine reuses one-use local `12`, while Binaryen allocates fresh one-use local `14`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 595` extraction under `.tmp/ssanm009b27-next595` plus local-use analysis shows the differing local appears exactly once as a same-typed `local.tee` target on each side. Both current full artifacts validate with `wasm-tools validate --features all` from the preceding `[SSANM-009b27]` validation. Defined function `568` encodes to the same `191` byte code body in both artifacts (`188` instruction bytes plus `3` local-declaration bytes), but Starshine has `12` body i32 locals versus Binaryen `13`. The Starshine raw trace for abs `595` reports `multiparam-value-if-branch-carrier-noop`, so this remains boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b29] - Classify current debug-WASI `defined=570 abs=597` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral same-type local-slot permutation, not an implementation gap and not a measured Starshine win.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b28]` before adding more SSANM artifact code.
      - Classification: neutral local-slot permutation in `abs=597` / `defined=570` after `defined=569 abs=596` matched in the whole-print scan. Starshine and Binaryen have identical masked `body_raw` token streams, the same `48` local operations (`32` `local.get`, `10` `local.set`, and `6` `local.tee`), the same 20 body i32 locals, and the same encoded body size. The only local-token differences are a same-type permutation among five local slots: Starshine uses `18` and `19` as one-use `local.tee` targets, `22` as a set/get pair, `20` as a set-only slot, and `21` as a one-use `local.tee` target; Binaryen uses corresponding slots `19`, `20`, `18`, `21`, and `22`. No call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs.
      - Evidence: targeted `--print-func 597` extraction under `.tmp/ssanm009b28-next597` plus local-use/diff-token analysis shows the differing slots are a self-contained same-type permutation. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `570` encodes to the same `239` byte code body in both artifacts (`236` instruction bytes plus `3` local-declaration bytes), with the same 20 body i32 locals. The Starshine raw trace for abs `597` reports `structured-local-writes`, so this remains artifact representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b30] - Classify current debug-WASI `defined=571 abs=598` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a body-size-neutral but Starshine-local-count-losing structured scratch-local parity gap.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b29]` before adding more SSANM artifact code.
      - Classification: local-count-losing structured memory-load scratch freshening in `abs=598` / `defined=571`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 16 local operations (`9` `local.get`, `4` `local.set`, and `3` `local.tee`). The only local-token differences are two i32 set/get pairs: Starshine freshens the offset-8 load through appended local `9` where Binaryen uses body local `1`, and freshens the offset-8 element load through appended local `10` where Binaryen uses body local `3`. Locals `1` and `3` are otherwise unused in the Starshine output, so this is not a semantic mismatch but is a Starshine-local-count-losing output-shape gap rather than a proven win.
      - Evidence: targeted `--print-func 598` extraction under `.tmp/ssanm009b29-next598` plus local-use/diff-token analysis shows no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `571` encodes to the same `92` byte code body in both artifacts (`89` instruction bytes plus `3` local-declaration bytes), but Starshine declares `10` body i32 locals versus Binaryen `8`. The Starshine trace reports `structured-local-writes-mutated`, with the observed delta owned by structured raw scratch freshening rather than a canonical LocalGraph no-merge success reason. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b30a] - Reduce structured memory-load scratch local count after abs 598
      - Status: completed 2026-06-14; reduced structured-load scratch fixture now compacts one-use appended or trailing scratch locals into unused same-typed body locals for the narrow 8-10 body-local debug-WASI family.
      - Goal: decide whether to reuse or compact unused same-typed base locals for structured/raw scratch freshening so body-size-neutral but local-count-losing cases like `defined=571 abs=598` and `defined=678 abs=705` stop appending avoidable locals.
      - Deliverables: added red-first `ssa-nomerge reuses unused body local for structured load scratch`, implemented narrow post-structured compaction for one-set/one-get and one-dead-tee scratch locals, preserved existing branch/table/typed-control helper budget tests, and refreshed direct `--pass ssa-nomerge` compare.
      - Evidence: focused `ssa_nomerge_test.mbt` passed `440/440`; `moon test src/passes` passed `2471/2471`; `moon info` passed with three pre-existing GenValid warnings; full `moon test` passed `5776/5776`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b30a-compact-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` cached Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`).
    - [x] [SSANM-009b31] - Classify current debug-WASI `defined=573 abs=600` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b30]` before adding more SSANM artifact code.
      - Classification: one-shot temp-local reuse in `abs=600` / `defined=573` after `defined=572 abs=599` matched in the prior scan. Starshine and Binaryen have identical masked `body_raw` token streams and the same 48 local operations (`35` `local.get`, `10` `local.set`, and `3` `local.tee`). The only local-token differences are two dead stack-preserving `local.tee` targets: Starshine uses locals `3` and `13`, while Binaryen allocates fresh locals `15` and `16`. Binaryen still declares locals `3` and `13` but does not use them.
      - Evidence: targeted `--print-func 600` extraction under `.tmp/ssanm009b30-next600` plus local-use/diff-token analysis shows no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `573` encodes to the same `217` byte code body in both artifacts (`214` instruction bytes plus `3` local-declaration bytes), but Starshine declares `13` body i32 locals versus Binaryen `15`. The Starshine trace reports `multiparam-value-if-branch-carrier-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b32] - Classify current debug-WASI `defined=574 abs=601` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b31]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=601` / `defined=574`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 228 local operations (`156` `local.get`, `39` `local.set`, and `33` `local.tee`). The only local-token differences are 20 one-use same-typed write targets: 19 dead stack-preserving `local.tee` targets and one dead `local.set` target. Starshine reuses locals `7`, `8`, `16`, `17`, `40`, `64`, `42`, `43`, `46`, `48`, `47`, `69`, `51`, `23`, `54`, `68`, `39`, `33`, `56`, and `55`, while Binaryen allocates fresh one-use locals `75..94`.
      - Evidence: targeted `--print-func 601` extraction under `.tmp/ssanm009b31-next601` plus local-use/diff-token analysis shows each differing local appears exactly once as a same-typed write target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `574` encodes to the same `999` byte code body in both artifacts (`996` instruction bytes plus `3` local-declaration bytes), but Starshine declares `72` body i32 locals versus Binaryen `92`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b33] - Classify current debug-WASI `defined=575 abs=602` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b32]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=602` / `defined=575`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 278 local operations (`182` `local.get`, `53` `local.set`, and `43` `local.tee`). The only local-token differences are 27 one-use same-typed write targets: 26 dead stack-preserving `local.tee` targets and one dead `local.set` target. Starshine reuses locals `7`, `8`, `51`, `75`, `20`, `21`, `53`, `45`, `84`, `47`, `48`, `54`, `56`, `58`, `57`, `89`, `61`, `28`, `64`, `65`, `67`, `88`, `44`, `38`, `69`, `68`, and `72`, while Binaryen allocates fresh one-use locals `99..125`.
      - Evidence: targeted `--print-func 602` extraction under `.tmp/ssanm009b32-next602` plus local-use/diff-token analysis shows every differing local appears exactly once as the corresponding same-typed write target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `575` encodes to the same `1251` byte code body in both artifacts (`1248` instruction bytes plus `3` local-declaration bytes), but Starshine declares `96` body i32 locals versus Binaryen `123`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b34] - Classify current debug-WASI `defined=576 abs=603` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b33]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=603` / `defined=576`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 161 local operations (`108` `local.get`, `29` `local.set`, and `24` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `17`, `48`, `19`, `43`, `29`, `23`, `51`, `25`, `26`, `30`, `36`, `39`, and `42`, while Binaryen allocates fresh one-use locals `55..69`.
      - Evidence: targeted `--print-func 603` extraction under `.tmp/ssanm009b33-next603` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `576` encodes to the same `699` byte code body in both artifacts (`696` instruction bytes plus `3` local-declaration bytes), but Starshine declares `53` body i32 locals versus Binaryen `68`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b35] - Classify current debug-WASI `defined=577 abs=604` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b34]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=604` / `defined=577`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 519 local operations (`343` `local.get`, `94` `local.set`, and `82` `local.tee`). The only local-token differences are 52 one-use same-typed write targets: 47 dead stack-preserving `local.tee` targets and five dead `local.set` targets. Starshine reuses i32 tee locals `89`, `91`, `62`, `63`, `64`, `65`, `67`, `68`, `70`, `71`, `76`, `83`, `133`, `132`, `135`, `136`, `138`, `50`, `145`, `52`, `118`, `120`, `58`, `126`, `125`, `128`, `129`, `131`, `37`, `107`, `106`, `109`, `110`, `112`, `27`, `100`, `99`, `102`, `103`, `105`, `16`, `93`, `92`, `95`, `96`, `98`, and `140`, plus i64 set locals `179`, `178`, `177`, `176`, and `175`; Binaryen allocates fresh one-use i32 locals `175..221` and fresh one-use i64 set locals `227..231`.
      - Evidence: targeted `--print-func 604` extraction under `.tmp/ssanm009b34-next604` plus local-use/diff-token analysis shows every differing local appears exactly once as the corresponding same-typed write target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `577` encodes smaller on Starshine (`2558` byte code body, `2552` instruction bytes, `6` local-declaration bytes) than Binaryen (`2595` / `2589` / `6`), and Starshine declares `(169 i32, 5 i64)` body locals versus Binaryen `(216 i32, 10 i64)`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b36] - Classify current debug-WASI `defined=578 abs=605` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b35]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=605` / `defined=578`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 221 local operations (`150` `local.get`, `40` `local.set`, and `31` `local.tee`). The only local-token differences are 20 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `14`, `63`, `16`, `17`, `38`, `24`, `66`, `26`, `57`, `42`, `29`, `69`, `31`, `32`, `43`, `50`, `53`, and `56`, while Binaryen allocates fresh one-use locals `73..92`.
      - Evidence: targeted `--print-func 605` extraction under `.tmp/ssanm009b35-next605` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `578` encodes to the same `947` byte code body in both artifacts (`944` instruction bytes plus `3` local-declaration bytes), but Starshine declares `71` body i32 locals versus Binaryen `91`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b37] - Classify current debug-WASI `defined=579 abs=606` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b36]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=606` / `defined=579`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 169 local operations (`110` `local.get`, `35` `local.set`, and `24` `local.tee`). The only local-token differences are 14 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `18`, `47`, `46`, `33`, `27`, `54`, `29`, `30`, `41`, `39`, `40`, and `45`, while Binaryen allocates fresh one-use locals `60..73`.
      - Evidence: targeted `--print-func 606` extraction under `.tmp/ssanm009b36-next606` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `579` encodes to the same `735` byte code body in both artifacts (`732` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `72`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b38] - Classify current debug-WASI `defined=580 abs=607` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b37]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=607` / `defined=580`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 187 local operations (`126` `local.get`, `36` `local.set`, and `25` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `8`, `9`, `19`, `50`, `49`, `30`, `57`, `32`, `33`, `40`, `36`, `44`, `42`, `43`, and `47`, while Binaryen allocates fresh one-use locals `63..77`.
      - Evidence: targeted `--print-func 607` extraction under `.tmp/ssanm009b37-next607` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `580` encodes to the same `810` byte code body in both artifacts (`807` instruction bytes plus `3` local-declaration bytes), but Starshine declares `60` body i32 locals versus Binaryen `75`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b39] - Classify current debug-WASI `defined=582 abs=609` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b38]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=609` / `defined=582`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 174 local operations (`114` `local.get`, `27` `local.set`, and `33` `local.tee`). The only local-token differences are 12 one-use dead `local.tee` targets: Starshine reuses locals `38`, `6`, `7`, `8`, `17`, `55`, `19`, `20`, `30`, `27`, `29`, and `32`, while Binaryen allocates fresh one-use locals `60..71`.
      - Evidence: targeted `--print-func 609` extraction under `.tmp/ssanm009b38-next609` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `582` encodes to the same `773` byte code body in both artifacts (`770` instruction bytes plus `3` local-declaration bytes), but Starshine declares `59` body i32 locals versus Binaryen `71`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b40] - Classify current debug-WASI `defined=584 abs=611` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b39]` before adding more SSANM artifact code.
      - Classification: `abs=610` / `defined=583` matched exactly in targeted `--print-func` output. Repeated one-shot temp-local reuse resumes in `abs=611` / `defined=584`: Starshine and Binaryen have identical masked `body_raw` token streams and the same 180 local operations (`119` `local.get`, `28` `local.set`, and `33` `local.tee`). The only local-token differences are 12 one-use dead `local.tee` targets: Starshine reuses locals `40`, `7`, `8`, `9`, `19`, `57`, `21`, `22`, `31`, `28`, `30`, and `35`, while Binaryen allocates fresh one-use locals `62..73`.
      - Evidence: targeted `--print-func 611` extraction under `.tmp/ssanm009b40-next611` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `584` encodes to the same `794` byte code body in both artifacts (`791` instruction bytes plus `3` local-declaration bytes), but Starshine declares `60` body i32 locals versus Binaryen `72`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b41] - Classify current debug-WASI `defined=585 abs=612` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b40]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=612` / `defined=585`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 163 local operations (`106` `local.get`, `24` `local.set`, and `33` `local.tee`). The only local-token differences are 12 one-use dead `local.tee` targets: Starshine reuses locals `36`, `7`, `8`, `9`, `18`, `53`, `20`, `21`, `27`, `25`, `26`, and `31`, while Binaryen allocates fresh one-use locals `58..69`.
      - Evidence: targeted `--print-func 612` extraction under `.tmp/ssanm009b41-next612` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `585` encodes to the same `729` byte code body in both artifacts (`726` instruction bytes plus `3` local-declaration bytes), but Starshine declares `56` body i32 locals versus Binaryen `68`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b42] - Classify current debug-WASI `defined=586 abs=613` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b41]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=613` / `defined=586`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 136 local operations (`87` `local.get`, `19` `local.set`, and `30` `local.tee`). The only local-token differences are nine one-use dead `local.tee` targets: Starshine reuses locals `28`, `5`, `6`, `7`, `11`, `45`, `13`, `14`, and `21`, while Binaryen allocates fresh one-use locals `50..58`.
      - Evidence: targeted `--print-func 613` extraction under `.tmp/ssanm-scan-next613` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `586` encodes to the same `615` byte code body in both artifacts (`612` instruction bytes plus `3` local-declaration bytes), but Starshine declares `49` body i32 locals versus Binaryen `58`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b43] - Classify current debug-WASI `defined=587 abs=614` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b42]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=614` / `defined=587`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 121 local operations (`79` `local.get`, `22` `local.set`, and `20` `local.tee`). The only local-token differences are 10 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `15`, `35`, `17`, `19`, `38`, `21`, `22`, and `27`, while Binaryen allocates fresh one-use locals `43..52`.
      - Evidence: targeted `--print-func 614` extraction under `.tmp/ssanm009b43-next614` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `587` encodes to the same `532` byte code body in both artifacts (`529` instruction bytes plus `3` local-declaration bytes), but Starshine declares `42` body i32 locals versus Binaryen `52`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b44] - Classify current debug-WASI `defined=588 abs=615` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b43]` before adding more SSANM artifact code.
      - Classification: neutral local-slot permutation in `abs=615` / `defined=588`. Starshine and Binaryen have identical masked `body_raw` token streams, the same 72 local operations (`50` `local.get`, `15` `local.set`, and `7` `local.tee`), the same 26 body i32 locals, and the same `327` byte encoded body. The only local-token differences are a same-type permutation among locals `26..30`: four one-use `local.tee` targets and one set/get pair move between slots, with no local-count or body-size change.
      - Evidence: targeted `--print-func 615` extraction under `.tmp/ssanm009b44-next615` plus local-use/diff-token analysis shows the set/get pair remains paired on one same-typed local and every differing tee target is one-use on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `588` encodes to the same `327` byte code body in both artifacts (`324` instruction bytes plus `3` local-declaration bytes). The Starshine trace reports `structured-local-writes`, so this remains artifact representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b45] - Classify current debug-WASI `defined=589 abs=616` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b44]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=616` / `defined=589`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 588 local operations (`391` `local.get`, `106` `local.set`, and `91` `local.tee`). The only local-token differences are 59 one-use same-typed write targets: 53 `local.tee` targets plus six `local.set` targets. Starshine declares body locals `(190 i32, 5 i64)` versus Binaryen `(244 i32, 10 i64)`.
      - Evidence: targeted `--print-func 616` extraction under `.tmp/ssanm-scan-next616` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `589` is smaller on Starshine (`2931` byte body: `2925` instruction bytes plus `6` local-declaration bytes) than Binaryen (`2971` byte body: `2965` instruction bytes plus `6` local-declaration bytes). The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b46] - Classify current debug-WASI `defined=590 abs=617` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b45]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=617` / `defined=590`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 80 local operations (`54` `local.get`, `13` `local.set`, and `13` `local.tee`). The only local-token differences are eight one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `17`, `13`, `25`, `15`, `16`, and `21`, while Binaryen allocates fresh one-use locals `28..35`.
      - Evidence: targeted `--print-func 617` extraction under `.tmp/ssanm009b45-next617` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `590` encodes to the same `341` byte code body in both artifacts (`338` instruction bytes plus `3` local-declaration bytes), but Starshine declares `26` body i32 locals versus Binaryen `34`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b47] - Classify current debug-WASI `defined=591 abs=618` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b46]` before adding more SSANM artifact code.
      - Classification: neutral local-slot permutation in `abs=618` / `defined=591`. Starshine and Binaryen have identical masked `body_raw` token streams, the same 72 local operations (`50` `local.get`, `15` `local.set`, and `7` `local.tee`), the same 26 body i32 locals, and the same `329` byte encoded body. The only local-token differences are a same-type permutation among locals `25..29`: four one-use `local.tee` targets and one set/get pair move between slots, with no local-count or body-size change.
      - Evidence: targeted `--print-func 618` extraction under `.tmp/ssanm009b46-next618` plus local-use/diff-token analysis shows the set/get pair remains paired on one same-typed local and every differing tee target is one-use on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `591` encodes to the same `329` byte code body in both artifacts (`326` instruction bytes plus `3` local-declaration bytes). The Starshine trace reports `structured-local-writes`, so this remains artifact representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b48] - Classify current debug-WASI `defined=592 abs=619` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b47]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=619` / `defined=592`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 240 local operations (`169` `local.get`, `35` `local.set`, and `36` `local.tee`). The only local-token differences are 20 one-use dead `local.tee` targets: Starshine reuses locals `18`, `57`, `20`, `21`, `60`, `23`, `24`, `64`, `63`, `26`, `27`, `67`, `31`, `42`, `39`, `35`, `13`, `54`, `44`, and `47`, while Binaryen allocates fresh one-use locals `73..92`.
      - Evidence: targeted `--print-func 619` extraction under `.tmp/ssanm009b47-next619` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `592` encodes to the same `1089` byte code body in both artifacts (`1086` instruction bytes plus `3` local-declaration bytes), but Starshine declares `69` body i32 locals versus Binaryen `89`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b49] - Classify current debug-WASI `defined=593 abs=620` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-debug-wasi-ssanm009b13a-disable-tuple-20260614` after `[SSANM-009b48]` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=620` / `defined=593`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 171 local operations (`112` `local.get`, `36` `local.set`, and `23` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `18`, `46`, `45`, `27`, `53`, `29`, `30`, `40`, `37`, `39`, and `43`, while Binaryen allocates fresh one-use locals `59..71`.
      - Evidence: targeted `--print-func 620` extraction under `.tmp/ssanm009b48-next620` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `593` encodes to the same `738` byte code body in both artifacts (`735` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `71`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b50] - Classify current debug-WASI `defined=594 abs=621` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b49]`; `abs=621` was extracted under `.tmp/ssanm009b49-next621`.
      - Classification: repeated one-shot temp-local reuse in `abs=621` / `defined=594`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 181 local operations (`118` `local.get`, `38` `local.set`, and `25` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `17`, `55`, `19`, `40`, `34`, `39`, `26`, `58`, `28`, `29`, `47`, `46`, and `49`, while Binaryen allocates fresh one-use locals `63..77`.
      - Evidence: targeted `--print-func 621` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `594` encodes to the same `799` byte code body in both artifacts (`796` instruction bytes plus `3` local-declaration bytes), but Starshine declares `62` body i32 locals versus Binaryen `77`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b51] - Classify current debug-WASI `defined=595 abs=622` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b50]`; `abs=622` was extracted under `.tmp/ssanm009b49-next622`.
      - Classification: repeated one-shot temp-local reuse in `abs=622` / `defined=595`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 119 local operations (`78` `local.get`, `17` `local.set`, and `24` `local.tee`). The only local-token differences are five one-use dead `local.tee` targets: Starshine reuses locals `23`, `5`, `6`, `7`, and `18`, while Binaryen allocates fresh one-use locals `42..46`.
      - Evidence: targeted `--print-func 622` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `595` encodes to the same `542` byte code body in both artifacts (`539` instruction bytes plus `3` local-declaration bytes), but Starshine declares `41` body i32 locals versus Binaryen `46`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this is boundary/no-op representation classification rather than a new mutation owner. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b52] - Classify current debug-WASI `defined=597 abs=624` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b51]`; targeted `--print-func 623` matched exactly, so the next candidate is `abs=624` / `defined=597`.
      - Classification: repeated one-shot temp-local reuse in `abs=624` / `defined=597`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 194 local operations (`125` `local.get`, `39` `local.set`, and `30` `local.tee`). The only local-token differences are 18 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `20`, `61`, `22`, `23`, `17`, `58`, `19`, `40`, `39`, `30`, `64`, `32`, `33`, `49`, `48`, and `51`, while Binaryen allocates fresh one-use locals `69..86`.
      - Evidence: targeted `--print-func 624` extraction under `.tmp/ssanm009b51-next624` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `597` encodes to the same `854` byte code body in both artifacts (`851` instruction bytes plus `3` local-declaration bytes), but Starshine declares `68` body i32 locals versus Binaryen `86`. The Starshine trace reports `structured-local-writes`, so this remains artifact representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b53] - Classify current debug-WASI `defined=598 abs=625` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b52]`; `abs=625` was preliminarily extracted under `.tmp/ssanm009b51-next625`.
      - Classification: repeated one-shot temp-local reuse in `abs=625` / `defined=598`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 86 local operations (`56` `local.get`, `18` `local.set`, and `12` `local.tee`). The only local-token differences are seven one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `14`, `28`, `16`, `17`, and `23`, while Binaryen allocates fresh one-use locals `31..37`.
      - Evidence: targeted `--print-func 625` extraction under `.tmp/ssanm009b51-next625` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `598` encodes to the same `353` byte code body in both artifacts (`350` instruction bytes plus `3` local-declaration bytes), but Starshine declares `30` body i32 locals versus Binaryen `37`. The Starshine trace reports `structured-local-writes`, so this remains artifact representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b54] - Classify current debug-WASI `defined=599 abs=626` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b53]`; `abs=626` was preliminarily extracted under `.tmp/ssanm009b51-next626`.
      - Classification: larger repeated one-shot temp-local reuse in `abs=626` / `defined=599`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 576 local operations (`383` `local.get`, `98` `local.set`, and `95` `local.tee`). The only local-token differences are 62 one-use same-typed write targets: 55 `local.tee` targets plus seven dead `local.set` targets. Starshine declares `191` body i32 locals versus Binaryen `253`.
      - Evidence: targeted `--print-func 626` extraction under `.tmp/ssanm009b51-next626` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `599` is smaller on Starshine (`2932` byte body: `2928` instruction bytes plus `4` local-declaration bytes) than Binaryen (`2974` byte body: `2970` instruction bytes plus `4` local-declaration bytes). The Starshine trace reports `structured-local-writes-mutated`, so this is a structured helper output-shape classification, not a new semantic gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b55] - Classify current debug-WASI `defined=600 abs=627` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b54]`; targeted `--print-func 627` extraction under `.tmp/ssanm009b54-next627` shows a non-exact but masked-equal candidate (`4043` Starshine body_raw characters versus `4045` Binaryen body_raw characters).
      - Classification: repeated one-shot temp-local reuse in `abs=627` / `defined=600`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 98 local operations (`66` `local.get`, `18` `local.set`, and `14` `local.tee`). The only local-token differences are seven one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `13`, `29`, `15`, `16`, and `23`, while Binaryen allocates fresh one-use locals `34..40`.
      - Evidence: targeted `--print-func 627` extraction under `.tmp/ssanm009b54-next627` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `600` encodes to the same `423` byte code body in both artifacts (`420` instruction bytes plus `3` local-declaration bytes), but Starshine declares `32` body i32 locals versus Binaryen `39`. The Starshine trace reports `multiparam-value-if-branch-carrier-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b56] - Classify current debug-WASI `defined=601 abs=628` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b55]`; targeted `--print-func 628` extraction under `.tmp/ssanm009b55-next628` shows a non-exact but masked-equal candidate with equal `3189` character `body_raw` strings.
      - Classification: neutral local-slot permutation in `abs=628` / `defined=601`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 73 local operations (`51` `local.get`, `15` `local.set`, and `7` `local.tee`). Both declare `26` body i32 locals. The differences are a same-typed permutation among four one-use `local.tee` targets plus one set/get pair: Starshine uses locals `26`, `27`, `28`, and `29` for one-use tees and `30` for the set/get pair; Binaryen uses locals `27`, `28`, `29`, and `30` for the one-use tees and `26` for the set/get pair.
      - Evidence: targeted `--print-func 628` extraction under `.tmp/ssanm009b55-next628` plus local-use/diff-token analysis shows the only non-one-use differing local on either side is the matched same-typed set/get pair; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `601` encodes to the same `331` byte code body in both artifacts (`328` instruction bytes plus `3` local-declaration bytes), with equal body-local counts. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b57] - Classify current debug-WASI `defined=602 abs=629` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b56]`; targeted `--print-func 629` extraction under `.tmp/ssanm009b55-next629` shows a non-exact but masked-equal candidate with equal `6695` character `body_raw` strings.
      - Classification: repeated one-shot temp-local reuse in `abs=629` / `defined=602`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 158 local operations (`113` `local.get`, `19` `local.set`, and `26` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `39`, `19`, `20`, `21`, `22`, `24`, `25`, `27`, `28`, `33`, `35`, `16`, `42`, `37`, and `38`, while Binaryen allocates fresh one-use locals `47..61`.
      - Evidence: targeted `--print-func 629` extraction under `.tmp/ssanm009b55-next629` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `602` encodes to the same `733` byte code body in both artifacts (`730` instruction bytes plus `3` local-declaration bytes), but Starshine declares `42` body i32 locals versus Binaryen `57`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b58] - Classify current debug-WASI `defined=603 abs=630` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b57]`; targeted `--print-func 630` extraction under `.tmp/ssanm009b55-next630` shows a non-exact but masked-equal candidate (`7842` Starshine body_raw characters versus `7844` Binaryen body_raw characters).
      - Classification: repeated one-shot temp-local reuse in `abs=630` / `defined=603`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 193 local operations (`130` `local.get`, `38` `local.set`, and `25` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `8`, `9`, `19`, `52`, `51`, `31`, `59`, `33`, `34`, `46`, `37`, `42`, `40`, `41`, and `45`, while Binaryen allocates fresh one-use locals `65..79`.
      - Evidence: targeted `--print-func 630` extraction under `.tmp/ssanm009b55-next630` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `603` encodes to the same `831` byte code body in both artifacts (`828` instruction bytes plus `3` local-declaration bytes), but Starshine declares `62` body i32 locals versus Binaryen `77`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b59] - Classify current debug-WASI `defined=604 abs=631` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b58]`; targeted `--print-func 631` extraction under `.tmp/ssanm009b55-next631` shows a non-exact but masked-equal candidate (`6370` Starshine body_raw characters versus `6372` Binaryen body_raw characters).
      - Classification: repeated one-shot temp-local reuse in `abs=631` / `defined=604`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 154 local operations (`99` `local.get`, `32` `local.set`, and `23` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `17`, `42`, `41`, `26`, `49`, `28`, `29`, `36`, `34`, `35`, and `39`, while Binaryen allocates fresh one-use locals `55..67`.
      - Evidence: targeted `--print-func 631` extraction under `.tmp/ssanm009b55-next631` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `604` encodes to the same `673` byte code body in both artifacts (`670` instruction bytes plus `3` local-declaration bytes), but Starshine declares `54` body i32 locals versus Binaryen `67`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b60] - Classify current debug-WASI `defined=605 abs=632` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b59]`; targeted `--print-func 632` extraction under `.tmp/ssanm009b59-next632` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=632` / `defined=605`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 136 local operations (`87` `local.get`, `19` `local.set`, and `30` `local.tee`). The only local-token differences are nine one-use dead `local.tee` targets: Starshine reuses locals `28`, `5`, `6`, `7`, `11`, `45`, `13`, `14`, and `21`, while Binaryen allocates fresh one-use locals `50..58`.
      - Evidence: targeted `--print-func 632` extraction under `.tmp/ssanm009b59-next632` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `605` encodes to the same `615` byte code body in both artifacts (`612` instruction bytes plus `3` local-declaration bytes), but Starshine declares `49` body i32 locals versus Binaryen `58`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b61] - Classify current debug-WASI `defined=606 abs=633` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b60]`; targeted `--print-func 633` extraction under `.tmp/ssanm009b59-next633` shows a non-exact but masked-equal candidate with equal canonical `body_raw` lengths.
      - Classification: neutral local-slot permutation in `abs=633` / `defined=606`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 70 local operations (`45` `local.get`, `15` `local.set`, and `10` `local.tee`). Both declare `32` body i32 locals. The differences are a same-typed permutation among six one-use `local.tee` targets, two `local.set` / `local.get` pairs, and one dead set-only slot.
      - Evidence: targeted `--print-func 633` extraction under `.tmp/ssanm009b59-next633` plus local-use/diff-token analysis shows no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `606` encodes to the same `342` byte code body in both artifacts (`339` instruction bytes plus `3` local-declaration bytes), with equal body-local counts. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b62] - Classify current debug-WASI `defined=607 abs=634` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b61]`; targeted `--print-func 634` extraction under `.tmp/ssanm009b59-next634` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=634` / `defined=607`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 205 local operations (`133` `local.get`, `33` `local.set`, and `39` `local.tee`). The only local-token differences are 16 one-use dead `local.tee` targets: Starshine reuses locals `47`, `6`, `7`, `8`, `23`, `67`, `25`, `26`, `37`, `19`, `64`, `21`, `22`, `34`, `36`, and `39`, while Binaryen allocates fresh one-use locals `72..87`.
      - Evidence: targeted `--print-func 634` extraction under `.tmp/ssanm009b59-next634` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `607` encodes to the same `913` byte code body in both artifacts (`910` instruction bytes plus `3` local-declaration bytes), but Starshine declares `71` body i32 locals versus Binaryen `87`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b63] - Classify current debug-WASI `defined=608 abs=635` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b62]`; targeted `--print-func 635` extraction under `.tmp/ssanm009b59-next635` shows a non-exact but masked-equal candidate with equal canonical `body_raw` lengths.
      - Classification: neutral local-slot permutation in `abs=635` / `defined=608`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 62 local operations (`43` `local.get`, `13` `local.set`, and `6` `local.tee`). Both declare `23` body i32 locals. The differences are a same-typed permutation among four one-use `local.tee` targets plus one `local.set` / `local.get` pair.
      - Evidence: targeted `--print-func 635` extraction under `.tmp/ssanm009b59-next635` plus local-use/diff-token analysis shows no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `608` encodes to the same `279` byte code body in both artifacts (`276` instruction bytes plus `3` local-declaration bytes), with equal body-local counts. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b64] - Classify current debug-WASI `defined=609 abs=636` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b63]`; targeted `--print-func 636` extraction under `.tmp/ssanm009b63-next636` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=636` / `defined=609`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 233 local operations (`155` `local.get`, `40` `local.set`, and `38` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `54`, `7`, `8`, `9`, `31`, `24`, `71`, `26`, `27`, `34`, `33`, `44`, `41`, `43`, and `46`, while Binaryen allocates fresh one-use locals `78..92`.
      - Evidence: targeted `--print-func 636` extraction under `.tmp/ssanm009b63-next636` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `609` encodes to the same `1030` byte code body in both artifacts (`1027` instruction bytes plus `3` local-declaration bytes), but Starshine declares `76` body i32 locals versus Binaryen `91`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b65] - Classify current debug-WASI `defined=610 abs=637` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe neutral local-slot permutation / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b64]`; targeted `--print-func 637` extraction under `.tmp/ssanm009b64-next637` shows a non-exact but masked-equal candidate with equal canonical `body_raw` lengths.
      - Classification: neutral local-slot permutation in `abs=637` / `defined=610`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 70 local operations (`48` `local.get`, `15` `local.set`, and `7` `local.tee`). Both declare `26` body i32 locals. The differences are a same-typed permutation among four one-use `local.tee` targets plus one `local.set` / `local.get` pair.
      - Evidence: targeted `--print-func 637` extraction under `.tmp/ssanm009b64-next637` plus local-use/diff-token analysis shows no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `610` encodes to the same `321` byte code body in both artifacts (`318` instruction bytes plus `3` local-declaration bytes), with equal body-local counts. The Starshine trace reports `straight-line-local-writes-localgraph-plan`, so this remains LocalGraph-plan output-shape classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b66] - Classify current debug-WASI `defined=611 abs=638` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b65]`; targeted `--print-func 638` extraction under `.tmp/ssanm009b64-next638` shows a non-exact but masked-equal candidate with equal canonical `body_raw` lengths.
      - Classification: repeated one-shot temp-local reuse in `abs=638` / `defined=611`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 178 local operations (`118` `local.get`, `31` `local.set`, and `29` `local.tee`). The only local-token differences are 16 one-use dead `local.tee` targets: Starshine reuses locals `14`, `46`, `16`, `17`, `49`, `19`, `20`, `53`, `52`, `22`, `23`, `56`, `27`, `37`, `34`, and `40`, while Binaryen allocates fresh one-use locals `62..77`.
      - Evidence: targeted `--print-func 638` extraction under `.tmp/ssanm009b64-next638` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `611` encodes to the same `774` byte code body in both artifacts (`771` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `74`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b67] - Classify current debug-WASI `defined=612 abs=639` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b66]`; targeted `--print-func 639` extraction under `.tmp/ssanm009b64-next639` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=639` / `defined=612`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 132 local operations (`91` `local.get`, `25` `local.set`, and `16` `local.tee`). The only local-token differences are 10 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `17`, `39`, `19`, `20`, `23`, `29`, `32`, and `35`, while Binaryen allocates fresh one-use locals `43..52`.
      - Evidence: targeted `--print-func 639` extraction under `.tmp/ssanm009b64-next639` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `612` encodes to the same `565` byte code body in both artifacts (`562` instruction bytes plus `3` local-declaration bytes), but Starshine declares `41` body i32 locals versus Binaryen `51`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b68] - Classify current debug-WASI `defined=613 abs=640` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b67]`; targeted `--print-func 640` extraction under `.tmp/ssanm009b64-next640` shows a non-exact but masked-equal candidate.
      - Classification: larger repeated one-shot temp-local reuse in `abs=640` / `defined=613`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 531 local operations (`355` `local.get`, `95` `local.set`, and `81` `local.tee`). The only local-token differences are 51 one-use same-typed write targets: 46 `local.tee` targets plus five dead `local.set` targets. Starshine declares `174` body i32 locals versus Binaryen `225`.
      - Evidence: targeted `--print-func 640` extraction under `.tmp/ssanm009b64-next640` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `613` is smaller on Starshine (`2602` byte body: `2598` instruction bytes plus `4` local-declaration bytes) than Binaryen (`2639` byte body: `2635` instruction bytes plus `4` local-declaration bytes). The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b69] - Classify current debug-WASI `defined=614 abs=641` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b68]`; targeted `--print-func 641` extraction under `.tmp/ssanm009b68-next641` shows a non-exact but masked-equal candidate.
      - Classification: larger repeated one-shot temp-local reuse in `abs=641` / `defined=614`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 1028 local operations (`845` `local.get`, `69` `local.set`, and `114` `local.tee`). The only local-token differences are 39 one-use dead `local.tee` targets. Starshine declares `178` body i32 locals versus Binaryen `217`.
      - Evidence: targeted `--print-func 641` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `614` is smaller on Starshine (`6063` byte body: `6059` instruction bytes plus `4` local-declaration bytes) than Binaryen (`6097` byte body: `6093` instruction bytes plus `4` local-declaration bytes). The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b70] - Classify current debug-WASI `defined=615 abs=642` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b69]`; targeted `--print-func 642` extraction under `.tmp/ssanm009b69-next642` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=642` / `defined=615`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 136 local operations (`88` `local.get`, `15` `local.set`, and `33` `local.tee`). The only local-token differences are eight one-use dead `local.tee` targets: Starshine reuses locals `20`, `19`, `15`, `27`, `12`, `13`, `7`, and `16`, while Binaryen allocates fresh one-use locals `50..57`.
      - Evidence: targeted `--print-func 642` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `615` encodes to the same `629` byte code body in both artifacts (`626` instruction bytes plus `3` local-declaration bytes), but Starshine declares `48` body i32 locals versus Binaryen `56`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b71] - Classify current debug-WASI `defined=616 abs=643` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b70]`; targeted `--print-func 643` extraction under `.tmp/ssanm009b69-next643` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=643` / `defined=616`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 136 local operations (`87` `local.get`, `19` `local.set`, and `30` `local.tee`). The only local-token differences are nine one-use dead `local.tee` targets: Starshine reuses locals `28`, `5`, `6`, `7`, `11`, `45`, `13`, `14`, and `21`, while Binaryen allocates fresh one-use locals `50..58`.
      - Evidence: targeted `--print-func 643` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `616` encodes to the same `615` byte code body in both artifacts (`612` instruction bytes plus `3` local-declaration bytes), but Starshine declares `49` body i32 locals versus Binaryen `58`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b72] - Classify current debug-WASI `defined=617 abs=644` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b71]`; targeted `--print-func 644` extraction under `.tmp/ssanm009b69-next644` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=644` / `defined=617`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 83 local operations (`53` `local.get`, `18` `local.set`, and `12` `local.tee`). The only local-token differences are 10 one-use same-typed write targets: nine `local.tee` targets plus one dead `local.set`. Starshine reuses locals `11`, `23`, `15`, `18`, `26`, `27`, `16`, `4`, `21`, and `25`, while Binaryen allocates fresh one-use locals `32..41`.
      - Evidence: targeted `--print-func 644` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `617` encodes to the same `399` byte code body in both artifacts (`396` instruction bytes plus `3` local-declaration bytes), but Starshine declares `30` body i32 locals versus Binaryen `40`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b73] - Classify current debug-WASI `defined=618 abs=645` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b72]`; targeted `--print-func 645` extraction under `.tmp/ssanm009b69-next645` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=645` / `defined=618`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 115 local operations (`73` `local.get`, `22` `local.set`, and `20` `local.tee`). The only local-token differences are 10 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `15`, `35`, `17`, `19`, `38`, `21`, `22`, and `27`, while Binaryen allocates fresh one-use locals `43..52`.
      - Evidence: targeted `--print-func 645` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `618` encodes to the same `505` byte code body in both artifacts (`502` instruction bytes plus `3` local-declaration bytes), but Starshine declares `42` body i32 locals versus Binaryen `52`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b74] - Classify current debug-WASI `defined=619 abs=646` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b73]`; targeted `--print-func 646` extraction under `.tmp/ssanm009b73-next646` shows a non-exact but masked-equal candidate (`6536` Starshine `body_raw` characters versus `6538` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=646` / `defined=619`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 140 local operations (`89` `local.get`, `30` `local.set`, and `21` `local.tee`). The only local-token differences are 16 one-use same-typed write targets: 14 `local.tee` targets plus two dead `local.set` targets. Starshine reuses locals `19`, `41`, `23`, `36`, `44`, `45`, `34`, `5`, `33`, `25`, `26`, `46`, `9`, `32`, `39`, and `43`, while Binaryen allocates fresh one-use locals `54..69`.
      - Evidence: targeted `--print-func 646` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `619` encodes to the same `689` byte code body in both artifacts (`686` instruction bytes plus `3` local-declaration bytes), but Starshine declares `51` body i32 locals versus Binaryen `67`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b75] - Classify current debug-WASI `defined=621 abs=648` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference. Targeted `abs=647` / `defined=620` output matched exactly and was skipped as a candidate.
      - Goal: classify the next non-exact current self-artifact print diff after `[SSANM-009b74]`; targeted `--print-func 648` extraction under `.tmp/ssanm009b74-next648` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=648` / `defined=621`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 80 local operations (`54` `local.get`, `13` `local.set`, and `13` `local.tee`). The only local-token differences are eight one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `17`, `13`, `25`, `15`, `16`, and `21`, while Binaryen allocates fresh one-use locals `28..35`.
      - Evidence: targeted `--print-func 648` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `621` encodes to the same `341` byte code body in both artifacts (`338` instruction bytes plus `3` local-declaration bytes), but Starshine declares `26` body i32 locals versus Binaryen `34`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b76] - Classify current debug-WASI `defined=622 abs=649` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b75]`; targeted `--print-func 649` extraction under `.tmp/ssanm009b74-next649` shows a non-exact but masked-equal candidate.
      - Classification: same-typed local-slot permutation in `abs=649` / `defined=622`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 72 local operations (`50` `local.get`, `15` `local.set`, and `7` `local.tee`). The only local-token differences are four one-use `local.tee` targets plus one set/get pair: Starshine uses tee locals `25`, `26`, `27`, and `28` plus set/get local `29`; Binaryen uses tee locals `26`, `27`, `28`, and `29` plus set/get local `25`.
      - Evidence: targeted `--print-func 649` extraction plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one set/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `622` encodes to the same `329` byte code body in both artifacts (`326` instruction bytes plus `3` local-declaration bytes), and both declare `26` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b77] - Classify current debug-WASI `defined=623 abs=650` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b76]`; targeted `--print-func 650` extraction under `.tmp/ssanm009b74-next650` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=650` / `defined=623`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 239 local operations (`169` `local.get`, `35` `local.set`, and `35` `local.tee`). The only local-token differences are 19 one-use dead `local.tee` targets: Starshine reuses locals `18`, `56`, `20`, `21`, `59`, `23`, `24`, `63`, `62`, `26`, `27`, `66`, `31`, `41`, `38`, `13`, `53`, `43`, and `46`, while Binaryen allocates fresh one-use locals `72..90`.
      - Evidence: targeted `--print-func 650` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `623` encodes to the same `1086` byte code body in both artifacts (`1083` instruction bytes plus `3` local-declaration bytes), but Starshine declares `68` body i32 locals versus Binaryen `87`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b78] - Classify current debug-WASI `defined=624 abs=651` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b77]`; targeted `--print-func 651` extraction under `.tmp/ssanm009b77-next651` shows a non-exact but masked-equal candidate (`6949` Starshine `body_raw` characters versus `6951` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=651` / `defined=624`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 162 local operations (`104` `local.get`, `32` `local.set`, and `26` `local.tee`). The only local-token differences are 15 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `29`, `15`, `49`, `17`, `32`, `34`, `25`, `52`, `27`, `28`, `41`, `40`, and `43`, while Binaryen allocates fresh one-use locals `58..72`.
      - Evidence: targeted `--print-func 651` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `624` encodes to the same `728` byte code body in both artifacts (`725` instruction bytes plus `3` local-declaration bytes), but Starshine declares `57` body i32 locals versus Binaryen `72`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b79] - Classify current debug-WASI `defined=625 abs=652` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b78]`; targeted `--print-func 652` extraction under `.tmp/ssanm009b78-next652` shows a non-exact but masked-equal candidate (`7008` Starshine `body_raw` characters versus `7010` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=652` / `defined=625`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 171 local operations (`112` `local.get`, `36` `local.set`, and `23` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `18`, `46`, `45`, `27`, `53`, `29`, `30`, `40`, `37`, `39`, and `43`, while Binaryen allocates fresh one-use locals `59..71`.
      - Evidence: targeted `--print-func 652` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `625` encodes to the same `738` byte code body in both artifacts (`735` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `71`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b80] - Classify current debug-WASI `defined=626 abs=653` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b79]`; targeted `--print-func 653` extraction under `.tmp/ssanm009b78-next653` shows a non-exact but masked-equal candidate.
      - Classification: same-typed local-slot permutation in `abs=653` / `defined=626`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 67 local operations (`45` `local.get`, `15` `local.set`, and `7` `local.tee`). The only local-token differences are four one-use `local.tee` targets plus one set/get pair: Starshine uses tee locals `23`, `24`, `25`, and `26` plus set/get local `27`; Binaryen uses tee locals `24`, `25`, `26`, and `27` plus set/get local `23`.
      - Evidence: targeted `--print-func 653` extraction plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one set/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `626` encodes to the same `313` byte code body in both artifacts (`310` instruction bytes plus `3` local-declaration bytes), and both declare `26` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b81] - Classify current debug-WASI `defined=627 abs=654` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b80]`; targeted `--print-func 654` extraction under `.tmp/ssanm009b78-next654` shows a non-exact but masked-equal candidate (`6868` Starshine `body_raw` characters versus `6870` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=654` / `defined=627`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 168 local operations (`109` `local.get`, `36` `local.set`, and `23` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `6`, `7`, `18`, `46`, `45`, `27`, `53`, `29`, `30`, `40`, `37`, `39`, and `43`, while Binaryen allocates fresh one-use locals `59..71`.
      - Evidence: targeted `--print-func 654` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `627` encodes to the same `721` byte code body in both artifacts (`718` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `71`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b82] - Classify current debug-WASI `defined=628 abs=655` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b81]`; targeted `--print-func 655` extraction under `.tmp/ssanm009b78-next655` shows a non-exact but masked-equal candidate.
      - Classification: repeated one-shot temp-local reuse in `abs=655` / `defined=628`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 93 local operations (`62` `local.get`, `20` `local.set`, and `11` `local.tee`). The only local-token differences are eight one-use dead `local.tee` targets: Starshine reuses locals `15`, `26`, `19`, `21`, `29`, `30`, `24`, and `28`, while Binaryen allocates fresh one-use locals `35..42`.
      - Evidence: targeted `--print-func 655` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `628` encodes to the same `431` byte code body in both artifacts (`428` instruction bytes plus `3` local-declaration bytes), but Starshine declares `31` body i32 locals versus Binaryen `39`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b83] - Classify current debug-WASI `defined=629 abs=656` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b82]`; targeted `--print-func 656` extraction under `.tmp/ssanm009b82-next656` shows a non-exact but masked-equal candidate (`8602` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=656` / `defined=629`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 209 local operations (`151` `local.get`, `27` `local.set`, and `31` `local.tee`). The only local-token differences are 11 one-use dead `local.tee` targets: Starshine reuses locals `49`, `17`, `18`, `19`, `24`, `66`, `26`, `27`, `31`, `42`, and `45`, while Binaryen allocates fresh one-use locals `70..80`.
      - Evidence: targeted `--print-func 656` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `629` encodes to the same `882` byte code body in both artifacts (`879` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `69`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b84] - Classify current debug-WASI `defined=631 abs=658` candidate after exact `abs=657`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: skip targeted exact `defined=630 abs=657`, then classify the next current self-artifact print diff; targeted `--print-func 658` extraction under `.tmp/ssanm009b83-next658` shows a non-exact but masked-equal candidate (`3002` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=658` / `defined=631`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 67 local operations (`45` `local.get`, `15` `local.set`, and `7` `local.tee`). The only local-token differences are four one-use `local.tee` targets plus one set/get pair: Starshine uses tee locals `23`, `24`, `25`, and `26` plus set/get local `27`; Binaryen uses tee locals `24`, `25`, `26`, and `27` plus set/get local `23`.
      - Evidence: targeted `--print-func 657` output matches exactly, and targeted `--print-func 658` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one set/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `631` encodes to the same `313` byte code body in both artifacts (`310` instruction bytes plus `3` local-declaration bytes), and both declare `26` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b85] - Classify current debug-WASI `defined=632 abs=659` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b84]`; targeted `--print-func 659` extraction under `.tmp/ssanm009b83-next659` shows a non-exact but masked-equal candidate (`4826` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=659` / `defined=632`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 117 local operations (`67` `local.get`, `29` `local.set`, and `21` `local.tee`). The local-token differences are a permutation among 12 one-use `local.tee` targets, one set-only slot, and two set/get pairs: Starshine puts the set/get pairs on locals `60` and `61`, while Binaryen puts them on locals `47` and `48`; the one-use set-only slot is Starshine local `50` versus Binaryen local `52`.
      - Evidence: targeted `--print-func 659` extraction plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one set/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `632` encodes to the same `503` byte code body in both artifacts (`500` instruction bytes plus `3` local-declaration bytes), and both declare `60` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b86] - Classify current debug-WASI `defined=633 abs=660` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b85]`; targeted `--print-func 660` extraction under `.tmp/ssanm009b83-next660` shows a non-exact but masked-equal candidate (`5911` Starshine `body_raw` characters versus `5914` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=660` / `defined=633`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 136 local operations (`87` `local.get`, `19` `local.set`, and `30` `local.tee`). The only local-token differences are nine one-use dead `local.tee` targets: Starshine reuses locals `28`, `5`, `6`, `7`, `11`, `45`, `13`, `14`, and `21`, while Binaryen allocates fresh one-use locals `50..58`.
      - Evidence: targeted `--print-func 660` extraction plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `633` encodes to the same `615` byte code body in both artifacts (`612` instruction bytes plus `3` local-declaration bytes), but Starshine declares `49` body i32 locals versus Binaryen `58`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b87] - Classify current debug-WASI `defined=634 abs=661` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine mixed i32/i64 local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b86]`; targeted `--print-func 661` extraction under `.tmp/ssanm009b83-next661` shows a non-exact but masked-equal candidate (`9373` Starshine `body_raw` characters versus `9374` Binaryen `body_raw` characters).
      - Classification: mixed i32/i64 temp-local reuse in `abs=661` / `defined=634`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 222 local operations (`139` `local.get`, `52` `local.set`, and `31` `local.tee`). The local-token differences are 12 one-use i32 `local.tee` targets, three one-use i64 `local.tee` targets, and three same-typed i64 tee/get pairs. Starshine declares `(73 i32, 6 i64)` body locals; Binaryen declares `(85 i32, 9 i64)`.
      - Evidence: targeted `--print-func 661` extraction plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one tee/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `634` encodes to the same `1001` byte code body in both artifacts (`996` instruction bytes plus `5` local-declaration bytes), but Starshine declares 15 fewer body locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b88] - Classify current debug-WASI `defined=636 abs=663` candidate after exact `abs=662`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: skip targeted exact `defined=635 abs=662`, then classify the next current self-artifact print diff; targeted `--print-func 663` extraction under `.tmp/ssanm009b87-next663` shows a non-exact but masked-equal candidate (`7264` Starshine `body_raw` characters versus `7265` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=663` / `defined=636`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 165 local operations (`106` `local.get`, `30` `local.set`, and `29` `local.tee`). The local-token differences are 16 one-use `local.tee` targets plus one one-use `local.set` target: Starshine reuses locals `34`, `7`, `43`, `12`, `13`, `46`, `15`, `16`, `50`, `49`, `18`, `19`, `53`, `23`, `33`, `30`, and `37`, while Binaryen allocates fresh one-use locals `59..75`.
      - Evidence: targeted `--print-func 663` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed write target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `636` encodes to the same `772` byte code body in both artifacts (`769` instruction bytes plus `3` local-declaration bytes), but Starshine declares `57` body i32 locals versus Binaryen `74`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b89] - Classify current debug-WASI `defined=637 abs=664` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b88]`; targeted `--print-func 664` extraction under `.tmp/ssanm009b87-next664` shows a non-exact but masked-equal candidate (`18192` Starshine `body_raw` characters versus `18221` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=664` / `defined=637`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 440 local operations (`302` `local.get`, `90` `local.set`, and `48` `local.tee`). The only local-token differences are 30 one-use dead `local.tee` targets: Starshine reuses locals `7`, `8`, `51`, `29`, `118`, `31`, `32`, `63`, `55`, `58`, `61`, `62`, `74`, `69`, `72`, `73`, `76`, `80`, `83`, `101`, `47`, `100`, `99`, `98`, `92`, `86`, `88`, `90`, `91`, and `95`, while Binaryen allocates fresh one-use locals `135..164`.
      - Evidence: targeted `--print-func 664` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `637` encodes smaller in Starshine: `1900` bytes versus Binaryen `1930` (`1896` versus `1926` instruction bytes plus `4` local-declaration bytes), and Starshine declares `133` body i32 locals versus Binaryen `163`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b90] - Classify current debug-WASI `defined=638 abs=665` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b89]`; targeted `--print-func 665` extraction under `.tmp/ssanm009b88-next665` shows a non-exact but masked-equal candidate (`7745` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=665` / `defined=638`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 190 local operations (`130` `local.get`, `31` `local.set`, and `29` `local.tee`). The only local-token differences are 16 one-use dead `local.tee` targets: Starshine reuses locals `15`, `47`, `17`, `18`, `50`, `20`, `21`, `54`, `53`, `23`, `24`, `57`, `28`, `38`, `35`, and `41`, while Binaryen allocates fresh one-use locals `63..78`.
      - Evidence: targeted `--print-func 665` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `638` encodes to the same `814` byte code body in both artifacts (`811` instruction bytes plus `3` local-declaration bytes), but Starshine declares `58` body i32 locals versus Binaryen `74`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b91] - Classify current debug-WASI `defined=639 abs=666` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b90]`; targeted `--print-func 666` extraction under `.tmp/ssanm009b88-next666` shows a non-exact but masked-equal candidate (`3395` Starshine `body_raw` characters versus `3397` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=666` / `defined=639`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 78 local operations (`54` `local.get`, `5` `local.set`, and `19` `local.tee`). The only local-token differences are three one-use dead `local.tee` targets: Starshine reuses locals `11`, `5`, and `6`, while Binaryen allocates fresh one-use locals `27..29`.
      - Evidence: targeted `--print-func 666` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `639` encodes to the same `365` byte code body in both artifacts (`362` instruction bytes plus `3` local-declaration bytes), but Starshine declares `24` body i32 locals versus Binaryen `27`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b92] - Classify current debug-WASI `defined=640 abs=667` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b91]`; targeted `--print-func 667` extraction under `.tmp/ssanm009b88-next667` shows a non-exact but masked-equal candidate (`3005` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=667` / `defined=640`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 77 local operations (`47` `local.get`, `18` `local.set`, and `12` `local.tee`). The local-token differences are five one-use `local.tee` targets plus three set/get pairs permuted among body i32 locals `29..36`.
      - Evidence: targeted `--print-func 667` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one set/get slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `640` encodes to the same `320` byte code body in both artifacts (`317` instruction bytes plus `3` local-declaration bytes), and both declare `35` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b93] - Classify current debug-WASI `defined=641 abs=668` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b92]`; targeted `--print-func 668` extraction under `.tmp/ssanm009b88-next668` shows a non-exact but masked-equal candidate (`5890` Starshine `body_raw` characters versus `5891` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=668` / `defined=641`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 135 local operations (`82` `local.get`, `20` `local.set`, and `33` `local.tee`). The only local-token differences are 10 one-use dead `local.tee` targets: Starshine reuses locals `11`, `12`, `20`, `47`, `8`, `19`, `31`, `16`, `17`, and `18`, while Binaryen allocates fresh one-use locals `53..62`.
      - Evidence: targeted `--print-func 668` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `641` encodes to the same `622` byte code body in both artifacts (`619` instruction bytes plus `3` local-declaration bytes), but Starshine declares `52` body i32 locals versus Binaryen `62`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b94] - Classify current debug-WASI `defined=642 abs=669` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b93]`; targeted `--print-func 669` extraction under `.tmp/ssanm009b93-next669` shows a non-exact but masked-equal candidate (`3432` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=669` / `defined=642`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 81 local operations (`54` `local.get`, `16` `local.set`, and `11` `local.tee`). The only local-token differences are two one-use dead `local.tee` targets: Starshine reuses locals `11` and `26`, while Binaryen allocates fresh one-use locals `29` and `30`.
      - Evidence: targeted `--print-func 669` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `642` encodes to the same `365` byte code body in both artifacts (`362` instruction bytes plus `3` local-declaration bytes), but Starshine declares `27` body i32 locals versus Binaryen `29`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b95] - Classify current debug-WASI `defined=644 abs=671` candidate after exact `abs=670`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: skip targeted exact `defined=643 abs=670`, then classify the next current self-artifact print diff; targeted `--print-func 671` extraction under `.tmp/ssanm009b94-next671` shows a non-exact but masked-equal candidate (`5393` Starshine `body_raw` characters versus `5395` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=671` / `defined=644`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 122 local operations (`73` `local.get`, `15` `local.set`, and `34` `local.tee`). The only local-token differences are 12 one-use dead `local.tee` targets: Starshine reuses locals `12`, `13`, `26`, `8`, `9`, `25`, `31`, `22`, `23`, `24`, `28`, and `27`, while Binaryen allocates fresh one-use locals `49..60`.
      - Evidence: targeted `--print-func 671` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `644` encodes to the same `574` byte code body in both artifacts (`571` instruction bytes plus `3` local-declaration bytes), but Starshine declares `47` body i32 locals versus Binaryen `59`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b96] - Classify current debug-WASI `defined=645 abs=672` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b95]`; targeted `--print-func 672` extraction under `.tmp/ssanm009b94-next672` shows a non-exact but masked-equal candidate (`6990` Starshine `body_raw` characters versus `6993` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=672` / `defined=645`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 158 local operations (`103` `local.get`, `21` `local.set`, and `34` `local.tee`). The only local-token differences are 12 one-use dead `local.tee` targets: Starshine reuses locals `33`, `6`, `7`, `8`, `22`, `18`, `50`, `20`, `21`, `24`, `23`, and `26`, while Binaryen allocates fresh one-use locals `56..67`.
      - Evidence: targeted `--print-func 672` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `645` encodes to the same `736` byte code body in both artifacts (`733` instruction bytes plus `3` local-declaration bytes), but Starshine declares `54` body i32 locals versus Binaryen `66`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b97] - Classify current debug-WASI `defined=646 abs=673` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b96]`; targeted `--print-func 673` extraction under `.tmp/ssanm009b94-next673` shows a non-exact but masked-equal candidate (`4422` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=673` / `defined=646`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 95 local operations (`58` `local.get`, `10` `local.set`, and `27` `local.tee`). The local-token differences are a same-typed permutation among nine one-use `local.tee` targets plus two paired set/get slots over body i32 locals `35..45`.
      - Evidence: targeted `--print-func 673` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `646` encodes to the same `465` byte code body in both artifacts (`462` instruction bytes plus `3` local-declaration bytes), and both declare `45` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b98] - Classify current debug-WASI `defined=647 abs=674` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b97]`; targeted `--print-func 674` extraction under `.tmp/ssanm009b94-next674` shows a non-exact but masked-equal candidate (`5319` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=674` / `defined=647`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 134 local operations (`89` `local.get`, `21` `local.set`, and `24` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `12`, `32`, `14`, `15`, `35`, `17`, `18`, `39`, `38`, `20`, `21`, `42`, and `25`, while Binaryen allocates fresh one-use locals `47..59`.
      - Evidence: targeted `--print-func 674` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `647` encodes to the same `557` byte code body in both artifacts (`554` instruction bytes plus `3` local-declaration bytes), but Starshine declares `43` body i32 locals versus Binaryen `56`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b99] - Classify current debug-WASI `defined=649 abs=676` candidate after exact `abs=675`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: skip targeted exact `defined=648 abs=675`, then classify the next current self-artifact print diff; targeted `--print-func 676` extraction under `.tmp/ssanm009b98-next676` shows a non-exact but masked-equal candidate (`7713` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=676` / `defined=649`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 179 local operations (`118` `local.get`, `24` `local.set`, and `37` `local.tee`). The only local-token differences are 13 one-use dead `local.tee` targets: Starshine reuses locals `11`, `13`, `14`, `16`, `17`, `26`, `27`, `28`, `33`, `35`, `36`, `40`, and `43`, while Binaryen allocates fresh one-use locals `62..74`.
      - Evidence: targeted `--print-func 676` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `649` encodes to the same `805` byte code body in both artifacts (`802` instruction bytes plus `3` local-declaration bytes), but Starshine declares `60` body i32 locals versus Binaryen `73`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b100] - Classify current debug-WASI `defined=650 abs=677` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b99]`; targeted `--print-func 677` extraction under `.tmp/ssanm009b99-next677` shows a non-exact but masked-equal candidate (`12565` Starshine `body_raw` characters versus `12581` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=677` / `defined=650`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 312 local operations (`226` `local.get`, `47` `local.set`, and `39` `local.tee`). The only local-token differences are 23 one-use dead `local.tee` targets: Starshine reuses locals `23`, `25`, `27`, `29`, `30`, `32`, `33`, `35`, `36`, `40`, `44`, `54`, `58`, `61`, `63`, `64`, `65`, `68`, `74`, `77`, `80`, `81`, and `84`, while Binaryen allocates fresh one-use locals `93..115`.
      - Evidence: targeted `--print-func 677` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `650` encodes to the same `1326` byte code body in both artifacts (`1323` instruction bytes plus `3` local-declaration bytes), but Starshine declares `84` body i32 locals versus Binaryen `107`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b101] - Classify current debug-WASI `defined=651 abs=678` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b100]`; targeted `--print-func 678` extraction under `.tmp/ssanm009b99-next678` shows a non-exact but masked-equal candidate (`1522` Starshine `body_raw` characters versus `1526` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=678` / `defined=651`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 33 local operations (`19` `local.get`, `6` `local.set`, and `8` `local.tee`). The only local-token differences are five one-use dead `local.tee` targets: Starshine reuses locals `4`, `5`, `6`, `7`, and `10`, while Binaryen allocates fresh one-use locals `14..18`.
      - Evidence: targeted `--print-func 678` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `651` encodes to the same `173` byte code body in both artifacts (`170` instruction bytes plus `3` local-declaration bytes), but Starshine declares `13` body i32 locals versus Binaryen `18`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b102] - Classify current debug-WASI `defined=652 abs=679` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b101]`; targeted `--print-func 679` extraction under `.tmp/ssanm009b99-next679` shows a non-exact but masked-equal candidate (`4285` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=679` / `defined=652`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 98 local operations (`60` `local.get`, `20` `local.set`, and `18` `local.tee`). The local-token differences are a same-typed permutation among six one-use `local.tee` targets plus two set/get/set slots over body i32 locals `35..42`.
      - Evidence: targeted `--print-func 679` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `652` encodes to the same `455` byte code body in both artifacts (`452` instruction bytes plus `3` local-declaration bytes), and both declare `43` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b103] - Classify current debug-WASI `defined=653 abs=680` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b102]`; targeted `--print-func 680` extraction under `.tmp/ssanm009b99-next680` shows a non-exact but masked-equal candidate (`3582` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=680` / `defined=653`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 84 local operations (`50` `local.get`, `15` `local.set`, and `19` `local.tee`). The local-token differences are a same-typed permutation among 11 one-use `local.tee` targets plus one paired set/get slot over body i32 locals `32..43`.
      - Evidence: targeted `--print-func 680` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `653` encodes to the same `395` byte code body in both artifacts (`392` instruction bytes plus `3` local-declaration bytes), and both declare `42` body i32 locals. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b104] - Classify current debug-WASI `defined=655 abs=682` candidate after exact `abs=681`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: skip targeted exact `defined=654 abs=681`, then classify the next current self-artifact print diff; targeted `--print-func 682` extraction under `.tmp/ssanm009b99-next682` shows a non-exact but masked-equal candidate (`2910` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=682` / `defined=655`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 64 local operations (`36` `local.get`, `13` `local.set`, and `15` `local.tee`). The local-token differences are a same-typed permutation among three one-use `local.tee` targets, one set/get/set slot, and one set/get slot over body i32 and i64 locals.
      - Evidence: targeted `--print-func 682` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `655` encodes to the same `335` byte code body in both artifacts (`330` instruction bytes plus `5` local-declaration bytes), and both declare `(33 i32, 4 i64)` body locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b105] - Classify current debug-WASI `defined=657 abs=684` candidate after exact `abs=683`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: skip targeted exact `defined=656 abs=683`, then classify the next current self-artifact print diff; targeted `--print-func 684` extraction under `.tmp/ssanm009b104-next684` shows a non-exact but masked-equal candidate (`6916` Starshine and Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=684` / `defined=657`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 150 local operations (`98` `local.get`, `27` `local.set`, and `25` `local.tee`). The only local-token differences are 16 one-use dead `local.tee` targets: Starshine reuses locals `21`, `22`, `24`, `25`, `26`, `28`, `29`, `31`, `32`, `33`, `34`, `35`, `36`, `39`, `41`, and `43`, while Binaryen allocates fresh one-use locals `53..68`.
      - Evidence: targeted `--print-func 684` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `657` encodes to the same `742` byte code body in both artifacts (`739` instruction bytes plus `3` local-declaration bytes), but Starshine declares `50` body i32 locals versus Binaryen `66`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b106] - Classify current debug-WASI `defined=662 abs=689` candidate after exact `abs=685..688`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win.
      - Goal: skip targeted exact `defined=658..661 abs=685..688`, then classify the next current self-artifact print diff; targeted `--print-func 689` extraction under `.tmp/ssanm009b105-next689` shows a non-exact but masked-equal candidate (`52006` Starshine `body_raw` characters versus `52027` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=689` / `defined=662`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 1233 local operations (`724` `local.get`, `287` `local.set`, and `222` `local.tee`). The only local-token differences are 106 one-use dead `local.tee` targets: Starshine reuses lower same-typed i32 locals, while Binaryen allocates fresh one-use locals `490..595`.
      - Evidence: targeted `--print-func 689` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `662` is smaller in Starshine (`6141` bytes: `6137` instruction bytes plus `4` local-declaration bytes) than Binaryen (`6163` bytes: `6159` instruction bytes plus `4` local-declaration bytes), and Starshine declares `489` body i32 locals versus Binaryen `595`. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b107] - Classify current debug-WASI `defined=663 abs=690` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b106]`; targeted `--print-func 690` extraction under `.tmp/ssanm009b105-next690` shows a non-exact but masked-equal candidate (`2544` Starshine `body_raw` characters versus `2545` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=690` / `defined=663`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 52 local operations (`33` `local.get`, `7` `local.set`, and `12` `local.tee`). The only local-token differences are seven one-use dead `local.tee` targets: Starshine reuses locals `8`, `10`, `11`, `12`, `13`, `14`, and `15`, while Binaryen allocates fresh one-use locals `19..25`.
      - Evidence: targeted `--print-func 690` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `663` encodes to the same `295` byte code body in both artifacts (`292` instruction bytes plus `3` local-declaration bytes), but Starshine declares `18` body i32 locals versus Binaryen `25`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b108] - Classify current debug-WASI `defined=664 abs=691` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b107]`; targeted `--print-func 691` extraction under `.tmp/ssanm009b107-next691` shows a non-exact but masked-equal candidate (`12966` Starshine `body_raw` characters versus `12983` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=691` / `defined=664`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 342 local operations (`218` `local.get`, `84` `local.set`, and `40` `local.tee`). The only local-token differences are 17 one-use dead `local.tee` targets: Starshine reuses locals `18`, `19`, `29`, `33`, `38`, `51`, `53`, `55`, `64`, `65`, `66`, `67`, `68`, `72`, `75`, `78`, and `81`, while Binaryen allocates fresh one-use locals `126..142`.
      - Evidence: targeted `--print-func 691` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `664` is smaller in Starshine (`1333` bytes: `1330` instruction bytes plus `3` local-declaration bytes) than Binaryen (`1349` bytes: `1345` instruction bytes plus `4` local-declaration bytes), and Starshine declares `124` body i32 locals versus Binaryen `141`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b109] - Classify current debug-WASI `defined=669 abs=696` candidate after exact `abs=692..695`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: skip targeted exact `defined=665..668 abs=692..695`, then classify the next current self-artifact print diff; targeted `--print-func 696` extraction under `.tmp/ssanm009b108-next696` shows a non-exact but masked-equal candidate (`941` Starshine and Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=696` / `defined=669`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 26 local operations (`17` `local.get`, `7` `local.set`, and `2` `local.tee`). The local-token differences are a same-typed permutation over body i32 locals `10` and `11`: one paired set/get slot and one one-use `local.tee` target swap sides.
      - Evidence: targeted `--print-func 696` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `669` encodes to the same `101` byte code body in both artifacts (`98` instruction bytes plus `3` local-declaration bytes), and both declare `10` body i32 locals. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b110] - Classify current debug-WASI `defined=670 abs=697` candidate
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as semantic-safe neutral local-slot drift.
      - Goal: classify the next current self-artifact print diff after `[SSANM-009b109]`; targeted `--print-func 697` extraction under `.tmp/ssanm009b108-next697` shows a non-exact but masked-equal candidate (`903` Starshine `body_raw` characters versus `902` Binaryen `body_raw` characters).
      - Classification: same-typed local-slot permutation in `abs=697` / `defined=670`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 25 local operations (`16` `local.get`, `7` `local.set`, and `2` `local.tee`). The local-token differences are a same-typed permutation over body i32 locals `9..11`: two paired set/get slots and one one-use `local.tee` target rotate across those slots.
      - Evidence: targeted `--print-func 697` plus local-use/diff-token analysis shows every differing local is same-typed and either one-use or paired on one local slot; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `670` encodes to the same `97` byte code body in both artifacts (`94` instruction bytes plus `3` local-declaration bytes), and both declare `10` body i32 locals. The Starshine trace reports `structured-local-writes`, so this remains representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b111] - Classify current debug-WASI `defined=676 abs=703` candidate after exact `abs=698..702`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count and encoded-body-size win.
      - Goal: skip targeted exact `defined=671..675 abs=698..702`, then classify the next current self-artifact print diff; targeted `--print-func 703` extraction under `.tmp/ssanm009b111-next703` shows a non-exact but masked-equal candidate (`15975` Starshine `body_raw` characters versus `16011` Binaryen `body_raw` characters).
      - Classification: repeated one-shot temp-local reuse in `abs=703` / `defined=676`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 384 local operations (`212` `local.get`, `70` `local.set`, and `102` `local.tee`). The only local-token differences are 50 one-use dead `local.tee` targets: Starshine reuses same-typed body i32 locals `14`, `15`, `20`, `21`, `27`, `29`, `31`, `35`, `37`, `39`, `43`, `45`, `47`, `51`, `57`, `59`, `61`, `63`, `65`, `67`, `69`, `71`, `73`, `75`, `77`, `79`, `81`, `83`, `85`, `87`, `89`, `91`, `93`, `95`, `97`, `99`, `101`, `103`, `105`, `107`, `109`, `111`, `113`, `115`, `123`, `124`, `126`, `127`, `128`, and `129`, while Binaryen allocates fresh one-use locals `139..188`.
      - Evidence: targeted `--print-func 703` plus local-use/diff-token analysis shows every differing local appears exactly once as a same-typed `local.tee` target on its side; no non-local semantic token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `676` is smaller in Starshine (`1706` bytes: `1702` instruction bytes plus `4` local-declaration bytes) than Binaryen (`1754` bytes: `1750` instruction bytes plus `4` local-declaration bytes), and Starshine declares `137` body i32 locals versus Binaryen `187`. The Starshine trace reports `call-heavy-memory-structured-noop`, so this remains boundary/no-op representation classification rather than a new parity gap. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b112] - Classify current debug-WASI `defined=678 abs=705` candidate after exact `abs=704`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a body-size-neutral but Starshine-local-count-losing parity gap, not an accepted representation win.
      - Goal: skip targeted exact `defined=677 abs=704`, then classify the next current self-artifact print diff; targeted `--print-func 705` extraction under `.tmp/ssanm009b111-next705` shows a non-exact but masked-equal candidate (`477` Starshine `body_raw` characters versus `475` Binaryen `body_raw` characters).
      - Classification: local-count-losing same-type local-slot drift in `abs=705` / `defined=678`. Starshine and Binaryen have identical masked `body_raw` token streams and the same 12 local operations (`6` `local.get`, `2` `local.set`, and `4` `local.tee`). The local-token differences are three one-use `local.tee` targets plus one paired set/get slot: Starshine uses `6`, `7`, and `8` as one-use tee targets and appends paired set/get local `10`, while Binaryen uses `7`, `8`, and `9` as the one-use tee targets and reuses paired set/get local `6`.
      - Evidence: targeted `--print-func 705` plus local-use/diff-token analysis shows no call, load/store memarg, branch/table opcode, label, constant, result type, or structured-control token differs once local indexes are masked. Both current full artifacts validate with `wasm-tools validate --features all`. Defined function `678` encodes to the same `57` byte code body in both artifacts (`54` instruction bytes plus `3` local-declaration bytes), but Starshine declares `10` body i32 locals versus Binaryen `9`. The Starshine trace reports `call-heavy-memory-structured-noop`; because this has no measured Starshine size/local-count win, it is kept open under `[SSANM-009b30a]` rather than classified as acceptable drift. No executable code changed, so direct compare was not rerun beyond the current green `.tmp/pass-fuzz-ssa-nomerge-ssanm009b14-classify-20260614` lane.
    - [x] [SSANM-009b113] - Classify current HEAD debug-WASI `defined=1451 abs=1478` loop-carrier candidate
      - Status: completed 2026-06-14; fresh current-HEAD replay after `[SSANM-009b30a]` and `[SSANM-010b1]` classifies the new first diff as a semantic-safe Starshine local-count and encoded-body-size win, not an implementation gap.
      - Goal: refresh the checked-in debug-WASI direct `--ssa-nomerge` artifact after the scratch-compaction and typed-loop repair commits, then classify the current first diff before adding more SSANM artifact code.
      - Classification: loop-carrier representation difference in `abs=1478` / `defined=1451`. Binaryen routes the next loop pair through a block result carrier, then writes the loop-carried locals and branches to the loop; Starshine writes the same next values directly into the loop-carried locals immediately before the same loop backedge. The two `local.set`s are adjacent to the `br $label` and have no intervening uses/effects, so this is a no-behavior-change carrier-shape difference. Starshine declares `15` body i32 locals versus Binaryen's `16`, avoids Binaryen's block carrier wrapper, and encodes a smaller function body (`174` bytes versus `182`).
      - Evidence: `moon build --target native --release src/cmd` reported `no work to do`. Fresh replay `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --out-dir .tmp/self-ssa-nomerge-current-head-20260614 --starshine-bin target/native/release/build/cmd/cmd.exe --ssa-nomerge` validated both outputs, reported Starshine size `3,149,379` bytes versus Binaryen `3,155,990`, pass-local time `0.195ms` versus `439.084ms`, and current first diff `defined=1451 abs=1478`. Targeted artifacts `func-defined1451-abs1478.{starshine,binaryen}.wat` show the direct-local-update versus block-carrier loop shape; `wasm-tools dump` reports the function body/local deltas above. No executable code changed, so direct compare was not rerun beyond the current committed green lanes.
    - [x] [SSANM-009b114] - Classify current HEAD debug-WASI `defined=1453 abs=1480` candidate after exact `abs=1479`
      - Status: completed 2026-06-14; targeted extraction classifies the candidate as a semantic-safe Starshine local-count win / no-regression representation difference.
      - Goal: skip targeted exact `defined=1452 abs=1479`, then classify the next current self-artifact print diff from `.tmp/self-ssa-nomerge-current-head-20260614` before adding more SSANM artifact code.
      - Classification: repeated one-shot temp-local reuse in `abs=1480` / `defined=1453`. Starshine and Binaryen have identical masked `body_raw` token streams and the same `72` local operations. The only local-token differences are three one-use `local.tee` targets: Starshine uses locals `9`, `7`, and `8`, while Binaryen allocates fresh same-typed locals `13`, `14`, and `15`.
      - Evidence: targeted `--print-func 1480` plus masked-body/local-token analysis shows no non-local semantic token differs once local indexes are masked. Both current full artifacts already validate from `.tmp/self-ssa-nomerge-current-head-20260614`. `wasm-tools dump` shows equal `342` byte function bodies, while Starshine declares `10` body i32 locals versus Binaryen `13`. No executable code changed, so direct compare was not rerun beyond the current committed green lanes.
    - [x] [SSANM-009b115] - Classify current HEAD debug-WASI `abs=1482..1494` candidates
      - Status: completed 2026-06-14; targeted extraction classifies the next batch as semantic-safe representation differences, not implementation gaps.
      - Goal: after `[SSANM-009b114]`, skip exact `abs=1481`, `1483`, `1485`, `1489`, `1491`, and `1493`, then classify the non-exact candidates through `abs=1494` before adding more SSANM artifact code.
      - Classification: `abs=1482`, `1484`, `1486`, `1487`, `1488`, `1492`, and `1494` are repeated one-shot temp-local local-count wins with identical masked `body_raw` token streams and equal encoded function bodies; Starshine declares fewer body locals (`9` vs `12`, `10` vs `14`, `9` vs `12`, `22` vs `27`, `7` vs `9`, `9` vs `12`, and `25` vs `41`, respectively). `abs=1490` is semantic-safe neutral same-typed local-slot drift with identical masked body tokens, equal `66` byte bodies, and equal body-local declarations.
      - Evidence: targeted `--print-func` comparison and masked-body/local-token analysis under `.tmp/self-ssa-nomerge-current-head-20260614`; `wasm-tools dump` function body/local metrics listed above. Both current full artifacts already validate from the current replay. No executable code changed, so direct compare was not rerun beyond the current committed green lanes.
    - [x] [SSANM-009b116] - Fix current HEAD debug-WASI `defined=1474 abs=1501` already-SSA tee drift
      - Status: completed 2026-06-14; reduced an actionable local-count/body-size parity gap and added a narrow raw-path no-op guard.
      - Goal: after `[SSANM-009b115]`, continue targeted current-HEAD comparison from `abs=1495` and fix any non-winning implementation gap before resuming classification.
      - Finding: `abs=1495`, `1497`, and `1498` are masked-local equal Starshine local-count/body-size wins; `abs=1496`, `1499`, and `1500` match exactly. `defined=1474 abs=1501` was not an acceptable representation win: Starshine freshened an already-SSA nested compare-chain `local.tee` from body local `1` to appended local `2`, adding an extra i32 body local and increasing the reduced body metric versus Binaryen's canonical local `1`.
      - Deliverables: red-first fixture `ssa-nomerge leaves already SSA nested compare tee canonical` in `src/passes/ssa_nomerge_test.mbt`; `pass_manager.mbt` now recognizes a narrow branch-free, no-body-`local.set`, single body-local `local.tee` chain whose body-local reads all target that tee and returns the original function with trace reason `structured-already-ssa-tee-noop`.
      - Evidence: focused `ssa_nomerge_test.mbt` passed, `moon fmt`, focused file rerun passed, `moon test src/passes` passed `2473/2473`, `moon info` completed with pre-existing warnings, native `moon build --target native --release src/cmd` completed with pre-existing unused-function warnings, full `moon test` passed `5778/5778`, and direct `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass ssa-nomerge --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` compared `7607/10000` with `0` mismatches/validation/property failures and `20` cached Binaryen command failures. A debug-WASI replay under `.tmp/self-ssa-nomerge-ssanm009b116-already-ssa-tee-20260614` timed out after 600s but produced validating Starshine/Binaryen artifacts; targeted `abs=1501` is now exact, both bodies measure `183` total bytes / `180` instruction bytes / one body i32 local, and the trace reports `structured-already-ssa-tee-noop`. Post-fix targeted scan through `abs=1520` left only exact matches or masked-local equal local-count wins/neutral representation diffs.
    - [x] [SSANM-009b117] - Fix current HEAD debug-WASI `defined=1500 abs=1527` medium structured scratch drift
      - Status: completed 2026-06-14; reduced and fixed the next local-count-losing structured-load scratch family without widening branch-table compaction.
      - Goal: continue targeted current-HEAD comparison after `[SSANM-009b116]`, classify `abs=1521..1534`, and fix the first non-winning local-count gap that has a narrow structured-load scratch owner.
      - Finding: `abs=1521..1524` are masked-local equal Starshine local-count wins, `abs=1525` matches exactly, `abs=1526` is neutral same-count drift, and `defined=1500 abs=1527` was local-count-losing before the fix. The reduced family has 11-12 body locals around one-use structured-load scratches; appending or retaining the scratch is byte-neutral but loses the local-count comparison when unused i32 locals are already present. Neighboring `defined=1501 abs=1528` and `defined=1506 abs=1533` remain local-count-losing masked-equal gaps for a later slice because they are not the same narrow medium structured-load scratch owner.
      - Deliverables: red-first fixture `ssa-nomerge reuses unused body local for medium structured load scratch`; `pass_manager.mbt` now lets the existing scratch compactor handle 11-12 local load-bearing structured scratch functions, re-runs existing scratch compaction after raw structured rewriting, and keeps widened cases off `br_table` shapes so prior branch-table spill guards remain canonical.
      - Evidence: focused `ssa_nomerge_test.mbt` passed `442/442`; `moon test src/passes` passed `2474/2474`; native `moon build --target native --release src/cmd` completed with pre-existing unused-function warnings; `moon info` completed with the three pre-existing GenValid warnings; full `moon test` passed `5779/5779`; direct `ssa-nomerge` compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b117-medium-scratch-fixed-10000` compared `7605/10000` with `0` mismatches/validation/property/generator failures and `20` cached Binaryen command failures. The debug-WASI replay under `.tmp/self-ssa-nomerge-ssanm009b117-medium-scratch-fixed4-20260614` timed out after 600s but produced validating Starshine/Binaryen artifacts; targeted `abs=1527` is now a Starshine local-count win with equal encoded body size (`142` bytes / `139` instruction bytes) and fewer body i32 locals (`8` versus Binaryen `11`).
    - [x] [SSANM-009b118] - Reduce current HEAD debug-WASI local-losing `abs=1528` / `abs=1533` candidates
      - Status: completed 2026-06-15; fixed the actionable nested-compare tee chain and classified the remaining canonicalized `abs=1528` local-count loss as a post-`wasm-opt --strip-debug` representation artifact over a raw Starshine win.
      - Goal: reduce and fix or explicitly classify the remaining post-`[SSANM-009b117]` masked-equal local-count-losing candidates: `defined=1501 abs=1528` (`20` Starshine i32 locals versus Binaryen `19`, equal `467` byte body) and `defined=1506 abs=1533` (`5` versus `4`, equal `339` byte body).
      - Deliverables: red-first fixtures `ssa-nomerge reuses unused body locals for branch dead tees` and `ssa-nomerge reuses unused body local for nested compare tee chain`; `pass_manager.mbt` now compacts no-`local.set` small loaded tee chains, branch/call/load dead-tee loops, and drops local-name subsections when raw `ssa-nomerge` rewrites code so local-declaration trimming remains module-valid.
      - Finding: `defined=1506 abs=1533` is now a Starshine local-count win after canonical strip-debug output: Starshine declares `3` body i32 locals versus Binaryen `4`, with equal `339` body bytes / `336` instruction bytes. For `defined=1501 abs=1528`, Starshine raw output is a measured win (`458` body bytes / `455` instruction bytes / `17` body i32 locals versus Binaryen raw `467` / `464` / `19`), validates after local-name dropping, and has identical masked local-token semantics; Binaryen's subsequent `wasm-opt --strip-debug` canonicalizer expands the Starshine artifact back to `467` / `464` / `20`, so the remaining canonical local-count loss is classified as a post-tool representation artifact, not an SSANM implementation gap.
      - Evidence: focused `ssa_nomerge_test.mbt` passed `444/444`; `moon test src/passes` passed `2476/2476`; native `moon build --target native --release src/cmd` completed with pre-existing unused-function warnings; `moon info` completed with the three pre-existing GenValid warnings; full `moon test` passed `5781/5781`; direct `ssa-nomerge` compare `.tmp/pass-fuzz-ssa-nomerge-ssanm009b118-local-compaction-10000` compared `7600/10000` with `0` mismatches/validation/property/generator failures and `20` cached Binaryen command failures (`19` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`). Debug-WASI replay `.tmp/self-ssa-nomerge-ssanm009b118-local-compaction4-20260615` timed out after 650s but produced validating artifacts for both raw and canonical outputs.
    - [x] [SSANM-009b119] - Refresh direct debug-WASI replay after diagnostic batching
      - Status: completed 2026-06-15; the current authoritative full self-compare anchor is `.tmp/self-ssa-nomerge-artifact-refresh-20260615`, which completes in `16.871s` and reports `defined=2087 abs=2114` because the huge WAT-level function exceeds the conservative pretty-fallback cap.
      - Goal: reconcile the prior `.tmp/self-ssa-nomerge-loop-carrier-fix2-20260615` `defined=32 abs=59` first diff with the post-diagnostic-speed full replay and classify the first current family before adding pass behavior.
      - Classification: `abs=59` is now stale as an actionable semantic issue and is masked-local-equal representation drift in current artifacts (`9` Starshine body i32 locals versus Binaryen `7`, equal `134` body bytes / `131` instruction bytes, identical local-op counts and masked body tokens); because it is local-count-losing, it is not used as a measured Starshine win. The current capped full-replay first diff `defined=2087 abs=2114` is a semantic-safe one-use `local.tee` temp allocation difference: targeted `--print-func 2114` succeeds outside the capped fallback, both sides have identical masked `body_raw` tokens and identical local-op counts (`1828` `local.get`, `406` `local.set`, `331` `local.tee`), and all `156` differing local-token positions are one-use `local.tee` targets with a one-to-one local mapping. Starshine declares fewer body i32 locals (`734` versus Binaryen `890`) with equal encoded body size (`12269` body bytes / `12265` instruction bytes), so `abs=2114` is a measured local-count win and not a parity gap.
      - Evidence: `env SELF_OPT_COMPARE_TIMINGS=1 bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --out-dir .tmp/self-ssa-nomerge-artifact-refresh-20260615 --starshine-bin target/native/release/build/cmd/cmd.exe --ssa-nomerge` exited `0`; both `.tmp/self-ssa-nomerge-artifact-refresh-20260615/{starshine,binaryen}.wasm` validate with `wasm-tools validate --features all`; Starshine size `3149564` bytes versus Binaryen `3156337`; Starshine command `4139.293ms`, raw timed region `1513.860ms`, pass-local `0.165ms` versus Binaryen pass `386.715ms`; trace batches were `512 + 512` canonical print entries before the cap skipped `abs=2114` (`1565066` / `1568030` WAT bytes). Targeted `--print-func 59` and `--print-func 2114` outputs live under `.tmp/ssanm-artifact-targets-20260615`; `bun test scripts/lib/self-opt-task.test.ts` passed `15/15` for the uncommitted diagnostic batching change.
    - [x] [SSANM-009b120] - Classify/fix targeted debug-WASI `defined=6070 abs=6097` branch-exit alias-copy gap
      - Status: completed 2026-06-15; targeted batch printing showed every function from `abs=2116` through `abs=6096` was either exact or masked-local-equal after the `abs=2114` capped huge-function stop, and the first targeted non-masked diff was `defined=6070 abs=6097`.
      - Classification/fix: the pre-fix `abs=6097` tuple was a size-losing / behavior-parity output-shape gap in a structured-local-writes function, not a proven Starshine win: Starshine emitted two extra branch-exit alias scratch/copy pairs before a plain `br` where Binaryen wrote the branch-carried locals directly (`54` body i32 locals and `564` encoded body bytes versus Binaryen `52` locals and `556` bytes). `pass_manager.mbt` now redirects branch-adjacent alias-copy suffixes back to their local-set producers before appended one-use scratch-local compaction, and the reduced fixture now uses the artifact-shaped stack-carried branch-exit input.
      - Evidence: replay `.tmp/self-ssa-nomerge-abs6097-branchcopyfix-20260615` exited `0` in `16.465s`, validated both canonical outputs, and still reports the capped full first diff `defined=2087 abs=2114`; targeted `abs=6097` extraction is now masked-body-equal with identical local-op counts (`101` `local.get`, `40` `local.set`, `8` `local.tee`) and equal encoded function size (`556` body bytes), though Starshine remains local-count-losing (`54` versus Binaryen `52`). The next targeted function diff, `defined=6078 abs=6105`, is masked-body-equal and a Starshine local-count win/no-size-regression representation difference (`33` versus Binaryen `37` body i32 locals, equal `302` body bytes). A normalized WAT scan from `abs=6098` through the end of the module found `364` exact and `223` body-masked matches and no remaining body-nonmasked diffs. Validation: `moon info` passed with the three pre-existing GenValid warnings, `moon fmt`, focused `ssa_nomerge_test.mbt` `449/449`, `moon test src/passes` `2484/2484`, full `moon test` `5789/5789`, native build with pre-existing pass-manager unused-function warnings, and direct compare `.tmp/pass-fuzz-ssa-nomerge-abs6097-branchcopyfix-10000` (`7603/10000` compared, `7603` normalized matches, `0` mismatches, `20` cached Binaryen command failures). Dedicated GenValid smoke/coverage compare probes remain blocked by existing `[SSANM-012c]` profile behavior gaps rather than this artifact slice: `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-abs6097-branchcopyfix-10000` stopped at `27` mismatches out of `54` compared, and `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-abs6097-branchcopyfix-10000` stopped at `29` mismatches out of `59` compared.
    - [ ] [SSANM-009c] - Retire obsolete debug-WASI-specific heuristics
      - Goal: remove or narrow raw artifact-specific allocation hacks once the LocalGraph policy supersedes them.
      - Deliverables: deletion/narrowing commits with focused guards proving retained behavior, artifact replay before/after, and a parity-page table mapping each retired heuristic to the LocalGraph rule that replaced it.
      - Suggested evidence: red/green `ssa_nomerge_test.mbt` for retained guards, `moon test src/passes`, native build, targeted artifact replay, and direct compare evidence.
    - [ ] [SSANM-009d] - Publish debug-WASI artifact parity status
      - Status: child-sliced 2026-06-14; execute only after the current print-diff chain and final direct/profile closeout evidence are available.
      - Goal: make the final artifact state durable without treating raw byte identity as the direct-pass closeout criterion.
      - Deliverables: parity-page summary of final first-diff/equality status, output-size and validation summary, pass-local timing, remaining accepted boundaries with reopening criteria, and backlog cleanup for completed artifact slices.
      - Suggested evidence: docs/wiki update, wiki log entry, targeted replay command, and final direct/pass-profile compare links from `[SSANM-012*]` when available.
    - [ ] [SSANM-009d1] - Gather final debug-WASI artifact status inputs
      - Goal: collect final first-diff/equality status, targeted replay paths, validation status, size/code-section/local-count deltas, and pass-local timing once `[SSANM-009b]` stops moving.
      - Suggested evidence: targeted print extraction, `wasm-tools validate --features all`, artifact byte/section metrics, and current direct/profile compare links.
    - [ ] [SSANM-009d2] - Write the durable artifact status report
      - Goal: publish the final artifact interpretation in the parity page and implementation wiki without treating raw byte identity as the closeout criterion.
      - Suggested evidence: docs/wiki update that separates semantic-safe representation drift, Starshine wins, accepted boundaries with reopening criteria, and unresolved implementation gaps.
    - [ ] [SSANM-009d3] - Prune artifact replay backlog after publication
      - Goal: collapse completed `[SSANM-009b*]` classification chatter into durable docs and leave only active blockers, explicit follow-ups, and final closeout dependencies in `agent-todo.md`.
      - Suggested evidence: backlog diff plus cross-links to the final status report and `[SSANM-012*]` closeout evidence.
    - [x] [SSANM-010] - Decide O4z and preset `ssa-nomerge` scheduling
      - Goal: reconcile Starshine’s removed `o4z-ssa-nomerge-noop` guard with Binaryen’s early no-DWARF `ssa-nomerge` scheduling and nested reruns.
      - Status: completed 2026-06-15 through `[SSANM-010a]` through `[SSANM-010d]`; the broad O4z no-op is removed by explicit user approval, and any exposed correctness issue is release-blocking follow-up work unless a narrow minimized guard is justified.
      - Deliverables: early slot replay, downstream `dead-code-elimination` / `remove-unused-names` / `remove-unused-brs` neighborhood replay, explicit decision to remove the O4z no-op with reopening criteria, and scheduling-mode tests.
    - [x] [SSANM-010a] - Refresh O4z `ssa-nomerge` scheduling anchors
      - Status: completed 2026-06-14; source/docs review refreshed the current Starshine preset/no-op guard and Binaryen `version_130` scheduler anchors before any scheduling decision.
      - Goal: confirm the current Starshine no-DWARF/O4z preset position and Binaryen `version_130` no-DWARF scheduling facts before asking for or applying a scheduling decision.
      - Deliverables: source-backed table with Binaryen early `ssa-nomerge` slot, Starshine preset/no-op guard location, related repeated/nested reruns, and which local tests currently lock the public preset shape.
      - Evidence: `wasm-opt --version` reports `wasm-opt version 130 (version_130)`. The 2026-06-13 Binaryen source refresh confirms `src/passes/pass.cpp` still registers `ssa-nomerge` and schedules it early when `optimizeLevel >= 3 || shrinkLevel >= 1`; the source refresh records the `version_130` `pass.cpp` anchor; source review shows the O4z function prelude as `ssa-nomerge -> flatten -> simplify-locals-notee-nostructure -> local-cse -> dce -> remove-unused-names -> remove-unused-brs ...`, subject to DWARF gating. Starshine `optimize` / `shrink` preset arrays still contain the early `ssa-nomerge` neighborhood and are locked by `registry_test.mbt`, but `pass_manager.mbt` still skips `ssa-nomerge` under O4z-shaped options (`optimize_level >= 4 && shrink_level >= 1`) with `o4z-ssa-nomerge-noop`, locked by `ssa_nomerge_test.mbt`. Focused validation: `moon test --package jtenner/starshine/passes --file registry_test.mbt` passed `6/6`; `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `440/440`. A narrower `--filter 'O4z'` attempt found no matching test entry, so the full adjacent SSANM file was run instead. No preset mutation was made; `[SSANM-010b]` owns replay evidence and `[SSANM-010c]` remains the user-approved scheduling decision.
    - [x] [SSANM-010b] - Replay the early `ssa-nomerge` neighborhood
      - Status: completed 2026-06-14; direct native debug-WASI prefix replay found that the early slot is not currently safe to admit under the O4z-shaped neighborhood.
      - Goal: measure the effect of admitting or bypassing the early O4z `ssa-nomerge` slot before deciding public preset behavior.
      - Deliverables: ordered-prefix or artifact replay covering `ssa-nomerge -> dead-code-elimination -> remove-unused-names -> remove-unused-brs`, validation/runtime status, output-size deltas, pass-local timing, and mismatch classification against Binaryen where practical.
      - Evidence: using the prebuilt native `target/native/release/build/cmd/cmd.exe` on `tests/node/dist/starshine-debug-wasi.wasm`, `--ssa-nomerge` exited `0` in about `4s` and wrote `7,496,071` bytes; `--ssa-nomerge --dead-code-elimination` exited `0` in about `5s` and wrote `7,482,974` bytes; adding `--remove-unused-names` exited `1` in about `5s` with `error: final module validate: stack underflow`, offending `Func 254`. The full requested neighborhood through `--remove-unused-brs --remove-unused-names` also aborts instead of producing a comparable artifact. Binaryen `wasm-opt --all-features` validates all matching prefixes (`3,155,990`, `3,154,783`, `3,150,555`, `3,131,051`, and `3,124,589` bytes respectively). Agent classification: validation failure / scheduling blocker, not a Binaryen-vs-Starshine semantic parity claim. This historical blocker was later superseded by `[SSANM-010b1]` and the 2026-06-15 `[SSANM-010c]` decision to remove the broad guard while treating exposed correctness issues as release-blocking follow-up work.
    - [x] [SSANM-010b1] - Minimize the early-neighborhood `remove-unused-names` validation failure
      - Status: completed 2026-06-14; split from `[SSANM-010b]`; execute before any implementation that admits O4z `ssa-nomerge`.
      - Goal: turn the debug-WASI `--ssa-nomerge --dead-code-elimination --remove-unused-names` stack-underflow blocker into a focused regression fixture and either fix it in `remove-unused-names` cleanup or document the narrow guard needed by `[SSANM-010d]`.
      - Deliverables: minimized or extracted-function reproducer for offending `Func 254` / extracted `Func 25`, red/green test in the owning pass file, validated native replay of the three-pass prefix, and updated scheduling docs.
      - Evidence: the failing extracted `Func 25` / original `Func 254` body contained a no-continue typed loop with an entry-param type index (`loop (type 44)`) after an entry operand. Raw `remove-unused-names` demoted TypeIdx loops to blocks, which can make HOT/lowered entry operands end up inside the replacement block and produce stack underflow. Added red-first `remove-unused-names preserves dead typed loop with entry params`, then made both the raw dispatcher and HOT pass preserve TypeIdx/entry-param loops instead of demoting them. Focused `remove_unused_names_test.mbt` passed `26/26`; `moon fmt` passed; `moon test src/passes` passed `2472/2472`; native `moon build --target native --release src/cmd` completed with pre-existing pass-manager unused-function warnings. Replay with rebuilt native `target/native/release/build/cmd/cmd.exe`: direct `--remove-unused-names` on `.tmp/self-ssa-nomerge-o4z-early-neighborhood-20260614/func254-before.wasm` exited `0`, and `wasm-tools validate --features all .tmp/ssanm010b1-replay/func254-after.wasm` passed; the three-pass prefix `--ssa-nomerge --dead-code-elimination --remove-unused-names tests/node/dist/starshine-debug-wasi.wasm` exited `0` in about `5s` and wrote `7,482,266` bytes. The broader five-pass early neighborhood was not admitted by this slice alone; `[SSANM-010c]` later made the top-level scheduling decision explicitly.
    - [x] [SSANM-010c] - Make the O4z scheduling decision
      - Status: completed 2026-06-15 by explicit user approval after `[SSANM-009b118]`: remove the broad `o4z-ssa-nomerge-noop` guard now, and fix correctness issues before release rather than bypassing the scheduled pass.
      - Goal: choose whether to remove, narrow, or retain the `o4z-ssa-nomerge-noop` guard after source and replay evidence are available.
      - Deliverables: explicit user-approved decision with reopening criteria, recorded in the parity page/backlog/wiki log.
      - Evidence: decision recorded in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`, `docs/wiki/binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `docs/wiki/log.md`. Reopening criteria: any future guard must be narrow, minimized, and documented as release-blocking follow-up work.
    - [x] [SSANM-010d] - Apply and test the chosen preset scheduling policy
      - Status: completed 2026-06-15; the O4z no-op guard was removed and the scheduling-mode regression now asserts `ssa-nomerge` actually runs under `optimize_level=4, shrink_level=1`.
      - Goal: implement the approved scheduling outcome without widening public presets before direct-pass and neighborhood evidence justify it.
      - Deliverables: guard code removal, scheduling-mode test, targeted O4z replay, updated docs/backlog, and rollback criteria.
      - Evidence: red-first `ssa-nomerge runs in O4z scheduling mode` failed under the old guard and now passes; focused `ssa_nomerge_test.mbt` passed `444/444`; `moon fmt`; `moon test src/passes` passed `2478/2478`; native `moon build --target native --release src/cmd` completed with pre-existing unused-function warnings; CLI `-O4z` replay on `tests/repros/o4z-debug-startup-map-init-repro.wasm` wrote `.tmp/ssanm010c-o4z/repro-o4z.wasm` and `wasm-tools validate --features all` passed. Full debug-WASI `-O4z` replay timed out after 300s without producing an artifact, so it is not counted as green release evidence.
    - [x] [SSANM-011] - Add LocalGraph-driven GenValid coverage
      - Goal: generate inputs that exercise the LocalGraph no-merge decision table directly.
      - Status: completed 2026-06-15; child slices `[SSANM-011a]` through `[SSANM-011f]` are complete, and remaining profile compare gaps are final closeout work under `[SSANM-012c]`, not generator-surface work.
      - Evidence: `[SSANM-011a]` inventoried the smoke/coverage/parity profiles; `[SSANM-011b]` added straight-line/default-entry labels and templates; `[SSANM-011c]` added merge and mixed fresh/canonical labels/templates; `[SSANM-011d]` added `local.tee` labels/templates; `[SSANM-011e]` added EH, typed-control, branch-operand, nested-loop, and large-structured boundary floors; `[SSANM-011f]` added branch-block alias, nested enclosing-label, parallel branch-carrier, and tee branch-carrier labels/templates after dedicated lanes showed that branch/enclosing-label local-move shapes needed clearer profile ownership. Focused GenValid/profile tests and manifest probes for each child are recorded below and in the parity page. Dedicated profile compare remains intentionally open for `[SSANM-012c]` because it is blocked by pass behavior/local-allocation differences, not missing generator labels.
    - [x] [SSANM-011a] - Inventory current SSA GenValid profiles and feature labels
      - Status: completed 2026-06-13; inventory lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md` under the `[SSANM-011a]` signoff note.
      - Goal: map the existing `ssa-nomerge-smoke`, `ssa-nomerge-coverage`, and `ssa-nomerge-parity` generator surfaces against the LocalGraph decision table before adding cases.
      - Deliverables: concise table in the SSA no-merge wiki/parity docs covering existing labels/floors, missing families, and which later slice owns each missing shape.
      - Evidence: focused GenValid/profile tests passed (`gen_valid_tests.mbt` `74/74`, `gen_valid_ssa_tests.mbt` `35/35`, `fuzz/main_wbtest.mbt` `90/90`); one-artifact manifest probes for smoke/coverage/parity/stress at seed `0x5eed` all generated valid modules with no skips; native `src/cmd` build completed. Compare probes documented a current caveat: `ssa-nomerge-smoke` `1000` requested stopped at `215` compared / `215` mismatches, and `ssa-nomerge-parity` `100` requested had `100` compared / `100` mismatches, with no validation/generator/command failures; sampled mismatch is a Binaryen behavior-parity/local-allocation gap, so the current `ssa-nomerge-parity` profile name must not be used as green compare evidence until later LocalGraph mutation slices close or rename it.
    - [x] [SSANM-011b] - Add straight-line and default-entry GenValid coverage
      - Status: completed 2026-06-13; signoff note lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: generate small positive shapes for already-SSA locals, single-source write/get influence, dead writes/no-read locals, body-default reads, and parameter-entry reads.
      - Deliverables: added explicit `ssa-already-ssa-local` and `ssa-param-entry-read` feature labels, deterministic smoke/coverage templates for single-source local reads and parameter-entry reads, a five-i32-local SSA profile prefix so the templates are valid on first attempt, and manifest/fuzz JSON output for the new facts.
      - Evidence: red-first tests failed on missing feature fields/labels, then `moon info` passed with the three pre-existing GenValid warnings, `moon fmt` passed, `gen_valid_tests.mbt` passed `74/74`, `gen_valid_ssa_tests.mbt` passed `35/35`, `fuzz/main_wbtest.mbt` passed `90/90`, full `moon test` passed `5708/5708`, and native `src/cmd` build completed with pre-existing pass-manager warnings. One-artifact smoke and coverage manifest probes with required floors for `ssa-already-ssa-local`, `ssa-param-entry-read`, `ssa-dead-local-set`, and `ssa-default-local-read-after-branchy-control` generated `1/1` valid modules with no skips. Direct profile compare remains blocked by existing `ssa-nomerge` local-allocation behavior gaps, not generator validity: smoke `10000` requested stopped at `215/215` mismatches with no validation/generator/command failures; coverage `100` requested with `--max-failures 10` stopped at `25/25` mismatches with no validation/generator/command failures after wider coverage attempts timed out before summary emission.
    - [x] [SSANM-011c] - Add merge and per-write mixed GenValid coverage
      - Status: completed 2026-06-13; signoff note lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: generate no-merge decision-table shapes where one local has freshenable single-source writes plus canonical merge-feeding writes.
      - Deliverables: added explicit feature labels for one-arm body/default merges, one-arm parameter merges, both-arm body-local merges, loop-carried local merges, and mixed fresh/canonical per-write locals; smoke and coverage templates now include deterministic one-arm default and parameter-entry merge slices before mutation-heavy parameter writes.
      - Evidence: red-first `gen_valid_tests.mbt` failed on missing feature fields, then `moon info` passed with the three pre-existing GenValid warnings, `moon fmt` passed, `gen_valid_tests.mbt` passed `74/74`, `gen_valid_ssa_tests.mbt` passed `35/35`, `fuzz/main_wbtest.mbt` passed `90/90`, full `moon test` passed `5708/5708`, and native `src/cmd` build completed with pre-existing pass-manager warnings. One-artifact smoke/coverage manifest probes with required merge/mixed floors generated `1/1` valid modules with no skips. Direct mixed-generator compare requested `10000`, compared `9977`, normalized `9977`, and had `0` mismatches with `23` Binaryen/tool command failures. Direct profile compare remains blocked by existing `ssa-nomerge` local-allocation behavior gaps, not generator validity: smoke `10000` requested stopped at `215/215` mismatches with no validation/generator/command failures; coverage `10000` requested with `--max-failures 50` stopped at `65/65` mismatches with no validation/generator/command failures.
    - [x] [SSANM-011d] - Add `local.tee` GenValid coverage
      - Status: completed 2026-06-13; signoff note lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: make tee writes exercise the same write/get influence paths as `local.set` while preserving stack-result semantics.
      - Deliverables: added explicit `ssa-tee-freshen-candidate`, `ssa-tee-merge-feeding-candidate`, and `ssa-tee-child-expression` feature labels while proving the existing `ssa-dead-local-tee` floor; smoke and coverage templates now include deterministic tee merge, child-expression, and dead tee slices.
      - Evidence: red-first `gen_valid_tests.mbt` failed on missing feature fields, then `moon fmt`, `moon info` passed with the three pre-existing GenValid warnings, focused GenValid tests passed (`74/74`, `35/35`), fuzz wbtests passed (`90/90`), full `moon test` passed `5708/5708`, and native `src/cmd` build completed with pre-existing pass-manager warnings. One-artifact smoke/coverage manifest probes with required tee floors generated `1/1` valid modules with no skips. Direct mixed-generator compare requested `10000`, compared `9977`, normalized `9977`, and had `0` mismatches with `23` Binaryen/tool command failures. Direct profile compare remains blocked by existing `ssa-nomerge` local-allocation behavior gaps, not generator validity: smoke `10000` requested stopped at `215/215` mismatches with no validation/generator/command failures; coverage `10000` requested with `--max-failures 50` stopped at `65/65` mismatches with no validation/generator/command failures.
    - [x] [SSANM-011e] - Add boundary GenValid coverage for EH, typed control, and large structured mixes
      - Status: completed 2026-06-13; signoff note lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: generate fail-closed or classification shapes for exceptional flow, typed loop params/results, branch operands, `br_table`, nested-loop targets, and large structured local-write mixes.
      - Deliverables: added explicit feature labels for `ssa-branch-operand-local-boundary`, `ssa-nested-loop-target-boundary`, and `ssa-large-structured-local-mix`; coverage/stress templates now deterministically floor-prove these labels alongside the existing EH, typed-loop, and nested-CFG fail-closed labels.
      - Evidence: red-first `gen_valid_tests.mbt` failed on missing feature fields; then `moon fmt`, `moon info` passed with the three pre-existing GenValid warnings, focused GenValid tests passed (`74/74`, `36/36`), fuzz wbtests passed (`90/90`), full `moon test` passed (`5709/5709`), and native `src/cmd` build completed with pre-existing pass-manager warnings. One-artifact coverage/stress manifest probes requiring the new boundary floors plus existing EH/typed/nested-CFG labels generated `1/1` valid modules with no skips. Direct mixed-generator compare requested `10000`, compared `9977`, normalized `9977`, and had `0` mismatches with `23` Binaryen/tool command failures. Direct profile compare remains blocked by existing `ssa-nomerge` local-allocation behavior gaps, not generator validity: smoke `10000` requested stopped at `215/215` mismatches, coverage `10000` requested with `--max-failures 50` stopped at `65/65` mismatches, and stress `10000` requested with `--max-failures 50` stopped at `65/65` mismatches; no validation/generator/command failures.
    - [x] [SSANM-011f] - Add branch-carrier GenValid coverage for dedicated no-merge lanes
      - Status: completed 2026-06-15; signoff note lives in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`.
      - Goal: make dedicated `ssa-nomerge` profiles explicitly cover the branch/enclosing-label local-move families exposed by `[SSANM-012c]`, without conflating them with full-`ssa` merge-local behavior.
      - Deliverables: added explicit feature labels for `ssa-branch-block-alias-copy`, `ssa-nested-enclosing-label-copy`, `ssa-parallel-branch-carrier-copy`, and `ssa-tee-branch-carrier-copy`; smoke now covers the single-carrier branch-block/nested-enclosing alias shape, while coverage/stress also cover parallel branch carriers and tee branch carriers.
      - Evidence: focused scanner/profile tests passed (`gen_valid_ssa_tests.mbt` `37/37`, `gen_valid_tests.mbt` `74/74`); `moon fmt`; `moon test src/validate` passed `1605/1605`; `moon test src/fuzz` passed `627/627`; `moon info` passed with the three pre-existing GenValid warnings; native `moon build --target native --release src/cmd` passed with pre-existing pass-manager warnings. Dedicated smoke `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-branchcarrier2-10000` requested `10000`, compared `58`, had `29` normalized matches and `29` raw mismatches; all current smoke failures are equal-size (`0` byte delta) with only a `+1 local.get` aggregate local-op delta and fewer Starshine locals. Dedicated coverage `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-branchcarrier2-10000` requested `10000`, compared `58`, had `29` normalized matches and `29` raw mismatches; all current coverage failures are also equal-size (`0` byte delta) with the same `+1 local.get` aggregate drift and fewer Starshine locals. This is not final closeout; `[SSANM-012c]` remains open for broader grouping and parity/stress decisions.
    - [ ] [SSANM-012] - Final direct `ssa-nomerge` closeout
      - Goal: close direct `ssa-nomerge` only after every known Binaryen-visible gap is matched, measured as a Starshine win, or explicitly deferred.
      - Status: epic only; execute through child slices after the LocalGraph rewrite, generator, huge-function, artifact, and preset-decision work is complete.
      - Deliverables: focused tests, `moon test src/passes`, full `moon test`, native build, `100000`-case direct compare, relevant GenValid profile lanes, huge-function classification, debug-WASI classification, O4z scheduling decision, docs/wiki updates, and a final mismatch/boundary report.
      - Non-goal: full `ssa` merge-local closeout; that sibling pass needs separate signoff.
    - [ ] [SSANM-012a] - Closeout prerequisite census
      - Goal: before declaring final direct parity, verify that source refresh, fixture matrix, LocalGraph mutation slices, tee/default/merge/control coverage, huge-function classification, debug-WASI replay, and preset decision slices are all complete or explicitly deferred.
      - Deliverables: checklist in the parity page tying each prerequisite slice to evidence or an approved deferral; no implementation mutation.
      - Suggested evidence: docs/wiki review, `agent-todo.md` cross-check, and targeted source/test links; tests are not required unless executable examples change.
    - [ ] [SSANM-012b] - Run and triage the final direct oracle lane
      - Goal: run the final direct `ssa-nomerge` `100000`-case compare after implementation prerequisites are complete.
      - Deliverables: exact command, seed, out dir, compared count, normalized/cleanup-normalized counts, command-failure classes, cache counters, sampled mismatch classification, and replayed reduced cases for any remaining non-green family.
      - Suggested evidence: `moon info`, `moon fmt`, focused pass tests, `moon test src/passes`, full `moon test`, native `src/cmd` build, and `bun scripts/pass-fuzz-compare.ts --count 100000 --seed 0x5eed --pass ssa-nomerge --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures`.
    - [ ] [SSANM-012c] - Run dedicated GenValid closeout lanes
      - Goal: prove the SSA-specific generator profiles no longer expose unclassified no-merge behavior gaps.
      - Deliverables: smoke, coverage, and any intentionally retained parity/stress profile lanes with feature-floor evidence, mismatch classification, timeout notes, and a decision to keep or rename `ssa-nomerge-parity`.
      - Suggested evidence: required-feature manifest probes plus at least `10000` requested cases for ordinary dedicated-profile lanes, using the native binary and persistent compare cache.
      - 2026-06-15 triage update: the first coverage/smoke mismatch family was an actionable size-losing EH/unreachable debris gap, not a safe validation-only difference. Coverage sample `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-abs6097-branchcopyfix-10000/failures/case-000022-gen-valid` kept a local-only `try_table` ending in `ref.null noexn; throw_ref` plus dead local debris after a self-branching loop, while Binaryen lowered that debris to `unreachable`; Starshine was initially larger by `19` bytes, then by `3` bytes after only removing the EH shell.
      - Fix/evidence: `src/passes/ssa_nomerge_test.mbt` now has `ssa-nomerge lowers null throw_ref try_table debris to unreachable`; `pass_manager.mbt` collapses local-only null `throw_ref` `try_table` debris, trims following unreachable tails, and removes local-only debris between a self-branching loop and the resulting `unreachable`. Focused `ssa_nomerge_test.mbt` passed `450/450`, `moon test src/passes` passed `2485/2485`, native build passed with pre-existing pass-manager warnings, and direct compare `.tmp/pass-fuzz-ssa-nomerge-nullthrowfix4-10000` compared `7605/10000` with `7605` normalized matches, `0` mismatches, and `20` cached Binaryen command failures.
      - Dedicated-lane status after the fix: coverage `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-nullthrowfix4-10000` still stops at `27` raw mismatches out of `54` compared, but the sampled family now has equal encoded output size and Starshine has fewer locals; classify it as representation-only/no-size-regression for the inspected family, not a closeout green lane. Smoke `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-nullthrowfix4-10000` still stops at `28` raw mismatches out of `56` compared, all sampled failures are still size-losing by `12` bytes with extra branch/parallel-copy local moves, so `[SSANM-012c]` remains open.
      - 2026-06-15 follow-up: the smoke `+12` family was an actionable size-losing branch/parallel-copy output-shape gap, not safe drift. Sample `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-nullthrowfix4-10000/failures/case-000022-gen-valid` copied each `if` arm through fresh locals and a join scratch before copying back to local `1` at an enclosing-label branch; Binaryen writes the branch carrier directly in the arms. `src/passes/ssa_nomerge_test.mbt` now covers `ssa-nomerge redirects if-arm branch copy aliases` and updates the enclosing-label divergent-alias fixture to the Binaryen-shaped direct writes. `pass_manager.mbt` redirects branch-adjacent and branch-block alias suffixes back into the arm producers when the scratch has no later reads. Evidence: `moon fmt`; focused `ssa_nomerge_test.mbt` passed `451/451`; `moon test src/passes` passed `2486/2486`; native build passed with pre-existing pass-manager warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-ifbranchblockfix-10000` compared `7603/10000`, normalized `7603`, mismatches `0`, cached Binaryen command failures `20`. Dedicated smoke `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-ifbranchblockfix-10000` still has `27` raw mismatches out of `54` compared, but every current failure has equal encoded size (`0` byte delta); dedicated coverage `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-ifbranchblockfix-10000` has `28` raw mismatches out of `56` compared, also all equal-size. Classify the inspected remaining dedicated families as representation-only/no-size-regression for now, not final closeout; `[SSANM-012c]` remains open pending broader grouping and parity/stress decisions.
      - 2026-06-15 GenValid breadth update: `[SSANM-011f]` added branch-block alias, nested enclosing-label, parallel branch-carrier, and tee branch-carrier feature labels/templates so future smoke/coverage failures are easier to attribute to branch/enclosing-label local-move families rather than generic SSA shapes. New dedicated lanes with the native binary stayed equal-size rather than reintroducing the old `+12` family: smoke `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-branchcarrier2-10000` requested `10000`, compared `58`, normalized `29`, mismatches `29`, command failures `0`, all failures `0` byte delta; coverage `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-branchcarrier2-10000` requested `10000`, compared `58`, normalized `29`, mismatches `29`, command failures `0`, all failures `0` byte delta. Current aggregate mismatch signature in both lanes is `+1 local.get` with fewer Starshine locals; do not treat this as final closeout until grouped beyond these aggregate metrics and parity/stress decisions are made.
      - 2026-06-15 pre-write/default-debris materialization follow-up: grouped smoke and coverage failures exposed actionable LocalGraph parity gaps, not final closeout evidence. Binaryen materializes body-local entry reads that occur before later writes as `i32.const 0`, including after ordinary `br` / void `br_table`; Starshine previously skipped the rewrite whenever the local was written anywhere later in the function. A follow-up coverage reduction showed the same default read can sit before a typed self-branching loop and a local-only null-`throw_ref` `try_table` suffix; Starshine collapsed the EH debris but returned before composing LocalGraph default/freshening over the surviving prefix. `pass_manager.mbt` now uses prefix-seen writes for default materialization, broadens the default-materialization control scanner to void `br_table` and otherwise supported `try_table` bodies, composes collapsed null-throwref debris with LocalGraph planned structured rewriting/default materialization, and keeps branch-carried tee continuation reads protected by canonical tests. Focused regressions: `ssa-nomerge LocalGraph materializes pre-write body defaults`, `ssa-nomerge materializes defaults before null throw_ref debris`, and `ssa-nomerge composes defaults with typed-loop null throw_ref debris`. Evidence: red-first focused tests failed on the pre-write and typed-loop debris gaps, then `ssa_nomerge_test.mbt` passed `454/454`; `moon test src/passes` passed `2489/2489`; full `moon test` passed `5795/5795`; `moon fmt`; `moon info` passed with the three pre-existing GenValid warnings; native build passed with pre-existing pass-manager warnings; `git diff --check` passed; direct compare `.tmp/pass-fuzz-ssa-nomerge-default-debris-localgraph-10000` compared `7601/10000`, normalized `7601`, mismatches `0`, cached Binaryen command failures `20`. Dedicated smoke `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-default-debris-localgraph-10000` requested `10000`, compared `55`, normalized `28`, mismatches `27`, command failures `0`; dedicated coverage `.tmp/pass-fuzz-ssa-nomerge-genvalid-coverage-default-debris-localgraph-10000` requested `10000`, compared `54`, normalized `27`, mismatches `27`, command failures `0`. The repaired sample now has the default `i32.const 0` and localgraph/debris trace reason, but the remaining dedicated failures still show output-shape drift with Starshine declaring fewer locals; `[SSANM-012c]` remains open for the broader freshening/merge-local grouping rather than final closeout.
    - [ ] [SSANM-012d] - Close artifact, huge-function, and preset scheduling evidence
      - Goal: tie the final LocalGraph policy back to the checked-in debug-WASI artifact, the six current large structured-local-write functions plus three drifted old anchors, and the O4z `ssa-nomerge` scheduling/no-op decision.
      - Deliverables: debug-WASI replay from the latest first-diff anchor, classification for the six current huge guards plus three drifted old anchors, pass-local timing, output-size/validation evidence, and an explicit O4z scheduling decision with preset-order tests for any public preset change.
      - Suggested evidence: artifact/function replay commands, native build, direct pass compare evidence as needed, and `src/cmd`/`src/passes/optimize.mbt` tests if preset behavior changes.
    - [ ] [SSANM-012e] - Publish the final `ssa-nomerge` closeout report
      - Goal: close the audit only after every result and remaining boundary is durable in docs.
      - Deliverables: final parity-page closeout note, wiki log entry, backlog pruning, links to raw/research artifacts if substantial, and explicit reopening criteria for any accepted non-goal or tool-blocked boundary.
      - Suggested evidence: docs diff review plus `moon fmt` / `moon info` / full tests only when executable examples or generated contracts changed.

- [O4Z-AUDIT-SSA-FULL] - Deep audit public full `ssa` sibling
  - Status: active sibling backlog split out of `[O4Z-AUDIT-SSA]` by `[SSANM-007c]` on 2026-06-13. Keep these slices about Binaryen `SSAify(true)` full merge-local behavior; do not count them as `ssa-nomerge` closeout prerequisites except where a contrast test documents the sibling boundary.
  - Scope: direct public `--pass ssa` behavior, including non-merge freshening/default materialization already active in Starshine, plus still-open merge-local materialization, incoming `local.tee`/entry-prepend handling, full-SSA loop/branch/EH/typed-control boundaries, and direct-pass signoff.
  - Current finding: `src/passes/ssa.mbt` registers an active partial full-`ssa` hot pass. It builds an analysis-only merge plan, routes non-merge families through the shared no-merge rewrite, and deliberately leaves merge families fail-closed until the remaining `[SSA-FULL-*]` slices materialize Binaryen-style merge locals.
  - Active `SSA-FULL` work slices:
    - [x] [SSA-FULL-001] - Make the public full `ssa` pass explicit
      - Status: completed before 2026-06-13; `src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, and `src/passes/ssa.mbt` expose an active `ssa` hot-pass descriptor rather than silently aliasing full `ssa` to `ssa-nomerge`.
      - Evidence: registry/public-dispatch coverage in `src/passes/registry_test.mbt` and sibling docs in `docs/wiki/binaryen/passes/ssa/`.
    - [x] [SSA-FULL-002A] - Add an analysis-only full-SSA merge planner
      - Status: completed before 2026-06-13; `src/passes/ssa.mbt` exposes `SsaFullRewritePlan` / `SsaFullMergeGetRewrite` and planner tests for explicit, parameter-entry, default-entry, nondefaultable, and single-source cases.
      - Evidence: `src/passes/ssa_test.mbt` planner tests document the intended Binaryen `SSAify(true)` merge-local inputs without mutating merge families yet.
    - [x] [SSA-FULL-002B] - Activate full `ssa` for non-merge families
      - Status: completed before 2026-06-13; direct public `ssa` can freshen repeated parameter overwrites and materialize default exact-ref reads, while merge families remain fail-closed.
      - Evidence: `src/passes/ssa_test.mbt` public-pipeline tests for non-merge freshening/defaults and fail-closed merge families.
    - [ ] [SSA-FULL-002C] - Materialize simple explicit-write merge locals
      - Goal: implement the first Binaryen-style `SSAify(true)` merge-local mutation for simple both-arm explicit-write diamond merges.
      - Deliverables: red-first public-pipeline tests showing incoming writes become merge-local `local.tee`/copy inputs as appropriate, the merge read retargets to the fresh merge local, output validates, and direct `ssa-nomerge` sibling behavior stays canonical.
      - Suggested evidence: focused `src/passes/ssa_test.mbt`, focused `src/passes/ssa_nomerge_test.mbt` sibling contrast if touched, `moon test src/passes`, native build, and direct `--pass ssa` compare when the harness supports it.
    - [ ] [SSA-FULL-002D] - Handle entry/default merge inputs and prepend ordering
      - Goal: complete full-SSA merge materialization for parameter-entry and default-entry sources, including function-entry prepends for parameter inputs and default materialization without illegal nondefaultable refs.
      - Deliverables: red-first tests for parameter-entry one-arm merges, default-entry one-arm merges, exact nullable ref defaults, skipped nondefaultable default entries, and deterministic local/prepend ordering.
      - Suggested evidence: focused `src/passes/ssa_test.mbt`, validation of lowered modules, and direct `--pass ssa` compare/fuzz when available.
    - [ ] [SSA-FULL-002E] - Classify loop, branch, EH, and typed-control full-SSA boundaries
      - Goal: decide which loop/backedge, branch-exit, `br_table`, EH, and typed-control families can safely use full-SSA merge materialization and which need fail-closed or dedicated typed/EH helpers.
      - Deliverables: positive or deliberately fail-closed fixtures, source-backed ownership notes, and docs updates that keep full `ssa` behavior separate from `SSANM` no-merge boundaries.
      - Suggested evidence: focused `src/passes/ssa_test.mbt`, direct compare for admitted mutation families, and wiki/backlog updates.
    - [ ] [SSA-FULL-003] - Direct full `ssa` closeout signoff
      - Goal: close the direct public `ssa` pass only after merge-local materialization, entry/default handling, and boundary decisions are implemented or explicitly deferred.
      - Deliverables: final parity/status note in `docs/wiki/binaryen/passes/ssa/`, exact test/fuzz commands and results, remaining accepted boundaries with reopening criteria, and backlog cleanup.
      - Suggested evidence: `moon info`, `moon fmt`, focused `ssa_test.mbt`, `moon test src/passes`, full `moon test`, native build, and a direct `--pass ssa` compare lane if supported.

- [O4Z-AUDIT-DCE] - Deep audit `dead-code-elimination`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: unreachable tails, dropped-value safety, structured-control result repair, EH/try_table behavior, writeback guards, raw-skip paths, and repeated cleanup interactions.
  - Deliverables: apply the common checklist; add missing dead-tail/control/EH fixtures; refresh direct compare and `DCE` slot evidence; classify Binaryen-shape differences as semantic, representation, or size tradeoffs.

- [O4Z-AUDIT-RUB] - Deep audit `remove-unused-brs`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit. 2026-06-09 merge-blocks/result-fallback slice fixed constant payload `br_if` folding and added a focused fallback-preservation guard; it does not close the audit because EH/GC/switch/metadata behavior surfaces remain open.
  - Scope: tail branch/return removal, constant/payload `br_if`, branch-to-trap rewrite, value-if/select lowering, br_table labels, raw gate thresholds, EH caught-throw cleanup, GC `br_on_*` cleanup, branch-hint/never-unconditionalize behavior, and retired slot14/slot40 corruption families.
  - Deliverables: apply the common checklist; coordinate with `[AUDIT002-B]` through `[AUDIT002-E]`; refresh direct compare and all `RUB` slot evidence; measure pass-local impact of raw skip choices; use `docs/wiki/raw/research/0721-2026-06-09-remove-unused-brs-merge-blocks-audit.md` as the latest merge-blocks/fallback evidence.

- [O4Z-AUDIT-OI] - Deep audit `optimize-instructions`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: exact peepholes, shift/compare/boolean rewrites, effect-aware folds, use-def/effects metadata, raw O4z slot16/slot44 predecessors, and pass-local runtime.
  - Deliverables: apply the common checklist; coordinate descriptor work with `[AUDIT001-A]`/`[AUDIT001-B]`; refresh direct compare and all `OI` slot evidence; classify any missed Binaryen folds by safety/effect reason.

- [O4Z-AUDIT-HSO] - Deep audit `heap-store-optimization`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: struct.new/struct.set folding, local escape analysis, effect ordering, GC descriptor/refinalization shapes, and allocation-heavy performance.
  - Deliverables: apply the common checklist; add missing GC/effect/escape fixtures; refresh direct compare and `HSO` slot evidence; record unsafe fold blockers separately from missed profitable folds.

- [O4Z-AUDIT-PC] - Deep audit `precompute`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit; branch-heavy direct compare blocker cleared on 2026-06-06, but the broader audit still needs the common checklist and O4z slot evidence.
  - Scope: constant folding, trap/effect preservation, raw precleaner/writeback guards, precompute-propagate prefix distinction, GC/array atomic exclusions, and O4z slot19/slot43 history.
  - Latest evidence: `.tmp/pass-fuzz-precompute-branch-heavy-slice4-norm4-10000` with `--normalize drop-consts --normalize local-cleanup-debris --normalize unreachable-control-debris` compared `6769/10000`, normalized `3375`, cleanup-normalized `3394`, mismatches `0`, command failures `20` (Binaryen/tool decode classes). Branch-heavy constant-control drift is covered by focused raw cleanup and semantic-safe normalizer tests.
  - Deliverables: apply the common checklist; coordinate descriptor work with `[AUDIT001-E]`/`[AUDIT001-F]`; refresh all `PC` slot evidence; record missed folds versus deliberate trap/effect bailouts.

- [O4Z-AUDIT-CP] - Deep audit `code-pushing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: safe local/global computation sinking, effect/trap boundaries, if/else arm use analysis, nested control, and tuple/local-cleanup neighborhood effects.
  - Deliverables: apply the common checklist; add missing push/bailout fixtures; refresh direct compare and `CP` slot evidence; classify downstream code-size wins and regressions.

- [O4Z-AUDIT-TO] - Deep audit `tuple-optimization`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: multivalue spill/copy splitting, passthrough chains, branch-exit carriers, local compaction, interaction with SLNS/RL/RUB, and candidate-heavy function runtime.
  - Deliverables: apply the common checklist; add missing tuple carrier fixtures; refresh direct compare and `TO` exact-slot neighborhood evidence; record any host-lane/local-map invariants clearly.

- [O4Z-AUDIT-SLNS] - Deep audit `simplify-locals-nostructure`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: no-structure local sinking, no-tee variant spelling, dead write cleanup, interaction with tuple/reorder/vacuum, and raw gate behavior.
  - Deliverables: apply the common checklist; add missing no-structure/no-tee fixtures; refresh direct compare and `SLNS` slot evidence; keep structure-producing rewrites out unless intentionally scheduled.

- [O4Z-AUDIT-VQ] - Deep audit `vacuum`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: nop/drop cleanup, explicit unreachable preservation, nested value-expression debris, empty if/block rewrites, repeated VQ slots, and retired slot23/slot33 histories.
  - Deliverables: apply the common checklist; add missing pure/nontrapping and control cleanup fixtures; refresh direct compare and all `VQ` slot evidence; measure cleanup value versus HOT traversal cost.

- [O4Z-AUDIT-RL] - Deep audit `reorder-locals`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: access-count sorting, zero-count truncation, parameter stability, local-name repair, multivalue scratch locals, TypeIdx invariant docs, and module-pass runtime.
  - Deliverables: apply the common checklist; coordinate invariant docs with `[AUDIT006-E]`; add missing reorder/name/multivalue fixtures; refresh direct compare and `RL` slot evidence.

- [O4Z-AUDIT-H2L] - Deep audit `heap2local`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: non-escaping struct locals, scalar field locals, null comparisons, GC type/refinalization, primary artifact fixtures, and heap-heavy function runtime.
  - Deliverables: apply the common checklist; add missing escape/null/field fixtures; refresh direct compare and `H2L` slot evidence; record memory and code-size effects.

- [O4Z-AUDIT-OC] - Deep audit `optimize-casts`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: ref.cast/ref.test/br_on_cast folding, descriptor casts, nullability trap preservation, local-subtyping neighborhood, and GC validation.
  - Deliverables: apply the common checklist; add missing cast/descriptor/nullability fixtures; refresh direct compare and `OC` slot evidence; classify any trap-mode-sensitive decisions.

- [O4Z-AUDIT-LS] - Deep audit `local-subtyping`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: local type refinement, write-site and join behavior, GC refs, call_ref and try_table shapes, interaction with optimize-casts/DAE, and validation refinalization.
  - Deliverables: apply the common checklist; add missing local-refinement fixtures; refresh direct compare and `LS` slot evidence; document conservative join/bailout policy.

- [O4Z-AUDIT-CL] - Deep audit `coalesce-locals`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: interference graph/coloring, local remap/name cleanup, TypeIdx invariant docs, copy-pair cleanup, and large-function coloring runtime.
  - Deliverables: apply the common checklist; coordinate invariant docs with `[AUDIT006-D]`; add missing interference/ref/name fixtures; refresh direct compare and `CL` slot evidence.

- [O4Z-AUDIT-CF] - Deep audit and widen `code-folding` to Binaryen behavior parity
  - Status: completed v0.1.0 release-gating `-O4z` per-pass audit and implementation track; 2026-06-04 audit evidence lives in `docs/wiki/raw/research/0713-2026-06-04-code-folding-o4z-pass-audit.md`.
  - Goal: make Starshine `code-folding` semantically implement Binaryen's `CodeFolding.cpp` transform families, not merely remain a correct narrow subset. Byte-for-byte wasm/text parity is not required, but direct `--pass code-folding` output must be semantically equivalent to Binaryen across the standard compare lane, and remaining representation/code-size differences must be classified with evidence.
  - Scope: expression-exit tail sharing for named block exits and foldable `if` arms; function-ending terminating-tail sharing for `return`, `return_call*`, and `unreachable`; unsupported branch poisoning; branch-target/EH movement safety; helper labels/blocks; profitability/fixpoint behavior; late cleanup neighborhood; and pass-local runtime.
  - Current finding: source/test review found active owner, registry, dispatcher, and CLI coverage in place for a narrowed HOT pass. Focused coverage now includes full-`if` `unreachable` terminal suffix sharing, unsupported `br_on_null` label poisoning, live-label structured suffix bailouts, single-result typed block-exit branch-payload sharing with fallthrough or other plain-`br` payloads, a source-backed single-result multi-root named-block suffix slice that shares a matching void/effectful root before the final branch/fallthrough value root, and a source-backed multi-value branch-plus-fallthrough payload suffix. At that early checkpoint this was not yet Binaryen behavior parity; later CF slices below close the direct behavior gaps. Baseline slice `[O4Z-AUDIT-CF-A]` is green: `moon info`, `moon fmt`, `moon test src/passes` (`1590/1590`), native `src/cmd` build at `_build/native/release/build/cmd/cmd.exe`, direct 1000-case compare (`998` normalized matches, `0` mismatches, `2` `binaryen-rec-group-zero` command failures), timing-only debug-WASI replay (`172.276ms` Starshine pass vs `169.576ms` Binaryen pass, within <=2x), and full `moon test` (`4775/4775`). Follow-up `[O4Z-AUDIT-CF-B]` through `[O4Z-AUDIT-CF-D]` evidence is also green: test-first `moon test src/passes` failed the two new multi-root tests before implementation; after implementation `moon test src/passes` passed `1592/1592`, full `moon test` passed `4777/4777`, and `moon info` completed; native direct 1000-case compare at `.tmp/pass-fuzz-code-folding-bd-1000` had `998` normalized matches, `0` mismatches, and `2` `binaryen-rec-group-zero` command failures; timing-only debug-WASI replay at `.tmp/code-folding-bd-self-compare` was `196.213ms` Starshine pass vs `187.281ms` Binaryen pass, within <=2x. Late-slot replay, 10000-case closeout, and broader Binaryen behavior parity remain open. `[O4Z-AUDIT-CF-E]` has concrete progress after the safe one-block/one-non-block and two-unnamed-block `if` value-suffix slices and now includes a HOT-level unreachable-condition bailout; exact unreachable-condition fixtures remain blocked by local HOT/lower support for bottom-typed conditions. `[O4Z-AUDIT-CF-F]` now has a root-anchored helper-label terminating-tail slice beyond the earlier adjacent no-else `if` case: non-adjacent `return` tails and block-backed `unreachable` tails can share a deeper common suffix through a fresh wrapper label while preserving unique fallthrough prefixes. `[O4Z-AUDIT-CF-G]` has started with the same root-anchored helper-label model for typed-result direct `return_call`, `return_call_indirect`, and core-built `return_call_ref` tails. Latest focused continuation validation used serialized per-slice `moon fmt` plus `moon test src/passes` and is green through `1647/1647`; the earlier five-slice closeout retained `moon info`, full `moon test` (`4822/4822`), native `src/cmd` build at `_build/native/release/build/cmd/cmd.exe`, and native direct 1000-case compare at `.tmp/pass-fuzz-code-folding-five-slices-2026-06-05-1000` with `998/1000` compared cases, `998` normalized matches, `0` mismatches, and `2` `binaryen-rec-group-zero` command failures. Pass-local timing was not refreshed in this coverage batch; the previous timing-only debug-WASI replay at `.tmp/code-folding-hij-self-compare` was within <=2x. `[O4Z-AUDIT-CF-H]` now has focused `br_table`/outside-target/switch-scope negatives, `[O4Z-AUDIT-CF-I]` now preserves tested `try_table` terminal/block-exit bailout shapes while allowing only non-terminal EH-body `if` suffix folds, and `[O4Z-AUDIT-CF-J]` now runs the current local root-anchored implementation to an intra-pass fixpoint and has a small late-neighborhood fixture. A requested pre-slice 10000-case compare smoke used the actual native binary at `_build/native/release/build/cmd/cmd.exe` and reached `6768/10000` compared cases with `6768` normalized matches, `0` mismatches, and `20` tool/Binaryen command failures; the requested `target/native/...` binary path failed earlier with ENOENT and is recorded separately in the research note. New follow-up coverage now includes a simple operand-only return-tail profitability negative, simple result `return_call` and `return_call_indirect` sibling-tail profitability negatives, a late-neighborhood guard for the simple direct tail-call bailout, and a `catch_ref` `try_table` terminal-tail EH bailout matching Binaryen's preserved shapes. The invalid-output continuation then fixed Starshine command/validation failure families by preserving needed bottom tails, refusing multi-result dead-block flattening, using function return arity for branches to the implicit function label in HOT lift, and clearing the validator stack on repeated unreachable/branch escapes; focused validation passed `moon test src/passes` (`1658/1658`) and `moon test src/validate` (`1552/1552`), and final docs/code signoff passed `moon info`, `moon fmt`, and full `moon test` (`4843/4843`) after refreshing the local Moon registry cache with `moon update`. The latest 100000-case direct smoke at `.tmp/pass-fuzz-code-folding-100000-after-td-fixes` compared `99747/100000` cases with `99742` normalized matches, `0` Starshine command failures, `0` validation failures, `253` tool/Binaryen command failures, and `5` agent-classified semantic-safe representation/cleanup mismatches (`012741`/`043481`, `023083`, `082547`, and `046375`). The follow-up `046375` cleanup made that family size-winning (`73B` Starshine vs `74B` Binaryen) rather than size-losing; a 1000-case smoke stayed green (`998` normalized matches, `0` mismatches, `2` tool/Binaryen command failures), but the requested debug-WASI timing lane is outside the pass-local floor (`5051.616ms` Starshine pass vs `185.490ms` Binaryen pass). The next EH-body breadth slice then fixed the open throwing-body `catch_ref` / `catch_all_ref` non-terminal `if` suffix gap with failing-first local tests; `moon info`, `moon fmt`, and full `moon test` passed (`4846/4846`), native `src/cmd` rebuilt at `_build/native/release/build/cmd/cmd.exe`, and direct 1000-case compare at `.tmp/pass-fuzz-code-folding-eh-catch-ref-body-1000` had `998` normalized matches, `0` mismatches, and `2` tool/Binaryen command failures. The next `[O4Z-AUDIT-CF-E]` one-block/one-non-block `if` breadth slices matched Binaryen's partial value suffix fold when both arms have distinct prefixes, the full one-block value-arm fold to `drop(condition)` plus one suffix, and then full tiny/larger one-block void-arm folds to `drop(condition)` plus the shared void suffix; the next source-backed two-block void-arm suffix slice hoists a shared `call $sink` / `i32.const 7; drop` suffix out of two unused block arms while leaving each unique prefix in place; the latest embedded-select continuation folds a partial value `if` child by wrapping the demoted void `if` plus shared suffix in a typed result block, after the prior full multi-root non-block value-arm slice folded top-level `if` arms to `drop(condition)` plus the shared `nop` / value suffix and folded the same full shape as a `select` child; focused pass tests are green at `moon test src/passes` (`1694/1694`) after another fifteen source-backed embedded value-parent slices: first `br` payload, `unary`, `convert`, `load`, and `compare`; then `ref.is_null`, `table.get`, `table.set` index, `call_indirect`, and `memory.grow`; and now `table.grow`, `table.fill`, `memory.fill`, `memory.copy`, and `memory.init` partial value-`if` suffixes; final `moon fmt`, `moon info`, and full `moon test` passed (`4879/4879`), native `src/cmd` rebuilt at `_build/native/release/build/cmd/cmd.exe`, and direct 1000-case compare at `.tmp/pass-fuzz-code-folding-table-memory-bulk-1000` had `998` normalized matches, `0` mismatches, and `2` tool/Binaryen command failures. The code-folding audit is closed for v0.1.0 direct semantic parity: safe arbitrary non-root terminating-tail subsets are covered across return, unreachable, and tail-call terminators; non-EH movement safety was reopened and source-backed through internal-target `br_table` movement in `[O4Z-AUDIT-CF-H]`; EH movement/repair/bailout behavior is classified in `[O4Z-AUDIT-CF-I]`; `[O4Z-AUDIT-CF-L]` is fixed/attributed with pass-local timing back inside the repo floor; `[O4Z-AUDIT-CF-K]` accepts the five documented semantic-safe cleanup/representation mismatch families after the required 100000-case oracle; and exact helper cost/text-shape parity is documented as future non-semantic work rather than a release blocker.
  - Deliverables:
    - [x] `[O4Z-AUDIT-CF-A]` Baseline the current widened implementation: run `moon info`, `moon fmt`, `moon test src/passes`, build native `src/cmd`, run direct `--pass code-folding --count 1000 --jobs auto --starshine-bin <actual cmd.exe>`, run timing-only `self-optimize-compare ... --code-folding`, and record exact binary path, compared count, command failures, mismatches, and pass-local Starshine/Binaryen timings in the research note.
    - [x] `[O4Z-AUDIT-CF-B]` Build a source-backed Binaryen shape matrix from `CodeFolding.cpp` and `test/lit/passes/code-folding.wast`: rows for named-block exits, `if` arms, terminating returns, terminating `return_call*`, terminating `unreachable`, unsupported-branch poison, outside-target movement bailouts, EH-sensitive bailouts/repair, helper/profitability cases, and fixpoint exposure. Matrix lives in `docs/wiki/raw/research/0713-2026-06-04-code-folding-o4z-pass-audit.md` with local tests, gaps, and next-slice owners.
    - [x] `[O4Z-AUDIT-CF-C]` Finish expression-exit candidate modeling for named block exits: `CodeFoldingValueExitTail` now carries owning region/tail pointer, branch/fallthrough kind, target label, branch payload roots, result arity, movement proof state, selected suffix length, and profitability score; unsupported `br_on_*` / `br_if` / `br_table` / `delegate` target traffic still poisons the fold.
    - [x] `[O4Z-AUDIT-CF-D]` Widen named-block expression-exit folding beyond the current single-result payload-root slice: added failing tests first and implemented the safe single-result multi-root subset for branch-plus-fallthrough payload suffixes and duplicate branch-only tail groups without fallthrough; later extended the same plain-`br`/fallthrough model to a source-backed multi-value payload suffix and added a branch-only multi-value payload suffix with an unreachable fallback. Helper-block/profitability search, non-plain branch participation, and broader multi-value shapes with live terminal fallbacks remain deferred until additional HOT/lower proof justifies them.
    - [x] `[O4Z-AUDIT-CF-E]` Widen `if` expression-exit folding to Binaryen's arm rules: unnamed block/unnamed block suffixes, one-block/one-non-block synthetic wrapping where safe, partial suffix folds, full-arm folds, named-arm negatives, unreachable-condition bailouts, refined-result preservation, and profitability thresholds. Progress: safe value-result one-block/one-non-block wrapping now folds in both then-block and else-block orientations with focused tests and green 1000-case compare/timing evidence, and source-backed coverage now includes then-block and else-block multi-root `call $sink` / `f32.const` value suffixes matched against a non-block arm; the one-block matcher now also hoists a shared value suffix when both the block arm and non-block arm have distinct void prefixes, folds the full one-block/non-block value-arm case to `drop(condition)` plus one shared suffix like Binaryen, and folds tiny/larger full one-block/non-block void arms to `drop(condition)` plus the shared void suffix. Source-backed two-unnamed-block void-arm suffixes now also hoist a shared void/effectful suffix without demoting already-void arm blocks. Source-backed two-unnamed-block value-arm shapes now hoist the shared value suffix after demoting both unused-label block arms and the `if` to void, including a multi-root shared `call $sink` / `f32.const` suffix with distinct arm prefixes; crossed live-label structured if-arm suffixes now have a Binaryen-preserving negative; exact partial non-block value-arm coverage now locks the Binaryen lit/doc shape `(then (nop) (f32.const 0))` / `(else (f32.const 0))`, with local assertions limited to suffix sharing because HOT/lower does not preserve `nop` in the final pretty output; the implementation also now refuses `if` suffix folding when the HOT condition is already an exit, keeps simple full-value non-block arms for `optimize-instructions` instead of folding them in `code-folding`, folds source-backed full multi-root non-block value arms, handles the same full multi-root shape when it is a single-result `select` child by inserting a typed wrapper block, wraps embedded `select` partial value-`if` suffix folds in the same typed-block style while preserving the demoted void `if` prefixes, and now source-backed embedded value-child wrappers cover partial suffixes under `drop`, `call`, `call_ref`, `unary`, `binary`, `compare`, `convert`, `load`, `local.set`, `local.tee`, `global.set`, `store`, `br` payload, `br_if` payload/condition, `return`, `return_call`, `return_call_indirect`, `return_call_ref`, `struct.new`, `struct.get`, `struct.get_s`, `struct.get_u`, `array.new`, `array.new_default`, `array.new_fixed`, `array.new_data`, `array.new_elem`, `array.new_elem`, `struct.set`, `array.get`, `array.get_s`, `array.get_u`, `array.len`, `array.set`, `array.fill`, `array.copy`, `array.init_data`, `array.init_elem`, `ref.eq`, `ref.as_non_null`, `any.convert_extern`, `extern.convert_any`, `ref.i31`, `ref.is_null`, `ref.test`, `ref.cast`, `table.get`, `table.set` index, `table.copy` dest, `table.init` dest, `call_indirect`, `memory.grow`, `table.grow`, `table.fill`, `memory.fill`, `memory.copy`, and `memory.init` parents plus full multi-root suffixes under `call` and `binary` parents. The latest miscellaneous heap parent increment was source-backed by `wasm-opt version_129 --all-features --code-folding -S -o - .tmp/cf-heap-misc-probes.wat`, which emitted typed wrapper blocks for `ref.eq`, `any.convert_extern`, `extern.convert_any`, `ref.i31`, `i31.get_s`, and `i31.get_u` operands. Initial `moon test src/passes` failed the new/tentative behavior tests (`1726/1732`); the landed feasible subset covers `ref.eq`, `any.convert_extern`, `extern.convert_any`, and `ref.i31`, while `i31.get_s` / `i31.get_u` remain local HOT caveats. Final `moon test src/passes`, `moon fmt`, `moon info`, full `moon test` (`4915/4915`), native `src/cmd` build, and direct 1000-case compare at `.tmp/pass-fuzz-code-folding-misc-heap-parents-1000` passed with `998` normalized matches, `0` mismatches, and `2` tool/Binaryen command failures. Closeout: `moon test src/passes` passed (`1730/1730`), and direct 10000-case compare at `.tmp/pass-fuzz-code-folding-cf-e-closeout-10000` compared `6771/10000` cases with `6771` normalized matches, `0` mismatches, `0` validation failures, and `20` tool/Binaryen command failures. Local caveats are documented and are not treated as unresolved CF-E correctness gaps: exact named-unused arm negatives are limited by local HOT name preservation; unreachable-condition public fixtures still hit local HOT/lower bottom-condition limits but the HOT-level bailout remains covered; source-backed `br_on_null` / `br_on_non_null` and `i31.get_s` / `i31.get_u` embedded operands remain local HOT/surface caveats after probes, while broader branch/EH movement negatives stay owned by `[O4Z-AUDIT-CF-H]`/`[O4Z-AUDIT-CF-I]`. Keep live-label and branch-target movement negatives green.
    - [x] `[O4Z-AUDIT-CF-F]` Implement the function-ending terminating-tail helper-label algorithm for `return` and `unreachable`: collect block-backed and root-level terminators, search deeper common suffixes before shallower ones, choose profitable subsets deterministically, create fresh helper labels, rewrite old tails to branch to the helper label, prevent old-body fallthrough into the shared suffix, and add focused root-level/block-backed/fallthrough-prevention tests. Progress: adjacent no-else `if` then-tail plus immediate fallthrough `return`/`unreachable` tails share one terminal suffix through a fresh void wrapper block label; the helper-label path collects root-level and nested region terminators, selects the deepest profitable safe common suffix, rewrites old nested tails to `br` to a fresh wrapper label, and covers non-adjacent `return`, block-backed `unreachable`, arbitrary non-root `return`/`unreachable` subsets with distractors, groups that exclude a different root `return`, deeper-subset-then-shallow-fixpoint exposure, label-unused structured block/if/loop suffix roots, and narrow alpha-equivalent self-branching block suffixes (including nested internal-label variants). No-root-tail void roots get a skip wrapper so ordinary fallthrough skips the shared terminal suffix. Current closeout added/validated a safe outside-target distractor subset and confirmed the existing implementation already covers the remaining safe arbitrary subset surface; broader branch-containing suffix movement, exact Binaryen helper-block cost modeling, generated late-slot evidence, and pass-local timing remain owned by `[O4Z-AUDIT-CF-H/J/L]` rather than this subset-sharing slice.
    - [x] `[O4Z-AUDIT-CF-G]` Extend terminating-tail sharing to `return_call`, `return_call_indirect`, and `return_call_ref` with the same subset/deeper-suffix/helper-label model; add direct-call, indirect-call, ref-call, typed-result, and unsupported/local-syntax bailout fixtures before implementation. Progress: the helper-label model covers typed-result direct `return_call`, WAT `return_call_indirect`, and core-built `return_call_ref` fixtures; call-signature side-table equality compares indirect/ref tail-call roots by logical type/table signature instead of allocation id. The non-root subset wrapper path has direct `return_call`, `return_call_indirect`, and core-built `return_call_ref` coverage with root fallthrough, arbitrary non-root direct/indirect/ref subset-with-distractor coverage, an indirect deeper-subset-vs-shallow-simple-operand fixture, direct `return_call` coverage for narrow and nested self-branching block suffix cases, `return_call_indirect` coverage for the nested internal-label self-branching suffix case, crossed nested-label direct `return_call` and `return_call_indirect` negatives, and core-built non-null `ref.func` coverage for the nested self-branching `return_call_ref` positive while the local text frontend still rejects that WAT surface. Remaining broader movement-safety negatives and natural text-level non-null `return_call_ref` fixtures stay owned by `[O4Z-AUDIT-CF-H/J]`; safe arbitrary tail-call subset sharing itself is complete.
    - [x] `[O4Z-AUDIT-CF-H]` Implement Binaryen-equivalent movement-safety gates: branch targets defined inside the moved-out region must remain in scope, candidate items with external break targets must be rejected, `br_table` / switch-style equal-looking unsafe tails must remain negative, and label alpha-equivalence must not override real target scope. Reopened after the first closeout and widened for source-backed Binaryen `br_table` movement: terminating-tail movement still uses a function-end target-scope proof, so branch/control-bearing candidate items are movable only when every branch target is owned by a label inside the moved item; non-anchor external-target candidates are filtered out of deeper groups instead of poisoning safe subsets. Added failing-first ancestor-target `br_if` return-tail regression (`moon test src/passes` initially failed `1745/1746` by producing an out-of-scope branch during HOT lowering), positive internal-target conditional branch return-tail coverage, and then positive internal-target `br_table` `return`, `unreachable`, and direct `return_call` suffix coverage after Binaryen probes showed those tails fold. Implementation now compares `br_table` targets through the label alpha map and treats void self-targeting `br_table` blocks like self-branching blocks for terminating-tail following. Expression-exit probes/tests lock the source-backed split: `br_if` block-exit suffixes targeting an enclosing block and `br_table` `if`-arm suffixes targeting an enclosing block fold, while `br_table` block-exit suffixes targeting an enclosing block, `br_if`/`br_table` target traffic to the folded block, outside-target switch-like return tails, careful switch target scopes, live-label structured suffixes, crossed nested-label negatives, operand-only tail-call profitability, and safe subsets past outside-target distractors remain guarded. Closeout validation after reopening passed `moon test src/passes` (`1753/1753`), `moon fmt`, full `moon test` (`4938/4938`), `moon info`, native build, direct 1000 compare at `.tmp/pass-fuzz-code-folding-cf-h-brtable-1000` (`998` normalized matches, `0` mismatches), and direct 10000 compare at `.tmp/pass-fuzz-code-folding-cf-h-brtable-10000` (`6771` normalized matches, `0` mismatches, `20` tool/Binaryen command failures). No non-EH branch/control-bearing movement gap remains documented for CF-H without a source-backed Binaryen counterexample; exact helper cost modeling stays owned by future parity work, and EH-specific movement/repair stays owned by `[O4Z-AUDIT-CF-I]`.
    - [x] `[O4Z-AUDIT-CF-I]` Address EH parity deliberately: implemented the source-backed EH movement subset that is safe without a local `EHUtils::handleBlockNestedPops` equivalent, and documented the remaining repair-surface limits. `canMove` parity is now represented by local EH gates that reject throwing/call/throw-ref suffix movement when the expression being exited contains `try` / `try_table`, while still allowing body-local movement inside an enclosing `try_table`; `if` suffix folding in EH bodies now includes partial terminal `return` suffixes and throwing `throw` suffixes that remain inside the same handler scope. Focused failing-first tests cover `code-folding folds try-table if-arm partial terminal return suffixes` and `code-folding folds try-table if-arm throwing suffixes`, alongside the existing `catch_all`, explicit `catch $e`, `catch_ref`, `catch_all_ref`, nested `try_table`, terminal-tail bailout, and block-exit bailout fixtures. Source probes in `.tmp/cf-i-probes/` classify Binaryen's conservative barriers (`effects.danglingPop`, throwing-through-`try` when `outOf` contains `Try`/`TryTable`) and its repair hook (`needEHFixups -> EHUtils::handleBlockNestedPops`); local plain-`try`/pop text coverage remains blocked by the WAT/HOT surface, so CF-I does not claim general nested-pop repair parity.
    - [x] `[O4Z-AUDIT-CF-J]` Add fixpoint and neighborhood proof: tests where one fold exposes another, plus `code-folding -> merge-blocks -> remove-unused-brs -> remove-unused-names -> merge-blocks` fixtures showing helper structure is consumed safely without changing semantics. Closed: `code_folding_run` recomputes label use and repeats the local visit/terminating-tail sequence until no local change; focused root-anchored and deeper non-root fixtures prove second folds exposed by earlier folds, the indirect tail-call deeper-subset fixture proves shallow simple-operand suffixes do not win over a deeper profitable subset, and late-neighborhood fixtures now validate helper-label return-tail, fixpoint-exposed return-tail, non-root return-subset, arbitrary non-root return-subset-with-distractor, shared direct `return_call`, and simple tail-call bailout shapes. Generated late-neighborhood evidence is recorded in the research note: wasm-smith 1000 was green (`997` normalized matches, `0` mismatches), wasm-smith 10000 keep-going with cleanup normalizers had `9950` normalized matches, `1` cleanup-normalized match, and `1` representation-only downstream cleanup mismatch; direct `code-folding` 1000 stayed green (`998` normalized matches, `0` mismatches). Exact Binaryen helper cost/profitability modeling remains a documented future parity gap, not a semantic/neighborhood blocker, because no local fixture or generated replay showed unsafe helper cleanup.
    - [x] `[O4Z-AUDIT-CF-K]` Refresh oracle signoff after every behavior-widening batch: direct `--pass code-folding --count 1000` first, then `10000` before closing; classify each mismatch as semantic-safe/size-winning, representation-only, size-losing, unknown/risky, validation failure, tool/Binaryen failure, or true semantic mismatch with rationale. Closed with the CF-I-required 100000-case oracle at `.tmp/pass-fuzz-code-folding-cf-i-100000` using `_build/native/release/build/cmd/cmd.exe`: `99747/100000` compared, `99742` normalized matches, `5` already-known semantic-safe cleanup/representation mismatches (`012741`, `023083`, `043481`, `046375`, `082547`), `0` validation/property/generator failures, and `253` tool/Binaryen command failures (`221` `binaryen-rec-group-zero`, `8` `binaryen-bad-section-size`, `10` `binaryen-table-index-out-of-range`, `2` `binaryen-invalid-tag-index`, `10` `binaryen-command-failed`, `2` `binaryen-invalid-type-index`). The five residual direct mismatches are accepted for this audit as semantic-safe cleanup/representation drift: four pure nontrapping value-debris families around `unreachable` and the now-size-winning typed-dead-wrapper/nop-elision family. A final helper profitability probe matrix is documented in the research note; exact Binaryen cost-shape parity is accepted as future text/size work, not a semantic release blocker.
    - [x] `[O4Z-AUDIT-CF-L]` Measure pass-local performance after the broader implementation: Starshine must stay within the repo floor (`starshine_time <= 2 * binaryen_time`) on the available direct artifact/timing lane, or the slowdown must be attributed to a specific pass-local owner with a follow-up slice rather than hidden under `[WALL]001`. Closed: the inherited debug-WASI timing blocker reproduced as pass-local `code-folding` work (`.tmp/code-folding-cf-l-repro` timed out after 1200s; direct CLI replay had also timed out), and profiling-by-isolation attributed the dominant cost to broad arbitrary terminating-tail search on large HOT functions, with large embedded/if suffix scans also contributing. The fix adds semantic no-op scan budgets for oversized functions, `if` arm scans, embedded value-parent visits, terminating-tail candidate counts, and terminating-tail node counts. Final timing at `.tmp/code-folding-cf-l-final` is inside the floor (`226.109ms` Starshine pass vs `177.218ms` Binaryen pass). Direct 1000/10000 compares stayed semantically green (`998` and `6769` normalized matches, `0` mismatches; tool/Binaryen command failures only).
    - [x] `[O4Z-AUDIT-CF-M]` Close the audit only after docs/backlog reflect the final parity status: update the code-folding wiki pages, research note, and `docs/wiki/log.md` with exact commands, counts, timings, mismatch classes, late-slot evidence, and any explicitly deferred Binaryen differences. Closed: docs now record the invalid-output fixes, 100000-case counts, tool/Binaryen command-failure breakdown, the `046375` size-winning follow-up, the fixed/attributed `[O4Z-AUDIT-CF-L]` timing blocker, the five residual semantic-safe direct mismatch classifications, `[O4Z-AUDIT-CF-J]` generated late-neighborhood evidence plus downstream cleanup mismatch classifications, reopened `[O4Z-AUDIT-CF-H]` movement-safety closeout, `[O4Z-AUDIT-CF-I]` EH source-probe matrix plus 100000-case oracle signoff, and final helper profitability classification. The remaining exact helper-cost/text-shape differences are accepted as future non-semantic parity work; final closeout validation passed `moon fmt`, `moon info`, full `moon test` (`4940/4940`), native `src/cmd` build, and direct 1000-case compare at `.tmp/pass-fuzz-code-folding-cf-final-1000` (`998` normalized matches, `0` mismatches); `[O4Z-AUDIT-CF]` has no remaining release-blocking direct semantic parity item.

- [O4Z-AUDIT-RSE] - Deep audit `redundant-set-elimination`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: same-value set/tee removal, branch/default facts, loop invariants, refined local.get retargeting, GC wrapper/accessor facts, and late-tail preset role.
  - Deliverables: apply the common checklist; add missing CFG/value-flow fixtures; refresh direct compare and `RSE` slot evidence; classify any full fixed-point loop convergence gap.

- [O4Z-AUDIT-DAE] - Deep audit `dae-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: dead argument/result removal, nested cleanup scheduler, selected-shape genericization, raw cleanup policy, type liveness, and pass-local runtime.
  - Deliverables: apply the common checklist; coordinate with `[AUDIT003-*]`; refresh DAE-normalized direct compare and late `DAE` slot evidence; keep accepted raw-cleanup drift separate from semantic mismatches.

- [O4Z-AUDIT-INL] - Deep audit `inlining-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit. Direct compare is green with documented cleanup normalizers as of 2026-06-05: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass inlining-optimizing --normalize drop-consts --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-inlining-optimizing-local-cleanup-norm1-10000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` produced 6772 compared, 3376 normalized matches, 3396 cleanup-normalized matches, 0 mismatches, and 20 Binaryen/tool command failures (17 rec-group-zero, 1 bad-section-size, 1 table-index-out-of-range, 1 invalid-tag-index). Agent classification: cleanup-normalized cases are semantic-safe Starshine cleanup of closed dropped constant-expression trees, unused local declarations/local renumbering, and standalone `nop` debris; these are no-effect transforms by the WebAssembly execution model, not evidence inferred from size alone.
  - Scope: direct inlining heuristics, optimizing cleanup scheduler, no-inline policy interaction, helper compaction, tail-call/multivalue surfaces, and local-declaration drift.
  - Deliverables: apply the common checklist; refresh `INL` slot evidence; file partial-inlining/name repair only if new evidence warrants reopening deferred slices.

- [O4Z-AUDIT-DIE] - Deep audit `duplicate-import-elimination`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: duplicate import equivalence, import remapping, call/ref.func/table/export users, nonfunction and signature negatives, and metadata preservation.
  - Deliverables: apply the common checklist; coordinate tests with `[AUDIT004-G]`/`[AUDIT004-H]`; refresh direct compare and `DIE` slot evidence; record any ABI-visible import-order constraints.

- [O4Z-AUDIT-SGO] - Deep audit `simplify-globals-optimizing`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: global value propagation, nested cleanup runtime, late-tail scheduling, closed/open-world boundaries, string/GC/refinalization breadth, and accepted default-local drift.
  - Deliverables: apply the common checklist; refresh direct compare and `SGO` tail evidence; coordinate with `[SGO]003` through `[SGO]005`; keep accepted v0.1.0 signoff separate from new improvement findings.

- [O4Z-AUDIT-SG] - Deep audit `string-gathering`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: string.const gathering, canonical global reuse, data/name/export interactions, late-tail ordering, and string-heavy module code-size impact.
  - Deliverables: apply the common checklist; add missing stringref/global fixtures; refresh direct compare and `SG` tail evidence; document any Binaryen canonical-string reuse gap.

- [O4Z-AUDIT-RG] - Deep audit `reorder-globals`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: dependency-preserving global order, global.get/set remaps, exports/imports/start/elem/data interactions, string global late tail, and names/metadata.
  - Deliverables: apply the common checklist; add missing dependency/remap fixtures; refresh direct compare and `RG` tail evidence; record any order-only representation drift separately from correctness.

- [O4Z-AUDIT-DIR] - Deep audit `directize`
  - Status: active v0.1.0 release-gating `-O4z` per-pass audit.
  - Scope: immutable table facts, holes/traps, type matching/subtyping, select lowering, imported/exported/mutated table negatives, tail calls, and late-tail code-size impact.
  - Deliverables: apply the common checklist; coordinate tests with `[AUDIT004-D]` through `[AUDIT004-F]`; refresh direct compare and final `DIR` tail evidence; document any broader directization opportunities or safety bailouts.

## Deferred Until MoonBit Threading Support

- [HOT]002 - Native Parallel Hot-Batch Queue
  - Status: deferred; MoonBit does not currently support the threading model Starshine needs for a safe native worker queue.
  - Resume when: MoonBit native threading is stable enough to run per-function optimizer workers with deterministic, byte-stable output and explicit runtime gating.
  - Deliverables: add a native-only worker queue over eligible defined functions for the hot batch payload (`ssa-nomerge -> dead-code-elimination -> vacuum -> optimize-instructions -> simplify-locals`); keep final output byte-stable and deterministic; gate behind an explicit native-only option.
  - Dependencies: [HOT]001 replay hardening must stay green.

## v0.1.1 Backlog

### Optimizer Audit Follow-Ups

- [AUDIT]001 - Hot Pass Descriptor Metadata Truthfulness
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: make hot pass `requires` metadata describe every analysis a pass may request through `pass_require_*`, so the registry remains a truthful pass-author contract and future scheduling/perf tooling can trust descriptors.
  - Why: the audit found passes that lazily request analyses without listing them in `requires`; tests currently pass because the helper layer builds analyses on demand, but the metadata contract is stale.
  - Deliverables:
    - [ ] `[AUDIT001-A]` Add focused descriptor tests for `optimize-instructions` requiring `use_def` and `effects`; confirm failure before metadata changes.
    - [ ] `[AUDIT001-B]` Update `optimize-instructions` descriptor metadata to declare `use_def` and `effects`; rerun the focused descriptor test and `moon test src/passes`.
    - [ ] `[AUDIT001-C]` Add focused descriptor test for `remove-unused-brs` requiring `use_def`; confirm failure before metadata changes.
    - [ ] `[AUDIT001-D]` Update `remove-unused-brs` descriptor metadata to declare `use_def`; rerun the focused descriptor test and `moon test src/passes`.
    - [ ] `[AUDIT001-E]` Add a focused descriptor test proving `precompute-propagate-prefix` requires `ssa` and direct `precompute` does not, if direct `precompute` truly never reaches SSA helpers.
    - [ ] `[AUDIT001-F]` If `[AUDIT001-E]` proves direct `precompute` can reach SSA helpers, update direct `precompute` descriptor in its own test-first slice; otherwise leave it unchanged and document why.
    - [ ] `[AUDIT001-G]` Add a lightweight static/registry audit helper or test table for future `pass_require_*` additions so new metadata drift is caught near the implementing pass.
    - [ ] `[AUDIT001-H]` Refresh `docs/wiki/ir2/pass-porting-checklist.md` with the descriptor audit rule and cite the new tests.
  - Suggested tests: `moon test src/passes`, registry/descriptor focused tests, and one small trace/perf smoke proving analysis timers still appear under the expected pass.
  - Exit criteria: every hot descriptor is either mechanically covered by tests or explicitly documented as intentionally lazy; no pass silently depends on undeclared analysis metadata.

- [AUDIT]002 - Pass-Manager Raw Skip And Gate Boundary Coverage
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: protect large-function raw precleaner / skip heuristics from silent coverage holes and performance cliffs.
  - Why: `src/passes/pass_manager.mbt` contains many threshold-shaped gates for `remove-unused-brs`, `simplify-locals`, and related raw adapters. They are performance-motivated, but nearby shapes can miss cleanup or unexpectedly fall back to expensive HOT lowering. The 2026-06-04 `[O4Z-AUDIT-SL]` closeout retired the simplify-locals semantic/direct and generated late-neighborhood audit gate, but intentionally left small structured call-mesh and giant/no-structure raw threshold boundary coverage here under `[AUDIT002-F]` / `[AUDIT002-G]`.
  - Deliverables:
    - [ ] `[AUDIT002-A]` Inventory every `can_skip`, `unchecked`, `giant`, `large`, and threshold-based raw gate in `pass_manager.mbt`, grouping them by owning pass and intended artifact/function family.
    - [ ] `[AUDIT002-B]` Add white-box tests for `remove-unused-brs` large-result `br_table` dispatch ladder gates: exact match, locals/instruction lower-bound miss, block-depth miss, and HOT-only-candidate miss.
    - [ ] `[AUDIT002-C]` Add white-box tests for `remove-unused-brs` large value-if branch ladder gates: locals range, instruction range, call range, return/block range, and drop/select/br_if negatives.
    - [ ] `[AUDIT002-D]` Add white-box tests for `remove-unused-brs` drop-heavy / typed-`br_table` / void-if-return ladder gates, one predicate family per test so boundary failures identify the exact gate.
    - [ ] `[AUDIT002-E]` Add white-box tests for `remove-unused-brs` moderate and unchecked call-mesh gates, including ±1 thresholds around locals, instruction count, if count, and call count.
    - [ ] `[AUDIT002-F]` Add white-box tests for `simplify-locals` small structured call mesh gates: exact match, instruction lower/upper bounds, local-get/call ranges, and exact-tail negative.
    - [ ] `[AUDIT002-G]` Add white-box tests for `simplify-locals` giant validator and no-structure gates, with one fixture per threshold family rather than one mega-fixture.
    - [ ] `[AUDIT002-H]` For each broad skip family touched by `[AUDIT002-B]` through `[AUDIT002-G]`, add one public-pipeline fixture proving the intended cleanup either still runs or intentionally stays skipped with a trace reason.
    - [ ] `[AUDIT002-I]` Add trace strings for currently silent broad skip decisions where a future agent cannot tell whether a function was skipped for safety, performance, or shape mismatch.
    - [ ] `[AUDIT002-J]` Run a focused artifact timing replay for the debug artifact after adding trace/tests; document any gate that exists only to stay within `Starshine <= 2x Binaryen`.
  - Suggested tests: `moon test src/passes`, focused `pass_manager_wbtest`, and a timing-only `self-optimize-compare` replay only after asking if it will be long.
  - Exit criteria: every raw skip/gate family has boundary tests and a documented purpose; no threshold is an unexplained magic number.

- [AUDIT]003 - DAE Selected-Shape De-Artifacting And Genericization
  - Status: active v0.1.1 follow-up; not a v0.1.0 blocker because current DAE direct-pass surfaces are accepted.
  - Goal: convert remaining selected `FuncXXX` artifact lanes into generic, named, semantics-first shape recognizers where practical, while keeping accepted artifact evidence intact.
  - Why: the audit found many selected-function helpers in `dead_argument_elimination.mbt`. They are covered and historically justified, but they are hard to maintain and weak as a general optimizer surface.
  - Deliverables:
    - [ ] `[AUDIT003-A]` Inventory selected-function helpers and tests into a markdown table with columns: helper, selected def(s), fixture/test, transform family, safety guard, artifact reason, and current generic candidate.
    - [ ] `[AUDIT003-B]` Classify Func237 carrier/loop/local-map helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-C]` Classify Func256/Func298 loop-carrier helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-D]` Classify Func287/Func288 setup/switch-carrier helpers only; decide which are genericizable and which are intentionally artifact-local.
    - [ ] `[AUDIT003-E]` Classify Func299 inverted-if, Func311/Func313 terminal-call-arg, and Func408/Func505/Func521 literal/control helpers, one family at a time.
    - [ ] `[AUDIT003-F]` Pick the smallest safe classified family and add a reduced non-artifact fixture that fails without a generic recognizer and does not depend on absolute debug-artifact function indices.
    - [ ] `[AUDIT003-G]` Implement only that one family behind semantic guards, keep the selected artifact fixture as a regression, and remove only the corresponding selected-index gate if artifact/timing/fuzz evidence stays green.
    - [ ] `[AUDIT003-H]` Repeat `[AUDIT003-F]`/`[AUDIT003-G]` for the next smallest family; every repeat gets its own focused test-first commit-sized slice, not a broad DAE rewrite.
    - [ ] `[AUDIT003-I]` For families that remain selected-only, add comments in source and wiki explaining why they are intentionally artifact-local and what evidence would reopen them.
    - [ ] `[AUDIT003-J]` Refresh direct `--dae-optimizing` compare with DAE normalizers and classify any mismatch families by agent judgment, keeping accepted raw-cleanup drift separate from true semantic mismatches.
  - Suggested tests: focused DAE fixtures first, `moon test src/passes`, direct `bun scripts/pass-fuzz-compare.ts --pass dae-optimizing --normalize drop-consts --normalize unreachable-control-debris --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe`, and artifact timing when selected lanes move.
  - Exit criteria: selected-function lanes are either replaced by generic recognizers or explicitly documented as intentionally artifact-local with current evidence.

- [AUDIT]004 - Thin Module-Pass Shape Coverage Expansion
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: add small, public-pipeline tests for module passes whose current direct coverage is thin relative to their implementation breadth.
  - Why: the all-pass fuzz smoke found no validation failures, but several module passes have only a small number of focused tests and are likely places for missing shapes to hide.
  - Deliverables:
    - [ ] `[AUDIT004-D]` `directize` table visibility negatives: add separate imported-table and exported-table tests proving indirect calls remain indirect.
    - [ ] `[AUDIT004-E]` `directize` element coverage: add passive element, declarative element, active nonconstant offset, active out-of-range hole, and type-mismatch-trap tests.
    - [ ] `[AUDIT004-F]` `directize` multi-table and tail-call coverage: add one multi-table partial optimization fixture, one `return_call_indirect` fixture, and one unsupported `call_ref`/reference-call non-interference fixture if WAT support exists.
    - [ ] `[AUDIT004-G]` `duplicate-import-elimination`: add separate tests for same module/name duplicate function imports, different module/name same-signature duplicates, nonfunction-import negatives, and different-signature negatives.
    - [ ] `[AUDIT004-H]` `duplicate-import-elimination` remapping: add separate call, `ref.func`, table element, export/start, and name/custom-section preservation tests.
    - [ ] `[AUDIT004-I]` `merge-locals`: add separate tests for multi-value functions, GC/ref locals with equal types, GC/ref locals with unequal types, dead-tail local traffic, write-interrupted copy chains, effect-interrupted copy chains, and local-name cleanup.
    - [ ] `[AUDIT004-L]` `once-reduction`: add separate tests for table escape, global escape, `ref.func` escape, export escape, start-function behavior, imported function negative, exported function negative, and multi-use once-positive.
    - [ ] `[AUDIT004-M]` `global-refining`: add separate tests for non-null-to-null negative, incompatible subtype negative, descriptor-bearing type body publicity, and broader stringref expression surfaces where supported; the O4z audit already covered nullable-to-non-null bottom refinements, exported mutability/closed-world guards, subtype-chain joins, `ref.func`, `ref.i31`, `struct.new_default`, and exported exact/private initializer bailouts.
    - [ ] `[AUDIT004-N]` After each pass-family test batch, run the pass-targeted compare smoke with `--count 1000` before moving to the next family; do not batch all behavior changes behind one signoff.
  - Suggested tests: adjacent `*_test.mbt` public-pipeline fixtures, `moon test src/passes`, and pass-targeted compare smoke for each touched pass.
  - Exit criteria: each listed module pass has positive and negative coverage for its most important module shapes, with no new validation failures or unexplained Binaryen mismatches.

- [AUDIT]005 - Standalone No-Inline Pass Contract Tests
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: give `no-inline`, `no-full-inline`, and `no-partial-inline` their own focused module-pass tests instead of relying only on inlining integration tests.
  - Why: `src/passes/no_inline.mbt` has meaningful standalone behavior but no adjacent `no_inline_test.mbt` file.
  - Deliverables:
    - [ ] `[AUDIT005-A]` Add `src/passes/no_inline_test.mbt` with a helper that runs a no-inline module pass and extracts function annotations.
    - [ ] `[AUDIT005-B]` Add wildcard positive coverage for a defined WAT function name.
    - [ ] `[AUDIT005-C]` Add no-match negative coverage proving no annotation section is created.
    - [ ] `[AUDIT005-D]` Add imported function name matching coverage.
    - [ ] `[AUDIT005-E]` Add WAT identifier lowering coverage for defined functions.
    - [ ] `[AUDIT005-F]` Add repeated marker deduplication coverage.
    - [ ] `[AUDIT005-G]` Add metadata-code-inline non-interaction coverage.
    - [ ] `[AUDIT005-H]` Add `no-full-inline` only marker coverage.
    - [ ] `[AUDIT005-I]` Add `no-partial-inline` only marker coverage.
    - [ ] `[AUDIT005-J]` Add combined policy coverage and retain one narrow inlining integration smoke as the end-to-end guard.
  - Suggested tests: `moon test src/passes`, plus a narrow inlining integration smoke after standalone tests are green.
  - Exit criteria: no-inline policy marker semantics are protected independently from the inliner.

- [AUDIT]006 - Function TypeIdx/RecIdx Invariant Documentation
  - Status: active documentation follow-up from user clarification on 2026-05-31.
  - Goal: document clearly that `func_sec` function type references are absolutely invariant as global `TypeIdx` entries, not local recursive `RecIdx` entries; optimizer code may treat `RecIdx` in function-section type-index positions as impossible after valid decode/validation.
  - Why: the audit initially flagged `RecIdx` aborts in locals passes as potential user-input risk. User clarified this shape is a 100% invariant. The issue is documentation clarity, not behavior change.
  - Deliverables:
    - [ ] `[AUDIT006-A]` Add or update a wiki page covering the function-section type-index invariant: module-level function declarations use global `TypeIdx`; `RecIdx` is only meaningful inside a rec-group-local type context.
    - [ ] `[AUDIT006-B]` Cite concrete source anchors in that page: `src/lib/types.mbt`, validation/type-section handling, and at least one optimizer module-pass function-signature cache.
    - [ ] `[AUDIT006-C]` Add inline comment near the `merge-locals` `RecIdx` abort explaining it is an unreachable invariant assertion for function-section type references.
    - [ ] `[AUDIT006-D]` Add inline comment near the `coalesce-locals` `RecIdx` abort explaining the same invariant.
    - [ ] `[AUDIT006-E]` Add inline comment near the `reorder-locals` `RecIdx` abort explaining the same invariant.
    - [ ] `[AUDIT006-F]` Add a pass/helper test or validation test, if practical, demonstrating that valid module-level function declarations resolve through global `TypeIdx`; if no practical test exists, document why the source/validation references are sufficient.
    - [ ] `[AUDIT006-G]` Update `docs/wiki/ir2/pass-porting-checklist.md` or a related architecture page with guidance: do not cargo-cult broad `RecIdx` support into module-pass function-signature caches unless the source is actually inside a rec-group-local type context.
    - [ ] `[AUDIT006-H]` Refresh `docs/wiki/index.md` and `docs/wiki/log.md` for the new/updated invariant documentation.
  - Suggested tests: docs-only changes should still run `moon test src/passes`; if a validation/helper test is added, run the specific package plus `moon test` when practical.
  - Exit criteria: future agents can see that function-section `RecIdx` handling is an invariant assertion and do not spend time trying to support an impossible module shape.

- [AUDIT]007 - Audit Compare Refresh And Mismatch Classification Hygiene
  - Status: active follow-up from the 2026-05-31 optimizer audit.
  - Goal: turn the ad hoc audit smoke into durable, reproducible evidence without treating accepted representation drift as a correctness failure.
  - Why: the 50-case all-pass smoke and 1000-case DAE refresh found no validation failures, but the evidence currently lives only in `.tmp/deep-audit-20260531` and this thread.
  - Deliverables:
    - [ ] `[AUDIT007-A]` File a numbered research note summarizing the exact 2026-05-31 all-pass smoke commands and environment, including `--starshine-bin`, `--jobs auto`, seed, and out dirs.
    - [ ] `[AUDIT007-B]` Add the per-pass count table to that note: compared count, normalized matches, compare-normalized matches, validation failures, command failures, and mismatch count.
    - [ ] `[AUDIT007-C]` Add a lightweight docs recipe for running an all-pass smoke compare with `--jobs auto` and a prebuilt `--starshine-bin`, explicitly not part of normal `moon test`.
    - [ ] `[AUDIT007-D]` Add or update script tests only if the docs recipe requires script behavior changes; keep recipe-only changes docs-only.
    - [ ] `[AUDIT007-E]` Refresh direct `--pass inlining` mismatch classification with a small replay set, explicitly separating local-declaration representation drift from semantic mismatch.
    - [x] `[AUDIT007-F]` Refresh direct `--pass inlining-optimizing` mismatch classification with a small replay set, separately from plain `inlining`. 2026-06-05 replayed `.tmp/pass-fuzz-inlining-optimizing-large-guard1-1000` mismatches with `--normalize drop-consts --normalize local-cleanup-debris` into `.tmp/pass-fuzz-inlining-optimizing-replay-normalized3`: 26/26 cleanup-normalized, 0 mismatches; the follow-up 10000 run is recorded under `[O4Z-AUDIT-INL]`.
    - [ ] `[AUDIT007-G]` Refresh direct `--pass dae-optimizing` classification with documented normalizers and keep generated dropped-constant cleanup drift separate from validation or semantic failures.
    - [ ] `[AUDIT007-H]` Link the research note from the relevant pass wiki pages or the audit/log page so future agents do not rediscover the same smoke results.
  - Suggested tests: no implementation tests unless tooling changes; if tooling/docs recipe changes, run script tests and a small compare replay.
  - Exit criteria: the audit evidence is durable and indexed, with exact commands and agent-classified mismatch families.

### SGO - Follow-Up Improvements

- [SGO]003 - Optional Binaryen `SimplifyGlobals.cpp` Breadth
  - Status: deferred to v0.1.1; not a v0.1.0 blocker.
  - Resume when: a new semantic mismatch, wasm validation failure, targeted artifact/code-size need, or string/GC/refinalization requirement points at SGO breadth.
  - Candidate families: side-effecting-but-safe `read-only-to-write` value-flow positives, broader same-as-init expression matching beyond direct literal / `ref.null` / `ref.func`, broader runtime linear-trace propagation, and additional GC/refinalization-safe replacement surfaces.
  - Deliverables when resumed: add focused shape tests first, rerun direct `--pass simplify-globals-optimizing` oracle fuzz for the new family, and update the SGO wiki pages with the exact accepted subset.

- [SGO]004 - Nested Cleanup Runtime And Exact-Scheduler Experiment
  - Status: deferred to v0.1.1.
  - Resume when: a measured SGO-specific wall-time owner appears, the nested `vacuum` wrapper overhead becomes a concrete runtime target, or an artifact/code-size case demonstrates value from omitted nested default-function slots.
  - Candidate work: reduce nested `vacuum` / lift-lower wrapper cost, add cheaper function-filtered adapters for currently module-shaped cleanup passes, or run a controlled exact-scheduler experiment with artifact and fuzz evidence.
  - Deliverables when resumed: trace before/after nested timers, preserve validation and direct 10k SGO fuzz parity, and document whether the accepted artifact-tuned lane changes.

- [SGO]005 - Default-Local Compare Normalization
  - Status: deferred to v0.1.1 tooling/cosmetic follow-up.
  - Goal: eliminate or normalize the accepted direct SGO artifact diff where Binaryen preserves an explicit default local initialization and Starshine relies on WebAssembly default local zero-initialization.
  - Resume when: exact artifact diffs need to become quieter for release QA or compare-harness work.
  - Deliverables when resumed: either teach the compare helper to ignore explicit default local initialization when semantically equivalent, or add a deliberate Starshine emission/canonicalization option if preserving the explicit set proves more useful.

## v0.2.0 Backlog

### SET - Shared-Everything Threads Proposal Surface

Release stance: proposal-tracking backlog, not a v0.1.0 release gate. Starshine currently has the focused `struct.atomic.get`, `struct.atomic.get_s`, and `struct.atomic.get_u` surface plus conservative optimizer handling. The remaining shared-everything threads surface is intentionally sliced below so future agents can add core/type/binary/WAST/validation/fuzzer/pass support in dependency order instead of treating proposal examples as already-supported syntax.

Common rules for every `[SET-*]` slice:
- Start from `docs/wiki/raw/wasm/2026-06-04-struct-atomic-get-sources.md`, `docs/wiki/wast/gc-aggregate-instruction-authoring.md`, `docs/wiki/wast/atomic-memory-instruction-authoring.md`, and the current shared-everything threads proposal text. Refresh the source snapshot first if the proposal changed.
- Add tests first for the exact surface being widened; do not add parser keywords, binary opcodes, or optimizer rewrites without validator/effect coverage.
- Keep linear-memory atomics (`MemArg` instructions) separate from shared-GC aggregate atomics and proposal shared resource/thread-local features.
- Treat `struct.atomic.get*` as the only already-supported shared-GC aggregate atomic family until a slice below lands.
- Preserve generic optimizer conservatism for any atomic read/write/RMW/cmpxchg/wait operation unless a pass-specific proof documents why rewriting is safe.
- Update `docs/wiki/wast/gc-aggregate-instruction-authoring.md`, `docs/wiki/wast/atomic-memory-instruction-authoring.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, and pass-specific wiki pages when behavior changes.

- [SET-SRC]001 - Refresh shared-everything source manifest and local gap matrix
  - Status: active planning slice; run before implementing any remaining proposal family.
  - Goal: replace the current struct-atomic-get-only source snapshot with a broader proposal tracking manifest and a local implementation matrix.
  - Why: the proposal is moving and the current durable source capture intentionally scoped itself to `struct.atomic.get*`; future implementers need exact opcodes, text spellings, immediates, feature flags, validation rules, and local unsupported surfaces before coding.
  - Deliverables:
    - [ ] Capture the current proposal instruction/type/resource list with date, source URLs, and caveats in `docs/wiki/raw/wasm/` or a numbered research note.
    - [ ] Build a matrix with rows for shared types/resources, thread-local globals, `pause`, `ref.i31_shared`, global atomics, table atomics, struct atomic set/RMW/cmpxchg, array atomic get/set/RMW/cmpxchg, waitqueue operations, aggregate waits, proposal memory waits, and release/acquire ordering.
    - [ ] For each row, record local status across `src/lib`, `src/binary`, `src/wast`, `src/validate`, `src/ir`, `src/passes`, `src/validate/gen_valid`, and docs.
    - [ ] Decide exact local spellings for order operands, including whether compact Binaryen spellings like `seqcst` should remain unsupported or become accepted aliases.
    - [ ] Update the WAST aggregate/atomic pages so they link the new matrix and continue warning that only `struct.atomic.get*` is implemented today.
  - Suggested tests: docs-only unless a source-refresh helper changes; run `bun validate readme-api-sync` if docs references affect README/API sync.
  - Exit criteria: future agents can pick any `[SET-*]` slice without rediscovering proposal scope or conflating shared-GC atomics with linear-memory atomics.

- [SET-TYPE]001 - Sharedness and thread-local resource type model
  - Status: blocked on `[SET-SRC]001` exact rules.
  - Goal: add the type/resource metadata needed by the rest of the proposal before instruction families depend on it.
  - Why: aggregate atomics and shared resource validation require knowing whether heaps, arrays, structs, tables, globals, functions, elements, data, and memories are shared/thread-local where the proposal requires it.
  - Deliverables:
    - [ ] Add focused type/model tests for shared annotations, thread-local globals, and any proposal `bot-share` / shared-heap distinctions selected by `[SET-SRC]001`.
    - [ ] Extend `src/lib/types.mbt` and related constructors only as far as the proposal-backed matrix requires.
    - [ ] Extend binary encode/decode and pretty/show helpers for new type/resource metadata.
    - [ ] Extend WAST parser/printer/lowering for shared/thread-local annotations with round-trip tests.
    - [ ] Extend validation/typecheck with positive and negative sharedness/thread-local tests.
    - [ ] Update arbitrary/module generators only after validation rejects unsupported combinations deterministically.
  - Suggested tests: package-local type/binary/WAST/validate tests, `moon test src/lib`, `moon test src/binary`, `moon test src/wast`, `moon test src/validate`, then `moon test` when the model becomes public.
  - Exit criteria: sharedness/thread-local metadata round-trips and validates independently of the later atomic instruction families.

- [SET-CORE]001 - `pause` and `ref.i31_shared` scalar proposal instructions
  - Status: blocked on `[SET-SRC]001`; independent from aggregate atomic families once exact encodings are confirmed.
  - Goal: implement the smallest non-aggregate proposal instructions as a low-risk proving ground for proposal feature plumbing.
  - Deliverables:
    - [ ] Add core enum/constructor/show/equality/arbitrary coverage for `pause` and `ref.i31_shared` if `[SET-SRC]001` confirms they remain in scope.
    - [ ] Add binary encode/decode tests for exact opcodes and immediates.
    - [ ] Add WAST keyword/parser/printer/lowering tests for text spellings.
    - [ ] Add validation stack/effect tests, including any sharedness restrictions for `ref.i31_shared`.
    - [ ] Lift/lower HOT and model effects conservatively; ensure cleanup passes preserve the instructions unless proved pure/nontrapping by the spec.
  - Suggested tests: `moon test src/lib src/binary src/wast src/validate src/ir src/passes`, then `moon test` after public surface changes.
  - Exit criteria: scalar proposal instructions can parse, encode, validate, lift/lower, print, and survive optimizer passes.

- [SET-GLOBAL]001 - Global atomic get/set/RMW/cmpxchg
  - Status: blocked on `[SET-SRC]001` and any `[SET-TYPE]001` global sharedness model needed.
  - Goal: implement proposal global atomic operations as effectful global/resource operations.
  - Deliverables:
    - [ ] Add instruction enum variants and helpers for `global.atomic.get`, `global.atomic.set`, and `global.atomic.rmw.{add,sub,and,or,xor,xchg,cmpxchg}` or the exact current proposal spellings.
    - [ ] Add binary encode/decode, WAST parse/print/lower, and validation tests for index kind, value type, mutability/sharedness, order operands, stack behavior, and invalid cases.
    - [ ] Model effects as global/resource reads/writes plus trap/order barriers as applicable; HOT lift/lower must not assume `MemArg`.
    - [ ] Update module remapping/liveness passes so global indices and referenced types remain valid after reordering/removal/dedup passes.
    - [ ] Add generic pass guard tests proving optimization passes do not delete, merge, or move global atomic operations unsafely.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass tests, `moon test src/passes`, and a small all-pass fuzz smoke once generation can emit the family.
  - Exit criteria: global atomic operations are first-class, valid, remapped correctly, and optimizer-conservative.

- [SET-TABLE]001 - Table atomic get/set/RMW/cmpxchg
  - Status: blocked on `[SET-SRC]001` and any `[SET-TYPE]001` table sharedness model needed.
  - Goal: implement proposal table atomic operations as effectful table/resource operations.
  - Deliverables:
    - [ ] Add instruction enum variants/helpers for `table.atomic.get`, `table.atomic.set`, and `table.atomic.rmw.{xchg,cmpxchg}` or exact current proposal spellings.
    - [ ] Add binary/WAST/validation tests for table indices, element reference types, stack behavior, sharedness, order operands, and invalid type/index cases.
    - [ ] Extend HOT/effects without reusing linear-memory `MemArg` assumptions.
    - [ ] Update table-index remapping users in directize, duplicate-import/module-element removal, reorder-like paths, and any table-liveness helpers.
    - [ ] Add pass guard tests proving table atomic operations are barriers for CSE, local sinking, DCE, and instruction folding.
  - Suggested tests: `moon test src/lib src/binary src/wast src/validate src/ir src/passes`, then targeted directize/RUME/DIE tests if remapping changes.
  - Exit criteria: table atomic operations preserve table indices/types through all module passes and remain effect barriers.

- [SET-STRUCT]001 - Struct atomic set/RMW/cmpxchg aggregate families
  - Status: blocked on `[SET-SRC]001`; likely depends on `[SET-TYPE]001` shared-struct metadata.
  - Goal: extend the current `StructAtomicGet*` surface to proposal struct atomic writes and read-modify-write operations.
  - Deliverables:
    - [ ] Add core enum variants/helpers for `struct.atomic.set`, `struct.atomic.rmw.*`, and `struct.atomic.rmw.cmpxchg` or exact current proposal spellings.
    - [ ] Add binary encode/decode tests for opcodes, order operands, type index, field index, and value immediates/operands.
    - [ ] Add WAST parse/print/lower tests using canonical Starshine order spellings and explicit compatibility-alias decisions from `[SET-SRC]001`.
    - [ ] Add validator tests for field mutability, packed-field signedness/width, sharedness, stack behavior, null traps, and invalid field/type indices.
    - [ ] Extend HOT lift/lower/effects as aggregate atomic write/RMW/cmpxchg barriers, not linear-memory atomics.
    - [ ] Update duplicate-function/module-element/dead-argument/type-liveness/remap passes for any referenced type indices.
    - [ ] Add generic pass guard tests proving dropped writes/RMW/cmpxchg are preserved and loads/stores are not moved across them.
    - [ ] Revisit GSI/heap2local/global-type-optimization docs after implementation: writes/RMW/cmpxchg keep fields live/mutable and should block immutable-field assumptions unless the proof explicitly excludes them.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass tests, `moon test src/passes`, direct `--pass global-struct-inference` compare after GSI liveness assumptions are touched.
  - Exit criteria: struct aggregate atomic writes/RMW/cmpxchg are first-class and all optimizer passes treat them as effectful field traffic.

- [SET-ARRAY]001 - Array atomic get/set/RMW/cmpxchg aggregate families
  - Status: blocked on `[SET-SRC]001`; likely depends on broader array WAST/core surface and `[SET-TYPE]001` shared-array metadata.
  - Goal: implement proposal array aggregate atomic operations without confusing them with linear-memory atomics.
  - Deliverables:
    - [ ] Audit current array instruction support and add any missing ordinary array core/WAST prerequisites needed for atomic fixtures.
    - [ ] Add core enum variants/helpers for `array.atomic.get*`, `array.atomic.set`, `array.atomic.rmw.*`, and `array.atomic.rmw.cmpxchg` or exact current proposal spellings.
    - [ ] Add binary/WAST/validation tests for array type index, element index operand, packed signedness, order operands, sharedness, null traps, bounds traps, and invalid element types.
    - [ ] Extend HOT/effects as aggregate atomic operations that may trap and synchronize.
    - [ ] Update type-index/remap/liveness passes for referenced array type indices.
    - [ ] Add pass guard tests for optimization passes and any array-specific passes once they exist.
  - Suggested tests: focused array package tests plus `moon test src/passes`; add generator coverage only after validation is stable.
  - Exit criteria: array atomic families are implemented as shared-GC aggregate operations with correct traps, signedness, and optimizer barriers.

- [SET-WAIT]001 - Waitqueue and aggregate wait operations
  - Status: blocked on `[SET-SRC]001`, `[SET-TYPE]001`, and likely core shared resource decisions.
  - Goal: implement proposal waitqueue operations and aggregate waits as a separate synchronization surface from existing linear-memory atomics.
  - Deliverables:
    - [ ] Add core resource/type representation for waitqueues if still present in the proposal.
    - [ ] Add instructions such as `waitqueue.new`, `waitqueue.notify`, `struct.wait`, `array.wait`, `global.wait`, `table.wait`, and proposal `memory.wait32` / `memory.wait64` only after `[SET-SRC]001` confirms exact spelling and behavior.
    - [ ] Add binary/WAST/validation tests for waitqueue indices/resources, waited locations, timeout/value operands, sharedness, trap/blocking semantics, and invalid cases.
    - [ ] Model effects as synchronization/blocking barriers; HOT/pass logic must preserve exact order and never precompute/delete waits or notifies.
    - [ ] Update generators cautiously: deterministic validation fixtures first, randomized emission later with clear feature gates.
  - Suggested tests: focused core/binary/WAST/validate/IR/pass guard tests; avoid runtime execution assumptions unless the harness has deterministic wait behavior.
  - Exit criteria: waitqueue/wait operations validate and round-trip, and optimizers treat them as hard synchronization barriers.

- [SET-MEMORD]001 - Proposal release/acquire orderings for memory atomics and fence
  - Status: blocked on `[SET-SRC]001`; separate from existing stable linear-memory atomic opcode support.
  - Goal: widen existing `MemArg`-based atomic memory instructions and `atomic.fence` only where the proposal adds explicit order operands or altered validation.
  - Deliverables:
    - [ ] Compare current `AtomicOrder` modeling against proposal ordering requirements for linear-memory atomics, aggregate atomics, and fences.
    - [ ] Add binary/WAST/validation tests for any new order immediates, default order behavior, aliases, and invalid encodings.
    - [ ] Keep current linear-memory atomic authoring docs accurate: Starshine core/binary/validator/generator support does not imply WAST text support unless keywords/parser/lowering are added.
    - [ ] Extend HOT/effects and pass guard tests only if order operands change instruction identity or barrier strength.
  - Suggested tests: `moon test src/binary src/wast src/validate src/ir src/passes` plus existing atomic-memory fixture tests.
  - Exit criteria: memory-order semantics are represented consistently across linear-memory and shared-GC atomics without regressing existing atomic tests.

- [SET-GEN]001 - Generator, invalid-fuzzer, and compare harness coverage for shared-everything features
  - Status: blocked until at least one `[SET-CORE]`, `[SET-GLOBAL]`, `[SET-TABLE]`, `[SET-STRUCT]`, `[SET-ARRAY]`, or `[SET-WAIT]` implementation slice lands.
  - Goal: move implemented shared-everything features from hand-written fixtures into systematic valid/invalid generation and pass-compare coverage.
  - Deliverables:
    - [ ] Add GenValid feature-gated generation for each implemented family with a ledger row or update to the existing atomics/GC aggregate rows.
    - [ ] Add invalid-AST strategies for sharedness mismatches, order operand mistakes, index-kind errors, packed signedness mistakes, mutability violations, and waitqueue/resource misuse.
    - [ ] Add binary-invalid tests for malformed encodings that cannot be represented in the AST.
    - [ ] Update compare-pass generation profiles only after Binaryen and Starshine agree on the same proposal surface or the harness can gate proposal features explicitly.
    - [ ] File durable mismatch classifications for any Binaryen proposal-syntax drift or unsupported oracle behavior.
  - Suggested tests: `moon test src/validate src/fuzz src/binary`, targeted `bun fuzz run` smokes, and small `bun scripts/pass-fuzz-compare.ts --count 1000 --jobs auto --starshine-bin ...` lanes once generation is enabled.
  - Exit criteria: implemented proposal features are exercised by valid and invalid fuzzing without destabilizing existing pass parity lanes.

- [SET-PASS]001 - Optimizer-wide shared-everything proposal audit after each new family
  - Status: repeat after each `[SET-*]` instruction-family slice.
  - Goal: prevent new shared-everything instructions from bypassing pass invariants, remapping, liveness, effect barriers, or Binaryen parity docs.
  - Deliverables:
    - [ ] For the newly implemented family, grep every active pass for adjacent ordinary/global/table/struct/array/memory handling and add explicit preserve/remap/barrier coverage where needed.
    - [ ] Update `docs/wiki/raw/research/0708-2026-06-04-struct-atomic-get-pass-opportunity-audit.md` or create a successor numbered audit if the scope outgrows struct atomic gets.
    - [ ] Add public-pipeline guard fixtures for optimization and module-remapping passes touched by the family.
    - [ ] Re-run direct pass compare smokes for any pass whose behavior changes; scale to 10000 only for behavior-changing optimizer rewrites.
    - [ ] Keep GSI/heap2local/global-type-optimization claims conservative unless a family-specific semantic proof supports optimization.
  - Suggested tests: family-specific pass tests, `moon test src/passes`, targeted compare-pass smokes, and docs/wiki log updates.
  - Exit criteria: no newly implemented shared-everything instruction is treated as pure/deletable/remappable-by-accident, and all durable pass decisions are documented.

### INL - Deferred Inliner Breadth

- [INL]005 - Partial Inlining Splitter
  - Status: deferred to v0.2.0; not a v0.1.0 blocker.
  - Resume only if a reduced Pattern A/B fixture demonstrates one of: semantic or validation correctness, a clear pass-local performance win, a downstream size/optimization win that offsets code growth, or a user-facing need for `no-partial-inline` splitter behavior.
  - Deliverables when resumed: implement or explicitly reject/scope out Pattern A and Pattern B splitting, add reduced splitter and helper-cleanup fixtures, define `no-partial-inline` behavior against the splitter, and compare direct `--pass inlining` / `--pass inlining-optimizing` on split-family repros.

- [INL]006 - Residual Name/Annotation Repair
  - Status: deferred to v0.2.0.
  - Resume only with a concrete user-facing debug-name requirement, annotation-collision requirement, semantic mismatch, wasm validation failure, or proven pass-local performance/code-size issue.
  - Deliverables when resumed: add focused repair tests first, then either implement deterministic Binaryen-like local/label name reconstruction and broader annotation collision repair or explicitly document the unsupported surface as rejected.

### Tooling And Runtime Follow-Up

- [TOOL]001 - Self-Optimize Compare Normalization Symmetry
  - Goal: make the exact artifact helper stop reporting harmless raw/debug-only outer-block drift as a pass blocker.
  - Deliverables: either canonicalize Binaryen through the same strip-debug path before canonical-function comparison while preserving `binaryen.raw.wasm`, or teach the canonical-function fallback to ignore transparent unused-label void block wrappers.
  - Acceptance: the exact-slot artifact command no longer reports `defined=200 abs=217` solely because Binaryen raw `--debug` kept an outer block that symmetric normalization removes.

- [HOT]003 - Node-Package Worker Queue Port
  - Status: deferred behind [HOT]002 and future Node package rebuild work.
  - Deliverables: reuse the native hot-batch queue contract in the shipped Node package with `worker_threads`, one WasmGC module per worker, worker-local heap state, and stable-order serialized merge.
  - Dependencies: [HOT]002 native queue contract, worker-local function serialization hooks, and future Node package rebuild work.

### FUZ - Fuzzer Hardening and GenValid Widening

#### Tiny FUZ work-slice board

Use this board as the tracking view for fuzzer work. Each slice should be small enough for one focused TDD loop: add/adjust focused tests, implement one narrow behavior, update docs/wiki counters, and run the targeted fuzz smoke plus `moon test src/fuzz` or the relevant package test. Prefer finishing p1 GenValid/metamorphic slices before p2 infrastructure unless a bug or release need says otherwise.

p1 next-up / active:
- None currently tracked. Prefer adding a new tiny GenValid/metamorphic slice before broad p2 infrastructure work if a release need appears.

p2 invalid/binary/text tiny slices:
- Census note: `[FUZ]1020D1` audited the checked-in invalid-AST registry and wiki history. The registry currently has 265 deterministic AST-invalid strategy specs across all 15 `ValidationIssueFamily` buckets after `[FUZ]1020F3` added packed-field plain-accessor coverage. Already-covered breadth includes the closed `[FUZ]1020A`/`B`/`C`/`D3`/`D4`/`D5`/`E1`/`E2`/`E3`/`E4`/`E5`/`F1`/`F2`/`F3` families: start out-of-range/wrong-kind plus imported-start signature variants, cross-kind duplicate export names, datacount absent/too-small/too-large plus mixed active/passive cardinality mismatch, element function-index/typed-expression/table-index basics plus active element/table ref-type mismatch, table/data/memory bulk operand/index families, atomics non-shared, natural-alignment, and stack/index families, SIMD lane/operand/index families, GC aggregate/input/variance/descriptor basics plus final-supertype policy, descriptor `describes` previous-definition boundary, and packed-field accessor-shape failures, ref/branch payloads including `br_on_null`, `br_on_non_null`, `br_on_cast`, multi-value `br_table` arity, `call_indirect` table element compatibility, `return_call_indirect` table element compatibility, ordinary `try_table` catch payload mismatch, and `try_table` `catch_ref` payload-plus-exnref mismatch, name-map out-of-range indices, and code-section cardinality/imported-body families. Name-section indirect-map ordering/count invalidity was classified by `[FUZ]1020G1` as binary/codec-invalid rather than AST-invalid: `NameMap` and `IndirectNameMap` arrays can be constructed in memory, but `src/binary/encode.mbt` rejects non-increasing indices with `InvalidNameMapOrder`, `src/binary/decode.mbt` rejects malformed ordered/count payloads before validation, and existing binary invalid tests cover the decode/validate split. Do not add an AST-invalid strategy for ordering/count unless the in-memory model gains a representation that bypasses codec ordering invariants and reaches `validate_name_sec(...)`.





p2 oracle/reporting/infrastructure tiny slices:
- None currently tracked. Add a new tiny slice before broad oracle/reporting/infrastructure work.
