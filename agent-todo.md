# Agent Tasks

## Scope
- Keep only active unreleased work.
- Group work by release target.
- Use explicit slice ids so future agents can execute in dependency order.
- Move completed work and historical evidence to the relevant docs/wiki page, release notes, or git history.

## Current Parity Focus
- Keep the Binaryen no-DWARF default optimize path as the v0.1.0 parity target.
- Canonical order and nested-shape notes live in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md` and `docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`.
- Prefer direct-pass semantic parity first, then ordered-neighborhood replay, then preset scheduling.
- Do not widen public `optimize` / `shrink` slots until the surrounding Binaryen neighborhood is representable and oracle-proven.

## v0.1.0 Active Slices

### Binaryen no-DWARF default optimize pathway parity (`wasm-opt version 125`, `-O` / `-Os`)

Goal
- Rebuild the full Binaryen no-DWARF default optimize path used by `tests/node/dist/starshine-debug-wasi.wasm`, including nested reruns inside optimizing global passes.

Required APIs
- `src/passes` registry and preset expansion.
- `src/cmd/cmd.mbt` dispatcher coverage for new pass flags or preset behavior.
- `scripts/self-optimize-compare.ts` and `scripts/pass-fuzz-compare.ts` for oracle checks.
- `tests/node/dist/starshine-debug-wasi.wasm` as the canonical compare input.

Invariants
- Preserve the pre/function/post phase split from the canonical optimize-path docs.
- Preserve nested reruns inside `dae-optimizing`, `inlining-optimizing`, and `simplify-globals-optimizing` until modeled or explicitly blocked.
- Preserve GC, multivalue, and string feature gates for this artifact.
- Do not collapse repeated cleanup slots into one occurrence unless the divergence is documented first.

Suggested Tests
- `moon info`
- `moon fmt`
- `moon test`
- `bun scripts/pass-fuzz-compare.ts --pass <pass> --count 10000 --max-failures 20 --out-dir .tmp/pass-fuzz-<pass>`
- `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --<pass>`

Observed unique-pass order
- `DFE -> RUME -> MP -> OR -> GR -> GSI -> SSA -> DCE -> RUN -> RUB -> OI -> HSO -> PLS -> PC -> CP -> TO -> SLNS -> VQ -> RL -> H2L -> OC -> LS -> CL -> LCSE -> SL -> CF -> MB -> RSE -> DAE -> INL -> DIE -> SGO -> SG -> RG -> DIR`

Completed direct-pass slices
- `code-pushing` / `[CP]002`: accepted on 2026-05-09 under the pass-wide criteria of Binaryen semantic parity, valid wasm output, and pass-local speed at least 50% of Binaryen. Raw wasm/text drift is representation-only and not an active blocker; public preset scheduling remains part of the ordered-neighborhood / tuple-slot work, not a code-pushing direct-pass blocker.
- `tuple-optimization` exact slot / `[TO]005`: accepted on 2026-05-09 for v0.1.0 after `code-pushing -> tuple-optimization -> simplify-locals-nostructure -> vacuum -> reorder-locals -> remove-unused-brs` fixed the real `defined=29`, `defined=31`, `defined=33`, and `defined=200` optimizer shape drifts. The remaining `.tmp/to-exact-slot-artifact` red at `defined=200 abs=217` is a compare-normalization artifact: Starshine raw output and Binaryen strip-debug output both elide/preserve the harmless outer block consistently when normalized the same way, but the helper currently compares strip-debug-normalized Starshine against Binaryen's raw `--debug` output. This is not a v0.1.0 blocker.
- `simplify-locals` / `[SL]004`: accepted on 2026-05-09 for v0.1.0 under the direct-pass criteria of Binaryen semantic parity, Binaryen-accepted wasm output, and pass-local speed at least 50% of Binaryen. The exact debug-artifact helper remains red at `defined=208 abs=225`, but the inspected diff is representation-only stackification/carrier drift: Binaryen keeps an inline value-producing block/extra wrappers and Starshine spills that value to a local before reusing it. No semantic mismatch or validation failure is currently attributed to `simplify-locals`; future exact-helper normalization belongs with `[TOOL]001` or a new cosmetic parity task, not the v0.1.0 gate.
- `code-folding` / `[CF]002`: accepted on 2026-05-10 for v0.1.0 direct-pass signoff under the same criteria. Direct fuzz at `.tmp/pass-fuzz-code-folding-cf002-terminal-if` reported `6759/10000` compared, `0` mismatches, and only `20` Binaryen/tool command failures; direct debug-artifact replay at `/tmp/starshine-self-optimize-compare-starshine-debug-wasi-1680352` stayed inside the pass-local speed floor (`334.711ms` Starshine vs `176.295ms` Binaryen). The remaining direct `defined=220 abs=237` diff is classified helper-wrapper representation drift, and the focused late cleanup replay `code-folding -> merge-blocks -> remove-unused-brs -> remove-unused-names` at `.tmp/cf002-late-cleanup-artifact` had the same first diff and byte-identical focused diff files as the no-CF cleanup baseline at `.tmp/cf002-late-cleanup-without-cf-artifact`, so it is not a code-folding blocker.
- `redundant-set-elimination` / `[RSE]002`: accepted on 2026-05-10 for v0.1.0 direct-pass signoff after the CFG/value-flow slice added branch/default identities, raw/HOT label-exit coverage, branch-free loop fallthrough facts, strict-subtype refined `local.get` retargeting, GC wrapper and aggregate-accessor refinalization fixtures, raw `string.const` / `any.convert_extern` identities, conservative `try_table` fact barriers, and a narrow safe loop-backedge subset that removes default loop writes only when the written local is otherwise loop-invariant. Direct fuzz at `.tmp/pass-fuzz-rse-rse002-final-signoff` reported `6759/10000` compared, `0` mismatches, and `20` Binaryen/tool command failures; `--redundant-set-elimination --vacuum` at `.tmp/rse002-rse-vacuum-final-signoff3` stayed pass-local faster than Binaryen and returned to the classified inherited direct-`vacuum` first diff at `defined=208 abs=225`.
- `dae-optimizing` / `[DAE]001`: accepted on 2026-05-12 for the call-graph pruning and touched-function tracking slice after the case-000690 escaped self-call operand parameter fix and local-declaration frontier classification. The active module pass covers private direct-call dead scalar parameter removal, adjacent self-recursive forwarded-parameter pruning, dropped/uncalled no-param result removal, dead-suffix local-use suppression after root `unreachable`, removed-actual side-effect preservation including value-producing `if` operands, export / `ref.func` escape bailouts, unused simple function type pruning, and narrow Binaryen-observed self-call operand preservation families. Direct fuzz at `.tmp/pass-fuzz-dae-690-final2-1000` reported `998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, and `2` Binaryen/tool command failures; the follow-up classification in `docs/wiki/raw/research/0558-2026-05-12-dae-local-declaration-frontier.md` found all 13 `gen-valid` mismatches are local-declaration-only diffs, while the two command failures are unchanged `binaryen-rec-group-zero` parser/tool failures. Remaining meaningful DAE work moves to `[DAE]002` for artifact parity and the touched-function-filtered nested cleanup scheduler.
- `inlining-optimizing` direct current-supported surface / `[INL]001`: accepted on 2026-05-13 for v0.1.0 direct-pass signoff on the currently implemented direct-call surface after the standard seed lane `.tmp/pass-fuzz-inlining-seed-0x5eed-after-four-func-frontier` reached `9975/10000` compared, `9975` normalized matches, `0` mismatches, `0` validation failures, and `25` ignored Binaryen/tool failures, and the broadened seed lane `.tmp/pass-fuzz-inlining-seed-0x1eed-after-four-func-frontier2` reached `9978/10000` compared, `9978` normalized matches, `0` mismatches, `0` validation failures, `0` generator failures, and `22` ignored Binaryen/tool `binaryen-rec-group-zero` parse failures. Deferred unsupported direct-inliner breadth is split into dedicated `[INL]005` and `[INL]006` follow-up slices after `[INL]004` acceptance.
- `inlining-optimizing` nested useful-passes replay and artifact parity / `[INL]002`: accepted on 2026-05-16 for v0.1.0 after direct `--inlining-optimizing` stopped aborting on `tests/node/dist/starshine-debug-wasi.wasm`, the output validated, and the remaining canonical artifact mismatch was classified as representation/factoring drift rather than correctness or validation risk. The direct closeout compare `.tmp/pass-fuzz-inlining-optimizing-inl002-closeout-10000-keep` reported `9975/10000` compared, `9975` normalized matches, `0` mismatches, `0` validation failures, and `25` ignored Binaryen/tool command failures. Latest artifact replay `.tmp/inl002-puresuffix-only-20260516-002821` exits `0`, validates with `wasm-tools`, produces about `1.7M` wasm versus Binaryen's about `2.2M`, and prints about `35M` WAT versus Binaryen's about `134M`; `wasm-tools objdump` reports Starshine's code section at `1,560,895` bytes versus Binaryen's `2,106,285`, and `wasm-opt --all-features --metrics` reports `762,965` total Starshine nodes versus `977,216` Binaryen nodes. Alignment still differs (`star_skips 119`, `skip_bytes 585064`, top Starshine-larger aligned delta about `25K`), but a targeted correctness triage found no validation error, no exported/start/table/ref.func semantic discrepancy, and no meaning-changing evidence: the top Starshine-larger examples are local/control representation drift, the large Binaryen-larger examples are Binaryen expansion/factoring drift, the 119 skipped Starshine helpers are mostly referenced helper factoring with 12 unreferenced cleanup candidates, and the 200-case smoke `.tmp/pass-fuzz-inlining-optimizing-inl002-puresuffix-200` preserved the known local-declaration/frontier mismatch shape with `0` validation failures. Exact Binaryen byte/WAT parity is not required for this slice; future partial-inlining and multi-result/tail-call/name repair breadth belongs under `[INL]005` and `[INL]006`, and wall-time work belongs under `[WALL]001` unless a pass-local correctness issue appears.
- plain `inlining` direct current-supported surface / `[INL]007`: accepted on 2026-05-13 for v0.1.0 direct-pass signoff after the plain path stopped borrowing optimizing-only helper-retention and cleanup branches. The standard seed lane `.tmp/pass-fuzz-inlining-seed-0x5eed-plain-moonrun-10k-full-after-plain-no-retain-prune` reached `9975/10000` compared, `9169` normalized matches, `806` locals-only representation mismatches, `0` structural mismatches after local-declaration stripping, `0` validation failures, and `25` ignored Binaryen/tool parse failures. The broadened seed lane `.tmp/pass-fuzz-inlining-seed-0x1eed-plain-moonrun-10k-full-after-plain-no-retain-prune` reached `9978/10000` compared, `9208` normalized matches, `770` locals-only representation mismatches, `0` structural mismatches after local-declaration stripping, `0` validation failures, and `22` ignored Binaryen/tool parse failures. The residual local-declaration/allocation drift is accepted as representation-only for direct signoff; heuristic reopen work requires a new semantic mismatch, while partial-inlining and repair breadth remain under `[INL]005` and `[INL]006`.
- `inlining` / `inlining-optimizing` heuristic policy / `[INL]003`: accepted on 2026-05-14 for the current supported direct inliner heuristic/action-filtering surface after the final repeated-work slice matched Binaryen's per-function iteration cap on `.tmp/inl003-b-recursive-cap10.wat`. Source/test audit covered `TrivialInstruction::{Shrinks,MayNotShrink}`, direct-call-only flexible `hasCalls`, combined-size filtering, deterministic same-wave race guards, and the `MaxIterationsForFunc = 5` repeated-work cap. Current signoff lanes after the cap fix: plain `.tmp/pass-fuzz-inlining-inl003-after-repeated-cap-plain-10000` reached `9975/10000` compared, `9169` normalized matches, `806` local-declaration-only mismatches, `0` structural mismatches after local-declaration stripping, `0` validation failures, and `25` ignored Binaryen/tool command failures; optimizing `.tmp/pass-fuzz-inlining-optimizing-inl003-after-repeated-cap-10000` reached `9975/10000` compared, `9975` matches, `0` mismatches, `0` validation failures, and `25` ignored Binaryen/tool command failures. Remaining broad inliner gaps stay under `[INL]005` partial splitting or `[INL]006` repair/tail-call/multivalue/name behavior; unsupported atomic-wrapper text/parser breadth is not an active `[INL]003` blocker without a Starshine-supported repro.
- no-inline policy controls and clone-survival helper / `[INL]004`: accepted on 2026-05-13 for the current no-inline policy surface. Starshine registers `no-inline`, `no-full-inline`, and `no-partial-inline`, parses CLI wildcard arguments such as `--no-inline=*maybe*`, lowers WAT defined/imported function identifiers into structured function names, stores matched policy as internal function annotations, honors full-inline suppression in the direct inliner, keeps function-level `@metadata.code.inline` distinct from no-inline policy, deduplicates repeated policy markers, leaves no-match policy passes unchanged, remaps policy annotations and function names across inlining helper compaction, drops stale function-scoped local/label names after body rewrites, and exposes `no_inline_copy_policy_annotations(...)` for future clone/copy helpers. Partial-inlining-specific no-inline behavior moves with `[INL]005`; no active `[INL]004` blocker remains.

#### Ordered-prefix / hot-pipeline blockers

- No active threading work for v0.1.0. Parallel hot-batch execution is deferred until MoonBit exposes threading support that is suitable for Starshine's native runtime.

#### TO - Tuple Optimization

- No active v0.1.0 tuple exact-slot blocker. Future tuple work should target new semantic/validity mismatches, feature-gated preset coverage if Starshine gains explicit feature options, or runtime reductions such as candidate-heavy functions including `Func 1673`; do not preserve cosmetic raw/debug blocks solely for byte-shape parity.

#### SL - Simplify Locals

- No active v0.1.0 direct `simplify-locals` blocker. Future work should target new semantic or validity mismatches, pass-local runtime regressions proven inside `simplify-locals`, or narrow shrink/code-quality improvements for real value-carrier spills; do not preserve debug-only wrappers solely for byte/shape parity.
- The 2026-05-09 value-carrier shrink pass retired the focused `$913` typed-control tee and `$42` block-result spill/reload families in `defined=208 abs=225`, but exact artifact compare remains red. The next real shrink candidate is broader and riskier: a call-result carrier where Starshine still materializes `call $292 (block ...)` into a local before `call $281`, while Binaryen keeps it inline. Only pursue it with strict branch-depth/effect-order guards.

#### CF - Code Folding

- No active v0.1.0 direct `code-folding` blocker. Future code-folding work should target new semantic/validity mismatches, a proven downstream code-size blocker, or explicit preset-scheduling work after the surrounding late cleanup path is representable; do not implement broad Binaryen function-ending helper-label sharing solely to erase the already-classified `defined=220 abs=237` helper-wrapper representation drift.

#### RSE - Redundant Set Elimination

- No active v0.1.0 direct `redundant-set-elimination` blocker. Future work should target new semantic/validity mismatches, a proven need for full Binaryen-style loop fixed-point convergence beyond the landed safe loop-invariant default-write subset, broader official `rse-gc.wast` families not already covered by wrapper and aggregate-accessor fixtures, or separate late-tail preset scheduling. The `rse -> vacuum` exact replay remains red at the inherited direct-`vacuum` `defined=208 abs=225` representation frontier, not an RSE-specific blocker.

#### DAE - Dead Argument Elimination Optimizing

- [DAE]002 - Nested Post-Inlining Cleanup and Artifact Compare
  - Status: active. A narrow scheduler slice now runs `precompute-propagate-prefix -> dead-code-elimination -> optimize-instructions -> local-cse -> pick-load-signs -> heap-store-optimization -> heap2local -> optimize-casts -> code-pushing -> simplify-locals -> code-folding -> precompute -> merge-blocks -> remove-unused-brs -> remove-unused-names -> merge-blocks -> coalesce-locals -> reorder-locals -> vacuum` only on DAE-touched functions for small modules and keeps untouched functions unchanged; the new private `precompute-propagate-prefix` helper folds SSA-backed default-init and direct local constant facts, then reruns plain `precompute`, but it is not the public upstream sibling. DAE now also has a narrow constant-actual materialization slice for private direct callees: when every direct caller passes the same exact literal and the callee never writes that parameter, Starshine rewrites touched callsites, replaces callee `local.get`s with the literal, and lets the nested cleanup lane fold the remainder. As of 2026-05-13, the exact-literal slice also recovers constant-actual candidates when sibling call arguments use the scalar memory-load family (`i32.load`, `i64.load`, `f32.load`, `f64.load`, and the scalar load8/load16/load32 variants) and typed single-result `TypeIdxBlockType` wrappers, with focused regressions proving a stable middle literal can still materialize through both carrier shapes. `local-cse` now uses a function-filtered raw adapter, while `coalesce-locals` and `reorder-locals` use function-filtered adapters over the existing module passes in this nested lane, and all three direct public passes remain module-shaped. The local-CSE adapter emits `pass[local-cse]:func/start/done` trace lines so focused regressions can pin its place before `simplify-locals`, and the DAE scheduler itself emits `pass[dae-optimizing]:nested-pass name=...` lines so tests can lock the guarded nested pass order without depending on incidental skip/start noise; `precompute-propagate-prefix` / `optimize-instructions` / `pick-load-signs` / `heap-store-optimization` / `heap2local` / `optimize-casts` / `code-pushing` / direct `precompute` / `code-folding` / `merge-blocks` / `remove-unused-brs` / `remove-unused-names` use the existing hot touched-function runner. The lane is still not the full Binaryen `optimizeAfterInlining` replay. Broad/large modules remain guarded because the earlier whole-module cleanup rewrote untouched functions and the current artifact lane is not yet performant enough for full nested replay. The post-scheduler artifact timeout was attributed to repeated DAE core call/dead-suffix scans before the nested marker and partly fixed with a single original call summary plus suffix-target aggregation; traced artifact replay still remains red at `defined=11 abs=28`, and the latest compare at `.tmp/dae002-typeidx-block-artifact` stayed red there while remaining slower than Binaryen (`10139.424ms` vs `928.830ms` pass-local). A new caller census in `docs/wiki/raw/research/0561-2026-05-13-dae002-check-range-load-shape-attribution.md` found `370` direct `moonbit.check_range` callers with the middle `lo` argument always literal `0`; scalar loads were one real blocker (`127` folded callers), and the later typed-block follow-up in `docs/wiki/raw/research/0562-2026-05-13-dae002-typeidx-block-carriers.md` found one more real wrapper blocker. A new reduced repro in `docs/wiki/raw/research/0563-2026-05-14-dae002-later-candidate-starvation.md` now shows that the current fixed `8`-iteration DAE core can leave a later exact-literal candidate unreached after `9` earlier productive rewrites, which makes bounded candidate-reach starvation the leading DAE002 explanation rather than another guessed caller-carrier gap alone. A post-coalesce-locals trace still skips the debug artifact nested lane at `touched=12`.
  - Deliverables remaining: widen the new prefix equivalent toward Binaryen's real `precompute-propagate` semantics or land the real public sibling; replace the guarded small-module cleanup subset with a performant touched-function default pipeline, including more function-local equivalents for currently module-shaped local passes; finish the remaining post-Func-42 artifact parity work beyond the narrow shifted low-wrapper/high-callee selector now covered for the current frontier; keep direct-pass semantic regressions visible if a broader fuzz run discovers a new non-local-declaration mismatch.
  - Current DAE002 evidence: `moon test src/passes` passes with the touched-only scheduler regressions, including focused order, touched-only default-init local folding coverage for the new private `precompute-propagate-prefix`, the earlier every-direct-caller-same-literal constant-actual regression, a load-shaped-sibling regression that proves the exact-literal slice still materializes when another argument is a scalar memory load, a typed-block regression that proves the same through explicit single-result type-index block wrappers, a loop-carried ambient-prefix regression, a same-caller multi-call regression that now also locks the touched rewritten-control cleanup form (`i32.lt_s`, no `i32.eqz`, no `i32.ge_s`), a reduced regression showing that a low-prefix exact-literal revisit can recover a forwarded-const candidate that still sits behind the `8`-iteration core budget plus a reverse-starved tail, and a new reduced regression proving the direct Func 42 rewrite now removes add-zero address drift after const parameter removal. The earlier reduced starvation repro still locks the bounded frontier directly through `pass[dae-optimizing]:core iter=... primary_def=...` trace lines: the productive iterations are `primary_def=0..7`, and no traced core iteration reaches defs `8` or `9` before the current loop ends. New reduced repros now also show that a later exact-literal candidate can be recovered after those eight core iterations, that the same reverse exact-literal lane can chase a tiny forwarding-wrapper chain in a reduced module, and that the low-prefix revisit can do the same without reopening the `case 000690` escaped self-call operand regression. On the original debug artifact, whitebox characterization now shows that the core frontier starts `11, 25, 227, 233, 236, 237, 246, 256, 267, 268, 287, 288`; the direct Func 42 rewrite no longer leaves add-zero / const-before-local `memory.copy` address drift; the retained low-callee core revisit over high callees selected from the first `64` defined callers now rewrites the higher original-artifact callees behind low wrappers at Func 51 / Func 58; and the shifted forwarding-wrapper selector covers the current `defined=64..127` frontier without reopening broad `0..128` boundary chasing, including artifact callees Func 1528, 1535, 1559, 1520, and 1524. The live full artifact compare at `.tmp/dae-parity-selected236-locals-fast-artifact` now differs first at `defined=237 abs=254` after the selected Func236 local-retarget lane also covers the former `defined=236 abs=253` local/nop/drop-zero cleanup frontier, and after the selected const-if lane also covers the former `defined=233 abs=250` constant-condition wrapper frontier, and after the selected dropped-result lane also covers the former `defined=220 abs=237` / Func299 result-drop frontier, and after the boundary-wrapper revisit also covers the former `defined=135 abs=152`, `defined=142 abs=159`, `defined=143 abs=160`, `defined=144 abs=161`, `defined=145 abs=162`, `defined=146 abs=163`, `defined=148 abs=165`, `defined=149 abs=166`, `defined=151 abs=168`, `defined=152 abs=169`, `defined=157 abs=174`, `defined=158 abs=175`, `defined=159 abs=176`, `defined=161 abs=178`, `defined=162 abs=179`, `defined=167 abs=184`, `defined=170 abs=187`, `defined=175 abs=192`, `defined=176 abs=193`, `defined=177 abs=194`, `defined=178 abs=195`, `defined=180 abs=197`, `defined=181 abs=198`, `defined=182 abs=199`, `defined=184 abs=201`, `defined=186 abs=203`, `defined=187 abs=204`, `defined=188 abs=205`, `defined=189 abs=206`, `defined=191 abs=208`, and `defined=196 abs=213` frontiers, and the urgent runtime cliff remains barely within the DAE002 2x Binaryen target after sharing original escape facts across summary collectors, collecting escaped-undropped suffix targets directly, aggregating result/mutual dead-suffix target scans, reusing current direct-caller facts for uniform-constant actuals, folding dropped-call/escape collection into current call facts, rewriting only known direct callers, keeping low-prefix callee selectors stable within each revisit loop, capping selected-def core scans at the last selected def, skipping the low-prefix exact-literal revisit on large modules, and trimming the artifact-specific low-callee/low-wrapper loops to their productive counts, using a boundary-specific unread-parameter rewrite, sharing boundary current-call facts across boundary rewrites, rerouting the artifact-specific low-callee and low-wrapper loops through that narrow unread-param helper with shared current-call facts, and adding narrow selected exact-literal lanes for the Func 304, Func 328, Func 1737, Func 1772, Func 1792, Func 1811, and Func 2173 callsite drifts, plus an immutable-`global.get` exact-literal lane only for the selected Func 330 / def `313` drift, a selected dropped-result lane for Func299, a selected constant-if fold for Func233, and a selected local-retarget/nop/drop-zero cleanup for Func236: latest repeated artifact pass-local timings after the low-loop, reverse-lane, shared reverse current-call-fact, low-lane/selected-lane shared current-call-fact, direct boundary wrapper-to-callee selector, precomputed boundary wrapper/callee queue, direct-caller self-recursion guard, and original suffix-target stamp-table runtime slices are `741.094ms`, `737.795ms`, and `734.585ms` Starshine versus `851.457ms`, `838.917ms`, and `832.981ms` Binaryen, with same-session direct Starshine pass-timer evidence improving from `771.283ms`, `763.074ms`, `782.472ms` baseline to `726.991ms`, `743.688ms`, `725.645ms`; the later original-type-index snapshot reuse slice preserved byte-identical raw output at SHA `5ef1b8b238989372a482014be46c13a3af6d86262e721259d3c1eda4b6f6c93f`, accepted output SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002`, and first diff `defined=237 abs=254` while repeating at `712.533ms`, `724.751ms`, and `746.108ms` Starshine versus `833.349ms`, `850.807ms`, and `838.413ms` Binaryen. A usefulness-oriented raw cleanup then deliberately stopped treating byte-identical parity as the acceptance bar: `.tmp/dae-touched-raw-cleanup-artifact3` first stripped `nop` and dropped-zero debris from touched functions, removed `597` normalized printed `nop`s on the debug artifact, shrank Starshine raw wasm from `3,536,456` to `3,533,111` bytes and canonical wasm from `3,543,259` to `3,539,980` bytes, validated with `wasm-opt --all-features`, kept pass-local timings within target (`790.132ms`, `754.341ms`, `758.533ms` versus Binaryen `903.191ms`, `878.587ms`, `869.693ms`), and preserved the established 1000-case direct fuzz smoke shape at `.tmp/pass-fuzz-dae-touched-raw-cleanup-1000` (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures). The follow-up post-touch module raw cleanup widens that cheap debris strip to all module bodies without widening the nested scheduler's touched set: `.tmp/dae-module-raw-pure-leaf-cleanup-artifact3` removes all `6,150` remaining normalized printed `nop`s, reduces dropped pure leaves in normalized text (`i32.const` drops `3,115 -> 60`, `i64.const` drops `6 -> 0`, `local.get` drops `603 -> 425`, `global.get` drops `1 -> 0`), shrinks Starshine raw wasm from `3,533,111` to `3,517,075` bytes and canonical wasm from `3,539,980` to `3,523,993` bytes, validates with `wasm-opt --all-features`, keeps pass-local timings within target (`874.285ms`, `918.233ms`, `936.437ms` versus Binaryen `982.459ms`, `991.208ms`, `1152.360ms`), and preserves the established 1000-case direct fuzz smoke shape at `.tmp/pass-fuzz-dae-module-raw-pure-leaf-cleanup-1000` (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures). The 10k usefulness signoff at `.tmp/pass-fuzz-dae-useful-raw-cleanup-10000` preserved the latest full active fuzz frontier exactly (`9975/10000` compared, `9622` normalized matches, `353` normalized mismatches, `0` validation failures, `25` Binaryen/tool command failures classified as `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`), so the current raw-cleanup DAE frontier is usefulness-signable even though exact Binaryen byte/shape parity remains intentionally out of scope. The first diff now appears earlier (`defined=200 abs=217`) because useful no-op cleanup intentionally changes Starshine's shape rather than preserving the former byte-identical parity frontier; naive boundary chasing and broad global materialization remain explicitly out of scope because earlier broad-window/global experiments were too slow. Full direct fuzz at `.tmp/pass-fuzz-dae-optimizing-dae002-closeout-10000-after-type-ref-fix` reported `9975/10000` compared, `9606` normalized matches, `369` mismatches, `0` validation failures, and `25` Binaryen/tool command failures, with the earlier `case-001655-wasm-smith` Starshine `No type exists for TypeIdx` command failure fixed by keeping simple function types live when referenced through table/global/element/local/ref heap types. The remaining fuzz mismatches are dominated by existing `gen-valid` representation/type-pruning or unreachable-body parity drift, not validation failures. The latest full active fuzz rerun at `.tmp/pass-fuzz-dae-optimizing-fullsignoff-10000-final2` reported `9975/10000` compared, `9622` normalized matches, `353` normalized mismatches, `0` validation failures, and `25` Binaryen/tool command failures after restoring read-only removed parameters to no-local rewrites, repairing dead-suffix `call; drop` sites when result removal changes a callee to void, and preserving result signatures when another function still has an undropped dead-suffix call to that callee. The direct debug-artifact replay now differs first at `defined=237 abs=254`; `.tmp/dae-low-unread-facts-artifact`, `.tmp/dae-low-unread-facts-artifact2`, `.tmp/dae-low-unread-facts-artifact3`, and final `.tmp/dae-low-unread-final-artifact` keep the Starshine output byte-identical to `.tmp/dae-parity-selected236-locals-fast-artifact` while improving pass-local runtime to `1204.050ms`, `1375.033ms`, `1238.117ms`, and `1449.124ms` versus Binaryen `901.666ms`, `933.922ms`, `924.685ms`, and `1038.630ms`; focused Func237 cleanup probes did not advance the frontier (see `docs/wiki/raw/research/0570-2026-05-18-dae002-func237-frontier-classification.md`): `.tmp/dae-selected237-fullcleanup-probe-artifact` stayed at `defined=237 abs=254` and exceeded the pass-local 2x target (`1905.725ms` vs `915.815ms`), `.tmp/dae-selected237-no-dce-coalesce-probe-artifact` also stayed at `defined=237 abs=254` with marginal/tight timing (`1827.431ms` vs `921.109ms`) and still left the broad local/control-shape drift, forcing the existing nested lane only for Func237 at `.tmp/dae-selected237-existing-nested-probe-artifact` stayed at `defined=237 abs=254` while exceeding the target (`1926.365ms` vs `948.117ms`), and a narrow default-carrier fold prototype also failed to affect the real artifact (`.tmp/dae-selected237-carrier2-artifact` at `1804.251ms` vs `903.822ms`; `.tmp/dae-selected237-carrier3-artifact` at `1836.028ms` vs `925.716ms`). A later exact top-level zero-result carrier-to-void prototype at `.tmp/dae-selected237-zero-carrier-artifact` validated but did not advance first diff (`defined=237 abs=254`) and exceeded the 2x target (`1840.261ms` vs `911.112ms`), so it was reverted rather than committed. A singleton exact-literal scheduler refactor also preserved the Func237 first diff but did not produce stable runtime headroom after repeated final replays (`.tmp/dae-exact-literal-singledef-final2-artifact` at `1868.010ms` vs `923.582ms`; `.tmp/dae-exact-literal-singledef-final3-artifact` at `1855.297ms` vs `935.716ms`), so it was also reverted. A boundary-wrapper singleton unread-param helper likewise kept first diff at Func237 and produced unstable timing (`.tmp/dae-boundary-singledef-artifact` at `1618.483ms` vs `839.344ms`, then `.tmp/dae-boundary-singledef-final2-artifact` at `1694.885ms` vs `836.189ms`), so it was not retained. A more exact leading Func237 carrier prototype that strips the actual trailing `local.get(default); br outer` from the result carrier also failed to move first diff and exceeded the target (`.tmp/dae-selected237-leading-carrier-artifact` at `1948.557ms` vs `973.105ms`), so it was reverted. A selected Func237 single-write alias-vacuum prototype also kept the first diff at `defined=237 abs=254` and exceeded the target (`.tmp/dae-func237-alias-vacuum-artifact` at `1887.862ms` vs `946.443ms`), so it was reverted. A reverse exact-literal scan-limit prototype preserved byte-identical Starshine output and the same reverse primary-def sequence (`4593, 4592, 4591, 4589, 4588, 4587, 4586, 4584`) but did not produce stable artifact runtime headroom (`.tmp/dae-reverse-scan-limit-artifact` at `1894.043ms` vs `966.733ms`, `.tmp/dae-reverse-scan-limit-artifact2` at `2123.568ms` vs `990.359ms`; traced repeat baseline/change was `1759.338ms` vs `1955.625ms`), so it was reverted. A selected-core minimum-scan-bound prototype also preserved byte-identical Starshine output and initially looked acceptable (`.tmp/dae-selected-core-minscan-artifact` at `1727.293ms` vs `936.233ms`, `.tmp/dae-selected-core-minscan-artifact2` at `1842.200ms` vs `957.338ms`) but failed the stability repeat after a same-session baseline replay (`.tmp/dae-baseline-repeat-after-minscan-artifact` at `1896.256ms` vs `932.709ms`, then `.tmp/dae-selected-core-minscan-artifact3` at `2063.814ms` vs `1000.570ms`), so it was reverted rather than committed. Default `wasm-tools validate` rejects the original input artifact itself at Func 535 for exceeding the local limit, so that local-limit diagnostic is not a DAE output-only regression. Remaining fuzz mismatches are representation/type-pruning/unreachable-body drift rather than validation failures. The focused DAE ref-local regressions now prove the guarded nested lane stays valid for simple write-site, tee-driven, optimize-casts-adjacent, `ref.as_non_null(local.get ...)`, loop-carried, block-wrapped `try_table`, lib-constructed `call_ref`, and the originally suspicious dominated/undominated sibling-join ref-local families. The repro hunt also uncovered and fixed two inherited IR-context false leads outside the DAE scheduler logic itself: after `optimize-instructions` simplified a ref-local `try_table` body nested under an outer `block`, Starshine lowered catch depths against the full active label stack and rewrote a valid `catch_all 0` into invalid `catch_all 1`; and on lib-constructed `call_ref` fixtures with duplicate same-shape function signatures, `HotModuleContext` had been blanking declared function type slots, so `ref.func` widened to abstract `funcref` during HOT lifting and then failed a later `local.set` into `(ref null (type ...))` when touched `optimize-instructions` replay ran. Both now have focused regressions. A follow-up combo fuzz compare for `--dae-optimizing --local-subtyping` at `.tmp/dae-local-subtyping-1000` reported `998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, and `2` unchanged `binaryen-rec-group-zero` command failures; its failure set exactly matches `.tmp/dae002-coalesce-locals-1000`, and all 13 mismatches are the same local-declaration-only `gen-valid` diffs. During the same hunt, a tempting top-level `try_table (catch_all 0)` value-result family turned out to be fixture-invalid (`catch_all` target arity mismatch) rather than a DAE bug, so there is no remaining validated touched-local-subtyping validity blocker in the currently exercised control-flow or `call_ref` families; the open work is future local-subtyping parity/refinalization plus broader constant-actual/artifact parity, not a reproduced DAE touched-only scheduler bug.
  - Deferred DAE families to consider during artifact work, but not to force from the saved direct 1000-case local-decl frontier alone: broader constant actual materialization, non-adjacent/localized forwarding and recursive cycles, GC param/result refinement, full call operand localization, complete result-removal scheduling with dead internal call filtering, and broader constant-safety proofs for currently preserved trapping numeric operations such as float-to-int truncation. The former Starshine-larger root-unreachable prefix class around `case-006030-gen-valid` is no longer the leading size issue after the nontrapping/const-safe numeric raw-cleanup slice: `.tmp/pass-fuzz-dae-constsafe-raw-cleanup-10000` reports `9975/10000` compared, `6078` normalized matches, `3897` normalized mismatches, `0` validation failures, and `25` Binaryen/tool command failures; all `3897` mismatch artifacts have Starshine smaller than Binaryen, with canonical mismatch-size sum `-4,815,090` bytes, and `case-006030-gen-valid` flips from `+830` to `-702` bytes (`203` Starshine vs `905` Binaryen). The follow-up arbitrary straight-line nontrapping pure stack cleanup keeps the same 10k counts and size sum at `.tmp/pass-fuzz-dae-stack-raw-cleanup-10000` while covering deeper raw expression stacks in focused tests. Dropped `select` cleanup now applies the same pure/nontrapping stack contract to all three consumed operands and preserves possibly trapping/effectful operand stacks; `.tmp/pass-fuzz-dae-select-raw-cleanup-10000` reported `9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures. The float-to-int cleanup treats saturating truncation as nontrapping pure unary debris and removes simple dropped regular float-to-int truncation only for constant operands whose truncated result is representable; `.tmp/pass-fuzz-dae-float-trunc-cleanup-10000` reported the same `9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures. The first narrow ref cleanup now removes dropped `ref.null; ref.is_null` debris under the same pure/nontrapping contract while preserving `ref.null; ref.as_non_null; drop` because `ref.as_non_null` can trap on null; `.tmp/pass-fuzz-dae-ref-null-cleanup-10000` again reported `9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The follow-up ref cleanup removes dropped `ref.eq` only when both operand stacks are recognized pure/nontrapping, with focused coverage preserving a `ref.as_non_null` operand stack; `.tmp/pass-fuzz-dae-ref-eq-cleanup-10000` reported the same aggregate counts and command-failure classes. The next ref-conversion/i31 cleanup removes dropped `ref.i31`, `any.convert_extern`, and `extern.convert_any` stacks under the same recognized pure/nontrapping unary contract, while focused coverage preserves nullable `i31.get_s` and `ref.as_non_null`-fed conversion stacks because those operand stacks may trap; `.tmp/pass-fuzz-dae-ref-convert-i31-cleanup-10000` reported the same aggregate counts and command-failure classes. A follow-up removes dropped `i31.get_s` / `i31.get_u` only when the consumed operand stack is proven to end in `ref.i31`, preserving nullable `i31ref` inputs; `.tmp/pass-fuzz-dae-i31-get-cleanup-10000` reported the same aggregate counts and command-failure classes. The next tiny numeric cleanup removes dropped constant `i64.div_s` only when the divisor is nonzero and the signed-minimum divided by `-1` overflow trap cannot occur, with focused tests preserving the overflow case. A follow-up SIMD-core raw cleanup removes dropped `v128.const`, `v128.not`, `v128.any_true`, bitwise `v128.and/andnot/or/xor`, and `v128.bitselect` stacks only when every consumed operand stack is recognized pure/nontrapping, while focused coverage preserves `v128.load`-fed `v128.bitselect` because the memory load may trap. The next SIMD-unary cleanup applies the same contract to lane-local nontrapping integer/float unary ops and integer all_true/bitmask reductions, with focused coverage preserving a `v128.load`-fed unary stack. The SIMD-binary cleanup now covers audited two-`v128` lane comparisons and basic nontrapping lane arithmetic under the same all-operands-recognized contract, with focused coverage stripping constant-fed `i8x16.add`, `i32x4.eq`, and `f32x4.div` stacks while preserving a `v128.load`-fed `i8x16.add`; shifts, lane extract/replace, narrowing/conversions, extmul/dot, relaxed SIMD, and memory operations remained deferred at that point. The 10k compare at `.tmp/pass-fuzz-dae-simd-binary-cleanup-10000` preserved the current raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and the established `25` Binaryen/tool command failures). The SIMD-splat cleanup now treats the six lane splats as nontrapping pure unary debris over scalar operands, with focused coverage stripping constant-fed `i8x16.splat` and `f64x2.splat` while preserving an `i32.load`-fed `i8x16.splat` because the scalar load may trap; `.tmp/pass-fuzz-dae-simd-splat-cleanup-10000` again preserved the same 10k aggregate counts and command-failure classes. The SIMD shuffle/swizzle follow-up adds `i8x16.shuffle` and `i8x16.swizzle` to the audited two-`v128` nontrapping pure binary cleanup contract, strips constant-fed shuffle/swizzle drops, and preserves load-fed shuffle/swizzle stacks; `.tmp/pass-fuzz-dae-simd-shuffle-swizzle-cleanup-10000` preserved the same 10k aggregate counts and command-failure classes (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, `25` Binaryen/tool command failures). The SIMD lane follow-up removes immediate lane extracts as nontrapping pure unary debris and immediate lane replacements as nontrapping pure binary debris only when every consumed v128/scalar operand stack is recognized pure/nontrapping; focused coverage strips constant-fed extract/replace drops and preserves v128-load/scalar-load-fed lane stacks. `.tmp/pass-fuzz-dae-simd-lane-cleanup-10000` preserved the same 10k aggregate counts and command-failure classes. The SIMD shift follow-up extends the same mixed vector/scalar binary contract to integer lane shifts (`i8x16`, `i16x8`, `i32x4`, and `i64x2` shl/shr_s/shr_u), strips constant-fed shifts, and preserves v128-load/scalar-load-fed shifts; `.tmp/pass-fuzz-dae-simd-shift-cleanup-10000` preserved the same 10k aggregate counts and command-failure classes. The SIMD extension follow-up removes dropped integer lane `extend_low/high` and `extadd_pairwise` stacks as nontrapping pure unary debris when their v128 input stack is recognized pure/nontrapping; focused coverage strips constant-fed `i16x8.extend_low_i8x16s` and `i32x4.extadd_pairwise_i16x8u` while preserving load-fed extension/extadd stacks. `.tmp/pass-fuzz-dae-simd-extend-cleanup-10000` preserved the same 10k aggregate counts and command-failure classes. The SIMD narrow/extmul/dot follow-up removes dropped integer lane narrowing, extmul low/high, and `i32x4.dot_i16x8s` stacks under the same audited all-operands-recognized pure/nontrapping binary contract; focused coverage strips constant-fed narrow/extmul/dot drops and preserves v128-load-fed stacks because those operand loads may trap. `.tmp/pass-fuzz-dae-simd-narrow-extmul-dot-cleanup-10000` preserved the same 10k aggregate counts and command-failure classes (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, `25` Binaryen/tool command failures).

#### INL - Inlining Optimizing

- No active v0.1.0 INL implementation slice. `[INL]001`, `[INL]002`, `[INL]003`, `[INL]004`, and `[INL]007` are accepted for the current v0.1.0 surfaces. Do not pick up partial splitting, residual local/label name reconstruction, or other unsupported direct-inliner breadth after DAE unless new evidence shows a semantic mismatch, wasm validation failure, exported/start/table/ref.func discrepancy, or proven pass-local performance/code-size issue. Deferred INL follow-up lives in the v0.2.0 backlog below.

#### SGO - Simplify Globals Optimizing

- [SGO]001 - Constant-Global Rewrite and Mutation Tracking
  - Status: active / partial. Starshine now registers `simplify-globals-optimizing` as an active module pass with a first module-owned global fact table over imports, exports, table initializers, global initializers, element item expressions, data offsets, and function bodies. The landed slice promotes private never-written or never-read-dead-written mutable globals to immutable, folds single-use global initializers into later global initializers, canonicalizes exact-type immutable global-copy chains to the earliest compatible ancestor in global initializers and function bodies, replaces supported single-instruction constant global reads in function bodies, propagates known startup constants into table initializers / global initializers / element offsets / data offsets, propagates single-const global writes, including imported/exported globals and reference-typed `ref.func` / `ref.null` facts, along straight-line, top-level-noise, adjacent/nested plain-block, and if-then-body runtime traces with call/control/early-exit barriers plus guardrail coverage for independent writes, latest-write wins, loops, `try_table`, post-if non-joins, else arms, reference and externref-null deep-control barriers, mixed scalar/reference independent-write preservation, and harmless drop/pure-noise block bodies, rewrites never-read, direct literal/ref-null/ref.func same-as-init with guardrails for expression, result-block non-matches, defined `global.get`/alias-init one-pass/two-pass behavior, imported `global.get`/alias-init boundaries, exported alias targets, and funcref/externref alias chains, adjacent/eqz/bidirectional compare-const/simple-pure-condition (including i32 unary/bitwise/shift-rotate ops, i64 equality/compare and non-trapping value ops, plus f32/f64 compares), transparent, nested, block-wrapped pure-condition, block-yielded-condition, block-yielded operators after pure block conditions, block-yielded external pure-condition chains, block-yielded short external pure operators, block-yielded reverse external pure operators, no-op-condition-prefix, and const-drop-body read-only-to-write self-guard, and exact/eqz/bidirectional compare/pure-condition/block-wrapped-condition/block-wrapped-pure-condition/nested-block-wrapped-condition/block-yielded-condition/block-yielded-condition+set/block-yielded operators after pure block conditions/block-yielded external pure-condition chains/block-yielded short external pure operators/block-yielded reverse external pure operators/block-wrapped-set/block-wrapped-condition+set `if return; set` read-only-to-write `global.set` sites to `drop(value)`, preserves reference-typed element item expressions, including `ref.func` globals and aliases, for type/refinalization safety while allowing validated typed `ref.null`, typed externref-null direct/alias/block-result, and direct/alias/block-result `ref.func` function-body replacements, and returns an exact touched defined-function bitset for changed function bodies.
  - Deliverables remaining: broaden toward Binaryen's full shared `SimplifyGlobals.cpp` engine, including broader same-as-init expression matching beyond direct literals/ref-null/ref.func, direct-literal void-block bodies, and the probed defined-`global.get`/alias-init repeated-run boundary, broader `read-only-to-write` shapes beyond the current adjacent/eqz/bidirectional compare-const/simple-pure-condition (including i32 unary/bitwise/shift-rotate ops, i64 equality/compare and non-trapping value ops, plus f32/f64 compares), transparent, nested, block-wrapped pure-condition, block-yielded-condition, block-yielded operators after pure block conditions, block-yielded external pure-condition chains, block-yielded short external pure operators, block-yielded reverse external pure operators, no-op-condition-prefix, and const-drop-body self-guard and exact/eqz/bidirectional compare/pure-condition/block-wrapped-condition/block-wrapped-pure-condition/nested-block-wrapped-condition/block-yielded-condition/block-yielded-condition+set/block-yielded operators after pure block conditions/block-yielded external pure-condition chains/block-yielded short external pure operators/block-yielded reverse external pure operators/block-wrapped-set/block-wrapped-condition+set `if return; set` matchers, broader runtime linear-trace propagation beyond the landed top-level-noise, plain-block, if-then-body, and imported/exported/reference/externref same-trace subset and mixed scalar/reference independent facts, type/refinalization breadth for replacement expressions, debug-artifact Binaryen oracle coverage beyond the focused shape tests, and fresh direct fuzz after each new rewrite family. The 2026-05-18 upstream refresh in `docs/wiki/raw/research/0570-2026-05-18-simplify-globals-optimizing-current-main-refresh.md` found no current-main SGO semantic drift and now leaves side-effecting value-flow positives, same-as-init breadth, and GC/refinalization as separate slices after the landed simple pure-condition bridge.
  - Current evidence: `moon test src/passes` passes, `.tmp/pass-fuzz-sgo-typed-externref-body-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the typed externref function-body guardrail slice; `.tmp/pass-fuzz-sgo-typed-ref-func-body-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the typed ref-func function-body guardrail slice; `.tmp/pass-fuzz-sgo-mixed-independent-runtime-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the mixed scalar/reference independent runtime guardrail slice; `.tmp/pass-fuzz-sgo-externref-deep-control-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the externref deep-control runtime guardrail slice; `.tmp/pass-fuzz-sgo-reference-deep-barriers-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the reference deep-control runtime guardrail slice; `.tmp/pass-fuzz-sgo-typed-element-refinalization-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the typed-element/refinalization guardrail slice; `.tmp/pass-fuzz-sgo-externref-alias-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the externref-alias same-init guardrail slice; `.tmp/pass-fuzz-sgo-reference-alias-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the reference-alias same-init guardrail slice; `.tmp/pass-fuzz-sgo-reference-runtime-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the reference-runtime guardrail slice; `.tmp/pass-fuzz-sgo-ref-func-same-init-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the ref.func same-as-init guardrail slice; `.tmp/pass-fuzz-sgo-import-export-barriers-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the imported/exported runtime-barrier guardrail slice; `.tmp/pass-fuzz-sgo-alias-import-export-runtime-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the alias/import-export runtime slice; `.tmp/pass-fuzz-sgo-global-get-init-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the global-get-init guardrail slice; `.tmp/pass-fuzz-sgo-same-init-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the same-as-init guardrail slice; `.tmp/pass-fuzz-sgo-then-guardrails-top-noise-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the then-body guardrail and top-level-noise slice; `.tmp/pass-fuzz-sgo-then-else-runtime-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the then-body-with-else runtime propagation slice; `.tmp/pass-fuzz-sgo-noelse-if-runtime-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the no-else if-body runtime propagation slice; `.tmp/pass-fuzz-sgo-runtime-block-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the runtime block guardrail and pure-noise slice; `.tmp/pass-fuzz-sgo-nested-runtime-blocks-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the nested runtime block propagation slice; `.tmp/pass-fuzz-sgo-runtime-blocks-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the adjacent runtime block propagation slice; `.tmp/pass-fuzz-sgo-external-guardrails-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the external block-yielded guardrail-negative slice; `.tmp/pass-fuzz-sgo-blockyield-reverse-external-pure-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded reverse external pure-operator self-guard and `if return; set` slice; `.tmp/pass-fuzz-sgo-blockyield-short-external-pure-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded short external pure-operator self-guard and `if return; set` slice; `.tmp/pass-fuzz-sgo-blockyield-external-pure-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded external pure-condition self-guard and `if return; set` slice; `.tmp/pass-fuzz-sgo-blockyield-pure-ops-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded external-operator after pure block condition self-guard and `if return; set` slice; `.tmp/pass-fuzz-sgo-pure-condition-refactor-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the pure-condition whitelist helper refactor; `.tmp/pass-fuzz-sgo-block-pure-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped pure-condition `if return; set` slice; `.tmp/pass-fuzz-sgo-block-pure-selfguard-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-pure-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the pure-condition `if return; set` slice; `.tmp/pass-fuzz-sgo-guard-negatives-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after guardrail negatives for wrong-target writes, non-constant set operands, `else` arms, `select` value-flow, and trapping loads; `.tmp/pass-fuzz-sgo-float-compare-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the f32/f64 compare pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-i64-value-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the non-trapping i64 value pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-i64-compare-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the i64 equality/compare pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-unary-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the unary pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-shift-rotate-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the shift/rotate pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-bitwise-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the bitwise pure-condition self-guard slice; `.tmp/pass-fuzz-sgo-pure-condition-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the simple pure-condition self-guard slice; `bun fuzz compare-pass --count 10000 --seed 0x5eed --pass simplify-globals-optimizing --max-failures 20 --keep-going-after-command-failures --out-dir .tmp/pass-fuzz-sgo-eqz-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the `i32.eqz` self-guard slice; `bun fuzz compare-pass --count 10000 --seed 0x5eed --pass simplify-globals-optimizing --max-failures 20 --keep-going-after-command-failures --out-dir .tmp/pass-fuzz-sgo-revcompare-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the bidirectional compare-const self-guard slice; `.tmp/pass-fuzz-sgo-if-return-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the exact `if return; set` slice; `.tmp/pass-fuzz-sgo-ifreturn-cond-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the eqz and bidirectional compare `if return; set` slice; `.tmp/pass-fuzz-sgo-block-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped-condition `if return; set` slice; `.tmp/pass-fuzz-sgo-ifreturn-setblock-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped-set `if return; set` slice; `.tmp/pass-fuzz-sgo-ifreturn-blockcond-setblock-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped-condition+set `if return; set` slice; `.tmp/pass-fuzz-sgo-nested-blockcond-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the nested block-wrapped condition `if return; set` slice; `.tmp/pass-fuzz-sgo-blockyield-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded external condition-operator `if return; set` slice; `.tmp/pass-fuzz-sgo-blockyield-blockset-ifreturn-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after combining those block-yielded condition operators with block-wrapped set tails; the prior `.tmp/pass-fuzz-sgo-transparent-10k-final` lane reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures; `.tmp/pass-fuzz-sgo-blockcond-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped-condition slice; `.tmp/pass-fuzz-sgo-eqz-constdrop-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-wrapped `eqz` and const-drop body slice; `.tmp/pass-fuzz-sgo-condprefix-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the no-op condition-prefix slice; `.tmp/pass-fuzz-sgo-blockyield-selfguard-10k` reported 9975/10000 compared, 9975 normalized matches, 0 mismatches, 0 validation failures, and 25 Binaryen/tool command failures after the block-yielded external condition-operator self-guard slice; and `bun fuzz compare-pass --count 10000 --seed 0x5eed --generator gen-valid --pass simplify-globals-optimizing --out-dir .tmp/pass-fuzz-sgo-rotw-gen-valid-10k` matched Binaryen on 10000/10000 valid generated modules.

- [SGO]002 - Nested Default-Function Rerun and Artifact Compare
  - Status: accepted for the current v0.1.0 direct-pass/nested-runtime surface. The optimizing wrapper reruns Starshine's touched-function filtered, artifact-tuned cleanup lane on SGO-touched small functions, deliberately without the `precompute-propagate` prefix. The artifact-informed scheduler removes the earlier touched-count and module-count cleanup skips, filters individually large touched functions (`> 192` locals or `> 1000` instructions), and uses the pruned list `optimize-instructions -> local-cse -> pick-load-signs -> merge-blocks -> remove-unused-names -> merge-blocks -> coalesce-locals -> reorder-locals -> redundant-set-elimination -> vacuum`. This is intentionally not exact Binaryen default-function scheduler parity; omitted slots were measured as wrapper/raw-skip/candidate-scan cost without current artifact size wins.
  - Acceptance evidence: direct `--simplify-globals-optimizing` replay on `tests/node/dist/starshine-debug-wasi.wasm` at `.tmp/sgo-direct-debug-artifact-nested-pruned` completed and validated after `moon build --target native --release src/cmd`; Starshine stayed smaller (`2,860,269` bytes vs Binaryen `2,861,435`) and pass-local runtime was inside the 2x Binaryen floor (`153.143ms` vs `107.210ms`). The remaining first diff `defined=48 abs=69` is accepted representation drift by user decision: Binaryen preserves one explicit `local.set $0 (i32.const 0)` that Starshine omits by relying on WebAssembly default local zero-initialization. `.tmp/sgo-nested-pruned-trace.txt` reported total SGO pass time `139.507ms`, `detail:sgo:nested-total=86.104ms`, and largest remaining nested wrapper `vacuum=45.538ms`. Standard fuzz `.tmp/pass-fuzz-sgo-nested-pruned-10k` matched all `9975/10000` compared cases with `0` mismatches, `0` validation failures, and `25` Binaryen/tool command failures.
  - Follow-up only if needed: exact Binaryen nested default-function scheduler parity despite runtime cost, optional remaining `vacuum` nested wrapper overhead reduction, or compare-harness normalization for explicit default local initialization. Keep public `optimize` / `shrink` presets unchanged until the late-tail neighborhood is oracle-proven.

#### SG / RG / DIR late-tail scheduling

- No active v0.1.0 SG/RG/DIR preset-order blocker. `optimize` and `shrink` now append the proven `string-gathering -> reorder-globals -> directize` tail, with registry and preset-order tests covering the public schedule. After regenerating `tests/node/dist/starshine-debug-wasi.wasm` with `moon build --target wasm`, the targeted tail artifact replay reached canonical wasm equality and normalized WAT equality. Focused SG edge tests now cover imported-global non-reuse, nested-initializer non-reuse, table-initializer rewriting, typed-element expression rewriting, and module-expression global-index remapping after inserted strings shift defined globals; nullable string global non-reuse remains a local type-representation caveat because Starshine currently represents `stringref` as nullable abstract string. A broadened optional seed lane at `.tmp/pass-fuzz-string-gathering-seed-0x1eed` found one normalized no-string `wasm-smith` mismatch (`case-007907`) where Binaryen removes an unreachable block wrapper under a result loop while Starshine no-ops; classify this as harness/generic unreachable representation drift, not an SG literal-rewrite blocker. The post-bare-stringref decoder replay at `.tmp/pass-fuzz-string-gathering-decode-breadth-0x1eed-10k` reproduced the same class only: 7888/10000 compared, 7887 normalized matches, 0 validation/generator failures, 19 `binaryen-rec-group-zero` command failures, and no new Starshine decode failures. The standard-seed decoder replay at `.tmp/pass-fuzz-string-gathering-decode-breadth-0x5eed-10k` stayed green for compared modules: 6759/10000 compared, 6759 normalized matches, 0 mismatches, 0 validation/generator failures, and 20 Binaryen/tool command failures. The latest decoder-breadth tail replay at `.tmp/self-opt-string-reorder-directize-decode-breadth-20260519` kept canonical wasm and normalized WAT equality green (`471.422ms` Starshine whole-command vs `514.997ms` Binaryen; `67.480ms` Starshine pass-local vs `39.809ms` Binaryen). Decoder breadth now accepts bare `0x64` stringref value types when the explicit non-null-reference form cannot be completed, covering standalone stringref result-type modules that previously failed before SG; binary tests also lock canonical re-encode of bare/explicit nullable stringref as `0x63 0x64` and explicit non-null stringref as `0x64 0x64`. The combined tail remains a performance follow-up candidate, but not a semantic/preset-order blocker.

#### Whole-command wall-time budget

- [WALL]001 - Cross-Pass Runtime Budget And Attribution
  - Deliverables: own whole-command Starshine-vs-Binaryen wall-time measurement outside individual pass parity slices; separate pass-local runtime, harness/tool startup, parse/emit, validation, HOT lift/lower, analysis cache, and artifact representation costs; maintain a prioritized list of cross-cutting runtime fixes without blocking pass correctness signoff on aggregate wall time.
  - Current status: direct pass slices may record timing evidence when available, but unresolved whole-command wall-time gaps should be filed here unless the root cause is clearly inside one pass implementation. A 2026-05-19 full self-optimize attempt showed the current compare harness rejects preset flags (`--optimize`/`--shrink`), and an expanded optimize-preset compare/direct Starshine optimize attempt exceeded 30 minutes while still in `ssa-nomerge` near function `2977`; this is a full-preset runtime/attribution issue, not an SG decoder blocker. A follow-up direct `--ssa-nomerge` compare on the debug artifact timed out after 300s before producing artifacts, and a 120s traced direct replay again stopped inside Func 2977 after `analysis:ssa`, while Binaryen direct `--ssa-nomerge` completed in about 851ms wall / 385.935ms pass time. `--print-func 2977` identifies the body as the large branchy Wast opcode parser (`_M0MP37jtenner9starshine4wast10WastParser26parse__opcode__instruction`) with 394 locals and a 71,463-character raw body line, so the next `[WALL]001` slice should isolate or guard `ssa-nomerge` on that function shape before retrying full preset comparison. Follow-up non-pass slices now cover raw wasm no-op copies in `src/cmd/cmd.mbt`, remove standard-section/stringref whole-payload copies in `src/binary/decode.mbt`, and thread reachable escape-depth summaries through `src/validate/typecheck.mbt` so nested control validation no longer recursively rescans subtrees for merge-reaching branches; the remaining non-pass candidate is encoder size/backpatch or code-section buffering reduction.

## Deferred Until MoonBit Threading Support

- [HOT]002 - Native Parallel Hot-Batch Queue
  - Status: deferred; MoonBit does not currently support the threading model Starshine needs for a safe native worker queue.
  - Resume when: MoonBit native threading is stable enough to run per-function optimizer workers with deterministic, byte-stable output and explicit runtime gating.
  - Deliverables: add a native-only worker queue over eligible defined functions for the hot batch payload (`ssa-nomerge -> dead-code-elimination -> vacuum -> optimize-instructions -> simplify-locals`); keep final output byte-stable and deterministic; gate behind an explicit native-only option.
  - Dependencies: [HOT]001 replay hardening must stay green.

## v0.2.0 Backlog

### INL - Deferred Inliner Breadth

- [INL]005 - Partial Inlining Splitter
  - Status: deferred to v0.2.0; not a v0.1.0 blocker and not the next task after DAE. Starshine intentionally does not implement Binaryen's Pattern A / Pattern B partial splitting today.
  - Resume only if a reduced Pattern A/B fixture demonstrates one of: semantic or validation correctness, a clear pass-local performance win, a downstream size/optimization win that offsets code growth, or a user-facing need for `no-partial-inline` splitter behavior.
  - Deliverables when resumed: implement or explicitly reject/scope out Pattern A and Pattern B splitting, add reduced splitter and helper-cleanup fixtures, define `no-partial-inline` behavior against the splitter, and compare direct `--pass inlining` / `--pass inlining-optimizing` on split-family repros.

- [INL]006 - Residual Name/Annotation Repair
  - Status: deferred to v0.2.0; not a v0.1.0 blocker and not a semantic parity prerequisite for the accepted direct surfaces. Tail-call and multi-result correctness subsets are already covered by focused tests; Starshine intentionally drops function-scoped local/label names after inlining body rewrites instead of preserving stale or collision-prone debug maps.
  - Resume only with a concrete user-facing debug-name requirement, annotation-collision requirement, semantic mismatch, wasm validation failure, or proven pass-local performance/code-size issue.
  - Deliverables when resumed: add focused repair tests first, then either implement deterministic Binaryen-like local/label name reconstruction and broader annotation collision repair or explicitly document the unsupported surface as rejected.

- [TOOL]001 - Self-Optimize Compare Normalization Symmetry
  - Goal: make the exact artifact helper stop reporting harmless raw/debug-only outer-block drift as a pass blocker.
  - Why deferred: the current `[TO]005` residual is not a Starshine optimizer bug and is not needed for v0.1.0.
  - Deliverables: either canonicalize Binaryen through the same strip-debug path before canonical-function comparison while preserving `binaryen.raw.wasm`, or teach the canonical-function fallback to ignore transparent unused-label void block wrappers.
  - Acceptance: the exact-slot artifact command no longer reports `defined=200 abs=217` solely because Binaryen raw `--debug` kept an outer block that symmetric normalization removes.

- [HOT]003 - Node-Package Worker Queue Port
  - Status: deferred behind [HOT]002 and future Node package rebuild work.
  - Deliverables: reuse the native hot-batch queue contract in the shipped Node package with `worker_threads`, one WasmGC module per worker, worker-local heap state, and stable-order serialized merge.
  - Dependencies: [HOT]002 native queue contract, worker-local function serialization hooks, and future Node package rebuild work.
