# Refactoring

Refactor after tests are green.

Guidelines:
- make one structural improvement at a time
- rerun tests after each meaningful change
- remove duplication only when the duplication is real
- prefer simplifying code over introducing new abstraction layers
