- 2026-03-09: After fixing `lift_to_texpr` stack-value arity handling, `scripts/self-optimize.sh` no longer fails during lift. The run now proceeds to optimization passes and fails later at pass `Flatten` with `typed function stack underflow` (`Offending function idx=(Func 495)` in `output.log`). This appears to be a separate optimizer bug that was previously masked by the lift failure.
