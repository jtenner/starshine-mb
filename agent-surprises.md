# Agent Surprises

- 2026-03-06: Full `/home/jtenner/.moon/bin/moon test` is currently red with 15 failing tests outside `merge_blocks.mbt` (not touched in this task), including suites in `passes/unsubtyping.mbt`, `passes/remove_unused_brs.mbt`, `passes/optimize.mbt`, `passes/inlining.mbt`, and `passes/simplify_locals.mbt`.
