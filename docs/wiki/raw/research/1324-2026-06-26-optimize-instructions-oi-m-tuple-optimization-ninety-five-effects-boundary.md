# Optimize Instructions OI-M Tuple Optimization Ninety-Five-Effect Boundary

Date: 2026-06-26

## Question

Does the public `optimize-instructions` plus `tuple-optimization` neighbor still expose Starshine's tuple-scratch reconstruction gap at ninety-five later non-selected effects, and does Binaryen continue to localize the multivalue block through tuple scratch?

## Oracle probe

Probe: `.tmp/oi-m-tuple-optimization-ninety-five-effects-probe.wat`

The fixture is generated by the same shape as `optimize_instructions_tuple_optimization_effect_boundary_fixture(95)`: a public multivalue block returns the selected `local.get $x` followed by ninety-five one-result side-effect calls, then drops every non-selected result.

Command:

```sh
wasm-opt --all-features -S --optimize-instructions --tuple-optimization .tmp/oi-m-tuple-optimization-ninety-five-effects-probe.wat -o .tmp/oi-m-tuple-optimization-ninety-five-effects-probe.out.wat
```

Observed Binaryen output:

- `tuple.make 96` appears once.
- `local.set` appears ninety-five times.

This is successful Binaryen tuple-scratch localization, not a Binaryen/tool failure.

## Starshine slice

Added boundary/status coverage in `src/passes/optimize_instructions_test.mbt` with `optimize-instructions intentionally keeps public multivalue block with ninety-five later effects through tuple-optimization boundary`.

The test intentionally asserts Starshine keeps the public `block`, `drop`, every effect call, and `local.get` spelling, and does not introduce `local.set` traffic. This is not parity implementation; it keeps the mismatch visible until Starshine can reconstruct Binaryen-style tuple/scalar scratch localization safely.

## Validation

- `wasm-opt --all-features -S --optimize-instructions --tuple-optimization .tmp/oi-m-tuple-optimization-ninety-five-effects-probe.wat -o .tmp/oi-m-tuple-optimization-ninety-five-effects-probe.out.wat` passed and produced one `tuple.make 96` plus ninety-five `local.set` occurrences.
- `moon test --target native src/passes/optimize_instructions_test.mbt --filter '*ninety-five later effects through tuple-optimization*'` passed `1/1`.

Full required slice validation is recorded in the commit that cites this note.

## Remaining work

The OI-M tuple/multivalue gap remains open. This slice only extends the public effect-count boundary ladder; it does not implement tuple-scratch localization for selected multi-results, non-selected multi-result siblings, full `simplify-locals`, or dedicated `tuple-optimization` parity.
