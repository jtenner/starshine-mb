# 0890 - code-pushing regular GenValid post-0889 100000 refresh

Date: 2026-06-25

## Question

Refresh the required regular GenValid 100000 lane after the current post-`0884` behavior and post-`0887`/`0888`/`0889` evidence, using the current native Starshine binary.

## Command

```sh
moon build --target native --release src/cmd
bun scripts/pass-fuzz-compare.ts \
  --count 100000 \
  --seed 0x5eed \
  --pass code-pushing \
  --out-dir .tmp/pass-fuzz-code-pushing-regular-100000-20260625-post-0889 \
  --jobs auto \
  --starshine-bin _build/native/release/build/cmd/cmd.exe \
  --max-failures 2000 \
  --keep-going-after-command-failures
```

## Result

Native build reported `moon: no work to do`; the existing `_build/native/release/build/cmd/cmd.exe` was current for the lane.

`.tmp/pass-fuzz-code-pushing-regular-100000-20260625-post-0889`:

- requested/compared: `100000/100000`
- normalized matches: `100000`
- cleanup-normalized matches: `0`
- raw mismatches: `0`
- validation failures: `0`
- generator failures: `0`
- property failures: `0`
- command failures: `0`
- jobs: `16` (`--jobs auto`)
- cache: wasm-smith `0 hits/0 misses`; Binaryen `100000 hits/0 misses`; Binaryen failures `0 hits/0 misses`
- selected profile: `binaryen-oracle-portable: 100000`

## Classification

This is green post-`0889` regular GenValid evidence: all `100000` compared cases normalized, and there were no raw mismatches, validation failures, generator failures, property failures, or command failures. No cleanup-normalized classification was needed for this lane.

## Follow-up

This satisfies the current regular GenValid 100000 final-closeout lane for post-`0884`/post-`0887` evidence. Final `[O4Z-AUDIT-CP]` closeout is still blocked by the current broad named `pass-fuzz-stress` 10000 lane and an explicit source-backed stop condition.
