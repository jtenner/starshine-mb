# 0084 — Code-pushing standalone `Func 1977` writeback frontier

- Date: 2026-04-13
- Area: `code-pushing`
- Status: open frontier note

## Goal

- Recheck the reopened `Func 1977` family with current-source standalone probing
  after closing the standalone `Func 1975` hot-pass frontier in [`0083`](./0083-2026-04-13-code-pushing-nested-result-if-cond-sink.md).
- Determine whether the remaining standalone `Func 1977` drift is still a
  `code-pushing` root-motion gap or a later lower/writeback problem.

## Reduced Standalone Probe

- Reused the autogenerated standalone module at:
  - `.tmp/codex-tmp/standalone-scan-20260413d/func1977.wat`
- Current-source traced replay used:
  - `moon run src/cmd --target native -- --debug-serial-passes --tracing pass --code-pushing --out .tmp/codex-tmp/standalone-scan-20260413d/func1977-traced.wasm .tmp/codex-tmp/standalone-scan-20260413d/func1977.wat`

## Key Finding

The standalone `Func 1977` drift is **not** currently blocked inside
`cp_try_push_to_pushpoint`.

On the standalone reconstruction:

- the candidate root is still the expected alias move:
  - `LocalSet(45)` before `LocalSet(50)` before the later `If`
- `cp_root_pushable_local(...)` returns `Some(45)`
- the ancestor-continuation later-read guard is `false`
- the same-source crossed-condition-set guard is `false`
- the later target `if` neither reads nor writes local `45`
- the later read after the target is still `true`
- a direct whitebox call to `cp_try_push_to_pushpoint(...)` succeeds
- full `hot_pass_run(...)` on the same standalone HOT function also succeeds and
  leaves the root region as:
  - `... LocalSet(51), If, LocalSet(50), If, LocalSet(45), LocalSet(46), ...`

So the hot pass itself still performs the expected move on this standalone case.

## Actual Blocker

The current standalone failure appears one stage later:

- traced `cmd` replay reports:
  - `pass[code-pushing]:done changed=true`
  - then `pass[code-pushing]:skip-invalid-lower func=(Func 8) reason=suspicious-escape-carrier`
- whitebox inspection of the lowered rewritten function reports:
  - `run_hot_pipeline_lowered_func_has_suspicious_escape_carrier(lowered) == true`
  - `run_hot_pipeline_precompute_writeback_validation_error(...) == Some("stack underflow")`

That means this standalone frontier is currently a **reduced lower/writeback
failure**, not another missed `code-pushing` pushpoint admission.

## Why This Matters

- The full debug-artifact contract still expects `Func 1977` to rewrite without
  `skip-invalid-lower`, and the current artifact test still encodes that.
- But the standalone reconstruction shows there is still a sharper reduced
  writeback frontier in the same family.
- This also means that comparing only final standalone `cmd` output against
  Binaryen can be misleading here unless the trace is checked too: the hot pass
  may already be making the move, and the fallback may then restore the original
  lowered function.

## Practical Conclusion

- Do **not** spend the next round reopening `cp_try_push_to_pushpoint` guards for
  this standalone `Func 1977` slice.
- The next useful reduction target is the standalone lowered-function
  `stack underflow` / suspicious-escape-carrier path.
- That work likely belongs around `hot_lower` and the writeback-validation
  boundary, not another `code-pushing` root-motion guard.
