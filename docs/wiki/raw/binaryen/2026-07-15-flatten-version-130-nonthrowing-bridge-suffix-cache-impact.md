# Binaryen v130 `flatten`: nonthrowing bridge cleanup and proof-cache performance

## Scope

This note records the bounded internal-only iteration that:

1. proves whether a resultless synthetic catch-all `Try` body may throw before HOT lowering;
2. removes dead handler scaffolding for nonthrowing bodies, retaining only a void block when the try label is targeted;
3. admits direct `i32.mul` and `i32.and` resultless-call argument roots after unconditional legacy-try `br_table` transfer;
4. replaces repeated suffix use-def construction with one run-wide snapshot and admission-time cache of exact owned nodes plus owner region;
5. truncates an admitted terminal-table suffix without rebuilding the region holder's child span;
6. keeps scalar branch-target rewrite checks on the immutable pre-mutation branch index rather than rebuilding a live-node index after rewriting starts;
7. threads that same exact branch population through multivalue block/if target support, recursive table checks, deferred admission, and mutation-time routing;
8. resolves each admitted `br_table` target's complete typed local vector once and reuses it for every payload lane;
9. makes multivalue legacy-try label support consume the immutable pre-mutation branch population instead of rescanning every live node;
10. replaces full use-def construction in multivalue legacy-try and loop conditional ownership proofs with exact lightweight reachable use counts;
11. replaces exact multivalue region-tail `TupleMake` use-site allocation with the same pre-mutation reachable counts plus the already-known tail root/slot;
12. threads the immutable per-label branch population and reachable counts through the complete inputful-loop multivalue conditional/backedge support and rewrite chain;
13. replaces full use-site allocation for exact tuple-made inputful-loop entries with the frozen reachable counts plus the structurally known entry slots and reversed body-prefix drops;
14. replaces full use-site allocation for exact scalar legacy-try `br_if` false flow with the same frozen counts plus the known branch and immediately adjacent consumer;
15. replaces full tuple branch-payload use-site allocation with the frozen reachable counts plus the structurally known payload slots;
16. replaces the final full node-use/use-site build for generic tuple-made block/if `br_if` flow with one bounded reachable locator cached during admission and consumed only behind the rewrite boundary;
17. caches exact distinct non-tuple multivalue block/if `br_if` false-flow spans during admission and requires that positive or negative result after mutation starts;
18. snapshots scalar `br_if` false-flow parent populations before mutation, re-resolves only those parents after expected prelude-induced slot shifts, and never discovers a post-snapshot parent during rewrite;
19. replaces seven node-sized conditional-flow and chained-replacement arrays with four sparse exact entry vectors allocated only for admitted branches and values;
20. replaces eight node-sized dead-suffix, terminal-table, and scalar-try arrays with three sparse exact entry vectors allocated only for inspected owners;
21. dispatches generic postorder branch routing only for carried plain `br` nodes because payload-bearing `br_if` and `br_table` already route and return in dedicated arms;
22. retains sequenced-root holder/node identity only for nodes with multiple reachable pre-mutation owners, with an explicit snapshot node limit for rewrite-created ids;
23. reuses the exact resolved target-local vector as the payload staging vector when a table's repeated/default labels deduplicate to one unique target, while retaining separate staging and fanout for multiple targets;
24. freezes each inspected inputful-loop support result before mutation and rejects an absent rewrite-time entry;
25. replaces each per-label prior-population scan during branch-index construction with one monotonic last-recorded-node guard;
26. records exact live loop, legacy-try, and payload-bearing branch admission rosters in that same immutable pre-mutation node scan, eliminating three later whole-live-node scans;
27. records exact typed-catch-payload and exceptional-transfer repair prerequisites in that same scan and reuses them in rewrite state instead of running a separate whole-live-node EH classifier;
28. records the complete Flat IR violation report in the same scan, freezes it in rewrite state, and makes `flatten_run` consume it while keeping standalone classification on shared exact helpers;
29. keeps sparse scalar legacy-try proof entries sorted by exact pre-mutation try id and replaces repeated linear lookup with binary search;
30. keeps sparse dead-suffix and terminal-table proof entries sorted by exact table node id and replaces repeated linear lookup with binary search while retaining first-proof authority;
31. keeps sparse inputful-loop support entries sorted by exact pre-mutation loop id and replaces repeated linear lookup with binary search;
32. keeps sparse scalar `br_if` flow-site and same-state replacement entries sorted by exact pre-mutation branch/value id and replaces repeated linear lookup while preserving replacement updates;
33. keeps sparse tuple-made and distinct non-tuple multivalue `br_if` flow entries sorted by exact pre-mutation branch id and removes the final linear conditional-flow cache lookup;
34. replaces repeated growing-vector `br_table` target deduplication with one label-sized mark set while preserving first explicit-target order and appending a previously unseen default target last;
35. keeps sparse terminal payload roots sorted by exact node id and replaces recursive traversal plus scalar-block-repair linear membership scans with binary lookup;
36. keeps sparse shared-root holder/node pairs in lexicographic order and replaces terminal-placeholder linear membership with binary lookup;
37. marks exact multivalue `br_if` payload ids once for source-ordered duplicate rejection and constant-time payload-root exclusion during false-flow discovery;
38. maps each distinct non-tuple multivalue `br_if` payload node id to its exact source slot once, replacing the reachable child-edge-to-every-payload scan with direct indexed count/parent updates;
39. marks tuple-made false-flow child slots once per exact non-branch parent, replacing growing-vector duplicate scans while retaining ordered slots for the final contiguous-span proof;
40. admits an independently scalar multivalue `br_if` false-flow lane as either child of one immediate directly dropped same-typed binary when the opposite child is Flat-IR-simple;
41. admits the same exact reversed binary-child rule for one exclusively owned repeated `TupleMake`, preserving complete branch-slot plus consumer ownership;
42. admits one exact one-use supported rich right operand when an independently scalar restored lane is the left child of the immediate false-path binary;
43. admits the same rich-right rule for one exclusively owned repeated `TupleMake`, with the shared consumer proof also covering the exact inputful-loop family;
44. admits one exact independently scalar legacy-try rich-left/payload-right binary when the rich left is evaluated before the complete simple payload vector, pairs with lane zero after higher lanes are consumed, and is materialized before payload staging and `br_if`;
45. admits the same exact pre-branch rich-left rule for one exclusively owned repeated `TupleMake` whose ordered components are all simple;
46. admits the independently scalar inputful-loop counterpart by freezing the exact branch root plus rich-left/binary identities and staging that left before the complete simple payload vector and branch;
47. admits the same inputful-loop rule for one exclusively owned repeated `TupleMake`, preserving ordered components, complete branch-plus-consumer ownership, and deletion only after exact replacement;
48. admits at most one supported rich payload origin alongside that independently scalar inputful-loop rich left while preserving left-before-vector staging;
49. admits the same bounded rich-payload rule for one exclusively owned repeated `TupleMake`, inserting the left store before child-generated payload preludes and retaining complete tuple deletion proof;
50. admits at most one supported rich payload origin alongside the pre-branch rich left for independently scalar legacy-try flow, inserting the left store before child-generated payload preludes;
51. admits the same bounded legacy-try rule for one exclusively owned repeated `TupleMake`, retaining complete branch-plus-consumer ownership and deletion proof;
52. removes the temporary rich-payload count cap for exact independently scalar legacy-try and inputful-loop rich-left vectors while retaining individual supported-origin and branch-plus-consumer ownership proof;
53. removes the same cap for one exclusively owned repeated `TupleMake` in both control families while retaining complete branch-slot/consumer coverage and delayed shell deletion;
54. adds one atomic HOT mutation that deletes an exact detached control subtree together with all labels it owns after complete rooted/child/shared/cyclic/external-target preflight;
55. admits one exact void `block { drop(const) }` suffix after an unconditional scalar legacy-try `br_table`, using the atomic subtree deletion only after complete node-use, label-use, owner, and current-structure proof;
56. admits one exact void `if` suffix with a constant condition, complete direct `drop(const)` then/else arms, exclusive region holders and descendants, and no users of the if or region labels;
57. admits one exact zero-input/no-backedge void `loop { drop(const) }` suffix with zero branch arity, exclusive descendants, and no loop-label users;
58. generalizes detached control deletion to an exact forest, rejecting empty/duplicate/overlapping roots, cross-tree sharing, cycles, outside owners, incomplete label ownership, and outside branch/delegate/try-table-catch users before one union tombstone;
59. admits any positive control-only suffix vector whose roots independently satisfy the exact block, if, or loop proof;
60. generalizes atomic detached-forest deletion to ordinary or control roots while retaining complete descendant, control-label, outside-owner, and outside-target preflight and keeping the control-only APIs as wrappers;
61. admits positive scalar/control-mixed suffix vectors only when every root passes an existing exact recognizer and the vector contains both root kinds, then detaches and deletes the complete forest once;
62. adds a whole-function failure-atomic scalar catch-payload repair plan that requires one ordered marker/lane, one exact first-descendant nested use, exclusive ancestry, and no loop, nested try/catch path, sharing, outside use, catch-all marker, or second lane;
63. admits that exact source-backed `try_catch_pop_fixup` family in internal flatten, applies entry-local capture before ordinary rewrites, and rebuilds every immutable proof snapshot from the repaired HOT;
64. generalizes the whole-function transaction to one exact ordered two-lane same-tag family under an exclusive direct block chain, allocating lane locals in source order, capturing the handler stack in reverse lane order, and rejecting a partial lane without mutation;
65. admits that exact two-lane plan in internal flatten and lowers the pair through an existing result-only multivalue handler block type;
66. supersedes the temporary two-lane ceiling by admitting any positive ordered same-tag payload vector under the same exclusive direct block-chain and exact lane-use proof, with lowering through an existing result-only handler block type of matching arity;
67. follows Binaryen v130 `getFirstPop(...)` through an `if` condition for the scalar first-descendant family while continuing to reject either selected arm, loops, nested tries, repeated uses, sharing, and outside ownership;
68. removes the one-rethrow lowering ceiling so any positive admitted depth-zero rethrow population under one active catch shares one captured exception-reference local;
69. extends exact rethrow ancestry through at most one resultless, untargeted `if` arm and extends lowering discovery through then/else regions while still excluding conditions, loops, and nested try/catch regions;
70. supersedes that temporary single-if ceiling by admitting any strict direct chain of resultless untargeted `if` arms and blocks while retaining exact owning-catch ancestry and the same lowering discovery exclusions;
71. admits a resultless outer-target delegate whose sole catch representation is a strict direct chain of resultless single-root unused-label blocks ending in the delegate, and makes lowering recognize the same exact chain before transparent propagation;
72. admits the delegated try through an arbitrary strict direct outer chain of resultless untargeted single-root blocks and if arms to the exact active target while preserving those surrounding controls during lowering;
73. removes the ordered catch-lane unary-only ceiling so every lane may follow Binaryen v130's exact first-child walk through a non-control expression with additional later children, while retaining ordered markers, exact ownership, and all loop/try/nested-control exclusions;
74. admits the first ordered post-marker catch-region roots as independently owned lanes, retaining the common direct-block-chain representation and exact first-child/two-use proof while preserving later catch roots;
75. admits one direct depth-zero rethrow as the terminal arm of a defaultable scalar value `if` whose unused label, direct active-catch ownership, and opposite matching Flat-IR-simple value are proven before scalar-if and scalar-try result routing;
76. replaces the lowering-only stack of captured exception locals with one optional slot per active legacy catch, discovers which enclosing catch is targeted through nested try bodies/catches, and lowers `Rethrow(depth)` through the exact captured slot;
77. admits any positive nonzero rethrow depth only through an exact direct chain of markerless resultless catch-all try owners, preserving the depth immediate through flatten and validating depth-two lowering through one outer `catch_all_ref` capture plus `throw_ref`;
78. supersedes the direct-root requirement for strict resultless unused-label single-root block ancestry between every markerless resultless catch-all owner;
79. extends the same nonzero-depth ownership proof through arbitrary strict mixes of those blocks and resultless unused-label selected `if` arms, while rejecting shared, multi-root, targeted, value-carrying, loop, typed, try-body, and mixed exceptional populations;
80. supersedes the blanket targeted-if ancestry blocker for one exact opposite-arm payloadless plain exit: the targeted resultless if has one root per arm, exactly one selected arm owns the current rethrow/nested try, and the sole opposite root is also the label's only indexed user and a direct `br` to that label;
81. extends that exact opposite-arm exit proof to a payloadless `br_if` whose sole condition is an already-simple scalar `i32`, preserving the condition, target, rethrow depth, and target-catch lowering slot;
82. admits one exact one-use supported rich scalar `i32` condition on that payloadless opposite-arm `br_if`, allowing ordinary flattening to materialize the condition immediately before the branch inside the same arm while preserving target, selected-arm ownership, rethrow depth, and target-catch slot;
83. extends ordered same-tag independent direct-root catch repair so unrelated roots may occur before or between lane roots, while lane selection remains forward source ordered and every chosen lane retains exact entry-plus-old-position and first-child ownership;
84. admits one exact resultless unused-label single-root block around the sole opposite-arm payloadless targeted-if exit, retaining the existing one-user, one-root-per-arm, selected-owner, condition, rethrow-depth, and target-catch-slot proof;
85. admits one exact scalar typed catch with one repairable leading payload marker and one direct depth-zero rethrow, lowering it through `catch_ref`, an existing payload-plus-exnref handler result type, exnref-first capture order, and `throw_ref`;
86. supersedes that scalar payload ceiling for any positive same-tag scalar payload vector with exactly one direct `Rethrow(0)`, lowering through an existing `[lane0, ..., laneN, exnref]` handler type while preserving source-order locals and reverse handler-stack capture;
87. supersedes the one-block targeted-exit ceiling with any positive strict chain of resultless unused-label single-root blocks, retaining the sole if-label user and innermost rich-condition prelude placement;
88. supersedes the direct-only typed-rethrow ancestry ceiling by admitting the sole `Rethrow(0)` through any positive strict chain of resultless unused-label single-root blocks while retaining the payload-vector handler and whole-function ownership transaction;
89. admits a resultless outer-target delegate whose sole catch representation follows any positive exact constant-selected resultless unused-label if chain with one root per arm and a childless `nop` in every unselected arm, and makes HOT lowering recognize the same representation before transparent propagation.

The public registry, dispatcher, CLI execution path, preset scheduler, compare allowlist, and flatten API snapshots remain unchanged. Across the iterations recorded here, the generic IR `.mbti` additions are `hot_region_truncate_suffix(...)`, `hot_delete_detached_forest(...)`, `hot_delete_detached_control_forest(...)`, `hot_delete_detached_control_subtree(...)`, `hot_label_is_live(...)`, `hot_scalar_catch_payloads_repairable(...)`, `hot_repair_scalar_catch_payloads(...)`, `hot_ordered_catch_payloads_repairable(...)`, and `hot_repair_ordered_catch_payloads(...)`; these are generic IR surfaces, not public flatten wiring. `flatten` is still public-removed.

## Pinned source and commits

- Iteration start: `4c8a02ebc`.
- Code commit 1: `d20ebd21c` (`perf: elide nonthrowing flatten catch bridges`).
- Code commit 2: `e9cb90b84` (`perf: share flatten suffix use-def proofs`).
- Code slice 3: run-wide suffix cache plus direct `i32.and` admission, committed with this note.
- Later label/shift iteration: `de1bc430f`, `64a86a381`, and docs commit `e14935de2`.
- Current proof-cache/routing iteration: `23f779aa8` (`perf: cache flatten terminal table proofs`) and `32da5c798` (`perf: avoid duplicate flatten terminal routing`).
- Current lightweight-analysis/batch-mutation iteration: `b610394b4` (`perf: use lightweight flatten ownership counts`) and `56908b781` (`perf: batch flatten detached suffix deletion`).
- Current branch-index/in-place-tail iteration: `c6181e26d` (`perf: index flatten branch targets once`) and `0a415161f` (`perf: replace flatten value tails in place`).
- Current suffix-truncation/proof-boundary iteration: `13fe7b744` (`perf: truncate flatten dead suffixes in place`) and `9acdac744` (`fix: keep flatten scalar routing on pre-mutation proofs`).
- Current multivalue-index/table-vector iteration: `9aa7499e9` (`perf: index flatten multivalue target branches`) and `710cdc910` (`perf: resolve flatten table target vectors once`).
- Current legacy-try branch/ownership iteration: `e39faf79e` (`perf: index flatten multivalue try branches`) and `e64428dc1` (`perf: reuse flatten conditional ownership counts`).
- Current region-tail/loop-branch iteration: `3d0acb44e` (`perf: reuse flatten region-tail ownership counts`) and `19fa4eda8` (`perf: index flatten multivalue loop branches`).
- Current loop-entry/scalar-try ownership iteration: `3a88b5bd6` (`perf: reuse flatten loop-entry ownership counts`) and `5c0235d71` (`perf: reuse flatten scalar try ownership counts`).
- Current tuple branch/conditional-flow iteration: `24ca31723` (`perf: reuse flatten tuple branch ownership counts`) and `32690a37d` (`perf: cache flatten tuple conditional flow sites`).
- Distinct/scalar conditional-site iteration: `ae096a883` (`perf: cache flatten multivalue conditional flow sites`) and `b87464d25` (`fix: freeze flatten scalar conditional flow sites`).
- Current sparse proof-cache iteration: `e165fde1c` (`perf: sparsify flatten conditional proof caches`) and `476848f9d` (`perf: sparsify flatten terminal proof caches`).
- Current postorder-routing/shared-root iteration: `7801166ac` (`perf: dispatch flatten branch routing by node kind`) and `18101a947` (`perf: retain only shared flatten sequence roots`).
- Current single-target-table/inputful-loop iteration: `81cfb9619` (`perf: reuse single flatten table target locals`) and `dda2bdfe3` (`perf: freeze flatten inputful loop support`).
- Current branch-append/admission-roster iteration: `6a74918d6` (`perf: append flatten branch users in constant time`) and `1acb9bc14` (`perf: share flatten admission node rosters`).
- Current EH/flatness scan-sharing iteration: `7706110c1` (`perf: share flatten EH prerequisite discovery`) and `2c5a54ac3` (`perf: share flatten flatness discovery`).
- Sparse proof-lookup iteration: `c420a9950` (`perf: binary-search flatten scalar try proofs`) and `9b5c4170a` (`perf: binary-search flatten terminal proofs`).
- Loop/scalar-flow proof-lookup iteration: `e32819f5b` (`perf: binary-search flatten inputful loop proofs`) and `fc5c89bff` (`perf: binary-search flatten scalar flow proofs`).
- Current tuple/distinct-flow proof-lookup iteration: `80e6a652b` (`perf: binary-search flatten tuple flow proofs`) and `efb8fdfa2` (`perf: binary-search flatten multivalue flow proofs`).
- Current table-target/terminal-payload iteration: `bdad9efaf` (`perf: mark flatten table targets once`) and `902848fca` (`perf: binary-search flatten terminal payload roots`).
- Allocation/index iteration: `a08af563b` (`perf: reuse flatten indexes and rewrite scratch`), with maintained skipped sequenced-root measurement surface `02f402a19` and documentation corrections in `59f05ff97` / `7a11ab789`.
- Current sequenced-root/multivalue-payload iteration: `4a03de7f3` (`perf: binary-search flatten sequenced roots`) and `aa295d38b` (`perf: mark flatten multivalue flow payloads`).
- Current multivalue flow-index iteration: `f1dc57565` (`perf: index flatten multivalue flow payload slots`) and `24b909b2d` (`perf: mark flatten tuple flow slots`).
- Current behavior-first reversed-binary iteration: `2ae0a6adb` (`feat: route reversed flatten multivalue flow binaries`) and `d64535310` (`feat: route reversed flatten tuple flow binaries`).
- Current behavior-first rich-right iteration: `5c4a664dd` (`feat: route rich-right flatten multivalue flow binaries`) and `a4055b7a9` (`feat: route rich-right flatten tuple flow binaries`).
- Current behavior-first rich-left iteration: `e5c2a91ea` (`feat: route rich-left flatten multivalue flow binaries`) and `d0a53acf9` (`feat: route rich-left flatten tuple flow binaries`).
- Current inputful-loop rich-left iteration: `843614438` (`feat: route rich-left flatten loop flow binaries`) and `35ac3740a` (`feat: route rich-left flatten tuple loop binaries`).
- Current inputful-loop rich-payload iteration: `3cb5577ad` (`feat: route rich-payload flatten scalar loop flow`) and `c5a0a738a` (`feat: route rich-payload flatten tuple loop flow`).
- Current legacy-try rich-payload iteration: `61055698d` (`feat: route rich-payload flatten scalar try flow`) and `d9aa4cd94` (`feat: route rich-payload flatten tuple try flow`).
- Current multiple-rich-payload iteration: `6e4229bae` (`feat: route multiple rich scalar flatten payloads`) and `f223f14d6` (`feat: route multiple rich tuple flatten payloads`).
- Current structured-subtree iteration: `62c330a12` (`feat: delete detached structured HOT subtrees`) and `f7829507b` (`feat: delete owned structured flatten suffixes`).
- Current structured-if/loop iteration: `51d080e09` (`feat: delete owned conditional flatten suffixes`) and `cbb1a2395` (`feat: delete owned loop flatten suffixes`).
- Current structured-forest iteration: `8b69a8e4c` (`feat: delete detached structured HOT forests`) and `f524b8bcb` (`feat: delete owned structured flatten forests`).
- Current mixed-forest iteration: `04cc07657` (`feat: delete detached mixed HOT forests`) and `ca159e6d0` (`feat: delete mixed owned flatten forests`).
- Scalar catch-repair iteration: `55e508e83` (`feat: capture scalar catch payloads atomically`) and `864d32c06` (`feat: repair scalar typed catches in flatten`).
- Current ordered two-lane catch-repair iteration: `43ea95972` (`feat: capture ordered catch payload lanes atomically`) and `49079c0dc` (`feat: repair ordered two-lane catches in flatten`).
- Direct exceptional-transfer iteration: `d76a91b3f` (`feat: preserve direct catch-all rethrows in flatten`) and `7bfb10372` (`feat: preserve direct legacy delegates in flatten`).
- Ordered catch breadth iteration: `82c4c260c` (`feat: repair arbitrary ordered catch payload lanes`) and `95f71db1e` (`feat: repair catch payloads in if conditions`).
- Multiple/nested-if rethrow iteration: `3c819922c` (`feat: preserve multiple direct catch rethrows`) and `41533e603` (`feat: preserve catch rethrows through nested if arms`).
- Strict exceptional-ancestry iteration: `9c237165d` (`feat: preserve rethrows through nested if chains`) and `88197c97e` (`feat: preserve delegates through catch block chains`).
- Outer-delegate/first-child catch-lane iteration: `0800efc79` (`feat: preserve delegates through strict outer control ancestry`) and `57013d100` (`feat: repair ordered catch payloads through first-child expressions`).
- Independent-catch-root/value-if-rethrow iteration: `fb9d071e8` (`feat: repair independent direct catch payload lanes`) and `52fc64b49` (`feat: preserve rethrows through scalar value ifs`).
- Nested-rethrow-depth iteration: `1ac52d9fa` (`feat: lower nested legacy rethrow depths`) and `23f9ba164` (`feat: preserve rethrows through direct nested catches`).
- Nested-rethrow strict-wrapper iteration: `70280e159` (`feat: preserve nested rethrows through catch block wrappers`) and `1fc7c6077` (`feat: preserve nested rethrows through catch if wrappers`).
- Targeted catch-if exit iteration: `c90bed031` (`feat: preserve nested rethrows through plain-targeted catch ifs`) and `8529deb42` (`feat: preserve nested rethrows through conditional-targeted catch ifs`).
- Rich-condition/interleaved-catch-root iteration: `61fb9919b` (`feat: preserve targeted catch ifs with rich conditions`) and `1fb8f64ec` (`feat: repair interleaved direct catch payload lanes`).
- Block-wrapped-target/scalar-typed-rethrow iteration: `404252e63` (`feat: preserve block-wrapped targeted catch if exits`) and `a24539c99` (`feat: preserve scalar typed catch rethrows`).
- Typed-vector/strict-targeted-block-chain iteration: `740dfa2b5` (`feat: preserve two-lane typed catch rethrows`) and `374040a26` (`feat: preserve targeted catch exits through block chains`).
- Typed-rethrow-block-chain/selected-catch-if-delegate iteration: `b7b85a8bf` (`feat: preserve typed catch rethrows through block chains`) and `3595d6563` (`feat: preserve delegates through selected catch if chains`).
- Captured owner: `.tmp/binaryen-v130/Flatten.cpp`.
- Owner SHA-256: `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`.
- Captured nested-pop helper: `.tmp/binaryen-v130/eh-utils.cpp`, SHA-256 `40ebd508dd295c98fbde151c8c3e4e5fe559249c95a00aea19e455e3571025f5`.
- Captured helper declaration: `.tmp/binaryen-v130/eh-utils.h`, SHA-256 `b06137d77e663797927f4c46904a6844ac135a1b37061aaee6d89dc027145271`.
- Captured legacy `Try` representation: `.tmp/binaryen-v130/wasm.h`, SHA-256 `a925165ad042d697ba0b795bba17382f03781119946b8b9a530b128cab6970dc`.
- Oracle: `/mise/http-tarballs/78d28b82d329cecc96d14b1872ee2a890d09be4705c634ffb04ebf8c592c1e48/binaryen-version_130/bin/wasm-opt`.
- Direct probe form: `wasm-opt <probe>.wat --all-features --flatten -S`.

Binaryen v130 direct flatten retains the legacy try and unreachable suffix in Flat-IR order. Its matched `--vacuum --dce` neighborhood removes a catch whose body cannot throw. Starshine now performs that nonthrowing proof during lowering, while preserving the bridge for calls or other possibly throwing bodies.

## Red-first tests

Exactly one behavioral test was added for each code slice:

- `hot lower elides a nonthrowing synthetic catch-all bridge` failed on the nested `block` / `try_table` bridge, then passed with direct body emission and module validation; the prior bridge test was changed to contain a possibly throwing call and still proves the bridge path;
- `flatten removes an exclusively owned direct multiply call suffix` failed unchanged at `238/239`, then passed after direct `i32.mul` root admission and per-attempt use-def reuse;
- `flatten removes an exclusively owned direct and call suffix` failed unchanged at `239/240`, then passed after direct `i32.and` admission and run-wide cached ownership.

The whitebox outside-roster boundary moved from newly admitted `i32.mul`, then `i32.and`, through the shift family and now rests at still-gated `i32.rotl`; it remains a fail-closed boundary rather than a second positive slice.

The terminal-proof/routing iteration added exactly one red-first test per commit:

- `flatten terminal table proofs require an exact pre-mutation cache` failed at `142/143` because a suffix-only cache still authorized the broader terminal-table proof after mutation started; exact table id, label, payload arity, mixed-target policy, and support are now cached together, and the test passes at `143/143`;
- `flatten removes an exclusively owned direct unsigned-shift call suffix` failed unchanged at `244/245`, then passed after direct `i32.shr_u` admission under the existing recursive distinct one-use proof. The pinned v130 probe retains `i32.shr_u`, `call $dead`, and unreachable Flat-IR debris.

The current lightweight-analysis/batch-mutation iteration also added exactly one red-first invariant per code commit:

- `flatten lightweight use counts match exact pre-mutation ownership` first failed to compile because the lightweight builder did not exist. The first implementation exposed detached-but-live parent references and failed two existing suffix-sequence tests; changing the builder to traverse only the reachable root graph made its counts agree with full use-def for roots, structured regions, repeated child uses, and every live node in the fixture. Private flatten then passed `144/144`;
- `batched detached node deletion tombstones the whole set with one revision` first failed because `hot_delete_detached_nodes` did not exist. The new exact distinct-set mutation preflights every id before mutation, tombstones the whole detached set, preserves survivor liveness and no-immediate-reuse behavior, and bumps the HOT revision once. Focused HOT mutation passes `10/10`.

The branch-index/in-place-tail follow-up again added exactly one red-first invariant per code commit:

- `flatten label branch cache records each exact targeting node once` failed because `flatten_build_label_branch_nodes` was unbound. The implemented immutable index records each live branch-like node once per targeted label, deduplicates repeated/default `br_table` labels, derives `label_used` from the same scan, and lets scalar target admission inspect only exact target users. Private flatten moved to `145/145`;
- `flatten replaces one region root without rebuilding its siblings` failed because `flatten_set_region_root` was unbound. The helper uses public HOT root replacement for one-for-one value-tail writes, preserves sibling identity and the old value as the new `local.set` child, invalidates pass analyses, and advances the HOT revision exactly once. Scalar and multivalue block, if, loop, and legacy-try result tails now avoid full region child-array reconstruction. Private flatten moved to `146/146`.

The suffix-truncation/proof-boundary follow-up added exactly one red-first invariant per code commit:

- `body region suffix truncation preserves the existing child span` failed because `hot_region_truncate_suffix` was unbound. The new HOT mutation detaches an ordered suffix by reducing the existing holder span, preserves the prefix and child-storage count, returns detached roots in order, and advances the revision once. Flatten uses it before the existing exact distinct-node tombstone. Focused region-edit tests pass `4/4`;
- `flatten rewrite scalar target proof uses the pre-mutation branch index` failed because `flatten_rewrite_target_can_route_scalar_branches` was unbound. The rewrite-only helper consumes `FlattenRewriteState.label_branch_nodes` for scalar `br`, `br_if`, and `br_table` routing, so post-snapshot live nodes cannot widen mutation-time admission. Private flatten moved to `147/147`.

The multivalue-index/table-vector follow-up also added exactly one red-first invariant per code commit:

- `flatten rewrite multivalue target proof uses the pre-mutation branch index` failed because the support function had no `state` parameter and `flatten_rewrite_multivalue_branch_target_is_supported` was unbound. The implemented path uses the immutable label branch population during recursive target support, deferred rich-payload admission, and rewrite-time `br`, `br_if`, and `br_table` checks. A malformed post-snapshot branch can make the uncached proof fail but cannot widen the rewrite proof. Private flatten moved to `148/148`;
- `flatten resolves each br_table target local vector once` failed because `flatten_rewrite_br_table_target_locals` was unbound. The helper preflights every target kind and existing vector type without mutation, requires the rewrite boundary, resolves each unique target vector once, and reuses the exact vectors on repeated calls without adding locals. Private flatten moved to `149/149`.

The legacy-try branch/ownership follow-up added exactly one red-first invariant per code commit:

- `flatten rewrite multivalue try proof uses the pre-mutation branch index` failed at `149/150` because multivalue legacy-try support still scanned every live node after the snapshot. It now consumes `state.label_branch_nodes[label]` during admission and rewrite; an uncached malformed post-snapshot branch fails without widening the mutation-time branch population;
- `flatten rewrite multivalue try flow uses pre-mutation ownership counts` first failed to compile because the exact flow proof had no `state` parameter and the rewrite-only helper was unbound. Legacy-try and loop `br_if` flow proofs now use `HotNodeUseCounts`, and the rewrite helper requires the mutation boundary before consuming the pre-mutation ownership population. Private flatten moved to `151/151`.

The region-tail/loop-branch follow-up added exactly one red-first invariant per code commit:

- `flatten rewrite region tail tuple proof uses pre-mutation ownership counts` first failed to compile because the tuple-tail helper had no `state` parameter and `flatten_rewrite_multivalue_region_tail_tuple_make_values` was unbound. A post-snapshot extra tuple use now fails the uncached proof while rewrite consumes the frozen one-use population; private flatten moved to `152/152`;
- `flatten rewrite multivalue loop proof uses the pre-mutation branch index` first failed to compile because the loop flow helper had no `state` parameter and `flatten_rewrite_multivalue_loop_br_if_flows_are_exact` was unbound. A malformed post-snapshot loop branch now fails uncached support without widening mutation-time loop proof; private flatten moved to `153/153`.

The loop-entry/scalar-try ownership follow-up also added exactly one red-first invariant per code commit:

- `flatten rewrite tuple loop entry proof uses pre-mutation ownership counts` first failed to compile because the tuple-entry helper had no `state` parameter and `flatten_rewrite_inputful_loop_tuple_entry_values` was unbound. The frozen proof requires exactly the structurally known entry-slot and reversed-drop uses; a post-snapshot extra tuple use fails uncached without widening rewrite-time ownership. Private flatten moved to `154/154`;
- `flatten rewrite scalar try flow uses pre-mutation ownership counts` first failed to compile because the scalar try-flow helper had no `state` parameter and `flatten_rewrite_scalar_try_br_if_flow_is_exact` was unbound. The frozen proof retains exact two-use payload ownership and one-use rich-consumer ownership; a post-snapshot extra payload use fails uncached without widening rewrite-time proof. Private flatten moved to `155/155`.

The tuple branch/conditional-flow follow-up added exactly one red-first invariant per code commit:

- `flatten rewrite tuple branch payload proof uses pre-mutation ownership counts` first failed to compile because the helper had no `state` parameter and `flatten_rewrite_multivalue_tuple_branch_payload_values` was unbound. Every payload slot is structurally checked to contain the same tuple, so exact reachable count `payload_count` proves complete ownership; a post-snapshot extra use fails uncached without widening rewrite-time extraction. Private flatten moved to `156/156`;
- `flatten rewrite tuple br_if flow uses pre-mutation exact site cache` first failed to compile because the tuple-flow helper had no `state` parameter and `flatten_rewrite_multivalue_tuple_br_if_flow` was unbound. Admission now locates the exact reachable false-flow parent/start once, caches positive or negative proof by branch id, and rewrite refuses an uncached result after mutation starts. A post-snapshot extra tuple use fails uncached while the exact cached site remains available. Private flatten moved to `157/157`.

The distinct/scalar conditional-site follow-up added exactly one red-first invariant per code commit:

- `flatten rewrite multivalue br_if flow uses pre-mutation exact site cache` first failed because the helper had no `state` parameter and the rewrite-only helper was unbound. Admission now caches the exact distinct payload parent/start or a negative result; a post-snapshot extra payload use fails uncached without widening rewrite-time flow. Private flatten moved to `158/158`;
- `flatten rewrite scalar br_if flow uses pre-mutation exact site cache` first failed because the scalar site and cached replacement helpers were unbound. Admission snapshots exact live parent populations. Rewrite may re-resolve shifted slots only within those parents, chained branches may consume only a replacement created by the same state, and a use added under a new post-snapshot parent remains untouched. Private flatten moved to `159/159`.

The sparse proof-cache follow-up also added exactly one red-first invariant per code commit:

- `flatten conditional proof caches allocate only admitted entries` first failed because the sparse conditional entry fields did not exist. A function with 256 unrelated nodes now starts with zero tuple, multivalue, scalar, and chained-replacement entries; scalar admission adds exactly one branch entry and rewrite adds exactly one same-state replacement entry. Private flatten moved to `160/160`;
- `flatten terminal proof caches allocate only inspected owners` first failed because sparse suffix, terminal-table, and scalar-try entry fields did not exist. A function with 256 unrelated nodes starts with zero entries and adds exactly one entry for each inspected suffix/table/try owner. Private flatten moved to `161/161`.

The postorder-routing/shared-root follow-up added exactly one red-first invariant per code commit:

- `flatten postorder branch routing admits only carried br nodes` first failed because `flatten_node_needs_postorder_branch_routing` did not exist. The implemented classifier admits only a plain `br` with one or more carried values; payloadless `br`, payload-bearing `br_if`, payload-bearing `br_table`, and ordinary nodes remain outside the generic late router. Private flatten moved to `162/162`;
- `flatten sequenced roots track only pre-mutation shared nodes` first failed because `flatten_root_needs_sequence_tracking` did not exist. The first implementation then exposed five focused failures by querying rewrite-created ids against the frozen count array. The corrected proof retains only roots with pre-mutation count greater than one and rejects ids at or beyond the snapshot node limit. The invariant keeps one shared branch holder/node pair, excludes 256 unique roots and one post-snapshot node, and still leaves one placeholder without duplicating the earlier branch. Private flatten moved to `163/163`.

The single-target-table/inputful-loop follow-up also added exactly one red-first invariant per code commit:

- `flatten single-target table stages in the exact target vector` first failed because `flatten_br_table_staging_locals` did not exist. The implemented helper returns the resolved target vector unchanged for one unique target, allocates no local, and still creates a distinct typed staging vector for multiple targets. Existing behavior expectations then moved from the Binaryen-style staging-plus-copy shape to the smaller direct target writes across scalar, tuple, loop, legacy-try, suffix, and nested-placeholder families. Private flatten moved to `164/164`;
- `flatten inputful loop support freezes the exact admission result` first failed because `FlattenRewriteState` had no inputful-loop support entries. Admission now stores the exact supported/unsupported result per inspected loop; a post-snapshot malformed payloadless backedge makes the uncached support fail, while rewrite consumes the pre-mutation positive result. An absent entry after mutation starts fails closed. Private flatten moved to `165/165`.

The branch-append/admission-roster follow-up again added exactly one red-first invariant per code commit:

- `flatten branch index deduplicates only the current node target` first failed because `flatten_record_label_branch_node` did not exist. The monotonic live-node scan now suppresses only repeated labels emitted by the current node and appends each later branch user without scanning the prior per-label population. Private flatten moved to `166/166`;
- `flatten admission node index shares the exact live-node scan` first failed because `flatten_build_pre_mutation_node_index` did not exist. The same scan now records exact loop, legacy-try, and payload-bearing branch rosters while excluding 256 unrelated roots; admission consumes those frozen populations instead of rescanning all live nodes. Private flatten moved to `167/167`.

The EH/flatness scan-sharing follow-up also added exactly one red-first invariant per code commit:

- `flatten pre-mutation node index shares EH prerequisite classification` first failed because `FlattenPreMutationNodeIndex` had no `eh_repair_requirement`. The indexed scan now records `Catch`/`CatchAll` payload tracking and `Rethrow`/`Delegate` exceptional-transfer requirements; `Delegate` still records its label target, and rewrite state consumes the exact indexed result. Private flatten moved to `168/168`;
- `flatten pre-mutation node index shares flatness classification` first failed because neither the node index nor rewrite state had a `flatness_report`. Exact per-live-node and function-tail helpers now serve both standalone classification and indexed run admission. The invariant covers rich operands, value-carrying control, hard-unsupported control, and concrete body flow. Private flatten moved to `169/169`.

The sparse proof-lookup follow-up again added exactly one red-first invariant per code commit:

- `flatten scalar try proof cache keeps sparse entries sorted` queried three scalar tries in descending/mixed order and failed at `169/170` because append order produced `[16, 4, 10]` instead of `[4, 10, 16]`. Sorted insertion plus binary lookup preserves sparse allocation and exact owner identity; private flatten moved to `170/170`;
- `flatten terminal proof caches keep sparse entries sorted` queried three table owners out of order and failed at `170/171` because both terminal populations produced `[33, 7, 20]` instead of `[7, 20, 33]`. Dead-suffix and terminal-table entries now remain sorted by exact table id, retain the first proof for that owner, and preserve every region/label/arity/mixed-target/ownership discriminator; private flatten moved to `171/171`.

The loop/scalar-flow proof-lookup follow-up also added exactly one red-first invariant per code commit:

- `flatten inputful loop proof cache keeps sparse entries sorted` queried three supported loops out of order and failed at `171/172` because append order produced `[11, 1, 6]` instead of `[1, 6, 11]`. Inputful-loop support entries now remain sparse, sorted by exact pre-mutation loop id, and binary-searchable while the first support result and missing-entry rewrite rejection remain authoritative;
- `flatten scalar conditional proof caches keep sparse entries sorted` queried three scalar `br_if` flows and their same-state replacements out of order and failed at `172/173` because branch entries produced `[17, 3, 10]` instead of `[3, 10, 17]`. Flow-site entries now retain the first admission proof in branch-id order; replacement entries remain value-id ordered and still update an existing same-state replacement.

The tuple/distinct-flow proof-lookup follow-up also added exactly one red-first invariant per code commit:

- `flatten tuple conditional proof cache keeps sparse entries sorted` queried three tuple-made multivalue `br_if` flows out of order and failed at `173/174` because append order produced `[17, 5, 11]` instead of `[5, 11, 17]`. Tuple flow entries now remain sorted by exact pre-mutation branch id and binary-searchable while explicit negative results and missing-entry rewrite rejection remain authoritative;
- `flatten multivalue conditional proof cache keeps sparse entries sorted` queried three distinct non-tuple flows out of order and failed at `174/175` because append order produced `[14, 4, 9]` instead of `[4, 9, 14]`. Distinct flow now uses the same sorted sparse helper, removing the final linear conditional-flow cache lookup without changing current parent/slot checks.

The table-target/terminal-payload iteration added exactly one red-first invariant per code commit:

- `flatten unique table target marks preserve first source order` failed because `flatten_record_unique_br_table_target` was unbound. The implemented mark helper records each valid label once, retains first explicit-target order, rejects duplicates and invalid ids, and lets the existing collector append an unseen default target last; private flatten moved to `176/176`;
- `flatten terminal payload cache keeps sparse entries sorted` failed because the record and membership helpers were unbound. Mixed-order insertion now produces `[4, 12, 19]`, duplicate records are rejected, and exact present/absent lookup uses binary search; private flatten moved to `177/177`.

The sequenced-root/multivalue-payload iteration again added exactly one red-first invariant per code commit:

- `flatten sequenced root cache keeps sparse pairs sorted` failed because the exact pair record and membership helpers were unbound. Mixed-order insertion now produces lexicographically sorted holder/node pairs, duplicate and cross-pair matches reject, and terminal placeholder placement uses binary membership;
- `flatten multivalue flow payload marks preserve exact distinct ids` failed because the distinct payload collector was unbound. Ordered payload ids now populate one temporary mark set, repeated ids reject, and admission-time root exclusion uses the same exact membership while cached parent/start validation remains unchanged.

The multivalue flow-index iteration again added exactly one red-first invariant per code commit:

- `flatten multivalue flow payload slots preserve exact source positions` failed because `flatten_collect_distinct_branch_payload_slots` was unbound. Sparse payload ids now map to exact source slots, unrelated ids remain absent, duplicate payloads reject, and reachable flow discovery updates the corresponding count/parent directly;
- `flatten tuple flow slot marks preserve exact first positions` failed because `flatten_record_distinct_flow_slot` was unbound. The mark helper preserves first encounter order, rejects duplicates and invalid slots, and lets tuple flow retain its ordered vector for the final sorted contiguous-span proof.

The behavior-first reversed-binary iteration added exactly one red-first behavior test per commit:

- `flatten preserves reversed binary false flow from scalar multivalue try lanes` failed unchanged at `245/246`; independently scalar lanes now accept the payload in either binary child slot when the opposite operand is simple, and rewrite replaces only that exact child;
- `flatten preserves reversed binary false flow from tuple-made multivalue try lanes` failed unchanged at `246/247`; an exclusively owned repeated `TupleMake` now accepts the same rule while retaining complete branch-slot and immediate-consumer ownership.

The behavior-first rich-right iteration also added exactly one red-first behavior test per commit:

- `flatten preserves rich right binary false flow from scalar multivalue try lanes` failed unchanged at `247/248`; a payload-left binary now accepts one exact one-use supported rich right operand, which ordinary operand flattening materializes after the branch;
- `flatten preserves rich right binary false flow from tuple-made multivalue try lanes` failed unchanged at `248/249`; an exclusively owned repeated `TupleMake` now accepts the same right-operand rule while complete branch-slot and one-consumer-per-lane ownership remains mandatory.

The behavior-first rich-left iteration again added exactly one red-first behavior test per commit:

- `flatten preserves rich left binary false flow from scalar multivalue try lanes` failed unchanged at `249/250`; one exact one-use rich left call is now staged before the complete simple independently scalar payload vector and payload-free branch, then read by lane zero's immediate false-path binary;
- `flatten preserves rich left binary false flow from tuple-made multivalue try lanes` failed unchanged at `250/251`; one exclusively owned repeated `TupleMake` with simple ordered components now accepts the same source-stack rule and is deleted only after every branch and consumer slot is replaced.

The inputful-loop rich-left iteration added exactly one red-first behavior test per commit:

- `flatten preserves rich left binary false flow from scalar inputful loop lanes` failed unchanged at `251/252`; independently scalar loop flow now freezes the exact branch root and rich-left/binary identities, requires a complete simple payload vector, and stages the rich left before entry-channel payload writes;
- `flatten preserves rich left binary false flow from tuple-made inputful loop lanes` failed unchanged at `252/253`; one exclusively owned repeated `TupleMake` now follows the same loop rule and is deleted only after every branch and consumer use is replaced.

The inputful-loop rich-payload iteration also added exactly one red-first behavior test per commit:

- `flatten preserves rich left binary false flow with one rich scalar loop payload` failed unchanged at `253/254`; independently scalar loop flow now admits at most one supported rich payload origin under the existing exact ownership and type proof;
- `flatten preserves rich left binary false flow with one rich tuple loop payload` failed unchanged at `254/255`; the first admission-only implementation exposed the rich payload prelude before the rich-left prelude, and the final route inserts the left store first before complete tuple replacement and deletion.

The legacy-try rich-payload iteration added exactly one red-first behavior test per commit:

- `flatten preserves rich left binary false flow with one rich scalar try payload` failed unchanged at `255/256`; independently scalar legacy-try flow now admits at most one supported rich payload origin and inserts the rich-left store before child-generated payload work;
- `flatten preserves rich left binary false flow with one rich tuple try payload` failed unchanged at `256/257`; one exclusively owned repeated `TupleMake` now follows the same bounded vector rule and is deleted only after complete branch-plus-consumer replacement.

The multiple-rich-payload iteration also added exactly one red-first behavior test per commit:

- `flatten preserves rich left flow with two rich independently scalar try and loop payloads` failed unchanged at `257/258`; both control families now admit every individually supported independently scalar payload origin under exact branch-plus-consumer ownership and vector-order proof;
- `flatten preserves rich left flow with two rich tuple-made try and loop payloads` failed unchanged at `258/259`; both control families now admit the same complete vector under exclusive repeated-`TupleMake` ownership and delete the shell only after branch-plus-consumer replacement.

The structured-subtree iteration added exactly one red-first behavior test per code commit:

- `detached structured subtree deletion removes owned labels atomically` first failed because `hot_delete_detached_control_subtree` and `hot_label_is_live` were unbound. The implemented path deletes a complete nested control tree and its four labels with one revision advance, while externally targeted and shared-descendant fixtures return false without mutation;
- `flatten removes one owned structured block suffix after a scalar legacy try table` failed unchanged at `259/260`. The exact block/drop/constant/label family now deletes after unconditional transfer; the externally targeted block remains the whitebox fail-closed boundary.

The rich-condition/interleaved-catch-root iteration added exactly one red-first behavior test per code commit:

- `flatten preserves a depth-one rethrow through a rich-conditional targeted catch if wrapper` failed at whitebox `205/206` because exceptional-transfer admission still required a simple condition. The final proof accepts only a scalar `i32` condition that is simple or an exactly one-use supported origin; ordinary flattening inserts its prelude immediately before the branch inside the opposite arm;
- `flatten repairs ordered catch payloads across interleaved direct roots` failed at `206/207` with `DeferredCatchPayloadRepair` because the direct-root planner required the first post-marker roots to be the lane roots. It now scans forward in source order, skips unrelated roots, and preserves exact first-child/two-use ownership and reverse stack capture.

## Ownership and mutation safety

`FlattenRewriteState` now owns:

- one exact Flat IR violation report frozen by the immutable pre-mutation node-index scan;
- one exact typed-catch-payload / exceptional-transfer repair requirement from that same scan;
- one lightweight exact reachable node-use-count snapshot built before admission, without full use-site or local-use allocation;
- one immutable live-node index containing exact per-label branch populations plus loop, legacy-try, and payload-bearing branch admission rosters;
- sparse exact conditional-flow and chained-replacement entries keyed only by admitted branch/value ids;
- sparse exact dead-suffix entries containing table id, owner region, and owned node vector;
- sparse exact terminal-table support entries keyed by table node, try label, payload arity, and mixed-target policy;
- sparse exact scalar-try support entries keyed by the inspected try owner;
- a `rewrites_started` boundary.

Admission computes and caches the complete distinct one-use proof. The lightweight count builder traverses only the reachable root graph, so detached-but-live stale parents cannot inflate ownership counts; focused comparison locks agreement with full use-def. Rewrite consumes only a suffix cache entry whose table id and owner region match and a terminal-table proof whose table, label, payload arity, and mixed-target policy match exactly. After the exact same-region suffix roots are detached, one batch mutation tombstones the complete cached distinct node vector and invalidates the HOT revision once. Suffix detachment now truncates the holder's existing child span instead of allocating a replacement span. Scalar and multivalue mutation-time target checks consume the cached exact branch-node population; they do not discover nodes allocated after the admission snapshot. Recursive multivalue table support receives the same snapshot rather than falling back to a live-node scan.

After every target/type/control family has passed admission, table routing performs one additional nonmutating preflight over all unique targets and any existing label-temp vectors. Only then does it allocate missing block/if/try vectors, while loop vectors must already match exactly. The resulting vector array is reused for every payload lane and on repeated resolution. If mutation has started and any required proof is absent, or any target vector is inconsistent, the corresponding check fails closed. This prevents a stale, partial, or post-snapshot fact from widening ownership, target support, or local-channel construction after structural edits.

Multivalue legacy-try label support now iterates only the exact branch nodes captured for the try label. Conditional flow ownership no longer allocates node-use-site arrays or CFG/local-use data: admission uses the run-wide lightweight reachable counts, rewrite uses the same frozen population behind the explicit `rewrites_started` boundary, and uncached checks can still rebuild current lightweight counts for whitebox comparison. Exact multivalue region-tail tuples use that same population: the exact tail root and slot are known structurally, so total reachable use count one replaces full site allocation. The complete inputful-loop support/rewrite chain now uses `label_branch_nodes[label]` for general backedges and multivalue `br_if` flow and reuses `state.use_def` for tuple conditional ownership.

Exact tuple-made inputful-loop entries now consume the same count snapshot. Structural proof already identifies the tuple in every loop entry slot and every immediate reversed body-prefix drop, so the reachable count must equal twice the input arity. Exact scalar legacy-try `br_if` flow also uses the snapshot: the payload must have exactly the branch use plus one immediately adjacent false-flow use, and any unary/conversion/binary consumer must itself be one-use.

Tuple-made plain `br` and `br_table` payloads now use the snapshot as well. Every branch payload slot is checked directly before exact reachable count `payload_count` replaces a full site vector. Generic tuple-made block/if `br_if` still needs the exact false-flow parent and contiguous start slot, so counts alone are insufficient. Admission performs one reachable-root traversal for that tuple, caches the exact `[tuple, parent, start]` result (including a negative result), and rewrite requires both `rewrites_started` and an existing cache entry. Distinct non-tuple multivalue flow now follows the same boundary with a cached `[parent, start]`. Scalar flow caches its exact pre-mutation parent population; because prelude insertion can shift region-holder slots, rewrite rescans only each cached parent and requires the same occurrence count. It does not scan unrelated parents, and chained branches accept only a replacement node recorded by the same rewrite state. Current structure is rechecked before consumption, so the snapshots cannot widen after mutation.

These proofs are now stored only for inspected owners rather than in node-count-sized arrays. Conditional negative results remain explicit sparse entries, scalar entries retain the exact payload and parent population, terminal entries retain every identity field used by the old arrays, and suffix entries retain exact region ownership. Any absent entry after mutation begins still fails closed. These changes admit no new control, payload, effect, trap, type, or deletion family.

Generic recursive routing now has a similarly exact dispatch boundary. Payload-bearing `br_if` and `br_table` complete their routing in dedicated match arms and return, so only carried plain `br` reaches the generic postorder router. Sequence-root deduplication still records exact holder/node identity, but only when the immutable reachable count proves the source node has multiple owners. `use_count_node_limit` freezes the count population: rewrite-created ids are never looked up, and therefore cannot widen or crash the mutation-time proof. Unique roots need no deduplication entry.

Table-local construction now distinguishes one and many unique targets after complete target/type/control preflight. One target uses that exact typed target vector as the staging vector, so payloads still execute before selector work but no self-copy is built. Multiple targets retain a separate staging vector and complete fanout. Inputful-loop support is stored as another sparse exact admission entry. The cached result binds the complete pre-mutation type, entry, branch, conditional-flow, ownership, backedge, and tail proof; rewrite cannot recompute or widen it after structural edits.

Candidate-dense lookup now preserves that sparsity while avoiding repeated linear scans. Scalar-try entries are sorted by pre-mutation try id; dead-suffix and terminal-table entries are sorted by pre-mutation table id; inputful-loop support entries are sorted by loop id; tuple-made, distinct multivalue, and scalar conditional flow-site entries are sorted by branch id; terminal payload roots are sorted by exact node id; and same-state scalar replacement entries are sorted by value id. Binary search locates exact entries, sorted insertion adds only inspected owners, and existing admission proofs are not overwritten. Replacement identity retains its prior update behavior for the same value. Explicit negative multivalue flow results remain first proofs. Rewrite still rejects an absent entry, and every cached region, label, payload-arity, mixed-target, support, ownership, parent-population, current-structure, and same-state discriminator remains mandatory.

Unique table-target collection remains separate from semantic admission. A temporary label-sized mark set only suppresses repeated explicit/default labels while preserving first source order; every later target-kind, result/parameter type, ownership, EH, branch-flow, local-vector, and mutation-time proof still runs in the existing helpers. Terminal payload ids are recorded only after an admitted payload is staged, and sparse membership merely finds the exact pre-mutation root during scalar block repair or recursive region removal; it cannot admit a target or authorize a deletion by itself.

Shared-root pair sorting and multivalue payload marks are likewise lookup aids, not admission facts. A sequenced-root entry still requires the frozen pre-mutation reachable count to prove multiple owners, binds one exact region holder plus node id, and excludes rewrite-created ids. Payload marks exist only for one current branch query: they preserve slot order, reject repeated ids, and accelerate root exclusion; exact branch arity, cached false-flow parent/start, current child slots, use counts, target types, and mutation-boundary rules still decide support.

The newer payload-slot and tuple-slot indexes remain query-local lookup aids too. Distinct non-tuple payload ids map to their exact branch slots so each reachable edge can update one lane's total count and unique non-branch parent/slot directly. Tuple flow marks are allocated only after the first non-branch parent is fixed and reject any repeated child slot. Neither index replaces exact total site counts, branch-slot coverage/exclusion, one-parent ownership, ordered contiguous flow, cached parent/start validation, current-slot checks, types, use counts, EH, effects, traps, or the pre-mutation rewrite boundary.

Reversed binary flow changes the admitted consumer shape, not its proof boundary. A non-tuple payload still has exactly the branch use plus one immediate consumer, and a tuple still owns exactly every branch payload slot plus one immediate consumer per lane. The consumer remains a directly dropped, single-use, same-typed binary with an exact payload child. A payload-right binary may keep a Flat-IR-simple left sibling in place after `br_if`.

One source-representable rich-left/payload-right subset is now admitted for legacy tries and inputful loops. Exactly one one-use supported rich left may precede the complete payload vector, so it must pair with lane zero after all higher lanes are consumed in reverse order. The independently scalar payload vector, or the component vector of one exclusively owned repeated `TupleMake`, may contain any number of rich origins when every origin is individually supported and the complete scalar or tuple ownership proof succeeds. Rewrite stages the rich left before every payload local and the branch, replaces only the binary's left child with its read, and leaves the binary itself on the not-taken path. Four pinned-v130 probes place the rich left call before all payload calls in vector order and `br_if`, then retain the binary after the branch.

For a payload-left binary, the right sibling may instead be one exact one-use supported scalar origin. Generic postorder operand flattening materializes it after the payload-free branch and before the binary. Pinned-v130 legacy-try and inputful-loop probes place rich right calls in that same order, so effects and traps remain not-taken-only. Inputful-loop admission freezes the branch root and rich-left/binary identities, and tuple flow inserts the left store before child-generated payload preludes. Commits `61055698d` and `d9aa4cd94` established the same order for legacy tries; commits `6e4229bae` and `f223f14d6` remove the temporary payload-count cap for independently scalar and exclusively tuple-made vectors in both controls. Type changes, multiple or non-lane-zero rich-left operands, sharing, nesting, delayed consumers, mixed tuple/scalar ownership, and repair-sensitive EH remain rejection.

## Refreshed output matrix

The same three synthetic probes from the prior impact note were regenerated after the lowering change. Binaryen artifacts remain the pinned prior-oracle outputs.

| Probe | Input | Starshine direct | Starshine cleanup | Binaryen direct | Binaryen cleanup |
| --- | ---: | ---: | ---: | ---: | ---: |
| catch-all result bridge | 66 | 64 | 64 | 87 | 72 |
| recursive call tree | 69 | 74 | 74 | 94 | 82 |
| direct subtract call | 69 | 74 | 74 | 94 | 82 |
| **Aggregate** | **204** | **212** | **212** | **275** | **236** |

Every current Starshine direct and cleanup artifact passed `wasm-tools validate --features all`.

The narrow bridge/control/local family is now a measured Starshine size win:

- direct: 63 bytes smaller than Binaryen (`212` versus `275`);
- matched cleanup: 24 bytes smaller than Binaryen (`212` versus `236`);
- Starshine cleanup is a no-op on these already-clean emitted shapes.

The WAT difference is source-backed: nonthrowing catch bodies are unreachable, live try-label branches are preserved by one void block, and Starshine avoids Binaryen's extra result-copy locals. This classification is limited to the measured synthetic catch-all subset; it is not evidence for typed catches or broader EH.

## Runtime semantics

Node instantiated all input, Binaryen direct, Binaryen cleanup, current Starshine direct, and current Starshine cleanup artifacts with a no-op `env.sink` import. Every probe returned the expected deterministic result (`3` for the branch-free bridge and `7` for both table-transfer probes).

Sum of per-probe medians after 10,000 warmups and 15 samples of 100,000 calls:

| Lane | Sum of medians |
| --- | ---: |
| input | 1.342171 ms |
| Binaryen direct | 1.524702 ms |
| Binaryen cleanup | 1.364372 ms |
| Starshine direct | 1.379081 ms |
| Starshine cleanup | 1.365836 ms |

These samples establish deterministic agreement and no obvious runtime regression. They remain too narrow and noisy for a runtime-win claim.

## Pass-local performance

The representative native release benchmark prebuilt 120 HOT functions, evenly split across the one-multiply-child, two-multiply-child, and deeper-two-multiply-plus-constant call-argument families. Five warmups preceded 20 measured `flatten_run(...)` batches.

| Lane | Median | Range | Ratio to Binaryen |
| --- | ---: | ---: | ---: |
| prior current | 3,682.5 us | 3,529..3,765 us | 13.84x |
| run-wide cached current | 2,345.5 us | 2,248..2,660 us | 8.82x |
| cached EH/effective-terminal current | 1,641 us | 1,583..1,813 us | 6.17x |
| cached scalar-try support current | 1,275 us | 1,224..1,324 us | 4.79x |
| explicit EH admission-gate current | 1,347.5 us | 1,280..1,931 us | 5.06x |
| cached label-use / direct-shift current | 1,218.5 us | 1,159..1,310 us | 4.58x |
| signed-shift / state-threaded current | 1,259 us | 1,219..1,321 us | 4.73x |
| exact terminal-table proof cache | 1,195 us | 1,166..1,485 us | 4.49x |
| unsigned-shift / duplicate-router removal | 1,182.5 us | 1,166..1,425 us | 4.44x |
| same-session current-iteration baseline | 1,266.5 us | 1,221..1,517 us | 4.76x |
| lightweight reachable ownership counts | 1,115.5 us | 1,050..1,471 us | 4.19x |
| batched detached suffix deletion current | 970.5 us | 955..1,161 us | 3.65x |
| Binaryen v130 | 266.05 us | 250.16..380.856 us | 1.00x |

The follow-up profile attributed most measured time to repeated legacy-try support during rewrite. `FlattenRewriteState` now caches the immutable pre-admission EH prerequisite classification, try-target terminal checks consume the existing cached suffix proof instead of rebuilding an uncached use-def snapshot, and the complete scalar legacy-try support decision is cached per owner before mutation and consumed only by matching later rewrite checks. The representative median improved another 22.30% from `1,641 us` to `1,275 us`. After adding explicit catch-payload and exceptional-transfer admission outcomes, the same 120-function native-release shape reran at `1,347.5 us`. The next iteration cached immutable label-use facts once per rewrite state and reused them through scalar and multivalue control support/routing, reaching `1,218.5 us` in the first post-change sample. After adding direct signed-shift roots and threading state through rich branch admission, the rerun measured `1,259 us`.

The prior exact phase profile, measured across the same 120 prebuilt candidate-dense functions after five warmup batches, reported median batch attribution of `55.5 us` classify, `152.5 us` rewrite-state construction, `251.5 us` admission, `684.5 us` rewrite, and `5 us` body-result handling. The current iteration profiled inside those two dominant areas before choosing each slice. The state-construction subprofile attributed medians of `11.5 us` label-temp arrays, `15 us` EH prerequisite scan, `18.5 us` label-use computation, `131 us` full node-use use-def construction, and `16.5 us` cache-array initialization. Replacing only the immutable ownership need with reachable node-use counts reduced that subprofile's ownership component to `26.5 us`; the same-session pass median moved from `1,266.5 us` to `1,115.5 us`, an `11.92%` improvement.

The rewrite subprofile then attributed the successful terminal-table route to `27.5 us` preflight, `30 us` payload staging, `3.5 us` cached suffix lookup, `185 us` suffix mutation, `48.5 us` target copies, and `13 us` selector handling. Inside suffix mutation, region splice accounted for `64 us` and per-node detached deletion for `73.5 us`, with the remaining time in root search and repeated mutation bookkeeping. The batch deletion API preserves the exact cached distinct ownership set while replacing per-node revision bumps with one invalidation. The final pass-only median is `970.5 us` (`955..1,161 us`), down `13.00%` from code 1 and `23.37%` from the same-session iteration baseline.

The final exact phase profile at the lightweight-analysis/batch-mutation checkpoint reports `53.5 us` classify, `67.5 us` state construction, `245.5 us` admission, `590.5 us` rewrite, and `5.5 us` body-result handling. Relative to the prior exact phase profile, state construction fell `55.74%` and rewrite fell `13.73%`; rewrite remained dominant and admission second.

The next iteration profiled before each slice. In the pre-code-1 admission sample, scalar-try support consumed `265 us` across 120 functions, including `170 us` in the full-node target-use scan and `54 us` in terminal-tail checks; inputful-loop and rich-branch admission contributed `10 us` and `33 us`. After indexing exact branch targets once, a diagnostic one-batch profile reported `48 us` classify, `65 us` state, `222 us` admission, and `647 us` rewrite. Within rewrite, terminal-table routing contributed `238 us` and scalar-try result routing `134 us`, motivating one-for-one region-root replacement as code 2. These instrumented one-batch sums are diagnostic attribution, not substitutes for the repeated timing medians.

On one same-session reconstructed 120-function chain, the median moved from the pre-iteration `1,197.5 us` (`1,120..1,432`) to `1,092.5 us` (`1,057..1,135`) after the branch index, then `1,030 us` (`1,005..1,064`) after in-place value tails: a `13.99%` total reduction. On the exact candidate shape, code 1 measured `1,052.5 us` (`1,001..1,111`), while code 2 measured `1,033.5 us` (`969..1,122`) with a noisier `1,047 us` rerun (`997..1,536`). The prior stable checkpoint remains `970.5 us`; all samples remain outside the maximum acceptable `2x` threshold.

The suffix-truncation iteration reconstructed the same three-family, 120-function native-release chain from clean `1f1a2dfd0`. Clean HEAD measured `1,116.5 us` (`1,060..1,377`); `13fe7b744` measured `1,053.5 us` (`1,030..1,070`), a `5.64%` reduction from avoiding holder-span rebuilds during terminal suffix removal. The proof-boundary commit did not produce a measurable timing win: exact-candidate samples were `1,173 us` (`1,087..1,415`) and `1,202.5 us` (`1,064..1,572`), with a same-session `1,191.5 us` sample versus code 1 at `1,133.5 us`; a branch-index-heavy probe was similarly noisy and slower. Classify `9acdac744` as failure-atomic proof reuse and scan-removal, not a measured performance win. Relative to Binaryen v130's `266.05 us`, the best current-iteration sample is still `3.96x`, and the prior stable `970.5 us` checkpoint is `3.65x`.

The multivalue branch-index slice used 120 independently lifted two-lane block-branch functions with 256 extra roots to magnify whole-live-node target scans. In the clearer same-session ordering, clean code-1 baseline `9aa7499e9^` measured `5,581.5 us` (`5,183..7,300`) and `9aa7499e9` measured `5,099 us` (`4,711..6,343`), an `8.64%` reduction. An earlier ordering measured `5,323.5 -> 5,199 us` (`2.34%`). The representative scalar terminal-table fixture moved only `2,939 -> 2,904.5 us`, which is noise-level and expected because it does not exercise multivalue target scanning.

The table-vector slice used 120 two-target, four-payload-lane functions with 256 extra roots. Code 1 and code 2 measured `7,848.5` versus `7,767.5 us` in one order, then `8,324` versus `8,619 us` in reversed order. Classify `710cdc910` as bounded repeated-resolution removal and stable failure-atomic local-vector reuse, not a measured speed win.

The legacy-try branch/ownership slice used 120 tuple-made two-lane legacy-try `br_if` functions with 256 extra roots. The iteration baseline measured `16,524.5 us` (`15,971..16,676`); branch indexing measured `16,167 us` (`16,048..16,354`), a `2.16%` reduction; lightweight conditional ownership measured `6,724.5 us` (`5,999..7,291`), a further `58.41%` reduction and `59.31%` total reduction from baseline. This is a measured win for the targeted multivalue conditional family, but it does not replace the prior stable representative candidate checkpoint of `970.5 us`, which remains `3.65x` Binaryen v130. Performance remains a hard blocker for public exposure.

The region-tail ownership slice used 120 two-lane `TupleMake` block-tail functions with 256 extra roots. Clean `3216eee8e` measured `6,569 us` (`6,124..6,951`); `3d0acb44e` measured `4,144.5 us` (`3,962..4,307`), a `36.91%` reduction from replacing full use-site allocation with the existing exact reachable count.

The loop-branch slice used 120 exact two-lane inputful-loop `br_if` functions with 256 extra roots. Code 1 measured `8,344 us` (`7,236..10,591`); `19fa4eda8` measured `6,870 us` (`6,100..8,309`), a `17.67%` reduction. The reconstructed terminal-table representative was noisy and did not improve: code 1 measured `2,953.5 us` (`2,839..3,777`) and code 2 measured `3,038.5 us` (`2,804..3,869`).

The tuple-entry ownership slice used 120 exact two-lane tuple-made inputful-loop entry functions with 256 extra roots. Clean `b624a0441` measured `10,895.5 us` (`10,725..12,085`); `3a88b5bd6` measured `4,506 us` (`4,397..4,546`), a `58.64%` reduction.

The scalar-try ownership slice used 120 exact scalar legacy-try `br_if` functions with 256 extra roots. Code 1 measured `8,867.5 us` (`8,801..8,929`); `5c0235d71` measured `4,214.5 us` (`4,165..4,349`), a `52.47%` reduction. These are targeted family wins; the stable `970.5 us` / `3.65x` representative checkpoint remains the durable public gate result.

The tuple branch-payload slice used 120 exact two-lane tuple-made plain-branch functions with 256 extra roots. Clean `160fd52a7` measured `13,697 us` (`13,637..13,783`); `24ca31723` measured `5,238 us` (`5,106..5,367`), a `61.76%` reduction.

The generic tuple conditional-flow slice used 120 exact two-lane tuple-made block `br_if` functions with 256 extra roots. Code 1 measured `12,764.5 us` (`12,620..12,922`); `32690a37d` measured `5,578.5 us` (`5,380..5,676`), a `56.30%` reduction. A reconstructed terminal-table fixture moved `3,216.5 -> 2,820.5 us`, but that fixture is not the durable public gate.

The distinct non-tuple conditional-site slice used 600 independently produced two-lane block `br_if` functions with 256 extra reachable roots. Detached clean `62f9abfd9` measured a 30 ms median and `ae096a883` measured 28 ms with 1 ms timer resolution, a directional `6.67%` reduction. The scalar site slice used 600 rich scalar block `br_if` functions with the same extra roots. Code 1 measured 22 ms and `b87464d25` measured 23 ms; this is slight targeted overhead and is classified only as a mutation-boundary correctness improvement, not a speed win.

The sparse-cache iteration reconstructed the documented three-family representative mix as 1,200 functions to overcome the 1 ms timer floor: 400 each of one-multiply-child, two-multiply-child, and deeper-two-multiply-plus-constant resultless-call suffixes. Clean `f57114579` scaled to a `1,900 us` median per 120 functions and conditional-cache sparsification scaled to `1,800 us` (`5.26%`, overlapping ranges). The terminal-cache comparison measured `1,900 us` on both sides. A 600-function version with 256 extra reachable roots per function scaled from `8,200 us` to `8,300 us`, also with overlapping ranges.

The postorder-routing iteration recovered a repeatable high-resolution native-release pass-only path through the existing `passes_perf_long` package; the temporary benchmark sources are preserved under `.tmp/flatten-iteration13-*-bench-source.mbt`. A 1,200-function fixture with 256 unrelated roots plus one carried branch per function measured a stable `57,498 us` median at `be140ee73` and `52,402 us` after `7801166ac`, an `8.86%` targeted recursive-traversal improvement. The shared-root slice measured `52,534.5 us` on the same fixture. The reconstructed 120-function three-family representative measured `1,177 us` at `be140ee73`, `1,176 us` after code 1, and `1,171.5 us` after code 2, with overlapping ranges. The recovered path is repeatable and high resolution, but it is a reconstruction rather than the lost original gate harness. It therefore does not replace the stable `970.5 us` / `3.65x` Binaryen v130 checkpoint.

The single-target-table iteration reused that reconstructed representative. The pre-iteration median was `1,128.5 us`; code 1 measured `1,087 us`, while later code-2 samples measured `1,136 us` and `1,122 us`. Treat the timing as overlapping rather than a stable speed win. The structural win is directly measured: on a focused scalar repeated-target table, encoded output shrank from `51` to `47` bytes because one staging local and one local-copy pair disappeared. A pinned Binaryen v130 probe emits the separate staging local and target copy. The inputful-loop cache used a 600-function native-release fixture: code-1 baseline measured `2,589 us`, and the reliable current rerun measured `2,584 us`; one earlier `3,304 us` current sample was not reproduced. Classify code 2 as proof-boundary/repeated-support removal, not a timing win. Neither result requalifies the durable public performance gate.

The branch-append/admission-roster iteration used the recovered native-release path. A 200-function fixture with 256 `br_if` users of one label improved from `17,065 us` (`16,122..18,472`) to `14,723 us` (`14,245..16,413`), a `13.72%` reduction from replacing growing per-label `contains` scans with the monotonic current-node guard. The 1,200-function root-heavy fixture then improved from a code-1 baseline of `54,596 us` (`50,430..56,353`) to `51,076.5 us` (`48,892..54,148`), with a `49,953.5 us` rerun, after admission reused the exact loop/try/branch rosters from the same node scan. The contemporaneous reconstructed representative moved directionally from `1,111.5 us` to `1,066 us`. These are targeted/reconstructed wins; they do not recover the original gate contract or replace the durable `970.5 us` / `3.65x` checkpoint.

The EH/flatness scan-sharing iteration used the same recovered native-release reconstruction. After `7706110c1`, the 120-function representative improved from a clean-HEAD median of `1,131 us` (`1,104..1,181`) to `1,060 us` (`1,022..1,576`), a `6.28%` ordered reduction; the 1,200-function root-heavy fixture was thermally/order noisy (`49,099.5 us` baseline versus a reliable `49,947 us` current rerun), so the representative result is the only directional claim. After `2c5a54ac3`, representative pairs were overlapping/order-sensitive (`1,057` and `1,010.5 us` baselines versus `1,058` and `1,045.5 us` current), while the root-heavy ordering measured `49,148 -> 50,592 us`. Classify code 2 as exact scan consolidation and frozen-analysis reuse, not a measured speed win. Neither slice recovers the original gate harness or replaces the durable `970.5 us` / `3.65x` checkpoint.

The sparse proof-lookup iteration held total candidate counts fixed while increasing candidates per function. For scalar legacy tries, the 512-candidate median improved from `7,115.5 us` (`6,604..7,407`) to `6,689.5 us` (`6,546..6,824`), a `5.99%` targeted reduction; lower densities overlapped. For admitted terminal tables, the 256-candidate median improved from `22,708.5 us` (`13,402..26,354`) to `9,426 us` (`9,144..9,600`), a `58.49%` reduction, and the lower-density growth trend also collapsed. The reconstructed representative was order-sensitive: code-1 baseline measured `1,069.5 us` (`1,047..1,250`), while current measured `1,108 us` (`1,043..1,173`) and then `1,063 us` (`1,030..1,406`). Treat the targeted density wins as real owner-specific improvements and the representative as overlapping; the original gate remains unrequalified.

The loop/scalar-flow proof-lookup iteration used the same fixed-total density method with 2,048 candidates. Inputful loops improved from median `14,038 us` (`13,941..14,311`) to `12,694 us` (`12,332..13,615`) at 128 candidates per function, a `9.57%` reduction; at 256 candidates they improved `20,589.5 -> 19,854 us` (`3.57%`). Scalar block `br_if` flows improved from `36,469 us` (`34,803..39,369`) to `35,250.5 us` (`34,585..38,382`) at 128 candidates (`3.34%`) and `64,969 -> 63,702.5 us` at 256 candidates (`1.95%`). These are measured candidate-density lookup reductions, not representative or public-gate requalification; the durable `970.5 us` / `3.65x` checkpoint remains authoritative.

The tuple/distinct-flow proof-lookup iteration used an exact cached-lookup reconstruction with 1,048,576 lookups per sample. Tuple-made flow improved at 256 candidates from median `210,832 us` (`203,197..220,461`) to `154,616 us` (`151,648..159,995`), a `26.66%` reduction, and at 512 candidates from `304,931 us` (`293,521..330,427`) to `160,577 us` (`156,617..174,599`), a `47.34%` reduction. Distinct non-tuple flow, measured from detached code-1 commit `80e6a652b` against code 2, improved at 256 candidates from `213,988.5 us` (`159,462..329,668`) to `107,946.5 us` (`78,946..117,240`), a `49.55%` reduction, and at 512 candidates from `365,408.5 us` (`344,324..461,662`) to `120,985.5 us` (`115,280..133,989`), a `66.89%` reduction. These are exact owner-specific lookup results; they do not recover the original representative harness or requalify the public gate.

The table-target/terminal-payload iteration profiled both owners before changing them. The unique-target reconstruction performed 4,096 collections over a table with two explicit entries per candidate plus a repeated default. At 256 labels the baseline measured `115,000 us` and code 1 measured a `9,000 us` median; at 512 labels it improved `437,000 -> 16,000 us`, a `96.34%` targeted reduction. Terminal payload membership held 1,048,576 successful queries per sample. Against code 1, the median improved at 256 roots from `58,000 -> 17,000 us` (`70.69%`) and at 512 roots from `110,000 -> 20,000 us` (`81.82%`). These are exact dense-owner improvements. The temporary benchmark source is not a maintained contract, the original representative harness remains unrecovered, and the durable `970.5 us` / `3.65x` public gate remains authoritative.

The immutable-index/reusable-scratch iteration measured the five requested allocation owners with the same release-mode micro source before and after implementation. Median ancestry queries over a 64-control chain improved `185,000 -> 44,000 us` (`76.22%`); 20,000 repeated 256-target table queries improved `32,000 -> 1,000 us` (`96.88%`); one million repeated four-result type queries improved `15,000 -> 4,000 us` (`73.33%`); one million two-target local preflights improved `77,000 -> 68,000 us` (`11.69%`); and 100 independently built 1,000-root no-prelude functions improved `31,000 -> 28,000 us` (`9.68%`) while retaining 100 depth buffers instead of allocating 100,000 empty root arrays. The recovered `passes_perf_long` 120-function representative used clean `29748c203` as the detached baseline: median `1,001.5 us` (`963..1,379`) versus current `989.5 us` (`948..1,216`), a directional `1.20%` reduction with overlapping ranges. This demonstrates no representative regression, but it does not replace or requalify the durable original `970.5 us` / `3.65x` Binaryen checkpoint.

The implementation freezes each live `br_table` side table into one sparse entry over a flat unique-target pool and requires that entry during rewrite. Structural parents are intentionally lazy so functions with no nontrivial ancestry query pay no region-root scan; the first such query after mutation starts rejects. One lazily allocated node-generation array serves ancestry, dead-suffix uniqueness, and inputful-loop visitation with separate epochs, while recursive traversal keeps one cleared prelude buffer per active region depth. Exact one-use or branch-plus-consumer counts remove only duplicate scans they fully dominate. All-target local preflight still finishes before any missing label vector is allocated, but rewrite reads vectors from `state.label_temps` directly. Type-result arrays use a lazy dense cache, and scalar type checks inspect immutable type records without creating arrays.

The sequenced-root/multivalue-payload iteration profiled both exact lookup owners before changing them. For 262,144 successful holder/node queries, the median improves `16,087 -> 2,371 us` at 256 pairs (`85.26%`) and `31,569 -> 2,664 us` at 512 pairs (`91.56%`). For 2,048 source-ordered distinct-payload collections, the median improves `18,072 -> 1,567 us` at 256 payloads (`91.33%`) and `64,554 -> 2,757 us` at 512 payloads (`95.73%`). These local-only reconstructions measure the exact linear owners but do not recover the original representative harness, and the skipped maintained sequenced-root fixture from `02f402a19` still encodes the old linear baseline until a future code slice deliberately updates its contract.

The multivalue flow-index iteration profiled both remaining exact quadratic lookup owners before changing them. For 32 complete reachable edge scans per sample, direct non-tuple payload-slot indexing improves the median from `495 -> 20 us` at 256 payloads (`95.96%`) and `1,878 -> 39 us` at 512 payloads (`97.92%`). For 2,048 tuple false-flow slot collections, exact slot marks improve `16,426 -> 749 us` at 256 slots (`95.44%`) and `59,644 -> 1,368 us` at 512 slots (`97.71%`). These local-only reconstructions measure the exact nested scans but do not recover or requalify the original representative harness. The maintained skipped sequenced-root fixture was not touched or reused and remains explicitly stale.

The reversed-binary, rich-right, legacy-try rich-left, inputful-loop rich-left, inputful-loop rich-payload, and legacy-try rich-payload iterations intentionally made no timing claim. They follow the updated feature-first priority: close semantic/control families before spending further effort on millisecond-scale micro-optimization. The durable `970.5 us` / `3.65x` checkpoint remains unrequalified rather than being replaced by a new small-owner benchmark.

## Validation

- HOT-lower focused file: `88/88`.
- Flatten focused file: `240/240`.
- Private flatten file: `142/142`.
- Pass package: `5,712/5,712`.
- Full suite: `9,171/9,171`.
- `moon info`: passed with existing warnings.
- Every regenerated matrix artifact validated with all features.
- `git diff --check` and docs link/source review are required before commit.

Follow-up proof-cache validation: direct `i32.or` behavior was red at `240/241` and green at `241/241`; direct `i32.xor` behavior was red at `241/242` and green at `242/242`. Private flatten passed `142/142`, the pass package passed `5,714/5,714`, and `moon info` passed with the existing warnings.

Follow-up EH-gate validation used a pinned Binaryen v130 typed-catch probe. Direct `--flatten` stages the typed `(pop i32)` into a fresh local before the original drop, confirming that payload extraction is semantic work rather than optional cleanup. The updated private behavior test first failed because a rooted `Catch` plus an otherwise flattenable rich operand returned `{ changed: true }`; explicit `DeferredCatchPayloadRepair` and `DeferredExceptionalTransferRepair` admission outcomes now stop `Catch`/`CatchAll` and `Rethrow`/`Delegate` functions before locals or operand rewrites can begin. That iteration passed private flatten `142/142`, passes `5,714/5,714`, and the full suite `9,173/9,173`.

The next two red-first behavior slices admitted direct `i32.shl` and `i32.shr_s` resultless-call argument roots under the same complete distinct one-use proof. Each pinned Binaryen v130 probe retains the operation, call, and unreachable Flat-IR debris. Focused flatten moved from `242/243` to `243/243`, then `243/244` to `244/244`; the private boundary remained `142/142` while moving from `i32.shl` to `i32.shr_s` and then `i32.shr_u`. That iteration passed passes `5,716/5,716`, the full suite `9,175/9,175`, `moon info`, targeted formatting, and `git diff --check`.

The terminal-proof/routing validation moved private flatten from red `142/143` to green `143/143`, then focused flatten from red `244/245` to green `245/245`. That iteration passed private flatten `143/143`, passes `5,718/5,718`, the full suite `9,177/9,177`, `moon info`, targeted formatting, and `git diff --check`.

The lightweight-analysis/batch-mutation validation moved private flatten to `144/144` and focused HOT mutation to `10/10`; focused flatten remained `245/245`. That checkpoint passed passes `5,719/5,719`, the full suite `9,180/9,180`, `moon info`, targeted formatting, and `git diff --check`. The reviewed `src/ir/pkg.generated.mbti` diff added `HotNodeUseCounts`, `hot_node_use_counts_build`, `hot_node_use_count`, and `hot_delete_detached_nodes`; no pass or CLI public API changed.

The branch-index/in-place-tail iteration passes focused flatten `245/245`, private flatten `146/146`, passes `5,721/5,721`, the full suite `9,181/9,181`, `moon info`, targeted formatting, and `git diff --check`. It changes no `.mbti` snapshot and adds no public pass, CLI, or IR API.

The suffix-truncation/proof-boundary iteration passes focused region editing `4/4`, focused flatten `245/245`, private flatten `147/147`, passes `5,722/5,722`, the full suite `9,183/9,183`, `moon info`, targeted formatting, and `git diff --check`. The reviewed `src/ir/pkg.generated.mbti` diff adds only `hot_region_truncate_suffix`; no pass, CLI, compare, or preset API changed. Repository-wide `moon fmt --check` remains blocked only by the pre-existing `moon.mod` syntax disagreement.

The multivalue-index/table-vector iteration passes focused flatten `245/245`, private flatten `149/149`, passes `5,724/5,724`, the full suite `9,185/9,185`, `moon info`, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash was rechecked after both commits and remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Neither commit admits a new semantic family, so no additional Binaryen probe was required.

The legacy-try branch/ownership iteration passes focused flatten `245/245`, private flatten `151/151`, passes `5,726/5,726`, the full suite `9,187/9,187`, `moon info`, targeted formatting, and `git diff --check`. No `.mbti` or public pass surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The region-tail/loop-branch iteration passes focused flatten `245/245`, private flatten `153/153`, passes `5,728/5,728`, the full suite `9,189/9,189`, `moon info`, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The loop-entry/scalar-try ownership iteration passes focused flatten `245/245`, private flatten `155/155`, passes `5,730/5,730`, the full suite `9,191/9,191`, `moon info`, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The tuple branch/conditional-flow iteration passes focused flatten `245/245`, private flatten `157/157`, passes `5,732/5,732`, the full suite `9,193/9,193`, `moon info`, targeted formatting, and `git diff --check`. The final full node-use/use-site builder has been removed from `src/passes/flatten.mbt`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The distinct/scalar conditional-site iteration passes focused flatten `245/245`, private flatten `159/159`, passes `5,734/5,734`, the full suite `9,195/9,195`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The sparse proof-cache iteration passes focused flatten `245/245`, private flatten `161/161`, passes `5,736/5,736`, the full suite `9,197/9,197`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The postorder-routing/shared-root iteration passes focused flatten `245/245`, private flatten `163/163`, passes `5,738/5,738`, the full suite `9,199/9,199`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, generator, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. Both commits preserve the admitted semantic surface, so no new Binaryen probe was required.

The single-target-table/inputful-loop iteration passes focused flatten `245/245`, private flatten `165/165`, passes `5,740/5,740`, the full suite `9,201/9,201`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, generator, or preset surface changed. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`. The table-shape change was probed directly against Binaryen v130; the loop-support commit admits no semantic family and required no additional oracle probe.

The branch-append/admission-roster iteration passes focused flatten `245/245`, private flatten `167/167`, passes `5,742/5,742`, the full suite `9,203/9,203`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI, compare/API, generator, or preset surface changed. Both commits preserve the admitted semantic surface, so no Binaryen probe was required. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`.

The EH/flatness scan-sharing iteration passes focused flatten `245/245`, private flatten `169/169`, passes `5,744/5,744`, the full suite `9,205/9,205`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed. Both commits preserve the admitted semantic surface, so no Binaryen probe was required. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`.

The sparse proof-lookup iteration passes focused flatten `245/245`, private flatten `171/171`, passes `5,746/5,746`, the full suite `9,207/9,207`, `moon info` with 11 existing warnings, targeted formatting, and `git diff --check`. No `.mbti`, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed. Both commits preserve the admitted semantic surface, so no Binaryen probe was required. The pinned owner hash remained `5b8836c46490095e98ba8202f866b153cfacc6f9c24ac498b703702adc3455b6`.

The loop/scalar-flow proof-lookup iteration passes focused flatten `245/245`, private flatten `173/173`, passes `5,748/5,748`, the full suite `9,209/9,209`, and `moon info` with 11 existing warnings. Targeted formatting, `git diff --check`, and pinned-owner verification pass. No `.mbti`, semantic family, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed, so no new Binaryen probe was required.

The tuple/distinct-flow proof-lookup iteration passes focused flatten `245/245`, private flatten `175/175`, passes `5,750/5,750`, the full suite `9,211/9,211`, and `moon info` with 11 existing warnings. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. No `.mbti`, semantic family, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed, so no new Binaryen probe was required.

The table-target/terminal-payload iteration passes focused flatten `245/245`, private flatten `177/177`, passes `5,752/5,752`, the full suite `9,213/9,213`, and `moon info` with 11 existing warnings. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. No `.mbti`, semantic family, output shape, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed, so no Binaryen probe was required.

The immutable-index/reusable-scratch iteration passes focused flatten `245/245`, private flatten `182/182`, passes `5,757/5,757`, the full suite `9,218/9,218`, `moon info` with 11 existing warnings and 0 errors, targeted formatting, `git diff --check`, and a native release CLI build. The red-first failure was the expected set of unbound cache/scratch helpers and missing state fields. No `.mbti`, semantic family, output shape, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed. The public four-lane matrix remains inapplicable while flatten stays removed and has no GenValid aggregate.

The sequenced-root/multivalue-payload iteration passes focused flatten `245/245`, private flatten `184/184`, passes `5,759/5,759`, the full suite `9,220/9,220`, and `moon info` with 11 existing warnings and 0 errors. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. Both red-first failures were the expected unbound helper errors. No `.mbti`, semantic family, output shape, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed, so no new Binaryen probe was required.

The multivalue flow-index iteration passes focused flatten `245/245`, private flatten `186/186`, passes `5,761/5,761`, the full suite `9,222/9,222`, and `moon info` with 11 existing warnings and 0 errors. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. Both red-first failures were the expected unbound helper errors. No `.mbti`, semantic family, output shape, registry, dispatcher, CLI execution, compare/API, generator, or preset surface changed, so no new Binaryen probe was required.

The reversed-binary behavior iteration passes focused flatten `247/247`, private flatten `186/186`, passes `5,763/5,763`, the full suite `9,224/9,224`, and `moon info` with 11 existing warnings and 0 errors. Both red-first fixtures failed because `flatten_run` returned unchanged. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. Fresh v130 runs of the existing multivalue/tuple binary probes and the scalar reversed-binary probe confirm the source rule: payload or tuple extraction precedes the condition, `br_if` becomes payload-free, and the binary stays after the branch with its simple opposite operand unchanged.

The rich-right behavior iteration passes focused flatten `249/249`, private flatten `186/186`, passes `5,765/5,765`, the full suite `9,226/9,226`, and `moon info` with 11 existing warnings and 0 errors. Both red-first fixtures failed because `flatten_run` returned unchanged. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. Fresh v130 legacy-try, tuple, and inputful-loop probes confirm rich right calls are materialized after payload-free `br_if` and before their binary consumers. No `.mbti` or public surface changed.

The rich-left behavior iteration passes focused flatten `251/251`, private flatten `186/186`, passes `5,767/5,767`, the full suite `9,228/9,228`, and `moon info` with 11 existing warnings and 0 errors. The red-first fixtures failed unchanged at `249/250` and `250/251`. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. Fresh v130 independently scalar and tuple-oriented probes confirm the source order: the rich left call precedes payload calls and `br_if`, while the binary executes only after the branch is not taken. No `.mbti` or public surface changed.

The inputful-loop rich-left iteration passes focused flatten `253/253`, private flatten `186/186`, passes `5,769/5,769`, the full suite `9,230/9,230`, and `moon info` with 11 existing warnings and 0 errors. The red-first fixtures failed unchanged at `251/252` and `252/253`. Targeted formatting, `git diff --check`, pinned-owner verification, and public-wiring absence checks pass. A fresh v130 loop probe confirms `call $left` before the complete payload vector and `br_if`, with `i32.sub` after the branch. No `.mbti` or public surface changed.

The inputful-loop rich-payload iteration passes focused flatten `255/255`, private flatten `186/186`, passes `5,771/5,771`, the full suite `9,232/9,232`, and `moon info` with 11 existing warnings and 0 errors. The red-first fixtures failed unchanged at `253/254` and `254/255`; the tuple slice then caught and corrected payload-before-left prelude order. Targeted formatting, `git diff --check`, pinned-owner verification, no-`.mbti` review, and public-wiring absence checks pass. The fresh v130 probe orders `call $left`, `call $payload_i32`, the remaining payload, condition, and `br_if`, then retains `i32.sub`. No public surface changed.

The legacy-try rich-payload iteration passes focused flatten `257/257`, private flatten `186/186`, passes `5,773/5,773`, the full suite `9,234/9,234`, and `moon info` with 11 existing warnings and 0 errors. The red-first fixtures failed unchanged at `255/256` and `256/257`. Targeted formatting, `git diff --check`, pinned-owner verification, no-`.mbti` review, and public-wiring absence checks pass. Fresh v130 scalar and tuple-oriented probes order `call $left`, the payload calls, condition, and `br_if`, then retain `i32.sub`. No public surface changed.

The multiple-rich-payload iteration passes focused flatten `259/259`, private flatten `186/186`, passes `5,775/5,775`, the full suite `9,236/9,236`, and `moon info` with 11 existing warnings and 0 errors. The red-first fixtures failed unchanged at `257/258` and `258/259`. Targeted formatting, `git diff --check`, pinned-owner verification, no-`.mbti` review, and public-wiring absence checks pass. Four fresh v130 probes—legacy try and inputful loop, heterogeneous independently scalar and homogeneous tuple-oriented—order `call $left`, every payload call in vector order, condition, and `br_if`, then retain `i32.sub`.

The structured-subtree iteration passes focused HOT mutation `11/11`, IR `319/319`, focused flatten `260/260`, private flatten `186/186`, passes `5,776/5,776`, the full suite `9,238/9,238`, and `moon info` with 11 existing warnings and 0 errors. The first red test failed on unbound APIs; the second failed unchanged at `259/260`. Targeted formatting, `git diff --check`, pinned-owner verification, `.mbti` review, and public-wiring absence checks pass. The IR `.mbti` adds only `hot_delete_detached_control_subtree` and `hot_label_is_live`. The fresh v130 probe retains the dead block under direct flatten at `76` encoded bytes; matched `--vacuum --dce` removes it and encodes at `63` bytes.

The structured-if/loop iteration passes focused flatten `262/262`, private flatten `186/186`, passes `5,778/5,778`, the full suite `9,240/9,240`, and `moon info` with 11 existing warnings and 0 errors. The red-first if fixture failed unchanged at `260/261`; the red-first loop fixture failed unchanged at `261/262`. `moon fmt`, `git diff --check`, no-`.mbti` review, pinned-owner verification, and public-wiring absence checks pass. Fresh v130 direct probes retain both dead controls at `76` encoded bytes each; matched `--vacuum --dce` removes each and encodes at `63` bytes.

The structured-forest iteration passes HOT mutation `12/12`, IR `320/320`, focused flatten `263/263`, private flatten `186/186`, passes `5,779/5,779`, the full suite `9,242/9,242`, and `moon info` with 11 existing warnings and 0 errors. The first red-first test failed because `hot_delete_detached_control_forest` was unbound; the second failed unchanged at `262/263`. `moon fmt`, incidental `moon.mod` restoration, `git diff --check`, `.mbti` review, pinned-owner verification, and public-wiring absence checks pass. The fresh v130 block + if + loop probe is retained by direct flatten at `76` encoded bytes and removed by matched `--vacuum --dce` at `63` bytes.

The mixed-forest iteration passes HOT mutation `13/13`, IR `321/321`, focused flatten `263/263`, private flatten `186/186`, passes `5,779/5,779`, the full suite `9,243/9,243`, and `moon info` with 11 existing warnings and 0 errors. The first red-first test failed because `hot_delete_detached_forest` was unbound; the second failed unchanged at `262/263`. `moon fmt`, incidental `moon.mod` restoration, `git diff --check`, `.mbti` review, pinned-owner verification, and public-wiring absence checks pass. The fresh v130 `drop(const) + block + if + loop + unreachable` probe is retained by direct flatten at `76` encoded bytes and removed by matched `--vacuum --dce` at `63` bytes.

## Scalar and ordered two-lane catch-payload repair

Commits `55e508e83` and `864d32c06` implement the first scalar Binaryen v130 `handleBlockNestedPops(...)` subset. Commits `43ea95972` and `49079c0dc` extend it to one exact ordered two-lane same-tag family. The generic HOT planner still preflights the complete function. The scalar path retains its exact first-descendant rule. The two-lane path requires two childless leading markers, one subsequent exclusive direct block chain, and final-region roots zero and one whose unary paths end at the corresponding lanes with exactly the entry plus old-position uses. A partial second lane, mixed tag, third lane, loop, nested exceptional control, catch-all marker, sharing, outside use, or alternate ancestry rejects before mutation.

After complete preflight, HOT allocates locals in source lane order, replaces both old positions with matching local reads, and inserts captures in reverse lane order so the implicit handler stack is consumed correctly. The new red-first IR tests first failed on unbound ordered APIs, then passed the positive `i32`/`f32` capture plus a partial-second-lane outside-use negative that preserves revision, nodes, locals, roots, region roots, and edges.

Internal flatten applies either admitted transaction before ordinary operand/control rewriting, discards the stale state, rebuilds all immutable branch/use/type/flatness proof snapshots, and continues. HOT lowering accepts at most two same-tag lanes; exactly two require an existing result-only multivalue block type. The red-first whitebox fixture initially remained `DeferredCatchPayloadRepair`, then locked local/capture/read order, nested blocks, the enclosing branch, HOT verification, lowering, and module validation for a tag `(param i32 f32)`.

Validation is HOT mutation `16/16`, IR `326/326`, focused flatten `263/263`, whitebox flatten `188/188`, passes `5,781/5,781`, full `9,250/9,250`, `moon fmt`, and `moon info` with 11 existing warnings. The reviewed IR `.mbti` adds only `hot_ordered_catch_payloads_repairable(...)` and `hot_repair_ordered_catch_payloads(...)`; the lower/pass slice changes no API snapshot and no public flatten API is exposed. No performance lane was rerun; the durable representative remains `970.5 us`, or `3.65x` Binaryen v130. Registry, dispatcher, CLI execution, compare/API, generator/profile, presets, and public pass wiring remain absent.

## Direct rethrow and delegate subsets

Pinned Binaryen v130 source separates exceptional transfer from nested-pop relocation. `Flatten.cpp` does not special-case `Rethrow` or delegate; `eh-utils.cpp` searches and relocates only first-descendant legacy `Pop`; `wasm.h` stores delegation as `Try::delegateTarget`. The local implementation therefore preserves the exact transfer owner rather than routing these forms through payload repair.

Commit `d76a91b3f` admits one catch-all `Rethrow(0)` under a strict direct block ancestry owned by the active catch. HOT lowering converts it to modern Core EH by changing the synthetic handler to `catch_all_ref`, capturing the exception reference in one nullable `exnref` scratch local, and emitting `local.get` plus `throw_ref`. The red-first whitebox fixture failed at `188/189`, then passed and validated the lowered module. At that checkpoint nonzero depth, nested-catch ownership, multiple rethrows, and non-direct ancestry remained deferred; later sections supersede those blanket boundaries, while typed-payload composition, loops, nested try-body transfers, and broader ownership remain open.

Commit `7bfb10372` admits one resultless delegated try whose catch region contains exactly one `Delegate` targeting the directly enclosing outer try through a strict block ancestry. The inner try label must have no users, and the outer target must remain active during lowering. The modern lowerer omits only the obsolete inner legacy handler shell, leaving its body in the outer try's active context so exceptions propagate to that handler. The red-first fixture failed at `189/190`, then passed with the target immediate unchanged, HOT verification, modern EH output, and module validation. Non-direct targets, value-carrying delegated tries, mixed catch/delegate regions, used inner labels, and broader nested populations remain deferred.

Final iteration validation is HOT lower `89/89`, IR `326/326`, focused flatten `263/263`, whitebox flatten `190/190`, passes `5,783/5,783`, full `9,252/9,252`, `moon fmt`, and `moon info` with 11 existing warnings. No `.mbti`, public registry, dispatcher, CLI execution, compare/API, profile, preset, or scheduler surface changed. Performance was not remeasured; `970.5 us` / `3.65x` remains the durable checkpoint.

## Multiple and nested-if catch rethrows

Commits `3c819922c` and `41533e603` supersede the one-rethrow and strict block-only boundaries in the direct exceptional-transfer section above. Lowering now allocates one nullable `exnref` scratch local whenever an active catch has any positive admitted rethrow count. The handler captures the exception once through `catch_all_ref`; every rethrow reads the same local and emits `throw_ref`. Admission still proves every node independently as depth zero and owned by that active catch.

One path may additionally cross at most one resultless `if` whose label has no branch users. Direct blocks may appear inside or outside that if. Structural-parent proof records then/else roots but not the condition, and lowering mirrors that exact boundary by scanning arm regions only. Loops, nested try/catch regions, nested-if chains, value-carrying ifs, and targeted ifs remain outside the admitted family.

The multiple-rethrow fixture was red at `192/193` because lowering aborted above one rethrow, then green at `193/193`. The nested-if fixture was red at `193/194` because admission rejected the arm ancestry, then green at `194/194`. Final validation is HOT lower `89/89`, IR `326/326`, focused flatten `263/263`, whitebox flatten `194/194`, passes `5,787/5,787`, full `9,256/9,256`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public flatten surface changed; performance was not remeasured.

## Strict rethrow and delegate ancestry

Commits `9c237165d` and `88197c97e` complete the next exact two-code behavior iteration. The first removes the temporary single-if counter from rethrow admission. Every direct-parent step must still be either a direct block or a resultless if whose label has no users, and the final current root must still belong to the exact active catch. The existing lowerer already visits block bodies and then/else regions recursively, so arbitrary strict direct block/if chains lower without entering conditions, loops, or nested try/catch regions. The two-if/interleaved-block fixture was red at `194/195` and green at `195/195`.

The second widens only the delegated try's catch representation. Admission walks from the delegate through resultless single-root blocks with unused labels, requires that resulting root to be the sole catch root of one resultless inner try, retains the unused inner-label proof, and keeps the exact active outer target/direct outer ancestry proof. Lowering walks the same catch chain before removing the semantically empty wrappers and obsolete inner handler shell. Ifs, loops, mixed catch roots, value-carrying blocks or tries, and used labels remain excluded. The two-block catch-chain fixture was red at `195/196` and green at `196/196`.

Final validation is HOT lower `89/89`, IR `326/326`, focused flatten `263/263`, whitebox flatten `196/196`, passes `5,789/5,789`, full `9,258/9,258`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; `970.5 us` / `3.65x` Binaryen remains the durable checkpoint.

## Arbitrary ordered lanes and scalar if-condition first descendants

Commits `82c4c260c` and `95f71db1e` complete the next exact two-code behavior iteration. The first supersedes the temporary third-lane boundary: `hot_ordered_catch_payloads_repairable(...)` and `hot_repair_ordered_catch_payloads(...)` now accept any positive same-tag vector when every marker is a leading catch root, one subsequent exclusive direct block chain reaches ordered unary lane-use roots, and each payload has exactly its entry plus old-position ownership. Locals retain source order, captures consume the handler stack in reverse order, and HOT lowering resolves any matching arity through an existing result-only multivalue block type. The red-first three-lane `i32`/`f32`/`i64` fixture failed at `190/191` on admission, then validated capture/read order and the lowered module.

The second follows pinned Binaryen v130 `EHUtils::getFirstPop(...)` through an `if` condition on the scalar first-descendant line. It does not enter then or else bodies. The red-first nested-if fixture failed at `191/192`, then passed with one entry local and an exact condition `local.get`. Final validation is IR `326/326`, focused flatten `263/263`, whitebox flatten `192/192`, passes `5,785/5,785`, full `9,254/9,254`, `moon fmt`, and `moon info` with 11 existing warnings. No `.mbti` or public flatten execution surface changed, and performance was not remeasured.

## Strict outer delegate ancestry and ordered first-child lane paths

Commits `0800efc79` and `57013d100` complete the next exact two-code behavior iteration. The first combines the already admitted strict catch-block-chain representation with a strict direct outer chain of resultless, unused-label, single-root blocks and resultless untargeted if arms. Admission keeps the delegate's exact target try active, rejects value-carrying controls, used labels, shared/non-root arms, loops, nested tries, and non-active targets, and preserves the surrounding block/if structure during lowering. The new fixture was red at whitebox `196/197` because the second outer step was an if, then green at `197/197` with validating `try_table`, `catch_all`, `if`, and throw output.

The second follows the pinned `EHUtils::getFirstPop(...)` contract for every ordered catch lane instead of imposing a local unary-only restriction. A lane may traverse the first child of a binary or other non-control expression while later children remain untouched. Exact leading same-tag markers, entry plus old-position ownership, one exclusive direct block chain, lane order, reverse stack capture order, and loop/try/try-table rejection remain mandatory. The two-lane binary fixture was red at `197/198` on admission, then green at `198/198`; the repaired payloads occupy the left children, the right constants retain identity, and lowering validates.

Final validation is HOT mutation `16/16`, HOT lower `89/89`, IR `326/326`, focused flatten `263/263`, whitebox flatten `198/198`, passes `5,791/5,791`, full `9,260/9,260`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; `970.5 us` / `3.65x` Binaryen remains the durable checkpoint.

The prior direct-outer delegate boundary and ordered-unary-lane blocker are superseded. Remaining delegate blockers are non-strict/non-direct ancestry, loops, nested tries, value-carrying or targeted controls, mixed catch populations, used labels, and non-active targets. The blanket independent-lane and value-carrying-if rethrow blockers recorded here are partially superseded by the next section.

## Independent direct catch roots and scalar value-if rethrow

Commits `fb9d071e8` and `52fc64b49` complete the next exact two-code behavior iteration. The first keeps the existing compact common block-chain lane representation but also admits a direct catch-region layout when the first ordered post-marker roots independently own the payload lanes. Each lane reuses the exact first-child walk and entry-plus-old-position ownership proof. The fixture combines a binary lane, an if-condition lane, and a later untouched root; it was red at `198/199` and green at `199/199`.

The second admits one direct `Rethrow(0)` as the terminal arm of a defaultable scalar value `if`. The opposite arm must end in one matching Flat-IR-simple scalar value, the if label must be unused, and the if must be directly rooted in the active catch. Scalar-if routing writes only the fallthrough arm; surrounding scalar legacy-try routing then erases the try result. The potentially throwing call fixture was red at `199/200` and green at `200/200`, lowered through the existing captured-exception `catch_all_ref`/`throw_ref` route, and validated.

Final validation is HOT mutation `16/16`, HOT lower `89/89`, IR `326/326`, focused flatten `263/263`, whitebox flatten `200/200`, passes `5,793/5,793`, full `9,262/9,262`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; `970.5 us` / `3.65x` Binaryen remains the durable checkpoint.

Remaining catch-payload blockers include partial/mixed tags, non-leading or non-first-descendant/repeated uses, direct-root composition outside the exact first ordered roots, then/else paths, nested catches, loops/multiple execution, sharing/outside ownership, and catch-all extraction. Remaining rethrow blockers include nonzero/typed-composed ownership, loops, nested catches/tries, targeted ifs, non-simple opposite arms, multivalue results, and broader value-carrying ancestry.

## Direct nested catch rethrow depths

Commits `1ac52d9fa` and `23f9ba164` supersede the blanket nonzero-depth and nested-catch rethrow blockers only for one exact catch-all subset. HOT lowering now maintains one optional exception-reference slot for every active legacy catch, rather than storing only catches that happened to need a local. A catch is converted to `catch_all_ref` and assigned a nullable `exnref` scratch local when a rethrow in its catch subtree targets that catch; nested catches without such a target retain an explicit empty slot. `Rethrow(depth)` indexes the active slot stack from the innermost catch and emits the exact captured `local.get` plus `throw_ref`.

At that checkpoint, internal flatten admitted any positive depth when the rethrow was a direct catch root and every depth step was another direct `Try` root in its enclosing catch. Every owner had to be resultless, markerless, and catch-all; block or if wrappers remained excluded until the next section. Typed payload markers, value-carrying tries, loops, try-body rethrows, and mixed exceptional populations remain deferred. The depth immediate is unchanged by flatten.

The lowering test was red at `89/90` because depth one still hit the direct-active-catch abort, then green at `90/90`. The flatten test was red at whitebox `200/201` with `DeferredExceptionalTransferRepair`, then green at `201/201` for a depth-two chain. Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `201/201`, passes `5,794/5,794`, full `9,264/9,264`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public pass surface changed. Performance was not remeasured; `970.5 us` / `3.65x` Binaryen v130 remains the durable checkpoint.

## Strict nested-catch block/if wrapper ancestry

Commits `70280e159` and `1fc7c6077` supersede the direct-root-only nested-catch policy for one strict control subset. `flatten_rethrow_catch_root_through_strict_ancestry(...)` first walks a chain of resultless unused-label blocks whose body contains the current root exactly once. The second slice extends that same walk through resultless unused-label `if` nodes when exactly one selected arm contains the current root as its sole root and the opposite arm does not share it. The walk repeats independently before matching every markerless resultless catch-all owner at the requested rethrow depth.

The block-only fixture was red at whitebox `201/202` because the first wrapper prevented catch-owner matching, then green at `202/202`. The mixed block/if fixture was red at `202/203`, then green at `203/203`. Both preserve `imm0 = 1`, verify HOT, lower through the existing exact target-catch `catch_all_ref` slot plus `throw_ref`, and validate.

Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `203/203`, passes `5,796/5,796`, full `9,266/9,266`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; `970.5 us` / `3.65x` Binaryen v130 remains the durable checkpoint.

The old blanket block/if-wrapper blocker is superseded only for this strict direct resultless unused-label single-root ancestry. Typed-composed rethrows, targeted or value-carrying wrappers, multi-root selected paths, loops, nested try-body rethrows, mixed transfers, and broader nested exceptional ownership remain deferred.

## Exact opposite-arm targeted catch-if exits

Commits `c90bed031` and `8529deb42` supersede the prior blanket targeted-if wrapper rejection only for one exact resultless ancestry shape. Every targeted if has one root in each arm and exactly one indexed user of its own label. One selected arm exclusively owns the current rethrow or nested try; the sole opposite-arm root is the label user and is directly owned by the if. Code 1 admits a payloadless plain `br` to the if label. Code 2 admits a payloadless `br_if` to the same label only when its one condition is already-simple scalar `i32`.

The first behavior fixture was red at whitebox `203/204` with `DeferredExceptionalTransferRepair`, then green at `204/204`. The second was red at `204/205`, then green at `205/205`. Both preserve `Rethrow(1)`, branch target identity, selected-arm ownership, and the exact enclosing catch slot; the conditional fixture also preserves the condition node. HOT verifies, modern lowering retains the if/branch and uses the existing outer `catch_all_ref` capture plus `throw_ref`, and both modules validate.

Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `205/205`, passes `5,798/5,798`, full `9,268/9,268`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. The pinned source hashes remain unchanged. Performance was not remeasured; the durable checkpoint remains `970.5 us`, or `3.65x` Binaryen v130.

Multiple or outside label users, payload-bearing `br`/`br_if`, same-arm target users, missing/multi-root opposite arms, value-carrying or multivalue wrappers, loops, typed composition, nested try-body rethrows, mixed transfers, and broader targeted ownership remain deferred before mutation. The blanket rich-condition blocker is superseded only for one exact one-use supported scalar `i32` origin; shared, control, tee, non-`i32`, and otherwise unsupported conditions remain deferred.

## Rich targeted conditions and interleaved direct catch roots

Commit `61fb9919b` composes the existing targeted-if ownership proof with ordinary Flat IR scalar operand routing. A payloadless opposite-arm `br_if` condition may be rich only when it is exactly scalar `i32`, has one reachable pre-mutation use, and is a supported non-control, non-tee origin. The input condition node becomes the child of an arm-local `local.set`; the original branch remains the next root with a matching `local.get`. Target identity, selected-arm ownership, `Rethrow(1)`, and the exact outer catch slot remain unchanged. The fixture was red at whitebox `205/206` with `DeferredExceptionalTransferRepair` and green at `206/206`.

Commit `1fb8f64ec` extends the ordered same-tag direct-root catch transaction from the exact first-N layout to ordered lane roots with unrelated roots interleaved. The planner searches only forward after the leading markers. A candidate lane still needs the marker's exact entry-root use plus one exact first-child nested use and exclusive ancestry; skipped and later roots retain identity. The fixture was red at `206/207` with `DeferredCatchPayloadRepair` and green at `207/207`.

Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `207/207`, passes `5,800/5,800`, full `9,270/9,270`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. The pinned source hashes remain unchanged. Performance was not remeasured; the durable checkpoint remains `970.5 us`, or `3.65x` Binaryen v130.

## Block-wrapped targeted exits and scalar typed rethrows

Commit `404252e63` admits one exact structural wrapper around the existing targeted catch-if opposite exit. The if still has one root per arm and one indexed target user; the selected arm still owns the current rethrow or nested try; the opposite arm may now own one resultless unused-label block with exactly one body root, the existing payloadless `br` or `br_if`. A rich scalar `i32` condition is materialized inside that block immediately before the branch. The behavior test was red at whitebox `207/208` with `DeferredExceptionalTransferRepair` and green at `208/208`.

Commit `a24539c99` supersedes the blanket typed-composed-rethrow blocker only for one scalar/direct/single-rethrow family. Whole-function catch-payload repair must prove one leading childless scalar marker and its exact first-descendant use. The same catch may then own exactly one direct `Rethrow(0)`. Lowering selects `catch_ref`, requires an existing result-only `[payload, exnref]` handler type, stores the top exception reference before the repaired catch body captures the payload, and lowers the rethrow through `throw_ref`. Distinct `i32`/nullable-exnref locals and module validation prove the handler stack order. The behavior test was red at `208/209` with `DeferredExceptionalTransferRepair` and green at `209/209`.

Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `209/209`, passes `5,802/5,802`, full `9,272/9,272`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; the durable checkpoint remains `970.5 us`, or `3.65x` Binaryen v130.

The old blanket typed-composition and direct-opposite-root statements are therefore superseded only for these exact subsets. Multiple payload lanes, multiple typed rethrows, non-direct or nonzero typed rethrows, typed wrappers/nesting/loops/value controls, deeper or targeted opposite blocks, and all other readiness gates remain open.

## Typed payload vectors and strict targeted block chains

Commit `740dfa2b5` supersedes the scalar-only direct typed-rethrow subset for any positive same-tag scalar payload vector that the existing whole-function catch repair transaction can own. The two-lane fixture uses distinct `i32` and `f32` lanes. Admission was red at whitebox `209/210` with `DeferredExceptionalTransferRepair`; after implementation it is green at `210/210`. Lowering requires an existing `[i32, f32, exnref]` result-only handler type, emits `catch_ref`, stores the top exception reference before catch-body repair captures `f32` then `i32`, preserves source-order payload locals, emits `throw_ref`, and validates the module.

Commit `374040a26` supersedes the one-block targeted-if opposite-exit ceiling with an arbitrary positive strict block chain. The two-block fixture keeps both wrappers resultless, single-root, directly owned, and unused as branch targets. The if label still has one indexed user, and the rich `i32.add` condition is materialized immediately before `br_if` in the innermost block. Admission was red at `210/211` with `DeferredExceptionalTransferRepair`; after implementation it is green at `211/211` and the lowered module validates.

Final validation is HOT mutation `16/16`, HOT lower `90/90`, IR `327/327`, focused flatten `263/263`, whitebox flatten `211/211`, passes `5,804/5,804`, full `9,274/9,274`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; the durable checkpoint remains `970.5 us`, or `3.65x` Binaryen v130.

The earlier one-scalar-payload and one-block blocker statements are superseded only for these exact vector and strict-chain subsets. Mixed tags, multiple typed rethrows, non-direct/nonzero/wrapped/nested/loop typed transfers, missing payload-vector-plus-exnref handler types, and multi-root/used-label/targeted/value-carrying/loop/try-like or otherwise non-strict wrappers remain open.

## Typed rethrow block chains and selected catch-if delegates

Commit `b7b85a8bf` supersedes the direct-only ancestry requirement for the exact typed depth-zero rethrow family. A positive leading same-tag scalar payload vector still needs complete whole-function repair ownership and exactly one `Rethrow(0)`, but that rethrow may now sit under any positive chain of resultless unused-label single-root blocks. The red-first two-lane fixture returned `DeferredExceptionalTransferRepair` at whitebox `211/212`; after implementation it passes `212/212`, retains both source-order locals, reverse payload captures, the wrapper root, and `imm0 = 0`, then lowers through `catch_ref` and `throw_ref` to a valid module.

Commit `3595d6563` adds one exact catch-side delegate representation. A resultless delegated try may own a sole catch root formed by any positive then/else-selected chain of resultless unused-label ifs when every selector is an exact `i32.const`, every arm has one root, the selected root continues toward the delegate, and every opposite root is a childless `nop`. Admission and `hot_lower_impl_direct_delegate_target(...)` use the same proof. The red-first two-if fixture returned `DeferredExceptionalTransferRepair` at `212/213`; after implementation it passes `213/213`, retains the HOT if chain and exact target, and lowers the proven representation transparently to a valid module.

Final validation is IR `327/327`, focused flatten `263/263`, whitebox flatten `213/213`, passes `5,806/5,806`, full `9,276/9,276`, `moon fmt`, and green `moon info` with 11 existing warnings. No `.mbti` or public registry, dispatcher, CLI execution, compare/API, profile, preset, scheduler, or neighborhood-ready surface changed. Performance was not remeasured; the durable checkpoint remains `970.5 us`, or `3.65x` Binaryen v130.

The earlier direct-only typed-rethrow and catch-block-only delegate statements are superseded only for these exact subsets. Value-carrying, targeted, used-label, multi-root, if/loop/try-like or otherwise non-strict typed wrappers remain open. Delegate catch-if representations with nonconstant or effectful selectors, missing else arms, richer opposite roots, targeted or used labels, value results, loops, nested tries, mixed catches, or non-active targets also remain open.

## Classification and remaining blockers

- **Measured Starshine wins:** nonthrowing synthetic catch-all bridge/control/local output is 24 aggregate bytes smaller than Binaryen after matched cleanup, with deterministic runtime agreement. The exact structured block, constant-condition if, and zero-input/no-backedge loop suffixes remove complete unreachable control subtrees plus owned labels immediately; each Binaryen-retained direct probe shrinks `76 -> 63` bytes under matched `--vacuum --dce`, confirming the deleted families are cleanup debris under unconditional transfer.
- **Performance movement:** run-wide suffix, EH prerequisite, Flat IR classification, effective-terminal, scalar-try, label-use, exact terminal-table, scalar/multivalue exact branch-target caches, constant-time branch-user append, shared admission rosters, dedicated postorder branch dispatch, lightweight reachable ownership counts, shared-root-only sequence storage, binary shared-root pair lookup, single-target direct staging, cached inputful-loop support, batched detached deletion, in-place value-tail replacement, in-place suffix truncation, flat pooled table targets, lazy structural parents, reusable generation marks, depth-indexed prelude buffers, direct target-local retrieval, dense lazy type-result caching, marked multivalue payload distinctness/root exclusion, direct payload-slot indexing, marked tuple flow-slot distinctness, exact legacy-try/inputful-loop branch indexing, region-tail/tuple-entry/tuple-branch count proof, cached conditional-flow sites, sparse proof storage, sparse binary lookup, and binary terminal-payload membership reduce repeated immutable scans, generic router attempts, redundant one-target local copies, region rebuilds, full use-def/use-site allocation, quadratic target deduplication, nested payload-edge and flow-slot scans, linear payload/cache/pair scans, nested result-vector allocation, unconditional root bookkeeping, and per-root/per-query scratch allocation. The newest 512-candidate owner medians improve non-tuple flow indexing `97.92%` and tuple slot distinctness `97.71%`. The prior stable representative median remains `970.5 us`, outside the `<=2x` Binaryen target, and has not been requalified.
- **Behavior movement:** direct `i32.mul`, `i32.and`, `i32.or`, `i32.xor`, `i32.shl`, `i32.shr_s`, and `i32.shr_u` call roots now use the same recursive complete-ownership proof; `i32.rotl` remains the tested outside-roster boundary. Independently scalar and exclusively tuple-made multivalue conditional lanes may occupy either side of one immediate same-typed binary with a simple opposite operand. When the payload is left, one exact one-use rich right operand is also admitted and remains after the branch; the shared helper extends the exact inputful-loop family too. Legacy tries and inputful loops admit the same independently scalar or exclusively tuple-made pre-branch rich-left rule with any number of individually supported rich payload origins. Exact untargeted, unshared void block, constant-condition complete-arm void if, and zero-input/no-backedge void loop roots now delete together with every owned label after unconditional scalar legacy-try table transfer, either singly, in a positive control-only vector, or in a vector mixed with ordinary roots that already pass exact dead-suffix recognition.
- **Validation failure:** none observed.
- **True semantic mismatch:** none observed in the measured probes.
- **Durable representation/repair boundary:** ordered childless scalar `Catch` entry values carry exact tag/type. Lowering accepts one scalar lane or any positive same-tag lane vector through scalar/result-only multivalue handler types. Internal flatten repairs scalar first-descendant paths through direct blocks and an `if` condition, arbitrary ordered common-block-chain vectors, and ordered independent direct catch-region roots with unrelated roots before or between lane roots; every lane may follow exact first-child non-control expression paths through entry locals. It preserves any positive depth-zero catch-all rethrow population under one active catch through one `catch_all_ref` capture and repeated captured-exnref/`throw_ref` uses; each path may include an arbitrary strict direct chain of resultless untargeted if arms and blocks, or one direct scalar value-if arm with a matching simple opposite value. It now also preserves any positive nonzero depth through an exact chain of markerless resultless catch-all owners with optional strict resultless unused-label single-root block/selected-if ancestry or the exact targeted resultless if subset whose sole opposite-arm label user is a payloadless plain `br` or payloadless `br_if` with either a simple scalar `i32` condition or one exact one-use supported rich scalar `i32` origin, with one optional lowering slot per active catch. One exact typed catch may also combine any positive whole-function-repairable same-tag scalar payload vector with one `Rethrow(0)` reached directly or through any positive strict resultless unused-label single-root block chain; `catch_ref`, the payload-vector-plus-exnref handler channel, reverse handler-stack capture into source-order locals, and `throw_ref` remain exact. One resultless outer-target delegate may be direct, end a strict direct resultless single-root unused-label catch-block chain, or follow an exact constant-selected resultless unused-label catch-if chain with one root per arm and a childless opposite `nop`; the delegated try may reach its exact active target through an arbitrary strict direct chain of resultless untargeted single-root blocks and if arms. Every unsupported population still selects the matching deferred admission before ordinary mutation.
- **Still gated:** partial or mixed-tag payload vectors, non-leading/non-first-descendant or repeated payload uses outside the admitted condition/first-child paths, reverse or ambiguous independent-lane order and broader nested/direct-root composition, then/else payload ownership, nested typed catches, loops/multiple execution, catch-all payload extraction, sharing/outside ownership, typed composition beyond the exact positive same-tag scalar-payload-vector/one-depth-zero-`Rethrow(0)` direct-or-strict-unused-block-chain subset, nonzero rethrow through targeted wrappers outside the exact opposite-arm payloadless direct-or-strict-unused-block-chain plain/simple-or-one-use-rich-conditional exit subset, value-carrying, multi-root, loop, used-label, targeted, try-like, or other non-strict wrappers, rethrow in loops or nested try bodies, targeted ifs outside the exact opposite-arm subset, shared/control/tee/non-`i32` conditions, multivalue, non-simple-opposite-arm, or broader value-carrying rethrow ifs, loop/mixed/non-strict catch delegate ancestry, catch-side delegate ifs with nonconstant/effectful selectors, missing else arms, richer opposite roots, targeted/used labels, value results, loops, nested tries, mixed catches, or non-active targets, and other non-active-target/value-carrying/mixed/used-label/nested delegate populations outside the admitted strict chains, multiple or non-lane-zero rich-left/payload-right flow, nonconstant/effectful or partial/richer if suffixes, inputful/value loops or loop backedges, try-like structured roots, unsupported mixed-root combinations, shared descendants or external label users, broader mixed/shared/nested branch/control families, result calls, indirect/reference calls, and public execution.
- **Public signoff:** not run. No flatten GenValid aggregate or four-lane compare surface exists, and public wiring remains intentionally removed.

## Next work

1. continue behavior-first implementation: extend catch repair beyond the admitted scalar condition, common-block vectors, first ordered independent direct roots, and ordered interleaved direct roots with a source-backed non-first-descendant or repeated-use policy, broader nested/direct-root composition, selected-arm policy, or another exact payload family; widen rethrow beyond positive depth-zero strict ancestry, the direct scalar value-if arm, and the exact markerless resultless nested-catch chain with strict block/if wrappers plus exact direct-or-one-unused-block payloadless opposite-arm exits and the exact scalar typed direct-rethrow subset into broader typed composition, loops, nested try bodies, targeted/value-carrying/multi-root wrappers, targeted/multivalue value controls, or broader value-if chains only with exact proof; widen delegate beyond the admitted direct/strict-block/exact constant-selected catch representations and strict outer block/if chains only with matching admission/lowering proof; then close broader nested/loop-safe EH handling, richer structured roots, or another bounded mixed/shared/nested control family; retain whole-function failure atomicity for every unsupported population;
2. retain whole-function pre-mutation failure atomicity and direct v130 probes for every semantic/output family; do not weaken ownership, type, effect, trap, label, or current-structure gates to widen coverage;
3. defer millisecond-scale micro-optimization unless a measured pass-local regression or the final public `<=2x` gate requires it. The maintained skipped sequenced-root fixture still encodes the old linear baseline and must be deliberately updated or reclassified before any reuse; the durable `970.5 us` checkpoint remains unrequalified;
4. add the flatten-specific GenValid aggregate, four-lane signoff, ordered neighborhood proof, final docs closeout, and only then public registry/dispatcher/CLI/compare/API/preset wiring.
