---
kind: concept
status: supported
last_reviewed: 2026-07-18
sources:
  - ../../../raw/research/1625-2026-07-16-daeo-terminal-call-argument-final-matrix.md
  - ../../../raw/research/1624-2026-07-16-daeo-pair-result-final-matrix.md
  - ../../../raw/research/1623-2026-07-16-daeo-single-traversal-readback-final-matrix.md
  - ../../../raw/research/1622-2026-07-16-daeo-pair-store-transport-final-matrix.md
  - ../../../raw/research/1621-2026-07-15-daeo-post-remap-transport-final-matrix.md
  - ../../../raw/research/1620-2026-07-15-daeo-remapped-carrier-cleanup-final-matrix.md
  - ../../../raw/research/1617-2026-07-15-daeo-sibling-terminal-transport-final-matrix.md
  - ../../../raw/research/1616-2026-07-15-daeo-type309-call-transport-final-matrix.md
  - ../../../raw/research/1615-2026-07-15-daeo-terminal-local-sinking-final-matrix.md
  - ../../../raw/research/1614-2026-07-15-daeo-selected-uniform-null-final-matrix.md
  - ../../../raw/research/1613-2026-07-15-daeo-selected-aggregate-spill-final-matrix.md
  - ../../../raw/research/1612-2026-07-15-daeo-selected-copy-readback-final-matrix.md
  - ../../../raw/research/1611-2026-07-15-daeo-branch-deadtee-compaction-final-matrix.md
  - ../../../raw/research/1610-2026-07-15-daeo-post-component-vacuum-final-matrix.md
  - ../../../raw/research/1609-2026-07-14-daeo-post-component-coalesce-final-matrix.md
  - ../../../raw/research/1608-2026-07-14-daeo-grouped-signature-reuse-final-matrix.md
  - ../../../raw/research/1607-2026-07-14-daeo-forwarding-component-final-matrix.md
  - ../../../raw/research/1606-2026-07-14-daeo-dead-callee-final-matrix.md
  - ../../../raw/research/1605-2026-07-14-daeo-computed-carrier-root-cause.md
  - ../../../raw/research/1604-2026-07-14-daeo-forwarded-cycle-final-matrix.md
  - ../../../raw/research/1603-2026-07-14-daeo-forwarded-cycle-precompute.md
  - ../../../raw/research/1602-2026-07-14-daeo-payoff-local-order-final-matrix.md
  - ../../../raw/research/1601-2026-07-14-daeo-payoff-local-order-single-scan.md
  - ../../../raw/research/1600-2026-07-14-daeo-payoff-type-stable-local-order.md
  - ../../../raw/research/1599-2026-07-14-daeo-adjacent-local-order-final-matrix.md
  - ../../../raw/research/1598-2026-07-14-daeo-adjacent-local-order-prefilter.md
  - ../../../raw/research/1597-2026-07-14-daeo-adjacent-type-stable-local-order.md
  - ../../../raw/research/1596-2026-07-13-daeo-func41-local-compaction-final-matrix.md
  - ../../../raw/research/1595-2026-07-13-daeo-removed-param-direct-prefilter.md
  - ../../../raw/research/1594-2026-07-13-daeo-broad-removed-param-local-compaction.md
  - ../../../raw/research/1593-2026-07-13-daeo-adjacent-chain-final-matrix.md
  - ../../../raw/research/1592-2026-07-13-daeo-adjacent-candidate-prefilter.md
  - ../../../raw/research/1591-2026-07-13-daeo-adjacent-constructor-chain-cleanup.md
  - ../../../raw/research/1590-2026-07-13-daeo-two-chain-final-matrix.md
  - ../../../raw/research/1589-2026-07-13-daeo-converged-chain-cleanup.md
  - ../../../raw/research/1588-2026-07-13-daeo-two-chain-bounded-closeout.md
  - ../../../raw/research/1587-2026-07-13-daeo-post-payoff-matrix-and-scheduled-refresh.md
  - ../../../raw/research/1586-2026-07-13-daeo-payoff-ranked-result-chain.md
  - ../../../raw/research/1585-2026-07-13-daeo-bounded-structured-copy-cleanup.md
  - ../../../raw/research/1584-2026-07-13-daeo-scheduled-mode-specific-blockers.md
  - ../../../raw/research/1583-2026-07-13-daeo-post-param-chain-gap-attribution.md
  - ../../../raw/research/1582-2026-07-13-daeo-scheduled-validation-and-timeout.md
  - ../../../raw/research/1581-2026-07-13-daeo-post-param-chain-direct-matrix.md
  - ../../../raw/research/1580-2026-07-13-daeo-exact-param-chain-closure.md
  - ../../../raw/research/1579-2026-07-13-daeo-exact-param-chain-blocker.md
  - ../../../raw/research/1578-2026-07-13-daeo-null-default-body-cleanup.md
  - ../../../raw/research/1577-2026-07-13-daeo-terminal-result-dependencies.md
  - ../../../raw/research/1576-2026-07-13-daeo-low-result-caller-closure.md
  - ../../../raw/research/1575-2026-07-13-daeo-wide-null-default-worklist.md
  - ../../../raw/research/1574-2026-07-13-daeo-artifact-gap-attribution.md
  - ../../../raw/research/1573-2026-07-13-daeo-flattened-rec-group-type-repair.md
  - ./index.md
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/dead_argument_elimination.mbt
  - ../../../../../src/passes/dead_argument_elimination_wbtest.mbt
  - ../../../../../src/passes/dae_optimizing_test.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/cmd/cmd.mbt
  - ../../../../../src/lib/types.mbt
  - ../../../../../src/validate/typecheck.mbt
  - ../../../../../src/validate/validate.mbt
  - ../../../../../src/wast/
  - ../../../../../agent-todo.md
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./signature-updates-and-nested-reruns.md
  - ./wat-shapes.md
  - ./starshine-port-readiness-and-validation.md
  - ../dead-argument-elimination/starshine-strategy.md
  - ../../../tooling/pass-fuzz-compare.md
  - ../precompute-propagate/index.md
---

# Starshine strategy for `dae-optimizing`

## Current status

Starshine now has an **active module-pass implementation** for Binaryen's upstream `dae-optimizing` pass on the current v0.1.0 surface. `[DAE]003` and `[DAE]004` are closed by research notes `0661` and `0687`; reopen only for a new semantic mismatch, validation failure, or measured DAE-owned regression. `[DAE]013` is closed for v0.1.0 by keeping this pass direct-pass-only: explicit `--dae-optimizing` / `--dead-argument-elimination-optimizing` requests are supported, but public `optimize` / `shrink` presets intentionally skip DAE until new ordered-prefix or artifact evidence proves default-preset safety and runtime. The current raw-cleanup policy is explicit: correctness comes first; audited pure/nontrapping cleanup is useful and should be kept when semantically proved; Binaryen-shape debris preservation is narrow and only justified by a documented diagnostic or active artifact frontier. Possibly trapping or effectful operand stacks must remain live, and any future policy-changing cleanup needs focused tests plus size/mismatch evidence from the relevant compare lane. This closes `[DAE]009` as a policy/backlog task; future raw-cleanup work should reopen only for a concrete implementation, measurement, or frontier need.

The local naming caveat was resolved in the first implementation slice:

- upstream Binaryen exposes the public pass name `dae-optimizing`;
- Starshine now registers the exact canonical spelling `dae-optimizing` as an active module pass;
- Starshine also keeps the descriptive compatibility spelling `dead-argument-elimination-optimizing` as an active alias for the same optimizing module pass;
- plain `dead-argument-elimination` remains boundary-only and does not run the optimizing nested-cleanup trace.

Current implemented behavior is intentionally narrower than full Binaryen DAE:

- private direct-call dead scalar parameter removal;
- adjacent self-recursive `local.get` forwarding is ignored when proving params dead;
- removed actual side-effect preservation with `drop` repair;
- export and `ref.func` / element escape bailouts;
- value-producing `if` operands are preserved with `drop` repair when their parameter is removed, after which the optimizing cleanup lane may remove pure value debris while retaining side effects;
- no-param dropped/uncalled result removal with conservative unreachable-prefix cleanup;
- a bounded dropped-result scheduler that derives candidate callees from current call facts before the artifact handpicked selected-def fallback, gated to private non-tail direct callees whose current direct calls all drop the single result; as of [`0605`](./index.md), the fact-driven queue runs ascending for modules up to `4096` defined functions while the huge debug artifact remains on the selected-list fallback until a measured batch can preserve the closed DAE011 runtime target; [`0607`](./index.md) rejected a naive `<= 8192` ascending widening because it starved high candidates, still needed the selected fallback, and failed artifact validation; [`0608`](./index.md) added a measured first large-module ordered step by trying one descending fact-driven candidate for `4096 < defined <= 8192`; [`0609`](./index.md) raised that large-module descending cap to two productive candidates; [`0610`](./index.md) raises it to three productive candidates, [`0611`](./index.md) raises it to four productive candidates, [`0612`](./index.md) raises it to five productive candidates, [`0613`](./index.md) raises it to six productive candidates, [`0614`](./index.md) raises it to seven productive candidates, [`0615`](./index.md) raises it to eight productive candidates, [`0621`](./index.md) raises it to nine only for the narrow `4096 < defined <= 4608` band after [`0620`](./index.md) unblocked native pass-suite validation, [`0622`](./index.md) advances that same narrow band to ten, [`0623`](./index.md) advances it to eleven, [`0624`](./index.md) advances it to twelve, [`0625`](./index.md) advances it to thirteen, and [`0626`](./index.md) advances it to fourteen with debug-artifact validation/timing still inside the project `<= 2x` pass-local target. Larger artifact modules remain at cap `8` to preserve the DAE011 runtime target, so selected-fallback removal still requires a measured artifact/fuzz slice; [`0628`](./index.md) inventories the current handpicked fallback on the latest traced artifact: `298`, `299`, `427`, `445`, `459`, `472`, `476`, `3566`, `3732`, `3814`, `3834`, `4106`, `4229`, `4232`, `503`, `4240`, `4241`, `4242`, and `4249` still report selected dropped-result changes, while `3799` was the only list entry not observed as productive; [`0662`](./index.md) retires that stale unobserved fallback entry with a white-box guard, leaving the productive selected entries listed above active; [`0664`](./index.md) adds selected dropped-result candidate trace metadata for the remaining fallback-family grouping work without changing optimizer behavior; [`0665`](./index.md) closes `[DAE004-D2]` by using that trace metadata to group the remaining productive entries into one-call singleton (`445`, `3834`, `4106`, `4249`), tiny multi-caller (`459`, `4229`, `4232`, `4241`, `4242`), mid-prefix dense (`298`, `299`, `427`, `472`, `476`, `503`), high-index bridge (`3566`, `3732`, `3814`), and caller-shape-split late-cluster (`4240..4249`) families; [`0666`](./index.md) completes the first singleton replacement by proving a broad-large Func445 fixture is handled without a selected-fallback trace and removing `445` from the handpicked fallback list without raising the broad cap or enabling the rejected broad bucketed switch; [`0667`](./index.md) validates that singleton removal on the debug artifact (`1575.557ms` Starshine pass versus `850.194ms` Binaryen pass, valid output, and no new semantic/validation regression in the direct compare refresh), so `445` can stay removed; [`0668`](./index.md) removes and validates singleton `3834` with pass-local timing still inside the `<= 2x` target (`1583.739ms` Starshine versus `874.786ms` Binaryen), valid output, and no new semantic/validation regression; [`0669`](./index.md) removes and validates singleton `4106` with pass-local timing still inside the `<= 2x` target (`1548.629ms` Starshine versus `857.949ms` Binaryen), valid output, and no new semantic/validation regression; [`0670`](./index.md) removes and validates singleton `4249` with pass-local timing still inside the `<= 2x` target (`1532.373ms` Starshine versus `847.036ms` Binaryen), valid output, and no new semantic/validation regression; [`0671`](./index.md) then removes and validates `503` as the first non-singleton fallback retirement, with pass-local timing inside the `<= 2x` target (`1531.921ms` Starshine versus `863.462ms` Binaryen), valid output, and no new semantic/validation regression; [`0680`](./index.md) removes `3566` with focused fallback-list red/green evidence plus a 1000-case direct compare refresh, [`0681`](./index.md) removes `3732` with the same focused guard and compare refresh, [`0682`](./index.md) removes `3814`, [`0683`](./index.md) removes `4232`, [`0684`](./index.md) removes `4240`, [`0685`](./index.md) removes `4241`, [`0686`](./index.md) removes final late-cluster entry `4242`, and [`0687`](./index.md) closes `[DAE004-H]`/`[DAE004-I]` with artifact, timing, validation, and 10k direct compare evidence; [`0629`](./index.md) classifies those still-productive fallback entries as blocked first by descending large-module ordering plus the eight-productive-attempt cap after `4651..4644`, not by stale facts, mixed dropped/undropped calls, dead-suffix repair, signature repair, or type-liveness in the latest trace; [`0631`](./index.md) closes `[DAE004-C]` as a design slice by specifying a bucketed, caller-filtered, fact-refreshing large-module worklist that keeps the broad productive cap unchanged while interleaving high descending, selected-fallback-neighborhood, and low ascending candidates before any selected fallback entry is removed; [`0632`](./index.md) adds a white-box bucketed broad-large attempt-order contract, but keeps the active broad-large descending scheduler unchanged because a trial active switch exceeded the DAE011 `Starshine <= 2x Binaryen` pass-local target; selected fallback deletion still requires artifact tracing, validation, timing, and compare evidence; [`0633`](./index.md) adds focused coverage for the live mixed dropped/undropped direct-call guard without changing behavior, [`0634`](./index.md) closes `[DAE004-E]` by classifying root-unreachable undropped suffix calls as dead observations that intentionally do not block result removal under the existing DAE policy, [`0635`](./index.md) closes `[DAE004-F]` by refreshing root-unreachable `call; drop` repair coverage with a live-prefix regression, and [`0637`](./index.md) closes `[DAE004-G]` by covering table/global plus typed element, local-declaration, and `ref.null` references used by simple function type pruning;
- local-use scanning ignores dead suffixes after a root `unreachable`;
- case-000690-style escaped-result self-call operand preservation: if the original single `f64` parameter is stranded under an escaped direct-call result that becomes an undropped dead-suffix self-call operand, Starshine preserves that parameter while still pruning direct simple self-call operands and dropped self-call escaped-result operands, matching the observed Binaryen shape in [research note 0557](./index.md);
- unused simple function type pruning after signature changes. The imported-tag-only gap recorded by research note [`1567`](./index.md) is repaired: imported tag payload `TypeIdx` references are marked live and remapped, the focused module validates, and the post-fix wasm-smith lane has zero Starshine mismatches or failures;
- a narrow exact-literal constant-actual materialization slice for private direct callees: if every direct caller passes the same literal and the callee never writes that parameter, Starshine removes the incoming param, rewrites callee `local.get`s to the literal, and relies on the touched-only nested cleanup lane for the remaining fold; as of 2026-05-13 this slice also handles sibling scalar memory-load carriers and typed single-result `TypeIdxBlockType` wrappers while staying on the same exact-literal/read-only guardrails; as of [`0606`](./index.md), the low-forwarded-const revisit runs for modules up to `4096` defined functions and scans the first `4096` candidate defs, covering mid-size exact-literal starvation outside the old first-`64` boundary while still leaving the huge debug artifact on selected high-wrapper lanes; [`0627`](./index.md) inventories the current DAE003 helper/test surface, including direct materialized constants, guarded forwarded `local.get`, selected unread-param lanes, selected immutable `global.get`, and selected default-argument families, so future DAE003 work should begin from the remaining gaps rather than duplicating closed selected lanes; [`0636`](./index.md) closes `[DAE003-B]` by classifying the current leading artifact/frontier misses away from constant/unread materialization: raw/default diffs remain type-section/type-index diagnostic drift, both-canonical Func509 remains the closed lowerer/diagnostic boundary, and current selected fallback changes remain `[DAE]004` dropped-result scheduling gaps rather than stale constant/unread facts; [`0638`](./index.md) closes `[DAE003-D]` for the current direct private wrapper-chain surface by tying the existing `dae-optimizing can chase a later exact-literal forwarding-wrapper chain` regression to the recursive parameter-local `local.get` resolver and trace-ordered `run* -> wrap1 -> wrap2 -> check` rewrite evidence; [`0639`](./index.md) advances `[DAE003-C]` by accepting the narrow straight-line caller-local carrier `const; local.set; local.get; call` when the same local has no earlier read, tee, or multiple write before the call actual; [`0640`](./index.md) adds focused negative guards proving `local.tee` prefixes and multiple same-local writes preserve the target parameter; [`0641`](./index.md) adds the explicit earlier same-local read guard, confirming the recognizer preserves the target parameter for `const; local.set; local.get; drop; local.get; call`; [`0642`](./index.md) adds trapping and effectful producer guards, confirming the recognizer preserves the target parameter for `i32.div_s; local.set; local.get; call` and `call $effect; local.set; local.get; call`; [`0643`](./index.md) closes `[DAE003-E]` for the current conservative local-carrier subset by preserving params for non-adjacent self-recursive local-carrier cycles and `ref.func`-escaped callees, leaving any broader recursive-cycle materialization as future behavior-widening work; [`0645`](./index.md) closes `[DAE003-C]` for the current conservative non-structured local-carrier surface with focused pass-suite validation and direct-DAE compare evidence, leaving new straight-line carrier widening to reopen only from concrete semantic/validation evidence; [`0644`](./index.md) advances `[DAE003-F]` by accepting a non-adjacent `block` carrier only when the prior `local.set` producer is a single-instruction block containing a materializable constant, while loops and other structured/control carriers remain conservative; [`0646`](./index.md) adds focused conservative guards for multi-instruction block carriers and equal-arm `if` carriers; [`0647`](./index.md) then accepts the narrow equal-arm `if` carrier when both arms are exactly one identical materializable constant; [`0649`](./index.md) accepts a narrow branch-free `loop` carrier when the loop body contains exactly one materializable leaf, and [`0656`](./index.md) broadens that loop surface to dropped materializable constant prefixes before the final materializable leaf; [`0650`](./index.md) accepts a narrow multi-instruction `block` carrier when every prefix pair is a materializable pure constant followed by `drop` and the final leaf is materializable; [`0651`](./index.md) accepts a typed outer-block plus single-leaf `try_table` carrier; [`0652`](./index.md) accepts equal-arm `if` carriers when each arm has only dropped materializable constants before the same final materializable leaf; [`0653`](./index.md) accepts `try_table` bodies with dropped materializable constants before the final materializable leaf; [`0655`](./index.md) accepts equal-arm `if` carriers when both arms resolve through the existing structured-carrier recognizer to the same nested single-leaf typed block constant; [`0657`](./index.md) accepts branch-free `loop` carriers whose single body instruction is itself a structured materializable constant carrier; [`0658`](./index.md) accepts dropped-prefix `block` carriers whose dropped prefix and final leaf are themselves recognized structured materializable constant carriers, and shares that recursive resolver with multi-instruction `loop` and `try_table` dropped-prefix carriers while preserving computed/trapping/effectful/control-sensitive guards; [`0659`](./index.md) closes `[DAE003-F]` for the v0.1.0 surface by treating branchy/computed multi-instruction positives, broader throwing/control-sensitive try/try_table, unequal/control-sensitive `if` shapes, and broader structured carriers as deferred unless a new artifact/fuzz frontier or semantic/validation repro attributes a miss to those families; [`0648`](./index.md) closes `[DAE003-G]` for the current v0.1.0 surface by adding a bounded `defined <= 4096` immutable-global revisit outside the selected Func313 lane while preserving mutable globals and keeping large-artifact timing-sensitive paths selected-only; [`0661`](./index.md) closes `[DAE003-I]` and `[DAE]003` by replaying direct fuzz and the debug artifact, classifying current known frontiers away from missed constant/unread materialization and toward the already documented diagnostic boundaries or remaining `[DAE]004` dropped-result scheduling/fallback work; [`1564`](./index.md) then adds a guarded suffix-only callsite collector so the exact-literal path can recover folded-multivalue forwarded defaults generically, while still rejecting the older Func323 inter-argument debris boundary that needs its separate selected rewrite. That same slice removes the bespoke Func3737/Func3765 forwarded-default helper pair and routes those high-definition lanes through the shared selected exact-literal machinery instead. [`1565`](./index.md) narrows the remaining inter-argument boundary further: a direct literal-conditioned result `if`, a dead `i32.const 0; if (void)` bridge, and a trailing literal can now be recovered and rewritten generically. [`1566`](./index.md) extends that guarded family to immutable defined `i32` global conditions and schedules matching callees before the ordinary core can peel only the final literal. The former selected Func323 8/9-parameter default rewrite and Func313-specific caller matcher are removed; the high-definition regression now uses the generic exact-literal machinery. Only the already-seven-parameter dropped folded-block cleanup remains selected and artifact-local, with a low-definition residual-carrier repro required to reopen it. Arbitrary zero-output inter-argument statements and other nonliteral condition facts remain outside this recognizer.
- an optimizing-only constant-anchored forwarded-parameter cycle transaction: starting from DAE-touched definitions, Starshine follows exact single-instruction `local.get` actuals through private direct-call parameter dependencies, requires a real cycle plus at least one identical materializable literal anchor, rejects conflicting literals, parameter writes, exports/`ref.func` escapes, tail-call boundaries, malformed call slices, and partial call-count coverage, then rewrites every participating callsite, callee body, local index map, and function signature as one all-or-nothing transaction. Plain DAE does not schedule this lane;
- an optimizing-only dead-callee convergence step over that exact cycle neighborhood: after cycle-member precompute, Starshine inspects direct calls from discovered member definitions, admits only exact caller-parameter `local.get` operands, and selects an outside callee only when the corresponding current parameter is unread. It removes those sink parameters through the existing owned/private/count-checked transactional rewrite, refreshes facts in bounded rounds, and then retries unread-parameter removal on the selected cycle definitions. The first 2026-07-14 artifact slice removed the unread middle parameters from defined Funcs `7014` and `7033` without replacing any computed value, improving raw size by `13` bytes and canonical size by `32` bytes. Plain DAE does not schedule this optimizing-only broad-neighborhood step;
- an optimizing-only forwarding-only parameter-component transaction: from the exact cycle neighborhood, Starshine counts every read of a selected read-only parameter and requires every get to be an exact direct-call operand into another selected parameter or a closed unread sink. It follows bounded stack effects through scalar/GC loads including `struct.get` and `array.get`, expands only through caller-parameter predecessors whose own callsites preflight successfully, requires a real selected cycle or closed sink with at least two proved cycle definitions, rejects semantic uses, writes, exports/`ref.func` escapes, tail calls, incomplete slices, and partial call counts, and then applies callsite/body/local-map/signature removal all-or-nothing. Arbitrary incoming producers are retained through effect-aware localization rather than treated as constants. Note [`1607`](../../../raw/research/1607-2026-07-14-daeo-forwarding-component-final-matrix.md) removed five artifact parameter nodes across defined Funcs `7007`, `7008`, `7010`, `7024`, and `7029`, closed the named forwarding-component parity gap, and improved canonical size by `38` bytes. Note [`1608`](../../../raw/research/1608-2026-07-14-daeo-grouped-signature-reuse-final-matrix.md) then closed the apparent `+32` raw localization/encoding regression: exact ownership/reference evidence let DAE rewrite uniquely owned unreferenced plain function subtypes in recursive groups in place, reducing the raw type payload by `202` bytes with identical canonical output. The endpoint became raw `3197221`, canonical `3274791`, and `1.52x` Binaryen pass-local. Note [`1609`](../../../raw/research/1609-2026-07-14-daeo-post-component-coalesce-final-matrix.md) added a bounded post-component replay for the exact changed-definition set: high-local functions were deep-cloned and coalesced one at a time, whole-module validation and strict encoded-size profitability decided retention, invalid Func `7024` rolled back, and small/non-improving Funcs `7029`/`7010` stayed unchanged. The retained artifact accepted only Funcs `7008` and `7007`, improving raw size by `516` bytes and canonical size by `426` bytes to raw `3196705` / canonical `3274365` at `1.82x` Binaryen pass-local. Note [`1610`](../../../raw/research/1610-2026-07-15-daeo-post-component-vacuum-final-matrix.md) then ran function-filtered `vacuum` only after accepted coalescing. Selected-definition validation and selected-function encoding preserved fail-closed rollback and strict raw-profitability proof without revalidating or re-encoding the whole artifact for unchanged type sections. The retained artifact accepted only Funcs `7008` and `7007`, reached raw `3196657` / canonical `3274317`, and remained within the pass-local target at `1.85x` / `1.84x` Binaryen on byte-identical repeats. Note [`1611`](../../../raw/research/1611-2026-07-15-daeo-branch-deadtee-compaction-final-matrix.md) added three narrower accepted-definition shapes after that vacuum result: branch-only value-`if` rotation to rebased `br_if`, never-read `local.tee` erasure that preserves the stack value, and declaration compaction/local remapping fused into the same selected-function traversal and validation/profitability decision. The retained artifact still accepted only Funcs `7008` and `7007`, reached raw `3195518` / canonical `3273130`, removed `614` / `1254` local declarations from those bodies relative to note `1610`, and remained within the pass-local target at controlled `1.97x` / `1.94x` Binaryen. Note [`1612`](../../../raw/research/1612-2026-07-15-daeo-selected-copy-readback-final-matrix.md) then added three exact SimplifyLocals-backed shapes inside that same accepted candidate: aggregate-field readback tees, non-void value-block readback tees, and exact-type one-use immutable-parameter copy forwarding with suffix dominance and exposed-local compaction. The retained artifact still accepted only Funcs `7008` and `7007`, reached raw `3195471` / canonical `3273077`, and remained within the pass-local target at controlled `1.95x` / `1.94x` Binaryen. The pair's printed traffic fell to `955` gets / `257` sets / `131` tees versus Binaryen `915` / `169` / `169`. Note [`1613`](../../../raw/research/1613-2026-07-15-daeo-selected-aggregate-spill-final-matrix.md) added three further exact non-null aggregate shapes in the same count/rewrite traversal: in-order two-field spill collapse, four-field field-2 leader forwarding, and residual field-0/field-1/field-3 spill collapse. The retained artifact still accepted only Funcs `7008` and `7007`, reached raw `3195388` / canonical `3272994`, and remained within the pass-local target at controlled `1.92x` / `1.94x` Binaryen. The pair's printed traffic fell to `943` gets / `245` sets / `131` tees versus Binaryen `915` / `169` / `169`. Note [`1614`](../../../raw/research/1614-2026-07-15-daeo-selected-uniform-null-final-matrix.md) then reused the component transaction's existing call facts to materialize a uniform final `ref.null` argument into Func `7105`, folded the resulting exact null test, and replaced two exact sentinel-wrap conditionals with equivalent selects. The retained artifact reached raw `3195331` / canonical `3272937` at controlled `1.96x` / `1.96x` Binaryen. Note [`1615`](../../../raw/research/1615-2026-07-15-daeo-terminal-local-sinking-final-matrix.md) closed the remaining Func `7105` cleanup gap with three exact SimplifyLocals-backed lanes: default aggregate sinking, first sentinel result sinking, and second sentinel set/readback removal. The cleanup was fused into one selected sentinel application over the changed callee `primary_def`, avoiding giant touched-caller scans. The retained artifact reached raw `3195309` / canonical `3272915` at controlled `1.98x` / `1.97x` Binaryen. Note [`1616`](../../../raw/research/1616-2026-07-15-daeo-type309-call-transport-final-matrix.md) then removed both surviving Type `309` terminal-self-branch/fallthrough wrappers and sank two exact one-use producer-call results through pure local arguments in Func `7008`, all inside the existing selected count/rewrite/validation/profitability traversal. The retained artifact reached raw `3195283` / canonical `3272837` at controlled `1.97x` / `1.96x` Binaryen. Note [`1617`](../../../raw/research/1617-2026-07-15-daeo-sibling-terminal-transport-final-matrix.md) then sank the final result in the exact two- and three-producer sibling arms and removed fifty exact terminal structured-result set/readback pairs, all inside the existing selected count/rewrite/validation/profitability traversal. The retained artifact reached raw `3194954` / canonical `3272509` at controlled `1.95x` / `1.96x` Binaryen. Func `7105` remained exact; Func `7008` fell to canonical `4757` (`+425`) and Func `7007` to `4451` (`+567`). Note [`1618`](../../../raw/research/1618-2026-07-15-daeo-complete-call-transport-final-matrix.md) extended the same traversal with three complete stack-value-slice transports: twelve Func `7007` block results through `i32.const 0`, a unary call, and terminal construction/return; four Func `7008` structured results through two pure caller-local call arguments; and one nested direct-call value slice including the prefix call required to avoid ambient-stack capture. Partial `call 7053` / `call 10815` transports were rejected because omitting ambient producers rolled back cleanup and enlarged the artifact. The retained artifact reached raw `3194881` / canonical `3272433` at controlled `16757.176ms` / `16741.469ms`, both `1.96x` Binaryen and below the absolute ceiling. Func `7105` remained exact; Func `7008` fell to canonical `4746` (`+414`) and Func `7007` to `4386` (`+502`). Note [`1619`](../../../raw/research/1619-2026-07-15-daeo-complete-carrier-cleanup-final-matrix.md) then closed the next three exact current shapes: complete Func `7008` `call 7053` producers beginning before `call 7096` and ending through `call 7106`, the conditional selector sibling preserving its ambient condition and `call 7123`, and a four-field terminal aggregate spill recognized only in the existing post-remap counted readback traversal. Remapped counts were emitted during local compaction, so no second local-count loop or instruction scan was added. Partial producer slices and pre-remap byte-identical probes were rejected. The retained artifact reached raw `3194836` / canonical `3272348` at controlled `16633.600ms` / `16929.736ms`, below the absolute ceiling, but the canonical module gap remained `+9892`; no direct residual was accepted as a Starshine win merely because it validated or used fewer local operations. Plain DAE does not schedule the component transaction or these replays; grouped signature storage is shared by already-approved DAE signature rewrites and does not add optimizing cleanup to plain DAE;
- note [`1620`](../../../raw/research/1620-2026-07-15-daeo-remapped-carrier-cleanup-final-matrix.md) extended the accepted post-remap selected-definition cleanup with three exact carrier shapes: first-result sinking in the two-producer sibling arm, paired-result sinking in the three-producer sibling arm, and typed complete-block result sinking through a pure leading zero into terminal `struct.new; return`. Every target required exact one-set/one-get/no-tee evidence; calls and nested structured producers retained original order; remapped local types were emitted during the existing compaction loop and reused by the recursive rewrite. Pre-remap and broader typed block probes regressed raw/canonical size and were reverted. The retained artifact reached raw `3194796` / canonical `3272308` at controlled `16804.456ms` / `16971.806ms`, both below the absolute ceiling, but the canonical module gap remained `+9852`. No direct residual was accepted as a Starshine win merely because it validated or used fewer local operations. Plain DAE does not schedule these post-remap replays;
- note [`1621`](../../../raw/research/1621-2026-07-15-daeo-post-remap-transport-final-matrix.md) added three more exact post-remap transport shapes in that same selected traversal: a one-argument single-result block carrier, a complete terminal four-argument carrier, and an identity block carrier. All retained exact remapped type and one-set/one-get/no-tee guards, complete producer/argument ordering, selected-definition validation, strict encoded-size profitability, and rollback. A split-argument block-carrier probe regressed to raw `3194830` / canonical `3272372` and was reverted. The retained artifact reached raw `3194767` / canonical `3272279`; three controlled repeats remained below the `17076.04ms` ceiling, but the canonical gap remained `+9823`. Plain DAE does not schedule these optimizing-only transports;
- note [`1622`](../../../raw/research/1622-2026-07-16-daeo-pair-store-transport-final-matrix.md) added two exact paired-store transports and one terminal wrapper-call block transport in the same counted selected traversal. The paired-store family hoisted only the first receiver's nontrapping local read before a complete one- or two-call typed producer, preserved producer effects/traps/result order, kept the second spill, and removed only the exact first one-set/one-get/no-tee carrier. The terminal-call family moved only a pure zero before the complete single-result producer. Isolated Func `7008` local `52` and `60` split-argument probes each regressed to raw `3194794` / canonical `3272318` and were reverted. The retained artifact reached raw `3194753` / canonical `3272265`; controlled quiescent repeats remained below the `17076.04ms` ceiling, but the canonical gap remained `+9809`. Plain DAE does not schedule these optimizing-only transports;
- note [`1623`](../../../raw/research/1623-2026-07-16-daeo-single-traversal-readback-final-matrix.md) closed the remaining pair-store traversal-order exposure without a retry scan by recursively rewriting only the preserved producer body inside the owning terminal-call replacement. Two additional exact aggregate shapes reread a tee-captured object to preserve three-field result order without a spill, then moved a receiver local read before its pure field projection and retained later field reads through `local.tee`. The three red-first commits improved the artifact by `16` raw / `16` canonical bytes to raw `3194737` / canonical `3272249`; controlled repeats `16608.001ms`, `16524.749ms`, and `16568.615ms` remained below the absolute ceiling, but the canonical gap remained `+9793`. Plain DAE does not schedule these optimizing-only readback rewrites;
- note [`1624`](../../../raw/research/1624-2026-07-16-daeo-pair-result-final-matrix.md) extended the owning paired-store transaction rather than adding a retry: exact two-call and one-call producers read the first receiver inside the successful terminal field arm, performed the first `struct.set` there, and returned only the second field directly to its store. A separate exact `i32.wrap_i64; local.set; local.get` readback became `local.tee` in the earlier selected forwarding traversal that owned the artifact shape. The three red-first commits improved the artifact by `10` raw / `28` canonical bytes to raw `3194727` / canonical `3272221`; controlled repeats `16669.816ms`, `16659.436ms`, and `16620.041ms` remained below the absolute ceiling, but the canonical gap remained `+9765`. Plain DAE does not schedule these optimizing-only pair-result/readback rewrites;
- note [`1625`](../../../raw/research/1625-2026-07-16-daeo-terminal-call-argument-final-matrix.md) added three exact post-remap terminal-call argument transports in the same selected traversal. The first sank a complete typed block result through a non-null aggregate projection, empty GC constructors, and a final call argument. The second and third reproduced Binaryen v130's target-first and target-last terminal-call shapes, delaying the complete block producer until after the exact prefix call while guarding every caller local moved across it. The three red-first commits removed Func `7007` local `61` and Func `7008` locals `29`, `126`, and `127`, improving the artifact by `16` raw / `20` canonical bytes to raw `3194711` / canonical `3272201`. CPU-affinity controlled repeats `16657.065ms`, `16567.610ms`, and `16862.325ms` remained below the absolute ceiling, but the canonical gap remained `+9745`. Plain DAE does not schedule these optimizing-only terminal-call transports;
- a touched-function cleanup scheduler that records the required `precompute-propagate` prefix and now starts with a touched-only private `precompute-propagate-prefix` helper before the narrow local cleanup subset (`dead-code-elimination -> optimize-instructions -> local-cse -> pick-load-signs -> heap-store-optimization -> heap2local -> optimize-casts -> code-pushing -> simplify-locals -> code-folding -> precompute -> tuple-optimization -> merge-blocks -> remove-unused-brs -> remove-unused-names -> merge-blocks -> coalesce-locals -> reorder-locals -> vacuum`) only on touched functions; that private prefix helper folds SSA-backed default-init and direct local constant facts, and as of [`1567`](./index.md) also derives constants through value-preserving `local.tee` and unbranched single-result block fallthrough while retaining effectful prefixes and rejecting branch-valued blocks, before rerunning plain `precompute`. The touched-only tuple slot removes pure all-dead multivalue spills while replaying branch-free effectful or trapping prefixes; plain DAE does not invoke this optimizing-only helper. `local-cse` now uses a function-filtered raw adapter, while `coalesce-locals` and `reorder-locals` use function-filtered adapters over the existing module passes in this nested lane, and all three remain public module passes for direct requests. The local-CSE adapter emits `pass[local-cse]:func/start/done` trace lines so DAE002 tests can pin its order before `simplify-locals`, and the scheduler itself emits `pass[dae-optimizing]:nested-pass name=...` lines so order tests can lock the guarded lane directly. Research note [`1562`](./index.md) removes the coarse `>100` defined-function skip, so size-skip traces now distinguish only `large-touched-set` and `large-touched-function`; research note [`1563`](./index.md) then closes the immediate follow-up blocker by fixing the shared zero-param multivalue drop-carrier `merge-blocks` abort behind the folded-multivalue Func3737 lane and by refreshing the high-defined-function DAE tests to assert durable post-cleanup semantics. `precompute-propagate-prefix` / `optimize-instructions` / `pick-load-signs` / `heap-store-optimization` / `heap2local` / `optimize-casts` / `code-pushing` / direct `precompute` / `code-folding` / `merge-blocks` / `remove-unused-brs` / `remove-unused-names` use the existing hot touched-function runner;
- a cheap large-module-safe post-touch raw cleanup runs after DAE rewrites and now preserves `nop` debris for current Binaryen-shape parity while still stripping selected dropped pure debris across the module once DAE has touched at least one function, preserving the original DAE touched set for the nested cleanup scheduler. The selected Func236 local-retarget lane opts into Binaryen's stricter raw-debris cleanup and strips its local-retarget-only `nop` / post-effect dropped-integer debris before the broad post-touch cleanup runs. It removes dropped pure leaves (`i32.const`, `i64.const`, `f32.const`, `f64.const`, `ref.null`, `local.get`, `global.get`, `memory.size`, and `table.size`) except integer constants in a post-observable-effect suffix or in live branch regions whose sibling/parent branch has observable effects or control, which are preserved for current Binaryen-shape parity, effect-suffix dropped `local.get` debris, local/global self-assignment debris (`local.get $x; local.set $x`, `global.get $g; global.set $g`), redundant local self-tee writes (`local.get $x; local.tee $x`) while preserving the read result when it remains live, same-slot readback debris (`local/global.set $x; local/global.get $x; drop` or same-slot `set`) while preserving the original write and any producer effects, and dropped `local.tee` writes (`value; local.tee $x; drop`) by rewriting them to `value; local.set $x`. The same local-tee-to-set rewrite now removes a following dropped nontrapping pure unary/binary suffix, preserving the producer evaluation and local write while deleting only the discarded computed result. Dropped nontrapping pure unary/binary wrappers are also peeled from possibly trapping/effectful values when the wrapper itself is nontrapping and any removed right operand is a pure leaf, e.g. `load; const; add; eqz; drop` becomes `load; drop`, while trapping wrappers such as integer division and effectful right operands remain intact. It also strips live integer constant identity tails (`value; const 0` / `1` / `-1`; op -> value`, plus stack-neutral `const 0` / `1` / `-1`; `value`; op -> value forms) for nontrapping `i32`/`i64` add/sub/or/xor/shift/rotate/mul/and operators where the constant is an actual identity, keeps equality-to-zero as an explicit `0; eq` compare for the current Binaryen-parity frontier while still booleanizing inequality-to-zero as `eqz; i32.eqz`, folds same-local/same-global integer and `ref.eq` self comparisons to `i32.const 0` or `i32.const 1` while preserving float self comparisons because NaN is not reflexive, and simplifies same-local/same-global integer self bitwise/subtraction tails (`x & x` / `x | x` to `x`, `x ^ x` / `x - x` to integer zero). It preserves left-zero subtraction, left-zero shifts, stack-consuming LHS comparison producers, different-slot comparisons, and other non-identity forms; the focused regressions for these slices live in [`../../../../../src/passes/dead_argument_elimination_wbtest.mbt`](../../../../../src/passes/dead_argument_elimination_wbtest.mbt). On DAE-touched functions it also rewrites pure-stack local copy pairs (`value; local.set $x; local.get $x` to `value; local.tee $x`, and `value; local.tee $x; local.set $x` to `value; local.set $x`) only when the producer stack does not contain `local.get`; local-get-sourced copy chains are preserved for current Binaryen-shape parity, as are local-get reads of a just-copied local and dropped local reads after observable effects, while memory/call/effect-fed live copies are left for the existing nested cleanup passes, arbitrary straight-line dropped stacks made only from those leaves and nontrapping pure unary/binary numeric/ref operators, dropped `select` stacks when both candidate values and the condition are recognized pure/nontrapping, saturating float-to-int truncation and simple dropped constant-safe regular float-to-int truncation, plus constant integer div/rem operations only when the divisor/overflow facts prove they cannot trap, including signed `i32.div_s` and `i64.div_s` overflow guards; those constant-safe float-to-int and integer div/rem operations may also sit under otherwise nontrapping pure wrappers when their literal operands are adjacent and syntactically prove the potentially trapping operation safe. It also treats zero-parameter single-result `block`, `loop`, catch-free `try_table`, and value-producing `if` wrappers as droppable pure values only when every executed value path is itself a full straight-line pure/nontrapping stack expression from its first instruction. For dropped value `if`s, a recognized pure/nontrapping condition stack is stripped with the branches; otherwise the pure branches are removed and the condition is kept with `drop` so condition traps/effects still occur. `ValTypeBlockType` and resolved `TypeIdxBlockType` `[] -> [T]` wrappers are covered, while type-index wrappers with parameters or multiple/no results, caught `try_table` value wrappers, and trapping/control/effectful bodies remain preserved. Zero-parameter zero-result `block` and `loop` wrappers are removed only after recursive cleanup proves their body is empty; `VoidBlockType` and resolved `TypeIdxBlockType` `[] -> []` wrappers are covered, while parameter-consuming or trapping/effectful structures stay intact. Empty zero-parameter zero-result `if` wrappers are simplified after both branches recursively empty: pure/nontrapping conditions are stripped, while effectful or trapping condition producers are preserved by replacing the empty `if` with a `drop`. Zero-parameter zero-result `try_table` wrappers are removed only after recursive cleanup proves their body empty; catch clauses do not block removal because an empty body cannot throw into them, while typed parameter-consuming or trapping/effectful `try_table` forms stay intact. It also strips narrow SIMD debris (`v128.const`, splats from scalar operands, unary lane-local integer/float ops such as abs/neg/popcnt/all_true/bitmask/ceil/floor/trunc/nearest/sqrt, `v128.not`, `v128.any_true`, bitwise `v128.and/andnot/or/xor`, `i8x16.shuffle` / `i8x16.swizzle`, lane extract/replace operations, lane shifts, integer lane extension/extadd-pairwise ops, narrowing/extmul/dot integer ops, non-relaxed SIMD conversion/trunc-saturate/demote/promote ops, lane comparisons, and basic lane arithmetic such as add/sub/mul, saturating add/sub, min/max, avgr/q15mulr, and float div/pmin/pmax, plus `v128.bitselect`) only when every consumed operand stack is recognized pure/nontrapping; memory-backed scalar/SIMD operands remain preserved. Dropped `ref.null; ref.is_null`, pure-operand `ref.test`, pure-operand `ref.test_desc`, and pure-operand `ref.eq` debris is removed because those instructions are side-effect-free and nontrapping boolean/comparison tests; nullable `ref.cast` is stripped only when its consumed stack is syntactically rooted in `ref.null`, because a nullable cast of null cannot take the null-trapping path. `ref.as_non_null` and non-nullable `ref.cast` remain preserved except for the narrow syntactic `ref.i31` producer family, because `ref.i31` constructs a non-null i31 reference so the null assertion or cast to `i31` / `eq` / `any` cannot trap. Unknown or possibly trapping numeric/ref/SIMD operations, including non-constant-safe regular float-to-int truncation, remain preserved.

Current compare-pass rule: DAE mixed-generator fuzz lanes should use `--normalize drop-consts --normalize unreachable-control-debris` and report both exact `normalizedMatchCount` and `cleanupNormalizedMatchCount`. The first normalizer covers conservative dropped constant debris; the second covers the inspected unreachable/control debris that Starshine strips earlier than Binaryen. See the harness contract in [`../../../tooling/pass-fuzz-compare.md`](../../../tooling/pass-fuzz-compare.md) and the source note [research note 0673](./index.md). These normalizers keep intentional cleanup noise out of the DAE mismatch budget, but any remaining mismatch after the explicit normalizers still needs ordinary semantic/validation classification.

Current performance status: `[DAE]011` is closed for the current debug-artifact pass-local target. Research note [research note 0593](./index.md) added `self-optimize-compare --timing-only` after the full normalized-WAT helper timed out in note [research note 0592](./index.md). Notes [research note 0594](./index.md) through [research note 0600](./index.md) attributed the over-target runtime to repeated selected dropped-result helper calls, dominated by whole-module dropped-call rewriting, whole-module call-drop cleanup, and undropped-call scans. Research note [research note 0601](./index.md) adds the behavior-changing caller-filtered path: `dae_try_remove_selected_defs_dropped_results_with_facts_once(...)` now passes precomputed `direct_callers[callee]` into `dae_try_remove_dropped_results(...)`, and that helper limits undropped-call checks, live dropped-call rewrites, post-result `call; drop` cleanup, and no-param dead-suffix repair to the caller set while preserving the old whole-module path for callers without facts. Timing-only artifact replay `.tmp/dae011-caller-filtered-timing-20260526` reports `1461.100ms` Starshine pass-local time versus `864.852ms` Binaryen pass-local time, and repeat `.tmp/dae011-caller-filtered-timing-repeat-20260526` reports `1577.403ms` versus `851.431ms`; both are inside the repo target of `Starshine <= 2x Binaryen`. Both Starshine outputs validate with `wasm-opt --all-features` with only the existing large-local-count VM warning. Future DAE performance work should reopen from a new measured DAE-owned regression, fuzz/runtime cliff, or preset-integration timing need rather than this closed selected dropped-result helper slice.

Fresh 2026-07-13 scheduled replay in research note [`1570`](../../../raw/research/1570-2026-07-13-daeo-scheduled-replay-localization-safety.md) added behavior evidence and fixed an artifact-discovered native local-map panic through transactional local-index bounds checks and body-aware scratch allocation. Public `optimize`, public `shrink`, and synthesized `-O4z` each ran DAEO exactly once in the late slot and matched Binaryen's valid 38-byte O4z output on the dedicated profile. Research note [`1573`](../../../raw/research/1573-2026-07-13-daeo-flattened-rec-group-type-repair.md) superseded the provisional validator attribution: DAEO had resolved flattened type indexes as rec-entry indexes and appended new function types at encoded-entry count. Red-first fixes restored grouped signatures and valid current-artifact output. Starshine then ran DAEO in `3327.318ms` versus Binaryen `8083.49ms`, but remained `+24159` raw / `+16350` canonical / `+333815` WAT bytes on the direct artifact. Research note [`1574`](../../../raw/research/1574-2026-07-13-daeo-artifact-gap-attribution.md) corrected the final touched count to `22` and narrowed ownership: guarded nested replay accounted for only `21` canonical bytes and added `6` raw bytes, while repeated core/revisit probes reached the low Func `164` default-argument candidate but failed performance and still increased raw size. Research note [`1575`](../../../raw/research/1575-2026-07-13-daeo-wide-null-default-worklist.md) implemented and measured the efficient replacement. The reusable worklist discovers candidates from one current caller-fact snapshot and revalidates each rewrite against the evolving module; the active broad-module policy selects wide boundaries with at least eight all-nullable-reference params in the first `4096` definitions. This reached only Func `164` on the current artifact, removed its ten null/default params, validated, and improved the committed Starshine output by `20` raw / `55` canonical / `675` current-tool WAT bytes while running in `3646.647ms` versus Binaryen `8083.49ms`. Unfiltered `54`-candidate batching was rejected because it added `31` raw bytes; the mixed Func `180` follow-up was excluded because it was individually raw-size-losing and not part of the attributed chain. The remaining direct gap became `+24139` raw / `+16295` canonical / `+346973` current-tool WAT bytes. Research note [`1576`](../../../raw/research/1576-2026-07-13-daeo-low-result-caller-closure.md) introduced the bounded result-only caller closure but still no-op'd on the artifact. Notes [`1577`](../../../raw/research/1577-2026-07-13-daeo-terminal-result-dependencies.md) and [`1578`](../../../raw/research/1578-2026-07-13-daeo-null-default-body-cleanup.md) closed that blocker: terminal direct-call dependencies refine in callee-first postorder, `struct.new` exposes exact non-null results, and only the productive wide-null candidate receives the proven `ref.null; ref.is_null; if` and terminal-local cleanup. Func `164` reached zero params/locals, exact result `$731`, and the same effectful default-producer/final-call shape as Binaryen. The retained artifact became `199` raw / `240` canonical / `2832` regenerated canonical-WAT bytes smaller than note `1576`, ran in `3980.121ms` versus Binaryen `8083.49ms`, and remained valid. Note [`1579`](../../../raw/research/1579-2026-07-13-daeo-exact-param-chain-blocker.md) narrowed the remaining chain: exact second-param forwarding through Funcs `37`, `38`, and `41` worked in a temporary probe, and immutable Global `501` materialization could remove Func `37`'s third param, but every partial endpoint was raw-size-losing (`+35`, `+23`, or `+43` bytes). The next safe owner became function-filtered propagation on Func `37` after that materialization, then exact/default and unread-param convergence through Funcs `38` and `41`; broad raw cleanup and blanket Func-41 replay were rejected. The remaining artifact gap stayed an open parity gap, not a Starshine win.

Research note [`1580`](../../../raw/research/1580-2026-07-13-daeo-exact-param-chain-closure.md) superseded note `1579`'s exact-parameter blocker. Optimizing-only current-fact replay refined Funcs `37`, `38`, and `41`, materialized immutable Global `501` only into Func `37`, ran function-filtered propagation, removed the downstream nullable arguments, and cleaned only Funcs `37`/`38`; plain DAE retained the original three-parameter carrier. The retained artifact was valid at raw `3201367` / canonical `3278451` / canonical-WAT `179304975`, a `+6` raw but `-60` canonical / `-616` WAT change versus note `1578`, with pass-local `5645.054ms` versus Binaryen `8083.49ms`. The general large-touched-set guard and oversized Func-41 boundary remained. Research note [`1581`](../../../raw/research/1581-2026-07-13-daeo-post-param-chain-direct-matrix.md) refreshed the required direct matrix: dedicated `10000/10000`, regular `100000/100000`, wasm-smith `9955 + 1` cleanup-normalized out of `9956` comparable cases with `44` Binaryen/oracle failures, and random-all `9633 + 367` unchanged measured/source-backed Starshine cleanup wins. No unknown/risky, size-losing generated, validation, or true-semantic residual remained.

Research note [`1582`](../../../raw/research/1582-2026-07-13-daeo-scheduled-validation-and-timeout.md) refreshed ordered scheduling and release validation. Dedicated-profile `optimize`, `shrink`, and synthesized O4z each executed DAEO exactly once immediately after late HSO and before `inlining-optimizing`, emitted the same valid 38-byte output, and spent `668us`, `665us`, and `733us` in DAEO. Full release validation was green and the `.mbti` diff was empty.

Research note [`1583`](../../../raw/research/1583-2026-07-13-daeo-post-param-chain-gap-attribution.md) reattributed the remaining `+15995` canonical gap after the Func-37/38/41 closure. Defined Func `8429` became the largest body owner. Binaryen removed the dropped `i32` result through the source-backed `12293 -> 8429` terminal-call chain, then consumed Func `8429`'s local-copy debris. Result removal alone was rejected as size-losing; selected `simplify-locals`/`vacuum` closed `1560` raw and `1401` canonical bytes but took `33027.052ms`, about `4.09x` Binaryen, so that endpoint was also rejected. The next implementation needed payoff-aware broad result-chain scheduling plus a bounded faster equivalent of the `structured-pure-copy-call-tail` cleanup; plain DAE remained outside that route.

Research note [`1584`](../../../raw/research/1584-2026-07-13-daeo-scheduled-mode-specific-blockers.md) superseded the assumption that every large scheduled mode shared one vacuum blocker. Public `--optimize` reproducibly stalled in direct `vacuum` work before DAEO, while public `-O4z` bypassed that raw vacuum path and remained in early `ssa-nomerge` at absolute Func `1862` after `300s`; a no-trace O4z run also timed out after `600s`.

Research notes [`1585`](../../../raw/research/1585-2026-07-13-daeo-bounded-structured-copy-cleanup.md) and [`1586`](../../../raw/research/1586-2026-07-13-daeo-payoff-ranked-result-chain.md) closed the next attributed direct family. The giant structured-copy shortcut gained a bounded staged cleanup, and a generic broad-module selector ranked owned dropped wrappers by downstream local-copy payoff, removed wrapper `12293` before callee `8429` with fresh call facts, and cleaned only the selected callee. The retained artifact was valid, improved the previous Starshine endpoint by `1308` raw / `1149` canonical / `4190560` current-tool WAT bytes, and ran DAEO in `6814.078ms` versus Binaryen `8083.49ms`. Plain DAE was unchanged. Func `9347` became the largest direct canonical body owner, and the remaining module gap was `+14846` canonical bytes.

Research note [`1587`](../../../raw/research/1587-2026-07-13-daeo-post-payoff-matrix-and-scheduled-refresh.md) refreshed three required direct lanes: dedicated `10000/10000` normalized, regular `100000/100000` normalized, and explicit wasm-smith `9955` normalized plus `1` cleanup-normalized with only the unchanged `44` Binaryen/oracle failures. The required random-all lane timed out after `1800s` with only `307` records, so the four-lane matrix remained incomplete. Dedicated optimize/shrink/O4z still executed DAEO exactly once in the locked late neighborhood. Large shrink became attributed to the same early `ssa-nomerge` owner family as O4z; optimize remained separately blocked in vacuum. These were separate `[WALL]001` owners.

Research notes [`1588`](../../../raw/research/1588-2026-07-13-daeo-two-chain-bounded-closeout.md) and [`1589`](../../../raw/research/1589-2026-07-13-daeo-converged-chain-cleanup.md) closed the next two source-backed direct owners without widening plain DAE. One invocation completed at most two payoff-ranked wrapper-before-callee result chains with fact refresh between them, selecting `12293 -> 8429` and then `12020 -> 9347`; the low exact-parameter cleanup set also followed productive convergence through Func `41`. Note [`1590`](../../../raw/research/1590-2026-07-13-daeo-two-chain-final-matrix.md) recorded the intermediate raw `3198681` / canonical `3276084` endpoint and its complete four-lane matrix.

Research notes [`1591`](../../../raw/research/1591-2026-07-13-daeo-adjacent-constructor-chain-cleanup.md), [`1592`](../../../raw/research/1592-2026-07-13-daeo-adjacent-candidate-prefilter.md), and [`1593`](../../../raw/research/1593-2026-07-13-daeo-adjacent-chain-final-matrix.md) added the next bounded direct family without widening plain DAE. The generic selector found a broad exact-literal aggregate root, searched only its nearby adjacent same-signature high-local caller/callee neighborhood, and applied `simplify-locals` plus `vacuum` only to the selected pair. On the retained artifact it reported `roots=2 pairs=1`, chose `root_def=7003` and defs `[7007, 7008]`, and advanced the valid endpoint to raw `3198310` / canonical `3275701`, leaving `+13245` canonical versus Binaryen, at `11088.465ms` versus `8083.49ms` (`1.37x`). The pair-first prefilter preserved output and reduced selector cost.

Research notes [`1594`](../../../raw/research/1594-2026-07-13-daeo-broad-removed-param-local-compaction.md), [`1595`](../../../raw/research/1595-2026-07-13-daeo-removed-param-direct-prefilter.md), and [`1596`](../../../raw/research/1596-2026-07-13-daeo-func41-local-compaction-final-matrix.md) closed the next generic optimizing-only family without widening plain DAE. DAEO ranked already-touched broad high-local definitions whose parameter count fell, compacted unreferenced locals only in the best candidate, and checked direct current-reference compaction before the exact lowered-cleanup fallback. The artifact selected Func `41` without hardcoded indexes and removed `168` locals, advancing to raw `3197559` / canonical `3275027`, leaving `+12571` canonical versus Binaryen, at `12763.150ms` versus `8083.49ms` (`1.58x`). Explicit native SHA-256 `648217241b157b7144a7e2d1b023688f5a78b52f8d0d7fec5358c28394bc08ac` drove the complete Binaryen-v130 dedicated `10000`, regular `100000`, wasm-smith `10000`, and random-all `10000` matrix; generated residual classifications were unchanged, public optimize/shrink/`--optimize -O4z` remained exact-once immediately before `inlining-optimizing`, full tests passed `8809/8809`, seeded full CI-profile validation was green, and `.mbti` review was empty.

Research notes [`1597`](../../../raw/research/1597-2026-07-14-daeo-adjacent-type-stable-local-order.md), [`1598`](../../../raw/research/1598-2026-07-14-daeo-adjacent-local-order-prefilter.md), and [`1599`](../../../raw/research/1599-2026-07-14-daeo-adjacent-local-order-final-matrix.md) added source-backed type-stable local ordering to the already selected broad adjacent pair. Referenced locals moved by frequency only inside existing contiguous type runs, unreferenced locals were dropped, and the final helper resolved the shared type once and scanned exactly two definitions. The artifact still selected `root_def=7003`, defs `[7007,7008]`, without hardcoded indexes and advanced to raw `3197484` / canonical `3274937`, leaving `+12481` canonical versus Binaryen, at `12448.452ms` versus `8083.49ms` (`1.54x`). Explicit native SHA-256 `2dc4e78b621484309c1d4469238605054365895e88734d28967f50bb800dcf98` drove the complete Binaryen-v130 four-lane matrix; generated residual classifications were unchanged, public optimize/shrink/`--optimize -O4z` remained exact-once immediately before `inlining-optimizing`, full tests passed `8809/8809`, seeded full CI-profile validation was green, and `.mbti` review was empty.

Research notes [`1600`](../../../raw/research/1600-2026-07-14-daeo-payoff-type-stable-local-order.md), [`1601`](../../../raw/research/1601-2026-07-14-daeo-payoff-local-order-single-scan.md), and [`1602`](../../../raw/research/1602-2026-07-14-daeo-payoff-local-order-final-matrix.md) extended that same type-stable local rewrite only to terminal callees already selected by the generic payoff-chain lane. The retained artifact improved by `64` raw / `60` canonical bytes, split as `-30` canonical in Func `8429` and `-30` in Func `9347`, reaching raw `3197420` / canonical `3274877` and leaving `+12421` canonical versus Binaryen. The shared scan validated local indexes while collecting counts and first-use ranks, preserving byte-identical output and fail-closed behavior; controlled timing was neutral, so no speedup was claimed. Fresh explicit-native SHA-256 `b27d010fcd0e98034e3f1c4adef4592028c06508823f2818dc0240dd7a12b142` drove the complete Binaryen-v130 four-lane matrix and full validation. Public optimize/shrink/O4z remained exact-once immediately before `inlining-optimizing`; plain DAE remained unchanged. Two rejected reachability-only Func `7007..7010` probes worsened size and missed the forwarding transaction, so the next cycle slice required a generic all-or-nothing parameter-position SCC proof. DAEO remained active: Funcs `7008`, `7007`, `8429`, `41`, and `9347` led the direct body gaps, while the large pre-slot optimize vacuum and shrink/O4z ssa-nomerge blockers remained under `[WALL]001`.

Research note [`1603`](../../../raw/research/1603-2026-07-14-daeo-forwarded-cycle-precompute.md) added the generic all-or-nothing constant-anchored parameter-cycle transaction and a source-backed optimizing-only precompute refinement over exact cycle members. The artifact's exact forwarded-reference backedge identified defs `[7007, 7008, 7010]` without reachability widening or hardcoded indexes; targeted propagation improved the valid direct output by `16` raw / `16` canonical bytes to raw `3197404` / canonical `3274861`, leaving `+12405` canonical versus Binaryen, at `13838.931ms` versus `8538.02ms` (`1.62x`). The constant transaction remained intentionally inactive on the artifact because the removable `i32` chain still reached Func `7010` through computed body-local call results rather than literals, immutable globals, or exact forwarded parameter gets. That signature family remained an open parity gap; treating computed recursive actuals as equal to the external anchor without a separate value proof was rejected.

Research note [`1604`](../../../raw/research/1604-2026-07-14-daeo-forwarded-cycle-final-matrix.md) refreshed the complete explicit-native signoff. SHA-256 `a8c076d2234c156db85362e69a0d0e4006a98c43dc2c349ddf9f34450ad7c7be` drove dedicated `10000/10000`, regular `100000/100000`, wasm-smith `9955 + 1` with only `44` Binaryen/oracle failures, and random-all `9633 + 367` with byte-identical residual artifacts and unchanged measured/source-backed cleanup-win classification. The deterministic valid artifact was raw `3197404` / canonical `3274861`, `+12405` canonical versus Binaryen, at `13702.810ms` versus `8538.02ms` (`1.60x`). Full tests passed `8814/8814`, full CI-profile validation and `.mbti` review were green, and optimize/shrink/`--optimize -O4z` remained exact-once immediately before `inlining-optimizing`.

Current non-parity caveat: the current preset tables already include the late `dae-optimizing` slot in public `optimize` and `shrink`, so preset placement is no longer an unimplemented boundary. This scheduling fact does not close the remaining behavior, performance, debug-artifact, or full-parity work. `[DAE003-H]` is closed by a focused mid-size guard proving the existing bounded low-revisit lane reaches a later exact-literal target after 73 earlier productive rewrites without raising the core fixed-loop budget. The call-graph pruning and touched-function tracking slice `[DAE]001` is complete as of 2026-05-12. `[DAE]002` was closed on 2026-05-26 for its historical guarded scheduler slice, but note [`1567`](./index.md) supplies the focused Binaryen behavior evidence needed to reopen nested replay breadth: the private prefix previously missed effect-preserving block fallthrough constants. That family is now implemented, while the public upstream `precompute-propagate` contract and full default-function replay remain active behavior-completeness work rather than preset-only follow-up. `[DAE]005` is closed as a raw-helper policy decision: the default `defined=336 abs=353` type-section/type-index first diff is a documented diagnostic boundary, so future body-frontier work should use both-canonical diagnostics unless the team changes the raw byte-reference contract. The 2026-05-25 Func505 high-bound subshape is narrowed: [research note 0578](./index.md) verified that the printed high guard reads an unwritten non-param default-zero local and landed a guarded cleanup that replaces the false `0 > 57` else arm with `i32.const 0` after DAE candidate rewriting. The successor replay in [research note 0579](./index.md) completed in `.tmp/dae-func505-recovery-artifact` and confirmed the first diff remained `defined=505 abs=522`; its final empty-input branch drift is now narrowed by [research note 0580](./index.md), which landed a selected-Func505-only branch inversion cleanup for the semantic-safe `(x == 0 ? A : B)` to `(x ? B : A)` shape. [research note 0581](./index.md) then closed the lower-bound bool-carrier subshape by removing the selected-Func505-only `if(result i32) 1 else 0` carrier before a void `if`. [research note 0582](./index.md) reduced the remaining underscore guard polarity around `Func4495` and classifies that local subshape as representation-only control inversion (`if (predicate) br K else BODY` versus `if (eqz predicate) BODY`) without changing pass behavior or compare tooling. [research note 0583](./index.md) then reduced the overflow/error-return subshape as representation-only unsigned comparison polarity and temp-local drift (`acc > limit` versus `limit < acc`) around the same limit expression and error-construction path. [research note 0584](./index.md) reduces the remaining loop induction/exit-carrier subshape as representation-only control/local-carrier drift: Starshine's high-local `index < length` carrier and extra exit block align with Binaryen's low-local `length > index` carrier and direct exit block under the same parse-loop effects and continuation. [research note 0585](./index.md) then adds a narrow diagnostic-only canonical-function normalizer for that inspected Func505 parser-loop family, backed by a fake-tool script fixture. The successor replay in [research note 0586](./index.md) completed in `.tmp/dae006-next-frontier-20260526` and moved the both-canonical first diff to `defined=509 abs=526`; both canonical outputs validate with only the existing large-local-count VM warning, pass-local timing remains over target (`2923.619ms` Starshine versus `891.787ms` Binaryen), and the new frontier is classified semantic-safe, size-losing dead-return-suffix cleanup gap because Starshine leaves unreachable allocation/store/value debris after a function `return` while Binaryen stops at the same returned object. [research note 0587](./index.md) then added focused root-return and terminal-wrapper cleanup regressions plus narrow pass helpers; a recovery follow-up fixed the pending lowered-return helper tuple bug and added a block-wrapped terminal-wrapper regression, but `.tmp/dae006-return-suffix-recovery-20260526` still first-diffs at `defined=509 abs=526` (`2870.440ms` Starshine pass versus `867.327ms` Binaryen pass). A later `--dae-optimizing --print-func 526` probe confirmed the missed pass-side shape is an outer `block I64` with an inner `block (Void)`, `br Label 1` exits, a fallthrough `local.get $20; return`, then `local.set $2` plus the wrapper suffix. [research note 0588](./index.md) added a focused white-box reduction and helper for that outer-block/no-fallthrough wrapper family, but the both-canonical replay `.tmp/dae006-outer-block-suffix2-20260526` still first-diffs at `defined=509 abs=526` (`2791.086ms` Starshine pass versus `843.419ms` Binaryen pass) and tracing still shows no final-return-suffix cleanup hit. [research note 0589](./index.md) then reduced the printed `--print-func 526` outer-block/fallthrough-return/post-return-wrapper shape and proved the existing helper already strips that textual family, so the printed shape is not the missing matcher. [research note 0590](./index.md) tested the exact function-level value-block suffix implied by `body_raw`; a broad DAE final-hook strip made the artifact invalid because the suffix still consumes the pre-encode `block (result i64)`, so the new regression preserves that shape. [research note 0591](./index.md) closes `[DAE]006` by documenting Func509 as a lowerer/diagnostic boundary rather than a DAE final-hook matcher miss: future cleanup, if desired, belongs to a later post-lowering or writer-side dead-code cleanup investigation. The 2026-05-25 `[DAE]010` direct fuzz refresh at `.tmp/pass-fuzz-dae-optimizing-dae010-20260525-full2` compared `9975/10000`, with `6078` normalized matches, `3897` normalized mismatches, `0` validation failures, and `25` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`); agent classification is size-winning semantic-safe raw-cleanup drift for the all-`gen-valid` mismatch set, because inspected samples differ by Starshine removing the generator's leading dropped pure/nontrapping constant debris while Binaryen preserves it, and every Binaryen-accepted `wasm-smith` case matched. A whole-module cleanup experiment rewrote unrelated functions and worsened direct parity; the active scheduler therefore remains size-guarded and skips broad artifact modules until a faster filtered batch runner exists. After the 2026-05-12 case-000690 fix, `.tmp/pass-fuzz-dae-690-final2-1000` removed `case-000690-gen-valid` from the failure set and reported `998/1000` compared, `985` normalized matches, `13` mismatches, and `2` Binaryen/tool command failures; a follow-up classification found that all 13 remaining `gen-valid` mismatches are local-declaration-only diffs with exactly one local-decl hunk apiece, and the two command failures are unchanged `binaryen-rec-group-zero` parser/tool failures. The first `[DAE]002` scheduler slice preserved that frontier in `.tmp/dae002-mincleanup-1000` (`998/1000`, `985` normalized matches, `13` mismatches, `2` command failures), the local-CSE adapter preserved it again in `.tmp/dae002-local-cse-1000`, the merge-blocks addition preserved it in `.tmp/dae002-merge-blocks-1000`, the remove-unused-brs addition preserved it in `.tmp/dae002-remove-unused-brs-1000`, the remove-unused-names addition preserved it in `.tmp/dae002-remove-unused-names-1000`, the second merge-blocks addition preserved it in `.tmp/dae002-second-merge-blocks-1000`, the optimize-instructions addition preserved it in `.tmp/dae002-optimize-instructions-1000`, the code-folding addition preserved it in `.tmp/dae002-code-folding-1000`, the direct-precompute addition preserved it in `.tmp/dae002-precompute-1000`, the pick-load-signs addition preserved it in `.tmp/dae002-pick-load-signs-1000`, the heap2local addition preserved it in `.tmp/dae002-heap2local-1000`, and the heap-store-optimization addition preserved it in `.tmp/dae002-hso-1000` with the same counts. The code-pushing addition preserved that frontier again in `.tmp/dae002-code-pushing-1000`, the optimize-casts addition preserved it in `.tmp/dae002-optimize-casts-1000`, the reorder-locals addition preserved it in `.tmp/dae002-reorder-locals-1000`, and the coalesce-locals addition preserved it in `.tmp/dae002-coalesce-locals-1000` (`998/1000`, `985` normalized matches, `13` mismatches, `2` command failures) while a post-coalesce-locals debug-artifact trace still skips nested cleanup at `touched=12`. The new private prefix helper is currently covered by focused Moon tests for pass order and touched-only default-init local folding, and the new constant-actual slice is covered by focused every-direct-caller-same-literal plus scalar-load-sibling and typed single-result block-carrier regressions; a follow-up compare at `.tmp/dae002-const-actual-200` still matched the earlier 200-case frontier exactly (`199/200`, `198` normalized matches, `1` mismatch, `1` unchanged Binaryen/tool command failure). A later DAE002 blocker hunt expanded the passing ref-local regression surface to `ref.as_non_null(local.get ...)`, loop-carried locals, block-wrapped `try_table`, dominated and undominated sibling-join locals, and lib-constructed `call_ref` families, and it also found two inherited false leads outside the scheduler logic itself: after `optimize-instructions` simplified a ref-local `try_table` body nested under an outer `block`, HOT lowering computed catch depths against the full active label stack and rewrote a valid `catch_all 0` into invalid `catch_all 1`; and on duplicate-signature `call_ref` fixtures, `HotModuleContext` dropped declared function type slots, so `ref.func` widened to abstract `funcref` during HOT lifting and later failed a touched `local.set` into `(ref null (type ...))`. Fixing both issues restored the direct optimize-instructions shape, made the active `dae-optimizing` call-ref regressions pass, and removed two apparent DAE/local-subtyping blockers without broadening the guarded touched-only lane. A follow-up combo fuzz compare for `--dae-optimizing --local-subtyping` at `.tmp/dae-local-subtyping-1000` reported `998/1000` compared, `985` normalized matches, `13` mismatches, and `2` unchanged `binaryen-rec-group-zero` command failures; its mismatch and command-failure case set exactly matches `.tmp/dae002-coalesce-locals-1000`, and the 13 mismatches remain local-declaration-only `gen-valid` diffs. That leaves no validated current touched-local-subtyping validity blocker in the exercised control-flow or `call_ref` families. `[DAE]012` is closed as of 2026-05-26 for this current validated surface; reopen it only for a new validated DAE/local-subtyping semantic or validation repro. The remaining open work is broader constant-actual/artifact/runtime parity, preset scheduling, and any future local-subtyping parity/refinalization evidence outside the exercised families, not a reproduced DAE scheduler bug. The follow-up runtime attribution in [research note 0560](./index.md) removed the 30s+ pre-nested-marker timeout by replacing repeated DAE core call/dead-suffix scans with a single original call summary and suffix-target aggregation; artifact replay is feasible again but remains red at `defined=11 abs=28` and still misses the pass-local speed floor. The new caller-family attribution in [research note 0561](./index.md) proved all `370` observed direct `moonbit.check_range` callers keep `lo` at literal `0`, fixed scalar-load sibling carriers for the exact-literal slice, and still left `.tmp/dae002-load-only-artifact` red at the same first diff. A second follow-up in [research note 0562](./index.md) added typed single-result `TypeIdxBlockType` carrier support and a focused regression, but `.tmp/dae002-typeidx-block-artifact` still stayed red at `defined=11 abs=28`. A third follow-up in [research note 0563](./index.md) then showed that the current fixed `8`-iteration DAE core can starve a later exact-literal candidate after `9` earlier productive rewrites: Binaryen still rewrites the later `moonbit.check_range`-shaped callee to 2 params, while Starshine leaves it at 3, and the new `pass[dae-optimizing]:core iter=... primary_def=...` trace locks the reduced frontier directly as `primary_def=0..7`. A fourth follow-up in [research note 0564](./index.md) then fixed two real caller-rewrite blockers in the artifact path itself: ambient typed-loop entry-value slices and same-caller multi-call undercount. That made `dae_try_rewrite_candidate(...)` rewrite all `370` direct `moonbit.check_range` calls, moved the artifact frontier to `primary_def=11` first, and left Func 28 at the same 2-param signature as Binaryen. A fifth follow-up in [research note 0565](./index.md) added a tiny touched-only rewritten-control simplifier for negated compare guards, which closed the old Func 28 body-form mismatch and moved the live artifact frontier forward to `defined=25 abs=42`. A sixth follow-up in [research note 0566](./index.md) then showed Func 42 is blocked by a forwarding-wrapper chain `4558 -> 4559 -> 42`: Func 4558 is already directly exact-literal rewritable on the original artifact, Func 4559 becomes rewritable after 4558, and Func 42 becomes rewritable only after both higher-index wrappers fall. A seventh follow-up in [research note 0567](./index.md) then showed that a cheap bounded reverse exact-literal lane still does not reach that chain soon enough on the real artifact: `4559` and `4558` only appear at reverse iterations `14` and `15`. An eighth follow-up in [research note 0568](./index.md) added narrow forwarded-const analysis through wrapper-local `local.get` chains plus a low-prefix exact-literal revisit over the first `64` defined functions. That new slice moves the core frontier to `11, 25, 227, 233, ...`, so Func 42 is now reached as the second productive core rewrite on the original artifact, but the full artifact compare still differs first at `defined=25 abs=42`. A ninth follow-up in [research note 0569](./index.md) then fixed the remaining direct Func 42 add-zero / const-order body drift and added a low-callee core revisit over high callees selected from the first `64` defined callers. That moves the full artifact compare forward again to `defined=64 abs=81`, but the retained slice still runs at `120946.581ms` pass-local versus Binaryen `939.053ms`, and a wider `128`-caller experiment was intentionally rejected after it pushed the frontier to `defined=128 abs=145` at roughly `211635.689ms`. So the next safe slice is now the remaining low-wrapper/high-callee family just beyond the current `64`-caller boundary, not the original Func 42 family shape.

A 2026-05-18 artifact push later moved the live frontier to `.tmp/dae-parity-selected236-locals-fast-artifact` with first diff `defined=237 abs=254`; a follow-up runtime slice kept the output byte-identical but rerouted the artifact-specific low-callee and low-wrapper loops through the narrower unread-param helper with shared current-call facts, bringing repeated artifact pass-local timings to `1204.050ms`, `1375.033ms`, `1238.117ms`, and `1449.124ms` versus Binaryen `901.666ms`, `933.922ms`, `924.685ms`, and `1038.630ms`. A later reverse-lane runtime slice kept the output byte-identical again while routing the same stable high-wrapper chain (`4593, 4592, 4591, 4589, 4588, 4587, 4586, 4584`) through selected exact-literal rewrites on large modules; repeated artifact pass-local timings after both runtime slices were `1210.633ms`, `1190.713ms`, and final post-format validation `1159.661ms` versus Binaryen `995.422ms`, `953.055ms`, and `972.156ms`. A subsequent runtime-only slice reused one current-call-fact snapshot across that stable reverse selected-def chain, preserving byte-identical output and the same Func237 frontier while improving repeated pass-local artifact timings to `954.237ms`, `1041.034ms`, and `1105.814ms` versus Binaryen `977.574ms`, `954.835ms`, and `946.393ms`. The next runtime slices reused the low-callee current-call-fact snapshot across the low-wrapper, boundary, selected dropped-result, mid exact-literal, immutable-global exact-literal, and high exact-literal lanes; this again preserved byte-identical output and the same Func237 frontier, with repeated artifact pass-local timings `954.009ms`, `953.685ms`, and `942.725ms` versus Binaryen `989.211ms`, `989.084ms`, and `1033.290ms`. Follow-up boundary-selector runtime slices replaced repeated single-wrapper boundary scans with a direct wrapper-to-callee lookup plus a reusable selected-def bitmap, then queued that fixed wrapper/callee family once before replaying selected unread-param rewrites; both preserved byte-identical output and the same Func237 frontier, with the queued version improving repeated artifact pass-local timings to `759.713ms`, `772.650ms`, and `813.049ms` versus Binaryen `828.849ms`, `816.225ms`, and `819.985ms`. A later selected-helper runtime slice reused the already-collected direct-caller lists to reject self-recursive selected unread-param and exact-literal candidates instead of rescanning each candidate body; it stayed byte-identical at SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002` and preserved the Func237 frontier, with same-session baseline/change pass-local timings `944.067ms` vs `846.089ms` and repeated changed timings `951.182ms`, `928.172ms`, and `856.597ms` versus Binaryen `1047.720ms`, `946.588ms`, and `912.286ms`. A 2026-05-19 runtime-only follow-up reused per-caller stamp tables across original dead-suffix target collectors instead of allocating fresh `defined`-sized bool arrays for each caller; it again stayed byte-identical at SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002`, preserved first diff `defined=237 abs=254`, and improved repeated artifact pass-local timings to `741.094ms`, `737.795ms`, and `734.585ms` versus Binaryen `851.457ms`, `838.917ms`, and `832.981ms` after same-session direct Starshine pass timers improved from `771.283ms`, `763.074ms`, `782.472ms` to `726.991ms`, `743.688ms`, `725.645ms`. A later same-day runtime slice reused the original function type-index snapshot for original parameter counts and the original call summary instead of rebuilding it twice; this kept raw Starshine output byte-identical at SHA `5ef1b8b238989372a482014be46c13a3af6d86262e721259d3c1eda4b6f6c93f`, kept the accepted output SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002`, preserved first diff `defined=237 abs=254`, and repeated clean artifact pass-local timings were `712.533ms`, `724.751ms`, and `746.108ms` versus Binaryen `833.349ms`, `850.807ms`, and `838.413ms`. The selected singleton bitmap reuse slice later kept the post-raw-cleanup artifact byte-identical at raw SHA `f488390d18ce1a5d0da510e387b2031411672104f854f564a0d7b456314485e0`, accepted SHA `55638ffbddd7d9daccb8bfa43f9523942c430e6d6fc18d511dcaf9a1f0d51ab7`, and first diff `defined=16 abs=33`, while reusing one selected-def bitmap per exact-literal singleton lane family and replaying at `958.441ms` and `987.611ms` Starshine pass-local versus `1052.190ms` and `966.653ms` Binaryen. Its 10k direct compare at `.tmp/pass-fuzz-dae-selected-singleton-reuse-10000` preserved the current raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` mismatches, `0` validation failures, `25` Binaryen/tool command failures) with Starshine smaller in all mismatch artifacts. The follow-up selected scratch-bitmap reuse slice keeps the artifact byte-identical at the same raw/accepted SHAs and first diff while reusing one singleton scratch bitmap across the Func236, const-if, dropped-result, mid literal, immutable-global, and high-literal lanes; `.tmp/dae-selected-scratch-reuse-artifact` replayed at `906.423ms` Starshine pass-local versus `948.792ms` Binaryen, and `.tmp/pass-fuzz-dae-selected-scratch-reuse-10000` preserved the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier with Starshine smaller in all mismatch artifacts. The core empty-skip-bitmap slice then replaces the per-core-iteration all-false `defined`-sized skip bitmap with one empty skip bitmap because no current core-loop caller marks defs between scans; it keeps the artifact byte-identical at the same raw/accepted SHAs and first diff, replays at `924.352ms` Starshine pass-local versus `911.320ms` Binaryen, and `.tmp/pass-fuzz-dae-core-empty-skip-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The selected scratch sharing slice then reuses one singleton selected-def scratch across the reverse high-wrapper chain, boundary wrapper/callee queue, Func236 local-retarget, const-if, dropped-result, mid literal, immutable-global, and high-literal lanes; it keeps the artifact byte-identical at raw SHA `f488390d18ce1a5d0da510e387b2031411672104f854f564a0d7b456314485e0`, accepted SHA `55638ffbddd7d9daccb8bfa43f9523942c430e6d6fc18d511dcaf9a1f0d51ab7`, and first diff `defined=16 abs=33`, replays at `917.564ms` Starshine pass-local versus `906.751ms` Binaryen, and `.tmp/pass-fuzz-dae-selected-scratch-shared-10000` preserves the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier with command failures classified as `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`; Starshine is smaller in all `3897` mismatch artifacts with canonical delta sum `-5,078,412`. The lazy mutation-buffer slice then defers the module raw-cleanup function copy until the first body rewrite and defers selected const-if function/touched copies until that helper actually changes a selected function; it keeps the same raw/accepted SHAs and first diff, replays at `960.959ms` Starshine pass-local versus `1073.920ms` Binaryen, and `.tmp/pass-fuzz-dae-lazy-mutation-buffers-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The raw-cleanup touched-flag reuse slice then stops allocating a fresh function-count touched bitmap inside module raw cleanup, using the existing touched-def bitmap for live-copy gating and returned touched state; it keeps the artifact byte-identical at the same raw/accepted SHAs and first diff, replays at `889.525ms` Starshine pass-local versus `878.035ms` Binaryen, and `.tmp/pass-fuzz-dae-raw-cleanup-touch-reuse-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The if-condition boolean cleanup slice then pivots back to useful raw cleanup at the live `defined=16 abs=33` frontier: `if (i32.eqz (i32.eqz x))` becomes `if (x)`, preserving evaluation while shrinking the debug artifact by `28` raw and canonical bytes (`3,508,220 -> 3,508,192` raw, `3,515,213 -> 3,515,185` accepted), validating with `wasm-opt --all-features`, and replaying at `937.401ms` Starshine pass-local versus `917.628ms` Binaryen; `.tmp/pass-fuzz-dae-if-condition-bool-cleanup-10000` preserves the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier and all-smaller mismatch size scan. The local-copy chain cleanup then folds pure producer copy chains through `local.tee` while preserving all local writes, shrinking the debug artifact by another `154` raw and canonical bytes (`3,508,192 -> 3,508,038` raw, `3,515,185 -> 3,515,031` accepted), validating with `wasm-opt --all-features`, and replaying at `870.772ms` Starshine pass-local versus `912.214ms` Binaryen; `.tmp/pass-fuzz-dae-local-copy-chain-cleanup-10000` preserves the same 10k frontier and all-smaller mismatch size scan. A bounded post-cleanup audit in `0574` found no next safe coherent raw-cleanup slice with current artifact impact: `br_if` double-`eqz` truthiness cleanup is semantically safe but byte-neutral on the debug artifact, value-position double-`eqz` cleanup is unsafe without downstream consumer proof because it would remove booleanization, and broader effect-fed local copy rewrites are deferred unless a future artifact/frontier diff justifies the proof burden. The touched-bitmap merge cleanup then makes the scheduler-local touched accumulator mutate in place, adopts the first returned touched bitmap as that one-shot accumulator instead of copying it, and lets the final module raw-cleanup call skip returning an identical touched bitmap, preserving byte-identical artifact output (raw SHA `12c2bbc49066e145c3818584ce95056095a233dfb2fbeaa70830b21023d2de03`, accepted SHA `6396b12f5e363adac45a6a81e04dec56b70ca1b200198c84f6eb48f7e15ab0c5`, first diff `defined=16 abs=33`) while `.tmp/pass-fuzz-dae-adopt-first-touched-10000` preserves the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier and all-smaller mismatch size scan. A follow-up nested-scheduler runtime cleanup returns immediately on `large-touched-set` before scanning touched bodies for per-function size reasons; the skip trace and output are unchanged because the large-set reason already dominates. [research note 0570](./index.md) classifies this remaining first diff as a local/control carrier shape, not another boundary rewrite miss: Binaryen's printed Func237 starts with a side-effect/trap-preserving void `block`, then materializes the live default carrier as `i32.const 0` in a later `local.set` before wrapper-object stores; Starshine still lowers the same region as a leading value block assigned to a temp local, with the surviving live edge carrying the default-zero value through local/control scaffolding. The exact Binaryen mechanics are inferred from artifact output and prior nested-rerun docs rather than from a dedicated upstream helper citation, so keep this claim scoped to the observed artifact family. Broad selected Func237 nested cleanup, reduced nested cleanup without DCE/coalesce, forced existing nested cleanup, selected coalescing, and a narrow default-carrier prototype all failed to advance the first diff and either consumed or exceeded the already tight runtime headroom. Future work should therefore avoid another broad selected cleanup pass and should only try a much narrower raw/HOT structural fold if it can match the real lowered shape and preserve trap/effect order. Because exact Binaryen byte/shape parity is not the signoff goal, the subsequent usefulness-oriented raw cleanup intentionally accepts a different first-diff frontier: `.tmp/dae-touched-raw-cleanup-artifact3` first stripped touched no-op debris, removed `597` normalized printed `nop`s on the debug artifact, shrank Starshine raw wasm from `3,536,456` to `3,533,111` bytes and canonical wasm from `3,543,259` to `3,539,980` bytes, validated with `wasm-opt --all-features`, and kept the 1000-case direct fuzz smoke at the established representation-only frontier (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures in `.tmp/pass-fuzz-dae-touched-raw-cleanup-1000`). A follow-up post-touch module raw cleanup widens that cheap debris strip to all module bodies without widening the nested scheduler's touched set: `.tmp/dae-module-raw-pure-leaf-cleanup-artifact3` removes all `6,150` remaining normalized printed `nop`s on the debug artifact, reduces dropped pure leaves in normalized text (`i32.const` drops `3,115 -> 60`, `i64.const` drops `6 -> 0`, `local.get` drops `603 -> 425`, `global.get` drops `1 -> 0`), shrinks Starshine raw wasm from `3,533,111` to `3,517,075` bytes and canonical wasm from `3,539,980` to `3,523,993` bytes, validates with `wasm-opt --all-features`, keeps pass-local timings within target (`874.285ms`, `918.233ms`, `936.437ms` versus Binaryen `982.459ms`, `991.208ms`, `1152.360ms`), and preserves the established 1000-case direct fuzz smoke shape (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures in `.tmp/pass-fuzz-dae-module-raw-pure-leaf-cleanup-1000`). The 10k usefulness signoff at `.tmp/pass-fuzz-dae-useful-raw-cleanup-10000` then preserved the latest full active fuzz frontier exactly (`9975/10000` compared, `9622` normalized matches, `353` normalized mismatches, `0` validation failures, `25` Binaryen/tool command failures classified as `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`), so the current raw-cleanup DAE output is usefulness-signable even though exact Binaryen byte/shape parity remains intentionally out of scope. A follow-up root-unreachable suffix cleanup in `.tmp/pass-fuzz-dae-live-dead-suffix-repair3-10000` deliberately prioritizes useful shrinking over normalized shape parity: it ignores direct-call blockers that occur only after a root `unreachable`, repairs no-param dead-suffix callers when result removal would otherwise strand typed call results, and removes no-param/parameterized private results from unreachable-only self/side-call suffixes when the live prefix is unobservable. That run kept validity at `0` validation failures with the same `25` Binaryen/tool command failures, but increased normalized mismatches to `581` because Starshine now collapses more private dead-suffix bodies than Binaryen in many generated cases; among mismatch artifacts Starshine was smaller in `425`, equal in `65`, larger in `91`, with canonical mismatch-size sum `-142,851` bytes (mean `-245.9` bytes), so this is a utility/size improvement rather than a byte-parity improvement. A follow-up numeric raw-cleanup slice at `.tmp/pass-fuzz-dae-constsafe-raw-cleanup-10000` extends that useful-shrinking policy from pure leaves to nontrapping numeric expressions and constant-safe integer div/rem. It keeps `0` Starshine validation failures with the same `25` Binaryen/tool command failures, but intentionally increases normalized mismatches to `3,897` because Starshine now removes much more pure debris than Binaryen in generated dead-prefix/suffix bodies. Among mismatch artifacts Starshine is smaller in all `3,897`, with canonical mismatch-size sum `-4,815,090` bytes (mean `-1,235.6` bytes). The former worst Starshine-larger `case-006030-gen-valid` flips from `+830` bytes (`1735` Starshine vs `905` Binaryen) to `-702` bytes (`203` Starshine vs `905` Binaryen). A subsequent stack-shaped cleanup generalization at `.tmp/pass-fuzz-dae-stack-raw-cleanup-10000` keeps the same aggregate 10k counts and size evidence while covering arbitrary straight-line nontrapping pure expression stacks in focused tests. Dropped `select` cleanup then extends that same stack contract to the three consumed operands, removing only when both values and the condition are themselves recognized pure/nontrapping; `.tmp/pass-fuzz-dae-select-raw-cleanup-10000` reported `9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures. A later float-to-int cleanup treats saturating truncation as nontrapping pure unary debris and removes simple dropped regular float-to-int truncation only for constant operands whose truncated result is representable; `.tmp/pass-fuzz-dae-float-trunc-cleanup-10000` reported the same aggregate counts (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures). The first narrow ref cleanup then adds `ref.null` leaves plus `ref.is_null` unary cleanup while explicitly preserving `ref.as_non_null` on a null operand; `.tmp/pass-fuzz-dae-ref-null-cleanup-10000` again reported the same aggregate counts (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The follow-up `ref.eq` cleanup extends the same contract to dropped ref comparisons only when both operand stacks are recognized pure/nontrapping, and focused coverage preserves a `ref.as_non_null` operand stack; `.tmp/pass-fuzz-dae-ref-eq-cleanup-10000` reported the same aggregate counts and command-failure classes. The next narrow ref cleanup adds dropped `ref.i31`, `any.convert_extern`, and `extern.convert_any` stacks under the same recognized pure/nontrapping unary contract, while still preserving `i31.get_*` on nullable `i31ref` and `ref.as_non_null`-fed conversion stacks because those operand stacks may trap; `.tmp/pass-fuzz-dae-ref-convert-i31-cleanup-10000` reported the same aggregate counts and command-failure classes. A follow-up recognizes `i31.get_s` / `i31.get_u` as removable only when the consumed operand stack is proven to produce the reference via `ref.i31`, making the `i31.get_*` nontrapping by construction while still preserving nullable `i31ref` inputs; `.tmp/pass-fuzz-dae-i31-get-cleanup-10000` reported the same aggregate counts and command-failure classes. The next tiny numeric cleanup fills the corresponding constant-safe signed-division hole for dropped `i64.div_s`: it removes only constant operands where the divisor is nonzero and the signed-minimum divided by `-1` overflow trap is impossible, with focused coverage preserving that overflow case. A follow-up SIMD-core cleanup removes only `v128.const`, nontrapping unary `v128.not` / `v128.any_true`, bitwise binary `v128.and` / `v128.andnot` / `v128.or` / `v128.xor`, and `v128.bitselect` stacks when all operands are recognized pure/nontrapping; focused coverage preserves a `v128.load`-fed `v128.bitselect` because the memory load may trap. The next SIMD-unary follow-up widens that same contract to lane-local nontrapping unary integer/float ops and integer all_true/bitmask reductions, again preserving a `v128.load`-fed unary stack. The SIMD-binary follow-up applies the same all-operands-recognized contract to audited two-`v128` lane comparisons and basic nontrapping lane arithmetic, while still excluding mixed-arity shifts, lane extract/replace, narrowing/conversion, extmul/dot, relaxed SIMD, and memory operations; focused coverage strips `i8x16.add`, `i32x4.eq`, and `f32x4.div` constant stacks and preserves a `v128.load`-fed `i8x16.add`. The 10k compare at `.tmp/pass-fuzz-dae-simd-binary-cleanup-10000` preserved the established raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`). The next SIMD-splat cleanup treats the six lane splats as nontrapping pure unary debris over scalar operands, with focused coverage removing constant-fed `i8x16.splat` and `f64x2.splat` while preserving an `i32.load`-fed `i8x16.splat` because the scalar load may trap; `.tmp/pass-fuzz-dae-simd-splat-cleanup-10000` again preserved the same 10k aggregate counts and command-failure classes. The SIMD shuffle/swizzle follow-up adds `i8x16.shuffle` and `i8x16.swizzle` to the same audited two-`v128` nontrapping pure binary contract; focused coverage strips constant-fed shuffle/swizzle drops and preserves load-fed shuffle/swizzle stacks because the memory load may trap. `.tmp/pass-fuzz-dae-simd-shuffle-swizzle-cleanup-10000` preserved the established raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`). The SIMD lane cleanup then treats immediate lane extracts as nontrapping pure unary debris and immediate lane replacements as mixed scalar/vector but still two-operand nontrapping pure binary debris when every consumed operand stack is recognized pure/nontrapping; focused coverage strips constant-fed `i32x4.extract_lane`, `f64x2.extract_lane`, `i8x16.replace_lane`, and `f64x2.replace_lane`, while preserving `v128.load`-fed extracts, scalar-load-fed replacements, and `v128.load`-fed replacements. `.tmp/pass-fuzz-dae-simd-lane-cleanup-10000` again preserved the same aggregate frontier and command-failure classes. The SIMD shift follow-up applies the same mixed vector/scalar binary contract to integer lane shifts (`i8x16`, `i16x8`, `i32x4`, and `i64x2` shl/shr_s/shr_u), stripping constant-fed `i8x16.shl` and `i32x4.shr_u` stacks while preserving v128-load-fed and scalar-load-fed shifts; `.tmp/pass-fuzz-dae-simd-shift-cleanup-10000` again preserved the established 10k frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures with the same classes). The SIMD extension follow-up treats integer lane `extend_low/high` and `extadd_pairwise` ops as nontrapping pure unary debris; focused coverage strips constant-fed `i16x8.extend_low_i8x16s` and `i32x4.extadd_pairwise_i16x8u` stacks while preserving `v128.load`-fed extension/extadd stacks. `.tmp/pass-fuzz-dae-simd-extend-cleanup-10000` again preserved the same 10k frontier and command-failure classes. The SIMD narrow/extmul/dot follow-up applies the audited two-`v128` binary cleanup contract to integer lane narrowing, extmul low/high, and `i32x4.dot_i16x8s`: typechecking confirms each consumes `v128 v128 -> v128`, and the instructions have no memory/control effects or trap condition. Focused coverage strips constant-fed `i8x16.narrow_i16x8s`, `i16x8.narrow_i32x4u`, representative extmul ops, and `i32x4.dot_i16x8s` stacks while preserving load-fed narrow/extmul/dot stacks. `.tmp/pass-fuzz-dae-simd-narrow-extmul-dot-cleanup-10000` preserved the same 10k frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures with the established classes). The SIMD conversion follow-up then covers only non-relaxed unary conversion/trunc-saturate/demote/promote operators (`f32x4.demote_f64x2_zero`, `f64x2.promote_low_f32x4`, `i32x4.trunc_sat_f32x4{s,u}`, `i32x4.trunc_sat_f64x2{s,u}_zero`, `f32x4.convert_i32x4{s,u}`, and `f64x2.convert_low_i32x4{s,u}`): typechecking confirms each is a single-`v128` value operator, the saturating variants do not trap on out-of-range/NaN inputs, and none has memory/control effects. Focused coverage strips representative constant-fed conversion stacks while preserving `v128.load`-fed conversion stacks because the input load may trap. `.tmp/pass-fuzz-dae-simd-conversion-cleanup-10000` preserved the same aggregate frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The next ref-test cleanup adds only `ref.test` to the pure unary debris set: typechecking confirms it consumes one reference and produces `i32`, its runtime behavior is a boolean test rather than a trapping cast, and focused coverage removes a `ref.null; ref.test; drop` stack while preserving a `ref.as_non_null; ref.test; drop` stack because the operand stack may still trap. `.tmp/pass-fuzz-dae-ref-test-cleanup-10000` preserved the same aggregate frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The descriptor-test follow-up extends the same audited unary contract to `ref.test_desc`: validation resolves/checks the target reftype statically, typechecking confirms one reference operand to `i32`, and the runtime operation is the descriptor-form boolean test rather than a trapping cast; focused coverage removes `ref.null; ref.test_desc; drop` while preserving `ref.as_non_null; ref.test_desc; drop` because the operand stack may trap. `.tmp/pass-fuzz-dae-ref-test-desc-cleanup-10000` preserved the same aggregate frontier and command-failure classes. A later size-leaf cleanup adds dropped `memory.size` and `table.size`: typechecking validates only the static memory/table index and pushes `i32`, HOT flags mark both as value-only/nontrapping, and reading the current size has no side effect when the result is immediately dropped. Focused coverage removes both dropped size queries from a module with defined memory/table sections while existing load-fed memory/SIMD stacks remain preserved. The nullable-cast follow-up removes `ref.null`-rooted `ref.cast (ref null ...)` stacks, including through a following pure `ref.test`, while preserving `ref.cast (ref null ...); ref.as_non_null` because the assertion would still trap on null. The next i31-supertype cast follow-up broadens only the already fact-sensitive `ref.i31` proof: a syntactic `ref.i31` result is non-null and is a subtype of `i31`, `eq`, and `any`, so dropped casts to those targets are removed while unrelated ref-cast targets remain preserved. The const-safe chain follow-up then allows already-audited constant-safe float-to-int truncation and integer div/rem operations to feed nontrapping wrappers such as `eqz`, but only when the immediate literal operands prove no trap is possible. The local/global self-assignment follow-up removes `local.get $x; local.set $x` and `global.get $g; global.set $g`, strips redundant `local.tee $x` after `local.get $x`, and then lets the existing pure-stack cleanup remove now-dropped self-tee reads; different-local and different-global writes remain preserved because they change state. The later state readback follow-up removes same-slot `local/global.set; local/global.get; drop` and `local/global.set; local/global.get; local/global.set` tails while preserving the original write and producer effects. The dropped-tee sibling rewrites `value; local.tee $x; drop` to `value; local.set $x`, and does the same when the tee result feeds a following dropped nontrapping pure unary/binary suffix, preserving producer evaluation and the local write while removing only the discarded computed stack result. The integer identity sibling strips live `i32`/`i64` identity tails on both safe sides: right-constant add/sub/or/xor/shift/rotate-by-zero, multiply-by-one, and bitwise-and-with-all-ones tails (`value; identity const; op -> value`), plus left-constant add/or/xor-by-zero, multiply-by-one, and bitwise-and-with-all-ones tails only when the right producer is a stack-neutral value leaf. It also canonicalizes right-zero `i32`/`i64` equality to `eqz` and inequality to `eqz; i32.eqz`, plus the same left-zero compare forms only when the right producer is stack-neutral. For live `if` conditions specifically, it rewrites trailing `i32.eqz; i32.eqz` to Binaryen-like `i32.const 0; i32.ne`, preserving producer evaluation/traps/effects while keeping the condition explicitly booleanized for artifact parity. Same-local/same-global integer and `ref.eq` self comparisons fold to constants (`eq`, `le`, `ge`, and `ref.eq` are true; `ne`, `lt`, and `gt` are false), while float self comparisons remain preserved because NaN is not reflexive. Same-local/same-global integer self binary tails also simplify `x & x` / `x | x` to one read and `x ^ x` / `x - x` to the typed integer zero. It preserves the live producer while removing only a nontrapping identity, zero-compare, reflexive self-comparison pair, or adjacent same-read integer self-binary pair; left-zero subtraction, left-zero shifts, stack-consuming right producers, stack-consuming left-compare producers, different-slot comparisons, float comparisons, and other non-identities remain preserved. Its live-copy part remains limited to DAE-touched functions and pure/nontrapping producer stacks: `value; local.set $x; local.get $x` becomes `value; local.tee $x`, `value; local.tee $x; local.set $x` becomes `value; local.set $x`, and short pure copy chains such as `value; local.set $a; local.get $a; local.set $b` / `...; local.get $b; local.set $c` become tee chains that evaluate the producer once while preserving each local write; memory/call/effect-fed live copies are preserved so touched-only nested pass evidence such as `pick-load-signs` is not preempted. The pure-control follow-up strips dropped zero-parameter single-result `block`, `loop`, catch-free `try_table`, and `if` wrappers only when every value-producing body path is a straight-line pure/nontrapping value stack. Dropped pure `if` branches are removed while either stripping a pure/nontrapping condition stack or preserving an unrecognized condition as `drop`; focused negatives preserve trapping `unreachable` bodies, caught value `try_table` wrappers, plus type-index wrappers that consume parameters. The type-index extension covers only resolved `[] -> [T]` `TypeIdxBlockType` wrappers, so deleting the wrapper cannot strand implicit control arguments. The void block/loop cleanup similarly removes only already-empty zero-parameter zero-result wrappers (`VoidBlockType` or resolved `[] -> []` type-index block types) after recursive pure-debris cleanup, preserving parameter-consuming and trapping/effectful structures. The empty-`if` cleanup uses the same zero-param/zero-result type guard and empty-branch proof, then either strips a recognized pure condition stack or emits `drop` to preserve an unrecognized condition's evaluation and traps/effects. The `try_table` sibling removes empty zero-param/zero-result wrappers even when they carry catches: after recursive cleanup empties the body there is no instruction left that can throw into a catch, while parameter-consuming and trapping/effectful bodies remain preserved. Possibly trapping numeric/ref/SIMD operations are still preserved unless local facts prove safety; when a dropped nontrapping pure wrapper around such an operation is removed, the trapping/effectful producer is retained and still dropped. For the current Binaryen-parity pivot, dropped local-read binary expressions such as `local.get; const; add; drop` are preserved even though equivalent pure debris could be removed, because Binaryen keeps that local-read drop shape in the live debug-artifact frontier. This remains a conservative cleanup with several parity-preserving exceptions rather than a pure size-only cleanup.

A 2026-05-22 Binaryen-shape slice made the selected Func236 local-retarget cleanup stricter than the default raw cleanup because the current artifact shows Binaryen stripping that function's extra `nop`s and dropped `i32.const 0` even though earlier frontiers needed raw debris preservation elsewhere. `.tmp/dae-func236-strict-debris-artifact` advances the first diff from `.tmp/dae-effect-localget-debris-artifact` `defined=236 abs=253` to the known Func237 local/control-carrier frontier `defined=237 abs=254`, validates with `wasm-opt --all-features`, and `.tmp/pass-fuzz-dae-func236-strict-debris-10000` preserves the established direct-fuzz counts: `9975/10000` compared, `6078` normalized matches, `3897` mismatches, `0` validation failures, and `25` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`).

A later Func237 Binaryen-shape slice folds selected immediate single-use temp copies inside Func237 when the temp has exactly one root-body write and read, the intervening straight-line gap does not reference either local or branch out, and no function-local `try_table` can observe call-thrown exceptions through a catch. `.tmp/dae-func237-local-copy-artifact` still differs first at `defined=237 abs=254`, but it removes the `$112 -> $51`, `$84 -> $68`, and `$83 -> $71` temp-copy pairs from the current frontier, validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`939.801ms` Starshine versus `958.692ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-local-copy-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The next Func237 Binaryen-shape slice handles the adjacent used-temp variant: if a high temp is written once, all its reads occur before a final `local.get temp; local.set target` copy, the intervening region has no target access, temp writes, branch exits, or local `try_table` catch exposure, Starshine retargets the original write to the target and rewrites those pre-copy temp reads to the target. `.tmp/dae-func237-used-temp-artifact` still differs first at `defined=237 abs=254`, but it removes the `$108 -> $33`, `$109 -> $27`, `$110 -> $17`, `$104 -> $35`, `$37 -> $34`, `$98 -> $29`, `$31 -> $28`, `$90 -> $23`, `$25 -> $22`, `$85 -> $47`, and `$82 -> $76` copy families from the current frontier, validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`902.703ms` Starshine versus `902.103ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-used-temp-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A follow-up declaration cleanup trims only trailing unused Func237 locals after the selected body-shape rewrites. `.tmp/dae-func237-trim-locals-artifact` still differs first at `defined=237 abs=254`, but the normalized Func237 declaration list now ends at `$143` instead of keeping unused `$144..$147`, validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`895.818ms` Starshine versus `854.642ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-trim-locals-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A selected Func237 low-local map then retargets the observed remaining setup carriers toward Binaryen's low-local shape: the initial object check now writes through `$0`, `$2`, `$1`, `$8`, and `$6`, the two wrapper allocations use `$6` and `$8`, and the `$257` result uses `$11` instead of Starshine-only high temps. `.tmp/dae-func237-low-local-map-artifact` still differs first at `defined=237 abs=254`, validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`877.674ms` Starshine versus `837.457ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-low-local-map-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A follow-up selected Func237 nested-carrier low-local map extends that direct Binaryen-shape retargeting into the first-loop branch and merge region, including the loop item/index carriers, call-indirect input/result carriers, inner collection loop carriers, and final return wrapper carriers. `.tmp/dae-func237-nested-local-map-artifact` still differs first at `defined=237 abs=254`, but the normalized Func237 declaration list now matches Binaryen's low local range (`$4..$14`) instead of keeping Starshine-only `$15..$143`; the output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`919.747ms` Starshine versus `886.848ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-nested-local-map-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A selected Func237 outer-loop carrier reshape then converts the first loop's explicit `if` / `local.set $0` / backedge form into Binaryen's result-block carrier wrapper when branch scanning proves the moved prefix and else body do not escape the original arm. The same slice rewrites the first post-`local.tee $14 (local.get $0)` indexed read back to `$0` before either local is written, matching Binaryen's preserved carrier source. `.tmp/dae-func237-outer-loop-carrier-artifact3` still differs first at `defined=237 abs=254` because the nested dropped result-block / branch-carrier region remains structurally different, but the outer loop wrapper and first indexed read now match Binaryen's shape; the output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`894.520ms` Starshine versus `927.153ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-outer-loop-carrier-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A follow-up selected Func237 nested branch-result slice removes the inner dropped result-block value wrapper inside the first branch/merge region by shifting label depths across the removed result label and preserving the wrapper block that branch-skipped paths target. `.tmp/dae-func237-deep-branch-result-artifact` still differs first at `defined=237 abs=254`, but the first inner merge now uses a direct `br` value carrier instead of Starshine's extra `br (block (result i32) ...)` wrapper; the output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`877.248ms` Starshine versus `857.222ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-deep-branch-result-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The next selected Func237 branch-shape slice folds the nested zero-branch wrapper, splices terminal `br 0` block wrappers, and hoists the leading retain-call prefix out of the nested zero-result block when the prefix has no intervening branch target. `.tmp/dae-func237-inner-loop-late-artifact` still differs first at `defined=237 abs=254`; the front of the nested branch/merge region lines up through the leading retain call, while the remaining frontier is the later `$867` literal-call arity difference and Binaryen's larger inner iteration-loop result-block carrier. The output validates with `wasm-opt --all-features` and keeps pass-local timing inside the 2x target (`821.315ms` Starshine versus `832.297ms` Binaryen).

A follow-up selected Func867 default-argument specialization removes the observed all-default `$867` call operands and materializes those constants in the callee only when the callee has the exact seven-parameter default-wrapper signature, the parameters are not written, the function is not escaped or tail-called, and the manually counted direct callers all have the same literal actuals under the existing uniform-constant collector. `.tmp/dae-func867-defaults-artifact6` still differs first at `defined=237 abs=254`, but the `$867` call arity in Func237 now matches Binaryen; the remaining live frontier is Binaryen's larger inner iteration-loop result-block carrier. The output validates with `wasm-opt --all-features` and keeps pass-local timing inside the 2x target (`962.717ms` Starshine versus `856.788ms` Binaryen).

The next Func237 carrier slice reruns the selected inner-iteration-loop wrapper to a fixed point and handles the real post-`$867` dropped result-block carrier by inlining a terminal `br 0` void block, preserving effect order while moving only the discarded result-block `i32.const 0` into Binaryen's `br`-value loop-carrier shape. `.tmp/dae-func237-inline-inner-block-artifact` still differs first at `defined=237 abs=254`, but the earlier inner iteration-loop carrier now matches Binaryen through the `local.set $4 (block (result i32) ...)` form; the new live frontier is the later nested dropped result-block in the `$4443` / `$872` branch. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`937.682ms` Starshine versus `906.346ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-inline-inner-block-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

A follow-up selected Func237 branch-carrier slice folds that later dropped result-block into Binaryen's void-block shape, then reruns the leading retain/call hoist because the fold exposes another hoistable prefix. `.tmp/dae-func237-hoisted-dropped-branch-final-artifact` still differs first at `defined=237 abs=254`, but the `$4443` / `$872` dropped result-block wrapper now matches Binaryen; the remaining frontier is a smaller branch-placement shape where Binaryen emits a common `(br $block10)` after an `if`, while Starshine keeps the branch inside the `else` arm and leaves the following `unreachable`. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`925.674ms` Starshine versus `900.733ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-hoisted-dropped-branch-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The selected Func237 common-branch slice then moves terminal else-arm branches to a common post-`if` branch when the then arm is proven not to fall through, covering both raw terminal-`if` shape and normalized `if; unreachable` shape. `.tmp/dae-func237-common-branch-final-artifact` advances the direct debug-artifact first diff out of Func237 to `defined=242 abs=259`; Func237 now matches the Binaryen normalized function. The new live frontier is a Func242 call to `$327` where Binaryen has removed three default operands and cleaned the callee, while Starshine still passes `i32.const -1`, `i64.const 4294967296`, and `i32.const -1`. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1015.657ms` Starshine versus `914.098ms` Binaryen), and `.tmp/pass-fuzz-dae-func237-common-branch-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure). This closes the historical `[DAE]008` Func237 frontier task: the landed Func237 selected local/control slices are recorded here, the reverted or intentionally deferred broad cleanup probes remain in [research note 0570](./index.md), and future Func237 work should require a new current-frontier repro rather than reopening the old direct-artifact blocker.

The next selected frontier slice removes Func242's `$327` default-operand gap by reusing the existing exact-literal constant-actual machinery for selected Func327, then inlines Func327's straight-line terminal call local-set operands when every temporary is distinct, written before the terminal call, and not read by any later operand segment. A small selected Func246 builder fold also matches Binaryen's reuse of released param `$0` as the allocation carrier and inlines the one-use payload constant. `.tmp/dae-func246-builder-final-artifact` advances the direct debug-artifact first diff to `defined=250 abs=267`; the new live frontier is a dropped `$4229` result call inside Func250 where Binaryen has removed the callee result and Starshine still emits `drop (call $4229 ...)`. The output validates with `wasm-opt --all-features` and keeps pass-local timing inside the 2x target (`1129.842ms` Starshine versus `1065.230ms` Binaryen).

A follow-up selected dropped-result/default-operand slice then originally extended the selected dropped-result lane to Func3566 and Func4229, and added Func869 to the selected exact-literal lane for its uniform `i64.const 4294967296` suffix operands. Func4229's handpicked fallback entry was later retired by note `0672` after the ordinary fact/core path proved sufficient for the current broad-large fixture; note `0674` likewise retired Func298 from handpicked fallback coverage, note `0675` retired Func427 under the same broad-large fact/core proof, note `0676` retired Func299 from the selected fallback loop/list with focused fallback-list and trace coverage, note `0677` retired Func459, note `0678` retired Func472 while retargeting selected-fallback trace coverage to Func476, notes `0679` through `0685` retired Func476, Func3566, Func3732, Func3814, Func4232, Func4240, and Func4241, and note `0686` retired final late-cluster Func4242. The handpicked selected dropped-result fallback list is now empty for the current DAE004 surface, pending `[DAE004-H]`/`[DAE004-I]` artifact, timing, and direct-compare closeout. `.tmp/dae-func869-defaults-artifact` advances the direct debug-artifact first diff to `defined=256 abs=273`; the new live frontier is Func256 body-shape drift where Binaryen has a tighter void loop over `$2/$3` and Starshine still keeps the pre-cleanup `$11..$13` carrier/debris shape. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1229.223ms` Starshine versus `1000.480ms` Binaryen), and `.tmp/pass-fuzz-dae-func869-defaults-1000` preserves the short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The 2026-05-24 Func256/Func260 helper slice then converts the selected Func256 single-`i32` typed-loop parameter into Binaryen's explicit `$2` carrier and `$3` payload local shape, swaps the pure `idx < load` condition to Binaryen's `load > idx` ordering while preserving load traps and call/effect order around `$4227`, wraps the loop in Binaryen's result-block carrier form, and strips the trailing dropped `i32.const 0` debris. The same slice adds selected exact-literal/default folding for Func267 and Func268, including repeated constant-if folding for Func268's nested equality chain. `.tmp/dae-func268-fold-artifact` advances the direct debug-artifact first diff to `defined=272 abs=289`; the new frontier is a Func272 call-operand/type-shape drift where Starshine still passes one extra `i32.const 0` compared with Binaryen. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1194.584ms` Starshine versus `859.217ms` Binaryen), and `.tmp/pass-fuzz-dae-func268-fold-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The next selected Func327/Func328 default-operand slice folds selected Func327 constant-result `if` operands immediately before the selected Func328 exact-literal pass. That exposes the uniform zero actual behind Func272's call to `$328`, lets the existing read-only exact-literal rewrite remove the extra parameter, and materializes the zero in Func328's store body. `.tmp/dae-func327-constif-artifact` advances the direct debug-artifact first diff to `defined=279 abs=296`; the new frontier is another call-operand/type-shape drift in Func279's call to `$1795`, where Starshine still passes two extra `i32.const 0` operands compared with Binaryen. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1228.554ms` Starshine versus `931.113ms` Binaryen), and `.tmp/pass-fuzz-dae-func327-constif-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The selected Func1794/Func1795 default-operand slice then folds Func1794's constant-result `if` locals and inlines its terminal-call local-set operands before selected Func1795 exact-literal/default rewriting. That exposes the same uniform zero actuals already syntactic at Func279's `$1795` call, lets the existing read-only exact-literal rewrite remove the two extra Func1795 parameters, and materializes zeros at the callee stores. `.tmp/dae-func1795-defaults-artifact` advances the direct debug-artifact first diff to `defined=280 abs=297`; the new frontier is Func280 result-shape drift where Binaryen leaves result-producing `$298` calls live in branch value position and Starshine still wraps those calls in `drop`. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1328.215ms` Starshine versus `971.553ms` Binaryen), and `.tmp/pass-fuzz-dae-func1795-defaults-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The selected Func298 dropped-result slice then adds Func298 to the existing selected dropped-result lane. The inspected Func280 frontier had all direct `$298` uses immediately dropped in Starshine while Binaryen had already made `$298` void, so the existing result-removal transform can preserve call side effects and operand evaluation order by removing the callee result type, stripping adjacent call drops at callsites, and dropping the callee's formerly returned final `$4228` result internally. `.tmp/dae-func298-dropped-result-artifact` advances the direct debug-artifact first diff to `defined=281 abs=298`; inspection of the normalized extracted function shows the live hunk is Func281's nested call to `$3737`, where Starshine still passes the suffix `i32.const 1024` that Binaryen removed. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1455.541ms` Starshine versus `973.849ms` Binaryen), and `.tmp/pass-fuzz-dae-func298-dropped-result-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `1` Binaryen/tool command failure).

The selected forwarded-default callee slice then removes Func3765's forwarded suffix `i32.const 1024` parameter after inspecting the Func281 -> Func3737 -> Func3765 chain: Func281's only `$3737` call passes literal `1024`, Func3737 forwards that value as the fourth `$3765` operand, and Binaryen has already removed the `$3765` parameter. `.tmp/dae-func3737-wrapper-artifact-final` kept the direct debug-artifact first diff at `defined=281 abs=298` because Func3737's now-dead wrapper parameter still remained at the callsite, but Func3765's signature and Func3737's call to it matched Binaryen's three-operand shape. The output validated with `wasm-opt --all-features`, kept pass-local timing inside the 2x target (`1288.554ms` Starshine versus `897.577ms` Binaryen), and `.tmp/pass-fuzz-dae-func3765-defaults-1000` preserved the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

A follow-up selected Func3737 wrapper-param slice then removes Func281's immediate suffix `i32.const 1024` call operand and rewrites Func3737 to three params. This lane is deliberately narrower than generic unread-param rewriting because the raw Func281 callsite carries the first `$3737` operand through an ambient `local.tee` stack shape that the generic callsite slicer cannot prove today; the selected helper instead proves the exact known direct-caller family, the immediate pure suffix literal, and the non-written removed param, then materializes `1024` for any remaining callee reads before remapping locals. `.tmp/dae-func3737-wrapper-param-artifact` advances the direct debug-artifact first diff to `defined=285 abs=302`; Func3737's signature now matches Binaryen, and Func281 no longer passes the suffix operand. The new live frontier is Func285's call to `$4206`, where Starshine still passes an extra `i32.const 0` operand that Binaryen has removed. The output validates with `wasm-opt --all-features`, keeps pass-local timing inside the 2x target (`1337.628ms` Starshine versus `884.364ms` Binaryen), and `.tmp/pass-fuzz-dae-func3737-wrapper-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected exact-literal frontier was then extended to the inspected Func1777, Func288, and Func4206 default-actual callees. Func4206 removes the uniform middle `i32.const 0` actual from the four direct callers (Func285, Func1780, Func1822, and Func1825), Func1777 removes Func286's suffix `i32.const 10000` pointer actual while materializing that pointer in the callee body, and Func288 removes Func287's zero default actual. `.tmp/dae-func288-1777-4206-defaults-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact frontier into Func287's remaining local/debris shape: the `$288` call operand now matches Binaryen, but Starshine still retains extra locals, a `nop`, and different local numbering around the `$4194` / `$24` setup. Pass-local timing remains inside the 2x target (`1402.134ms` Starshine versus `887.952ms` Binaryen), and `.tmp/pass-fuzz-dae-func288-1777-4206-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

A narrow selected Func287 setup-local compaction then strips that leading `nop`, rewrites the high setup locals `$2/$4/$5` into Binaryen's two-local `$0/$1` shape, and trims the now-unused declarations after checking the exact straight-line setup body. `.tmp/dae-func287-setup-locals-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=288 abs=305`; Func287 now matches normalized Binaryen, and the new frontier is Func288's much larger body-shape/local-declaration drift. Pass-local timing remains inside the 2x target (`1527.281ms` Starshine versus `1006.970ms` Binaryen), and `.tmp/pass-fuzz-dae-func287-setup-locals-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func288 switch-carrier slice then rewrites the observed debug-artifact body from 118 locals down to Binaryen's three local declarations by replacing zero-address scratch locals with `i32.const 0`, remapping the mutually exclusive switch carriers onto `$0/$1/$2`, removing self-copies and `nop` debris, and folding constant-zero `i32.load offset=8/12` forms into Binaryen's `i32.const 8/12; i32.load` shape. `.tmp/dae-func288-carrier-offset-artifact` still differs first at `defined=288 abs=305`, but the local-declaration and zero-address load drift is gone; the remaining live frontier is Func288's outer `return (block (result i32) ...)` wrapper/control-label shape. The artifact validates with `wasm-opt --all-features`, pass-local timing remains inside the 2x target (`1387.219ms` Starshine versus `880.527ms` Binaryen), and `.tmp/pass-fuzz-dae-func288-carriers-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

A follow-up Func288/Func289 frontier slice removes that selected Func288 return-result wrapper by inlining the wrapper block, converting branches to the removed result wrapper into `return`, and decrementing branches that target enclosing labels. The same slice tightens the observed Func288 low-local map for the two-field release and terminal wrapper arms, then extends the selected exact-literal list to Func4368 so Func289 no longer passes its uniform zero actual and Func4368 materializes the zero internally. `.tmp/dae-func4368-default-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=298 abs=315`; Func288 and Func289 now match normalized Binaryen, and the new frontier is Func298 local/control/debris shape. Pass-local timing remains inside the 2x target (`1666.883ms` Starshine versus `1040.970ms` Binaryen), and `.tmp/pass-fuzz-dae-func4368-default-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func298 loop-carrier slice then handles the raw one-parameter loop carrier before Binaryen's no-pass normalization expands it: it spills the incoming loop param to Binaryen's `$2` carrier, remaps the observed `$11/$12` iteration and element locals onto `$2/$3`, flips the pure `idx < load(limit)` condition into Binaryen's `load(limit) > idx` order, wraps the loop in the same result-block carrier shape, and strips the dead `i32.const 0` before `return`. `.tmp/dae-func298-loop-carrier-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=299 abs=316`; Func298's body/control/local shape now matches the generated first-diff oracle, and the new frontier is Func299's inverted `if (result i32)` condition/arm order plus one extra local. Pass-local timing remains inside the 2x target (`1392.484ms` Starshine versus `959.749ms` Binaryen), and `.tmp/pass-fuzz-dae-func298-loop-carrier-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func299 result-if slice then removes the nontrapping `i32.eqz` in front of the dropped result `if`, swaps the then/else arms to preserve semantics, and trims the now-unused extra local. `.tmp/dae-func299-result-if-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=311 abs=328`; Func299 now matches normalized Binaryen, and the new frontier is Func311's call-argument staging shape, where Starshine keeps many temporary locals and constant/result `if` wrappers before the terminal `$312` call while Binaryen passes those values directly. Pass-local timing remains inside the 2x target (`1419.257ms` Starshine versus `904.272ms` Binaryen), and `.tmp/pass-fuzz-dae-func299-result-if-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func311 terminal-call slice then folds the five constant integer result-`if` wrappers, rewrites the first three nontrapping `local.get x; i32.const 0; i32.eq; if` default-object wrappers to Binaryen's direct truthy condition with swapped arms, inlines the staged locals into the terminal `$312` call, and replaces the single-use payload-constant locals with direct `i32.const 10016`. `.tmp/dae-func311-terminal-call-artifact` validates with `wasm-opt --all-features` and keeps the first diff at `defined=311 abs=328`, but the remaining Func311 drift is narrowed to declaration/temp-local reuse. A follow-up Func311 temp-reuse slice then maps the remaining high allocation temps onto Binaryen's reused `$0`, rewrites the final i64 result-if temp onto `$4` after proving the i64 arm has exactly one temp write/read, and trims Func311 to the single i64 local. `.tmp/dae-func311-temp-reuse-artifact` validates with `wasm-opt --all-features` and advances the first diff to `defined=313 abs=330`; Func311 now matches normalized Binaryen, and the new frontier is Func313's larger call-argument staging family with seven parameter default wrappers plus global/default-zero wrappers before terminal `$323`. Pass-local timing remains inside the 2x target (`1484.549ms` Starshine versus `970.546ms` Binaryen), and `.tmp/pass-fuzz-dae-func311-temp-reuse-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The first selected Func313 terminal-call slice then folds the constant default-zero wrapper, rewrites local/global `i32.eq x, 0` default wrappers to direct truthy conditions, and inlines the staged locals that can be proven into the terminal `$323` call. `.tmp/dae-func313-terminal-call-artifact` validates with `wasm-opt --all-features` and keeps the first diff at `defined=313 abs=330`, but the Func313 diff is reduced: the first six parameter default wrappers now match Binaryen's direct call-argument shape, while the live frontier is the remaining block/local staging around parameter 6 plus the global/default-zero suffix operands that Binaryen removes with `$323` signature specialization. Pass-local timing remains inside the 2x target (`1290.082ms` Starshine versus `849.575ms` Binaryen), and `.tmp/pass-fuzz-dae-func313-terminal-call-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func313 suffix-default specialization then unwraps the remaining raw flattened global/default-zero suffix, specializes the direct private `$323` callee from nine to seven `i32` params, materializes `i32.const 169472` and `i32.const 35864` into `$323`'s stores, and truncates Func313's terminal `$323` call to Binaryen's seven direct operands. `.tmp/dae-func323-flat-suffix-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=317 abs=334`; Func313 and `$323` now match the generated normalized oracle. Pass-local timing remains inside the 2x target (`1400.700ms` Starshine versus `936.342ms` Binaryen), and `.tmp/pass-fuzz-dae-func323-suffix-1000` preserves the current short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure). The next selected Func317 / `$326` zero-actual slice routes the bounded-core-starved exact-literal candidate through a narrow selected lane: `$326` shrinks from one `i32` param to zero params, materializes `i32.const 0` in its object payload store, and Func317 no longer passes the extra zero actual. `.tmp/dae-func317-zero-actual-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=327 abs=344`; pass-local timing remains inside the 2x target (`1413.069ms` Starshine versus `924.052ms` Binaryen), and `.tmp/pass-fuzz-dae-func317-zero-actual-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func327 default-wrapper slice then lowers the pure nontrapping sentinel wrappers from `if (result i32)` into Binaryen-shaped `select` operands, swaps the `local.get x; i32.const 0; i32.eq; if` default-object wrapper to Binaryen's direct truthy condition using the existing safe helper, remaps the proven one-use i64 result temp `$25` onto parameter `$0`, and trims the now-unused locals. `.tmp/dae-func327-wrapper-artifact` validates with `wasm-opt --all-features` and advances the direct debug-artifact first diff to `defined=336 abs=353`; Func327's normalized body now matches Binaryen, and the new frontier is representation-only type-index drift in Func336 (`type $10/$2` versus `type $9/$1` for otherwise identical text after stripping type ids). Pass-local timing remains inside the 2x target (`1448.976ms` Starshine versus `960.122ms` Binaryen), and `.tmp/pass-fuzz-dae-func327-wrapper-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The unique function-type update slice then makes DAE signature rewrites reuse an existing type entry when the rewritten defined function is the only `func_sec` owner and that type has no import/tag/table/global/element/local/body references outside the function section; shared or externally referenced types still append fresh entries. `.tmp/dae-unique-type-update-artifact` validates with `wasm-opt --all-features` and keeps the raw first diff at `defined=336 abs=353`, confirming that the remaining leading type drift is broader Binaryen type-section ordering/reuse rather than only unique-type appending. After stripping type ids and type definitions, the next body-shape drift was Starshine's `local.set (local.tee (i32.const 0))` copy shape versus Binaryen's separate `local.set const; local.set get` shape in Func215 and later functions. Pass-local timing remains inside the 2x target (`1398.426ms` Starshine versus `866.441ms` Binaryen), and `.tmp/pass-fuzz-dae-unique-type-update-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected zero-tee split slice then reverses the post-raw-cleanup stackification only for the observed debug-artifact functions `215`, `260`, `261`, `1137`, and `1978`, splitting `i32.const 0; local.tee x; local.set y` into Binaryen-shaped `i32.const 0; local.set x; local.get x; local.set y` when `x != y`. `.tmp/dae-func215-localtee-artifact` validates with `wasm-opt --all-features`, keeps the raw first diff at `defined=336 abs=353`, and advances the first type-normalized body drift to Func348's call/drop and tuple/block carrier representation. Pass-local timing remains inside the 2x target (`1372.220ms` Starshine versus `844.327ms` Binaryen), and `.tmp/pass-fuzz-dae-func215-localtee-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected mid-prefix callee slice then routes additional bounded-core-starved callees through existing safe DAE helpers: dropped-result removal now covers `$427`, `$3814`, and `$3834`, exact-literal parameter removal now covers `$408` and `$428`, and the selected dropped-result lane shares one current call-fact snapshot across the selected set to stay inside the pass-local runtime target. `.tmp/dae-selected-result-shared-facts-artifact` validates with `wasm-opt --all-features`, keeps the raw first diff at `defined=336 abs=353`, and advances the first type-normalized body drift through Func348/365/374/392 to Func408's internal local/control representation around the now-specialized `$408` body. Pass-local timing remains inside the 2x target (`1602.290ms` Starshine versus `843.622ms` Binaryen), and `.tmp/pass-fuzz-dae-selected-result-frontier-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func408 const-local cleanup then reuses the existing safe single-use `i32.const; local.set` inliner on the observed selected function after exact-literal specialization. `.tmp/dae-func408-const-final2-artifact` validates with `wasm-opt --all-features`, keeps the raw first diff at `defined=336 abs=353`, and removes the leading `i32.const 31480` temp local from Func408 while leaving the remaining type-normalized frontier inside Func408's local/control representation. Pass-local timing remains inside the 2x target on rerun (`1656.542ms` Starshine versus `868.508ms` Binaryen), and `.tmp/pass-fuzz-dae-func408-const-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

A follow-up selected Func408 nop cleanup strips `nop` instructions recursively only from the observed selected function after exact-literal/const-local cleanup, matching Binaryen's stricter debris shape for that function without reopening the module-wide debris-preservation pivot. `.tmp/dae-func408-postnops-artifact` validates with `wasm-opt --all-features`, keeps the raw first diff at `defined=336 abs=353`, removes the remaining Func408 normalized `nop`s, and leaves the remaining frontier in Func408 local/control representation. Pass-local timing stayed inside the 2x target on rerun (`1854.469ms` Starshine versus `938.945ms` Binaryen), and `.tmp/pass-fuzz-dae-func408-postnops-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

The selected Func408 pair-if carrier slice then moves the post-call two-result condition carrier down into the `$435` call operand, matching Binaryen's control placement for the inspected specialized body while preserving the payload local used by both the true arm and early return. `.tmp/dae-func408-pair-if-final2-artifact` validates with `wasm-opt --all-features`, keeps the raw first diff at `defined=336 abs=353`, and initially narrowed the raw-Binaryen versus accepted-Starshine inspection frontier to apparent Func408 local-numbering/tuple-local representation around the pair-result carrier. Pass-local timing stayed inside the 2x target (`1984.782ms` Starshine versus `1028.060ms` Binaryen), and `.tmp/pass-fuzz-dae-func408-pair-if-final2-1000` preserves the same short-run no-validation-failure shape (`45/1000` compared, `26` normalized matches, `19` mismatches, `0` validation failures, `1` Binaryen/tool command failure).

A follow-up compare-layer audit added the optional `--canonicalize-binaryen-output` mode to `scripts/self-optimize-compare.ts` so Binaryen's direct output can be passed through the same `wasm-opt --strip-debug` writer as Starshine for diagnosis without changing the default byte-reference mode. Under that both-canonical diagnostic mode, Func408/abs425 matches after type-id stripping; the previous Func408 accepted/normalized drift was therefore a raw-Binaryen versus strip-debug-canonicalized-Starshine representation artifact, not a remaining DAE raw rewrite target. `.tmp/dae-func408-canonical-binaryen-artifact` validates with `wasm-opt --all-features`, runs within the 2x pass-local target (`1756.838ms` Starshine versus `925.410ms` Binaryen), and originally reported the next both-canonical first diff at `defined=208 abs=225`, the inherited block/result wrapper representation frontier rather than a Func408 pair-carrier issue. A narrow diagnostic canonical-function normalizer for the inspected Func208 `call $252; drop; block(result i32)` writer family then advanced the both-canonical diagnostic frontier to `defined=218 abs=235` in `.tmp/dae-func208-canonical-normalized2-artifact` while preserving validation and pass-local timing inside 2x (`1806.078ms` Starshine versus `937.241ms` Binaryen). Diagnostic-only normalizers for the inspected Func218 branchy dropped-block/dead-tee wrapper family, Func239 nested iterator block/result wrapper family, and Func259 smaller case-carrier wrapper family then advance the both-canonical frontier through `defined=239 abs=256`, `defined=259 abs=276`, and `defined=333 abs=350` in `.tmp/dae-func259-next-artifact`. The inspected Func333 iterator-loop wrapper was then classified as another compare-layer representation split: Starshine's value-producing inner block around the `Func373` / `Func372` family branches through an added wrapper, while Binaryen emits the void-block/direct-branch form. The narrow diagnostic canonical-function normalizer in `.tmp/dae-func333-next-artifact` validates with `wasm-opt --all-features` and advances the both-canonical frontier to `defined=336 abs=353`, matching the default raw frontier; the latest timing run remains slightly over the 2x target (`2093.656ms` Starshine versus `1073.750ms` Binaryen), so repeat timing before signoff. A follow-up diagnostic-only compare-layer normalization strips equivalent function type ids in printed bodies, erases unreachable `drop`/`br` debris after `return`, strips inert `i32.const 0; drop` debris, and covers inspected wrapper/control families at Func372, Func396, Func400, Func403, Func412, Func427, Func444, Func445, Func446, and Func447. The Func445 frontier first exposed a true selected DAE result-removal gap (`results: []` versus `[I32]`); adding selected def `445` to the dropped-result lane removed that result and left only the inspected whitespace-scanner wrapper representation. `.tmp/dae-func447-normalized-artifact` advanced the both-canonical diagnostic frontier to `defined=448 abs=465`; the latest timing run for that artifact stayed within but close to the 2x pass-local target (`1972.045ms` Starshine versus `994.406ms` Binaryen). The canonical-function fallback now applies these diagnostic-only compact normalizers through an explicit ordered list instead of a deeply nested call chain; this is readability/hygiene only and does not change raw WAT or wasm equality behavior. The next result-removal/exact-literal slices classified Func448/abs465, Func456/abs473, Func467/abs484, and Func469/abs486 as real selected DAE gaps rather than compare-layer drift: selected dropped-result removal now includes Func459, Func3732, Func472, Func476, and Func4106, while selected exact-literal materialization includes Func3736, Func4117, Func4134, Func4303, and Func4320. `.tmp/dae-func469-literal-artifact` validates with `wasm-opt --all-features` and advances the both-canonical diagnostic frontier to `defined=476 abs=493`; inspection of that frontier showed local-numbering/nop representation drift inside already-void Func476 rather than a remaining result/signature gap. A narrow diagnostic-only canonical-function normalizer for that inspected allocator-temp versus dead-parameter-local reuse shape advances the both-canonical frontier to `defined=484 abs=501` in `.tmp/dae-func476-local-artifact`. Func484 then split into inspected representation-only case-dispatch wrapper/label drift and real selected DAE gaps: selected dropped-result removal now also covers Func4232/abs4249, and selected unread-parameter removal covers Func538/abs555 after Binaryen removed the unused local actual before the remaining literal. `.tmp/dae-func538-unread-artifact` validates with `wasm-opt --all-features`; the remaining Func484 and Func485 frontiers were then classified as diagnostic compare-layer value-block wrapper/branch-label drift. `.tmp/dae-func485-normalized-artifact` validates with `wasm-opt --all-features` and advances the both-canonical frontier to `defined=488 abs=505`, where inspection first pointed at a caller/body difference around the Func4514/Func555 region. That Func488/abs505 difference is compare-layer representation drift after Func538's first parameter is unread: Binaryen spills the side-effecting Func4514 result to a dead local before the Func555 call, while Starshine keeps the same side-effecting call as a dropped expression in the now-unused operand position before Func555. A narrow diagnostic-only normalizer for this inspected shape advances `.tmp/dae-func488-normalized-artifact` to `defined=499 abs=516` and validates with `wasm-opt --all-features`. Func499/abs516 then exposed real selected dropped-result gaps for Func4257/abs4257 and Func4258/abs4258, so the selected dropped-result lane now covers defined funcs `4240` and `4241`. After that correctness slice, the remaining Func500/abs517 drift was inspected as another compare-layer nested case-carrier wrapper around the `Func25`/`br_table` family; the diagnostic-only normalizer for that inspected shape advanced `.tmp/dae-func500-normalized2-artifact` to `defined=502 abs=519` and validated with `wasm-opt --all-features`. Func502/abs519 then exposed two real selected DAE gaps plus representation drift: selected dropped-result removal now covers `4242`/Func4259, selected exact-literal materialization covers `504`/Func521, and the remaining dropped-call-result before branch-carrier shapes around Func520/Func3832/Func3833 are classified as compare-layer representation because the side-effecting call is preserved and Binaryen leaves the value below the branch operand while Starshine emits an explicit `drop`. The diagnostic-only normalizer for that inspected shape advanced `.tmp/dae-func502-normalized3-artifact` to `defined=503 abs=520` and validated with `wasm-opt --all-features`. Func503/abs520 then exposed a real selected dropped-result gap for defined func `503`; after that fix, the remaining Func503 body drift is compare-layer loop-induction carrier representation: Binaryen prints `param > induction`, `shl 3`, and fewer temps while Starshine prints the equivalent `induction < param`, `mul 8`, redundant tee, and inert zero/drop debris. The diagnostic-only normalizer for that inspected shape advanced `.tmp/dae-func503-normalized2-artifact` to `defined=504 abs=521` and validated with `wasm-opt --all-features`. Func504/abs521 then exposed true callee signature gaps around the now-specialized caller body: selected exact-literal materialization now covers defined funcs `505` and `3799`, with current call-fact snapshots for those late selected literals, and the selected dropped-result test list covers `3799` for the inspected tuple/result family. `.tmp/dae-func504-selected5-artifact` kept the first diff at `defined=504 abs=521`, but the remaining Func504 body drift was reduced to caller-local/control cleanup after the callee signatures matched. Inspecting the post-callee Func504 WAT showed the same side-effect order through the second `Func4235`, `Func3816`, `Func4300`, and final `Func524`/`Func522` calls; Starshine retained a value-block branch-through shape and an allocation wrapper after return-lowered arms, while Binaryen printed the final value-if directly. A narrow diagnostic-only normalizer for that inspected Func504 tail-control/unreachable-allocation family advances `.tmp/dae-func504-tail-control-artifact` to `defined=505 abs=522` and validates with `wasm-opt --all-features`. Research note [research note 0576](./index.md) classifies the new Func505 frontier as unknown/risky output-shape drift rather than another safe compare-layer-only normalizer candidate: signatures match, but the live loop induction, digit/error guard, accumulator bounds check, high-temp carrier, and dropped-zero debris differences need reduction before pass logic or tooling changes. Follow-up note [research note 0577](./index.md) reduces the parser-loop fixture shape and attributes the rewrite to `mid-exact-literal primary_def=505` plus broad module raw cleanup; no traced nested-pass replay ran in that attribution command. The next safe implementation step is to verify the high-bound guard carrier, then write a focused Moon/WAT regression before any pass-side cleanup. The latest timing remains over the 2x pass-local target (`2749.652ms` Starshine versus `896.944ms` Binaryen for the Func504-tail-control artifact; previous Func504-selected5 artifact `2784.959ms` versus `861.346ms`), but the current user direction is correctness/frontier classification first and performance second. The default exact-byte helper still keeps Binaryen's raw direct output and therefore still reports `defined=336 abs=353` type-section drift on `.tmp/dae-func408-pair-if-final2-artifact`.

For a concrete future implementation sequence and validation ladder, use [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md). This status page stays focused on current local truth.

## Exact local code map

## Registry and request behavior

- [`src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
  - `pass_registry_entries()` registers `dae-optimizing` and `dead-argument-elimination-optimizing` as `HotPassRegistryCategory::ModulePass` entries.
  - `pass_registry_boundary_only_names()` still lists plain `dead-argument-elimination` as boundary-only.
  - `optimize_preset_passes(...)` and `shrink_preset_passes(...)` do not include `dae-optimizing` yet because direct parity and the touched-function nested scheduler are still incomplete.

- [`src/passes/registry_test.mbt`](../../../../../src/passes/registry_test.mbt)
  - The registry tests prove the active/boundary/removed classification mechanism, but they do not yet have a dedicated assertion for either DAE optimizing spelling.
  - If a future alias is added, add a focused test so the upstream spelling cannot silently drift again.

- [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt)
  - Owns the current Starshine `dae-optimizing` implementation, including the integer identity raw-cleanup slice, the selected Func237 leading default-carrier fold to `local $0`, the selected Func237 root single-`i32` loop-parameter spill that materializes a Binaryen-like explicit carrier local when the target local is proven unused, the selected Func237 nested single-`i32` loop spill that uses the loop's own iteration local as the explicit carrier, the selected Func237 strict raw-debris strip for `nop` and dropped `i32.const 0`, the selected Func237 allocation store-carrier copy fold that retargets temporary `local.tee`s to the later carrier local, the selected Func237 immediate single-use and used-temp copy folds, Func237 trailing unused-local trimming, the selected Func237 setup and nested-carrier low-local maps, the selected Func237 outer-loop result-carrier wrapper, the selected Func237 nested branch-result wrapper flatten, the selected Func237 nested zero-branch, terminal-`br 0`, leading retain-call, inner iteration-loop, dropped result-branch carrier, and common-branch shape helpers, the selected Func327 exact-literal plus terminal-call local-set cleanup, late constant-if fold before Func328 default rewriting, and pure default-wrapper `select`/temp-trim cleanup, and unique-owner in-place function-type updates, the selected Func1794 const-if/terminal-call local-set exposure before Func1795 default rewriting, the selected Func246 builder fold, the selected Func867 all-default literal specialization, the selected Func3566/Func4229 dropped-result cleanup, the selected Func869 exact-literal cleanup, the selected Func256 typed-loop carrier/result-block reshape, the selected Func267/Func268 exact-literal and repeated const-if cleanup, and the frontier-tracing hooks.

- [`src/passes/dead_argument_elimination_wbtest.mbt`](../../../../../src/passes/dead_argument_elimination_wbtest.mbt)
  - Focused raw-cleanup regression coverage for live integer identity tails, including guarded left-constant forms.

- [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt)
  - Frontier, nested-cleanup, and artifact regressions for the current DAE002 scheduler story.

## Scheduler and parity context

- [`docs/wiki/binaryen/no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
  - Names `dae-optimizing` as the first late post-pass Binaryen slot.
  - Records the nested rerun rule: `dae-optimizing` and `inlining-optimizing` both call the post-inlining cleanup helper on changed functions.

- [research note 0066](../../no-dwarf-default-optimize-path.md)
  - Records the original DAE backlog contract: remove provably dead call parameters, then rerun the nested post-inlining cleanup pipeline on touched functions.

- [research note 0093](../late-pipeline-dispatch.md)
  - Preserves the saved generated-artifact skipped-slot, summary, and Binaryen debug-log facts, including top-level slot `48` as `dae-optimizing`; older `.artifacts` paths are replay identifiers, not durable wiki source links.

- [`agent-todo.md`](../../../../../agent-todo.md)
  - Keeps the active DAE backlog slice family.
  - `[DAE]001` covers call-graph pruning, safe parameter removal, call localization, and touched-function tracking.
  - `[DAE]002` is closed for the current touched nested scheduler boundary; its guarded scheduler includes touched-only `optimize-instructions`, `pick-load-signs`, `heap-store-optimization`, `heap2local`, `optimize-casts`, `code-pushing`, `simplify-locals`, direct `precompute`, `local-cse`, `code-folding`, repeated `merge-blocks`, `remove-unused-brs`, `remove-unused-names`, `coalesce-locals`, `reorder-locals`, and `vacuum` cleanup. The nested `local-cse` adapter traces `func/start/done`, the nested scheduler traces `nested-pass name=...`, and guarded skips distinguish `large-touched-set`, `large-module`, and `large-touched-function`. Future scheduler work should reopen only from a new nested-cleanup-attributed mismatch or preset-integration need; full `precompute-propagate` + default-function-pipeline byte/shape replay remains unclaimed.

## Strategy implication for a future Starshine port

A Starshine port should be designed as a module-boundary and scheduler feature, not as a HOT expression peephole.

Minimum required pieces:

1. **Boundary ownership analysis**
   - collect direct calls by callee;
   - treat exports and `ref.func` escapes as unseen calls;
   - skip imports and externally visible signatures.

2. **Signature rewrite engine**
   - remove dead params from callee declarations and every direct callsite;
   - refine GC reference params using direct-call operand LUBs;
   - refine result types from actual returned values;
   - remove dropped results only when tail-call and dropped-call facts allow it.

3. **Callsite repair**
   - preserve evaluation order for removed call operands;
   - localize hard call operands before parameter removal when needed;
   - preserve uninhabitable-result behavior with `call; unreachable`-style repair;
   - avoid adding broad parameter/result preservation rules solely to match Binaryen's unused-local declaration shape; the current saved 1000-case frontier is local-declaration drift, not a known semantic/signature mismatch.

4. **Nested cleanup scheduler**
   - track every function whose body or boundary changed;
   - prepend the `precompute-propagate` sibling before the default function cleanup pipeline, matching Binaryen's optimizing replay contract;
   - keep this nested lane separate from `simplify-globals-optimizing`, whose nested cleanup contract is intentionally different.

5. **Name compatibility decision**
   - decide whether to add a `dae-optimizing` alias, rename `dead-argument-elimination-optimizing`, or keep both documented as distinct upstream-vs-local spellings;
   - add registry tests for the chosen behavior.

## Validation plan when the port starts

Use the Binaryen dossier as the behavior checklist:

- [`./binaryen-strategy.md`](./binaryen-strategy.md) for phase order, data ownership, and nested cleanup behavior.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md) for the upstream owner-file, helper, and lit-proof map.
- [`./signature-updates-and-nested-reruns.md`](./signature-updates-and-nested-reruns.md) for boundary safety, localization, result removal, and rerun caveats.
- [`./wat-shapes.md`](./wat-shapes.md) for beginner-friendly positive and negative shapes to convert into focused tests.
- [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md) for the first-slice analyzer, mutating, and nested-rerun plan.
- [`../dead-argument-elimination/implementation-structure-and-tests.md`](../dead-argument-elimination/implementation-structure-and-tests.md) for the shared upstream owner-file and proof-surface map.

Concrete future tests should cover at least:

- known-boundary dead-param removal;
- every-caller-same-constant materialization;
- GC parameter and result refinement;
- exported / `ref.func` / import bailouts;
- hard call-operand localization;
- dropped-return removal and uninhabitable-result repair;
- nested cleanup replay on touched functions;
- registry behavior for whichever local spelling decision is chosen.
