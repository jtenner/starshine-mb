---
kind: concept
status: supported
last_reviewed: 2026-05-21
sources:
  - ../../../raw/research/0570-2026-05-18-dae002-func237-frontier-classification.md
  - ../../../raw/research/0567-2026-05-14-dae002-reverse-exact-literal-frontier-still-misses-4558.md
  - ../../../raw/research/0566-2026-05-14-dae002-func42-forwarding-wrapper-chain.md
  - ../../../raw/research/0565-2026-05-14-dae002-check-range-frontier-moved-to-func42.md
  - ../../../raw/research/0564-2026-05-14-dae002-check-range-rewrite-and-nested-skip.md
  - ../../../raw/research/0563-2026-05-14-dae002-later-candidate-starvation.md
  - ../../../raw/research/0562-2026-05-13-dae002-typeidx-block-carriers.md
  - ../../../raw/research/0560-2026-05-12-dae002-call-summary-runtime-attribution.md
  - ../../../raw/research/0559-2026-05-12-dae002-nested-cleanup-scheduler-slice.md
  - ../../../raw/research/0558-2026-05-12-dae-local-declaration-frontier.md
  - ../../../raw/research/0557-2026-05-12-dae-case-000690-escaped-self-operand.md
  - ../../../raw/binaryen/2026-05-05-dae-optimizing-current-main-recheck.md
  - ../../../raw/research/0487-2026-05-05-dae-optimizing-current-main-recheck.md
  - ../../../raw/binaryen/2026-04-25-dae-optimizing-current-main-and-test-map.md
  - ../../../raw/research/0366-2026-04-25-dae-optimizing-current-main-and-test-map.md
  - ../../../raw/binaryen/2026-04-24-dae-optimizing-primary-sources.md
  - ../../../raw/research/0285-2026-04-24-dae-optimizing-primary-sources-and-starshine-followup.md
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/dead_argument_elimination.mbt
  - ../../../../../src/passes/dead_argument_elimination_wbtest.mbt
  - ../../../../../src/passes/dae_optimizing_test.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/cmd/cmd.mbt
  - ../../../../../src/lib/types.mbt
  - ../../../../../src/validate/typecheck.mbt
  - ../../../../../src/validate/validate.mbt
  - ../../../../../src/wast/
  - ../../../../../agent-todo.md
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./signature-updates-and-nested-reruns.md
  - ./wat-shapes.md
  - ./starshine-port-readiness-and-validation.md
  - ../dead-argument-elimination/starshine-strategy.md
  - ../precompute-propagate/index.md
---

# Starshine strategy for `dae-optimizing`

## Current status

Starshine now has a **partial active module-pass implementation** for Binaryen's upstream `dae-optimizing` pass, with the current raw-cleanup frontier considered usefulness-signable for validity, artifact shrink, and pass-local runtime rather than exact Binaryen byte/shape parity.

The local naming caveat was resolved in the first implementation slice:

- upstream Binaryen exposes the public pass name `dae-optimizing`;
- Starshine now registers the exact canonical spelling `dae-optimizing` as an active module pass;
- Starshine also keeps the descriptive compatibility spelling `dead-argument-elimination-optimizing` as an active alias for the same optimizing module pass;
- plain `dead-argument-elimination` remains boundary-only and does not run the optimizing nested-cleanup trace.

Current implemented behavior is intentionally narrower than full Binaryen DAE:

- private direct-call dead scalar parameter removal;
- adjacent self-recursive `local.get` forwarding is ignored when proving params dead;
- removed actual side-effect preservation with `drop` repair;
- export and `ref.func` / element escape bailouts;
- value-producing `if` operands are preserved with `drop` repair when their parameter is removed, after which the optimizing cleanup lane may remove pure value debris while retaining side effects;
- no-param dropped/uncalled result removal with conservative unreachable-prefix cleanup;
- local-use scanning ignores dead suffixes after a root `unreachable`;
- case-000690-style escaped-result self-call operand preservation: if the original single `f64` parameter is stranded under an escaped direct-call result that becomes an undropped dead-suffix self-call operand, Starshine preserves that parameter while still pruning direct simple self-call operands and dropped self-call escaped-result operands, matching the observed Binaryen shape in [`../../../raw/research/0557-2026-05-12-dae-case-000690-escaped-self-operand.md`](../../../raw/research/0557-2026-05-12-dae-case-000690-escaped-self-operand.md);
- unused simple function type pruning after signature changes;
- a narrow exact-literal constant-actual materialization slice for private direct callees: if every direct caller passes the same literal and the callee never writes that parameter, Starshine removes the incoming param, rewrites callee `local.get`s to the literal, and relies on the touched-only nested cleanup lane for the remaining fold; as of 2026-05-13 this slice also handles sibling scalar memory-load carriers and typed single-result `TypeIdxBlockType` wrappers while staying on the same exact-literal/read-only guardrails;
- a small-module touched-function cleanup scheduler that records the required `precompute-propagate` prefix and now starts with a touched-only private `precompute-propagate-prefix` helper before the narrow local cleanup subset (`dead-code-elimination -> optimize-instructions -> local-cse -> pick-load-signs -> heap-store-optimization -> heap2local -> optimize-casts -> code-pushing -> simplify-locals -> code-folding -> precompute -> merge-blocks -> remove-unused-brs -> remove-unused-names -> merge-blocks -> coalesce-locals -> reorder-locals -> vacuum`) only on touched functions; that private prefix helper folds SSA-backed default-init and direct local constant facts and then reruns plain `precompute`, but it is not the public upstream sibling. `local-cse` now uses a function-filtered raw adapter, while `coalesce-locals` and `reorder-locals` use function-filtered adapters over the existing module passes in this nested lane, and all three remain public module passes for direct requests. The local-CSE adapter emits `pass[local-cse]:func/start/done` trace lines so DAE002 tests can pin its order before `simplify-locals`, and the scheduler itself emits `pass[dae-optimizing]:nested-pass name=...` lines so order tests can lock the guarded lane directly; size-skip traces distinguish `large-touched-set`, `large-module`, and `large-touched-function` while preserving the existing guard thresholds; `precompute-propagate-prefix` / `optimize-instructions` / `pick-load-signs` / `heap-store-optimization` / `heap2local` / `optimize-casts` / `code-pushing` / direct `precompute` / `code-folding` / `merge-blocks` / `remove-unused-brs` / `remove-unused-names` use the existing hot touched-function runner;
- a cheap large-module-safe post-touch raw cleanup runs after DAE rewrites and strips `nop` plus dropped pure debris across the module once DAE has touched at least one function, while preserving the original DAE touched set for the nested cleanup scheduler. It removes dropped pure leaves (`i32.const`, `i64.const`, `f32.const`, `f64.const`, `ref.null`, `local.get`, `global.get`, `memory.size`, and `table.size`), local/global self-assignment debris (`local.get $x; local.set $x`, `global.get $g; global.set $g`), redundant local self-tee writes (`local.get $x; local.tee $x`) while preserving the read result when it remains live, same-slot readback debris (`local/global.set $x; local/global.get $x; drop` or same-slot `set`) while preserving the original write and any producer effects, and dropped `local.tee` writes (`value; local.tee $x; drop`) by rewriting them to `value; local.set $x`. The same local-tee-to-set rewrite now removes a following dropped nontrapping pure unary/binary suffix, preserving the producer evaluation and local write while deleting only the discarded computed result. Dropped nontrapping pure unary/binary wrappers are also peeled from possibly trapping/effectful values when the wrapper itself is nontrapping and any removed right operand is a pure leaf, e.g. `load; const; add; eqz; drop` becomes `load; drop`, while trapping wrappers such as integer division and effectful right operands remain intact. It also strips live integer constant identity tails (`value; const 0` / `1` / `-1`; op -> value`, plus stack-neutral `const 0` / `1` / `-1`; `value`; op -> value forms) for nontrapping `i32`/`i64` add/sub/or/xor/shift/rotate/mul/and operators where the constant is an actual identity, canonicalizes live zero comparisons (`x == 0` to `eqz`, `x != 0` to `eqz; i32.eqz`) on the same RHS and guarded stack-neutral LHS shapes, folds same-local/same-global integer and `ref.eq` self comparisons to `i32.const 0` or `i32.const 1` while preserving float self comparisons because NaN is not reflexive, and simplifies same-local/same-global integer self bitwise/subtraction tails (`x & x` / `x | x` to `x`, `x ^ x` / `x - x` to integer zero). It preserves left-zero subtraction, left-zero shifts, stack-consuming LHS comparison producers, different-slot comparisons, and other non-identity forms; the focused regressions for these slices live in [`../../../../../src/passes/dead_argument_elimination_wbtest.mbt`](../../../../../src/passes/dead_argument_elimination_wbtest.mbt). On DAE-touched functions it also rewrites pure-stack local copy pairs (`value; local.set $x; local.get $x` to `value; local.tee $x`, and `value; local.tee $x; local.set $x` to `value; local.set $x`) while leaving memory/call/effect-fed live copies for the existing nested cleanup passes, arbitrary straight-line dropped stacks made only from those leaves and nontrapping pure unary/binary numeric/ref operators, dropped `select` stacks when both candidate values and the condition are recognized pure/nontrapping, saturating float-to-int truncation and simple dropped constant-safe regular float-to-int truncation, plus constant integer div/rem operations only when the divisor/overflow facts prove they cannot trap, including signed `i32.div_s` and `i64.div_s` overflow guards; those constant-safe float-to-int and integer div/rem operations may also sit under otherwise nontrapping pure wrappers when their literal operands are adjacent and syntactically prove the potentially trapping operation safe. It also treats zero-parameter single-result `block`, `loop`, catch-free `try_table`, and value-producing `if` wrappers as droppable pure values only when every executed value path is itself a full straight-line pure/nontrapping stack expression from its first instruction. For dropped value `if`s, a recognized pure/nontrapping condition stack is stripped with the branches; otherwise the pure branches are removed and the condition is kept with `drop` so condition traps/effects still occur. `ValTypeBlockType` and resolved `TypeIdxBlockType` `[] -> [T]` wrappers are covered, while type-index wrappers with parameters or multiple/no results, caught `try_table` value wrappers, and trapping/control/effectful bodies remain preserved. Zero-parameter zero-result `block` and `loop` wrappers are removed only after recursive cleanup proves their body is empty; `VoidBlockType` and resolved `TypeIdxBlockType` `[] -> []` wrappers are covered, while parameter-consuming or trapping/effectful structures stay intact. Empty zero-parameter zero-result `if` wrappers are simplified after both branches recursively empty: pure/nontrapping conditions are stripped, while effectful or trapping condition producers are preserved by replacing the empty `if` with a `drop`. Zero-parameter zero-result `try_table` wrappers are removed only after recursive cleanup proves their body empty; catch clauses do not block removal because an empty body cannot throw into them, while typed parameter-consuming or trapping/effectful `try_table` forms stay intact. It also strips narrow SIMD debris (`v128.const`, splats from scalar operands, unary lane-local integer/float ops such as abs/neg/popcnt/all_true/bitmask/ceil/floor/trunc/nearest/sqrt, `v128.not`, `v128.any_true`, bitwise `v128.and/andnot/or/xor`, `i8x16.shuffle` / `i8x16.swizzle`, lane extract/replace operations, lane shifts, integer lane extension/extadd-pairwise ops, narrowing/extmul/dot integer ops, non-relaxed SIMD conversion/trunc-saturate/demote/promote ops, lane comparisons, and basic lane arithmetic such as add/sub/mul, saturating add/sub, min/max, avgr/q15mulr, and float div/pmin/pmax, plus `v128.bitselect`) only when every consumed operand stack is recognized pure/nontrapping; memory-backed scalar/SIMD operands remain preserved. Dropped `ref.null; ref.is_null`, pure-operand `ref.test`, pure-operand `ref.test_desc`, and pure-operand `ref.eq` debris is removed because those instructions are side-effect-free and nontrapping boolean/comparison tests; nullable `ref.cast` is stripped only when its consumed stack is syntactically rooted in `ref.null`, because a nullable cast of null cannot take the null-trapping path. `ref.as_non_null` and non-nullable `ref.cast` remain preserved except for the narrow syntactic `ref.i31` producer family, because `ref.i31` constructs a non-null i31 reference so the null assertion or cast to `i31` / `eq` / `any` cannot trap. Unknown or possibly trapping numeric/ref/SIMD operations, including non-constant-safe regular float-to-int truncation, remain preserved.

Current non-parity caveat: the call-graph pruning and touched-function tracking slice `[DAE]001` is complete as of 2026-05-12, and `[DAE]002` now has narrow touched-function scheduler regressions plus a narrow exact-literal constant-actual slice, but complete Binaryen result-removal scheduling and the real public `precompute-propagate` + default-function-pipeline nested cleanup are not implemented yet. A whole-module cleanup experiment rewrote unrelated functions and worsened direct parity; the active scheduler therefore remains size-guarded and skips broad artifact modules until a faster filtered batch runner exists. After the 2026-05-12 case-000690 fix, `.tmp/pass-fuzz-dae-690-final2-1000` removed `case-000690-gen-valid` from the failure set and reported `998/1000` compared, `985` normalized matches, `13` mismatches, and `2` Binaryen/tool command failures; a follow-up classification found that all 13 remaining `gen-valid` mismatches are local-declaration-only diffs with exactly one local-decl hunk apiece, and the two command failures are unchanged `binaryen-rec-group-zero` parser/tool failures. The first `[DAE]002` scheduler slice preserved that frontier in `.tmp/dae002-mincleanup-1000` (`998/1000`, `985` normalized matches, `13` mismatches, `2` command failures), the local-CSE adapter preserved it again in `.tmp/dae002-local-cse-1000`, the merge-blocks addition preserved it in `.tmp/dae002-merge-blocks-1000`, the remove-unused-brs addition preserved it in `.tmp/dae002-remove-unused-brs-1000`, the remove-unused-names addition preserved it in `.tmp/dae002-remove-unused-names-1000`, the second merge-blocks addition preserved it in `.tmp/dae002-second-merge-blocks-1000`, the optimize-instructions addition preserved it in `.tmp/dae002-optimize-instructions-1000`, the code-folding addition preserved it in `.tmp/dae002-code-folding-1000`, the direct-precompute addition preserved it in `.tmp/dae002-precompute-1000`, the pick-load-signs addition preserved it in `.tmp/dae002-pick-load-signs-1000`, the heap2local addition preserved it in `.tmp/dae002-heap2local-1000`, and the heap-store-optimization addition preserved it in `.tmp/dae002-hso-1000` with the same counts. The code-pushing addition preserved that frontier again in `.tmp/dae002-code-pushing-1000`, the optimize-casts addition preserved it in `.tmp/dae002-optimize-casts-1000`, the reorder-locals addition preserved it in `.tmp/dae002-reorder-locals-1000`, and the coalesce-locals addition preserved it in `.tmp/dae002-coalesce-locals-1000` (`998/1000`, `985` normalized matches, `13` mismatches, `2` command failures) while a post-coalesce-locals debug-artifact trace still skips nested cleanup at `touched=12`. The new private prefix helper is currently covered by focused Moon tests for pass order and touched-only default-init local folding, and the new constant-actual slice is covered by focused every-direct-caller-same-literal plus scalar-load-sibling and typed single-result block-carrier regressions; a follow-up compare at `.tmp/dae002-const-actual-200` still matched the earlier 200-case frontier exactly (`199/200`, `198` normalized matches, `1` mismatch, `1` unchanged Binaryen/tool command failure). A later DAE002 blocker hunt expanded the passing ref-local regression surface to `ref.as_non_null(local.get ...)`, loop-carried locals, block-wrapped `try_table`, dominated and undominated sibling-join locals, and lib-constructed `call_ref` families, and it also found two inherited false leads outside the scheduler logic itself: after `optimize-instructions` simplified a ref-local `try_table` body nested under an outer `block`, HOT lowering computed catch depths against the full active label stack and rewrote a valid `catch_all 0` into invalid `catch_all 1`; and on duplicate-signature `call_ref` fixtures, `HotModuleContext` dropped declared function type slots, so `ref.func` widened to abstract `funcref` during HOT lifting and later failed a touched `local.set` into `(ref null (type ...))`. Fixing both issues restored the direct optimize-instructions shape, made the active `dae-optimizing` call-ref regressions pass, and removed two apparent DAE/local-subtyping blockers without broadening the guarded touched-only lane. A follow-up combo fuzz compare for `--dae-optimizing --local-subtyping` at `.tmp/dae-local-subtyping-1000` reported `998/1000` compared, `985` normalized matches, `13` mismatches, and `2` unchanged `binaryen-rec-group-zero` command failures; its mismatch and command-failure case set exactly matches `.tmp/dae002-coalesce-locals-1000`, and the 13 mismatches remain local-declaration-only `gen-valid` diffs. That leaves no validated current touched-local-subtyping validity blocker in the exercised control-flow or `call_ref` families; the remaining open work is future local-subtyping parity/refinalization plus broader constant-actual/artifact/runtime parity, not a reproduced DAE scheduler bug. The follow-up runtime attribution in [`../../../raw/research/0560-2026-05-12-dae002-call-summary-runtime-attribution.md`](../../../raw/research/0560-2026-05-12-dae002-call-summary-runtime-attribution.md) removed the 30s+ pre-nested-marker timeout by replacing repeated DAE core call/dead-suffix scans with a single original call summary and suffix-target aggregation; artifact replay is feasible again but remains red at `defined=11 abs=28` and still misses the pass-local speed floor. The new caller-family attribution in [`../../../raw/research/0561-2026-05-13-dae002-check-range-load-shape-attribution.md`](../../../raw/research/0561-2026-05-13-dae002-check-range-load-shape-attribution.md) proved all `370` observed direct `moonbit.check_range` callers keep `lo` at literal `0`, fixed scalar-load sibling carriers for the exact-literal slice, and still left `.tmp/dae002-load-only-artifact` red at the same first diff. A second follow-up in [`../../../raw/research/0562-2026-05-13-dae002-typeidx-block-carriers.md`](../../../raw/research/0562-2026-05-13-dae002-typeidx-block-carriers.md) added typed single-result `TypeIdxBlockType` carrier support and a focused regression, but `.tmp/dae002-typeidx-block-artifact` still stayed red at `defined=11 abs=28`. A third follow-up in [`../../../raw/research/0563-2026-05-14-dae002-later-candidate-starvation.md`](../../../raw/research/0563-2026-05-14-dae002-later-candidate-starvation.md) then showed that the current fixed `8`-iteration DAE core can starve a later exact-literal candidate after `9` earlier productive rewrites: Binaryen still rewrites the later `moonbit.check_range`-shaped callee to 2 params, while Starshine leaves it at 3, and the new `pass[dae-optimizing]:core iter=... primary_def=...` trace locks the reduced frontier directly as `primary_def=0..7`. A fourth follow-up in [`../../../raw/research/0564-2026-05-14-dae002-check-range-rewrite-and-nested-skip.md`](../../../raw/research/0564-2026-05-14-dae002-check-range-rewrite-and-nested-skip.md) then fixed two real caller-rewrite blockers in the artifact path itself: ambient typed-loop entry-value slices and same-caller multi-call undercount. That made `dae_try_rewrite_candidate(...)` rewrite all `370` direct `moonbit.check_range` calls, moved the artifact frontier to `primary_def=11` first, and left Func 28 at the same 2-param signature as Binaryen. A fifth follow-up in [`../../../raw/research/0565-2026-05-14-dae002-check-range-frontier-moved-to-func42.md`](../../../raw/research/0565-2026-05-14-dae002-check-range-frontier-moved-to-func42.md) added a tiny touched-only rewritten-control simplifier for negated compare guards, which closed the old Func 28 body-form mismatch and moved the live artifact frontier forward to `defined=25 abs=42`. A sixth follow-up in [`../../../raw/research/0566-2026-05-14-dae002-func42-forwarding-wrapper-chain.md`](../../../raw/research/0566-2026-05-14-dae002-func42-forwarding-wrapper-chain.md) then showed Func 42 is blocked by a forwarding-wrapper chain `4558 -> 4559 -> 42`: Func 4558 is already directly exact-literal rewritable on the original artifact, Func 4559 becomes rewritable after 4558, and Func 42 becomes rewritable only after both higher-index wrappers fall. A seventh follow-up in [`../../../raw/research/0567-2026-05-14-dae002-reverse-exact-literal-frontier-still-misses-4558.md`](../../../raw/research/0567-2026-05-14-dae002-reverse-exact-literal-frontier-still-misses-4558.md) then showed that a cheap bounded reverse exact-literal lane still does not reach that chain soon enough on the real artifact: `4559` and `4558` only appear at reverse iterations `14` and `15`. An eighth follow-up in [`../../../raw/research/0568-2026-05-14-dae002-forwarded-const-low-prefix-revisit.md`](../../../raw/research/0568-2026-05-14-dae002-forwarded-const-low-prefix-revisit.md) added narrow forwarded-const analysis through wrapper-local `local.get` chains plus a low-prefix exact-literal revisit over the first `64` defined functions. That new slice moves the core frontier to `11, 25, 227, 233, ...`, so Func 42 is now reached as the second productive core rewrite on the original artifact, but the full artifact compare still differs first at `defined=25 abs=42`. A ninth follow-up in [`../../../raw/research/0569-2026-05-15-dae002-func42-shape-fix-and-low-callee-prefix.md`](../../../raw/research/0569-2026-05-15-dae002-func42-shape-fix-and-low-callee-prefix.md) then fixed the remaining direct Func 42 add-zero / const-order body drift and added a low-callee core revisit over high callees selected from the first `64` defined callers. That moves the full artifact compare forward again to `defined=64 abs=81`, but the retained slice still runs at `120946.581ms` pass-local versus Binaryen `939.053ms`, and a wider `128`-caller experiment was intentionally rejected after it pushed the frontier to `defined=128 abs=145` at roughly `211635.689ms`. So the next safe slice is now the remaining low-wrapper/high-callee family just beyond the current `64`-caller boundary, not the original Func 42 family shape.

A 2026-05-18 artifact push later moved the live frontier to `.tmp/dae-parity-selected236-locals-fast-artifact` with first diff `defined=237 abs=254`; a follow-up runtime slice kept the output byte-identical but rerouted the artifact-specific low-callee and low-wrapper loops through the narrower unread-param helper with shared current-call facts, bringing repeated artifact pass-local timings to `1204.050ms`, `1375.033ms`, `1238.117ms`, and `1449.124ms` versus Binaryen `901.666ms`, `933.922ms`, `924.685ms`, and `1038.630ms`. A later reverse-lane runtime slice kept the output byte-identical again while routing the same stable high-wrapper chain (`4593, 4592, 4591, 4589, 4588, 4587, 4586, 4584`) through selected exact-literal rewrites on large modules; repeated artifact pass-local timings after both runtime slices were `1210.633ms`, `1190.713ms`, and final post-format validation `1159.661ms` versus Binaryen `995.422ms`, `953.055ms`, and `972.156ms`. A subsequent runtime-only slice reused one current-call-fact snapshot across that stable reverse selected-def chain, preserving byte-identical output and the same Func237 frontier while improving repeated pass-local artifact timings to `954.237ms`, `1041.034ms`, and `1105.814ms` versus Binaryen `977.574ms`, `954.835ms`, and `946.393ms`. The next runtime slices reused the low-callee current-call-fact snapshot across the low-wrapper, boundary, selected dropped-result, mid exact-literal, immutable-global exact-literal, and high exact-literal lanes; this again preserved byte-identical output and the same Func237 frontier, with repeated artifact pass-local timings `954.009ms`, `953.685ms`, and `942.725ms` versus Binaryen `989.211ms`, `989.084ms`, and `1033.290ms`. Follow-up boundary-selector runtime slices replaced repeated single-wrapper boundary scans with a direct wrapper-to-callee lookup plus a reusable selected-def bitmap, then queued that fixed wrapper/callee family once before replaying selected unread-param rewrites; both preserved byte-identical output and the same Func237 frontier, with the queued version improving repeated artifact pass-local timings to `759.713ms`, `772.650ms`, and `813.049ms` versus Binaryen `828.849ms`, `816.225ms`, and `819.985ms`. A later selected-helper runtime slice reused the already-collected direct-caller lists to reject self-recursive selected unread-param and exact-literal candidates instead of rescanning each candidate body; it stayed byte-identical at SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002` and preserved the Func237 frontier, with same-session baseline/change pass-local timings `944.067ms` vs `846.089ms` and repeated changed timings `951.182ms`, `928.172ms`, and `856.597ms` versus Binaryen `1047.720ms`, `946.588ms`, and `912.286ms`. A 2026-05-19 runtime-only follow-up reused per-caller stamp tables across original dead-suffix target collectors instead of allocating fresh `defined`-sized bool arrays for each caller; it again stayed byte-identical at SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002`, preserved first diff `defined=237 abs=254`, and improved repeated artifact pass-local timings to `741.094ms`, `737.795ms`, and `734.585ms` versus Binaryen `851.457ms`, `838.917ms`, and `832.981ms` after same-session direct Starshine pass timers improved from `771.283ms`, `763.074ms`, `782.472ms` to `726.991ms`, `743.688ms`, `725.645ms`. A later same-day runtime slice reused the original function type-index snapshot for original parameter counts and the original call summary instead of rebuilding it twice; this kept raw Starshine output byte-identical at SHA `5ef1b8b238989372a482014be46c13a3af6d86262e721259d3c1eda4b6f6c93f`, kept the accepted output SHA `0bf690b0202d043b25792d78acdc82601fb0c15c01a821ffa86eec9e060ce002`, preserved first diff `defined=237 abs=254`, and repeated clean artifact pass-local timings were `712.533ms`, `724.751ms`, and `746.108ms` versus Binaryen `833.349ms`, `850.807ms`, and `838.413ms`. The selected singleton bitmap reuse slice later kept the post-raw-cleanup artifact byte-identical at raw SHA `f488390d18ce1a5d0da510e387b2031411672104f854f564a0d7b456314485e0`, accepted SHA `55638ffbddd7d9daccb8bfa43f9523942c430e6d6fc18d511dcaf9a1f0d51ab7`, and first diff `defined=16 abs=33`, while reusing one selected-def bitmap per exact-literal singleton lane family and replaying at `958.441ms` and `987.611ms` Starshine pass-local versus `1052.190ms` and `966.653ms` Binaryen. Its 10k direct compare at `.tmp/pass-fuzz-dae-selected-singleton-reuse-10000` preserved the current raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` mismatches, `0` validation failures, `25` Binaryen/tool command failures) with Starshine smaller in all mismatch artifacts. The follow-up selected scratch-bitmap reuse slice keeps the artifact byte-identical at the same raw/accepted SHAs and first diff while reusing one singleton scratch bitmap across the Func236, const-if, dropped-result, mid literal, immutable-global, and high-literal lanes; `.tmp/dae-selected-scratch-reuse-artifact` replayed at `906.423ms` Starshine pass-local versus `948.792ms` Binaryen, and `.tmp/pass-fuzz-dae-selected-scratch-reuse-10000` preserved the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier with Starshine smaller in all mismatch artifacts. The core empty-skip-bitmap slice then replaces the per-core-iteration all-false `defined`-sized skip bitmap with one empty skip bitmap because no current core-loop caller marks defs between scans; it keeps the artifact byte-identical at the same raw/accepted SHAs and first diff, replays at `924.352ms` Starshine pass-local versus `911.320ms` Binaryen, and `.tmp/pass-fuzz-dae-core-empty-skip-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The selected scratch sharing slice then reuses one singleton selected-def scratch across the reverse high-wrapper chain, boundary wrapper/callee queue, Func236 local-retarget, const-if, dropped-result, mid literal, immutable-global, and high-literal lanes; it keeps the artifact byte-identical at raw SHA `f488390d18ce1a5d0da510e387b2031411672104f854f564a0d7b456314485e0`, accepted SHA `55638ffbddd7d9daccb8bfa43f9523942c430e6d6fc18d511dcaf9a1f0d51ab7`, and first diff `defined=16 abs=33`, replays at `917.564ms` Starshine pass-local versus `906.751ms` Binaryen, and `.tmp/pass-fuzz-dae-selected-scratch-shared-10000` preserves the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier with command failures classified as `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`; Starshine is smaller in all `3897` mismatch artifacts with canonical delta sum `-5,078,412`. The lazy mutation-buffer slice then defers the module raw-cleanup function copy until the first body rewrite and defers selected const-if function/touched copies until that helper actually changes a selected function; it keeps the same raw/accepted SHAs and first diff, replays at `960.959ms` Starshine pass-local versus `1073.920ms` Binaryen, and `.tmp/pass-fuzz-dae-lazy-mutation-buffers-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The raw-cleanup touched-flag reuse slice then stops allocating a fresh function-count touched bitmap inside module raw cleanup, using the existing touched-def bitmap for live-copy gating and returned touched state; it keeps the artifact byte-identical at the same raw/accepted SHAs and first diff, replays at `889.525ms` Starshine pass-local versus `878.035ms` Binaryen, and `.tmp/pass-fuzz-dae-raw-cleanup-touch-reuse-10000` preserves the same 10k frontier and all-smaller mismatch size scan. The if-condition boolean cleanup slice then pivots back to useful raw cleanup at the live `defined=16 abs=33` frontier: `if (i32.eqz (i32.eqz x))` becomes `if (x)`, preserving evaluation while shrinking the debug artifact by `28` raw and canonical bytes (`3,508,220 -> 3,508,192` raw, `3,515,213 -> 3,515,185` accepted), validating with `wasm-opt --all-features`, and replaying at `937.401ms` Starshine pass-local versus `917.628ms` Binaryen; `.tmp/pass-fuzz-dae-if-condition-bool-cleanup-10000` preserves the same `9975/10000`, `6078` match, `3897` mismatch, `0` validation-failure frontier and all-smaller mismatch size scan. The local-copy chain cleanup then folds pure producer copy chains through `local.tee` while preserving all local writes, shrinking the debug artifact by another `154` raw and canonical bytes (`3,508,192 -> 3,508,038` raw, `3,515,185 -> 3,515,031` accepted), validating with `wasm-opt --all-features`, and replaying at `870.772ms` Starshine pass-local versus `912.214ms` Binaryen; `.tmp/pass-fuzz-dae-local-copy-chain-cleanup-10000` preserves the same 10k frontier and all-smaller mismatch size scan. [`../../../raw/research/0570-2026-05-18-dae002-func237-frontier-classification.md`](../../../raw/research/0570-2026-05-18-dae002-func237-frontier-classification.md) classifies this remaining first diff as a local/control carrier shape, not another boundary rewrite miss: Binaryen's printed Func237 starts with a side-effect/trap-preserving void `block`, then materializes the live default carrier as `i32.const 0` in a later `local.set` before wrapper-object stores; Starshine still lowers the same region as a leading value block assigned to a temp local, with the surviving live edge carrying the default-zero value through local/control scaffolding. The exact Binaryen mechanics are inferred from artifact output and prior nested-rerun docs rather than from a dedicated upstream helper citation, so keep this claim scoped to the observed artifact family. Broad selected Func237 nested cleanup, reduced nested cleanup without DCE/coalesce, forced existing nested cleanup, selected coalescing, and a narrow default-carrier prototype all failed to advance the first diff and either consumed or exceeded the already tight runtime headroom. Future work should therefore avoid another broad selected cleanup pass and should only try a much narrower raw/HOT structural fold if it can match the real lowered shape and preserve trap/effect order. Because exact Binaryen byte/shape parity is not the signoff goal, the subsequent usefulness-oriented raw cleanup intentionally accepts a different first-diff frontier: `.tmp/dae-touched-raw-cleanup-artifact3` first stripped touched no-op debris, removed `597` normalized printed `nop`s on the debug artifact, shrank Starshine raw wasm from `3,536,456` to `3,533,111` bytes and canonical wasm from `3,543,259` to `3,539,980` bytes, validated with `wasm-opt --all-features`, and kept the 1000-case direct fuzz smoke at the established representation-only frontier (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures in `.tmp/pass-fuzz-dae-touched-raw-cleanup-1000`). A follow-up post-touch module raw cleanup widens that cheap debris strip to all module bodies without widening the nested scheduler's touched set: `.tmp/dae-module-raw-pure-leaf-cleanup-artifact3` removes all `6,150` remaining normalized printed `nop`s on the debug artifact, reduces dropped pure leaves in normalized text (`i32.const` drops `3,115 -> 60`, `i64.const` drops `6 -> 0`, `local.get` drops `603 -> 425`, `global.get` drops `1 -> 0`), shrinks Starshine raw wasm from `3,533,111` to `3,517,075` bytes and canonical wasm from `3,539,980` to `3,523,993` bytes, validates with `wasm-opt --all-features`, keeps pass-local timings within target (`874.285ms`, `918.233ms`, `936.437ms` versus Binaryen `982.459ms`, `991.208ms`, `1152.360ms`), and preserves the established 1000-case direct fuzz smoke shape (`998/1000` compared, `985` normalized matches, `13` mismatches, `0` validation failures, `2` Binaryen/tool command failures in `.tmp/pass-fuzz-dae-module-raw-pure-leaf-cleanup-1000`). The 10k usefulness signoff at `.tmp/pass-fuzz-dae-useful-raw-cleanup-10000` then preserved the latest full active fuzz frontier exactly (`9975/10000` compared, `9622` normalized matches, `353` normalized mismatches, `0` validation failures, `25` Binaryen/tool command failures classified as `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`), so the current raw-cleanup DAE output is usefulness-signable even though exact Binaryen byte/shape parity remains intentionally out of scope. A follow-up root-unreachable suffix cleanup in `.tmp/pass-fuzz-dae-live-dead-suffix-repair3-10000` deliberately prioritizes useful shrinking over normalized shape parity: it ignores direct-call blockers that occur only after a root `unreachable`, repairs no-param dead-suffix callers when result removal would otherwise strand typed call results, and removes no-param/parameterized private results from unreachable-only self/side-call suffixes when the live prefix is unobservable. That run kept validity at `0` validation failures with the same `25` Binaryen/tool command failures, but increased normalized mismatches to `581` because Starshine now collapses more private dead-suffix bodies than Binaryen in many generated cases; among mismatch artifacts Starshine was smaller in `425`, equal in `65`, larger in `91`, with canonical mismatch-size sum `-142,851` bytes (mean `-245.9` bytes), so this is a utility/size improvement rather than a byte-parity improvement. A follow-up numeric raw-cleanup slice at `.tmp/pass-fuzz-dae-constsafe-raw-cleanup-10000` extends that useful-shrinking policy from pure leaves to nontrapping numeric expressions and constant-safe integer div/rem. It keeps `0` Starshine validation failures with the same `25` Binaryen/tool command failures, but intentionally increases normalized mismatches to `3,897` because Starshine now removes much more pure debris than Binaryen in generated dead-prefix/suffix bodies. Among mismatch artifacts Starshine is smaller in all `3,897`, with canonical mismatch-size sum `-4,815,090` bytes (mean `-1,235.6` bytes). The former worst Starshine-larger `case-006030-gen-valid` flips from `+830` bytes (`1735` Starshine vs `905` Binaryen) to `-702` bytes (`203` Starshine vs `905` Binaryen). A subsequent stack-shaped cleanup generalization at `.tmp/pass-fuzz-dae-stack-raw-cleanup-10000` keeps the same aggregate 10k counts and size evidence while covering arbitrary straight-line nontrapping pure expression stacks in focused tests. Dropped `select` cleanup then extends that same stack contract to the three consumed operands, removing only when both values and the condition are themselves recognized pure/nontrapping; `.tmp/pass-fuzz-dae-select-raw-cleanup-10000` reported `9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures. A later float-to-int cleanup treats saturating truncation as nontrapping pure unary debris and removes simple dropped regular float-to-int truncation only for constant operands whose truncated result is representable; `.tmp/pass-fuzz-dae-float-trunc-cleanup-10000` reported the same aggregate counts (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures). The first narrow ref cleanup then adds `ref.null` leaves plus `ref.is_null` unary cleanup while explicitly preserving `ref.as_non_null` on a null operand; `.tmp/pass-fuzz-dae-ref-null-cleanup-10000` again reported the same aggregate counts (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The follow-up `ref.eq` cleanup extends the same contract to dropped ref comparisons only when both operand stacks are recognized pure/nontrapping, and focused coverage preserves a `ref.as_non_null` operand stack; `.tmp/pass-fuzz-dae-ref-eq-cleanup-10000` reported the same aggregate counts and command-failure classes. The next narrow ref cleanup adds dropped `ref.i31`, `any.convert_extern`, and `extern.convert_any` stacks under the same recognized pure/nontrapping unary contract, while still preserving `i31.get_*` on nullable `i31ref` and `ref.as_non_null`-fed conversion stacks because those operand stacks may trap; `.tmp/pass-fuzz-dae-ref-convert-i31-cleanup-10000` reported the same aggregate counts and command-failure classes. A follow-up recognizes `i31.get_s` / `i31.get_u` as removable only when the consumed operand stack is proven to produce the reference via `ref.i31`, making the `i31.get_*` nontrapping by construction while still preserving nullable `i31ref` inputs; `.tmp/pass-fuzz-dae-i31-get-cleanup-10000` reported the same aggregate counts and command-failure classes. The next tiny numeric cleanup fills the corresponding constant-safe signed-division hole for dropped `i64.div_s`: it removes only constant operands where the divisor is nonzero and the signed-minimum divided by `-1` overflow trap is impossible, with focused coverage preserving that overflow case. A follow-up SIMD-core cleanup removes only `v128.const`, nontrapping unary `v128.not` / `v128.any_true`, bitwise binary `v128.and` / `v128.andnot` / `v128.or` / `v128.xor`, and `v128.bitselect` stacks when all operands are recognized pure/nontrapping; focused coverage preserves a `v128.load`-fed `v128.bitselect` because the memory load may trap. The next SIMD-unary follow-up widens that same contract to lane-local nontrapping unary integer/float ops and integer all_true/bitmask reductions, again preserving a `v128.load`-fed unary stack. The SIMD-binary follow-up applies the same all-operands-recognized contract to audited two-`v128` lane comparisons and basic nontrapping lane arithmetic, while still excluding mixed-arity shifts, lane extract/replace, narrowing/conversion, extmul/dot, relaxed SIMD, and memory operations; focused coverage strips `i8x16.add`, `i32x4.eq`, and `f32x4.div` constant stacks and preserves a `v128.load`-fed `i8x16.add`. The 10k compare at `.tmp/pass-fuzz-dae-simd-binary-cleanup-10000` preserved the established raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`). The next SIMD-splat cleanup treats the six lane splats as nontrapping pure unary debris over scalar operands, with focused coverage removing constant-fed `i8x16.splat` and `f64x2.splat` while preserving an `i32.load`-fed `i8x16.splat` because the scalar load may trap; `.tmp/pass-fuzz-dae-simd-splat-cleanup-10000` again preserved the same 10k aggregate counts and command-failure classes. The SIMD shuffle/swizzle follow-up adds `i8x16.shuffle` and `i8x16.swizzle` to the same audited two-`v128` nontrapping pure binary contract; focused coverage strips constant-fed shuffle/swizzle drops and preserves load-fed shuffle/swizzle stacks because the memory load may trap. `.tmp/pass-fuzz-dae-simd-shuffle-swizzle-cleanup-10000` preserved the established raw-cleanup frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`). The SIMD lane cleanup then treats immediate lane extracts as nontrapping pure unary debris and immediate lane replacements as mixed scalar/vector but still two-operand nontrapping pure binary debris when every consumed operand stack is recognized pure/nontrapping; focused coverage strips constant-fed `i32x4.extract_lane`, `f64x2.extract_lane`, `i8x16.replace_lane`, and `f64x2.replace_lane`, while preserving `v128.load`-fed extracts, scalar-load-fed replacements, and `v128.load`-fed replacements. `.tmp/pass-fuzz-dae-simd-lane-cleanup-10000` again preserved the same aggregate frontier and command-failure classes. The SIMD shift follow-up applies the same mixed vector/scalar binary contract to integer lane shifts (`i8x16`, `i16x8`, `i32x4`, and `i64x2` shl/shr_s/shr_u), stripping constant-fed `i8x16.shl` and `i32x4.shr_u` stacks while preserving v128-load-fed and scalar-load-fed shifts; `.tmp/pass-fuzz-dae-simd-shift-cleanup-10000` again preserved the established 10k frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures with the same classes). The SIMD extension follow-up treats integer lane `extend_low/high` and `extadd_pairwise` ops as nontrapping pure unary debris; focused coverage strips constant-fed `i16x8.extend_low_i8x16s` and `i32x4.extadd_pairwise_i16x8u` stacks while preserving `v128.load`-fed extension/extadd stacks. `.tmp/pass-fuzz-dae-simd-extend-cleanup-10000` again preserved the same 10k frontier and command-failure classes. The SIMD narrow/extmul/dot follow-up applies the audited two-`v128` binary cleanup contract to integer lane narrowing, extmul low/high, and `i32x4.dot_i16x8s`: typechecking confirms each consumes `v128 v128 -> v128`, and the instructions have no memory/control effects or trap condition. Focused coverage strips constant-fed `i8x16.narrow_i16x8s`, `i16x8.narrow_i32x4u`, representative extmul ops, and `i32x4.dot_i16x8s` stacks while preserving load-fed narrow/extmul/dot stacks. `.tmp/pass-fuzz-dae-simd-narrow-extmul-dot-cleanup-10000` preserved the same 10k frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures with the established classes). The SIMD conversion follow-up then covers only non-relaxed unary conversion/trunc-saturate/demote/promote operators (`f32x4.demote_f64x2_zero`, `f64x2.promote_low_f32x4`, `i32x4.trunc_sat_f32x4{s,u}`, `i32x4.trunc_sat_f64x2{s,u}_zero`, `f32x4.convert_i32x4{s,u}`, and `f64x2.convert_low_i32x4{s,u}`): typechecking confirms each is a single-`v128` value operator, the saturating variants do not trap on out-of-range/NaN inputs, and none has memory/control effects. Focused coverage strips representative constant-fed conversion stacks while preserving `v128.load`-fed conversion stacks because the input load may trap. `.tmp/pass-fuzz-dae-simd-conversion-cleanup-10000` preserved the same aggregate frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The next ref-test cleanup adds only `ref.test` to the pure unary debris set: typechecking confirms it consumes one reference and produces `i32`, its runtime behavior is a boolean test rather than a trapping cast, and focused coverage removes a `ref.null; ref.test; drop` stack while preserving a `ref.as_non_null; ref.test; drop` stack because the operand stack may still trap. `.tmp/pass-fuzz-dae-ref-test-cleanup-10000` preserved the same aggregate frontier (`9975/10000` compared, `6078` normalized matches, `3897` raw normalized mismatches, `0` Starshine validation failures, and `25` Binaryen/tool command failures: `22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`, `1` `binaryen-table-index-out-of-range`, and `1` `binaryen-invalid-tag-index`). The descriptor-test follow-up extends the same audited unary contract to `ref.test_desc`: validation resolves/checks the target reftype statically, typechecking confirms one reference operand to `i32`, and the runtime operation is the descriptor-form boolean test rather than a trapping cast; focused coverage removes `ref.null; ref.test_desc; drop` while preserving `ref.as_non_null; ref.test_desc; drop` because the operand stack may trap. `.tmp/pass-fuzz-dae-ref-test-desc-cleanup-10000` preserved the same aggregate frontier and command-failure classes. A later size-leaf cleanup adds dropped `memory.size` and `table.size`: typechecking validates only the static memory/table index and pushes `i32`, HOT flags mark both as value-only/nontrapping, and reading the current size has no side effect when the result is immediately dropped. Focused coverage removes both dropped size queries from a module with defined memory/table sections while existing load-fed memory/SIMD stacks remain preserved. The nullable-cast follow-up removes `ref.null`-rooted `ref.cast (ref null ...)` stacks, including through a following pure `ref.test`, while preserving `ref.cast (ref null ...); ref.as_non_null` because the assertion would still trap on null. The next i31-supertype cast follow-up broadens only the already fact-sensitive `ref.i31` proof: a syntactic `ref.i31` result is non-null and is a subtype of `i31`, `eq`, and `any`, so dropped casts to those targets are removed while unrelated ref-cast targets remain preserved. The const-safe chain follow-up then allows already-audited constant-safe float-to-int truncation and integer div/rem operations to feed nontrapping wrappers such as `eqz`, but only when the immediate literal operands prove no trap is possible. The local/global self-assignment follow-up removes `local.get $x; local.set $x` and `global.get $g; global.set $g`, strips redundant `local.tee $x` after `local.get $x`, and then lets the existing pure-stack cleanup remove now-dropped self-tee reads; different-local and different-global writes remain preserved because they change state. The later state readback follow-up removes same-slot `local/global.set; local/global.get; drop` and `local/global.set; local/global.get; local/global.set` tails while preserving the original write and producer effects. The dropped-tee sibling rewrites `value; local.tee $x; drop` to `value; local.set $x`, and does the same when the tee result feeds a following dropped nontrapping pure unary/binary suffix, preserving producer evaluation and the local write while removing only the discarded computed stack result. The integer identity sibling strips live `i32`/`i64` identity tails on both safe sides: right-constant add/sub/or/xor/shift/rotate-by-zero, multiply-by-one, and bitwise-and-with-all-ones tails (`value; identity const; op -> value`), plus left-constant add/or/xor-by-zero, multiply-by-one, and bitwise-and-with-all-ones tails only when the right producer is a stack-neutral value leaf. It also canonicalizes right-zero `i32`/`i64` equality to `eqz` and inequality to `eqz; i32.eqz`, plus the same left-zero compare forms only when the right producer is stack-neutral. For live `if` conditions specifically, it strips trailing `i32.eqz; i32.eqz` and branches directly on the original i32 producer, preserving producer evaluation/traps/effects because WebAssembly `if` already treats any nonzero i32 as true. Same-local/same-global integer and `ref.eq` self comparisons fold to constants (`eq`, `le`, `ge`, and `ref.eq` are true; `ne`, `lt`, and `gt` are false), while float self comparisons remain preserved because NaN is not reflexive. Same-local/same-global integer self binary tails also simplify `x & x` / `x | x` to one read and `x ^ x` / `x - x` to the typed integer zero. It preserves the live producer while removing only a nontrapping identity, zero-compare, reflexive self-comparison pair, or adjacent same-read integer self-binary pair; left-zero subtraction, left-zero shifts, stack-consuming right producers, stack-consuming left-compare producers, different-slot comparisons, float comparisons, and other non-identities remain preserved. Its live-copy part remains limited to DAE-touched functions and pure/nontrapping producer stacks: `value; local.set $x; local.get $x` becomes `value; local.tee $x`, `value; local.tee $x; local.set $x` becomes `value; local.set $x`, and short pure copy chains such as `value; local.set $a; local.get $a; local.set $b` / `...; local.get $b; local.set $c` become tee chains that evaluate the producer once while preserving each local write; memory/call/effect-fed live copies are preserved so touched-only nested pass evidence such as `pick-load-signs` is not preempted. The pure-control follow-up strips dropped zero-parameter single-result `block`, `loop`, catch-free `try_table`, and `if` wrappers only when every value-producing body path is a straight-line pure/nontrapping value stack. Dropped pure `if` branches are removed while either stripping a pure/nontrapping condition stack or preserving an unrecognized condition as `drop`; focused negatives preserve trapping `unreachable` bodies, caught value `try_table` wrappers, plus type-index wrappers that consume parameters. The type-index extension covers only resolved `[] -> [T]` `TypeIdxBlockType` wrappers, so deleting the wrapper cannot strand implicit control arguments. The void block/loop cleanup similarly removes only already-empty zero-parameter zero-result wrappers (`VoidBlockType` or resolved `[] -> []` type-index block types) after recursive pure-debris cleanup, preserving parameter-consuming and trapping/effectful structures. The empty-`if` cleanup uses the same zero-param/zero-result type guard and empty-branch proof, then either strips a recognized pure condition stack or emits `drop` to preserve an unrecognized condition's evaluation and traps/effects. The `try_table` sibling removes empty zero-param/zero-result wrappers even when they carry catches: after recursive cleanup empties the body there is no instruction left that can throw into a catch, while parameter-consuming and trapping/effectful bodies remain preserved. Possibly trapping numeric/ref/SIMD operations are still preserved unless local facts prove safety; when a dropped nontrapping pure wrapper around such an operation is removed, the trapping/effectful producer is retained and still dropped. This remains a conservative size/utility cleanup rather than exact Binaryen parity.

For a concrete future implementation sequence and validation ladder, use [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md). This status page stays focused on current local truth.

## Exact local code map

## Registry and request behavior

- [`src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
  - `pass_registry_entries()` registers `dae-optimizing` and `dead-argument-elimination-optimizing` as `HotPassRegistryCategory::ModulePass` entries.
  - `pass_registry_boundary_only_names()` still lists plain `dead-argument-elimination` as boundary-only.
  - `optimize_preset_passes(...)` and `shrink_preset_passes(...)` do not include `dae-optimizing` yet because direct parity and the touched-function nested scheduler are still incomplete.

- [`src/passes/registry_test.mbt`](../../../../../src/passes/registry_test.mbt)
  - The registry tests prove the active/boundary/removed classification mechanism, but they do not yet have a dedicated assertion for either DAE optimizing spelling.
  - If a future alias is added, add a focused test so the upstream spelling cannot silently drift again.

- [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt)
  - Owns the current Starshine `dae-optimizing` implementation, including the integer identity raw-cleanup slice and the frontier-tracing hooks.

- [`src/passes/dead_argument_elimination_wbtest.mbt`](../../../../../src/passes/dead_argument_elimination_wbtest.mbt)
  - Focused raw-cleanup regression coverage for live integer identity tails, including guarded left-constant forms.

- [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt)
  - Frontier, nested-cleanup, and artifact regressions for the current DAE002 scheduler story.

## Scheduler and parity context

- [`docs/wiki/binaryen/no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
  - Names `dae-optimizing` as the first late post-pass Binaryen slot.
  - Records the nested rerun rule: `dae-optimizing` and `inlining-optimizing` both call the post-inlining cleanup helper on changed functions.

- [`docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`](../../../raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md)
  - Records the original DAE backlog contract: remove provably dead call parameters, then rerun the nested post-inlining cleanup pipeline on touched functions.

- [`.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`](../../../../../.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json)
  - Records saved generated-artifact skipped slot `48` as `dae-optimizing`.

- [`agent-todo.md`](../../../../../agent-todo.md)
  - Keeps the active DAE backlog slice family.
  - `[DAE]001` covers call-graph pruning, safe parameter removal, call localization, and touched-function tracking.
  - `[DAE]002` covers nested `optimizeAfterInlining` replay plus artifact comparison; its narrow scheduler now includes touched-only `optimize-instructions`, `pick-load-signs`, `heap-store-optimization`, `heap2local`, `optimize-casts`, `code-pushing`, `simplify-locals`, direct `precompute`, `local-cse`, `code-folding`, repeated `merge-blocks`, `remove-unused-brs`, `remove-unused-names`, `coalesce-locals`, `reorder-locals`, and `vacuum` cleanup. The nested `local-cse` adapter traces `func/start/done`, the nested scheduler traces `nested-pass name=...`, and guarded skips now distinguish `large-touched-set`, `large-module`, and `large-touched-function`, but the full `precompute-propagate` + default-function-pipeline replay and artifact parity remain open.

## Strategy implication for a future Starshine port

A Starshine port should be designed as a module-boundary and scheduler feature, not as a HOT expression peephole.

Minimum required pieces:

1. **Boundary ownership analysis**
   - collect direct calls by callee;
   - treat exports and `ref.func` escapes as unseen calls;
   - skip imports and externally visible signatures.

2. **Signature rewrite engine**
   - remove dead params from callee declarations and every direct callsite;
   - refine GC reference params using direct-call operand LUBs;
   - refine result types from actual returned values;
   - remove dropped results only when tail-call and dropped-call facts allow it.

3. **Callsite repair**
   - preserve evaluation order for removed call operands;
   - localize hard call operands before parameter removal when needed;
   - preserve uninhabitable-result behavior with `call; unreachable`-style repair;
   - avoid adding broad parameter/result preservation rules solely to match Binaryen's unused-local declaration shape; the current saved 1000-case frontier is local-declaration drift, not a known semantic/signature mismatch.

4. **Nested cleanup scheduler**
   - track every function whose body or boundary changed;
   - prepend the `precompute-propagate` sibling before the default function cleanup pipeline, matching Binaryen's optimizing replay contract;
   - keep this nested lane separate from `simplify-globals-optimizing`, whose nested cleanup contract is intentionally different.

5. **Name compatibility decision**
   - decide whether to add a `dae-optimizing` alias, rename `dead-argument-elimination-optimizing`, or keep both documented as distinct upstream-vs-local spellings;
   - add registry tests for the chosen behavior.

## Validation plan when the port starts

Use the Binaryen dossier as the behavior checklist:

- [`./binaryen-strategy.md`](./binaryen-strategy.md) for phase order, data ownership, and nested cleanup behavior.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md) for the upstream owner-file, helper, and lit-proof map.
- [`./signature-updates-and-nested-reruns.md`](./signature-updates-and-nested-reruns.md) for boundary safety, localization, result removal, and rerun caveats.
- [`./wat-shapes.md`](./wat-shapes.md) for beginner-friendly positive and negative shapes to convert into focused tests.
- [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md) for the first-slice analyzer, mutating, and nested-rerun plan.
- [`../dead-argument-elimination/implementation-structure-and-tests.md`](../dead-argument-elimination/implementation-structure-and-tests.md) for the shared upstream owner-file and proof-surface map.

Concrete future tests should cover at least:

- known-boundary dead-param removal;
- every-caller-same-constant materialization;
- GC parameter and result refinement;
- exported / `ref.func` / import bailouts;
- hard call-operand localization;
- dropped-return removal and uninhabitable-result repair;
- nested cleanup replay on touched functions;
- registry behavior for whichever local spelling decision is chosen.
