---
kind: entity
status: supported
last_reviewed: 2026-07-18
sources:
  - ../../release-horizon-and-oracles.md
  - ../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md
  - ../../../../../src/passes/optimize_instructions.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/passes/optimize_instructions_test.mbt
  - ../../../../../src/passes/registry_test.mbt
  - ../../../../../src/cmd/cmd_wbtest.mbt
  - ../../../../../src/passes/optimize.mbt
  - ../tracker.md
  - ../../no-dwarf-default-optimize-path.md
  - ../late-pipeline-dispatch.md
related:
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./gc-casts-call_ref-and-trap-sensitive-rewrites.md
  - ./wat-shapes.md
  - ./starshine-strategy.md
  - ./starshine-hot-ir-strategy.md
  - ../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md
  - ./parity-matrix.json
  - ./sweep-report.md
  - ../../../../../scripts/oi-parity-sweep.ts
  - ../tracker.md
  - ../../no-dwarf-default-optimize-path.md
  - ../precompute/index.md
  - ../heap-store-optimization/index.md
  - ../vacuum/index.md
---

# `optimize-instructions`

## Binaryen v131 status

The 2026-07-12 Starshine closeout remains the v130 baseline, but direct parity is **reopened** for Binaryen v131. Upstream changed equal-input `ref.eq`, identical-arm `select`, idempotent-call/deep-effect equality, directional movement checks, and non-concrete select-arm bailout behavior. `[V131-OI]001` in `agent-todo.md` owns red-first focused fixtures and a fresh explicit-v131 four-lane plus O4z closeout.

## Role

- `optimize-instructions` is an active implemented **hot pass** in Starshine.
- In upstream Binaryen `version_131`, `optimize-instructions` is a function-parallel post-walk peephole and canonicalization pass.
- The public summary in `pass.cpp` is only `optimizes instruction combinations`.

That summary is true, but it is far too small.

A better beginner summary is:

- Binaryen first canonicalizes many instruction spellings,
- then rewrites arithmetic, boolean, control, memory, `call_ref`, GC-cast, and tuple-adjacent shapes when helper analyses say the rewrite is safe,
- and finally repairs changed types and EH-pop structure before it finishes.

## Why this pass matters

- The canonical no-DWARF `-O` / `-Os` scheduler uses it **twice** in the default function pipeline:
  - once early
  - once late
- The saved generated-artifact `-O4z` audit also saw it at two real top-level Binaryen slots:
  - slot `16`
  - slot `44`
- The saved Binaryen debug log contains `36` `running pass: optimize-instructions` lines in total, so nested optimizing reruns make it much more common than the two visible top-level slots suggest.
- The pass sits directly beside other cleanup and simplification neighbors already tracked in the wiki:
  - `precompute`
  - `heap-store-optimization`
  - `vacuum`
  - `rse`

## Most important durable takeaways

- Binaryen `optimize-instructions` is **not** just constant folding.
- Binaryen `optimize-instructions` is **not** just integer arithmetic peepholes.
- The real `version_129` pass combines:
  1. local bit/sign-extension prescan
  2. canonicalization of compares and commutative shapes
  3. arithmetic, boolean, and ternary-shell cleanup
  4. memory and bulk-memory simplification
  5. `call_ref` target cleanup
  6. GC cast, null-trap, and constructor/default rewrites
  7. deferred `ReFinalize`, final cleanup, and EH-pop repair
- Current Starshine implements a real but narrower HOT subset centered on integer, boolean, control, and writeback-safety cleanup.
- The earlier generated-artifact failures in slots `16` and `44` are now retired.
  - The durable explanation is still that those failures were HOT-lowering / writeback issues, not a still-open pass-local corruption family.

## Beginner warning: what the name hides

The easy wrong mental model is:

- `optimize-instructions` is just `eqz`, compare-to-zero, and a few arithmetic identities

The safer mental model is:

- Binaryen uses the pass as a broad instruction-shape canonicalizer,
- then exploits the canonical form across arithmetic, boolean, memory, `call_ref`, and GC/reference-typed surfaces,
- while preserving effect order, trap behavior, and type validity.

That difference matters a lot for future parity work.

## What the pass sounds like versus what it actually does

What it sounds like:

- a small math peephole pass

What it actually is in `version_129`:

- a large function-parallel AST post-walk with local bit/sign-extension scanning, iterative canonicalization, arithmetic and ternary peepholes, memory and bulk-memory cleanup, `call_ref` directization, GC cast/trap logic, and deferred refinalization plus EH repair.

## Page map

- [`./binaryen-strategy.md`](./binaryen-strategy.md)
  - Deep dive into the actual Binaryen `version_129` implementation, helper dependencies, scheduler placement, main phases, and why the public name undersells the pass.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md)
  - Compact source-confirmed owner-file and lit-test map for the pass, including the exact split between `OptimizeInstructions.cpp`, registration files, helper headers, and the distributed dedicated lit surface.
- [`./gc-casts-call_ref-and-trap-sensitive-rewrites.md`](./gc-casts-call_ref-and-trap-sensitive-rewrites.md)
  - Focused guide to the easiest part of the pass to underestimate: null-trap reasoning, cast removal limits, descriptor/exactness handling, `call_ref` lowering, and unshared GC atomic rewrites.
- [`./wat-shapes.md`](./wat-shapes.md)
  - Beginner-friendly shape catalog covering positive, negative, bailout, control, memory, GC, `call_ref`, tuple, and metadata-sensitive rewrite families.
- [`./starshine-strategy.md`](./starshine-strategy.md)
  - Current Starshine strategy overview for the implemented HOT subset, with exact registry, dispatcher, owner-file, test, and CLI replay code locations.
- [`./starshine-hot-ir-strategy.md`](./starshine-hot-ir-strategy.md)
  - Exact MoonBit helper and code-map companion for the implemented HOT subset, plus the major upstream Binaryen behaviors the repo still does not model.
- [`./parity-matrix.json`](./parity-matrix.json)
  - Machine-readable OI-D through OI-M parity matrix with upstream owners, feature floors, oracle mode, Starshine status, priority, boundary reason, evidence, smoke profiles, and implemented/design transform metadata.
- [`./sweep-report.md`](./sweep-report.md)
  - Family-based parity sweep workflow and runner usage for replacing the old one-behavior / one-doc loop with grouped compare-pass discovery.
- [`./fuzzing.md`](./fuzzing.md)
  - Compare-pass smoke lane plus OI-specific GenValid profiles and first-layer metamorphic smoke transforms used by the parity matrix.
- research note 0444
  - Retained research bridge for the 2026-05-05 current-main no-drift spot check.
- [`../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md`](../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md)
  - `[O4Z-AUDIT-OI-A]` `version_130` source/lit matrix mapping upstream visitor and lit families to current Starshine coverage, explicit boundaries, and follow-up slice owners.

## Freshness and provenance note

Current durable answer:

- the detailed prose still mostly teaches from the reviewed `version_129` dossier because that is where the original deep read was filed;
- the 2026-06-19 `version_130` source/lit matrix remains the broad owner/family inventory and the 2026-07-12 closeout remains the historical local evidence baseline;
- Binaryen `version_131` is now the release oracle and changes equal-input `ref.eq`, identical-arm `select`, idempotent/deep-effect equality, directional movement checks, and non-concrete select bailout behavior;
- `[V131-OI]001` must layer focused v131 fixtures and renewed direct/O4z evidence over the existing matrix rather than treating the v130 closeout as current.

## Current O4z audit inventory

The older 2026-06-19 behavior inventory and `version_130` source matrix were the kickoff for the OI audit. The 2026-07-12 final matrix in [`./fuzzing.md`](./fuzzing.md) marks the v130 `[OI-INDEX]005` scope complete: regular GenValid is green at `100000/100000`, explicit wasm-smith leaves only three classified Starshine-win dead-value cleanups plus `44` Binaryen/tool failures, `pass-oi-all` leaves only the already-classified OI-M SB005 tuple-wrapper Starshine wins, and `random-all-profiles` leaves only classified `ssa-nomerge-smoke` Starshine cleanup wins. In other words, the reviewed Binaryen `version_130` OI contract was closed for true semantic mismatches. V131 has now met the release-drift reopen criterion, and `[V131-OI]001` is the active status bar.

## Current maintenance rule

- Treat this folder as the canonical home for future `optimize-instructions` parity and scheduler research.
- Start grouped parity work from [`./parity-matrix.json`](./parity-matrix.json) and [`./sweep-report.md`](./sweep-report.md). Do not add another one-off OI behavior probe without linking it to a matrix row or using it to update a row's status/evidence.
- Use Binaryen `version_130` as the release-gating O4z source/lit matrix for new implementation slices; use the older `version_129` dossier prose as historical explanatory material until fully refreshed.
- Keep the Binaryen strategy page, Starshine strategy page, parity matrix, and fuzzing page in sync whenever the in-tree implementation grows beyond the current integer / boolean / control-focused HOT subset.
- Keep the landing page honest about the ordered-artifact story:
  - slot `16` is retired
  - slot `44` is retired
  - the remaining work is only historical documentation depth and explicit reopening criteria; there is no active O4z behavior-gap backlog for OI

## Sources

- research note 1264 extends the public `tuple-optimization` effect-count boundary ladder to sixty-five later non-selected effects, recording Binaryen's `tuple.make 66` localization and Starshine's current tuple-scratch gap.
- research note 1262 extends the public `tuple-optimization` effect-count boundary ladder to sixty-four later non-selected effects, recording Binaryen's `tuple.make 65` localization and Starshine's current tuple-scratch gap.
- research note 1261 extends narrow identical local/local select-arm folding to direct same-ordered `i32.mul(local.get, local.get)` arms, matching Binaryen while keeping broader structural equality out of scope.
- research note 1260 extends the public `tuple-optimization` effect-count boundary ladder to sixty-three later non-selected effects, recording Binaryen's `tuple.make 64` localization and Starshine's current tuple-scratch gap.
- research note 1259 extends narrow identical local/local select-arm folding to direct same-ordered `i64.mul(local.get, local.get)` arms, matching Binaryen while keeping broader structural equality out of scope.
- research note 1258 extends the public `tuple-optimization` effect-count boundary ladder to sixty-two later non-selected effects, recording Binaryen's `tuple.make 63` localization and Starshine's current tuple-scratch gap.
- research note 1257 extends narrow identical local/local select-arm folding to direct same-ordered `i64.add(local.get, local.get)` arms, matching Binaryen while keeping broader structural equality out of scope.
- research note 1256 extends the public `tuple-optimization` effect-count boundary ladder to sixty-one later non-selected effects, recording Binaryen's `tuple.make 62` localization and Starshine's current tuple-scratch gap.
- research note 1254 extends the public `tuple-optimization` effect-count boundary ladder to sixty later non-selected effects, recording Binaryen's `tuple.make 61` localization and Starshine's current tuple-scratch gap.
- research note 1253 adds narrow same-instruction/same-local SIMD splat select-arm folding, matching Binaryen for the direct `i8x16.splat(local.get)` oracle while avoiding SIMD algebraic, lane-equivalent, or arbitrary structural equality claims.
- research note 1252 extends the public `tuple-optimization` effect-count boundary ladder to fifty-nine later non-selected effects, recording Binaryen's `tuple.make 60` localization and Starshine's current tuple-scratch gap.
- research note 1251 adds direct byte-identical `v128.const` select-arm folding, matching Binaryen's direct vector-constant oracle while avoiding SIMD algebraic, lane-equivalent, or arbitrary structural equality claims.
- research note 0131
- research note 0248
- [`../../../../../src/passes/optimize_instructions.mbt`](../../../../../src/passes/optimize_instructions.mbt)
- [`../../../../../src/passes/optimize_instructions_test.mbt`](../../../../../src/passes/optimize_instructions_test.mbt)
- [`../../../../../src/passes/registry_test.mbt`](../../../../../src/passes/registry_test.mbt)
- [`../../../../../src/cmd/cmd_wbtest.mbt`](../../../../../src/cmd/cmd_wbtest.mbt)
- [`../../../../../src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
- [`../tracker.md`](../tracker.md)
- [`../../no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
- [research note 0093](../late-pipeline-dispatch.md) preserves the saved generated-artifact `-O4z` slot, summary, and Binaryen debug-log facts; older `.artifacts` paths are replay identifiers, not durable wiki source links.
- research note 0726 records the current OI behavior-gap inventory and `[O4Z-AUDIT-OI-A]` through `[O4Z-AUDIT-OI-N]` backlog split.
- [`../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md`](../../../raw/binaryen/2026-06-19-optimize-instructions-version-130-source-refresh.md) completes `[O4Z-AUDIT-OI-A]` with the `version_130` source/lit matrix and slice ownership map.
- research note 0727 completes `[O4Z-AUDIT-OI-B]` with direct compare-pass and saved O4z slot16/slot44 replay evidence before behavior changes.
- research note 0728 completes `[O4Z-AUDIT-OI-C]` with raw no-op gate trace coverage, structured gate ordering repair, and outside-gate public cleanup evidence.
- research note 0729 completes `[O4Z-AUDIT-OI-D]` with default scalar arithmetic, float spelling, wrap-constant, and relational canonicalization coverage plus direct 10000 compare evidence.
- research note 1323 records the OI-D double-`eqz` rewrite slice: Starshine rewrites nonliteral direct `i32.eqz(i32.eqz(x))` and `i32.eqz(i64.eqz(x))` to the matching integer `ne` against zero, preserving the original operand; the 2026-06-27 family-matrix follow-up additionally folds literal double-`eqz` results to constants.
- research note 1325 records the OI-D zero-compare `eqz` inversion slice: Starshine now rewrites direct `eqz(ne(x, 0))` to integer `eqz(x)` and direct `eqz(eq(x, 0))` to integer `ne(x, 0)` for i32/i64 comparisons against exact zero constants.
- research note 1327 records the OI-D signed endpoint compare slice: Starshine now folds side-effect-free i32/i64 `lt_s`/`ge_s` against signed min and `gt_s`/`le_s` against signed max to constants, while preserving the Binaryen-observed effectful-call boundary.
- research note 0730 completes `[O4Z-AUDIT-OI-E]` with the first local scanner-style sign-extension facts, redundant sign-extension removal, shift-pair sign-extension idiom rewrites, focused tests, and direct 10000 compare evidence.
- research note 0731 completes `[O4Z-AUDIT-OI-F]` with boolean/select/ternary shell classification, constant-condition `select` cleanup, focused effect/trap negatives, branch-hint/no-fold option boundaries, and direct 10000 compare evidence.
- research note 1333 adds narrow OI-F De Morgan cleanup for direct `i32.and(i32.eqz X, i32.eqz Y)` to `i32.eqz(i32.or X Y)`, preserving operand order and leaving broader boolean algebra as an open boundary.
- research note 1335 adds direct i32 boolean-constant select cleanup: `select 1/0` becomes `i32.ne(cond, 0)` and `select 0/1` becomes `i32.eqz(cond)`.
- research note 1337 adds narrow direct select-condition negation cleanup: `(select A B (i32.eqz cond))` swaps the arms and uses `cond`, composing exact boolean-constant arms directly to `eqz`/`ne` forms.
- research note 1339 adds narrow direct zero-compare select-condition cleanup: `i32.eq(cond, 0)` swaps select arms and `i32.ne(cond, 0)` drops the redundant compare while preserving the condition operand/effects.
- research note 1341 adds narrow direct `i64.eqz` boolean-select cleanup: exact `1/0` arms become `i64.eqz(inner)`, exact `0/1` arms become `i64.ne(inner, 0)`, and non-boolean value selects remain unchanged.
- research note 1334 extends the OI-M public `tuple-optimization` ladder to one hundred later effects: Binaryen localizes through `tuple.make 101` and `100` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1336 extends the same public boundary ladder to one hundred one later effects: Binaryen localizes through `tuple.make 102` and `101` scalar local sets; Starshine keeps the mismatch open as tuple-scratch reconstruction work.
- research note 1338 extends the same public boundary ladder to one hundred two later effects: Binaryen localizes through `tuple.make 103` and `102` scalar local sets; Starshine keeps the mismatch open as tuple-scratch reconstruction work.
- research note 1340 extends the same public boundary ladder to one hundred three later effects: Binaryen localizes through `tuple.make 104` and `103` scalar local sets; Starshine keeps the mismatch open as tuple-scratch reconstruction work.
- research note 1342 extends the same public boundary ladder to one hundred four later effects: Binaryen localizes through `tuple.make 105` and `104` scalar local sets; Starshine keeps the mismatch open as tuple-scratch reconstruction work.
- research note 0732 records the first `[O4Z-AUDIT-OI-G]` byte bulk-memory sub-slice for size-1 `memory.copy` and `memory.fill` plus zero-size trap-mode boundary evidence.
- research note 0750 records the first `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: direct `ref.func` targets under `call_ref` and `return_call_ref` now directize to `call` and `return_call`.
- research note 0751 records the second `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: `table.get` targets under `call_ref` and `return_call_ref` now lower to `call_indirect` and `return_call_indirect`.
- research note 0752 records the third `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: zero-argument typed `select` targets whose arms are direct `ref.func`s now lower to an `if` with direct `call` / `return_call` arms; fallthrough-known and positive argument-bearing select-known-target lowering remained open at that point.
- research note 0753 records the fourth `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: argument-bearing select-of-`ref.func` targets were an explicit fail-closed boundary until Starshine had Binaryen-style argument localization.
- research note 0754 records the fifth `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: zero-argument fallthrough-known block targets now lower to a dropped target expression plus direct `call` / `return_call`, preserving target-side effects.
- research note 0755 records the sixth `[O4Z-AUDIT-OI-H]` `call_ref` sub-slice: argument-bearing select-of-`ref.func` targets now localize single-result call arguments before lowering to direct-call `if` arms.
- research note 0756 records the seventh `[O4Z-AUDIT-OI-H]` `call_ref` boundary sub-slice: mixed select arms remain intentionally kept unchanged, while the older argument-bearing fallthrough-known boundary is superseded by the 2026-06-24 localization implementation.
- research note 0811 records the earlier eighth `[O4Z-AUDIT-OI-H]` `call_ref` boundary sub-slice; the exact call-indexed `table.get` shape with a no-param one-result index call and pure local/constant call arguments is now superseded by the 2026-06-24 implementation, while broader effectful/localizing table-index forms remain open.
- The 2026-06-24 argument fallthrough localization sub-slice extends `[O4Z-AUDIT-OI-H]`: fallthrough-known block targets with single-result already-evaluated call arguments now store those arguments to temps, drop the target expression for effects, then reload the temps for direct `call` / `return_call`, matching Binaryen `version_130` for `.tmp/oi-h-argument-fallthrough-call-ref-probe.wat`.
- research note 0843 records a current `[O4Z-AUDIT-OI-H]` boundary: Binaryen localizes a multi-result argument producer through tuple scratch and scalar locals before select-of-`ref.func` directization, while Starshine keeps the shape unchanged until a safe multivalue argument localization helper exists.
- research note 0757 records the first `[O4Z-AUDIT-OI-I]` reference-null sub-slice: `ref.is_null(ref.null)` and null-operand `ref.eq` rewrites now cover the local null equality/test basics while broader casts and exactness-sensitive behavior remain open.
- research note 0758 records the second `[O4Z-AUDIT-OI-I]` reference sub-slice: first `ref.as_non_null` null/known-`i31` cleanup and exact `ref.cast(unreachable)` validity repair are covered while broader casts, tests, and descriptor/exactness-sensitive behavior remain open.
- research note 0759 records the third `[O4Z-AUDIT-OI-I]` reference sub-slice: known-non-null `ref.i31` / `ref.func` null tests and `ref.i31` null equality now fold to `i32.const 0`, with the supporting `ref.is_null` typechecker correction.
- research note 0760 records the fourth `[O4Z-AUDIT-OI-I]` reference sub-slice: `ref.as_non_null(ref.func f)` now reuses the local known-non-null constructor proof and rewrites to `ref.func f` while arbitrary cast/test/type proofs remain open.
- research note 0761 records the fifth `[O4Z-AUDIT-OI-I]` reference sub-slice: nullable null-operand `ref.test` folds to `i32.const 1` and nullable null-operand `ref.cast` rewrites to the null child; non-null cast/test fixture coverage is now separately locked by `0769`.
- research note 0762 records the sixth `[O4Z-AUDIT-OI-I]` reference sub-slice: exact successful `ref.test (ref i31)` and `ref.cast (ref i31)` forms fed by local `ref.i31` constructors now fold while broader successful cast/test proofs remain open.
- research note 0763 records the seventh `[O4Z-AUDIT-OI-I]` reference sub-slice: successful local-`ref.i31` cast/test folds now cover absolute target supertypes `eq` and `any` as well as exact `i31`.
- research note 0764 records the eighth `[O4Z-AUDIT-OI-I]` reference sub-slice: exact successful `ref.test (ref func)` and `ref.cast (ref func)` forms fed by local `ref.func` constructors now fold while target supertypes and broader cast/test proofs remain open.
- research note 0765 records the ninth `[O4Z-AUDIT-OI-I]` reference sub-slice: `ref.eq` between immediate `ref.i31(i32.const)` operands now folds to `i32.const 1` for equal payloads and `i32.const 0` for unequal payloads, while broader reference identity proofs remain open.
- research note 0766 records the tenth `[O4Z-AUDIT-OI-I]` reference sub-slice: declared non-null `local.get` refs now feed `ref.is_null`, `ref.as_non_null`, and null-equality folds through local type metadata, while nullable-local and broader flow-sensitive proofs remain open.
- research note 0767 records the eleventh `[O4Z-AUDIT-OI-I]` reference sub-slice: exact same-heap declared non-null local `ref.test` / `ref.cast` proofs now fold through local type metadata.
- research note 0768 records the twelfth `[O4Z-AUDIT-OI-I]` reference sub-slice: declared non-null `(ref i31)` locals now prove successful `ref.test` / `ref.cast` for absolute `eq` / `any` target supertypes.
- research note 0769 records the thirteenth `[O4Z-AUDIT-OI-I]` coverage/type-surface sub-slice: non-null null-operand `ref.test` now has validating direct-core coverage for folding to `i32.const 0`, and non-null null-operand `ref.cast` has validating direct-core coverage for rewriting to `unreachable`.
- research note 0770 records the fourteenth `[O4Z-AUDIT-OI-I]` reference sub-slice: ordinary `ref.test` / `ref.cast` validation now accepts disjoint `eq`-hierarchy sibling heap types, and local `ref.i31` values tested or cast against `struct` / `array` / indexed heap targets fold to `i32.const 0` or `unreachable`.
- research note 0771 records the fifteenth `[O4Z-AUDIT-OI-I]` reference sub-slice: `ref.eq` now folds to `i32.const 0` when a definitely non-null local `i31` value is compared with a local whose declared heap cannot be `i31`, such as a nullable struct local.
- research note 0772 records the sixteenth `[O4Z-AUDIT-OI-I]` reference sub-slice: `ref.eq` now folds to `i32.const 0` for absolute local `struct` / `array` heap pairs when at least one operand is declared non-null, while nullable-both equality remains unchanged because both operands can be null.
- research note 0773 records the seventeenth `[O4Z-AUDIT-OI-I]` reference sub-slice: declared non-null absolute `struct` / `array` locals now prove successful `ref.test` / `ref.cast` for absolute target supertypes `eq` and `any`, while broader subtype, nullable-local, descriptor, exactness, TNH, and IIT facts remain open.
- research note 0774 records the eighteenth `[O4Z-AUDIT-OI-I]` reference sub-slice: declared non-null absolute `struct` / `array` locals now prove failed `ref.test` / `ref.cast` against the other absolute aggregate sibling, while nullable-local, indexed/defined heap, arbitrary subtype-lattice, descriptor, exactness, TNH, and IIT facts remain open.
- research note 0775 records the nineteenth `[O4Z-AUDIT-OI-I]` reference sub-slice: direct `ref.eq(local.get N, local.get N)` now folds to `i32.const 1` for nullable and non-null local refs, while broader SSA identity and flow-sensitive equality proofs remain open.
- research note 0776 records the twentieth `[O4Z-AUDIT-OI-I]` reference sub-slice: direct `ref.eq(ref.i31(local.get N), ref.i31(local.get N))` now folds to `i32.const 1`, while broader SSA identity, flow-sensitive i31 value analysis, and non-local equality proofs remain open.
- research note 0777 records the twenty-first `[O4Z-AUDIT-OI-I]` reference sub-slice: direct same-local `ref.eq` now folds through an immediate nullable no-op `ref.cast` whose target heap exactly matches the local declaration, while arbitrary cast skipping, non-null cast trap removal, descriptor/exactness/TNH/IIT behavior, and broader flow-sensitive equality proofs remain open.
- research note 0778 records the twenty-second `[O4Z-AUDIT-OI-I]` reference sub-slice: direct same-local nullable `ref.as_non_null(local.get)` equality now folds to one preserved null-trapping check plus `i32.const 1`.
- research note 0779 records the twenty-third `[O4Z-AUDIT-OI-I]` coverage sub-slice: both-operands nullable no-op `ref.cast(local.get)` equality is locked by direct-core coverage.
- research note 0780 records the twenty-fourth `[O4Z-AUDIT-OI-I]` reference sub-slice: same-local equality now folds through immediate nullable absolute-heap upcast `ref.cast(local.get)` operands, currently using the existing absolute `struct` / `array` supertype helper.
- research note 0786 records the thirtieth `[O4Z-AUDIT-OI-I]` coverage sub-slice: nullable-source, nullable-target `ref.test` / `ref.cast` success on declared nullable absolute `i31` locals is now locked by direct-core coverage.
- research note 0787 records the thirty-first `[O4Z-AUDIT-OI-I]` reference sub-slice: immediate `ref.i31` known-miss `ref.test` / `ref.cast` rewrites now preserve effectful operands as `drop(operand)` before `i32.const 0` or `unreachable`.
- research note 0788 records the thirty-second `[O4Z-AUDIT-OI-I]` reference sub-slice: immediate `ref.i31` null-equality miss rewrites now preserve effectful operands as `drop(operand)` before `i32.const 0`.
- research note 0789 records the thirty-third `[O4Z-AUDIT-OI-I]` reference sub-slice: known-non-null `ref.is_null` folds now preserve effectful immediate `ref.i31` operands as `drop(operand)` before `i32.const 0`.
- research note 0790 records the thirty-fourth `[O4Z-AUDIT-OI-I]` reference sub-slice: definitely-successful `ref.test` folds now preserve effectful immediate `ref.i31` operands as `drop(operand)` before `i32.const 1`, while successful `ref.cast` keeps the effectful operand.
- research note 0791 records the thirty-fifth `[O4Z-AUDIT-OI-I]` reference sub-slice: represented impossible `ref.eq` folds now preserve effectful immediate `ref.i31` operands as `drop(operand)` before `i32.const 0`.
- research note 0792 records the thirty-sixth `[O4Z-AUDIT-OI-I]` coverage sub-slice: `ref.as_non_null(ref.i31(call $effect))` cleanup preserves the effectful `ref.i31(call $effect)` operand while removing the redundant assertion.
- research note 0793 records the thirty-seventh `[O4Z-AUDIT-OI-I]` coverage sub-slice: known-null `ref.as_non_null` cleanup preserves an already-evaluated `drop(call $effect)` prefix before the folded `unreachable`.
- research note 0794 records the thirty-eighth `[O4Z-AUDIT-OI-I]` coverage sub-slice: known-null non-null-target `ref.test` / `ref.cast` cleanup preserves an already-evaluated `drop(call $effect)` prefix before `i32.const 0` / `unreachable`.
- research note 0795 records the thirty-ninth `[O4Z-AUDIT-OI-I]` coverage sub-slice: known-null `ref.eq` cleanup preserves an already-evaluated `drop(call $effect)` prefix before `i32.const 1` or `i32.const 0` folds.
- research note 0796 records the fortieth `[O4Z-AUDIT-OI-I]` coverage sub-slice: direct same-local `ref.eq` cleanup preserves an already-evaluated `drop(call $effect)` prefix before `i32.const 1` folds.
- research note 0797 records the forty-first `[O4Z-AUDIT-OI-I]` coverage sub-slice: same-local `ref.i31(local.get)` equality cleanup preserves an already-evaluated `drop(call $effect)` prefix before `i32.const 1` folds.
- research note 0803 records the forty-seventh `[O4Z-AUDIT-OI-I]` coverage sub-slice: exact `ref.func` reference basics preserve an already-evaluated `drop(call $effect)` prefix while folding/removing `ref.test`, `ref.cast`, `ref.is_null`, and `ref.as_non_null` suffixes.
- research note 0804 records the forty-eighth `[O4Z-AUDIT-OI-I]` coverage sub-slice: nullable-source nullable-target `i31`→`eq` supertype `ref.test` / `ref.cast` cleanup preserves an already-evaluated `drop(call $effect)` prefix.
- research note 0805 records the forty-ninth `[O4Z-AUDIT-OI-I]` coverage sub-slice: impossible `ref.eq` between absolute aggregate sibling locals with at least one non-null operand preserves an already-evaluated `drop(call $effect)` prefix before folding to `i32.const 0`.
- research note 0806 records the fiftieth `[O4Z-AUDIT-OI-I]` coverage sub-slice: impossible `ref.eq` between a declared non-null `i31` local and a nullable struct local preserves an already-evaluated `drop(call $effect)` prefix before folding to `i32.const 0`.
- research note 0807 records the fifty-first `[O4Z-AUDIT-OI-I]` coverage sub-slice: non-null-source, non-null-target aggregate `ref.test` / `ref.cast` success and sibling-miss folds preserve an already-evaluated `drop(call $effect)` prefix.
- research note 0808 records the fifty-second `[O4Z-AUDIT-OI-I]` coverage sub-slice: declared non-null aggregate `ref.is_null` folds preserve an already-evaluated `drop(call $effect)` prefix before `i32.const 0`.
- research note 0809 records the fifty-third `[O4Z-AUDIT-OI-I]` implementation sub-slice: nullable `(ref null i31)` locals tested or cast against non-null aggregate targets now fold to `i32.const 0` / `unreachable` while preserving already-evaluated effect prefixes.
- research note 0810 records the fifty-fourth `[O4Z-AUDIT-OI-I]` coverage sub-slice: known-null operands tested or cast against non-null targets fold to `i32.const 0` / `unreachable` while preserving already-evaluated effect prefixes.
- research note 0812 records the fifty-fifth `[O4Z-AUDIT-OI-I]` coverage sub-slice: impossible `ref.eq` between declared non-null `i31` locals and nullable array locals preserves an already-evaluated `drop(call $effect)` prefix before folding to `i32.const 0`.
- research note 0813 records the fifty-sixth `[O4Z-AUDIT-OI-I]` implementation sub-slice: unknown nullable exact-heap non-null `ref.cast` now lowers to `ref.as_non_null`, preserving operand effects and the null trap while keeping narrowing heap-check casts as `ref.cast`.
- research note 0814 records the fifty-seventh `[O4Z-AUDIT-OI-I]` coverage sub-slice: indexed/defined struct and array locals are explicitly covered for impossible equality with definitely non-null `i31` references, including effect-prefix preservation before folding to `i32.const 0`.
- research note 1431 closes the current sampled non-descriptor `[O4Z-AUDIT-OI-I]` surface as an accepted v0.1 intentional boundary. Fresh `.tmp/oi-i-closeout-count256-20260703` compared 256/256 with 131 normalized matches, 125 classified residuals, zero validation/generator/property/command failures, Binaryen cache 256/0, runtime checked/unsupported/failed 256/0/0 with no executable runtime samples, and all 500 residual raw/canonical artifacts validating. Local-carried known-null matched 88/88; direct null/test/cast residuals are measured Starshine-win/canonical-smaller dropped-debris representation differences; branch-cast effectful-wrapper residuals are canonical-neutral/raw-smaller with preserved branch/effect/trap traffic; descriptor/exactness/TNH/IIT evidence remains quarantined to OI-J.
- research note 0818 records the first `[O4Z-AUDIT-OI-J]` exactness sub-slice: Starshine now preserves exact `ref.cast` checks when the operand has an inexact same-heap local type, matching Binaryen `version_130` and preventing invalid exact-result rewrites.
- research note 0819 records the second `[O4Z-AUDIT-OI-J]` exactness sub-slice: already-exact same-heap operands are covered for redundant exact `ref.cast` cleanup, including nullable-to-non-null exact casts becoming `ref.as_non_null` to keep the null trap.
- research note 0820 records the third `[O4Z-AUDIT-OI-J]` sub-slice: nullable-source / non-null-target `ref.test` now lowers to `i32.eqz(ref.is_null(x))`, preserving effectful operands while removing the redundant heap test.
- research note 0821 records the fourth `[O4Z-AUDIT-OI-J]` boundary sub-slice: Binaryen keeps official two-operand descriptor equality casts, while Starshine locks the local legacy unary `ref.cast_desc_eq` surface as fail-closed and documents the HOT descriptor-operand representation blocker.
- research note 1439 splits `[O4Z-AUDIT-OI-J]` into finite descriptor/exactness/TNH/IIT slices and records the focused `.tmp/oi-j-roadmap-probes-20260703` probe pack. The only implemented behavior slice selected there is default-mode standalone `ref.get_desc(ref.as_non_null(local.get nullable))` cleanup; non-local/effectful/trapping/control children, TNH, and IIT remain explicit follow-ups.
- research note 1440 lifts the narrow representation/tooling blockers for roadmap probe 01 descriptor-cast validation and probe 10 exact `ref.test` binary decode/encode support. `RefTest` now carries exactness, exact `ref.test` immediates preserve `0x62 <s33>` concrete heaps, and descriptor-cast validation accepts the probe-01 inexact non-null descriptor operand while rejecting nullable descriptor operands and descriptorless targets. Replay `.tmp/oi-j-probe-replay-20260703` produced `wasm-tools validate --features all`-clean Starshine outputs for probes 01 and 10; TNH/IIT, `ref.test_desc`, and broad descriptor optimizer behavior remain open.
- research note 1441 widens the standalone `ref.get_desc(ref.as_non_null(...))` default-mode cleanup from direct `LocalGet` to branch-free `Block` children with ordered zero-result effect/trap roots and a final nullable `local.get`. It keeps `br`/`br_if`/`if`/`loop`/`try_table`, ordinary non-descriptor `ref.as_non_null`, and TNH fail-closed; refreshed probes live under `.tmp/oi-j-standalone-refgetdesc-block-child-20260703/` and need shell replay.
- research note 1442 records the exact `ref.test` behavior slice: exact and same-heap non-null inexact successful exact predicates now fold to `i32.const 1`, effectful tested operands are preserved as dropped prefixes, and paired exact casts on inexact locals remain fail-closed.
- research note 1443 records the final concrete sibling ordinary `ref.test` / `ref.cast` miss slice: distinct final struct heaps now fold a non-null `$a` local tested/cast to unrelated `$b` to `i32.const 0` / `unreachable`, while avoiding the empty-struct probe pitfall where Binaryen can canonicalize sibling types together.
- research note 1444 extends that ordinary OI-J miss proof to non-final concrete sibling heaps when the closed module subtype table has no known heap that can overlap both source and target. The implementation scans module subtypes before folding to `i32.const 0` / `unreachable` and keeps an overlap guard fail-closed.
- research note 1445 records the exact-subtype exact-target miss slice: exact `$sub` operands tested/cast to exact `$base` now fold to `i32.const 0` / `unreachable` when `$sub != $base`, with effectful operands preserved as dropped prefixes and nullable-target/null boundaries still guarded.
- research note 1453 records the standalone `ref.get_desc` self-contained `br_on_null` trampoline slice: a zero-result inner block catches the null branch, the non-null fallthrough immediately branches to the descriptor child block, and the outer fallthrough is explicit `ref.null`, allowing the explicit `ref.as_non_null` to be removed while keeping arbitrary nested control, escaping labels, `br_on_cast*`, descriptor operands, EH, and multivalue children out of scope.
- research note 1454 records the standalone `ref.get_desc` self-contained `br_on_cast` slice: a nullable-to-non-null same-heap `br_on_cast` final root targets the descriptor child block itself, allowing the explicit `ref.as_non_null` to be removed while keeping descriptor BrOn variants, arbitrary targets, escaping labels, payload prefixes, effectful/control operands, EH, and multivalue children out of scope.
- research note 1455 records the standalone `ref.get_desc` self-contained `br_on_cast_fail` slice: a nullable-to-non-null same-heap `br_on_cast_fail` final root targets the descriptor child block itself on failure and falls through with a non-null same-heap value on success, allowing the explicit `ref.as_non_null` to be removed while keeping descriptor BrOn variants, arbitrary source/target heaps, escaping labels, payload prefixes, effectful/control operands, EH, and multivalue children out of scope.
- research note 1456 sharpens two OI-J blockers as unsupported/tooling/representation boundaries rather than implementable optimizer slices: local Binaryen `version_130` and `wasm-tools` text tooling reject `ref.test_desc`, Starshine's local legacy `ref.test_desc` path fails validation by decoding as descriptor cast traffic, and Binaryen descriptor BrOn forms (`br_on_cast_desc_eq*`) optimize descriptor child null checks while Starshine has no `BrOnCastDesc*`/`br_on_cast_desc*` lib/HOT/lower/validate representation.
- research note 1457 records the inexact strict-subtype exact-target miss slice: non-null `(ref $sub)` operands whose concrete heap strictly subtypes `$base` now fold `ref.test (ref (exact $base))` to false and `ref.cast (ref (exact $base))` to `unreachable`, preserving effectful producers as dropped prefixes and keeping same-heap inexact exact-target casts, descriptor casts, nullable null-matching cases, and broader useful-type-info surfaces outside scope.
- research note 1458 records the nullable exact-target null-only `ref.test` slice: nullable exact or inexact strict-subtype `$sub` operands tested against `(ref null (exact $base))` now rewrite to `ref.is_null`, preserving effectful operands as the predicate child.
- research note 1459 records the paired nullable exact-target `ref.cast` slice: nullable exact or inexact strict-subtype `$sub` operands cast to `(ref null (exact $base))` now narrow to `ref.cast nullref`, preserving effectful operands as the cast child so non-null values still trap and null remains the only successful result.
- research note 1460 records the final-heap exact `ref.test` slice: same-heap inexact operands now fold exact tests to true only when the heap is final, nullable final operands tested against non-null exact targets lower to the existing null check, explicit non-final `(sub ...)` same-heap exact tests stay as `ref.test`, and final exact casts remain deliberately unchanged to match Binaryen.
- research note 1461 records the exact descriptor-cast mismatch slice: a non-null strict-subtype operand paired with an exact descriptor for the supertype now folds `ref.cast_desc_eq (ref $base)` to `unreachable` for the pure/erasable operand subset, matching Binaryen while leaving exact-result descriptor-cast validation, nullable null-only descriptor casts, and effectful/control operands open.
- research note 1462 records the exact-result descriptor-cast validation slice: the public `optimize-instructions` pipeline now folds the direct non-null strict-subtype local plus fresh exact supertype descriptor exact-result spelling to `unreachable` in the raw lift-error path before final validation can reject the surviving `ref.cast_desc_eq`.
- research note 1463 records the nullable strict-subtype descriptor exactification validation slice: Binaryen exactifies `ref.cast_desc_eq (ref null $base)` fed by a nullable `$sub` operand plus fresh `$base` descriptor to `ref.cast_desc_eq (ref null (exact $base))`; Starshine already produced that shape, and final validation now accepts it while preserving mismatched and nullable descriptor-operand rejection.
- research note 1464 records the effectful-reference exact descriptor miss slice: Binaryen folds a branch-free effectful `(ref $sub)` operand plus fresh exact `$base` descriptor to `drop(effectful-ref); unreachable`; Starshine now preserves the reference effects before the unreachable replacement while keeping effectful descriptor operands and escaping control fail-closed.
- research note 1465 records the effectful-descriptor exact descriptor miss slice: Binaryen folds a pure `(ref $sub)` operand plus a branch-free effectful exact `$base` descriptor block to `drop(effectful-descriptor); unreachable`; Starshine now preserves the descriptor effects before the unreachable replacement while keeping both-effectful operand order, control/EH, and multivalue operands fail-closed.
- research note 1466 records the both-effectful exact descriptor miss slice: Binaryen folds branch-free effectful reference and descriptor operands to `drop(effectful-ref); drop(effectful-descriptor); unreachable`; Starshine now preserves both operands in reference-before-descriptor order while keeping escaping control, EH, multivalue operands, nullable descriptor casts, and TNH/IIT breadth fail-closed.
- research note 1467 records the call-produced exact descriptor miss slice: Binaryen folds a direct call returning strict-subtype `$sub` plus fresh exact `$base` descriptor to `drop(call); unreachable`; Starshine now preserves the direct call before the unreachable replacement while keeping call-indirect/call_ref, effectful descriptor calls, control/EH, and multivalue operands fail-closed.
- research note 1468 records the paired call-produced descriptor-operand exact descriptor miss slice: Binaryen folds a pure strict-subtype local reference plus a direct call returning an exact `$base` descriptor to `drop(call); unreachable`; Starshine now preserves the descriptor call before `unreachable` in the HOT proof path and in the public raw dispatcher before the stack-carried-effect skip, while keeping parameterized calls, indirect/call_ref descriptors, both-call operands, control/EH, multivalue, and TNH/IIT breadth fail-closed.
- research note 1469 records the both-call exact descriptor miss slice: Binaryen folds direct no-parameter calls producing the strict-subtype reference and exact supertype descriptor to `drop(call-ref); drop(call-desc); unreachable`; Starshine now preserves both direct calls in source order through the HOT two-drop path and the public raw dispatcher while keeping effectful/trapping parameterized calls, wider or mixed-arity calls, indirect/call_ref/multivalue producers, escaping control/EH, TNH/IIT, nullable descriptor null-only casts, `ref.test_desc`, and descriptor BrOn open or fail-closed.
- **Consolidated OI-J pure-local direct-call descriptor-miss matrix:** the absorbed notes 1470–1553 proved every `(reference-arity, descriptor-arity)` pair in `1..9 × 1..9`, plus `(10,10)`, `(10,9)`, and `(9,10)`. For each pair, Binaryen v130 and Starshine preserve all local argument evaluations, execute the strict-subtype reference call, drop its result, execute the exact-target descriptor call, drop its result, and replace `ref.cast_desc_eq` with `unreachable`. The current `RawOiDescriptorDirectCallMissFact` is count-derived: it queries maximal nontrapping argument runs and validates each call signature against the observed argument count rather than consulting an arity whitelist. Maintain representative low, high, and asymmetric regression cases; do not create another per-pair raw note or hardcoded pair matrix. Non-local, effectful or trapping arguments, indirect/return-call producers, multivalue, escaping control/EH, nullable null-only casts, `ref.test_desc`, and descriptor BrOn remain separate proof or representation surfaces.
- research note 0822 records the twenty-second `[O4Z-AUDIT-OI-G]` sub-slice: Binaryen `version_130` keeps superset low-bit masks before narrow stores, while Starshine locks the existing smaller output shape that removes those masks when all bits written by the store are preserved.
- research note 1306 extends the OI-M public `tuple-optimization` ladder to eighty-six later effects: Binaryen localizes through `tuple.make 87` and `86` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1308 extends the OI-M public `tuple-optimization` ladder to eighty-seven later effects: Binaryen localizes through `tuple.make 88` and `87` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1309 extends OI-F effectful-condition identical-arm cleanup to same-ordered local/local `ref.i31` payloads: Binaryen drops the effectful condition before one surviving `ref.i31(i32.add(local.get, local.get))`, and Starshine now admits the same narrow nontrapping i32 local/local payload shell family without claiming commuted or trapping payloads.
- research note 1310 extends the OI-M public `tuple-optimization` ladder to eighty-eight later effects: Binaryen localizes through `tuple.make 89` and `88` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1311 extends OI-F effectful-condition identical-arm cleanup to exact byte-identical `v128.const` arms: Binaryen drops the effectful condition before one surviving vector constant, and Starshine now admits the same narrow nontrapping constant-arm proof while keeping lane-equivalent SIMD spellings and vector algebra out of scope.
- research note 1312 extends the OI-M public `tuple-optimization` ladder to eighty-nine later effects: Binaryen localizes through `tuple.make 90` and `89` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1298 extends the OI-M public `tuple-optimization` ladder to eighty-two later effects: Binaryen localizes through `tuple.make 83` and `82` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1300 extends the OI-M public `tuple-optimization` ladder to eighty-three later effects: Binaryen localizes through `tuple.make 84` and `83` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1324 extends the OI-M public `tuple-optimization` ladder to ninety-five later effects: Binaryen localizes through `tuple.make 96` and `95` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1326 extends the OI-M public `tuple-optimization` ladder to ninety-six later effects: Binaryen localizes through `tuple.make 97` and `96` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1328 extends the OI-M public `tuple-optimization` ladder to ninety-seven later effects: Binaryen localizes through `tuple.make 98` and `97` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1296 extends the OI-M public `tuple-optimization` ladder to eighty-one later effects: Binaryen localizes through `tuple.make 82` and `81` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1294 extends the OI-M public `tuple-optimization` ladder to eighty later effects: Binaryen localizes through `tuple.make 81` and `80` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1290 extends the OI-M public `tuple-optimization` ladder to seventy-eight later effects: Binaryen localizes through `tuple.make 79` and `78` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1305 extends OI-F effectful-condition identical-arm cleanup to same-instruction direct-local v128 splat shells, preserving the dropped condition before the surviving nontrapping splat and retaining SIMD algebraic equality, lane-equivalent spellings, v128 unary/binary algebra, and arbitrary expression equality as open boundaries.
- research note 1297 extends OI-F effectful-condition identical-arm cleanup to same-ordered f32/f64 float binary local/local shells, preserving the dropped condition before the surviving nontrapping arithmetic expression and retaining broad NaN/algebraic equality, value-equivalent constants, commuted operands, SIMD, and arbitrary expression equality as open boundaries.
- research note 1299 extends OI-F effectful-condition identical-arm cleanup to direct f32/f64 float unary local shells, preserving the dropped condition before the surviving nontrapping unary expression and retaining arbitrary-expression, SIMD, trapping-integer, and trapping-conversion equality as open boundaries.
- research note 1295 extends OI-F effectful-condition identical-arm cleanup to same-ordered f32/f64 float compare local/local shells, preserving the dropped condition before the surviving nontrapping compare and retaining broad NaN/algebraic equality, value-equivalent constants, commuted operands, and arbitrary expression equality as open boundaries.
- research note 1293 extends OI-F effectful-condition identical-arm cleanup to same-ordered i32/i64 integer compare local/local shells, preserving the dropped condition before the surviving nontrapping compare and retaining trapping conversions, float/NaN equality, commuted operands, and arbitrary expression equality as open boundaries.
- research note 1289 extends OI-F effectful-condition identical-arm cleanup to same-ordered i32/i64 shift and rotate local/local shells, preserving the dropped condition before the surviving nontrapping arm and retaining div/rem, commuted operands, and arbitrary expression equality as open boundaries.
- research note 1288 extends the OI-M public `tuple-optimization` ladder to seventy-seven later effects: Binaryen localizes through `tuple.make 78` and `77` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1286 extends the OI-M public `tuple-optimization` ladder to seventy-six later effects: Binaryen localizes through `tuple.make 77` and `76` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1284 extends the OI-M public `tuple-optimization` ladder to seventy-five later effects: Binaryen localizes through `tuple.make 76` and `75` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1282 extends the OI-M public `tuple-optimization` ladder to seventy-four later effects: Binaryen localizes through `tuple.make 75` and `74` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1280 extends the OI-M public `tuple-optimization` ladder to seventy-three later effects: Binaryen localizes through `tuple.make 74` and `73` scalar local sets, while Starshine keeps block/drop/call/local.get spelling and records the retained tuple-scratch reconstruction gap.
- research note 1279 extends OI-F identical select-arm cleanup to direct nontrapping leaf arms with effectful/trapping conditions: Starshine now preserves the condition as `drop(condition)` before the folded leaf value, matching Binaryen for direct local/global leaf probes while leaving trapping expression-arm reordering open.
- research note 1281 extends that OI-F effectful-condition identical-arm cleanup to one nontrapping expression shell, same-ordered `i32.add(local.get, local.get)`, while retaining trapping div/rem, commuted operands, and arbitrary expression equality as open boundaries.
- research note 1283 extends the same effectful-condition proof to same-ordered `i64.add(local.get, local.get)`, preserving the dropped condition before the single nontrapping arithmetic arm.
- research note 0837 records the twenty-third `[O4Z-AUDIT-OI-G]` sub-slice: Starshine now widens `i32.store8` / `i32.store16` / `i32.store` of a direct one-use `i32.wrap_i64` operand to `i64.store8` / `i64.store16` / `i64.store32` of the original i64 operand, dropping the wrap, while keeping the reverse `i64.extend_i32_*` direction and multi-use wraps as boundaries.
- research note 0838 extends the completed `[O4Z-AUDIT-OI-D]` default-scalar surface: Starshine now folds identity binary operands (`or`/`xor`/`add`/`shl`/`shr_u`/`shr_s` with `0`, `and` with `-1`, `mul` with `1`, and `sub` with a `0` right operand, i32 and i64) to the other operand, matching Binaryen `version_130` direct `--optimize-instructions`, while keeping `sub(0, x)`.
- research note 0839 extends the OI-D binary-operand surface: Starshine now folds zero-absorbing `mul`/`and` with a `0` operand and signed `rem` with divisor `-1` (i32 and i64) to `0` when the non-constant operand is side-effect-free, while keeping effectful `mul`/`and`/`rem_s` forms.
- research note 0841 extends OI-D with nested constant shift/rotate combining, all-ones `or` absorbing with effect-preserving `drop`, and float mul/div-by-`-1` plus div-by-`1` spelling rewrites matching Binaryen `version_130` without fast-math float `0`/`1` identity folding.
- A 2026-06-25 OI-D slice extends the direct sign-extension range surface in [`../../../../../src/passes/optimize_instructions.mbt`](../../../../../src/passes/optimize_instructions.mbt): `i32.eq` / `i32.ne` on direct `i32.extend8_s` or `i32.extend16_s` values compared with constants outside the signed lane range now fold, preserving effectful inputs before the folded constant. The living log records the Binaryen probe and focused red-first evidence.
- A 2026-06-25 OI-G slice extends direct load/store cleanup in [`../../../../../src/passes/optimize_instructions.mbt`](../../../../../src/passes/optimize_instructions.mbt): one-use full-width load children under `f32.reinterpret_i32`, `f64.reinterpret_i64`, `i32.reinterpret_f32`, and `i64.reinterpret_f64` now rewrite to representation loads (`f32.load`, `f64.load`, `i32.load`, `i64.load`) matching Binaryen `version_130`. The living log records the Binaryen probe and red-first focused evidence.
- research note 0842 extends `[O4Z-AUDIT-OI-G]` load-result cleanup: one-use i32 loads under `i64.extend_i32_u` / `i64.extend_i32_s` now rewrite to matching i64 representation loads when the loaded-value semantics are identical.
- research note 0844 locks the memarg-preservation detail for OI-G representation-load rewrites: replacement `f32.load`, `i64.load32_u`, and `i64.load16_s` operations keep the source load offset and alignment.
- research note 0860 extends the OI-G stack-carried tiny-copy raw-gate escape: each address operand may now independently be a pure/local constant or no-param direct call, or a pure argument followed by a one-param direct call, before the existing HOT load/store lowering runs.
- research note 0861 extends the OI-G stack-carried byte-fill raw-gate escape to the matching one-pure-argument direct-call destination/value operands, while keeping wider call-backed fills as documented Binaryen keep-spelling boundaries.
- research note 0864 extends the OI-G tiny bulk-memory raw-gate escape to `global.get` operands used directly or as direct-call arguments, with the order-safety proof kept bulk-memory-specific rather than applying to commutative stack-call binops.
- research note 0847 records that local-carried/shared load-result spellings are a source-backed OI-G boundary: Binaryen keeps the probed `local.tee(i32.load)` plus reinterpret/extend forms, matching Starshine's direct one-use load-child guard.
- research note 0849 records the analogous local-carried/shared reinterpret-store boundary: Binaryen keeps the probed `local.tee(f32.reinterpret_i32(...))` and `local.set`/`local.get` forms before `f32.store`, matching Starshine's direct one-use stored-value guard.
- research note 0845 extends the OI-H multi-result argument select-of-`ref.func` boundary to `return_call_ref`: Binaryen lowers through tuple scratch plus scalar locals, while Starshine keeps the tail-call form until tuple-scratch localization is implemented.
- research note 0846 records an OI-M tuple boundary: Binaryen localizes a multi-result non-selected tuple sibling through tuple scratch and scalar drops, while Starshine keeps the direct-HOT tuple spelling until a safe multi-result sibling localizer exists.
- research note 0848 adds first OI-M local-cleanup neighbor coverage: the covered single-result effectful-sibling tuple localization remains a temp-local/effect-drop block after `simplify-locals-nostructure`, matching the Binaryen `version_130` probe without closing tuple-scratch or broader tuple-neighbor parity.
- research note 0865 locks trap preservation for the covered one-use tuple extraction localizer: earlier and later non-selected `i32.load` siblings remain dropped effects around the selected lane instead of being treated as pure tuple debris.
- research note 0866 extends OI-G exact constant-size `memory.copy` lowering to size `16`, matching Binaryen's `v128.load` / `v128.store` one-load/one-store shape and updating the tiny bulk-memory raw gate accordingly.
- research note 0880 adds public-pipeline status coverage for that same size-16 `memory.copy` lane when destination and source are no-parameter direct calls; Starshine reaches `v128.load` / `v128.store` without the stack-carried-effect raw skip and preserves destination-before-source call order.
- research note 0867 adds OI-M selected-lane trap coverage: a selected `i32.load` child of one-use `tuple.extract(tuple.make(...))` remains the returned load rather than being omitted as tuple debris.
- research note 0868 extends OI-G exact constant-size `memory.fill` lowering to the size-16 zero-fill subset, matching Binaryen's `v128.const 0` / `v128.store` shape; its temporary nonzero repeated-byte boundary is superseded by the next OI-G slice.
- research note 0869 adds OI-M coverage for the selected trapping tuple lane when a later sibling has effects; Starshine stores the exact selected `i32.load`, drops the sibling call, and reloads the selected temp.
- research note 0870 completes OI-G constant size-16 repeated-byte `memory.fill` lowering by materializing arbitrary constant low bytes as repeated-byte `v128.const` values before `v128.store`.
- research note 0871 adds OI-M coverage for a selected trapping tuple lane after an earlier effectful sibling; Starshine preserves `drop(call)` before the exact selected `i32.load`.
- research note 0872 records the effectful zero-size bulk-memory boundary: Binaryen and Starshine keep zero-size `memory.copy` / `memory.fill` with call operands instead of dropping operand effects.
- research note 0873 adds OI-M coverage for a selected trapping tuple lane with both earlier and later effectful siblings; Starshine preserves the same drop/store/drop/reload order as Binaryen's tuple-scratch shape.
- research note 0874 records the effectful dynamic-size bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with call-backed size operands instead of guessing exact tiny lowering.
- research note 0875 adds OI-M coverage for a selected trapping tuple lane with an earlier effectful sibling and two later effectful siblings; Starshine preserves the same drop/store/drop/drop/reload order as Binaryen's tuple-scratch shape.
- research note 0876 records the local dynamic-size bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with nonconstant local size operands instead of guessing exact tiny lowering.
- research note 0890 records the global dynamic-size bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with nonconstant `global.get` size operands and preserve local/global operand order.
- research note 0892 records the size-5 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 5` lengths rather than synthesizing mixed 4+1 load/store sequences.
- research note 0894 records the size-7 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 7` lengths rather than synthesizing mixed 4+2+1 load/store sequences.
- research note 0910 records the size-9 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 9` lengths rather than synthesizing mixed 8+1 load/store sequences.
- research note 0914 records the size-13 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 13` lengths rather than synthesizing mixed 8+4+1 load/store sequences.
- research note 1144 records the size-14 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 14` lengths rather than synthesizing mixed 8+4+2 load/store sequences.
- research note 1146 records the size-17 bulk-memory boundary: Binaryen and Starshine keep `memory.copy` / `memory.fill` with `i32.const 17` lengths rather than combining a SIMD 16-byte lane with a trailing scalar byte.
- research note 1145 records the direct selected-sixteenth multi-result boundary: Binaryen and Starshine keep `tuple.extract 16 15 (call $multi)` when there are no sibling tuple values to preserve or drop.
- research note 1147 records the direct selected-seventeenth multi-result boundary: Binaryen and Starshine keep `tuple.extract 17 16 (call $multi)` when there are no sibling tuple values to preserve or drop.
- research note 1149 records the direct selected-eighteenth multi-result boundary: Binaryen and Starshine keep `tuple.extract 18 17 (call $multi)` when there are no sibling tuple values to preserve or drop.
- research note 1151 records the direct selected-nineteenth multi-result boundary: Binaryen and Starshine keep `tuple.extract 19 18 (call $multi)` when there are no sibling tuple values to preserve or drop.
- research note 1152 records the size-19 bulk-memory boundary: Binaryen and Starshine keep constant-size-19 `memory.copy` and `memory.fill` instead of synthesizing SIMD-plus-scalar lowering.
- research note 1158 records the size-20 bulk-memory boundary: Binaryen and Starshine keep constant-size-20 `memory.copy` and `memory.fill` instead of synthesizing SIMD-plus-scalar lowering.
- research note 1160 records the size-21 bulk-memory boundary: Binaryen and Starshine keep constant-size-21 `memory.copy` and `memory.fill` instead of synthesizing SIMD-plus-scalar lowering.
- research note 1162 records the size-22 bulk-memory boundary: Binaryen and Starshine keep constant-size-22 `memory.copy` and `memory.fill` instead of synthesizing SIMD-plus-scalar lowering.
- research note 1164 records the size-23 bulk-memory boundary: Binaryen and Starshine keep constant-size-23 `memory.copy` and `memory.fill` instead of synthesizing SIMD-plus-scalar lowering.
- research note 1166 records the size-24 bulk-memory boundary: Binaryen and Starshine keep constant-size-24 `memory.copy` and `memory.fill` instead of synthesizing a SIMD lane plus eight-byte tail.
- research note 1168 records the size-25 bulk-memory boundary: Binaryen and Starshine keep constant-size-25 `memory.copy` and `memory.fill` instead of synthesizing SIMD plus scalar tail lowering.
- research note 1153 records a tuple-optimization neighbor boundary: Binaryen localizes a public multivalue block with two later non-selected effects through tuple scratch while Starshine keeps the block/drop spelling.
- research note 1155 records a tuple-optimization neighbor boundary: Binaryen localizes a public multivalue block with three later non-selected effects through tuple scratch while Starshine keeps the block/drop spelling.
- research note 1157 extends the public tuple-optimization neighbor boundary to four later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/four-drop spelling.
- research note 1159 extends the public tuple-optimization neighbor boundary to five later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/five-drop spelling.
- research note 1161 extends the public tuple-optimization neighbor boundary to six later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/six-drop spelling.
- research note 1163 extends the public tuple-optimization neighbor boundary to seven later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/seven-drop spelling.
- research note 1165 extends the public tuple-optimization neighbor boundary to eight later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/eight-drop spelling.
- research note 1167 extends the public tuple-optimization neighbor boundary to nine later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/nine-drop spelling.
- research note 1169 extends the public tuple-optimization neighbor boundary to ten later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/ten-drop spelling.
- research note 1191 extends the public tuple-optimization neighbor boundary to twenty-one later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/twenty-one-drop spelling.
- research note 1193 extends the public tuple-optimization neighbor boundary to twenty-two later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/twenty-two-drop spelling.
- research note 1195 extends the public tuple-optimization neighbor boundary to twenty-three later non-selected effects, documenting Binaryen tuple scratch plus scalar-local reconstruction and Starshine's retained block/twenty-three-drop spelling.
- research note 1197 extends the public tuple-optimization neighbor boundary to twenty-four later non-selected effects, documenting Binaryen `tuple.make 25` plus scalar-local reconstruction and Starshine's retained block/twenty-four-drop spelling.
- research note 1199 extends the public tuple-optimization neighbor boundary to twenty-five later non-selected effects, documenting Binaryen `tuple.make 26` plus scalar-local reconstruction and Starshine's retained block/twenty-five-drop spelling.
- research note 1201 extends the same public tuple-optimization neighbor boundary to twenty-six later non-selected effects, documenting Binaryen `tuple.make 27` plus scalar-local reconstruction and Starshine's retained block/twenty-six-drop spelling.
- research note 1203 extends the same public tuple-optimization neighbor boundary to twenty-seven later non-selected effects, documenting Binaryen `tuple.make 28` plus tuple/scalar scratch localization and Starshine's retained block/twenty-seven-drop spelling.
- research note 1205 extends the same public tuple-optimization neighbor boundary to twenty-eight later non-selected effects, documenting Binaryen `tuple.make 29` plus tuple/scalar scratch localization and Starshine's retained block/twenty-eight-drop spelling.
- research note 1198 extends OI-D maxBits producer coverage to `i64.extend_i32_u`, so Starshine folds out-of-range compares and rewrites nonnegative signed relational compare spellings through the unsigned extension while keeping dynamic-shift and mixed signed-constant relational boundaries open.
- research note 1200 narrows the `i64.extend_i32_u` maxBits producer by preserving child i32 range facts such as `x & 255`, matching Binaryen's out-of-range signed compare fold instead of only using the conservative `u32::MAX` fallback.
- research note 1202 records that signed relational constant pairs with literal-zero left operands fold to `i32.const` booleans in Binaryen and are already covered by Starshine, without generalizing the mixed signed constant-pair boundary.
- research note 0896 extends OI-D `maxBits` coverage to unsigned load widths: Starshine now folds out-of-range compares for `i32.load8_u` / `i32.load16_u` and the analogous i64 unsigned loads while preserving the trapping load as a dropped operand.
- research note 0898 extends the direct nonnegative `maxBits` proof to signed relational out-of-range folds while preserving effectful/trapping operands.
- research note 0900 adds in-range signed-to-unsigned compare spelling for the same direct nonnegative `maxBits` proof surface, including masked, shifted, and unsigned-load producers.
- research note 0902 adds the first straight-line local-carried unsigned max facts, covering direct `local.set` / `local.tee` roots assigned covered `and`, positive-`shr_u`, unsigned-load, or nested producers.
- research note 0904 extends OI-D `maxBits` coverage through `i32.wrap_i64` when the wrapped i64 producer has a proven nonnegative max no larger than `i32::MAX`, enabling direct folds and local-carried signed-spelling rewrites.
- research note 0906 records the dynamic-shift maxBits boundary: Binaryen folds the constant-zero shift variant but keeps a dynamic `i32.shr_u` amount over a masked value, and Starshine now locks the same keep-spelling behavior until dynamic-shift range reasoning is deliberately sourced.
- research note 0908 records the i64 sibling dynamic-shift maxBits boundary: Binaryen keeps a dynamic `i64.shr_u` amount over a masked value while folding the constant-zero variant, and Starshine locks the same keep-spelling behavior.
- research note 1150 records already-implemented OI-D coverage for constant shift/rotate effective-amount masking: Starshine matches Binaryen's reduction of masked-zero amounts to the original value and nonzero amounts to `31`/`63`.
- research note 1154 adds OI-D red-first coverage and implementation for the narrow `i32.extend8_s` / `i32.extend16_s` unsigned-top compare rewrite: `lt_u` / `ge_u` against `u32::MAX` becomes `ne` / `eq` over an `and` mask while preserving effectful inputs once.
- research note 1156 adds OI-D red-first coverage and implementation for the i64 sign-extension unsigned-top sibling: `i64.lt_u` / `i64.ge_u` against `u64::MAX` becomes `i64.ne` / `i64.eq` over the retained `i64.extend8_s` / `i64.extend16_s` / `i64.extend32_s` operand.
- research note 1190 adds OI-D red-first coverage and implementation for constant integer equality/inequality compare folding: direct i32/i64 `eq` / `ne` with two constant operands now lowers to the corresponding `i32.const` boolean result, while relational constant folding remains outside this narrow slice.
- research note 1192 adds OI-D red-first coverage and implementation for unsigned domain-edge compare folding: `x <_u 0` and `x >_u UINT_MAX` fold to false, while `x >=_u 0` and `x <=_u UINT_MAX` fold to true, preserving effectful operands with a drop.
- research note 1194 adds OI-D red-first coverage and implementation for direct i32/i64 unsigned relational constant pairs: `lt_u`, `le_u`, `gt_u`, and `ge_u` with two constants now fold to `i32.const` booleans, while signed relational constant folding remains deliberately outside this narrow slice.
- research note 0877 adds OI-M coverage for a selected trapping tuple lane after two earlier effectful siblings; Starshine preserves both earlier drops before the exact selected load.
- research note 0879 adds OI-M coverage for a selected trapping tuple lane with two earlier effectful siblings and one later effectful sibling; Starshine preserves two earlier drops, the selected temp store, the later drop, and the final reload.
- research note 0881 extends that selected trapping tuple coverage to two earlier plus two later effectful siblings, preserving the same drop/drop/store/drop/drop/reload order as Binaryen's tuple-scratch shape.
- research note 0882 extends the OI-G non-local wider `memory.fill` boundary to the call-backed size-16 SIMD-width lane: Binaryen keeps `call $dst; call $val; i32.const 16; memory.fill`, and Starshine now locks the same no-`v128.store` behavior.
- research note 0884 extends the same OI-G keep-spelling boundary to a local.get-backed size-16 SIMD-width `memory.fill`: Binaryen keeps `local.get $dst; local.get $value; i32.const 16; memory.fill`, and Starshine now locks the same no-`v128.store` behavior.
- research note 0883 extends selected trapping tuple coverage to two earlier plus three later effectful siblings, preserving the drop/drop/store/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0885 extends selected trapping tuple coverage to two earlier plus four later effectful siblings, preserving the drop/drop/store/drop/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0886 records the OI-G size-3 `memory.copy` boundary: Binaryen keeps the non-power-of-two three-byte copy as `memory.copy`, and Starshine now locks the same no-multi-store behavior in the exact tiny-copy boundary test.
- research note 0887 extends selected trapping tuple coverage to two earlier plus five later effectful siblings, preserving the drop/drop/store/drop/drop/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0891 extends selected trapping tuple coverage to two earlier plus six later effectful siblings, preserving the drop/drop/store/drop/drop/drop/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0893 extends selected trapping tuple coverage to two earlier plus seven later effectful siblings, preserving the drop/drop/store/drop/drop/drop/drop/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0895 extends selected trapping tuple coverage to two earlier plus eight later effectful siblings, preserving the drop/drop/store/drop/drop/drop/drop/drop/drop/drop/drop/reload order observed in Binaryen's tuple-scratch output.
- research note 0897 records the earlier multi-result tuple sibling boundary: Binaryen localizes the multi-result `$pair` child through tuple scratch before returning the selected scalar lane, while Starshine keeps the direct-HOT tuple spelling until multi-result tuple-scratch localization exists.
- research note 0899 records the selected-third multi-result tuple boundary: Binaryen localizes a selected `f64` third result through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0901 records the selected-fourth multi-result tuple boundary: Binaryen localizes a selected `f64` fourth result from an `(i32 i64 f32 f64)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0903 records the selected-fifth multi-result tuple boundary: Binaryen localizes a selected `i32` fifth result from an `(i32 i64 f32 f64 i32)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0905 records the selected-sixth multi-result tuple boundary: Binaryen localizes a selected `i64` sixth result from an `(i32 i64 f32 f64 i32 i64)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0907 records the selected-seventh multi-result tuple boundary: Binaryen localizes a selected `f32` seventh result from an `(i32 i64 f32 f64 i32 i64 f32)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0909 records the selected-eighth multi-result tuple boundary: Binaryen localizes a selected `f64` eighth result from an `(i32 i64 f32 f64 i32 i64 f32 f64)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0911 records the selected-ninth multi-result tuple boundary: Binaryen localizes a selected `i32` ninth result from an `(i32 i64 f32 f64 i32 i64 f32 f64 i32)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0915 records the selected-eleventh multi-result tuple boundary: Binaryen localizes a selected `i32` eleventh result from an `(i32 i64 f32 f64 i32 i64 f32 f64 i32 i64 i32)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0917 records the selected-twelfth multi-result tuple boundary: Binaryen localizes a selected `i64` twelfth result from an `(i32 i64 f32 f64 i32 i64 f32 f64 i32 i64 i32 i64)` call through tuple scratch and scalar temps, while Starshine keeps direct-HOT tuple spelling until selected-child tuple-scratch localization exists.
- research note 0888 records the OI-G size-3 `memory.fill` boundary: Binaryen keeps the non-power-of-two three-byte fill as `memory.fill`, and Starshine now locks the same no-partial-store behavior.
- research note 0889 records the OI-D i32 sign-extension relational boundary: Binaryen keeps signed relational comparisons over out-of-range `i32.extend8_s` / `i32.extend16_s` results, and Starshine now locks that the equality-only fold does not generalize there.
- research note 0823 records the first `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now forwards pure `struct.get` operations over matching `struct.new` constructors and keeps effectful sibling-field cases unchanged until a safe localizing lowering can preserve those effects.
- research note 0824 records the second `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now forwards pure packed `struct.get_s` / `struct.get_u` operations over matching `struct.new` constructors, preserving signedness with constants, sign-extension, or masks.
- research note 0825 records the third `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now folds pure one-use `array.len(array.new_fixed(...))` to the fixed length while keeping effectful element operands unchanged.
- research note 0826 records the fourth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now forwards constant-index plain `array.get` over matching one-use `array.new_fixed` elements and folds constant out-of-bounds indexes to `unreachable`, while keeping effectful sibling-element cases unchanged until safe localization exists.
- research note 0827 records the fifth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now forwards constant-index packed `array.get_s` / `array.get_u` over matching one-use `array.new_fixed` elements, preserving signedness through constants, sign-extension, or masks, while keeping dynamic indexes and effectful sibling-element cases unchanged until safe localization exists.
- research note 0828 records the sixth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now folds direct one-use plain and packed `struct.get(struct.new_default ...)` field reads to WebAssembly default values, while keeping descriptor default constructors and broader array/default/atomic/localization work open.
- research note 0829 records the seventh `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now folds direct one-use constant-length/index plain and packed `array.get(array.new_default ...)` reads to zero/default values or `unreachable` for out-of-bounds, while keeping dynamic lengths/indexes and broader array/default/localization work open.
- research note 0830 records the eighth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now folds direct one-use small non-negative constant-length `array.len(array.new_default ...)` to the length constant, while keeping dynamic, negative, and huge non-negative lengths unchanged to preserve allocation traps.
- research note 0831 records the ninth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now folds direct one-use `array.len(array.new ...)` to the bounded constant length when both the repeated value and length are `i32.const`, while keeping nonconstant/effectful repeated values and trap-preserving length boundaries unchanged.
- research note 0832 records the tenth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now forwards constant-index plain and packed `array.get(array.new ...)` repeated values for small constant lengths, preserves effectful selected values, folds pure out-of-bounds reads to `unreachable`, and keeps dynamic indexes plus effectful out-of-bounds localization open.
- research note 0833 records the eleventh `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now removes pure constant-index `array.set` operations targeting direct one-use fresh `array.new_fixed` or small constant-length `array.new_default` arrays, folding pure out-of-bounds writes to `unreachable` while keeping dynamic indexes and effectful value/sibling localization open.
- research note 0835 records the twelfth `[O4Z-AUDIT-OI-K]` sub-slice: Starshine now removes pure constant-index `array.set` operations targeting direct one-use fresh `array.new(value, len)` repeated-value arrays, folding pure out-of-bounds writes to `unreachable` while keeping effectful set-value/repeated-value operands and dynamic/huge lengths as explicit localizing/trap boundaries.
- research note 0836 records a `[O4Z-AUDIT-OI-K]` boundary classification: Binaryen `optimize-instructions` (direct and O4z-style) does not fold fresh-array `array.fill` / `array.copy`, so Starshine keeping them is correct OI parity rather than a gap; a named/commented direct-core boundary test locks the classification.
- research note 1432 materially narrows `[O4Z-AUDIT-OI-K]`: fresh `.tmp/oi-k-closeout-count256-20260703` compared 256/256 with zero validation/generator/property/command failures and all 572 residual raw/canonical artifacts validating, but the row still uses the non-dedicated `pass-oi-ref-gc` profile and sampled only OI-I labels with zero residual `struct.*` / `array.*` aggregate opcodes. Implemented pure/default OI-K subsets stay covered; `array.fill`/`array.copy` is a Binaryen no-rewrite boundary; dynamic/negative/huge length and dynamic-index cases stay fail-closed; descriptor/exactness/TNH/IIT stays OI-J and shared/atomic aggregate work stays OI-L. Its broad effect-localization wording is superseded by `1434` and terminalized by `1435` to one exact residual.
- research note 1434 implements the narrowed OI-K effect-localization follow-up for effectful fresh `array.set` values/siblings and effectful `array.new` repeated values on the already-covered len/get surfaces. It adds an ordered-effect materialization helper that drops single-result effects in source order before `nop`, folded constants, or `unreachable`, keeps dynamic/negative/huge length and dynamic-index traps fail-closed, and leaves OI-J/OI-L quarantined. Validation passed for full `moon test`, native build, JSON/diff checks, direct `optimize-instructions` compare-pass 10000/10000 normalized with zero failures/mismatches, and six focused wasm probes; refreshed Binaryen `-O4 -Oz` localizes the array.set/get probes but still keeps effectful `array.len(array.new(...))`, so that Starshine len fold is documented as a bounded cleanup extension rather than Binaryen-output parity.
- research note 1435 terminalizes OI-K after `77f559f8`: the only known OI-K-owned active behavior residual is effectful repeated-value `array.set` over one-use `array.new(value, const-len)` with constant index and locally non-trapping small length. A fresh local probe shows direct Binaryen `--optimize-instructions` keeps effectful repeated-value `array.set`, len, and get probes unchanged, while Binaryen `-O4 -Oz` localizes the repeated-value `array.set` and get probes but still keeps effectful `array.len(array.new(...))`. The `array.len(array.new(effectful,const))` Starshine fold is therefore a bounded cleanup extension, not direct Binaryen-output parity; missing `pass-oi-gc-aggregate` coverage is a tooling follow-up, not a behavior blocker. OI-J descriptor/exactness/TNH/IIT and OI-L shared/atomic surfaces remain quarantined.
- research note 1436 implements the exact residual named by `1435`: Starshine now localizes effectful repeated-value `array.set(array.new(value, const_len), const_index, set_value)` for direct one-use producers with matching types and small non-negative non-trapping constant lengths. Repeated-value and set-value effects are preserved exactly once in source order before `nop`/empty in-bounds replacements or `unreachable` out-of-bounds replacements. Focused tests cover in-bounds/out-of-bounds repeated, set-value, and both-effectful positives plus dynamic/negative/huge length, dynamic index, multi-use producer, control-shaped, and multi-result fail-closed boundaries. OI-K moves out of active behavior-mismatch status; dedicated `pass-oi-gc-aggregate` coverage remains a tooling follow-up.
- research note 1437 adds the dedicated `pass-oi-gc-aggregate` GenValid trigger-smoke profile for future OI-K reopening/signoff sweeps. It samples fifteen stable labels across `struct.get(struct.new(...))`, `array.get/array.set(array.new_fixed(...))`, repeated-value `array.new` len/get/set, effectful operand localization, and dynamic/negative/huge fail-closed boundaries, replacing the older non-dedicated `pass-oi-ref-gc` OI-K sweep row.
- research note 1438 now closes the current sampled OI-L probe surface as implemented/final. Binaryen v130 direct `--optimize-instructions` rewrites unshared struct aggregate RMW/cmpxchg probes to `struct.get`/`struct.set`/`select` or identity `struct.atomic.get` forms, while sampled array RMW/cmpxchg probes stay no-rewrite. Starshine now decodes/encodes the current reserved-zero aggregate atomic binary shape, validates and HOT-lifts/lowers aggregate atomic result bodies without trailing `unreachable` collapse, rewrites local- and global-receiver unshared struct probes, and preserves sampled `array.atomic.*` probes. No `pass-oi-gc-atomics` profile exists yet; future randomized reopening/signoff should add that profile rather than reuse `pass-oi-ref-gc`.
- research note 0834 records the first `[O4Z-AUDIT-OI-M]` sub-slice; Starshine now forwards one-use `tuple.extract(tuple.make(...))` when every non-selected tuple child is side-effect-free and localizes the selected lane for the covered single-result effectful-sibling cases. Additional 2026-06-24 focused coverage locks earlier-plus-later effectful sibling ordering (`drop` earlier effect, `local.set` selected lane, `drop` later effect, reload selected value). A 2026-06-25 boundary slice locks local-carried / direct-HOT multi-use tuple extraction as keep-spelling, matching the probed Binaryen `version_130` shape; broader tuple tee/drop reconstruction remains open.
- research note 0733 records the second `[O4Z-AUDIT-OI-G]` sub-slice for constant-value size-2 and size-4 `memory.fill` lowering and nonconstant wider-fill boundary evidence.
- research note 0734 records the third `[O4Z-AUDIT-OI-G]` sub-slice for constant-value size-8 `memory.fill` lowering to repeated-byte `i64.store` while keeping nonconstant wider fills open.
- research note 0735 records the fourth `[O4Z-AUDIT-OI-G]` sub-slice for local.get value size-2 and size-4 `memory.fill` lowering through low-byte mask and repeat multiply expressions while keeping effectful values and nonconstant size-8 fills open.
- research note 0736 records the fifth `[O4Z-AUDIT-OI-G]` sub-slice for local.get value size-8 `memory.fill` lowering through low-byte mask, `i64.extend_i32_u`, and repeated-byte `i64.mul`; a 2026-06-25 boundary refresh locks direct-call and computed `i32.add` wider fill values as keep-spelling forms matching Binaryen `version_130`.
- research note 0737 records the sixth `[O4Z-AUDIT-OI-G]` sub-slice for constant-size `2`/`4`/`8` `memory.copy` lowering to exact load/store pairs while preserving overlap and trap safety; research note 0866 extends the same one-load/one-store proof to size `16` using `v128.load` / `v128.store`.
- research note 0738 records the seventh `[O4Z-AUDIT-OI-G]` boundary sub-slice for intentionally keeping zero-size and nonconstant-size `memory.copy` unchanged without trap-relaxed mode support or an exact-size proof; its old size-16 boundary is superseded by the v128 lowering slice above.
- research note 0739 records the eighth `[O4Z-AUDIT-OI-G]` sub-slice for direct-core memory64 `memory.copy` fixtures that prove i64 address preservation for size-1 and size-8 exact lowering plus oversized/nonconstant memory64 length boundaries.
- research note 0740 records the ninth `[O4Z-AUDIT-OI-G]` sub-slice: validator/typechecker memory64 `memory.fill` length acceptance plus direct-core OI fixtures proving size-1 and size-8 fill lowering preserves `i64` destination operands.
- research note 0741 records the tenth `[O4Z-AUDIT-OI-G]` sub-slice: a narrow `optimizeStoredValue` subset that drops redundant `i32.and` masks before `i32.store8` / `i32.store16` when all written low bits are preserved, plus focused value-changing-mask boundaries.
- research note 0742 records the eleventh `[O4Z-AUDIT-OI-G]` sub-slice: a Starshine-win generalization that drops redundant `i64.and` masks before `i64.store8` / `i64.store16` / `i64.store32` when all written low bits are preserved, while explicitly documenting that Binaryen `version_130` keeps those exact i64 masks.
- research note 0743 records the twelfth `[O4Z-AUDIT-OI-G]` sub-slice: the source-backed `optimizeMemoryAccess` constant-pointer static-offset fold for memory32 loads/stores, plus the Binaryen-style positive-`i32` range boundary.
- research note 0744 records the thirteenth `[O4Z-AUDIT-OI-G]` sub-slice: the memory64 extension of constant-pointer static-offset folding for scalar loads/stores, using direct-core fixtures and Binaryen's unsigned `u64` no-wrap boundary.
- research note 0745 records the fourteenth `[O4Z-AUDIT-OI-G]` sub-slice: an explicit fail-closed `load-call-optimize-instructions-noop` decision for scalar load constant-offset folding. Binaryen folds the mixed load/call fixture, but Starshine keeps the public pipeline raw gate until a broader safe escape proof exists.
- research note 0746 records the fifteenth `[O4Z-AUDIT-OI-G]` sub-slice: redundant narrow-store mask cleanup now accepts the constant mask on either side of the `and`, matching Binaryen for the `i32.store8` / `i32.store16` cases and preserving the documented Starshine-win `i64` generalization.
- research note 0747 records the sixteenth `[O4Z-AUDIT-OI-G]` sub-slice: constant stored values are truncated before `i32.store8` / `i32.store16` and `i64.store8` / `i64.store16` / `i64.store32`, matching Binaryen-observable `optimizeStoredValue` behavior for the covered constants.
- research note 0748 records the seventeenth `[O4Z-AUDIT-OI-G]` sub-slice: size-1 constant `memory.fill` lowering now canonicalizes oversized values to their low byte before materializing `i32.store8`, matching Binaryen-observable `optimizeMemoryFill` spelling for the covered constants.
- research note 0749 records the eighteenth `[O4Z-AUDIT-OI-G]` sub-slice: a source-backed boundary decision that nonconstant pointer-add address forms such as `local.get + const` are not claimed as `optimize-instructions`-owned load/store offset canonicalization under Binaryen `version_130`; Starshine keeps the tested forms unchanged.
- research note 0815 records the nineteenth `[O4Z-AUDIT-OI-G]` boundary sub-slice: Binaryen `version_130` canonicalizes sign-extension shift pairs before narrow stores to explicit sign-extension opcodes but does not drop those opcodes, so Starshine locks the same keep-spelling boundary rather than treating sign-extension-before-store removal as an OI-owned gap.
- research note 0816 records the twentieth `[O4Z-AUDIT-OI-G]` boundary sub-slice: Binaryen `version_130` lowers stack-carried effectful-call size-1/8 `memory.copy` operands to load/store forms, while Starshine keeps the same shapes behind `stack-carried-effect-optimize-instructions-noop` until a localizing/HOT lowering can prove call-result preservation without reordering.
- research note 0817 records the twenty-first `[O4Z-AUDIT-OI-G]` boundary sub-slice: Binaryen `version_130` keeps signed loads before matching narrow stores, so Starshine locks those exact signed-load-before-store spellings as non-gaps.

### 2026-07-03 OI-L final implementation note

OI-L is no longer a pure representation blocker or a global-receiver HOT blocker for the current sampled probe surface. Starshine decodes/encodes/validates current aggregate atomic get/RMW/cmpxchg binaries, including the reserved-zero aggregate immediate, HOT-lifts/lowers aggregate atomic result bodies without trailing `unreachable` collapse, and `optimize-instructions` rewrites unshared struct RMW/xchg/cmpxchg for local and Binaryen-shaped global receivers. Identity RMW and same-value cmpxchg become `struct.atomic.get` plus dropped operands; non-identity struct RMW/xchg/cmpxchg become `struct.get`/`struct.set`/`select` shapes returning the old value. Sampled array aggregate atomics remain a Binaryen-backed no-rewrite boundary and preserve `array.atomic.*`. Future OI-L reopening should be driven by a dedicated `pass-oi-gc-atomics` profile, changed proposal binaries, new Binaryen array rewrites, or true shared/concurrent runtime evidence.
