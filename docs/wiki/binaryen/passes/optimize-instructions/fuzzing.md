---
kind: workflow
status: working
last_reviewed: 2026-06-28
sources:
  - ../../../tooling/pass-fuzz-compare.md
  - ../../../../../scripts/lib/pass-fuzz-compare-task.ts
  - ./parity-matrix.json
  - ./sweep-report.md
---

# `optimize-instructions` fuzzing and parity sweep

Recommended smoke lane: run the ordinary GenValid compare-pass lane for this pass:

```sh
bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass optimize-instructions --out-dir .tmp/pass-fuzz-optimize-instructions --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe
```

## Family-based sweep

Use the OI parity matrix and sweep wrapper before adding another one-off behavior probe:

```sh
bun scripts/oi-parity-sweep.ts --priority P0
```

Then execute a bounded family with the default GenValid generator before the dedicated profile exists:

```sh
moon build --target native --release src/cmd
bun scripts/oi-parity-sweep.ts \
  --family OI-D \
  --default-gen-valid \
  --execute \
  --count 100 \
  --starshine-bin target/native/release/build/cmd/cmd.exe
```

The dedicated row profiles now exist as smoke-level GenValid profiles, so the ordinary profile-backed row no longer needs `--include-profile-stubs`:

```sh
bun scripts/oi-parity-sweep.ts \
  --family OI-D \
  --execute \
  --count 100 \
  --starshine-bin target/native/release/build/cmd/cmd.exe
```

The wrapper calls `scripts/pass-fuzz-compare.ts --pass optimize-instructions`, which compares Binaryen `wasm-opt --all-features --optimize-instructions` against Starshine `--optimize-instructions` through the existing canonicalized normalized-WAT comparison path. Executed sweeps now print a `Result summaries` section with per-row match/mismatch/failure counts, `genValidProfileCaseCounts`, and `cases.jsonl` status groups by `genValidProfileCaseLabel`; when profile labels are absent, transformed GenValid cases fall back to `transform:<id>` groups. Pass `--summarize-existing` with the same filters and `--out-dir` to re-read existing sweep output without rerunning comparisons.

## Case-label classification convention

For trigger-profile and transform rows, classify the grouped summary before opening another one-off probe. Use `genValidProfileCaseLabel` or the fallback `transform:<id>` label as the grouping key, inspect one representative `binaryen.wat` / `starshine.wat` diff per label, and record the result in the row's `classifiedCaseLabels` array in [`parity-matrix.json`](./parity-matrix.json). Each entry records the label, transform id, representative failure directory, matrix status, agent classification, observed diff, next action, and evidence. The status is an agent judgment; the harness only reports mismatch mechanics.

The first classified count-3 smoke is `.tmp/oi-parity-sweep-summary-smoke` (2026-06-27):

- OI-D labels were mixed mismatches. The narrow implementable candidate was literal double-`eqz` final folding, where Starshine left `i32.ne(1, 0)` after the double-`eqz` rewrite while Binaryen printed `i32.const 1`. A follow-up red-first pass test now covers literal `i32.const 1; i32.eqz; i32.eqz`, and the fresh `.tmp/oi-parity-sweep-oi-d-double-eqz-fix-smoke` count-3 sweep confirms that drift is gone. The remaining OI-D raw mismatches are measured Starshine-win candidates in the sampled representatives: Starshine's pure constant compare/sign-extension folds reduce canonical wasm by 5 bytes in the commuted case and by 2 bytes in each direct/local-carried case, with both Binaryen and Starshine outputs validating.
- OI-E `local-carried-mask` and `direct-sign-extension` are Starshine-win candidates: Starshine removes redundant sign extensions from local facts while Binaryen keeps the explicit opcodes in the representative output. OI-E `local-tee-add-zero` is mixed because the commuted-operands transform also introduces OI-D scalar constant-compare drift.


## Dedicated GenValid smoke profiles

The matrix records the first pass-specific profiles and `src/validate/gen_valid.mbt` accepts them. Most remain smoke-level configs: each maps to a bounded Binaryen-oracle GenValid recipe with the right broad feature floor, but does not yet guarantee an OI-specific trigger in every generated module. `pass-oi-default-scalar`, `pass-oi-local-facts`, `pass-oi-memory-bulk`, and `pass-oi-ref-gc` are now stronger than broad configs: they emit trigger smoke modules for OI-D/OI-E/OI-G/OI-I. OI-D/OI-E/OI-I still use three-way selectors; OI-G now uses a twenty-four-way selector covering direct tiny bulk, memory64 dynamic/local-carried bulk, live private-global/dead-trap boundaries, no-param call/existing-producer wrapped bulk, narrow-store masks, dynamic i32 local.tee bulk operands, multi-memory bulk operations, seed-derived nonzero in-page copy/read addresses, seed-derived non-primary-memory in-page copy/read addresses, seed-derived private-destination cross-memory random-address copy/read addresses, seed-derived memory64 private-destination cross-memory random-address copy/read addresses, seed-derived source-visible cross-memory copy/read with destination-byte restore, seed-derived memory64 source-visible cross-memory copy/read with destination-byte restore, a Node-runtime-compatible single-memory restore/copy/read exported function with active data, a source-visible shared-memory wait/notify/load exported function, a Node-runtime-compatible single-memory fill/restore/read exported function with active data, a memory64 fill/restore/read function with active data, a multi-memory fill/restore/read function with active destination data, a memory64+multi-memory fill/restore/read function with active destination data on memory 1, a Node-runtime-compatible call-produced single-memory fill/restore/read exported function, a Node-runtime-compatible call-produced single-memory copy/restore/read exported function, a Node-runtime-compatible local.tee-produced single-memory copy/restore/read exported function, a Node-runtime-compatible select-produced single-memory copy/restore/read exported function, and a Node-runtime-compatible select-produced single-memory fill/restore/read exported function. The GenValid manifest records each selected OI trigger as `profile_case_label`, and compare-pass copies that into `result.json` `genValidProfileCaseCounts`, `summary.json` strategy counters, and `cases.jsonl` `genValidProfileCaseLabel` fields for mechanical family triage. They still need broader randomized trigger-biased constructors and broader existing-producer/live wrappers before family signoff. `pass-oi-ref-gc` is currently a non-descriptor OI-I trigger smoke because the prior broad descriptor-bearing profile caused wasm-tools baseline validation failures in compare-pass; OI-J descriptor/exactness/TNH/IIT needs a separate compatible profile or oracle path.

| Profile | Family | Intended surface |
| --- | --- | --- |
| `pass-oi-default-scalar` | OI-D | scalar arithmetic, compares, `eqz`, shifts, float/conversion, and direct/local-carried/local.tee spellings |
| `pass-oi-local-facts` | OI-E | `LocalScanner`-style maxBits, signExtBits, unsigned max, known nonnegative/nullness, local.tee carriers, and single-use facts |
| `pass-oi-boolean-select` | OI-F | boolean contexts, `if`/`select`, ternary shells, identical arms, effect/trap/no-reorder boundaries |
| `pass-oi-memory-bulk` | OI-G | memory32/memory64 load/store offsets, stored values, tiny copy/fill, zero-size trap-mode boundaries, live nonzero byte-0/page-middle/end-byte/cross-memory copy/read, byte-0/mid-page/end-byte fill-read wrappers, guarded atomic load, non-primary/private-memory varied atomic-load, non-primary/private-memory no-op integer atomic store/RMW/cmpxchg wrapper with module-shape-derived in-page byte-address probes plus byte-preserving xchg restore smoke, private shared-memory nonblocking wait/notify smoke, no-param call/existing-producer wrappers, dynamic i32 bulk operands, narrow-store masks, multi-memory bulk operations, seed-derived nonzero in-page copy/read addresses, seed-derived non-primary-memory in-page copy/read addresses, seed-derived private-destination cross-memory random-address copy/read, seed-derived memory64 private-destination cross-memory random-address copy/read, source-visible destination-restore cross-memory copy/read, memory64 source-visible destination-restore cross-memory copy/read, Node-runtime-compatible single-memory restore/copy/read exported-function evidence, source-visible shared-memory wait/notify/load source evidence, Node-runtime-compatible single-memory fill/restore/read exported-function evidence, memory64 fill/restore/read source evidence, multi-memory fill/restore/read source evidence, memory64+multi-memory fill/restore/read source evidence, Node-runtime-compatible call-produced fill/restore/read exported-function evidence, Node-runtime-compatible call-produced copy/restore/read exported-function evidence, Node-runtime-compatible local.tee-produced copy/restore/read exported-function evidence, Node-runtime-compatible select-produced copy/restore/read exported-function evidence, and private-memory commuted address/store transform evidence |
| `pass-oi-call-ref` | OI-H | `call_ref` / `return_call_ref` target directization and argument localization boundaries |
| `pass-oi-ref-gc` | OI-I, blocked seed for OI-J+ | non-descriptor reference basics: direct null/test/cast/equality, local-carried null/test/cast, and branch-cast boundaries; descriptor/exactness/TNH/IIT, GC constructors/fields/arrays/defaults, and GC atomics still need split profiles |
| `pass-oi-tuple` | OI-M | tuple extraction, selected lanes, multivalue, tuple-scratch, and effectful/trapping sibling boundaries |

First generator-quality priority remains scaling the new seed-indexed `pass-oi-default-scalar` / `pass-oi-local-facts` / `pass-oi-memory-bulk` / `pass-oi-ref-gc` trigger smokes into randomized family constructors, then replacing the remaining smoke configs such as `pass-oi-tuple` with trigger-biased constructors. OI-G now has first no-param call/existing-producer, dynamic i32, store-mask, multi-memory, seed-derived random-address copy/read, seed-derived non-primary-memory random-address copy/read, seed-derived private-destination cross-memory random-address copy/read, seed-derived memory64 private-destination cross-memory random-address copy/read, seed-derived source-visible destination-restore cross-memory copy/read, seed-derived memory64 source-visible destination-restore cross-memory copy/read, Node-runtime-compatible single-memory restore/copy/read, source-visible shared-memory wait/notify/load trigger labels, Node-runtime-compatible single-memory fill/restore/read trigger labels, memory64 fill/restore/read trigger labels, multi-memory fill/restore/read trigger labels, memory64+multi-memory fill/restore/read trigger labels, Node-runtime-compatible call-produced fill/restore/read trigger labels, Node-runtime-compatible call-produced copy/restore/read trigger labels, Node-runtime-compatible local.tee-produced copy/restore/read trigger labels, Node-runtime-compatible select-produced copy/restore/read trigger labels, and private-memory commuted address/store transform evidence; the size-losing store-mask + dead memory-size-boundary grouped residual has a red-first raw partial fix; the grouped count>=11 memory64 + memory-size-boundary command failure is fixed by memory64 transform support; the count-18 live-effect + commuted-operand size loss is fixed by a red-first raw ordered const eq/ne fold; the multi-memory generator-breadth slice breaks the old 45-case count-48/count-54 saturation; a red-first raw signed div/rem-by-zero spelling fix reduced the count-63 baseline to 24 mismatches with no canonical size-losing failures; a red-first guarded atomic-load transform produced count-120 evidence with no validation failures or canonical size-losing dirs; red-first guarded no-op atomic store/RMW/cmpxchg transform work produced count-130 evidence with both atomic transforms matching 10/10 sampled cases and no canonical size-losing dirs; red-first module-derived-address atomic RMW produced count350 evidence with the RMW transform still 10/10 matched and no Starshine-larger failure dirs; the red-first byte-preserving xchg restore slice kept that transform 10/10 matched with no Starshine-larger failure dirs; and the red-first private shared-memory nonblocking wait/notify slice sampled the new transform 9/9 matches with no Starshine-larger failure dirs. The `.tmp/oi-g-live-dynamic-copy-boundary-fix2-count150-20260628` refresh added `oi-live-nonzero-memory-dynamic-copy-boundary`, a guarded dynamic interior-address `memory.copy`/read transform using `memory.size * 32768`. The first count-150 run exposed a real raw OI parity gap: Binaryen lowered the length-1 self-copy and printed the address as `memory.size << 15`, while Starshine first kept `memory.copy` and then kept the multiply spelling. Red-first pass coverage now fixes both in the raw fallback, and the fix2 run compared 150/150 with 129 normalized matches, 21 mismatches, zero validation/generator/property/command failures, zero canonical size-losing failure dirs, and the dynamic-copy transform 10/10 matched. The `.tmp/oi-g-live-varied-copy-boundary-count170-20260629` refresh adds guarded fixed varied-address copy/read transform evidence; both tools lower every sampled varied self-copy to load8/store8 forms, and its residuals are the known store-mask low-byte-mask candidate.

The `.tmp/oi-g-multimemory-random-address-count198-20260629` refresh adds a ninth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:multi-memory-random-address-copy-read`, generated from a seed-derived nonzero in-page address on non-primary memory index 1. The red-first focused generator test failed because seed `0x5ef5` still selected the old memory64 case, then passed after `src/validate/gen_valid.mbt` added the non-primary-memory random-address copy/read body and label while preserving the seed `0x5eed` through `0x5ef4` case order. The grouped count-198 run compares 198/198 with 164 normalized matches, 34 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 162/36, zero canonical size-losing failure dirs, and two raw-size-larger Starshine dirs. The new multi-memory random-address profile case samples 25 cases with 24 matches; its one residual is the known commuted dropped-compare candidate. Representative WAT inspection for case 000056 shows both tools lower the seed-derived address 44129 non-primary self-copy to load8/store8 forms (`memory.copy` 0/0, `i32.load8_u` 2/2, `i32.store8` 1/1), while the diff is the unrelated dropped reverse `i32.eq(42, 7)` that Starshine folds away.

The `.tmp/oi-g-cross-copy-varied-count216-20260629` refresh broadened `oi-live-nonzero-memory-cross-copy-boundary` under red-first coverage from the original private-destination byte-17 copy/read smoke to fixed in-page private-destination bytes 17, 1024, 49152, and 65535. The focused metamorphic test first failed because the transform still emitted only byte 17, then passed after `src/fuzz/metamorphic.mbt` emitted the four fixed cross-memory copy/load pairs. The grouped count-216 run compared 216/216 with 181 normalized matches, 35 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 209/7, zero canonical size-losing failure dirs, and five raw-size-larger Starshine dirs. The broadened cross-copy transform sampled 12 cases with 11 matches; its one residual was the known store-mask low-byte-mask candidate. Representative case 000211 showed both tools lower all four private-destination cross-memory copies to load8/store8 forms (`memory.copy` 0/0, `i32.load8_u` 8/8, `i32.store8` 5/5), while the diff was the unrelated `i64.store8` mask where Binaryen keeps `i64.and 255` and Starshine removes it.

The `.tmp/oi-g-cross-memory-random-address-count240-20260629` refresh added the tenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:cross-memory-random-address-copy-read`, generated from a seed-derived nonzero in-page address copied from source memory 0 into private destination memory 1 and loaded from the destination. The red-first focused generator test failed at seed `0x5ef6` before the case label/body existed, then passed after `src/validate/gen_valid.mbt` added the case while preserving the prior `0x5eed` through `0x5ef5` sequence. A red-first focused metamorphic test also failed because `oi-live-nonzero-memory-cross-copy-boundary` required an i32 source memory, then passed after the transform added memory64-compatible source plus private memory64 destination support. The grouped count-240 run compared 240/240 with 197 normalized matches, 43 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 180/60, zero canonical size-losing failure dirs, and zero raw-size-larger Starshine dirs. The new cross-memory random-address profile case sampled 21 cases and all 21 matched; the cross-copy transform sampled 13 cases and all 13 matched.

The `.tmp/oi-g-memory64-cross-memory-random-address-count264-20260629` refresh added the eleventh `pass-oi-memory-bulk` profile case, `oi-memory-bulk:memory64-cross-memory-random-address-copy-read`, generated from a seed-derived nonzero in-page i64 address copied from source memory64 index 0 into private destination memory64 index 1 and loaded from the destination. The red-first focused generator test failed at seed `0x5ef7` before the case label/body existed, then passed after `src/validate/gen_valid.mbt` added the case while preserving the prior `0x5eed` through `0x5ef6` sequence. Direct count-50 samples the new label once and it matches; grouped count-264 compares 264/264 with 235 normalized matches, 29 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 174/90, zero canonical size-losing failure dirs, and nine raw-size-larger Starshine dirs. The new memory64 cross-memory random-address profile case samples 22 cases and all 22 match. Remaining mismatches are store-mask low-byte-mask or commuted/synthetic cleanup candidates, not memory64 cross-memory random-address drift. The prior count216b root remains useful as first byte-17 evidence, count216 varied remains useful for fixed-address breadth, and count240 remains useful for memory32 random cross-memory evidence; count264 remains the last all-264 comparable pre-restore grouped aggregate. The prior varied fixed-address, non-primary byte-0, dynamic end-byte, dynamic interior-address, fill/read, random-address single-memory, and atomic RMW transforms remain smoke evidence from their saved roots. OI-G still needs broader wait/notify beyond private nonblocking smoke, randomized or multi-memory atomic addresses beyond fixed/dynamic/module-derived smoke, true live memory/trap ordering, broader store/address commutation, broader source-visible cross-memory mutation/restore shapes, randomized memory64/multi-memory wrappers beyond these seed-indexed profiles, grouped mismatch reduction, and raw-size residual policy before family signoff. OI-J should not reuse the current `pass-oi-ref-gc` smoke as descriptor evidence and needs a descriptor/exactness-specific lane once the compare-pass oracle can accept those inputs.

The `.tmp/oi-g-cross-memory-restore-count288-20260629` refresh adds the twelfth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:cross-memory-restore-copy-read`, generated from a seed-derived nonzero in-page i32 address on two source-visible memories. The body saves the destination byte from memory 1 into a local, copies one byte from source memory 0 to destination memory 1, loads/drops the destination byte, and restores the saved destination byte with `i32.store8`. The red-first focused generator test failed at seed `0x5ef8` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added the case while preserving the prior `0x5eed` through `0x5ef7` sequence. Direct count-60 samples the new label 5 times and all 5 match; grouped count-288 compares 149/288 transform-applicable cases with 128 normalized matches, 21 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 99/50, zero canonical size-losing failure dirs, and zero raw-size-larger Starshine dirs. The new cross-memory restore profile case samples 15 grouped cases and all 15 match. Manual validation over the 21 grouped failure dirs validates all 84 Binaryen/Starshine raw/canonical outputs. Remaining grouped mismatches are store-mask low-byte-mask or commuted/synthetic cleanup candidates, not source-visible cross-memory restore drift. This is sampled destination-restore evidence only, not broad source-visible mutation closure, runtime semantic execution evidence, memory64 restore evidence, or OI-J descriptor evidence.

The latest post-memory64-restore `.tmp/oi-g-memory64-cross-memory-restore-count312-20260629` refresh adds the thirteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:memory64-cross-memory-restore-copy-read`, generated from a seed-derived nonzero in-page i64 address on two source-visible memory64 memories. The body saves the destination byte from memory 1 into an i64 local, copies one byte from source memory 0 to destination memory 1, loads/drops the destination byte, and restores the saved destination byte with `i64.store8`. The red-first focused generator test failed at seed `0x5ef9` before the label/body existed because the seed still selected `memory64-dynamic-bulk`, then passed after `src/validate/gen_valid.mbt` added the case while preserving the prior `0x5eed` through `0x5ef8` sequence. Direct count-65 samples the new label 3 times and all 3 match; grouped count-312 compares 201/312 transform-applicable cases with 179 normalized matches, 22 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 133/68, zero canonical size-losing failure dirs, and seven raw-size-larger Starshine dirs. The new memory64 cross-memory restore profile case samples 11 grouped cases and all 11 match. Manual validation over the 22 grouped failure dirs validates all 88 Binaryen/Starshine raw/canonical outputs. Remaining grouped mismatches are store-mask low-byte-mask or commuted/synthetic cleanup candidates, not memory64 source-visible cross-memory restore drift. This is sampled memory64 destination-restore evidence only, not broad source-visible mutation closure, runtime semantic execution evidence, OI-G closure, or OI-J descriptor evidence.

A follow-up `--runtime-execution node` probe shows runtime execution remains blocked rather than closed for the exact source-visible restore shapes. Direct runtime count-65 at `.tmp/oi-g-memory64-cross-memory-restore-runtime-count65-20260629` compared 65/65 with 60 normalized matches, the same five store-mask mismatches, zero validation/generator/property/command failures, and zero runtime semantic failures, but the runtime counters were `checked=36`, `unsupported=29`, `failed=0` and the runtime matrix outcome was blocked by unsupported runtime reports. Singleton restore probes make the boundary explicit: seed `0x5ef8` selected `oi-memory-bulk:cross-memory-restore-copy-read` and seed `0x5ef9` selected `oi-memory-bulk:memory64-cross-memory-restore-copy-read`; both normalized 1/1 with zero command failures, but each reported `runtimeExecutionCounts checked=0 unsupported=1 failed=0`. Do not treat these as runtime semantic execution evidence until the exact cross-memory restore modules are executable under a compatible Node/export adapter.

The `.tmp/oi-g-runtime-restore-count14-20260629` refresh adds a fourteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-restore-copy-read`, specifically to make a restore contract executable under the current Node runtime path without multi-memory or memory64. The case defines one i32 memory with active source byte `0x07` and destination byte `0x0d`, exports a no-arg `run` function, saves the destination byte, performs a length-1 same-memory copy from source to destination, reads the copied byte into a local, restores the saved destination byte, and returns the copied byte. The focused generator test failed red-first at seed `0x5efa` before the label/signature/export existed, then passed after `src/validate/gen_valid.mbt` added the body. Direct runtime count14 compared 14/14 with 13 normalized matches, one known store-mask mismatch, zero validation/generator/property/command failures, and runtime matrix `equalResults=1`, `unsupportedRuntimes=8`, `semanticMismatches=0`; the aggregate runtime outcome remains blocked by unrelated unsupported memory64/multi-memory cases. Grouped count336 at `.tmp/oi-g-runtime-restore-count336-20260629` compared 177/336 transform-applicable cases with 155 normalized matches, 22 mismatches, zero validation/generator/property/command failures, and `runtime-restore-copy-read` 15/15 matches. Manual validation/size review over the 22 grouped failure dirs found zero validation failures and zero Starshine-larger raw or normalized-WAT failure dirs. This is runtime-compatible restore-contract evidence only, not exact cross-memory/memory64 runtime closure, OI-G family closure, or OI-J descriptor evidence.

The `.tmp/oi-g-runtime-fill-restore-count425-20260629` refresh adds a sixteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-fill-restore-read`, specifically to make a single-memory `memory.fill` restore contract executable under the current Node runtime path without multi-memory or memory64. The case defines one i32 memory with active destination data, exports a no-arg `run` function, saves the destination byte, performs a length-1 `memory.fill`, reads the filled byte into a local, restores the saved destination byte, and returns the filled byte. The focused generator test failed red-first at seed `0x5efc` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added the body. Direct runtime count16 compared 16/16 with 15 normalized matches, one known store-mask mismatch, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=9`, `unsupported=7`, `failed=0`, and the new label sampled 1/1 match. Grouped count425 compared 425/425 with 377 normalized matches, 48 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 222/203, and `runtime-fill-restore-read` 24/26 matches. The two profile-case residuals are both `oi-commuted-operands` dropped pure reverse `i32.eq(42, 7)` cleanup, not `memory.fill`/load/store drift. Manual validation/size review over all 48 grouped failure dirs validated 192 Binaryen/Starshine raw/normalized wasm artifacts and found zero validation failures, zero raw-size-larger Starshine dirs, and zero normalized-WAT-larger Starshine dirs. This is runtime-compatible single-memory fill/restore/read evidence only, not multi-memory/memory64 fill runtime closure, broad source-visible mutation closure, true live trap-order closure, OI-G family closure, or OI-J descriptor evidence.

The `.tmp/oi-g-memory64-fill-restore-count459-20260629` refresh adds a seventeenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:memory64-fill-restore-read`, as memory64 fill/restore/read source evidence. The case defines one memory64 memory with active destination data and an i64-result function that saves the destination byte, performs length-1 `memory.fill` with i64 address/size operands, reads the filled byte, restores the saved byte with `i64.store8`, and returns the filled value. The focused generator test failed red-first at seed `0x5efd` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added the body. Direct count17 compared 17/17 with 17 normalized matches and zero validation/generator/property/command failures. Grouped count459 compared 459/459 with 411 normalized matches, 48 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 244/215, and `memory64-fill-restore-read` 27/28 matches. The one profile-case residual is an `oi-commuted-operands` dropped pure reverse `i32.eq(42, 7)` cleanup, not memory64 `memory.fill`/load/store/restore drift; representative case 000194 preserves the memory64 active data and restore/read contract on both tools. Manual validation/size review over all 48 grouped failure dirs validated 192 Binaryen/Starshine raw/normalized wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. This is sampled memory64 fill/restore/read evidence only, not multi-memory fill runtime closure, broad source-visible mutation closure, true live trap-order closure, OI-G family closure, or OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-multimemory-fill-restore-count486-20260629` refresh adds an eighteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:multi-memory-fill-restore-read`, as multi-memory fill/restore/read source evidence. The case defines two i32 memories with active destination data on memory 1 and an i32-result function that saves a memory-1 destination byte, performs length-1 `memory.fill` on memory 1, reads the filled byte from memory 1, restores the saved byte to memory 1, and returns the filled value. The focused generator test failed red-first at seed `0x5efe` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added the body. Direct count18 compared 18/18 with 18 normalized matches and zero validation/generator/property/command failures. Grouped count486 compared 486/486 with 422 normalized matches, 64 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 303/183, and `multi-memory-fill-restore-read` 19/20 matches. The one profile-case residual is an `oi-commuted-operands` dropped pure reverse `i32.eq(42, 7)` cleanup, not memory-1 `memory.fill`/load/store/restore drift; representative case 000362 preserves active data and the restore/read contract on memory 1 on both tools. Manual validation/size review over all 64 grouped failure dirs validated 256 Binaryen/Starshine raw/normalized wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. This is sampled multi-memory fill/restore/read evidence only, not multi-memory runtime closure, broad source-visible mutation closure, true live trap-order closure, OI-G family closure, or OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-memory64-multimemory-fill-restore-count513-20260629` refresh adds a nineteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:memory64-multi-memory-fill-restore-read`, as combined memory64+multi-memory fill/restore/read source evidence. The case defines two memory64 memories with active destination data on memory 1 and an i64-result function that saves a memory-1 destination byte, performs length-1 `memory.fill` on memory 1 with i64 address/size operands, reads the filled byte from memory 1, restores the saved byte to memory 1 with `i64.store8`, and returns the filled value. The focused generator test failed red-first at seed `0x5eff` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added the body. Direct count100 `.tmp/oi-g-memory64-multimemory-fill-restore-profile-count100-20260629` compared 100/100 with 95 normalized matches, five known store-mask residuals, zero validation/generator/property/command failures, and `memory64-multi-memory-fill-restore-read` 6/6 matches. Grouped count513 compared 513/513 with 456 normalized matches, 57 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 269/244, and the new label 23/24 matches. The one profile-case residual is an `oi-commuted-operands` dropped pure reverse `i32.eq(42, 7)` cleanup, not memory64 memory-1 `memory.fill`/load/store/restore drift; representative case 000362 preserves the two memory64 memories, active data on memory 1, and the fill/restore/read contract on both tools. Manual validation/size review over all 57 grouped failure dirs validated 228 Binaryen/Starshine raw/normalized wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. This is sampled memory64+multi-memory fill/restore/read source evidence only, not runtime closure, broad source-visible mutation closure, true live trap-order closure, OI-G family closure, or OI-J descriptor/exactness/TNH/IIT evidence.

A classification-only refresh re-read the same count513 root with `--summarize-existing` and inspected all 57 residual diffs before choosing another implementation slice. The residuals split into two sampled Starshine-win candidate buckets, both with zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. Thirty-three are store8 low-byte-mask residuals: Starshine removes Binaryen-retained `i32.and`/`i64.and` masks before `store8`, e.g. case 000001 changes `i64.store8(value=(i64.and 4660 255))` to `i64.store8(value=4660)` at raw/canonical 230/234 bytes, and private-memory commuted case 000279 changes `i32.store8 $1(value=(i32.and 255 63))` to `i32.store8 $1(value=63)` at 123/127 bytes. Their aggregate Starshine delta is raw/canonical -133 bytes and normalized-WAT -1254 bytes. The other twenty-four are `oi-commuted-operands` residuals where Starshine folds a dropped pure reverse `i32.eq(42, 7)` to `i32.const 0` while Binaryen keeps the compare; representative case 000005 is raw/canonical 90/93, and memory64+multi-memory fill/restore case 000362 is raw 113/119 and canonical 116/119 while preserving the memory-1 fill/read/restore contract on both tools. Their aggregate Starshine delta is raw -99 bytes, canonical -78 bytes, and normalized-WAT -936 bytes. These classifications are agent judgments with reopening criteria, not OI-G closure, and they do not provide OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-runtime-call-fill-restore-count540-20260629` refresh adds a twentieth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-call-fill-restore-read`, as Node-runtime-compatible single-memory fill/restore/read evidence where existing no-arg helper calls produce the destination address, fill byte, and fill size. The focused generator test failed red-first at seed `0x5f00` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added an exported `run` function that saves a destination byte, calls helpers before `memory.fill`, reads the filled byte, restores the saved byte, and returns the filled value. Direct runtime count100 at `.tmp/oi-g-runtime-call-fill-restore-profile-count100-20260629` compared 100/100 with 94 normalized matches, six known residuals, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=49`, `unsupported=51`, `failed=0`, and the new label 1/1 matched. Grouped count540 compared 540/540 with 486 normalized matches, 54 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 315/225, and `runtime-call-fill-restore-read` 20/21 matches. Its one residual is a store8 low-byte-mask candidate under `oi-memory-commuted-operands`, not call-produced `memory.fill`/load/store/restore drift; representative case 000426 preserves one `memory.fill`, 18 `i32.load8_u`, 13 `i32.store8`, and six helper calls on both tools while Starshine removes one retained `i32.and` before `i32.store8` (raw 315/322, canonical 318/322). Manual validation/size review over all 54 grouped failure dirs validated 216 Binaryen/Starshine raw/canonical wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. The count540 residuals split into 30 store8 low-byte-mask candidates (aggregate Starshine raw -127, canonical -124, normalized-WAT -1176 bytes) and 24 dropped pure reverse `i32.eq(42, 7)` candidates (raw -96, canonical -75, normalized-WAT -900 bytes). This is sampled call-produced single-memory runtime evidence only, not broad source-visible mutation closure, exact multi-memory/memory64 runtime closure, true live trap-order closure, OI-G family closure, or OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-runtime-call-copy-restore-count567-20260629` refresh adds a twenty-first `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-call-restore-copy-read`, as Node-runtime-compatible single-memory copy/restore/read evidence where existing no-arg helper calls produce the destination address, source address, and copy size. The focused generator test failed red-first at seed `0x5f01` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added an exported `run` function that saves a destination byte, calls helpers before length-1 `memory.copy`, reads the copied byte, restores the saved byte, and returns the copied value. Direct runtime count105 at `.tmp/oi-g-runtime-call-copy-restore-profile-count105-20260629` compared 105/105 with 95 normalized matches, ten known residuals, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=68`, `unsupported=37`, `failed=0`, and the new label 8/8 matched. Grouped count567 compared 567/567 with 505 normalized matches, 62 mismatches, zero validation/generator/property/command failures, Binaryen cache hits/misses 290/277, and `runtime-call-restore-copy-read` 28/30 matches. Its two residuals are known synthetic cleanup candidates, not call-produced `memory.copy`/load/store/restore drift: case 000131 preserves one `memory.copy`, two `i32.load8_u`, one `i32.store8`, and six helper calls on both tools while Starshine folds a dropped pure reverse `i32.eq(42, 7)` (raw 235/247, canonical 238/247), and case 000510 preserves one `memory.copy`, 18 `i32.load8_u`, 13 `i32.store8`, and six helper calls on both tools while Starshine removes one retained `i32.and` before `i32.store8` (raw 326/333, canonical 329/333). Manual validation/size review over all 62 grouped failure dirs validated 248 Binaryen/Starshine raw/canonical wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. The count567 residuals split into 36 store8 low-byte-mask candidates (aggregate Starshine raw -148, canonical -145, normalized-WAT -1368 bytes) and 26 dropped pure reverse `i32.eq(42, 7)` candidates (raw -126, canonical -99, normalized-WAT -1188 bytes). This is sampled call-produced single-memory copy/restore runtime evidence only, not broad source-visible mutation closure, exact multi-memory/memory64 runtime closure, true live trap-order closure, OI-G family closure, or OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-runtime-localtee-copy-restore-count594-20260629` refresh adds a twenty-second `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-localtee-restore-copy-read`, as Node-runtime-compatible single-memory copy/restore/read evidence where `local.tee` existing-producer wrappers provide the destination address, source address, and copy size. The focused generator test failed red-first at seed `0x5f02` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added an exported `run` function that saves a destination byte, uses local.tee-produced operands for length-1 `memory.copy`, reads the copied byte, restores the saved destination byte, and returns the copied value. Direct runtime count110 compared 110/110 with 103 normalized matches, seven known residuals, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=69`, `unsupported=41`, `failed=0`, and the new label 4/4 matches. Grouped count594 compared 594/594 with 537 normalized matches, 57 mismatches, zero failures, Binaryen cache hits/misses 309/285, and `runtime-localtee-restore-copy-read` 32/34 matches. Its residuals preserve the memory.copy/load/store/restore contract: case 000090 removes Binaryen-retained local.tee wrappers around constant source/size producers (raw 154/161, canonical 157/161), while case 000236 also folds a dropped pure reverse `i32.eq(42, 7)` (raw 129/139, canonical 132/139). Manual validation/size review over all 57 grouped failure dirs validated 228 Binaryen/Starshine raw/canonical wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. The latest residual aggregate is 33 store8 low-byte-mask candidates (raw -148, canonical -136, normalized-WAT -1290 for Starshine), 23 dropped pure reverse compare candidates (raw -121, canonical -97, normalized-WAT -1162), and one pure local.tee producer-cleanup candidate (raw -7, canonical -4, normalized-WAT -46). This broadens local.tee-produced single-memory copy/restore runtime evidence only; OI-G remains open and OI-J descriptor/exactness/TNH/IIT remains blocked.

The `.tmp/oi-g-runtime-select-fill-restore-fix-count648-20260629` refresh adds a twenty-fourth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-select-fill-restore-read`, as Node-runtime-compatible single-memory fill/restore/read evidence where pure `select` wrappers produce the destination address, fill byte, fill size, and returned filled value. The focused generator test failed red-first at seed `0x5f04` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added an exported `run` function that saves the destination byte, performs length-1 `memory.fill`, reads the filled byte, restores the destination byte, and returns the filled value through a constant-condition select. The first grouped count648 run exposed a real size-losing residual: Starshine kept select-produced size-1 `memory.fill` while Binaryen lowered it to `i32.store8`. Red-first `src/passes/optimize_instructions_test.mbt::optimize-instructions raw skip lowers select-wrapped byte memory fill` failed on that raw-skipped shape, then passed after `src/passes/pass_manager.mbt` lowered select-wrapped size-1 memory.fill. Direct runtime fix count120 `.tmp/oi-g-runtime-select-fill-restore-profile-fix-count120-20260629` compared 120/120 with 113 normalized matches, seven known store-mask residuals, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=77`, `unsupported=43`, `failed=0`, and the new label 4/4 matches. Grouped fix count648 compared 648/648 with 590 normalized matches, 58 mismatches, zero failures, Binaryen cache hits/misses 648/0, and `runtime-select-fill-restore-read` 23/24 matches. Manual validation/size review over all 58 grouped failure dirs validated 232 Binaryen/Starshine raw/canonical wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. Residuals split into 31 store8 low-byte-mask candidates (Starshine raw -134, canonical -128, normalized-WAT -1214 bytes) and 27 dropped pure reverse `i32.eq(42, 7)` candidates (raw -142, canonical -109, normalized-WAT -1306). This broadens select-produced fill/restore runtime evidence and reduces a size-losing grouped mismatch; it is not OI-G family closure and it is not OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-runtime-select-copy-restore-fix-count621-20260629` refresh adds a twenty-third `pass-oi-memory-bulk` profile case, `oi-memory-bulk:runtime-select-restore-copy-read`, as Node-runtime-compatible single-memory copy/restore/read evidence where pure `select` wrappers produce the destination address, source address, copy size, and returned copied value. The focused generator test failed red-first at seed `0x5f03` before the label/body existed, then passed after `src/validate/gen_valid.mbt` added an exported `run` function that saves the destination byte, performs length-1 `memory.copy`, reads the copied byte, restores the destination byte, and returns the copied value through a constant-condition select. The first grouped count621 run exposed a real size-losing residual: Starshine kept select-produced size-1 `memory.copy` while Binaryen lowered it to `i32.load8_u`/`i32.store8`. Red-first `src/passes/optimize_instructions_test.mbt::optimize-instructions raw skip lowers select-wrapped tiny memory copy` failed on that raw-skipped shape, then passed after `src/passes/pass_manager.mbt` folded pure constant-condition selects and lowered select-wrapped size-1 memory.copy. Direct runtime fix count115 `.tmp/oi-g-runtime-select-copy-restore-profile-fix-count115-20260629` compared 115/115 with 108 normalized matches, seven known store-mask residuals, zero validation/generator/property/command/runtime semantic failures, runtime counters `checked=67`, `unsupported=48`, `failed=0`, and the new label 7/7 matches. Grouped fix count621 compared 621/621 with 561 normalized matches, 60 mismatches, zero failures, Binaryen cache hits/misses 621/0, and `runtime-select-restore-copy-read` 24/26 matches. Manual validation/size review over all 60 grouped failure dirs validated 240 Binaryen/Starshine raw/canonical wasm artifacts and found zero validation failures and zero Starshine-larger raw, canonical-wasm, or normalized-WAT dirs. Residuals split into 32 store8 low-byte-mask candidates (Starshine raw -147, canonical -135, normalized-WAT -1288 bytes), 26 dropped pure reverse `i32.eq(42, 7)` candidates (raw -143, canonical -104, normalized-WAT -1244), and two local.tee producer-cleanup candidates (raw -14, canonical -8, normalized-WAT -92). This broadens select-produced copy/restore runtime evidence and reduces a size-losing grouped mismatch; it is not OI-G family closure and it is not OI-J descriptor/exactness/TNH/IIT evidence.

The `.tmp/oi-g-second-atomic-count350-20260629` refresh adds `oi-live-nonzero-memory-second-atomic-boundary`, a non-primary/private-memory atomic-load transform. The red-first metamorphic test failed while the transform only loaded byte 0, then passed after the transform used an existing non-primary memory or appended a private nonzero memory and emitted guarded `i32.atomic.load8_u` probes at bytes 17, 1024, 49152, and 65535. The grouped count350 run compared 192/350 transform-applicable cases with 172 normalized matches, 20 mismatches, zero validation/generator/property/command failures, and the new second-atomic transform 10/10 matched across memory32 and memory64 profile labels. Manual validation/size review over all 20 failure dirs found zero validation failures, zero normalized-WAT-larger Starshine dirs, and two unrelated raw-size-larger dirs under `oi-memory-size-boundary`/`oi-commuted-operands`. This is sampled non-primary/private-memory atomic-load evidence only, not atomic RMW/cmpxchg on randomized addresses, xchg, wait/notify, true live trap/effect ordering, OI-G closure, exact cross-memory runtime closure, or OI-J descriptor evidence.

The `.tmp/oi-g-default-memarg-canonical-count350-20260629` refresh reduces those two raw-size-larger grouped residuals with red-first tests instead of classifying them away. `src/binary/tests.mbt` first failed because `MemArg(Some(0))` encoded as explicit `0x40 0x00 0x00`; `src/passes/optimize_instructions_test.mbt` first failed because raw partial tiny-copy lowering emitted explicit memory-index-zero load/store memargs. After `src/binary/encode.mbt` canonicalized default-memory memargs and `src/passes/pass_manager.mbt` stopped reintroducing explicit memidx 0 in the raw partial helper, the grouped count350 run compared 193/350 transform-applicable cases with 173 normalized matches, 20 mismatches, and zero validation/generator/property/command failures. Manual validation/size review over all 20 failure dirs validated 80 raw/canonical Binaryen/Starshine outputs and found zero raw-size-larger and zero normalized-WAT-larger Starshine dirs; the prior case 000058 is now Starshine/Binaryen raw 230/234, and case 000100 is now 247/250. Remaining mismatches are store-mask low-byte-mask or commuted/synthetic cleanup candidates, not default-memory memarg encoding drift, and OI-G/OI-J remain open under the same blockers above.

The `.tmp/oi-g-nonprimary-atomic-rmw-count350-20260629` refresh broadens the no-op atomic store/RMW/cmpxchg transform from primary-memory-only smoke to non-primary/private-memory smoke. The focused `metamorphic_wbtest.mbt` test failed red-first while the transform guarded memory 0 and then passed after `src/fuzz/metamorphic.mbt` selected an existing non-primary memory or appended a private nonzero memory. The grouped count350 run compared 194/350 transform-applicable cases with 174 normalized matches, 20 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 187/7; `oi-live-nonzero-memory-atomic-rmw-boundary` sampled 10/10 matches. Manual validation/size review over all 20 failure dirs validated 80 raw/canonical Binaryen/Starshine outputs and found zero validation failures, zero raw-size-larger Starshine dirs, zero canonical-WASM-larger dirs, and zero normalized-WAT-larger dirs. Remaining mismatches are still store-mask low-byte-mask or commuted/synthetic cleanup candidates, not non-primary atomic RMW/cmpxchg drift. This does not close xchg, wait/notify, randomized atomic addresses, true shared-memory ordering, true live trap/effect ordering, OI-G, or OI-J descriptor/exactness/TNH/IIT.

The `.tmp/oi-g-randomized-atomic-rmw-count350-20260629` refresh broadens that same no-op atomic store/RMW/cmpxchg transform beyond fixed byte 0/16/32768 and dynamic end-byte probes by adding two module-shape-derived in-page byte addresses to the guarded byte-width atomic sequence. The focused `metamorphic_wbtest.mbt` coverage failed red-first because those module-derived addresses were absent, then passed after `src/fuzz/metamorphic.mbt` derived stable in-page addresses from defined-function and memory counts for memory32 and memory64 forms. The grouped count350 run compared 198/350 transform-applicable cases with 177 normalized matches, 21 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 189/9; `oi-live-nonzero-memory-atomic-rmw-boundary` again sampled 10/10 matches, and `--summarize-existing` reconfirmed the same result. Manual validation/size review over all 21 failure dirs validated 84 raw/canonical Binaryen/Starshine outputs and found zero validation failures, zero raw-size-larger Starshine dirs, zero canonical-WASM-larger dirs, and zero normalized-WAT-larger dirs. Remaining mismatches are still store-mask low-byte-mask or commuted/synthetic cleanup candidates, not module-derived atomic RMW/cmpxchg address drift. This is module-derived address smoke evidence only; it still does not close broader wait/notify, true seeded/random atomic address closure, full-width/source-visible xchg closure, true shared-memory ordering, true live trap/effect ordering, OI-G, or OI-J descriptor/exactness/TNH/IIT.

The `.tmp/oi-g-atomic-xchg-count350-20260629` refresh broadens `oi-live-nonzero-memory-atomic-rmw-boundary` with byte-preserving xchg restore smoke. The focused `metamorphic_wbtest.mbt` test failed red-first because the transform emitted no `atomic.rmw*.xchg` probes, then passed after `src/fuzz/metamorphic.mbt` added `i32.atomic.rmw8.xchg_u` and `i64.atomic.rmw8.xchg_u` sequences that atomically load a byte and exchange the same value back at fixed, dynamic end-byte, and module-shape-derived in-page addresses. The grouped count350 run compared 196/350 transform-applicable cases with 175 normalized matches, 21 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 189/7; `oi-live-nonzero-memory-atomic-rmw-boundary` sampled 10/10 matches and `--summarize-existing` reconfirmed the same result. The sampled atomic-RMW input case 000016 contains 24 xchg RMW opcodes. Manual validation/size review over all 21 failure dirs validated 84 raw/canonical Binaryen/Starshine outputs and found zero validation failures, zero raw-size-larger Starshine dirs, zero canonical-WASM-larger dirs, and zero normalized-WAT-larger dirs. Remaining mismatches are still store-mask low-byte-mask or commuted/synthetic cleanup candidates, not xchg or atomic-RMW drift. This is byte-width xchg restore smoke evidence only; it still does not close broader wait/notify, true seeded/random atomic address closure, full-width/source-visible xchg closure, true shared-memory ordering, true live trap/effect ordering, OI-G, or OI-J descriptor/exactness/TNH/IIT.


The `.tmp/oi-g-atomic-wait-notify-count350-20260629` refresh adds `oi-live-nonzero-memory-atomic-wait-notify-boundary`, a private shared-memory nonblocking wait/notify transform. The focused `metamorphic_wbtest.mbt` test failed red-first because the transform id/constructor did not exist, then passed after `src/fuzz/metamorphic.mbt` appended a private shared i32 memory with `min=max=1`, guarded `memory.size != 0`, emitted `memory.atomic.notify`, and emitted `memory.atomic.wait32`/`wait64` probes whose expected values differ from the zero-initialized private memory with timeout `0`. A direct emit smoke at `.tmp/oi-g-wait-notify-emit-smoke-20260629` produced one notify, one wait32, and one wait64 opcode and validated with `wasm-tools validate --features all`. The grouped count350 run compared 182/350 transform-applicable cases with 160 normalized matches, 22 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 81/101; the new wait-notify transform sampled 9/9 matches and `--summarize-existing` reconfirmed the same result. Manual validation/size review over all 22 failure dirs validated 88 Binaryen/Starshine raw/canonical outputs and found zero validation failures, zero raw-size-larger Starshine dirs, zero canonical-WASM-larger dirs, and zero normalized-WAT-larger dirs. Remaining mismatches are still store-mask low-byte-mask or commuted/synthetic cleanup candidates, not wait/notify drift. This is private nonblocking wait/notify smoke only; it still does not close true shared-memory ordering, source-visible shared-memory effects, blocking wait behavior, full-width/source-visible xchg closure, OI-G, or OI-J descriptor/exactness/TNH/IIT.


The `.tmp/oi-g-memory-commuted-operands-fix-count400-20260629` refresh adds `oi-memory-commuted-operands`, an OI-G-specific metamorphic transform that appends a private nonzero i32 memory and prepends commuted constant-address load/store/copy probes plus commuted low-byte store masks. The focused transform test failed red-first while the transform id/constructor was absent, then passed after `src/fuzz/metamorphic.mbt` added the transform and `scripts/lib/oi-parity-sweep.ts` forwarded it. The first count400 run at `.tmp/oi-g-memory-commuted-operands-count400-20260629` exposed two size-losing memory-commuted residuals where Starshine's raw-partial path kept a size-1 same-address `memory.copy` and a reversed `i32.and 255` mask that Binaryen removed. Red-first `src/passes/optimize_instructions_test.mbt::optimize-instructions raw partial lowers commuted constant memory addresses` failed on that shape, then passed after `src/passes/pass_manager.mbt` lowered commuted-constant size-1 copies and dropped reversed low-byte store masks in the raw-partial path. The fixed count400 run compared 400/400 with 350 normalized matches, 50 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 400/0; the new transform sampled 19 cases with 14 matches and five residuals. Those residuals are now store-mask Starshine-win candidates, not address/copy opcode drift: representative memory-commuted case 000090 differs only by Starshine dropping Binaryen's retained `i32.and 255` before `i32.store8` (raw 123/127), and case 000363 repeats the known `i64.store8` low-byte-mask candidate (111/115). Manual validation/size review over all 50 failure dirs found zero validation failures and zero Starshine-larger raw or normalized-WAT dirs. This is private-memory commuted address/store evidence only, not broad source-visible store/address commutation, nonconstant/effectful ordering closure, OI-G closure, or OI-J descriptor evidence.

The `.tmp/oi-g-shared-wait-notify-profile-count375-20260629` refresh adds the fifteenth `pass-oi-memory-bulk` profile case, `oi-memory-bulk:shared-wait-notify`, as source-visible shared-memory wait/notify/load source evidence. The focused generator test failed red-first at seed `0x5efb` because the seed still selected the old multi-memory case, then passed after `src/validate/gen_valid.mbt` added a shared memory with `min=max=1`, exported `run` and `memory`, emitted `memory.atomic.notify`, nonblocking `memory.atomic.wait32`/`wait64` with mismatching expected values and timeout `0`, and reloaded the shared memory. Direct runtime count15 at `.tmp/oi-g-shared-wait-notify-profile-count15-20260629` compared 15/15 with 15 normalized matches, zero mismatches/failures, and sampled the new label 1/1; runtime remained blocked by unsupported cases (`checked=8`, `unsupported=7`, `failed=0`). Grouped count375 compared 375/375 with 329 normalized matches, 46 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 172/203. The new label sampled 25 cases with 22 matches and three residuals, all from `oi-commuted-operands` dropped pure reverse `i32.eq(42, 7)` cleanup rather than wait/notify, shared-memory, export, or memory-index drift. Manual validation/size review over all 46 failure dirs found zero validation failures and zero Starshine-larger raw or normalized-WAT dirs. This is not true shared-memory ordering/concurrency, blocking wait behavior, source-visible imported-memory closure, OI-G closure, or OI-J descriptor evidence.

A 2026-06-29 classification-only refresh re-read the atomic wait/notify root with `--summarize-existing` and inspected representative current residual diffs before choosing another implementation slice. The 22 residuals split into 13 `oi-memory-bulk:store-mask-boundaries` low-byte-mask candidates and nine `oi-commuted-operands` dropped-compare candidates. Store-mask representatives differ only by Binaryen keeping `i64.and 255` before `i64.store8` while Starshine stores the same low byte directly (for example case 000002 raw/canonical Starshine/Binaryen 68/72; atomic RMW wrapper case 000036 is 2089/2093) with no atomic opcode, memory index, or memory-effect drift. Commuted representatives differ only by Starshine folding a dropped pure reverse `i32.eq(42, 7)` to `i32.const 0` while Binaryen keeps the compare (case 000005 raw/canonical 90/93), covering memory64, multi-memory, and memory64 cross-memory random-address profile cases without memory address-width or index drift. This refresh records agent classifications only; it does not close grouped mismatch reduction, broad store/address commutation, OI-G, or OI-J descriptor/exactness/TNH/IIT.

## Metamorphic transform layer

The first OI-specific transform layer should cover:

- `oi-local-carried`;
- `oi-local-tee-wrapped`;
- `oi-effectful-sibling`;
- `oi-trapping-sibling`;
- `oi-commuted-operands`;
- `oi-memory-commuted-operands`;
- `oi-if-select-shell`;
- `oi-memory-size-boundary`;
- `oi-live-zero-memory-boundary`;
- `oi-live-nonzero-memory-copy-boundary`;
- `oi-live-nonzero-memory-mid-copy-boundary`;
- `oi-live-nonzero-memory-end-copy-boundary`;
- `oi-live-nonzero-memory-dynamic-copy-boundary`;
- `oi-live-nonzero-memory-varied-copy-boundary`;
- `oi-live-nonzero-memory-second-copy-boundary`;
- `oi-live-nonzero-memory-cross-copy-boundary`;
- `oi-live-nonzero-memory-atomic-boundary`;
- `oi-live-nonzero-memory-second-atomic-boundary`;
- `oi-live-nonzero-memory-atomic-rmw-boundary`;
- `oi-live-nonzero-memory-atomic-wait-notify-boundary`;
- `oi-live-nonzero-memory-fill-restore-boundary`;
- `oi-live-nonzero-memory-mid-fill-restore-boundary`;
- `oi-live-nonzero-memory-end-fill-restore-boundary`;
- `oi-call-ref-target-wrapper`; and
- `oi-tuple-selected-lane`.

`pass-oi-default-scalar` now has seed-indexed trigger-smoke generation in `src/validate/gen_valid.mbt`: every case keeps direct `i32.add`, double `i32.eqz`, `i64.eqz`, `i32.extend8_s`, `i64.extend32_s`, and `i32.shl` shapes, while seeds also rotate through direct, local-carried, and local.tee/commuted scalar shells. A one-case compare at `.tmp/oi-d-trigger-profile-compare-smoke` compared 1/1 with one raw mismatch and no validation/generator/property/command failures. The stronger count-3 smoke at `.tmp/oi-d-trigger-profile-cases-compare-smoke` compared 3/3 with three raw mismatches and no validation/generator/property/command failures, and `bun scripts/oi-parity-sweep.ts --family OI-D --execute --count 3 --starshine-bin target/native/release/build/cmd/cmd.exe` reached the grouped row with the same 3/3 raw-mismatch profile. The metadata refresh at `.tmp/oi-d-trigger-profile-cases-metadata-smoke` also compared 3/3 with three raw mismatches and recorded one each of `oi-default-scalar:local-tee-commuted-add-zero`, `oi-default-scalar:direct`, and `oi-default-scalar:local-carried-add-zero` in `genValidProfileCaseCounts` and `cases.jsonl`. The grouped runner summary smoke at `.tmp/oi-parity-sweep-summary-smoke` re-ran OI-D/OI-E with `--count 3`; OI-D compared 3/3 with three raw mismatches and the runner printed all three OI-D case labels as one mismatch each. A fresh `.tmp/oi-parity-sweep-oi-d-double-eqz-fix-smoke` OI-D count-3 sweep after the literal double-`eqz` fix still compared 3/3 with three raw mismatches and no validation/generator/property/command failures, but the representative WAT diffs now agree on `i32.const 1`; the remaining drifts are constant compare/sign-extension output-shape candidates. Measuring the saved canonical wasm outputs gives Starshine size wins of -5 bytes for `local-tee-commuted-add-zero` and -2 bytes each for `direct` and `local-carried-add-zero`, and `wasm-tools validate --features all` passes for both tools' canonical outputs. This is open OI-D direct/local-carried/local.tee parity evidence with measured Starshine-win candidates, not automatic semantic-safety proof for broader ungenerated shapes.

`pass-oi-local-facts` now has seed-indexed trigger-smoke generation in `src/validate/gen_valid.mbt`: seeds rotate through direct local.set/local.get sign-extension facts, a `local.tee` carried add-zero shell, and a local-carried mask use. A one-case compare at `.tmp/oi-e-trigger-profile-compare-smoke` compared 1/1 with one raw mismatch and no validation/generator/property/command failures. The stronger count-3 smoke at `.tmp/oi-e-trigger-profile-cases-compare-smoke` compared 3/3 with three raw mismatches and no validation/generator/property/command failures, and `bun scripts/oi-parity-sweep.ts --family OI-E --execute --count 3 --starshine-bin target/native/release/build/cmd/cmd.exe` reached the grouped row with the same 3/3 raw-mismatch profile. The metadata refresh at `.tmp/oi-e-trigger-profile-cases-metadata-smoke` also compared 3/3 with three raw mismatches and records one each of `oi-local-facts:local-carried-mask`, `oi-local-facts:direct-sign-extension`, and `oi-local-facts:local-tee-add-zero`. The grouped runner summary smoke at `.tmp/oi-parity-sweep-summary-smoke` compared OI-E 3/3 with three raw mismatches and printed all three OI-E case labels as one mismatch each. This is open OI-E LocalScanner direct/local.tee/local-mask parity evidence, not semantic-safety proof.

`pass-oi-ref-gc` now has seed-indexed trigger-smoke generation in `src/validate/gen_valid.mbt`: seeds rotate through branch-cast boundaries, direct null/test/cast/equality, and local-carried null/test/cast forms, with labels `oi-ref-gc:branch-cast-boundaries`, `oi-ref-gc:direct-null-test-cast-eq`, and `oi-ref-gc:local-carried-null-test-cast`. This landed after an initial grouped OI-I/OI-J attempt at `.tmp/oi-ref-gc-grouped-sweep-20260627` produced generator failures only: the previous descriptor-bearing profile emitted inputs that `wasm-tools validate --features all` rejected with `malformed section id: 14`. The focused test `OI-I ref-gc trigger-smoke profile selects distinct seed-indexed reference cases` failed red-first before the selector and now passes. A fresh grouped OI-I sweep at `.tmp/oi-ref-gc-trigger-grouped-sweep-20260627/oi-i/OI-I-ref-null-cast-basics` compared 3/3 with zero validation/generator/property/command failures, one match (`local-carried-null-test-cast`), and two raw mismatches (`branch-cast-boundaries`, `direct-null-test-cast-eq`). The branch-cast representative was a size-losing Starshine output-shape gap: Binaryen narrowed block results to `nullref` and collapsed two non-null trap tails to one `unreachable` (raw/canonical 89/85 bytes), while Starshine kept `anyref` result blocks and two dropped unreachable result blocks (108/96 bytes); both raw outputs validated. The branch-cast gap now has a focused fix: red-first `optimize_instructions_test.mbt` coverage for null-fed `br_on_cast`/`br_on_cast_fail` block-result narrowing and repeated dropped null-trap blocks passed after OI learned nullref narrowing, unreachable-guard cast branch collapse, dropped-unreachable-block collapse, and root dead-suffix trimming. The fresh `.tmp/oi-ref-gc-branch-cast-fix4-20260627/oi-i/OI-I-ref-null-cast-basics` grouped sweep compares 3/3 with two normalized matches and one raw mismatch; `oi-ref-gc:branch-cast-boundaries` and `oi-ref-gc:local-carried-null-test-cast` now match. A follow-up no-transform reduction at `.tmp/oi-ref-gc-direct-no-transform-triage-20260627` proves the remaining `oi-ref-gc:direct-null-test-cast-eq` diff is OI-I-owned, not caused only by `oi-effectful-sibling`: without metamorphic transforms the count-3 lane still compares 3/3 with one raw mismatch and no validation/generator/property/command failures. The representative is currently a measured Starshine-win candidate, not a size-losing parity gap: Starshine folds dropped pure null tests and trims one side-effect-free nullable-ref drop before the same guaranteed non-null-cast trap tail, both tools' raw/canonical outputs validate, and canonical wasm is Starshine 40 bytes vs Binaryen 48 bytes (raw 48 bytes each). The earlier `oi-effectful-sibling` representative remains useful for OI-F/OI-D because it also differs on private-global if sinking and constant-add spelling. The disabled OI-J run under the branch-cast fix root also compares 3/3 with branch-cast and local-carried labels matching, but because the trigger smoke is non-descriptor it is still not descriptor/exactness/TNH/IIT coverage; its remaining direct/trapping-sibling mismatch is only a sampled Starshine-win candidate for a dead synthetic trap shell.

`oi-local-carried` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it adds one i32 local to each defined function, prepends `i32.const` / `local.set` / `local.get` / `i32.add` / `drop`, and leaves the original body after that stack-neutral prefix. This gives OI-D/OI-E sweeps a first local-carried scalar/fact surface, but it is not yet trigger-biased around existing producers, non-i32 types, or selected OI facts.

`oi-local-tee-wrapped` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it adds one i32 local to each defined function, prepends `i32.const` / `local.tee` / `drop` plus a `local.get` / `i32.add` / `drop` use, and leaves the original body after that stack-neutral prefix. This gives OI-D/OI-E sweeps a first local.tee-carried scalar/fact surface. It remains smoke-only: it does not yet wrap existing producers, cover non-i32 carriers, or bias toward specific `LocalScanner` facts.

`oi-commuted-operands` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it prepends stack-neutral i32 constant pairs for `i32.add`, `i32.and`, and `i32.eq` in both operand orders before each defined function body. This gives OI-D/OI-E/OI-F/OI-G sweeps a first operand-order canonicalization/no-reorder surface. It remains smoke-only: it does not yet wrap existing producers, include i64/float/select/memory address operands, or inject effectful/trapping sibling boundaries.

`oi-if-select-shell` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it prepends stack-neutral i32-result `if`, void `if/else`, identical-arm `select`, and `eqz`-condition `select` shells before each defined function body. This gives OI-F sweeps a first boolean/select/ternary-shell surface. A one-case `pass-oi-boolean-select` compare smoke with this transform compared 1/1 and found 1 raw mismatch with no validation/generator/command failures, which is open OI-F parity evidence rather than semantic-safety proof. The transform remains smoke-only: it does not yet wrap existing producers, model branch-hint/no-fold metadata, generate effectful/trapping conditions, or cover reference/SIMD/select-typed lanes.

`oi-effectful-sibling` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it appends a private mutable i32 global and prepends each defined function with stack-neutral `global.set` / `global.get` / `drop`, effectful `if`/`else`, select-sibling, and arithmetic-sibling shells. This gives OI-F/OI-G/OI-H/OI-M sweeps a first side-effect/no-reorder surface while preserving source-visible semantics because only the new unexported global is written. A one-case `pass-oi-boolean-select` compare smoke with this transform compared 1/1 and found 1 raw mismatch with no validation/generator/command failures, which is open OI-F parity evidence rather than semantic-safety proof. It remains smoke-only: it does not yet wrap existing producers, model typed/reference/SIMD lanes, exercise memory-specific effect ordering, or cover tuple selected-lane localization.

`oi-trapping-sibling` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it prepends each defined function with a stack-neutral dead `if` shell containing scalar div/rem-by-zero trap-shaped siblings plus a live `i32.const` / `drop` sibling before the original body. This gives OI-F/OI-G/OI-J/OI-K/OI-M sweeps a first trap/no-reorder surface while preserving source-visible semantics because the trapping forms are unreachable. A one-case `pass-oi-boolean-select` compare smoke with this transform compared 1/1 and found 1 raw mismatch with no validation/generator/command failures, which is open OI-F parity evidence rather than semantic-safety proof. It remains smoke-only: it does not wrap existing producers, cover live-but-preserved trap ordering, model memory/ref/GC/tuple trap lanes, or prove Binaryen semantic parity for any family.

A first grouped OI-F transform classification at `.tmp/oi-f-effectful-family-classify-20260627` ran `bun scripts/oi-parity-sweep.ts --family OI-F --count 4 --out-dir .tmp/oi-f-effectful-family-classify-20260627 --starshine-bin target/native/release/build/cmd/cmd.exe --execute`. It compared 4/4 with four raw mismatches and zero validation/generator/property/command failures, mechanically grouped as `transform:oi-if-select-shell`, `transform:oi-effectful-sibling`, `transform:oi-trapping-sibling`, and `transform:oi-commuted-operands`. All representative raw/canonical outputs validate with `wasm-tools validate --features all`. The pure `oi-if-select-shell`, dead `oi-trapping-sibling`, and pure `oi-commuted-operands` representatives are sampled canonical-size Starshine-win candidates under their transform contracts, not OI-F closure: canonical Starshine/Binaryen sizes are 2732/2844, 2705/2845, and 2755/2855 bytes respectively, while raw Starshine output is larger in each case. The `oi-effectful-sibling` representative was different and initially size-losing. Follow-up red-first slices taught HOT OI and the raw skip path to sink same-private-global `if` arms into `global.set(if(result i32 ...))`, fold the first synthetic dropped `i32.const 2 + i32.const 3` add, fold the nested raw-skipped `i32.const 6 + i32.const 7` add under the dropped multiply, fold sampled raw-skipped `i32.sub`, `i64.add`/`i64.sub`, and `i32.wrap_i64` constants, rewrite sampled raw-skipped nonnegative signed compare/div/rem/shift forms to unsigned spellings, rewrite sampled finite `f32.sub`/`f64.sub` constants to add-negative spellings, and rewrite the sampled finite `(f64.div x 2.0) - c` shell to `f64.mul x 0.5` plus add-negative. `.tmp/oi-f-effectful-family-fix7-20260627` now compares 4/4 with one normalized match and three raw mismatches: `transform:oi-effectful-sibling` is the match, while `oi-if-select-shell`, `oi-trapping-sibling`, and `oi-commuted-operands` remain mismatches, with zero validation/generator/property/command failures. Manual measurement of the fix7 effectful case keeps normalized wasm size parity (Binaryen 2893, Starshine 2893) and raw Starshine 4135 vs Binaryen 2893; all sampled scalar and float opcode counts now match, including `f32.sub/add/div/mul` 0/4/2/2 and `f64.sub/add/div/mul` 0/6/2/4 for both tools. Both raw/normalized outputs validate. The label is no longer a sampled normalized OI-D/OI-F mismatch, but raw-byte size remains a separate encoder/structure residual and OI-F stays open because the other three grouped labels still mismatch. A fix7 `--summarize-existing` refresh re-measured those three labels and kept their classifications as sampled canonical Starshine-win candidates under synthetic transform contracts: `oi-if-select-shell` 2732 vs 2844 canonical bytes, `oi-trapping-sibling` 2705 vs 2845, and `oi-commuted-operands` 2755 vs 2855, with all Binaryen/Starshine raw and canonical outputs validating. This is not OI-F family signoff; live producer/effect/trap wrappers and branch-hint/no-fold metadata remain open.

`oi-memory-size-boundary` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: for modules with an i32 memory, it prepends a dead `if` shell containing `memory.copy` and `memory.fill` variants for sizes `0`, `1`, `2`, `3`, `4`, `5`, `8`, and `16` using i32 address/size operands. It now also supports memory64-only modules by injecting the same dead shell with i64 address/size operands. This keeps source semantics stable while giving OI-G sweeps first bulk-memory tiny-size coverage across memory32 and memory64. It is not yet a complete trigger-biased memory boundary generator: live-but-equivalent variants, trapping/effectful siblings, dynamic operands, broader atomics, and multi-memory shapes remain future work.

A first grouped OI-G count-5 classification at `.tmp/oi-g-memory-family-classify-20260628` compared 5/5 with five raw mismatches and zero validation/generator/property/command failures. The `transform:oi-memory-size-boundary` representative exposed an OI-G tiny bulk-memory lowering parity gap rather than safe drift: Binaryen lowered sampled size `1`/`2`/`4`/`8`/`16` `memory.copy`/`memory.fill` variants while Starshine left all 20 copy plus 20 fill spellings. A red-first fix now covers dead/raw-skipped `if` shells and applies tiny lowering inside HOT regions and the raw partial path without erasing zero-size or size-3/5 spellings. The grouped fix4 sweep at `.tmp/oi-g-memory-family-fix4-20260628` still compares 5/5 with five raw mismatches and zero validation/generator/property/command failures, but the memory-size-boundary sample now has matching 10 copy / 10 fill and v128 load/store counts on both tools. Follow-up WAT and section inspection classifies its remaining mismatch as structural/code-section drift rather than unlowered bulk-memory: Starshine folds more constant ordinary/atomic memory addresses into offsets (186 offset-bearing memory ops vs Binaryen 104), keeps one fewer i32 local in function 0, and all section-size deltas are in code (raw code Starshine/Binaryen 6162/6100; canonical code 6080/6108). The case remains a sampled canonical Starshine-win candidate (6209/6237 bytes) with raw-byte residual (6291/6229). The other grouped labels still show matching sampled memory opcode counts; after fix4, `oi-local-carried`, `oi-trapping-sibling`, `oi-commuted-operands`, and `oi-effectful-sibling` are sampled canonical-size Starshine-win candidates with raw-size residuals, not OI-G family signoff. All 20 fix4 raw/canonical outputs validate.

`pass-oi-memory-bulk` now has fifteen trigger-smoke-plus cases in `src/validate/gen_valid.mbt`: live private-global plus dead-trap boundaries, direct i32 tiny `memory.copy`/`memory.fill` plus offset folding, memory64 dynamic/local-carried bulk, no-param call/existing-producer-wrapped tiny bulk, narrow-store masks, dynamic i32 local.tee bulk operands, multi-memory bulk operations, seed-derived nonzero in-page copy/read addresses, seed-derived non-primary-memory in-page copy/read addresses, seed-derived private-destination cross-memory random-address copy/read addresses, seed-derived memory64 private-destination cross-memory random-address copy/read addresses, seed-derived source-visible cross-memory copy/read with destination-byte restore, seed-derived memory64 source-visible cross-memory copy/read with destination-byte restore, a Node-runtime-compatible single-memory restore/copy/read exported function with active data, and a source-visible shared-memory wait/notify/load exported function. The focused test `OI-G memory-bulk trigger-smoke profile selects distinct seed-indexed memory cases` first failed red-first on missing labels before the three-case implementation, then extended red-first for existing-producer/store-mask/dynamic-i32, multi-memory, random-address, multi-memory random-address, cross-memory random-address, memory64 cross-memory random-address, source-visible restore, memory64 source-visible restore, and runtime-compatible restore labels/signatures. The count-198 run at `.tmp/oi-g-multimemory-random-address-count198-20260629` samples all nine then-existing labels; `oi-memory-bulk:multi-memory-random-address-copy-read` has 24/25 matches and one commuted dropped-compare residual, not non-primary memory.copy/read opcode drift. The earlier no-transform count-3 compare at `.tmp/oi-g-memory-bulk-trigger-profile-20260628-fix2` matched the first three labels. A direct count-18 compare at `.tmp/oi-g-memory-bulk-expanded-profile-count18-20260628` sampled all six then-existing labels and compared 18/18 with 14 normalized matches, four `oi-memory-bulk:store-mask-boundaries` mismatches, and zero validation/generator/property/command failures. The no-transform store-mask residual is a sampled Starshine-win candidate for the exact low-byte-store contract: store8/store16 truncate the stored value, Binaryen removes the i32 masks but keeps `i64.and 255` before `i64.store8`, Starshine removes it too, all outputs validate, and raw/canonical Starshine/Binaryen sizes are 56/60. The original grouped count-6 OI-G sweep at `.tmp/oi-g-memory-family-expanded-profile-20260628` compared 6/6 with two normalized matches and four mismatches, and exposed a size-losing store-mask plus `oi-memory-size-boundary` residual (raw 258/234, canonical 243/234 Starshine/Binaryen) because the dead-shell/raw path left masks that the no-transform path removes. A red-first pass/raw-shell test now covers that case; `src/passes/pass_manager.mbt` raw partial cleanup drops redundant i32/i64 low-bit masks after the dead shell. The direct rerun `.tmp/oi-g-memory-bulk-store-mask-raw-fix-count18-20260628` still has the same four no-transform store-mask mismatches, and grouped rerun `.tmp/oi-g-memory-family-store-mask-raw-fix-20260628` still has four mismatches, but the grouped store-mask representative is reduced to the known low-byte-store output-shape residual: Binaryen keeps only the i64 mask, Starshine removes it, raw Starshine/Binaryen is 245/234, wasm-opt no-pass canonical Starshine/Binaryen is 230/234, and all measured outputs validate. The grouped count>=11 blocker is now fixed in the generator/transform layer: red-first `src/fuzz/metamorphic_wbtest.mbt` and `src/fuzz/main_wbtest.mbt` coverage drove `oi-memory-size-boundary` support for memory64-only modules, so the transform emits i64-address/size dead shells instead of aborting on later memory64-dynamic cases. Grouped reruns completed: count 11 compares 11/11 with 4 normalized matches and 7 mismatches, count 12 compares 12/12 with 4 normalized matches and 8 mismatches, and the first count 18 compared 18/18 with 6 normalized matches and 12 mismatches, all with zero validation/generator/property/command failures. The new count-18 memory64 + memory-size-boundary representative keeps matching memory.copy/fill counts and canonical size parity (234/234), with raw Starshine/Binaryen 249/234 and all raw/canonical outputs validating; its WAT drift is the existing memory64 offset-folding residual (`i64.load offset=24` with `i64.const 8` vs Binaryen `i64.const 32`). A follow-up grouped count-18 classification found one remaining canonical size-losing case, `oi-memory-bulk:live-effect-trap-boundaries + oi-commuted-operands`, caused by the raw-skipped fallback leaving Binaryen's sampled ordered const equality spelling (`7 == 42`). Red-first `src/passes/optimize_instructions_test.mbt` coverage now guards the raw ordered adjacent i32/i64 const eq/ne fold, and `src/passes/pass_manager.mbt` applies that narrow raw fallback. Fresh grouped reruns compare count 18 at 18/18 with 7 normalized matches and 11 mismatches, then count 24 at 24/24 with 9 normalized matches and 15 mismatches, both with zero validation/generator/property/command failures and all sampled live-effect labels matching. Manual size review found zero remaining canonical size-losing mismatches in the count-18/count-24 failure dirs; all 44 count-18 and 60 count-24 Binaryen/Starshine raw/canonical outputs validate. A bounded count-30 scale-up at `.tmp/oi-g-memory-family-count30-20260628` compares 30/30 with 10 normalized matches, 20 mismatches, and zero validation/generator/property/command failures; manual review of all 20 failure dirs again found zero canonical size-losing mismatches and all 80 raw/canonical outputs validate. New count-30 residuals are classified as sampled candidates, not OI-G closure: dynamic-i32 + commuted keeps memory.copy/fill/load counts equal and differs only by Starshine's dropped pure reverse const-eq fold (raw/canonical 89/92 Starshine/Binaryen); memory64 + trapping keeps memory.copy/fill/i64.load counts equal and removes a dead if-false trap shell that Binaryen leaves (raw 66/86, canonical 63/86); store-mask effectful/commuted extends the known low-byte-store mask removal with equal store counts and canonical wins (112/116 and 86/93). A count-36 scale-up at `.tmp/oi-g-memory-family-count36-20260628` compares 36/36 with 13 normalized matches, 23 mismatches, and zero validation/generator/property/command failures; manual review of all 23 failure dirs found zero canonical size-losing mismatches and all 92 raw/canonical outputs validate. The new count-36 mismatches repeat existing classifications rather than exposing a new size-losing family: memory64 + memory-size-boundary repeats the i64.load offset-folding residual with memory op counts equal and canonical parity 234/234, direct-tiny + trapping repeats dead scalar div spelling plus offset-folding/raw residuals with tiny bulk counts equal and canonical parity 243/243, and memory64 + commuted repeats the pure dropped reverse const-eq fold with raw/canonical 90/93 Starshine/Binaryen. A count-42 scale-up at `.tmp/oi-g-memory-family-count42-20260628` compares 42/42 with 17 normalized matches, 25 mismatches, and zero validation/generator/property/command failures; manual review of all 25 failure dirs found zero canonical size-losing mismatches and all 100 raw/canonical outputs validate. The two new count-42 mismatches also repeat existing classifications: existing-producer + trapping keeps memory load/store counts equal and repeats dead-helper trap-shell trimming with raw/canonical Starshine/Binaryen 116/151 and 105/151, while store-mask + local-carried repeats the low-byte-store mask removal contract where Starshine removes `i64.and 255` before `i64.store8` and Binaryen keeps it, with raw/canonical Starshine/Binaryen 68/72. A count-48 requested scale-up at `.tmp/oi-g-memory-family-count48-20260628` generated and compared 45/48 transform-balanced cases with 18 normalized matches, 27 mismatches, and zero validation/generator/property/command failures; manual review of all 27 failure dirs found zero canonical size-losing mismatches and all 108 raw/canonical outputs validate. The new case-000044 live-effect + trapping mismatch keeps memory load/store counts equal but reopens a sampled dead scalar trap signed/unsigned spelling residual (`i32.div_s` vs `i32.div_u`) with raw Starshine/Binaryen 101/98 and canonical parity 98/98. The new case-000045 memory64 + commuted mismatch repeats the pure dropped reverse const-eq fold with memory.copy/fill/i64.load counts equal and raw/canonical Starshine/Binaryen 90/93. A count-54 requested saturation check at `.tmp/oi-g-memory-family-count54-20260628` compared the same 45 transform-balanced cases, with the same 18 normalized matches, 27 mismatches, zero validation/generator/property/command failures, and zero canonical size-losing failure dirs; all 108 raw/canonical failure outputs validate. Because raising the requested count from 48 to 54 produced no additional comparable cases, the next OI-G scale-up needed generator/transform scheduling breadth rather than only a higher count. The multi-memory extension supplied that breadth: direct count-7 at `.tmp/oi-g-memory-bulk-multimemory-count7-20260628` compared 7/7 with six normalized matches, one known store-mask mismatch, zero validation/generator/property/command failures, and the new multi-memory case matching; grouped count-63 at `.tmp/oi-g-memory-family-multimemory-count63-20260628` compared 53/63 transform-balanced cases with 27 normalized matches, 26 mismatches, zero validation/generator/property/command failures, and profile-case counts multi-memory=6, dynamic-i32=10, store-mask=5, existing-producer=12, live-effect=4, direct-tiny=9, memory64=7. Manual review validated all 104 failure-dir raw/canonical outputs and found zero canonical size-losing failure dirs. The only multi-memory mismatch was a trapping-sibling dead scalar div signed/unsigned spelling residual with canonical parity 94/94 and no remaining memory.copy/fill in either normalized output, so it is classified as sampled trigger evidence, not multi-memory closure. The remaining representatives are classified with concrete reopening criteria: store-mask low-byte-store mask removal is a canonical Starshine win with raw residual; memory64 cases are offset-folding, dead-trap-shell, or commuted-compare residuals; existing-producer grouped residuals are helper dead-trap or dropped-compare Starshine wins; direct-tiny, live-effect, and multi-memory trapping residuals agree on relevant memory opcode counts or have no remaining bulk ops but keep raw-size/offset-folding/dead-trap spelling drift. A follow-up raw-spelling slice then removed the sampled live-effect/multi-memory dead scalar trap residual: Binaryen prints nonnegative signed i32/i64 div/rem by constant zero as unsigned spellings because both signed and unsigned forms trap before producing a value, and red-first raw-skip coverage now matches that in `src/passes/pass_manager.mbt`. The fresh grouped baseline `.tmp/oi-g-memory-family-divzero-spelling-fix-count63-20260628` compares 63/63 with 39 normalized matches, 24 mismatches, zero validation/generator/property/command failures, and profile cases multi-memory=6 (6 matches), live-effect=4 (4 matches), dynamic-i32=13 (12 matches/1 mismatch), store-mask=5 (5 mismatches), existing-producer=17 (13 matches/4 mismatches), direct-tiny=9 (4 matches/5 mismatches), and memory64=9 (9 mismatches). Manual review validated all 96 Binaryen/Starshine raw/canonical failure outputs and found zero canonical size-losing failure dirs; seven failure dirs remain raw-size-larger for Starshine. A later red-first raw constant-memory-offset slice targeted the count-110 direct-tiny and memory64 offset-folding residuals: `src/passes/optimize_instructions_test.mbt::optimize-instructions raw skip folds constant memory access offsets` failed before implementation and passed after `src/passes/pass_manager.mbt` folded raw-skipped nonnegative memory32 constant address plus static offset and non-overflowing memory64 constant address plus static offset to Binaryen's zero-offset/folded-address spelling. The fresh grouped run `.tmp/oi-g-raw-const-mem-offset-fix-count110-20260628` compares 110/110 with 89 normalized matches, 21 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 110/0. Manual review validates all 84 Binaryen/Starshine raw and normalized failure outputs, finds zero normalized/canonical size-losing dirs, and leaves two raw-size-larger Starshine dirs, both store-mask plus `oi-memory-size-boundary` residuals. Direct-tiny samples are now 15/15 matches; live-effect, trapping-sibling, live-nonzero mid-copy, and live-nonzero end-fill transforms all match every sampled case. Remaining mismatches are store-mask low-byte-mask candidates and commuted dropped-compare Starshine-win candidates under existing-producer, memory64, dynamic-i32, or multi-memory wrappers, not memory.copy/fill opcode drift or the prior direct-tiny/memory64 constant-offset residuals. OI-G remains open for grouped mismatch reduction, live memory effect/trap ordering, broader atomics, store/address commutation, randomized multi-memory and memory64 evidence, and raw-size alignment; OI-J descriptor/exactness/TNH/IIT remains blocked on true descriptor-compatible evidence.

`oi-live-zero-memory-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it prepends reachable zero-size `memory.copy` and `memory.fill` operations to each defined function, using i32 operands for memory32 modules and i64 operands for memory64-only modules. This is a live-but-no-op bulk-memory boundary surface, not trap-order closure. The focused transform test failed red-first on the missing transform id/constructor and now passes. The first grouped run with this transform, `.tmp/oi-g-live-zero-memory-boundary-count70-20260628`, compared 70/70 with 49 normalized matches, 21 mismatches, and zero validation/generator/property/command failures; all 11 sampled `oi-live-zero-memory-boundary` cases matched. Manual review of the 21 failure dirs validated all 84 Binaryen/Starshine raw/canonical outputs, found zero canonical size-losing mismatches, and kept seven raw-size-larger Starshine dirs as raw-byte residuals. The run adds live zero-size evidence but does not close OI-G: residual mismatches are still store-mask low-byte-store candidates, memory64/direct-tiny offset-folding or dead-shell/cross-family compare residuals, and commuted dropped-compare candidates.

`oi-live-nonzero-memory-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, then executes a reachable length-1 `memory.copy` from address 0 to itself plus a byte `load`/`drop`, using i32 operands for memory32 and i64 operands for memory64-only modules. The transform preserves source-visible memory while adding live nonzero copy/read evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The first count-77 run exposed a raw-skip spelling gap where Starshine kept `i32.ne(memory.size, 0)` as an if condition; red-first `optimize_instructions_test.mbt` coverage now fixes the raw partial path to match Binaryen's `memory.size` condition spelling. The post-fix grouped run `.tmp/oi-g-live-nonzero-copy-boundary-raw-if-fix-count77-20260628` compared 66/77 with 43 normalized matches, 23 mismatches, and zero validation/generator/property/command failures. Six of nine live-nonzero samples match; the three remaining live-nonzero mismatches are direct-tiny offset-folding/raw residuals with canonical size parity 238/238. Manual review of all 23 failure dirs validated 92 raw/canonical outputs, found zero canonical size-losing dirs, and found nine raw-size-larger Starshine dirs. This is not memory.fill restore, atomics, randomized-address, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-mid-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, then executes a reachable length-1 `memory.copy` from byte 32768 to itself plus a byte `load`/`drop` at the same address. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding live nonzero fixed page-middle copy/read evidence beyond the byte-0 copy probe. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-110 run `.tmp/oi-g-live-nonzero-mid-copy-boundary-count110-20260628` compared 110/110 with 62 normalized matches, 48 mismatches, and zero validation/generator/property/command failures. The new mid-copy transform sampled ten cases: six match; two memory64 residuals repeat the existing `i64.load offset=24` versus `i64.const 32` raw/offset-folding drift at canonical parity 90/90; and two direct-tiny residuals repeat i32 load/store offset-folding/raw drift at canonical parity 244/244. Manual review of all 48 failure dirs validated 192 raw/canonical outputs, found zero canonical size-losing dirs, and found 27 raw-size-larger Starshine dirs. A follow-up red-first raw constant-memory-offset fix removes these sampled mid-copy residuals in `.tmp/oi-g-raw-const-mem-offset-fix-count110-20260628`: the transform samples ten cases and all ten match. This remains fixed-address copy/read evidence only, not randomized-address, atomics, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-end-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, computes the last byte as `memory.size * 65536 - 1`, executes a reachable length-1 `memory.copy` from that address to itself, then byte `load`/`drop`s the same address. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding dynamic end-byte copy/read evidence beyond byte 0 and page-middle probes. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-140 run `.tmp/oi-g-live-end-copy-boundary-count140-20260628` compared 140/140 with 119 normalized matches, 21 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 130/10. The new end-copy transform sampled ten cases: nine match and one repeats the known store-mask low-byte-mask residual; manual review of all 21 failure dirs validates 84 raw/canonical outputs, finds zero canonical size-losing dirs, and leaves two raw-size-larger Starshine dirs. This is dynamic end-byte copy/read evidence only, not randomized-address, atomics, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-dynamic-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, computes a dynamic interior byte address as `memory.size * 32768`, executes a reachable length-1 `memory.copy` from that address to itself, then byte `load`/`drop`s the same address. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding dynamic copy/read evidence beyond byte 0, fixed page-middle, and dynamic end-byte probes. The focused transform test failed red-first on the missing id/constructor and now passes. The first grouped count-150 run `.tmp/oi-g-live-dynamic-copy-boundary-count150-20260628` exposed a real raw parity gap: all ten dynamic-copy samples mismatched because Starshine did not lower the dynamic length-1 self-copy and then kept the multiply spelling where Binaryen printed `memory.size << 15`. Red-first `src/passes/optimize_instructions_test.mbt::optimize-instructions expands dynamic self tiny memory.copy` failed first on retained `memory.copy` and then on retained `I32(32768)`/multiply spelling; `src/passes/pass_manager.mbt` now lowers this raw dynamic self-copy shape and rewrites the address to the Binaryen-shaped shift. The fix2 grouped count-150 run `.tmp/oi-g-live-dynamic-copy-boundary-fix2-count150-20260628` compares 150/150 with 129 normalized matches, 21 mismatches, zero validation/generator/property/command failures, and the new transform 10/10 matched. Manual review of all 21 failure dirs validates 84 raw/canonical outputs, finds zero canonical size-losing dirs, and leaves three raw-size-larger Starshine dirs. This is dynamic interior-address copy/read evidence only, not randomized-address, multi-memory-specific ordering, true live trap-order, or OI-G closure.

`oi-live-nonzero-memory-second-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it requires at least one memory, uses an existing non-primary memory when present or adds a private second i32 memory when the input has only one memory, guards that non-primary `memory.size != 0`, executes a reachable length-1 `memory.copy` from byte 0 of that memory to itself, then byte `load`/`drop`s the same memory. This preserves source-visible memory while adding non-primary-memory live nonzero copy/read evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The first grouped count-160 attempt `.tmp/oi-g-live-second-copy-boundary-count160-20260628` failed before comparison because the non-primary-only transform could not be selected for single-memory profile positions; the private-second-memory applicability fix removed that generator stall. The grouped count-160 run `.tmp/oi-g-live-second-copy-boundary-fix-count160-20260628` compares 160/160 with 138 normalized matches, 22 mismatches, zero validation/generator/property/command failures, and the new transform 9/10 matched. Its one residual is the known store-mask low-byte-mask candidate: both tools lower the non-primary self-copy to `i32.load8_u` plus `i32.store8` under the same `memory.size $1` guard, while Starshine removes `i64.and 255` before an unrelated `i64.store8` that Binaryen keeps. Manual review of all 22 failure dirs validates 88 raw/canonical outputs, finds zero canonical size-losing dirs, and leaves two raw-size-larger Starshine dirs. This is non-primary-memory copy/read smoke evidence only, not cross-memory copy mutation, randomized-address, atomics, true live trap-order, or OI-G closure.

`oi-live-nonzero-memory-varied-copy-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, then executes reachable length-1 self `memory.copy` plus byte `load`/`drop` at fixed bytes 17, 1024, 49152, and 65535. This preserves source-visible memory while adding varied fixed-address live nonzero copy/read evidence beyond byte 0, page-middle, dynamic interior, dynamic end-byte, and non-primary memory probes. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-170 run `.tmp/oi-g-live-varied-copy-boundary-count170-20260629` compares 170/170 with 145 normalized matches, 25 mismatches, zero validation/generator/property/command failures, and the new transform 8/10 matched. Its two residuals are the known store-mask low-byte-mask candidate: both tools lower all varied self-copies to load8/store8 forms with `memory.copy` counts 0/0, `i32.load8_u` counts 8/8, and `i32.store8` counts 5/5 in the representative residuals, while Starshine removes `i64.and 255` before an unrelated `i64.store8` that Binaryen keeps. Manual review of all 25 failure dirs validates 100 raw/canonical outputs, finds zero canonical size-losing dirs, and leaves six raw-size-larger Starshine dirs. This is fixed varied-address copy/read smoke evidence only, not randomized-address, atomics, true live trap-order, cross-memory mutation, OI-G closure, or OI-J evidence.

`oi-live-nonzero-memory-atomic-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, then executes a reachable `i32.atomic.load8_u` from byte 0 and drops the result. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding first live atomic-load evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-120 run `.tmp/oi-g-live-atomic-boundary-count120-20260628` compared 120/120 with 99 normalized matches, 21 mismatches, and zero validation/generator/property/command failures; the new atomic transform sampled ten cases with eight matches and two known store-mask low-byte-mask residual mismatches. In both residuals, Binaryen and Starshine keep one `i32.atomic.load8_u`; the WAT diff is the existing low-byte-store `i64.and 255` mask that Starshine removes and Binaryen keeps. Manual review of all 21 failure dirs validated 84 raw/canonical outputs, found zero canonical size-losing dirs, and found five raw-size-larger Starshine dirs. A later count-130 run after adding `oi-live-nonzero-memory-atomic-rmw-boundary` samples the atomic-load transform ten times with all ten matching. This is not broader atomic RMW/cmpxchg, randomized-address, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-atomic-rmw-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it uses an existing non-primary memory or appends a private nonzero memory, guards `memory.size != 0`, then preserves byte 0, byte 16, and page-middle byte 32768 while executing reachable atomic store8 of the currently loaded byte; no-op full/narrow i32/i64 `atomic.rmw.add`, `sub`, `or`, and `xor` variants with value 0; no-op full/narrow i32/i64 `atomic.rmw.and` variants with all-ones values; and i32/i64 `atomic.rmw.cmpxchg` with expected/replacement 0. It also computes the dynamic end-byte address `memory.size * 65536 - 1` and emits byte-width store8/load8, i32/i64 `atomic.rmw8` add/sub/or/xor by zero, i32/i64 `atomic.rmw8.and` by all-ones, and i32/i64 `atomic.rmw8.cmpxchg` 0->0 there, avoiding full-width alignment traps at the last byte. It uses i32 addresses for memory32 and i64 addresses for memory64 memory. The focused transform test first failed on the missing id/constructor, then failed red-first for the broader integer RMW/cmpxchg variants, byte-16 coverage, byte-32768 page-middle extension, no-op operation breadth, dynamic end-byte coverage, and private non-primary memory coverage before each implementation. The grouped count-130 run `.tmp/oi-g-live-atomic-rmw-boundary-count130-20260628` compared 130/130 with 108 normalized matches, 22 mismatches, zero validation/generator/property/command failures, and Binaryen cache hits/misses 123/7; the first atomic RMW/store transform sampled ten cases and all ten matched. The integer-variant run `.tmp/oi-g-live-atomic-rmw-variants-count130-20260628`, the byte-16 refresh `.tmp/oi-g-live-atomic-rmw-nonzero-address-count130-20260628`, the byte-32768 refresh `.tmp/oi-g-live-atomic-rmw-midpage-count130-20260628`, the no-op operation refresh `.tmp/oi-g-live-atomic-rmw-noop-variants-count130-20260628`, and the dynamic end-byte refresh `.tmp/oi-g-live-atomic-rmw-end-byte-count130-20260628` all compare 130/130 with 108 normalized matches, 22 mismatches, zero validation/generator/property/command failures, and the broadened atomic RMW/store/cmpxchg transform still matching 10/10. The latest non-primary/private-memory refresh `.tmp/oi-g-nonprimary-atomic-rmw-count350-20260629` compares 194/350 transform-applicable cases with 174 normalized matches, 20 mismatches, zero failures, and the atomic RMW transform still 10/10 matched. Manual review of all 20 latest failure dirs validated 80 raw/canonical outputs and found zero validation failures, zero raw-size-larger dirs, zero canonical-WASM-larger dirs, and zero normalized-WAT-larger dirs. This is not atomic xchg, wait/notify, randomized-address beyond the fixed/dynamic probes, true shared-memory ordering, true live trap-order, or OI-G closure.

`oi-live-nonzero-memory-atomic-wait-notify-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it appends a private shared i32 memory with `min=max=1`, guards `memory.size != 0`, emits `memory.atomic.notify`, then emits `memory.atomic.wait32` and `memory.atomic.wait64` with expected values that differ from the zero-initialized private memory and timeout `0`, dropping all results. This preserves source-visible memory and avoids blocking while adding first wait/notify sweep evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count350 run at `.tmp/oi-g-atomic-wait-notify-count350-20260629` samples the transform 9/9 matches with zero failures and no Starshine-larger failure dirs. It is not true shared-memory ordering, source-visible shared-memory effect, blocking wait, OI-G closure, or OI-J descriptor evidence.

`oi-live-nonzero-memory-fill-restore-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, then executes a reachable length-1 `memory.fill` that restores byte 0 by loading the current byte as the fill value, followed by a byte `load`/`drop`. It uses i32 address/length operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding live nonzero fill/read evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-84 run `.tmp/oi-g-live-nonzero-fill-restore-boundary-count84-20260628` compared 84/84 with 58 normalized matches, 26 mismatches, and zero validation/generator/property/command failures; the new fill-restore transform sampled ten cases with eight matches and two memory64 offset-folding/raw residual mismatches. In the representative residual both tools keep `memory.fill=1`, `memory.copy=1`, `i32.load8_u=2`, `memory.size=1`, and `i64.ne=1`; the WAT differs only on the existing `i64.load offset=24 (i64.const 8)` versus Binaryen `i64.load (i64.const 32)` spelling. Manual review of all 26 failure dirs validated 104 raw/canonical outputs, found zero canonical size-losing dirs, and found eleven raw-size-larger Starshine dirs. This is not broader atomics, randomized-address, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-mid-fill-restore-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, restores byte 32768 with a reachable length-1 `memory.fill` using the currently loaded byte value, then byte `load`/`drop`s the same address. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding live nonzero fixed page-middle fill/read evidence beyond the byte-0 and end-byte probes. The focused transform test failed red-first on the missing id/constructor and now passes. The grouped count-100 run `.tmp/oi-g-live-nonzero-mid-fill-restore-boundary-count100-20260628` compared 100/100 with 61 normalized matches, 39 mismatches, and zero validation/generator/property/command failures. The new mid-fill transform sampled ten cases: five match; one direct-tiny residual is existing offset-folding/raw drift with canonical size parity 244/244; four residuals are the known store-mask low-byte-mask Starshine-win candidates with canonical Starshine/Binaryen size 83/87. Manual review of all 39 failure dirs validated 156 raw/canonical outputs, found zero canonical size-losing dirs, and found sixteen raw-size-larger Starshine dirs. This is not randomized-address, atomics, live trap-order, or OI-G closure.

`oi-live-nonzero-memory-end-fill-restore-boundary` is now implemented as an OI-G smoke transform in `src/fuzz/metamorphic.mbt`: it guards `memory.size != 0`, computes the last byte address as `memory.size * 65536 - 1`, restores that byte with a reachable length-1 `memory.fill` using the currently loaded byte value, then byte `load`/`drop`s the same address. It uses i32 operands for memory32 and i64 operands for memory64-only modules, preserving source-visible memory while adding live nonzero nonzero-address fill/read evidence. The focused transform test failed red-first on the missing id/constructor and now passes. The first grouped count-91 run `.tmp/oi-g-live-nonzero-end-fill-restore-boundary-count91-full-20260628` compared 91/91 with 48 normalized matches, 43 mismatches, and zero validation/generator/property/command failures, but manual review found ten canonical size-losing end-fill residuals caused by raw-skipped `memory.size * 65536` spellings. A red-first raw partial test now covers that exact gap, and `src/passes/pass_manager.mbt` rewrites non-constant-lhs i32/i64 power-of-two multiplies to shifts in raw-skipped OI functions. The post-fix grouped count-91 run `.tmp/oi-g-live-nonzero-end-fill-restore-boundary-raw-shift-fix-count91-20260628` compared 91/91 with 54 normalized matches, 37 mismatches, and zero validation/generator/property/command failures; the new end-fill transform sampled ten cases with six matches and four offset-folding/raw residual mismatches at canonical parity. Manual review of all 37 failure dirs validated 148 raw/canonical outputs, found zero canonical size-losing dirs, and found eighteen raw-size-larger Starshine dirs. The later raw constant-memory-offset fix removes these sampled end-fill residuals in `.tmp/oi-g-raw-const-mem-offset-fix-count110-20260628`: the transform samples ten cases and all ten match. This is not atomics, fully randomized-address, live trap-order, or OI-G closure.

`oi-call-ref-target-wrapper` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it appends a private no-op function/type plus a declarative element segment, then prepends each previously defined function with stack-neutral direct `ref.func` / `call_ref`, block-returned target, typed-select target, and dead `call_ref` shells. This gives OI-H sweeps a first target-wrapper surface while keeping source-visible semantics stable. A one-case default GenValid compare smoke with this transform compared 1/1 and found 1 raw mismatch with no validation/generator/command failures. The first `pass-oi-call-ref` compare smoke with the same transform hit a generator failure because the profile leaked stringref/exn value types into a wasm-tools oracle lane; `src/validate/gen_valid.mbt` now keeps stringref behind const-expression-rich profiles and exnref behind tag-enabled profiles, and the same seed/profile/transform now validates and reaches comparison. The fixed one-case `pass-oi-call-ref` smoke compared 1/1 and found 1 raw mismatch with 0 validation/generator/property/command failures. That representative first exposed a size-losing OI-H parity gap: Binaryen directized the synthetic direct `ref.func`/`call_ref` wrappers to direct calls (0 `call_ref` in normalized WAT), while Starshine initially left 40 `call_ref` opcodes and 25 `ref.func` occurrences; Binaryen raw/canonical-sized output was 554 bytes vs Starshine raw output over 12KB. The raw skip path now has focused red-first coverage for adjacent `ref.func`, block-returned `ref.func`, constant-select `ref.func`, single-use `local.set(ref.func)` / `local.get`, guaranteed `ref.null; ref.as_non_null` trap tails, dropped result blocks whose body is already unreachable, dropped null `ref.test` / nullable `ref.cast`, and dropped non-null `ref.cast` trap tails; it applies those rewrites before returning a skipped function. The fresh `.tmp/oi-call-ref-target-wrapper-compare-smoke-ref-null-cast-trim2` smoke still mismatches, but the sampled OI-H target spelling and the sampled cross-family GC cast/test debris are gone: Starshine and Binaryen both have `call_ref=0`, `return_call_ref=0`, `table.get=0`, `call $5=20`, `ref.test=0`, `ref.cast=0`, `br_on_cast=0`, `br_on_cast_fail=0`, and five `unreachable` roots; both raw outputs validate. Starshine raw size is now 572 bytes vs Binaryen 554, down from the original >12KB and the prior 718-byte smoke. A grouped count-3 OI-H sweep at `.tmp/oi-h-target-wrapper-grouped-sweep-20260627` then compared `oi-call-ref-target-wrapper`, `oi-local-carried`, and `oi-effectful-sibling` cases: it still has three raw mismatches and no validation/generator/property/command failures, but every sampled Starshine and Binaryen output has `call_ref=0`, `return_call_ref=0`, and `table.get=0`. The runner now groups those unlabeled transformed cases as `transform:oi-call-ref-target-wrapper`, `transform:oi-local-carried`, and `transform:oi-effectful-sibling`; the matrix classifies them as local-section/dropped-`ref.func`, local-section/dropped-null, and effectful-if/scalar spelling drift rather than sampled OI-H directization misses. It is not yet a complete trigger-biased call_ref generator: existing-producer wrapping, table-state known-targets, `return_call_ref`, argument localization, multivalue arguments, and effect/trap target boundaries remain future OI-H work.

`oi-tuple-selected-lane` is now implemented as a smoke transform in `src/fuzz/metamorphic.mbt`: it appends a multivalue block type returning `i32`, `i64`, `f32`, and `f64`, adds scratch locals for those lanes to each defined function, and prepends stack-neutral multivalue block / lane-store / selected-lane `local.get` / `drop` shells for all four selected lanes. This gives OI-M sweeps a first machine-generated multivalue selected-lane surface while preserving source-visible semantics. A one-case `pass-oi-tuple` compare smoke with this transform compared 1/1 and found 1 raw mismatch with 0 validation/generator/property/command failures. The representative mismatch is now classified in the matrix: for the pure synthetic selected-lane shell, Binaryen scalarizes through straight-line locals while Starshine keeps nested block-result expressions, Starshine canonical wasm is 4100 bytes vs Binaryen 4326 bytes (-226), raw wasm is 3975 vs 4229 bytes (-254), and both outputs validate. This is a sampled Starshine-win candidate for the smoke shell, not OI-M semantic signoff. It remains smoke-only: it uses synthetic constants and scratch locals rather than wrapping existing tuple producers, selected trapping lanes, multi-result effectful siblings, or tuple-scratch localization boundaries found by the one-off OI-M research notes.

All first-layer OI-specific transform ids plus the follow-up live-nonzero OI-G transforms are now implemented and forwarded by `scripts/oi-parity-sweep.ts`: `oi-local-carried`, `oi-local-tee-wrapped`, `oi-commuted-operands`, `oi-if-select-shell`, `oi-effectful-sibling`, `oi-trapping-sibling`, `oi-call-ref-target-wrapper`, `oi-memory-size-boundary`, `oi-live-zero-memory-boundary`, `oi-live-nonzero-memory-copy-boundary`, `oi-live-nonzero-memory-mid-copy-boundary`, `oi-live-nonzero-memory-atomic-boundary`, `oi-live-nonzero-memory-fill-restore-boundary`, `oi-live-nonzero-memory-mid-fill-restore-boundary`, `oi-live-nonzero-memory-end-fill-restore-boundary`, and `oi-tuple-selected-lane`. Future design-only `oi-*` ids should still be filtered until their MoonBit transform exists. Forwarded ids appear as `--gen-valid-metamorphic-transform` arguments and can show up in the GenValid manifest `transform_id`, compare-pass `cases.jsonl`, and grouped sweep report.
