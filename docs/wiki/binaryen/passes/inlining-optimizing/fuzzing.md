---
kind: workflow
status: complete
last_reviewed: 2026-07-05
sources:
  - ../../../tooling/pass-fuzz-compare.md
  - ../../../raw/research/1551-2026-07-05-inlining-optimizing-o4z-audit.md
  - ../../../../../scripts/lib/pass-fuzz-compare-task.ts
---

# `inlining-optimizing` Fuzzing Profile

Recommended smoke lane: run the ordinary GenValid compare-pass lane for this pass:

```sh
moon build --target native --release src/cmd
bun scripts/pass-fuzz-compare.ts --count 1000 --seed 0x5eed --pass inlining-optimizing --normalize drop-consts --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-inlining-optimizing --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe
```

Use `_build/native/release/build/cmd/cmd.exe` after current Moon builds; older `target/native/...` paths are stale unless their timestamp/hash is explicitly proven fresh.

Performance timing fixture as of the 2026-07-05 1x-requirement correction: `.tmp/io-perf-20260705/measure_io_perf.py` generates inline-heavy helper-chain fixtures under `.tmp/io-perf-20260705/measurements/`, runs Starshine with `--tracing pass --inlining-optimizing`, runs Binaryen with `BINARYEN_PASS_DEBUG=1 wasm-opt --all-features --inlining-optimizing`, and compares pass-local medians over five repeats. The fixture sizes are `1`, `5`, `10`, `20`, `50`, and `100` helpers, each a private `(param i32) (result i32)` helper returning `local.get 0; i32.const 1; i32.add`, with exported `main` chaining helpers through an accumulator local. Post-fix `.tmp/io-perf-20260705/measurements/summary.json` medians meet the stricter user bar (`Starshine <= Binaryen`) at every size: `1` helper `0.286ms` vs `0.645170ms` (`0.443x`), `5` `0.450ms` vs `0.612569ms` (`0.735x`), `10` `0.549ms` vs `0.688842ms` (`0.797x`), `20` `0.791ms` vs `0.815382ms` (`0.970x`), `50` `1.671ms` vs `3.123630ms` (`0.535x`), and `100` `3.135ms` vs `12.754000ms` (`0.246x`). Before the repair, the same quick family exposed a nested-cleanup/OI cliff (`20` helpers ~`82ms` vs ~`0.93ms`; `50` helpers timed out after `>20s`). Reopen if a current native build regresses above `1x` on these fixture medians or if a broader IO artifact exposes another nested cleanup scaling cliff. A 2026-07-05 ordinary 1000-case attempt at `.tmp/pass-fuzz-inlining-optimizing-ordinary-1000-20260705` timed out after `900s` with only partial cases/failure dirs and no summary, so it is not evidence. The follow-up small ordinary lane `.tmp/pass-fuzz-inlining-optimizing-ordinary-20-20260705-triage` completed `20/20` with `20` raw mismatches, all from `binaryen-oracle-portable` no-call/unreachable inputs. Agent classification for that sampled family: Starshine-win / accepted cleanup drift, not an inlining parity bug. Binaryen leaves large no-effect dropped-expression prelude/debris when `--inlining-optimizing` finds no callsites; Starshine's early no-change path prunes it while preserving reachable trap/control behavior. Evidence: no calls or side effects in the manifest facts, all sampled artifacts validate, case 1 runtime replay under Node reports `outcome="all-equal"` with `equalTraps=1`, sampled cases 1-5 shrink from roughly `4.2-5.6KB` Binaryen outputs to `315-416` byte Starshine outputs, and downstream shared Binaryen `-Oz --strip-debug` converges sampled cases 1-5 to identical WAT with `39-42` byte wasm outputs. After `--no-reduce-mismatches` landed, `.tmp/pass-fuzz-inlining-optimizing-ordinary-1000-noreduce-allnorm-20260705` completed `1000/1000` with `1000` raw mismatches, zero cleanup-normalized matches even with all then-current cleanup normalizers, and zero generator/validation/property/command failures; manifest effect facts were still `hasCall=0`, no side effects, `hasUnreachable=1000`, and `mayTrap=1000`. A follow-up compare-normalizer slice added narrow coverage for pure dropped float/signed-conversion numeric debris and standalone empty `if (i32.const 0|1) (then) (else)` debris while preserving potentially trapping `trunc_*_s` drops. With those normalizers, `.tmp/pass-fuzz-inlining-optimizing-ordinary-1000-allnorm-purefloat-20260705` reached `1000/1000` compared with `1000` cleanup-normalized matches and zero mismatches/failures, and `.tmp/pass-fuzz-inlining-optimizing-ordinary-10000-allnorm-purefloat-20260705` reached `10000/10000` compared with `10000` cleanup-normalized matches, `0` mismatches, and zero generator/validation/property/command failures. Cache for the 10000 lane was Binaryen `1002` hits / `8998` misses. After the tail-call cleanup guard landed, the current regular GenValid final lane `.tmp/pass-fuzz-inlining-optimizing-ordinary-100000-allnorm-tailguard-20260705` completed `100000/100000` with `0` normalized matches, `100000` cleanup-normalized matches, `0` mismatches, zero generator/validation/property/command failures, zero command failures, selected profile `binaryen-oracle-portable=100000`, input facts `hasCall=0`, `hasUnreachable=100000`, `mayTrap=100000`, no side-effect/global/table/memory/exception/atomic facts, and Binaryen cache `100000` hits / `0` misses. This satisfies the regular GenValid count for the current binary, but it still mostly proves no-call cleanup-drift classification rather than inline-then-cleanup behavior. Keep ordinary lanes separate from the focused dedicated profile because this broad default profile currently over-samples no-call cleanup-only shapes.

Dedicated GenValid profile: **landed as an initial focused aggregate**. The canonical profile is `inlining-optimizing-all`; aliases are `inlining-optimizing`, `inlining-optimizing-closeout`, `inlining-optimizing-all-profiles`, `io`, and `io-closeout`. It samples four focused leaves that guarantee a private direct call/`return_call` inline opportunity followed by cleanup debris in the touched caller/helper path:

- `inlining-optimizing-direct-wrapper`;
- `inlining-optimizing-param-spill`;
- `inlining-optimizing-return-call`;
- `inlining-optimizing-cleanup-payoff`.

Do not replace this with a shallow aggregate over existing broad `pass-inlining`, `precompute-*`, `pass-cleanup`, or `simplify-locals-nostructure-*` leaves: the 2026-07-05 probe in `1551-2026-07-05-inlining-optimizing-o4z-audit.md` found that naive shape produced generator failures and mismatches, so it is not closeout evidence.

The focused profile reports manifest `config_label="inlining-optimizing-all"`, a focused `selected_profile` leaf, a non-null `profile_case_label`, and normal feature facts. Initial validation on 2026-07-05:

- emit-batch smoke `.tmp/inlining-optimizing-all-focused-smoke`: `8` generated inputs, all validate with `wasm-tools --features all`; manifest selected all four leaves and all four non-null profile-case labels.
- compare smoke `.tmp/pass-fuzz-inlining-optimizing-all-focused-20`: `20/20` compared, `20` normalized matches, `0` mismatches, `0` generator/validation/property/command failures.
- widened focused lane `.tmp/pass-fuzz-inlining-optimizing-all-focused-1000`: `1000/1000` compared, `1000` normalized matches, `0` mismatches, `0` generator/validation/property/command failures. Selected-profile counts: direct-wrapper `303`, param-spill `285`, return-call `213`, cleanup-payoff `199`; profile-case counts match the same four families.
- required pass-specific lane `.tmp/pass-fuzz-inlining-optimizing-all-focused-10000`: `10000/10000` compared, `10000` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` generator/validation/property/command failures, `0` command failures. Cache: Binaryen `10000` hits / `0` misses. Selected-profile counts: direct-wrapper `3019`, param-spill `2993`, return-call `1998`, cleanup-payoff `1990`; profile-case counts matched the same four families. Manifest feature facts showed `hasCall=10000`, no side-effect/global/table/memory/exception/atomic facts, no `hasUnreachable`, and no `mayTrap`.
- after the tail-call cleanup guard, `.tmp/pass-fuzz-inlining-optimizing-all-focused-10000-tailguard-20260705` repeated the required pass-specific lane on the current native binary: `10000/10000` compared, `10000` normalized matches, `0` cleanup-normalized matches, `0` mismatches, and zero generator/validation/property/command failures; cache was Binaryen `10000` hits / `0` misses.

Use this profile for the pass-specific lane. The 10000-case pass-specific lane satisfies the dedicated-profile portion of the v0.1.0 signoff ladder; the broader regular GenValid, wasm-smith, and random-all lanes are now classified below with reopening criteria rather than active INL closeout blockers.

Explicit wasm-smith lane status as of 2026-07-05 after the tail-call cleanup guard: `.tmp/pass-fuzz-inlining-optimizing-wasm-smith-10000-tailguard-20260705` requested `10000` cases at seed `0x5eed` with no compare normalizers and current `_build/native/release/build/cmd/cmd.exe`; it compared `9956/10000`, had `9953` normalized matches, `3` raw mismatches, `0` generator/validation/property failures, and `44` Binaryen/oracle command failures (`39` rec-group-zero, `3` bad-section-size, `1` table-index-out-of-range, `1` invalid-tag-index). Cache: wasm-smith `10000` hits / `0` misses; Binaryen `9956` hits / `0` misses; Binaryen-failure cache `44` hits / `0` misses. The three raw mismatches are unchanged (`case-003694`, `case-009332`, `case-009726`) and remain agent-classified as Starshine-win / accepted cleanup drift, not inlining semantic mismatches: the inputs have no calls or side effects, artifacts validate, Starshine removes no-effect local-set/get, pure const-drop, `nop`, or dead local-declaration debris that Binaryen leaves, and shared downstream Binaryen `-Oz --strip-debug --all-features` converges each sampled pair to identical 8-byte wasm in `.tmp/io-wasm-smith-tailguard-downstream-oz-20260705`. Two cases are also raw-smaller for Starshine; `case-003694` is raw-size equal, so the classification rests on no-effect cleanup and downstream convergence rather than size alone. A supplemental normalized replay of the previous same three cases, `.tmp/pass-fuzz-inlining-optimizing-wasm-smith-10000-normalized`, with `drop-consts` and `local-cleanup-debris` compared `9956/10000`, classified `2` cleanup-normalized matches, and left only the local-set/get const-return cleanup case as a raw mismatch.

Random-all lane status as of 2026-07-05: the current full `--gen-valid-profile random-all-profiles --count 10000 --seed 0x5555` lane is accepted for v0.1.0 as cleanup/representation drift after the exhaustive downstream convergence and runtime spot checks below; earlier full attempts before the tail-call guard were not closeout evidence. The unnormalized attempt `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-10000` timed out after `2400s` with only `101` case records and no `result.json`; every recorded case was a raw mismatch across broad non-IO profiles such as `ssa-nomerge-*`, `pass-fuzz-stress`, `binaryen-oracle-portable`, `coverage-forced-portable`, `local-subtyping-*`, `coalesce-locals-*`, and `heap2local-*`. Normalized triage attempts also timed out before summary (`.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-100-normalized-triage` after `900s`, `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-mismatch-cap20-triage` after `600s`) because every GenValid mismatch still paid byte-slice reducer cost. A tooling follow-up added compare-pass `--no-reduce-mismatches` for broad triage lanes, then `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-1000-noreduce-allnorm-20260705` completed and exposed four Starshine command aborts in coverage-forced portable cases. Those aborts were a true Starshine cleanup robustness bug: `reorder-locals` could abort on stale local declarations left by an invalid intermediate cleanup candidate before the inlining validate-or-fallback step. `src/passes/reorder_locals.mbt` now fails closed on out-of-range local accesses instead of aborting, with focused boundary coverage in `reorder_locals_test.mbt`; replays of cases `000289`, `000601`, `000713`, and `000809` now exit `0` and validate. The refreshed triage lane `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-1000-noreduce-allnorm-fixed1-20260705` completed `1000/1000`: `7` normalized matches, `105` cleanup-normalized matches, `888` raw mismatches, and zero generator/validation/property/command failures. After the pure-float/conversion and empty-const-if normalizer tightening, `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-1000-allnorm-purefloat-20260705` completed `1000/1000` with `7` normalized matches, `473` cleanup-normalized matches, `520` raw mismatches, and zero generator/validation/property/command failures. The previously concerning effectful/profile-broad groups normalized completely in that 1000-case sample (`coverage-forced-portable`, `pass-fuzz-stress`, `binaryen-oracle-portable`, `heap2local-array`, and `heap2local-ref` were all match/cleanup-normalized), but the first full 10000 all-normalizer run before the tail guard, `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-10000-allnorm-purefloat-fullmax-20260705`, exposed one true Starshine validation failure: `coverage-forced-portable case-004369` produced an invalid `return_call` operand stack (`expected f32, found i64`) after nested/local cleanup. This was fixed fail-closed by skipping nested/final local cleanup on modules containing tail calls until the tail-call/local-compaction interaction has a narrower type-aware repair; red replay `.tmp/pass-fuzz-inlining-optimizing-random-all-case-004369-validation-replay-fixed-20260705` still failed, while the final replay `.tmp/pass-fuzz-inlining-optimizing-random-all-case-004369-validation-replay-tailguard-20260705` compared `1/1` with `1` normalized match and zero failures. The current random-all 10000 lane `.tmp/pass-fuzz-inlining-optimizing-random-all-profiles-10000-allnorm-tailguard-20260705` completed `10000/10000`: `1250` normalized matches, `3490` cleanup-normalized matches, `5260` raw mismatches, and zero generator/validation/property/command failures. Cache: Binaryen `10000` hits / `0` misses. Remaining raw mismatches are local-allocation/renumbering families: `ssa-nomerge-parity=1250`, `ssa-nomerge-smoke=1250`, `local-subtyping-straight-line=689`, `coalesce-locals-straight-line=545`, `heap2local-struct=455`, `local-subtyping-structured=366`, `coalesce-locals-structured=355`, and `coalesce-locals-loop-copy-through=350`. A broader downstream sample `.tmp/io-random-all-tailguard-downstream-oz-sample50-20260705` took the first `50` raw mismatches from each of those eight remaining profiles (`400` total); every sampled Binaryen/Starshine raw pair converged to identical `wasm-opt --all-features -Oz --strip-debug` output. The exhaustive follow-up `.tmp/io-random-all-tailguard-downstream-oz-all-20260705` then checked all `5260` remaining raw mismatches through the same shared downstream optimizer, and all `5260/5260` converged to identical wasm. Per-profile convergence was complete: `ssa-nomerge-parity 1250/1250`, `ssa-nomerge-smoke 1250/1250`, `local-subtyping-straight-line 689/689`, `coalesce-locals-straight-line 545/545`, `heap2local-struct 455/455`, `local-subtyping-structured 366/366`, `coalesce-locals-structured 355/355`, and `coalesce-locals-loop-copy-through 350/350`. Downstream wasm sizes were stable at `35` bytes for `ssa-nomerge-*` and `34` bytes for the other six profiles, with zero downstream byte deltas. Most raw pairs are Starshine-smaller, but `207` local-subtyping cases (`129` straight-line, `78` structured) are raw-size-losing before downstream cleanup (`-20` bytes, all `hasException`/`mayTrap`), so this is not a blanket size-win classification. Six size-losing local-subtyping replays (`case-000085`, `000101`, `000205`, `000229`, `000237`, and `000253`) all ran under Node as `all-equal` with zero semantic mismatches. Agent classification for the random-all raw family is accepted cleanup/representation drift: all raw pairs downstream-converge under the same optimizer, representative effectful/trapping size-losing cases replay equal, and visible diffs are local-allocation/renumbering/dead-temp cleanup rather than missed inlining behavior. Reopen if a residual random-all raw mismatch fails validation, fails downstream `-Oz --strip-debug` convergence, produces a runtime semantic mismatch, loses size without downstream convergence or another measured win, introduces calls/memory/table/global/atomic side effects into the residual family, or reduces to a concrete inlining/touched-cleanup transform gap.

Representative random-all raw mismatch sampling on 2026-07-05 inspected the first remaining mismatch from each raw-mismatch selected profile: coverage-forced-portable `case-000001`, ssa-nomerge-parity `case-000002`, pass-fuzz-stress `case-000003`, binaryen-oracle-portable `case-000004`, local-subtyping-straight-line `case-000005`, heap2local-struct `case-000006`, ssa-nomerge-smoke `case-000007`, coalesce-locals-structured `case-000008`, coalesce-locals-straight-line `case-000024`, local-subtyping-structured `case-000077`, and coalesce-locals-loop-copy-through `case-000096`. All sampled Binaryen/Starshine raw and canonical outputs validate. Visible diffs are pure dropped-expression/control preludes, dead local declarations, no-effect local copy/drop chains, local-set/get temp elision around dropped GC allocation, heap2local self-copy/dead-local cleanup, and local renumbering. Starshine is smaller for every sampled raw/canonical pair, and shared downstream `wasm-opt --all-features -Oz --strip-debug` converged all 11 sampled pairs to identical wasm (`271`, `35`, `34`, `41`, or `34` bytes depending on the case). Node runtime replay is only partial evidence: `case-000002` and `case-000003` replayed `all-equal`, `case-000001` was runtime-unsupported because of imports, and `case-000004` was stopped by the thread timeout before producing evidence. Agent classification for these sampled representatives: Starshine-win / accepted cleanup drift. This sampled evidence was later superseded by the exhaustive downstream check of all `5260` remaining raw random-all mismatches above, so random-all is accepted for the current v0.1.0 INL scope with the stated reopening criteria.

As of 2026-07-05, the existing `pass-inlining` GenValid profile is intentionally a scalar direct-call smoke lane: it disables stringref const-expression and GC-ref surfaces so its generated inputs validate through the compare harness' `wasm-tools --features all` gate. Ref/GC survival coverage for `inlining-optimizing` should be added through focused future leaves, not by widening `pass-inlining` again.

Seed `0x1e11e` with `--gen-valid-profile pass-inlining` has one known direct raw mismatch in `.tmp/pass-fuzz-inlining-optimizing-pass-inlining-1-refsafe/failures/case-000001-gen-valid`. Classification: Starshine-win / accepted cleanup drift. Binaryen and Starshine both remove the direct calls; Starshine additionally deletes pure dropped arithmetic/constant-expression prelude and unreachable-tail debris that Binaryen leaves in `--inlining-optimizing` output. Evidence from `.tmp/pass-fuzz-inlining-optimizing-pass-inlining-1-runtime`: runtime execution checked one export with equal traps and zero semantic mismatches. Both raw/canonical outputs validate, Starshine is smaller before downstream cleanup (`326` vs `4246` canonical bytes), extra Binaryen cleanup reduces both functions to `unreachable` bodies plus local-declaration drift, and a shared downstream `-Oz --strip-debug` pass produces identical `59`-byte wasm with an empty WAT diff. Do not treat this single classified case as a 1000/10000 profile closeout; the dedicated aggregate still needs focused inline-then-cleanup leaves.
