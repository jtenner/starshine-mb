---
kind: concept
status: supported
last_reviewed: 2026-07-18
sources:
  - ./index.md
  - ../late-pipeline-dispatch.md
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/optimize_test.mbt
  - ../../../../../src/passes/trace_golden_test.mbt
  - ../../../../../src/passes/perf_test.mbt
  - ../../../../../src/passes/pass_manager_wbtest.mbt
  - ../../../../../src/cmd/cmd.mbt
  - ../../../../../src/cmd/cmd_wbtest.mbt
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./effect-pruning-and-traps-never-happen.md
  - ./wat-shapes.md
---

# Starshine HOT-IR Strategy For `vacuum`

Use this page together with [`./binaryen-strategy.md`](./binaryen-strategy.md) and [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md); their direct `version_129` source/test URLs and retained research provide the upstream provenance.
The goal here is not to re-explain upstream Binaryen, but to show exactly where the current MoonBit implementation lives, how the local HOT-plus-pipeline split is wired today, and why the current direct-pass / checked-ordered-neighborhood audit is considered closed.

## Exact local code map

The fastest read-along path through the current Starshine implementation is:

- registry descriptor, summary, and preset visibility in `src/passes/optimize.mbt`
  - hot-pass registration for `vacuum`
  - summary text: `Remove \`nop\` roots and region entries through hot IR cleanup.`
  - invalidated analyses list on the `HotPassDescriptor`
- main hot-pass dispatch in `src/passes/pass_manager.mbt`
  - the `"vacuum"` dispatch arm in the hot-pass runner
  - `hot_pass_remove_region_nops(...)`, which is the whole current rewrite engine
- writeback and debug-pipeline support in `src/passes/pass_manager.mbt`
  - the `descriptor_name == "vacuum"` writeback-validation guard path
  - the `run_hot_pipeline_debug_dump_enabled(...)` special-case target for traced debug dumps on the saved artifact hotspot function
- focused local proof surfaces
  - `src/passes/optimize_test.mbt` for the reduced semantic locks
  - `src/passes/trace_golden_test.mbt` for deterministic trace output
  - `src/passes/perf_test.mbt` for perf timer and unchanged-pass behavior
  - `src/passes/pass_manager_wbtest.mbt` for registry-copy safety and traced debug-dump routing
  - `src/cmd/cmd.mbt` for pass-note and flag-limit plumbing
  - `src/cmd/cmd_wbtest.mbt` for CLI flag resolution plus saved generated-artifact replay lanes

That exact code map is the main practical addition in this refresh: readers can now jump directly from the strategy summary to the owning files and evidence surfaces.

## Current local implementation

The current in-tree `vacuum` implementation is intentionally much smaller than upstream Binaryen, but it now includes the first Binaryen-parity cleanup slice beyond `nop` sweeping.
The rewrite logic is currently centered on two HOT helpers in `src/passes/pass_manager.mbt`:

- `hot_pass_vacuum_remove_local_only_void_body(ctx, func)`, which can canonicalize an otherwise side-effect-free/local-only void function body to Binaryen's single `nop` body when the body contains local-only tee/write/control debris and no calls, memory/table mutation, throwing/trapping operations, or externally observable state changes
- `hot_pass_remove_region_nops(ctx, func, region_ref)`, which handles the ordinary recursive region cleanup

The recursive region helper walks a region root list and now handles these cleanup families:

1. if the current root is `HotOp::Nop`, it splices that root out of the region and deletes the detached node
2. if the current root is `drop` of a removable nontrapping pure scalar/ref/tuple expression, it removes the dropped expression while preserving potentially trapping conversions such as non-saturating float-to-int truncations
3. if the current root is an empty `block` with zero result arity, it removes the block while preserving typed/result blocks
4. if the current root is a `block` whose only payload is `unreachable`, it unwraps the block to the payload `unreachable`
5. if the current root is a void `if` with a constant `i32.const` condition and the selected arm can be spliced without branches to the removed `if` label, it replaces the `if` with the selected arm
6. if the current root is a void `if` with an empty then arm and a live else arm, it moves the else payload into a one-armed then arm and wraps the condition in `i32.eqz`
7. if the current root is a label-unused void block whose only HOT root is an `if`, it unwraps the block and splices the `if` into the parent region

The hot-pipeline writeback path also has a `vacuum`-specific empty-function canonicalization shim: if an otherwise unchanged or lowered `vacuum` body is empty, Starshine emits Binaryen's single `nop` function body rather than serializing an empty expression list. The local-only void-body cleanup intentionally feeds that canonicalization shape for functions where local writes, tees, and local-control flow have no observable effect.

Then, if the current root owns nested regions, the helper recurses into them. For small functions it also traverses nested value-expression children so RSE-exposed pure debris and empty-arm shapes inside value-producing controls are cleaned before lowering.

For large lowered functions, `src/passes/pass_manager.mbt` now runs a raw `vacuum` precleaner before HOT lift when the lowered instruction count exceeds the nested-child cleanup budget. That precleaner recursively removes lowered `nop`s and pure dropped stack expressions, including scalar/ref constants, local/global reads, `memory.size`, `table.size`, nontrapping unary/binary/compare/select expressions, sign-extension operators, nontrapping conversions, and constant-denominator integer div/rem cases that cannot trap. It also collapses void `if` scaffolds with an immediately preceding constant `i32` condition when the selected lowered arm does not branch to the removed `if` label or an outer label that would need depth rebasing. The raw precleaner now also unwraps void blocks whose lowered body is an `if` wrapper; it refuses exact wrapper-label targets, rebases branches that target outer labels after the removed block, and leaves unreachable-containing wrappers alone so terminal-unreachable cleanup does not accidentally erase suffix calls that other pass tests intentionally preserve. The same raw cleanup now runs for guarded call/local-set stack hazards before the HOT lift, so Starshine can still remove Binaryen-style pure debris without re-enabling the HOT lowering reassociation family that `[JSON-AS]002` exposed. Small functions also take the raw preclean path when they contain dense `nop` debris or branchy structured-write `nop` debris, avoiding pathological HOT-region per-`nop` mutation on current generated `-O4z` slot predecessors. Small terminal-`unreachable` functions take the raw path when a hard `unreachable` has immediately preceding scalar/nop/empty-try_table/typed-block-unreachable debris that HOT lowering would otherwise preserve; the cleanup deliberately leaves SIMD expression chains before `unreachable` alone until there is a source-backed Binaryen parity case for that wider surface. This keeps candidate-free functions on the existing `no-vacuum-candidates` raw skip path while letting large RSE-produced pure debris, generated branch scaffolds, guarded call/local-set hazard functions, wasm-smith terminal-unreachable debris, ordered-prefix `nop` debris, ordered if-wrapper debris, and the narrow singleton-parent call-prefix/`if` wrapper shrink without paying for broad HOT child traversal. A 2026-06-30 source-backed probe confirmed Binaryen sometimes flattens a structural wrapper around a call/local-set prefix and trailing `if`, but a broad raw implementation was rejected because it made current slot23 mismatch earlier (`defined=15 abs=42`) and size-losing. A later parent-context attempt also failed: one version advanced past `defined=840 abs=867` but over-unwrapped the same family in `defined=854 abs=881`, and the tightened singleton-parent version again regressed to `defined=15 abs=42`. The landed singleton call-prefix rule is narrower than those reverted attempts: it only splices a void lowered wrapper that is the sole instruction in its parent, begins with a void prefix block, ends in an `if`, contains no `unreachable`, and has no exact wrapper-label target. A follow-up landed a similarly narrow terminal-`unreachable` wrapper rule for the current slot23 `defined=15 abs=42` family: when a void lowered block's cleaned body is exactly a void child block followed by `unreachable`, and no branch targets the removed wrapper label, raw `vacuum` splices the two instructions and rebases outer branch depths. The next current slot23 reduction added raw lowered void-`if` cleanup that mirrors the HOT empty-then/live-else rule, drops else arms that clean to empty, and unwraps single-`if` void block wrappers containing `unreachable` when no branch targets the removed wrapper label; call/copy/fill prefixes are deliberately excluded from that unreachable-wrapper helper because prior broad call-prefix experiments over-unwrapped neighboring slot23 functions. A follow-up very narrow call-condition rule unwraps the same single-`if`/`unreachable` wrapper only when the condition prefix is local/constants plus one direct `call` feeding a `local.tee`, again with no exact wrapper-label target; it does not reopen arbitrary call/copy/fill prefixes. The next recursive call-prefix rule is similarly constrained: it only splices a void wrapper whose original body was exactly one child block after cleaning exposes a void prefix block, a condition prefix with exactly one direct call, and a trailing `if`, with no `unreachable` and no exact wrapper-label target. A broader post-recursive version over-unwrapped the current slot23 `defined=840 abs=867` family, so the original-single-child and one-direct-call guards are part of the contract. Residual structural-wrapper cleanup still needs a stronger control-shape model than broad top-level or immediate-parent flattening. A later side-effect singleton-child experiment, captured at `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-sideeffect-child-guarded-final`, confirmed that Binaryen can flatten one nested call/local-set/`if` wrapper but also showed that Starshine cannot safely land that broader family yet: the guarded raw implementation failed to remove the current `defined=4481 abs=4508` residual, increased writeback repairs from seven to fourteen functions, and regressed accepted slot23 sizes; an unguarded variant over-unwrapped outer structure. The landed follow-up is narrower: it only unwraps the current child block when both the original and cleaned bodies are side-effect call/local-set prefixes ending in a void `if`, without stores/copy/fill/control prefixes, `unreachable`, exact wrapper-label targets, or the local-set-leading/call-result sibling family that Binaryen preserves. Follow-up inspection of the `...-sideeffect-child-final2` line-`18765` residual found a contradictory load-leading sibling: the slot23 Binaryen output removes one wrapper around a load-leading local-set / side-effect-call / call-result local-set prefix, but reduced probes under `.tmp/vacuum-probes/load-leading-sideeffect-prefix-block-*` show Binaryen direct `--vacuum` preserving superficially similar wrappers while Starshine removes them. A current-head rerun then showed the line-`18765` wrapper itself is no longer the active first diff under the rebuilt binary: `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-current-rerun` still differs at `defined=4481 abs=4508`, but first at normalized-WAT line `24763`, where Starshine over-flattens a Binaryen-preserved local-set-leading prefix block before a void `if`. Reduced probe `.tmp/vacuum-probes/local-set-leading-prefix-block-before-if.wat` reproduced that Binaryen-preserve / Starshine-remove direction, and the follow-up local-set-leading prefix preservation rule now keeps the narrow `local.get; call; local.set; local.get; call` prefix block when it precedes a call-conditioned void `if` with either an else arm or a top-level outer branch. The reduced probe now matches Binaryen, and the full slot23 first diff advanced to line `26609`, but ordered parity remains open on a larger Binaryen-preserved prefix wrapper. A subsequent load-leading multi-local preservation experiment reproduced the line-`26609` Binaryen-preserve direction in `.tmp/vacuum-probes/multi-local-prefix-child-block-before-if.wat`, but it regressed full slot23 to earlier line-`18762` / line-`25763` contradictions and worsened accepted slot23 raw/canonicalized sizes; a branch-tail reduction also showed Binaryen preserving a superficially similar wrapper while full slot23 flattened its sibling. A parent/continuation audit narrowed the full-slot contrast but did not prove a safe rule: line `26609` is a two-level `block $block225 -> anonymous child` shape whose child branches directly to `$block225`, while the rejected line-`18762` and line-`25763` siblings are three-level `block $outer -> block $middle -> anonymous child` diamonds where the child branches to `$middle`, the middle fallthrough sets a carrier and branches to `$outer`, and the middle/outer continuations perform different allocation or control tails. That distinction is not sufficient because `.tmp/vacuum-probes/multi-local-prefix-child-block-before-branch-tail.wat` has the same middle-wrapper pattern yet Binaryen preserves it. Follow-up probe `.tmp/vacuum-probes/multi-local-prefix-extra-inner-before-if-chain.wat` added the line-`25763` middle break allocation/store/return and outer `local.set(call ...)` plus void `if` chain to that family, with the extra inner prefix wrapper that the rejected guard preserved. The landed paired rule now keeps the load-leading multi-local prefix child only when its parent continuation has a call/store/return tail, which closes the positive reduced probe `.tmp/vacuum-probes/multi-local-prefix-child-block-before-if.wat`, while the rebuilt extra-inner probe remains flattened apart from label names. Full slot23 evidence `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-multilocal-prefix-pair2` advances the active residual from line `26609` to line `26999`, but ordered parity remains open and is not a Starshine-win classification. Follow-up inspection found that the line-`26999` hunk is an extra Starshine anonymous wrapper around a local-set-leading call/release prefix plus trailing void `if`; hand-written local-set-leading middle/extra-wrapper probes did not reproduce the mismatch after canonicalization, and a bounded `wasm-reduce` pass produced `.tmp/vacuum-probes/line26999-reduced-working/input.wasm` (`27266` bytes) that still has an extra-wrapper mismatch but no longer preserves the exact first hunk. A tighter reducer script later rejected that 27K drift and produced `.tmp/vacuum-reduce-line26999-strict/working.wasm` (`46637` bytes), whose first hunk still has the desired `local.set(call ...)` / release-call / `if(call(local.tee ...))` shape; smaller 6K continuations drifted again by allowing `local.set` of a constant plus a sibling call, so the next reducer must require the changed `local.set` to consume the call directly before it can supply red-first coverage. A follow-up v4 reducer added that direct-call child constraint and rejected the prior constant-local-set drift, but its 6759-byte output still drifted toward a terminal-unreachable-adjacent shape. A v5 reducer also rejected first-hunk `unreachable` and produced `.tmp/vacuum-reduce-line26999-v5/working.wasm` (`6135` bytes), which was manually minimized to `.tmp/vacuum-probes/line26999-v5-input-shaped.wat`: Binaryen flattens the local-set-call/release prefix wrapper after branchy-`if` cleanup while Starshine keeps one extra wrapper. That v5 probe is now covered by `src/passes/optimize_test.mbt::vacuum raw-unwraps branchy local-set-call prefix parent wrappers`, paired with the earlier Binaryen-preserved `.tmp/vacuum-probes/local-set-leading-prefix-block-before-if.wat` negative. The landed rule only removes the parent wrapper when the original single-child wrapper contained a branchy else-to-outer `if` and cleanup exposes a direct-call-fed `local.set`, later release call, and call-conditioned final `if`; the positive probe normalizes under `.tmp/vacuum-probes/current-line26999-v5-input-shaped-after-branchy-localset` and the negative remains normalized under `.tmp/vacuum-probes/current-localset-prefix-after-branchy-localset`. Full slot23 still differs at `defined=4481 abs=4508`, now first at line `29378`, so the remaining empty-block/call-prefix wrapper is still an open parity gap. Further widening or tightening still needs branch/result-stack proof, parent/continuation-shape proof, and writeback-validity evidence.

The current line-`29378` empty-block/call-prefix residual remains open, but the safe leading-empty-block subset has landed. A 2026-06-30 broad experiment proved the isolated positive shape with `.tmp/vacuum-probes/empty-block-call-prefix-double-wrapper-before-if.wat`, but regressed full slot23 to `defined=4370 abs=4397` and worsened accepted slot23 raw/canonicalized sizes, so it was reverted. The landed replacement only removes leading empty child blocks from call-prefix/`if` wrappers and unwraps singleton parent wrappers when the original singleton chain contains that empty-block descendant; it is paired with a `defined=4370`-style negative that keeps single-`if` wrappers without leading empty prefixes flattenable. Full slot23 now stays at `defined=4481 abs=4508` and improves sizes slightly, but still has an extra wrapper at line `29378`. The follow-up no-leading-empty artifact-continuation probe `.tmp/vacuum-probes/artifact-continuation-call-prefix-before-if-noempty.wat` showed the sibling direction: Binaryen preserves one inner call-prefix/`if` block before a `$block256`-style `local.set` plus outer-`br` continuation, while Starshine over-flattened it. The landed raw rule removes only that singleton parent wrapper when the original body is one call-prefix/`if` child and the parent has the local-set/outer-branch continuation, preserving the inner block and normalizing the reduced probe exactly. This does not close full slot23 because the active full residual is still the leading-empty-derived extra wrapper. A later reduction-only slice confirmed that simplified leading-empty `$block256` continuation probes normalize exactly under current Starshine/Binaryen (`.tmp/vacuum-probes/artifact-continuation-call-prefix-before-if-leadingempty.wat` and stack-shaped `...-stack-leadingempty.wat`), so the remaining full hunk depends on context not captured by those hand probes. The new `.tmp/vacuum-reduce-line29378-interesting.sh` predicate accepts the original slot23 predecessor by requiring the Binaryen-direct-call / Starshine-extra-block hunk with local-set/outer-branch continuation and a first removed significant `(call ...)`, but a 200-attempt shrink only reached a multi-megabyte `.tmp/vacuum-reduce-line29378/working.wasm`. A follow-up tightened the reduction target with `.tmp/vacuum-reduce-line29378-callprefix-interesting.sh` after discovering the old predicate first matched an earlier generic call/if wrapper in the whole-module canonical diff. The tighter predicate requires the removed call-prefix to begin with the line-`29378`-style `i32.const 0`, `i32.const 0`, `i32.sub(..., 1)` argument pattern before the trailing `if`; it accepts the original and the prior partial reduction, but a 300-attempt continuation from the partial still timed out at `.tmp/vacuum-reduce-line29378-callprefix/working.wasm` (`3930445` bytes), so it is not a usable fixture. The latest reduction-only path extracts the owning function first: `wasm-opt --extract-function-index=4508` cuts the seed to `.tmp/vacuum-reduce-line29378-extract/func4508.wasm` (`272827` bytes), which the call-prefix predicate accepts. Shrinking that extracted seed with the call-prefix predicate timed out at `133426` bytes but allowed earlier drift hunks; a stricter `.tmp/vacuum-reduce-line29378-extract-firsttarget-interesting.sh` now requires the first diff hunk to be the line-`29378` call-prefix/extra-wrapper target, accepts the extracted seed, rejects the `133426`-byte drift, and first reached `.tmp/vacuum-reduce-line29378-extract-firsttarget/working.wasm` (`131138` bytes). Continuing with that first-target predicate reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-cont/working.wasm` (`53741` bytes) and `.tmp/vacuum-reduce-line29378-extract-firsttarget-cont2/working.wasm` (`44420` bytes). A forced Binaryen `wasm-reduce` run found `.tmp/vacuum-reduce-line29378-binaryen-reduce/working.wasm` (`28121` bytes) but drifted into a later huge `unreachable` hunk; `.tmp/vacuum-reduce-line29378-extract-firsttarget-nodrift-interesting.sh` rejects that drift and accepts the `44420`-byte seed. Under the no-drift predicate, `wasm-tools shrink` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-nodrift/working.wasm` (`43616` bytes), and a forced Binaryen `wasm-reduce` run reached `.tmp/vacuum-reduce-line29378-binaryen-nodrift/working.wasm` (`26512` bytes) but with many smaller structural drift hunks. The next tighter predicates reject those drifts: `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight-interesting.sh` requires exactly one leading Starshine wrapper before the first call-prefix/`if` target and reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight/working.wasm` (`43389` bytes), while `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight2-interesting.sh` also rejects insertion-only and non-wrapper later hunks and reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight2/working.wasm` (`41291` bytes, `15` wrapper-shaped hunks). A forced `wasm-reduce` under the first tight predicate reached `26398` bytes but `tight2` rejects it as too-many-hunk structural drift. Follow-up predicates `...tight3-interesting.sh` and `...tight4-interesting.sh` keep the same first-target call-prefix and continuation requirements while lowering the hunk cap from `15` to `12`; they reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight3/working.wasm` (`39315` bytes, `12` hunks) and `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight4/working.wasm` (`38869` bytes, `12` hunks). Follow-up `...tight5-interesting.sh` and `...tight6-interesting.sh` keep the same first target while lowering allowed changed hunk sizes from `90` to `80` minus/plus lines; they reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight5/working.wasm` (`37937` bytes, `12` hunks) and `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight6/working2.wasm` (`37586` bytes, `12` hunks), with `tight6` rejecting the older 38KB `tight4` seed as later-hunk drift. Follow-up `...tight7-interesting.sh`, `...tight8-interesting.sh`, and `...tight9-interesting.sh` lower that cap through `78`, `75`, and `70`, rejecting the older seeds as drift once tighter outputs exist; shrink continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight7/working.wasm` (`34438` bytes, `12` hunks), `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight8/working2.wasm` (`32594` bytes, `12` hunks), and `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight9/working.wasm` (`31293` bytes, `10` hunks). `...tight10-interesting.sh` lowers the hunk cap to `10`; continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight10/working2.wasm` (`30059` bytes, `10` hunks, max changed hunk `59`/`61`). `...tight11-interesting.sh` then lowers the changed-hunk cap to `62`, rejects the older 31KB/30KB/34KB/26KB drift seeds, and reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight11/working2.wasm` (`29589` bytes, `10` hunks, max changed hunk `59`/`61`). Continuing with `tight11` naturally dropped one hunk at `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight11-cont2/working.wasm` (`28775` bytes, `9` hunks), enabling `...tight12-interesting.sh` to lower the hunk cap to `9`; `tight12` continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight12-cont2/working.wasm` (`28161` bytes, `9` hunks, max changed hunk `59`/`61`) while rejecting the older 10-hunk and 26KB drift seeds. Further `tight12` continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight12-cont3/working.wasm` (`27919` bytes) and `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight12-cont4/working.wasm` (`27024` bytes, still `9` hunks, max changed hunk `59`/`61`). Follow-up `...tight13-interesting.sh` tightens only the first target hunk to at most `39` changed minus/plus lines, accepts the 27024-byte seed, rejects the older `tight12`/`tight11` seeds and known 26KB drift, and reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight13/working.wasm` (`27010` bytes, `9` hunks). A further `tight13` continuation reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight13-cont/working.wasm` (`26511` bytes, `9` hunks), naturally dropping the later maximum changed hunk from `59`/`61` to `52`/`54`; `...tight14-interesting.sh` lowers that later-hunk cap to `54`, rejects the older `tight13` / `tight12` / 26KB drift seeds, and continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight14-cont2/working.wasm` (`26250` bytes, `9` hunks). Follow-up `tight14` continuations with seeds `29412` and `29413` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight14-cont4/working.wasm` (`26111` bytes, `9` hunks), naturally lowering the first target hunk to `34`/`36` changed lines. `...tight15-interesting.sh` lowers the first-hunk cap to `36`, rejects the older `tight14-cont2` first-hunk drift, and `tight15` continuations with seeds `29414`, `29415`, and `29416` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont2/working.wasm` (`25673` bytes, `9` hunks, max changed hunk `52`/`54`). Further `tight15` continuations reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont3/working.wasm` (`25567` bytes) with seed `29417`; seed `29418` found no smaller written output; and seeds `29419` plus `29420` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont5/working.wasm` (`25444` bytes, still `9` hunks, first hunk `34`/`36`, max changed hunk `52`/`54`). The next `tight15` continuation accidentally had to be rerun after creating the output directory; the deterministic rerun with seed `29421` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont6/working.wasm` (`25418` bytes). Further seeds `29422`, `29423`, and `29424` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont9/working.wasm` (`25367` bytes), naturally shrinking later hunks `3` and `9` but leaving the global caps unchanged. Seed `29425` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont10/working.wasm` (`25316` bytes, still `9` hunks, first hunk `34`/`36`, max changed hunk `52`/`54`). Further `tight15` continuations with seeds `29426`, `29428`, `29429`, `29430`, and `29431` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont15/working.wasm` (`25230` bytes); seed `29427` wrote no accepted smaller output. Seeds `29432`, `29433`, `29434`, `29435`, and `29436` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont20/working.wasm` (`25173` bytes). A later slice continued the same `tight15` predicate through seed `29450`: seeds `29437`, `29438`, and `29439` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont23/working.wasm` (`25138` bytes); seed `29440` wrote no accepted output; seeds `29441`, `29442`, `29443`, and `29444` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont28/working.wasm` (`25122` bytes); seeds `29445`, `29447`, and `29448` wrote no accepted output; and seeds `29446`, `29449`, and `29450` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont34/working.wasm` (`25111` bytes), still with `9` wrapper-shaped hunks, first hunk `34`/`36`, and max changed hunk `52`/`54`. The next reduction-only slices continued from that seed: `tight15` seeds `29451` through `29475` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight15-cont59/working.wasm` (`24971` bytes), naturally lowering the later changed-hunk maximum to `46`/`48`; new `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight16-interesting.sh` lowers the later-hunk cap to `48`, accepts `tight15-cont59`, and rejects the older `tight15-cont34` seed. `tight16` continuations with seeds `29476` through `29575` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight16-cont100/working.wasm` (`24502` bytes), naturally lowering the later changed-hunk maximum to `44`/`46`; `...tight17-interesting.sh` lowers the later-hunk cap to `46`, accepts `tight16-cont100`, and rejects older `tight16-cont79` drift. `tight17` continuations with seeds `29576` through `29595` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight17-cont20/working.wasm` (`24379` bytes), naturally lowering the later changed-hunk maximum to `38`/`40`; `...tight18-interesting.sh` lowers the cap to `40`, accepts `tight17-cont20`, and rejects `tight16-cont100`. `tight18` continuations with seeds `29596` through `29825` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight18-cont229/working.wasm` (`23985` bytes), naturally lowering the later changed-hunk maximum from `38`/`40` to `37`/`39`. New `...tight19-interesting.sh` lowers the later-hunk cap to `39`, accepts `tight18-cont229`, and rejects `tight18-cont180` as later-hunk drift. `tight19` continuations with seeds `29826` through `29865` reached `.tmp/vacuum-reduce-line29378-extract-firsttarget-tight19-cont39/working.wasm` (`23949` bytes). The latest seed still has `9` wrapper-shaped hunks, first hunk `34`/`36`, max changed hunk `37`/`39`, first target `(call $fimport$4 => (block)`, and later wrapper-shaped `local.set` starts `$1660`, `$2748`, `$2751`, `$2754`, `$2757`, `$2763`, `$2766`, and `$2769`, so no lower hunk-count or first-hunk cap is justified yet. A follow-up manual extraction of the first reduced hunk produced `.tmp/vacuum-probes/line29378-firsthunk-manual.wat`, where Binaryen removes a parent wrapper when the original leading-empty call-prefix block is followed only by removable `nop` debris before the continuation. That is now covered by `vacuum raw-unwraps leading-empty call-prefix continuation wrappers`; the implementation lets the singleton empty-block call-prefix descendant guard ignore `nop` debris in the original parent. The probe now normalizes except for import/function names, and direct smoke `.tmp/pass-fuzz-vacuum-1000-after-leading-empty-trailing-nop` compared `1000/1000` with `1000` normalized and `0` failures. Full slot23 `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-leading-empty-trailing-nop` remains unchanged at `defined=4481 abs=4508` line `29378`, raw/canonical Starshine sizes `3098654`/`3113632` vs Binaryen `3113007`, so the active full hunk still depends on the `$block256` local-set/branch continuation rather than only trailing `nop` debris. After that fix, the old `23949`-byte `tight19-cont39` reduction no longer accepts the first-target predicate under current Starshine because its first hunk was the closed trailing-`nop` subfamily. The current active residual reduction therefore restarts from older accepted seeds: under the current binary, `tight6/working2.wasm` still accepts, and new continuations reached `.tmp/vacuum-reduce-line29378-current-tight14-cont-30016/working.wasm` (`27526` bytes, `8` wrapper-shaped hunks, first hunk `37`/`39`, max changed hunk `51`/`53`) after seeds `30000`-`30016`. A further current-binary reduction-only slice kept the active path on an 8-hunk predicate: `tight14` seeds `30017`-`30031` reached `.tmp/vacuum-reduce-line29378-current-tight14-cont-30031/working.wasm` (`26672` bytes, later max `50`/`52`); a smaller 9-hunk branch at `current-tight15-cont-30040` was rejected; `current-tight15h8` seeds `30041`-`30047` reached `.tmp/vacuum-reduce-line29378-current-tight15h8-cont-30047/working.wasm` (`26341` bytes, later max `44`/`46`); and `current-tight16h8` seeds `30049`-`30064` reached `.tmp/vacuum-reduce-line29378-current-tight16h8-cont-30064/working.wasm` (`25721` bytes, still `8` hunks, first hunk `37`/`39`, max changed hunk `44`/`46`). The next reduction-only continuation used `current-tight16h8` seeds `30065`-`30125` to reach `.tmp/vacuum-reduce-line29378-current-tight16h8-cont-30125/working.wasm` (`25099` bytes), naturally shrinking the first hunk to `32`/`34`; new `current-tight17h8` lowers the first-hunk cap to `34` and seeds `30126`-`30155` reached `.tmp/vacuum-reduce-line29378-current-tight17h8-cont-30155/working.wasm` (`24936` bytes), naturally lowering the later max to `42`/`44`; new `current-tight18h8` lowers the later cap to `44` and seeds `30156`-`30165` reached `.tmp/vacuum-reduce-line29378-current-tight18h8-cont-30165/working.wasm` (`24871` bytes, still `8` hunks: first `(call $fimport$6 => (block)`, then local-set starts `$1660`, `$2748`, `$2751`, `$2757`, `$2763`, `$2766`, and `$2769`). The next continuation used `current-tight18h8` seeds `30166`-`30250` to reach `.tmp/vacuum-reduce-line29378-current-tight18h8-cont-30250/working.wasm` (`24697` bytes), naturally lowering the later maximum to `34`/`36`; new `current-tight19h8` lowers the later cap to `36`, accepts that seed, rejects `current-tight18h8-cont-30220` as later-hunk drift, and seeds `30251`-`30270` reached `.tmp/vacuum-reduce-line29378-current-tight19h8-cont-30270/working.wasm` (`24672` bytes). The active seed still has `8` wrapper-shaped hunks, first hunk `32`/`34`, and later hunks capped at `36`. Do not revive broader call-prefix wrapper cleanup without an artifact-shaped parent/control discriminator, especially the `$block256` continuation shape, that preserves the accepted `defined=4481` progress and avoids the `defined=4370` regression; the 24.7KB current reduction is still a seed for future reduction, not focused coverage. A further 2026-07-01 reduction-only continuation used `current-tight19h8` seeds `30271`-`30400` to reach `.tmp/vacuum-reduce-line29378-current-tight19h8-cont-30400/working.wasm` (`24478` bytes), naturally shrinking the first hunk to `29`/`31`; new `.tmp/vacuum-reduce-line29378-current-tight20h8-interesting.sh` lowers the first-hunk cap to `31`, accepts that seed, and rejects `current-tight19h8-cont-30350` as first-hunk drift. `current-tight20h8` seeds `30401`-`30440` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-30434/working.wasm` (`24435` bytes), still `8` wrapper-shaped hunks with later hunks capped at `36`. A further `current-tight20h8` continuation with seeds `30441`-`30700` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-30696/working.wasm` (`24149` bytes), still with `8` wrapper-shaped hunks, first hunk `29`/`31`, and later hunks capped at `36`. Continuing the same predicate with seeds `30701`-`30920` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-30913/working.wasm` (`23997` bytes), still `8` wrapper-shaped hunks. Further `current-tight20h8` continuations with seeds `30921`-`31600` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-31597/working.wasm` (`23733` bytes), still with hunk starts `(call $fimport$4)`, `$1660`, `$2748`, `$2751`, `$2757`, `$2763`, `$2766`, and `$2769`; hunk sizes are `29`/`31`, `34`/`36`, `10`/`22`, `18`/`20`, `20`/`22`, `29`/`31`, `23`/`35`, and `31`/`33`. `current-tight20h8` continuations with seeds `31601`-`33000` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-32987/working.wasm` (`23453` bytes), still with `8` wrapper-shaped hunks and starts `(call $fimport$4)`, `$1660`, `$2748`, `$2751`, `$2757`, `$2763`, `$2766`, and `$2769`; hunk sizes are now `29`/`31`, `34`/`36`, `10`/`22`, `9`/`21`, `20`/`22`, `29`/`31`, `23`/`35`, and `20`/`22`. The `current-tight20h8` continuations with seeds `33001`-`33753` reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-33753/working.wasm` (`23387` bytes), preserving the same `8` hunk starts and hunk sizes. The `current-tight20h8` continuations attempted seeds `33754`-`34054` and `34055`-`34357`, reaching `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-34035/working.wasm` (`23363` bytes) and then `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-34319/working.wasm` (`23344` bytes). The latest `current-tight20h8` continuations attempted seeds `34320`-`34620` and `34620`-`34914`, reaching `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-34561/working.wasm` (`23334` bytes) and then `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-34906/working.wasm` (`23313` bytes). Further `current-tight20h8` continuations attempted seeds `34914`-`35225` and `35225`-`35537`, reaching `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-35223/working.wasm` (`23306` bytes) and then `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-35471/working.wasm` (`23289` bytes). The latest `current-tight20h8` continuations attempted seeds `35537`-`35848`, `35848`-`36033`, `36033`-`36350`, and `36351`-`36568`, reaching `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-35839/working.wasm` (`23281` bytes), `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-35925/working.wasm` (`23276` bytes), `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-36230/working.wasm` (`23273` bytes), and then `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-36366/working.wasm` (`23270` bytes). Later `current-tight20h8` continuations reached `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-37544/working.wasm` (`23233` bytes) and then `.tmp/vacuum-reduce-line29378-current-tight20h8-cont-38054/working.wasm` (`23204` bytes) after seeds `37551`-`38350`. Follow-up one-attempt loops over seeds `38351`-`39550` and an 80-attempt probe at seed `38351` found no smaller output, so the current path appears locally stalled. The latest active seed still accepts and has `8` wrapper-shaped hunks with starts `(call $fimport$4)`, `$1660`, `$2748`, `$2751`, `$2757`, `$2763`, `$2766`, and `$2769`; hunk sizes remain `29`/`31`, `34`/`36`, `10`/`22`, `9`/`21`, `20`/`22`, `19`/`21`, `23`/`35`, and `20`/`22`. No lower hunk-count, first-hunk, or later-hunk cap is justified yet because the first and second hunks still hit the current caps. The 23204-byte seed is the current active reduction seed, not focused coverage; next work should likely pivot to manual extraction of the first hunk while preserving the `$block256`-style continuation and existing negatives.

The 2026-07-01 binary-lowered first-hunk fixture narrows that open line-`29378` family: `src/passes/optimize_test.mbt::vacuum raw-unwraps binary-lowered line29378 continuation parent wrapper` embeds the reduced binary shape because WAT parsing rebuilds a higher-level call/`if` that does not preserve the extra wrapper. The fix unwraps a post-`nop` singleton child block around a call-prefix/`if` only when no branch targets the removed child label. The hand probe `.tmp/vacuum-probes/line29378-firsthunk-block256-manual.wat` now normalizes exactly and the old 23,204-byte reduction predicate rejects as `first-huge-drift`, leaving seven local-set wrapper hunks in that reduced seed. Full slot23 remains open at `defined=4481 abs=4508`; the latest compare `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-line29378-binarylowered` improved raw/canonical Starshine sizes to `3098609`/`3113587` but still differs from Binaryen, so the residual is still a parity gap and not a Starshine-win classification.

The follow-up no-`nop` line-`29378` fixture, `src/passes/optimize_test.mbt::vacuum raw-unwraps no-nop line29378 refcount call-prefix parent wrapper`, captures the remaining full-slot first hunk more directly: the singleton child block begins with refcount/load/local-set cleanup, contains an early non-branching refcount `if`, then a call-prefix and final branchy `if` before the parent continuation. The raw precleaner now unwraps that child only under a narrow refcount-call-prefix guard that rejects `unreachable` and label-targeting branches. This moves full slot23 past `defined=4481 abs=4508`; `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-after-refcount-wrapper` now first differs at `defined=5022 abs=5049`. That new residual is the opposite direction, where Binaryen preserves a prefix child block that Starshine flattens, so it remains an open parity gap and not a Starshine-win classification. A 2026-07-01 follow-up confirmed the visible shape as a five-argument `local.set(call ...)` plus three release calls before a branchy void `if`, but a first generalized preservation attempt regressed full slot23 to `defined=92 abs=119` by over-preserving an earlier wrapper; the experiment was reverted and recorded in `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-reverted-multiarg-attempt`. The reduced binary-lowered 3KB seed is now closed by a narrower discriminator in `src/passes/pass_manager.mbt`: it preserves only the observed five-operand direct-call local-set plus three release-call pairs when followed by either the reduced `i32.const; call; if` condition or the full `local.get; local.tee; call; i32.const; i32.eq; if` condition. That makes `.tmp/vacuum-reduce-defined5022-v4-final-cont/working-v4-final-timeout-3k.wasm` normalize exactly under `.tmp/vacuum-reduce-defined5022-v4-final-cont/compare-after-defined5022-tight`, but the full slot23 replay `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-after-defined5022-fullcondition` still first differs at `defined=5022 abs=5049` with unchanged raw/canonicalized Starshine sizes `3098543`/`3113521`. A later full-context inspection extracted the active function with `wasm-opt --all-features --extract-function-index=5049` to `.tmp/vacuum-defined5022-full-extract/func-abs5049.wasm` (`256K`), which still reproduces the mismatch directly: `.tmp/vacuum-defined5022-full-extract/compare-current` first differs at extracted `defined=0 abs=130`, raw/canonical Starshine `254336`/`254336` vs Binaryen `254342`/`254342`, and pass-local Starshine remains faster (`0.000ms` reported pass time plus `0.592ms` raw vs Binaryen `0.983ms`). Wasm-tools raw print confirms the full-context hunk is label/control depth sensitive: Binaryen keeps an additional void block around the prefix so the branchy `if` has the deeper label depth, while Starshine flattens that wrapper and rebases the else-arm branch. A broad first reduction predicate for this extracted seed drifted to unrelated giant-wrapper diffs and reached only `.tmp/vacuum-defined5022-full-extract/working-parent-timeout.wasm` (`194K`), so it is not a usable positive fixture. The current full-context tight predicate `.tmp/vacuum-defined5022-full-extract/interesting-fullcontext-tight.sh` rejects that 194K drift and requires the Binaryen-only void block around the five-operand local-set/release prefix, the following refcount `if`, and the same-target branch rebase from `br 2` to `br 1`. A bounded reduce timed out at `.tmp/vacuum-defined5022-full-extract/working-fullcontext-tight-timeout.wasm` (`218K`), which direct compare shows is still the same label-depth family at extracted `defined=0 abs=24` with raw/canonical Starshine/Binaryen sizes `216984/216990` and Starshine raw `vacuum` faster than Binaryen's pass (`0.415ms` vs `0.901ms`). A follow-up found the predicate still allowed later-target drift: `.tmp/vacuum-defined5022-full-extract/working-fullcontext-tight-cont2-timeout.wasm` (`201K`) accepted the old tight check but its first hunk had drifted to an unrelated Starshine-only constant-`if` wrapper. New `.tmp/vacuum-defined5022-full-extract/interesting-fullcontext-firsthunk.sh` requires the first grouped diff hunk itself to be the same label-depth target; it accepts the 218K seed and rejects both the 194K broad drift and the 201K later-target drift. A bounded first-hunk reduce timed out at `.tmp/vacuum-defined5022-full-extract/working-fullcontext-firsthunk-timeout.wasm` (`198K`), whose direct compare keeps the target as first hunk at extracted `defined=0 abs=24`, with raw/canonical Starshine/Binaryen sizes `196682/196688` and Starshine raw `vacuum` faster than Binaryen's pass (`0.430ms` vs `0.948ms`). Follow-up reduction reached `.tmp/vacuum-defined5022-full-extract/working-fullcontext-firsthunk-cont2-timeout.wasm` (`168K`), still accepted by the first-hunk predicate and initially still differing as the same first hunk with raw/canonical sizes `165440/165446` and raw `vacuum` `0.426ms` vs Binaryen pass `0.924ms`. Manual extraction produced `.tmp/vacuum-probes/fullcontext-firsthunk-manual.wasm` (`425` bytes) from `.tmp/vacuum-probes/fullcontext-firsthunk-manual.wat`; it reproduced the label-depth residual under wasm-tools parsing and native compare. A nop-insensitive five-arg/full-refcount preservation experiment that closed the manual probe and the 168K seed regressed full slot23 to the rejected `defined=92 abs=119` over-preservation family, so it was not kept. The landed follow-up is stronger than that broad experiment: `src/passes/optimize_test.mbt::vacuum preserves nop-separated full-context five-arg prefix parent wrapper` adds a 428-byte binary fixture with raw `nop` roots between the preserved parent and the continuation, and `src/passes/pass_manager.mbt` now treats a nop-separated continuation as protective only when the next significant instructions are the allocation/use continuation pattern `local.get local.get local.get call local.set local.get call`. The 425B and 428B probes now normalize exactly (`327/327` canonical bytes), and the 168K seed normalizes exactly in `.tmp/vacuum-after-fivearg-nop-continuation-seed168` (`165446/165446`). Full slot23 still differs in `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-after-fivearg-parent-nop-continuation-strong`, with Starshine/Binaryen canonical sizes `3113527/3124349`; this matches the earlier broad experiment's accepted full output, so the remaining slot23 residual is still an open parity gap rather than a Starshine win. Keep the `defined=92` branchy-refcount negative paired with the 425B/428B positives before any broader preservation rule.

A later lowered call-tee/unreachable wrapper slice closed the next reduced ordered-neighborhood residual without closing full slot23. The new focused test `vacuum raw-unwraps type-indexed empty branchy single-if wrappers that contain unreachable` models a 13K reduction where Binaryen treats an empty `block (type $empty)` like a void block and unwraps lowered condition instructions (`i32.const` / arithmetic / load / `local.tee` / call / `local.tee`) before an unreachable-containing branchy `if`. The implementation now builds a `HotModuleContext` only for functions that contain `TypeIdxBlockType` block candidates, resolves empty function types locally, and limits the unwrap to the lowered call-tee/unreachable-if shape with no owner-targeting branches. Direct replays normalize the 13K reduction (`12426/12426` canonical bytes), the 425B/428B probes (`327/327`), and the 168K seed (`165446/165446`). Full slot23 still differs in `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-after-lowered-calltee-unreachable`: raw Starshine/Binaryen sizes `3098525/3113007`, canonicalized sizes `3113503/3124349`, whole-command wall `0.636s/0.400s`, first normalized-WAT diff at function `$94` line `24023`. That hunk is another Binaryen-preserve / Starshine-flatten call/local-set prefix wrapper, so it remains an open parity gap rather than a Starshine-win classification.

The HOT recursion surface is explicit and limited:

- `Block` and `Loop` body regions
- `If` then and optional else regions
- `Try` body and optional catch regions
- `TryTable` body and catch-list regions

The dispatch arm then reports:

- `HotPassResult::changed()` if any explicit `nop` entry was removed
- `HotPassResult::unchanged()` otherwise

Perf tracing now also makes the slot-level cost boundary explicit: the hot pipeline emits an aggregate `pipeline` timer, `vacuum` raw preclean/no-candidate probes emit `raw:vacuum-*` timers, and `vacuum` writeback validation emits either `guard:vacuum-writeback-batch` or per-function `guard:vacuum-writeback:*` fallback timers. The current default collects changed candidate bodies, validates them under one module environment with `validate_defined_funcs_against_module`, and repairs invalid functions by restoring their originals in one batch; the older per-function guard remains as a fallback if changed-function validation cannot classify the repair locally. Repeated `vacuum` raw-skip trace lines for `no-vacuum-candidates` and `raw-vacuum-preclean` are batched into reason/count summaries so traced artifact replays do not print thousands of identical skip records. The raw preclean/no-candidate perf probes also batch per-function elapsed time into one `raw:vacuum-preclean` timing event and one `raw:vacuum-no-candidates` timing event per pass, so traced artifact replays keep raw attribution without formatting or emitting thousands of per-function raw timer lines. HOT mutation tracing for `vacuum` is similarly batched in two layers: helpers still invalidate analyses on each mutation, direct hot-pass application first suppresses per-region revision lines to a function-local count and then emits one pass-level `pass[vacuum]:mutated funcs=<n>` line instead of one mutation summary per changed function. The ordinary HOT lifecycle trace is now also batched for `vacuum`: traced runs emit one `pass[vacuum]:funcs count=<n> changed=<m>` summary instead of per-function `pass[vacuum]:func`, `pass[vacuum]:start`, and `pass[vacuum]:done` lines. The remaining HOT per-function perf events are batched too: direct `vacuum` runs emit one aggregate `lift`, one aggregate `pass:vacuum`, and one aggregate `lower` timer line per pass instead of one line per HOT function, while preserving the same timer names for existing compare parsers. Under the default final-module-only policy, HOT-function verification checkpoints now skip timer/checkpoint emission entirely instead of reporting `verify:before:<pass>` / `verify:after:<pass>` no-op timing lines for every changed function; after the 2026-06-30 `mutation-func-batch` replay, the slot23 `vacuum` trace contains zero `verify:before:vacuum` / `verify:after:vacuum` timer lines, zero per-function `pass[vacuum]:mutated count=` lines, and one `pass[vacuum]:mutated funcs=713` summary. These timers, repairs, and trace summaries protect writeback validity and attribution; they do not add a hidden cleanup phase.

There is still no hidden second-phase cleanup engine for this pass in-tree today.

## What Starshine already does well

The current local strategy is small, but it is not useless.
It already gives the repo a few concrete wins:

- cheap cleanup of explicit HOT `nop` residue after other hot passes
- Binaryen-aligned removal of dropped nontrapping pure scalar expressions
- removal of empty void block residue without deleting typed/result-producing blocks
- a straightforward fit with the `HotFunc` / region-reference ownership model
- honest invalidation of broad HOT analyses after mutation
- stable tracing and perf observability in the main hot pipeline
- a practical replay boundary for validation/writeback issues found during generated-artifact audits
- Binaryen-style cleanup of constant-condition void `if` scaffolds and the empty-then/live-else `if` family without waiting for `remove-unused-brs`

So the right teaching stance is not “this pass is fake.”
It is:

- the current Starshine pass is real and useful
- but its semantic scope is much narrower than Binaryen's `vacuum`

## Current proof surface in this repository

The local tests are small but meaningful.

### Reduced semantic locks

`src/passes/optimize_test.mbt` currently gives the cleanest direct pass-local proof points:

- `vacuum removes dropped pure scalar expressions`
  - proves the effect-aware dropped-result slice removes pure arithmetic while preserving local writes
- `vacuum removes pure drops in root local set call hazards`, `vacuum removes raw nontrapping int division and sign extends in call hazards`, and `vacuum raw-cleans root stack/local-set call hazards`
  - prove the raw guarded cleanup can remove Binaryen-style pure dropped stack expressions around call/local-set hazards while preserving calls, local writes, and the prior HOT-lowering safety boundary
- `vacuum preserves allocation barrier visit roots while cleaning debris`
  - covers the `[JSON-AS]002` runtime-corruption boundary with an allocation-shaped fixture: pure `const; drop` and `i32.add; drop` debris is removed while allocator calls, a field store, a write-barrier call, and a GC-visit call remain present and validating
- `vacuum removes empty void blocks`
  - proves empty zero-result block residue is deleted and the resulting module still validates
- `vacuum flips empty then with live else`
  - proves the Binaryen-style empty-then/live-else void `if` inversion and validates the lowered double-`eqz` form
- `vacuum raw-unwraps call-tee if wrappers that contain unreachable`
  - proves the narrow raw single-`if`/`unreachable` wrapper cleanup for direct call-result conditions captured by `local.tee`, including outer branch-depth rebasing
- `vacuum raw-unwraps recursive block-prefix if wrappers`
  - proves the source-backed recursive wrapper cleanup where an original singleton child block cleans to a prefix block plus direct-call condition and trailing `if`, while guarding against the broader `defined=840 abs=867` over-unwrapping family
- `vacuum raw-cleans empty-block call-prefix wrappers before void ifs`, `vacuum raw-unwraps nested empty-block call-prefix parent wrappers`, `vacuum raw-unwraps artifact continuation call-prefix parent wrappers`, `vacuum raw-unwraps leading-empty call-prefix continuation wrappers`, and `vacuum flattens single-if wrappers without leading empty prefixes`
  - prove the safe subset of the line-`29378` empty-block/call-prefix family: leading empty child blocks can be removed, singleton ancestors from the same empty-block chain can be unwrapped even when the original parent also contains removable `nop` debris, and the no-leading-empty `$block256`-continuation sibling removes only the parent wrapper while preserving Binaryen's inner call-prefix/`if` block; the rejected `defined=4370` single-`if` wrapper family still flattens instead of being preserved
- `vacuum flattens branchy refcount if wrappers before allocation continuations`
  - guards the rejected `defined=92 abs=119` regression from the first `defined=5022` multi-argument local-set preservation experiment: branchy refcount `if` wrappers before an allocation/store/return continuation must remain flattened, so future prefix-preservation rules need a discriminator stronger than local-set call arity or release-call count
- `vacuum preserves full-context first-hunk five-arg prefix parent wrapper`
  - covers the 425-byte wasm-tools-parsed full-context first-hunk probe from `.tmp/vacuum-probes/fullcontext-firsthunk-manual.wasm`: Binaryen keeps a parent wrapper around the five-argument allocator/release prefix when the next sibling starts the continuation with `local.get`, while Starshine previously flattened it. The test failed red at the child-block boundary and now passes under an immediate-`local.get` continuation guard. Direct native replay of the 425B probe normalizes exactly, but `.tmp/vacuum-after-fivearg-parent-immediate-localget-seed168` still differs on the 168K first-hunk reduction; allowing `nop*; local.get` also closes that seed but recreates the rejected full-slot `defined=92 abs=119` over-preservation size profile, so the broader full-context residual stays open.
- `vacuum preserves binary-lowered defined5022 five-arg prefix child block`
  - covers the reduced opposite-direction residual shape: Binaryen keeps a child block around a five-argument allocator `local.set(call ...)` plus three release calls before a branchy refcount `if`, while the prior Starshine build flattened it. The test embeds `.tmp/vacuum-reduce-defined5022-v4-final-cont/working-v4-final-timeout-3k.wasm` (`3114` bytes) because text parsing/rebuilding loses the mismatch; it failed red with the flattened first instruction (`I32Const(I32(0))`) and now passes under the narrow reduced/full-condition discriminator. Direct compare of that 3KB seed now normalizes exactly, but the full slot23 residual at `defined=5022 abs=5049` remains open, so this is reduced-seed progress rather than full ordered-neighborhood parity.
  - the fresh `.tmp/vacuum-reduce-defined5022-interesting.sh` reducer preserves the same active residual. A stricter `.tmp/vacuum-reduce-defined5022-v2-interesting.sh` additionally requires branchy refcount loads/`i32.gt_s`/`i32.sub` evidence and rejects the stale line-`29378` seed, the loose `126K` drift, and the broad multi-arg local-set probe. The earlier best accepted seed was `.tmp/vacuum-reduce-defined5022-v2-cont/working-v2-extract-f12-22k.wasm` (`22236` bytes), extracted from a 24KB v2 continuation; it had `4` hunks with the target at hunk `4`. A smaller 6KB v3 continuation is explicitly rejected as drift because it loses the clean Binaryen-preserve / Starshine-flatten target under v2 inspection. Follow-up `.tmp/vacuum-reduce-defined5022-v4-interesting.sh` keeps v3's live-local and clean-hunk intent while avoiding the mixed-hunk false positive: it uses before/after context only in hunks with at most `250` diff lines, requires four live allocator operands plus the leading zero, requires three release-call local operands, and requires the refcount `i32.gt_s` to compare a `local.tee(call ...)` result against `1`. It accepts the `43K`, `24K`, and `22K` seeds while rejecting stale/drift artifacts including the `8644`, `10830`, and `6467` byte reductions. A final v4 `wasm-reduce` continuation now makes the embedded 3KB seed the active positive: it accepts v4 and inspection `.tmp/vacuum-reduce-defined5022-v4-final-cont/inspect-3k/diff.txt` has exactly one target hunk (`@@ -10224,60 +10224,58 @@`). The adjacent reducer `test.wasm` rejects v4, so do not use it. The 3KB seed is now focused red-first coverage, but it is still not a behavior fix.
- `vacuum collapses constant void if to taken arm`, `vacuum collapses constant void if with nested block-local branch`, `vacuum collapses generated branch scaffold constant if`, and `vacuum collapses large generated branch scaffold constant if`
  - prove the 2026-06-29 constant-condition void-`if` parity fix for both ordinary HOT cleanup and generated large-function scaffold shapes
- `vacuum unwraps block that only contains unreachable`
  - proves the block-only-`unreachable` cleanup shape Binaryen emits on the generated scalar corpus
- `vacuum matches Binaryen empty function nop canonicalization`
  - proves direct `vacuum` keeps Binaryen's single-`nop` canonical form for otherwise empty function bodies
- `vacuum removes nested value-expression debris in large functions`
  - proves the raw large-function precleaner removes nested lowered pure `const`/`drop` and `nop` debris even when the HOT nested-child cleanup budget would otherwise skip that function
- `vacuum cleans simplify-locals structured return residue in the late cleanup pair`
  - proves that explicit `nop` cleanup helps a real late local-cleanup pipeline shape
- `vacuum removes pure scalar stack debris before root unreachable`, `vacuum removes pure scalar typed block debris before root unreachable`, and `vacuum removes empty try_table debris before root unreachable`
  - prove the raw terminal-`unreachable` precleaner removes the wasm-smith debris families where Binaryen drops scalar/nop prefixes, typed blocks that only become `unreachable`, and empty nonthrowing `try_table` wrappers before a hard `unreachable`
- `vacuum roundtrips dead simd prefixes before unreachable`
  - proves the terminal-`unreachable` cleanup does not widen to SIMD expression prefixes without source-backed evidence
- `vacuum roundtrips top-level try_table catches that target the implicit function label`
  - proves the recursive region walk keeps an important top-level EH/control shape valid

### Trace and perf locks

`src/passes/trace_golden_test.mbt` locks the deterministic traced mutation shape for a tiny `nop`-removal example.

`src/passes/perf_test.mbt` locks that:

- traced/perf pipelines record `pass:vacuum`
- unchanged `vacuum` runs skip lower/writeback work when possible
- the pass composes correctly with verify-policy timing checkpoints

### Pipeline and registry locks

`src/passes/pass_manager_wbtest.mbt` proves that:

- the pass registry lookup for `vacuum` returns a detached descriptor copy
- repeated `vacuum` no-candidate raw-skip trace lines are batched into one counted summary
- the traced debug-dump target logic includes the saved artifact hotspot function only for `vacuum`

`src/cmd/cmd.mbt` and `src/cmd/cmd_wbtest.mbt` prove that:

- `--vacuum` is a real CLI-visible pass flag
- resolved-pass summaries report it correctly
- replaying saved generated-artifact inputs through the CLI still stays wasm-tools-valid

### Saved artifact replay lanes

The most specific local replay evidence in `src/cmd/cmd_wbtest.mbt` is important because it documents the old failure visibility honestly.
The saved generated `-O4z` slot-23 predecessor and extracted function replay lanes now validate under `--vacuum`.
That is the concrete proof surface behind the older artifact notes that retired the slot-23 and slot-33 failures.

## Current local-vs-upstream split

The safest one-line contrast is:

- **Binaryen `vacuum`:** effect-aware unused-result pruning plus structural cleanup, TNH handling, and mandatory refinalization
- **Starshine `vacuum`:** recursive explicit-`nop` trimming, empty zero-result block removal, dropped nontrapping pure-result pruning, block-only `unreachable` unwrapping, constant-condition void-`if` collapse, empty-then/live-else `if` inversion, Binaryen-style empty-function single-`nop` canonicalization, and pipeline-level validation/writeback hygiene around those rewrites

Direct oracle evidence for the current slice:

- `.tmp/pass-fuzz-vacuum-genvalid-100000-after-case003694-classification`: after classifying the remaining wasm-smith output-shape residual, the required regular GenValid closeout lane compared `100000/100000` cases with `100000` normalized matches, `0` mismatches, `0` validation/property/generator/command failures, and Binaryen cache `10332` hits / `89668` misses.
- `.tmp/pass-fuzz-vacuum-audit-after-const-if-10000-current`: after the 2026-06-29 constant-condition void-`if` fix, direct GenValid `vacuum` replay compared `10000/10000` cases with `10000` normalized matches, `0` mismatches, `0` validation/property/generator/command failures, and Binaryen cache `1002` hits / `8998` misses. The command used `_build/native/release/build/cmd/cmd.exe` because the stale `target/native/...` copy did not refresh in this worktree.
- 2026-07-01 dedicated profile split: `vacuum` is now a composite GenValid aggregate over `vacuum-core`, `vacuum-terminal-unreachable`, `vacuum-structural-wrappers`, `vacuum-call-prefix-continuation`, `vacuum-localset-prefix-preserve`, and `vacuum-hazard-boundary`. Focused generator tests cover aggregate membership and emitted trigger scaffolds. Direct leaf smoke dirs `.tmp/pass-fuzz-vacuum-profile-smoke-vacuum-{core,terminal-unreachable,structural-wrappers,call-prefix-continuation,localset-prefix-preserve,hazard-boundary}-postfix` each compared `20/20`, normalized `20`, and had `0` mismatches/failures. Aggregate smoke `.tmp/pass-fuzz-vacuum-profile-smoke-vacuum-postfix` compared `60/60`, normalized `60`, and selected all six leaves with `0` mismatches/failures.
- `.tmp/pass-fuzz-vacuum-genvalid-vacuum-10000-after-local-only-safe-binary`: before the dedicated profile split, the singleton `vacuum` GenValid profile and local-only void-body cleanup lane compared `10000/10000` cases with `10000` normalized matches, `0` cleanup-normalized matches, `0` mismatches/failures, Binaryen cache `10000` hits / `0` misses, and manifest selected-profile count `vacuum=10000`.
- `.tmp/pass-fuzz-vacuum-random-all-profiles-10000-after-terminal-debris`: after adding raw terminal-`unreachable` debris cleanup, broad random all-profiles GenValid compared `10000/10000` cases with `10000` normalized matches, `0` cleanup-normalized matches, and `0` mismatches/failures. Selected subprofiles were `binaryen-oracle-portable=1958`, `ssa-nomerge-smoke=1973`, `coverage-forced-portable=2037`, `ssa-nomerge-parity=1970`, and `pass-fuzz-stress=2062`.
- `.tmp/pass-fuzz-vacuum-wasm-smith-10000-case003694-classified`: refreshed explicit wasm-smith compared `9956/10000` cases with `9955` normalized matches, `1` classified mismatch, and `44` Binaryen/tool parser/canonicalization failures (`binaryen-rec-group-zero=39`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`, `binaryen-bad-section-size=3`). The three terminal-unreachable scratch mismatches `case-009332`, `case-009390`, and `case-009956` now normalize. The remaining `case-003694` is classified as a non-pass Binaryen parser/IR materialization artifact plus a narrow Starshine size win: Binaryen's no-pass load of the input already introduces the scratch `f64` local needed to represent a loop-carried stack value before a trailing `nop`, and `--vacuum` only removes that `nop`; Starshine removes the `nop` without adding the local. The original case has smaller Starshine normalized/canonical wasm (`67` vs `73` bytes) but raw Starshine is larger due unrelated type/elem encoding (`76` vs `73`); the reduced `.tmp/vacuum-case003694-reduced.wat` removes that noise and shows Starshine `40` bytes vs Binaryen `46` bytes after direct `--vacuum`.
- `.tmp/vacuum-current-o4z-neighborhood`: current-head reconstruction of the old generated `-O4z` `VQ` neighborhoods rebuilt `_build/wasm/debug/build/cmd/cmd.wasm`, regenerated Binaryen predecessors for top-level slots `23`, `33`, `37`, and `47`, and directly replayed Starshine `--vacuum`. All four Starshine outputs now exit `0` and validate, so the old `0097`/`0098` validation blockers remain retired without the missing `.artifacts` inputs. The first slot23 ordered-neighborhood parity slice exposed `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-after`, which differed at `defined=840 abs=867` with Starshine retaining an extra block wrapper around a large nested branch cascade; traced slot23 pass-local time was faster than Binaryen (`19.685ms` vs `143.193ms`), but whole-command Starshine remained slower (`5154.218ms` vs `550.468ms`) with most time untraced. The follow-up `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-ifwrapper-final` added if-wrapper cleanup and moved the first diff later in the same function (`1689` to `5688` in the extracted normalized WAT), improved raw Starshine size to `3104585` bytes vs Binaryen `3113007`, and kept pass-local Starshine faster (`20.485ms` vs `155.442ms`). A later structural-wrapper raw experiment, captured in `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-structwrapper-parent2`, was reverted because it made the first difference earlier (`defined=15 abs=42`) and raw size-losing (`3116607` vs `3113007`). A second parent-context attempt was also reverted: `...-callprefix-narrow3` advanced the first diff to `defined=854 abs=881` but over-unwrapped a Binaryen-preserved call-prefix wrapper there, while `...-callprefix-narrow4` returned to the `defined=15 abs=42` regression. The remaining local-set/call block-wrapper disparity is still an open parity gap, not a Starshine-win classification. The timing-only attribution replay `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-attribution-final` identifies the whole-command hotspot as the `guard:vacuum-writeback` validation boundary (`5297.058ms` of the `6629.091ms` traced pipeline), while raw `vacuum` cleanup itself is `89.292ms` and direct pass-local work remains faster than Binaryen (`26.364ms` vs `160.004ms`).
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-batch-repair-final`: after replacing the default per-function `vacuum` writeback guard with batch validation plus function-local repair, the current slot23 timing-only replay dropped Starshine whole-command time from the attribution baseline `7162.576ms` to `2207.677ms` while preserving the same seven invalid-lower skips (`Func 883`, `1810`, `1829`, `1830`, `2711`, `3692`, and `5366`). `guard:vacuum-writeback-batch` was `992.963ms` instead of the old grouped per-function guard `5297.058ms`; raw `vacuum` was `94.525ms`, Starshine/Binaryen pass-local was `27.643ms/218.179ms`, and whole-command Starshine/Binaryen was `2207.677ms/814.248ms`. The residual ordered-neighborhood local-set/call wrapper gap remains open, and direct smoke `.tmp/pass-fuzz-vacuum-1000-after-batch-writeback-repair` compared `1000/1000` with `1000` normalized and `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-func-batch-final`: after changing batch repair to validate only changed candidate bodies under one reused module environment, the slot23 timing-only replay dropped Starshine whole-command time further to `916.276ms` vs Binaryen `580.744ms`, while preserving the same seven invalid-lower skips. `guard:vacuum-writeback-batch` is now `113.338ms`, aggregate `pipeline` is `460.791ms`, raw `vacuum` is `69.378ms`, Starshine/Binaryen pass-local is `23.454ms/153.775ms`, and self-compare canonicalized Starshine/Binaryen size remains `3119563/3113007` and the emitted Starshine raw file is `3104585` bytes. The ordered-neighborhood local-set/call wrapper gap remains open; direct smoke `.tmp/pass-fuzz-vacuum-1000-after-func-batch-writeback` compared `1000/1000` with `1000` normalized and `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-skip-trace-batch`: after batching repeated traced `vacuum` raw-skip reasons, the slot23 timing-only replay reported Starshine/Binaryen whole-command `875.122ms/541.529ms`, Starshine/Binaryen pass-local `19.325ms/142.650ms`, raw `vacuum` `68.550ms`, aggregate `pipeline` `421.195ms`, and `guard:vacuum-writeback-batch` `112.145ms`. The trace records one `reason=no-vacuum-candidates count=484` line and one `reason=raw-vacuum-preclean count=6211` line instead of thousands of repeated skip lines. This reduces traced replay overhead without changing direct `vacuum` output; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-raw-timer-aggregate`: after replacing per-function raw timer names with aggregate `raw:vacuum-preclean` and `raw:vacuum-no-candidates` names, the slot23 timing-only replay reported Starshine/Binaryen whole-command `703.228ms/545.988ms`, Starshine/Binaryen pass-local `18.912ms/144.886ms`, raw `vacuum` `63.015ms`, other traced `523.518ms`, and untraced/runtime overhead `97.783ms`. Red-first `src/passes/perf_test.mbt` coverage rejects `raw:vacuum-*:func` names. This is a timing/trace overhead reduction only; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-raw-timer-event-batch-final`: after batching raw `vacuum` timer events themselves, the slot23 timing-only replay reported Starshine/Binaryen whole-command `663.776ms/533.042ms`, Starshine/Binaryen pass-local `21.824ms/138.529ms`, raw `vacuum` `58.386ms`, other traced `511.979ms`, and untraced/runtime overhead `71.587ms`. Red-first `src/passes/perf_test.mbt` coverage now requires exactly one raw-preclean and one no-candidate timer line for a three-function no-candidate module. Trace line count dropped to `11770`; skip counts remain `484` no-candidates and `6211` raw-preclean. This is another timing/trace overhead reduction only; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-mutation-trace-batch-final`: after batching per-mutation `vacuum` trace lines, the slot23 timing-only replay reported Starshine/Binaryen whole-command `629.524ms/535.756ms`, Starshine/Binaryen pass-local `1.896ms/141.506ms`, raw `vacuum` `56.843ms`, other traced `500.277ms`, and untraced/runtime overhead `70.508ms`. Red-first `src/passes/trace_golden_test.mbt` coverage now expects one aggregate mutation line per changed function. Trace line count dropped to `6468`; per-mutation revision lines dropped to `0`, aggregate mutation-count lines are `713`, raw timer events remain batched, and skip counts remain `484` no-candidates plus `6211` raw-preclean. This is another timing/trace overhead reduction only; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-mutation-trace-batch-final` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-lifecycle-trace-batch-final`: after batching `vacuum` HOT lifecycle traces, the slot23 timing-only replay reported Starshine/Binaryen whole-command `632.986ms/541.272ms`, Starshine/Binaryen pass-local `1.918ms/144.994ms`, raw `vacuum` `58.214ms`, other traced `509.076ms`, and untraced/runtime overhead `63.778ms`; a repeat reported `634.384ms/537.173ms`. Red-first trace coverage expects no per-function `func`/`start`/`done` lines for `vacuum` and one aggregate `pass[vacuum]:funcs count=<n> changed=<m>` line. Trace line count dropped to `4325`; aggregate mutation-count lines remain `713`. This reduces trace volume but did not measurably close the remaining whole-command gap in the sampled runs; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-lifecycle-trace-batch-final` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-hot-timer-batch-final`: after batching the remaining HOT per-function `lift`, `pass:vacuum`, and `lower` timing events, the slot23 timing-only replay reported Starshine/Binaryen whole-command `634.275ms/548.956ms`, Starshine/Binaryen pass-local `1.971ms/142.775ms`, raw `vacuum` `56.874ms`, other traced `515.660ms`, and untraced/runtime overhead `59.770ms`; a repeat reported `616.325ms/550.907ms`, pass-local `1.891ms/141.626ms`, raw `57.553ms`, other traced `499.652ms`, and untraced/runtime `57.229ms`. Red-first perf coverage expects exactly one timing line for each of `lift`, `pass:vacuum`, and `lower` across a three-function changed `vacuum` module. Trace line count dropped to `2184`; raw timer events and aggregate mutation-count lines remain batched. This reduces trace volume but did not measurably close the sampled whole-command gap; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-hot-timer-batch-final2` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-verify-noop-skip-final`: after skipping no-op HOT-function verification timers under final-module-only policy, the slot23 timing-only replay reported Starshine/Binaryen whole-command `629.843ms/584.316ms`, Starshine/Binaryen pass-local `2.054ms/152.369ms`, raw `vacuum` `58.286ms`, other traced `513.336ms`, and untraced/runtime overhead `56.167ms`; a repeat reported `618.719ms/583.147ms`, pass-local `1.955ms/159.285ms`, raw `56.655ms`, other traced `504.912ms`, and untraced/runtime `55.197ms`. Red-first perf coverage now rejects `verify:before:vacuum` and `verify:after:vacuum` timers when final-module-only validation is active. Trace line count dropped to `754`, with zero per-function verify timer lines and the same `713` aggregate mutation-count lines. This reduces trace volume but did not materially close the sampled whole-command gap; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-verify-noop-skip` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-mutation-func-batch-final`: after batching the remaining per-function `vacuum` mutation-count summaries into one pass-level line, the slot23 timing-only replay reported Starshine/Binaryen whole-command `623.617ms/540.657ms`, Starshine/Binaryen pass-local `1.916ms/145.407ms`, raw `vacuum` `58.927ms`, other traced `510.535ms`, and untraced/runtime overhead `52.239ms`; a repeat reported `634.730ms/565.452ms`, pass-local `1.887ms/147.442ms`, raw `60.010ms`, other traced `517.266ms`, and untraced/runtime `55.567ms`. Red-first trace coverage rejects per-function `pass[vacuum]:mutated count=` lines and expects one `pass[vacuum]:mutated funcs=<n>` summary. Trace line count dropped to `42`, with zero per-function mutation-count lines and one `pass[vacuum]:mutated funcs=713` line. This reduces trace volume but did not materially close the sampled whole-command gap; canonical wasm still differs and the ordered-neighborhood local-set/call wrapper gap remains open. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-mutation-func-batch` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. Size reporting for this lane should distinguish the canonicalized `starshine.wasm` compare artifact (`3119563` bytes) from the emitted raw Starshine output (`3104585` bytes); `self-optimize-compare` now records both raw and canonical size fields.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-size-fields-final`: after adding raw/canonicalized size fields to `self-optimize-compare`, the timing-only slot23 replay reported Starshine/Binaryen whole-command `609.636ms/555.346ms`, Starshine/Binaryen pass-local `1.850ms/169.594ms`, raw `vacuum` `57.849ms`, other traced `498.933ms`, and untraced/runtime `51.004ms`; emitted raw sizes are Starshine/Binaryen `3104585/3113007`, while canonicalized compare artifacts are `3119563/3113007`. This is reporting/attribution only; the slot23 local-set/call wrapper parity gap remains open.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-callprefix-singleton-final`: after adding the narrow singleton-parent call-prefix/`if` raw cleanup, the full slot23 replay reported Starshine/Binaryen whole-command `630.252ms/543.370ms`, pass-local `1.963ms/143.280ms`, raw `vacuum` `59.163ms`, other traced `517.147ms`, and untraced/runtime `51.979ms`; emitted raw sizes are `3104054/3113007`, while canonicalized compare artifacts are `3119032/3113007`. The focused red-first fixture failed before the raw rule because Starshine kept the extra singleton wrapper and unre-based outer branch depth. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-callprefix-singleton` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: canonical wasm still differs and the full compare reports first differing function `defined=15 abs=42`, so this is a reduction step, not a classified Starshine win.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-terminal-wrapper-final`: after adding the narrow terminal-`unreachable` block-wrapper raw cleanup, the full slot23 replay reported Starshine/Binaryen whole-command `614.014ms/530.463ms`, pass-local `1.905ms/140.056ms`, raw `vacuum` `60.005ms`, other traced `500.219ms`, and untraced/runtime `51.885ms`; emitted raw sizes are `3102221/3113007`, while canonicalized compare artifacts are `3117199/3113007`. The focused red-first fixture failed before the raw rule because Starshine kept the extra wrapper and unre-based outer branches. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-terminal-wrapper` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: canonical wasm still differs, but first diff advanced from `defined=15 abs=42` to `defined=39 abs=66`; this is a reduction step, not a classified Starshine win.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-emptyelse-if-wrapper-final`: after adding raw lowered empty-then/live-else flipping, cleaned-empty else removal, and guarded single-`if`/`unreachable` wrapper unwrapping, the full slot23 replay reported Starshine/Binaryen whole-command `641.097ms/554.740ms`, pass-local `1.934ms/148.066ms`, raw `vacuum` `65.892ms`, other traced `520.073ms`, and untraced/runtime `53.198ms`; emitted raw sizes are `3100073/3113007`, while canonicalized compare artifacts are `3115051/3113007`. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-emptyelse-if-wrapper` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: first diff advanced from `defined=39 abs=66` to `defined=4481 abs=4508`, but canonical wasm still differs and the residual is not a classified Starshine win.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-calltee-if-wrapper-final`: after adding raw call-result-`local.tee` condition wrapper cleanup for single-`if` wrappers containing `unreachable`, the full slot23 replay reported Starshine/Binaryen whole-command `637.778ms/534.175ms`, pass-local `1.916ms/140.812ms`, raw `vacuum` `67.878ms`, other traced `515.399ms`, and untraced/runtime `52.585ms`; emitted raw sizes are `3099812/3113007`, while canonicalized compare artifacts are `3114790/3113007`. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-calltee-if-wrapper` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: first diff is still `defined=4481 abs=4508`, now at a remaining block-prefix/call-condition wrapper, so the residual is not a classified Starshine win.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-recursive-callprefix-parent-final`: after adding the guarded recursive call-prefix wrapper cleanup, the full slot23 replay reported Starshine/Binaryen whole-command `678.606ms/586.628ms`, pass-local `2.152ms/151.507ms`, raw `vacuum` `71.075ms`, other traced `548.664ms`, and untraced/runtime `56.715ms`; emitted raw sizes are `3099344/3113007`, while canonicalized compare artifacts are `3114322/3113007`. The focused red-first fixture failed before the raw rule because Starshine kept the recursive wrapper; a broader implementation was rejected because it over-unwrapped the current `defined=840 abs=867` family. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-recursive-callprefix` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: canonical wasm still differs, first differing function remains `defined=4481 abs=4508`, but the first diff inside that function moved to normalized-WAT line `18461`, so this is a reduction step rather than a residual Starshine-win classification.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-sideeffect-child-guarded-final`: a rejected side-effect singleton-child wrapper experiment for the same `defined=4481 abs=4508` residual. Binaryen probe `.tmp/vacuum-probes/nested-side-effect-prefix-block-before-if-unnamed.wat` showed direct `--vacuum` can flatten one nested wrapper after cleanup exposes call/local-set/call prefixes and a trailing void `if`; the Starshine guarded implementation did not remove the current residual, newly forced seven additional invalid writeback repairs (`3110`, `4383`, `4390`, `4391`, `4399`, `4405`, `4418`), and regressed raw/canonicalized slot23 sizes to `3101780`/`3116761` from the accepted `3099344`/`3114322` baseline. The experiment was reverted before commit.
- `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-sideeffect-child-final2`: after adding and tightening the narrower child-only side-effect wrapper cleanup, the full slot23 replay reported Starshine/Binaryen whole-command `636.931ms/564.491ms`, pass-local `1.998ms/147.369ms`, raw `vacuum` `68.883ms`, other traced `512.719ms`, and untraced/runtime `53.331ms`; emitted raw sizes are `3098813/3113007`, while canonicalized compare artifacts are `3113791/3113007`. The focused red-first fixture failed before implementation because Starshine kept the child wrapper, and a negative local-set-leading fixture now guards a sibling family that Binaryen preserves; the accepted rule does not unwrap the singleton parent and preserved the seven-function invalid-lower baseline. Direct smoke `.tmp/pass-fuzz-vacuum-1000-after-sideeffect-child-final2` compared `1000/1000`, normalized `1000`, with `0` mismatches/failures. The ordered slot remains open: first diff is still `defined=4481 abs=4508`, now line `18765`, at a remaining Starshine wrapper around a local-set-leading call/use region.
- `.tmp/pass-fuzz-vacuum-profile-smoke-100-ref`: after adding the dedicated `vacuum` GenValid profile, the profile smoke compared `100/100` cases with `100` normalized matches, `0` cleanup-normalized matches, `0` mismatches/failures, Binaryen cache `100` hits / `0` misses, and manifest selected-profile count `vacuum=100`. A traced sample recorded Starshine `pass:vacuum` total `924us` across the small and larger profile functions.
- `.tmp/pass-fuzz-vacuum-large-nested-preclean`: after the 2026-05-11 large-function raw precleaner, mixed-generator direct `vacuum` replay reached `6759/10000` comparable cases with `6759` normalized matches, `0` mismatches, `0` validation failures, and `20` Binaryen/tool command failures
- `.tmp/pass-fuzz-vacuum-empty-then-final`: after the 2026-05-10 empty-then/live-else inversion, mixed-generator replay reached `6759/10000` comparable cases with `6759` normalized matches, `0` mismatches, `0` validation failures, and `20` Binaryen empty-recursion-group parser/canonicalization command failures
- `.tmp/pass-fuzz-vacuum-gen-valid`: `10000/10000` direct `gen-valid` normalized matches, `0` mismatches, `0` validation failures, `0` command failures after the empty-void-block cleanup landed
- `.tmp/vacuum-perf-case003694-classified`: five broad random all-profiles inputs all had equal raw wasm or normalized WAT after direct `--vacuum`. Pass-local Starshine/Binaryen timing pairs were `5.539/0.124`, `0.037/0.037`, `0/0.185`, `5.517/0.134`, and `6.943/0.129` ms; median Starshine pass-local time was `5.517ms` vs Binaryen `0.129ms`. This meets the repo `<1s` pass-local target but not the `>=50%` Binaryen-speed target.
- `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --redundant-set-elimination --vacuum --out-dir .tmp/vacuum-large-nested-rse-debris`: the combined replay still has the inherited first differing function at `defined=208 abs=225`, but the real large-function size gap is mostly closed. Starshine normalized wasm is `3,145,318` bytes vs Binaryen `3,139,705`; `defined=518` shrinks to `497,292` body bytes vs Binaryen `495,884`, and Starshine pass-local time is `174.413ms` vs Binaryen `35,265.000ms`.

Audit-closeout note for the current scope:

- direct regular GenValid signoff is green at `100000/100000` in `.tmp/pass-fuzz-vacuum-genvalid-100000-after-case003694-classification`;
- explicit wasm-smith is classified in `.tmp/pass-fuzz-vacuum-wasm-smith-10000-case003694-classified` with one inspected non-pass Binaryen materialization / Starshine-size-win residual plus `44` Binaryen/tool failures;
- broad random-all-profiles is green at `10000/10000` in `.tmp/pass-fuzz-vacuum-random-all-profiles-10000-after-terminal-debris`;
- the reconstructed checked slot23 replay `.tmp/vacuum-current-o4z-neighborhood/slot23-pre22-vacuum-compare-f7263-i32-le-s-parser` reached normalized WAT equality and canonical function compare equality on 2026-07-04;
- remaining whole-command wall investigations belong under `[WALL]001` unless a fresh measurement proves a new vacuum-owned bottleneck.

Current Starshine does **not** yet model upstream behaviors such as:

- generalized effect-aware dropped-wrapper elimination beyond the current nontrapping pure scalar/ref/tuple subset, local-only void-body pruning, lowered large-function pure leaf precleaner, and narrow terminal-`unreachable` scalar/empty-wrapper cleanup
- multi-child effect preservation through dropped-child rebuilding
- non-void and label-carried constant `if` collapse plus unreachable-condition `if` collapse
- branch-hint metadata updates when an `if` is inverted
- `drop(local.tee(...)) -> local.set(...)`
- block-result popping with label-safety checks
- non-throwing `try` / `try_table` collapse
- TNH trap-path cleanup
- whole-function `nop` cleanup based on side effects
- explicit-`unreachable` preservation logic as a first-class rewrite rule
- Binaryen-style post-edit refinalization

That boundary should stay explicit in every future edit.

## Why the local implementation lives in `pass_manager.mbt`

Unlike several other hot passes in this repo, `vacuum` does not currently have its own dedicated `src/passes/vacuum.mbt` file.
The small current scope made it reasonable to keep the implementation close to the hot-pass runner itself:

- the rewrite is tiny
- it depends directly on core region-splice and detached-node cleanup helpers
- the pass-specific writeback-validation guard also lives in the pipeline manager

If the pass grows toward Binaryen parity, it will probably become worth moving into its own dedicated file.
Right now the code map is still small enough that keeping the exact `pass_manager.mbt` ownership visible is more helpful than pretending there is a larger local subsystem.

## Boundary lesson from the retired artifact failures

The saved generated-artifact notes remain important context.
They showed that `vacuum` was sometimes the place where invalid output became visible, but not necessarily the place where the real semantic bug originated.

The retired slot-23 and slot-33 notes now support a stable lesson:

- some earlier failures surfaced during `vacuum` replay
- the durable fixes lived in writeback / carrier / validation hygiene around the hot pipeline
- future parity work should port real Binaryen `vacuum` semantics instead of turning local `vacuum` into an ad hoc repair bucket

That is why this page treats the writeback guard and CLI replay lanes as part of the strategy story.
For the current implementation, they are part of the honest contract.

## Honest future port shape

If Starshine wants closer Binaryen parity, the likely path is still staged:

1. keep the current recursive `nop`, empty-void-block, dropped-pure-result, block-only-`unreachable`, constant-condition void-`if`, empty-then/live-else `if` inversion, and large lowered-function pure leaf precleaning slice green
2. move the pass into a dedicated owner file once the helper surface grows further
3. broaden effect-aware dropped-wrapper elimination
4. add the remaining easy structural cases
   - non-void / label-carried constant `if` and unreachable-condition `if`
   - drop-of-tee to set
   - trivial block and loop cleanup beyond block-only `unreachable`
5. add branch-result-aware and EH-aware cleanup only with explicit legality proofs
6. add whatever HOT writeback or refinalization-equivalent discipline is needed for broader GC and EH-sensitive rewrites

The important rule is that future work should grow from the current exact code map, not from a fuzzy idea that `vacuum` already means Binaryen parity.

## Bottom line

Current Starshine `vacuum` is a tiny but useful hot pass.
Its exact local implementation is now easy to follow:

- registration in `src/passes/optimize.mbt`
- recursion, dispatch, and writeback guards in `src/passes/pass_manager.mbt`
- semantic, trace, perf, registry, and CLI evidence in the focused test files

That makes the local subset easy to teach honestly:

- **what it does today:** remove explicit HOT `nop` region entries, remove empty zero-result blocks, remove dropped nontrapping pure scalar/ref/tuple results, canonicalize side-effect-free/local-only void function bodies with tee/write/control debris to one `nop`, unwrap block-only `unreachable`, collapse constant-condition void `if`s when branch-label safety is local, flip empty-then/live-else void `if`s, preclean large lowered functions with pure leaf `const`/`drop`, constant void-`if`, cleaned-empty else arms, `nop` debris, narrow singleton and recursive call-prefix/`if` wrappers, narrow single-`if`/`unreachable` wrappers including the call-result-`local.tee` condition subset, and narrow terminal-`unreachable` child-block wrappers, and keep the pipeline safe
- **what it does not do yet:** the broader Binaryen `vacuum` rewrite family
