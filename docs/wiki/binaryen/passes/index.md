---
kind: entity
status: supported
last_reviewed: 2026-04-21
sources:
  - ../../../../src/passes/optimize.mbt
  - ../../../../src/passes/registry_test.mbt
  - ./late-pipeline-dispatch.md
  - ../../raw/research/0111-2026-04-20-merge-blocks-binaryen-research.md
  - ../../raw/research/0112-2026-04-20-code-folding-binaryen-research.md
  - ../../raw/research/0113-2026-04-20-optimize-casts-binaryen-research.md
  - ../../raw/research/0114-2026-04-20-rse-binaryen-research.md
  - ../../raw/research/0115-2026-04-20-code-pushing-binaryen-research.md
  - ../../raw/research/0116-2026-04-20-local-subtyping-binaryen-research.md
  - ../../raw/research/0117-2026-04-20-simplify-locals-nostructure-binaryen-research.md
  - ../../raw/research/0118-2026-04-20-coalesce-locals-binaryen-research.md
  - ../../raw/research/0119-2026-04-20-local-cse-binaryen-research.md
  - ../../raw/research/0120-2026-04-20-dae-optimizing-binaryen-research.md
  - ../../raw/research/0121-2026-04-20-inlining-optimizing-binaryen-research.md
  - ../../raw/research/0122-2026-04-20-simplify-globals-optimizing-binaryen-research.md
  - ../../raw/research/0123-2026-04-20-duplicate-import-elimination-binaryen-research.md
  - ../../raw/research/0205-2026-04-21-duplicate-import-elimination-source-confirmation-followup.md
  - ../../raw/research/0124-2026-04-20-string-gathering-binaryen-research.md
  - ../../raw/research/0125-2026-04-20-reorder-globals-binaryen-research.md
  - ../../raw/research/0126-2026-04-20-directize-binaryen-research.md
  - ../../raw/research/0127-2026-04-20-flatten-binaryen-research.md
  - ../../raw/research/0128-2026-04-20-merge-locals-binaryen-research.md
  - ../../raw/research/0129-2026-04-20-simplify-locals-notee-nostructure-binaryen-research.md
  - ../../raw/research/0130-2026-04-20-vacuum-binaryen-research.md
  - ../../raw/research/0210-2026-04-21-vacuum-source-confirmation-followup.md
  - ../../raw/research/0166-2026-04-21-simplify-locals-notee-binaryen-research.md
  - ../../raw/research/0168-2026-04-21-global-effects-binaryen-research.md
  - ../../raw/research/0172-2026-04-21-avoid-reinterprets-binaryen-research.md
  - ../../raw/research/0173-2026-04-21-loop-invariant-code-motion-binaryen-research.md
  - ../../raw/research/0175-2026-04-21-i64-to-i32-lowering-binaryen-research.md
  - ../../raw/research/0223-2026-04-21-legalize-js-interface-binaryen-research.md
  - ../../raw/research/0224-2026-04-21-legalize-and-prune-js-interface-binaryen-research.md
  - ../../raw/research/0176-2026-04-21-monomorphize-binaryen-research.md
  - ../../raw/research/0177-2026-04-21-inline-main-binaryen-research.md
  - ../../raw/research/0187-2026-04-21-monomorphize-always-binaryen-research.md
  - ../../raw/research/0178-2026-04-21-dataflow-optimization-binaryen-research.md
  - ../../raw/research/0179-2026-04-21-reorder-functions-binaryen-research.md
  - ../../raw/research/0211-2026-04-21-reorder-functions-source-confirmation-followup.md
  - ../../raw/research/0180-2026-04-21-reorder-functions-by-name-binaryen-research.md
  - ../../raw/research/0181-2026-04-21-type-merging-binaryen-research.md
  - ../../raw/research/0182-2026-04-21-const-hoisting-binaryen-research.md
  - ../../raw/research/0225-2026-04-21-const-hoisting-literal-identity-followup.md
  - ../../raw/research/0183-2026-04-21-rereloop-binaryen-research.md
  - ../../raw/research/0184-2026-04-21-de-nan-binaryen-research.md
  - ../../raw/research/0185-2026-04-21-untee-binaryen-research.md
  - ../../raw/research/0186-2026-04-21-simplify-locals-nonesting-binaryen-research.md
  - ../../raw/research/0131-2026-04-20-optimize-instructions-binaryen-research.md
  - ../../raw/research/0132-2026-04-20-precompute-binaryen-research.md
  - ../../raw/research/0133-2026-04-20-heap-store-optimization-binaryen-research.md
  - ../../raw/research/0134-2026-04-20-dead-code-elimination-binaryen-research.md
  - ../../raw/research/0135-2026-04-20-heap2local-binaryen-research.md
  - ../../raw/research/0136-2026-04-20-pick-load-signs-binaryen-research.md
  - ../../raw/research/0228-2026-04-21-pick-load-signs-implementation-followup.md
  - ../../raw/research/0244-2026-04-22-pick-load-signs-primary-sources-and-code-map-followup.md
  - ../../raw/research/0137-2026-04-20-memory-packing-binaryen-research.md
  - ../../raw/research/0138-2026-04-20-once-reduction-binaryen-research.md
  - ../../raw/research/0202-2026-04-21-once-reduction-implementation-followup.md
  - ../../raw/research/0139-2026-04-20-global-refining-binaryen-research.md
  - ../../raw/research/0208-2026-04-21-global-refining-source-confirmation-followup.md
  - ../../raw/research/0140-2026-04-20-global-struct-inference-binaryen-research.md
  - ../../raw/research/0234-2026-04-21-global-struct-inference-starshine-strategy-followup.md
  - ../../raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md
  - ../../raw/research/0142-2026-04-20-reorder-locals-binaryen-research.md
  - ../../raw/research/0143-2026-04-20-remove-unused-names-binaryen-research.md
  - ../../raw/research/0220-2026-04-21-remove-unused-names-source-confirmation-followup.md
  - ../../raw/research/0235-2026-04-21-remove-unused-names-starshine-strategy-followup.md
  - ../../raw/research/0144-2026-04-20-tuple-optimization-binaryen-research.md
  - ../../raw/research/0145-2026-04-20-remove-unused-module-elements-binaryen-research.md
  - ../../raw/research/0146-2026-04-20-remove-unused-brs-binaryen-research.md
  - ../../raw/research/0147-2026-04-20-duplicate-function-elimination-binaryen-research.md
  - ../../raw/research/0148-2026-04-21-simplify-locals-binaryen-research.md
  - ../../raw/research/0149-2026-04-21-remove-unused-types-binaryen-research.md
  - ../../raw/research/0150-2026-04-21-type-refining-binaryen-research.md
  - ../../raw/research/0192-2026-04-21-type-finalizing-binaryen-research.md
  - ../../raw/research/0193-2026-04-21-type-un-finalizing-binaryen-research.md
  - ../../raw/research/0194-2026-04-21-remove-unused-non-function-elements-binaryen-research.md
  - ../../raw/research/0195-2026-04-21-remove-unused-binaryen-research.md
  - ../../raw/research/0151-2026-04-21-signature-pruning-binaryen-research.md
  - ../../raw/research/0152-2026-04-21-signature-refining-binaryen-research.md
  - ../../raw/research/0153-2026-04-21-global-type-optimization-binaryen-research.md
  - ../../raw/research/0154-2026-04-21-unsubtyping-binaryen-research.md
  - ../../raw/research/0155-2026-04-21-abstract-type-refining-binaryen-research.md
  - ../../raw/research/0156-2026-04-21-minimize-rec-groups-binaryen-research.md
  - ../../raw/research/0157-2026-04-21-reorder-types-binaryen-research.md
  - ../../raw/research/0199-2026-04-21-reorder-types-source-confirmation-followup.md
  - ../../raw/research/0158-2026-04-21-constant-field-propagation-binaryen-research.md
  - ../../raw/research/0159-2026-04-21-dead-argument-elimination-binaryen-research.md
  - ../../raw/research/0169-2026-04-21-constant-field-null-test-folding-binaryen-research.md
  - ../../raw/research/0216-2026-04-21-constant-field-null-test-folding-source-confirmation-followup.md
  - ../../raw/research/0160-2026-04-21-simplify-globals-binaryen-research.md
  - ../../raw/research/0161-2026-04-21-inlining-binaryen-research.md
  - ../../raw/research/0162-2026-04-21-propagate-globals-globally-binaryen-research.md
  - ../../raw/research/0163-2026-04-21-gufa-binaryen-research.md
  - ../../raw/research/0190-2026-04-21-gufa-cast-all-binaryen-research.md
  - ../../raw/research/0164-2026-04-21-optimize-added-constants-binaryen-research.md
  - ../../raw/research/0165-2026-04-21-optimize-added-constants-propagate-binaryen-research.md
related:
  - ../no-dwarf-default-optimize-path.md
  - ./late-pipeline-dispatch.md
---

# Binaryen Pass Folder Map

## Scope

- This page is the namespace catalog for active implemented pass folders under `docs/wiki/binaryen/passes/`.
- It can also link explicitly marked working dossiers for upstream passes that are still unimplemented in Starshine, when a pass has enough research to justify a stable folder.
- Treat each pass folder as the stable home for:
  - `wat-shapes.md` when the pass is meaningfully shape-driven
  - `binaryen-strategy.md`
  - `starshine-hot-ir-strategy.md`
  - pass-specific notes, decisions, or parity pages

## Active Module Passes

- [`duplicate-function-elimination/index.md`](duplicate-function-elimination/index.md) - Expanded module-pass dossier with a landing page plus Binaryen strategy, upstream implementation/test map, WAT-shape, Starshine strategy, upstream-vs-local-extra notes, parity pages, and an immutable raw primary-source capture; the folder now makes explicit that upstream `version_129` DFE is a small hash/equality/rewrite loop with an option-dependent iteration budget, while the current type-compaction/name-strip work lives on the Starshine-local side of the dossier and the Starshine page now points to exact MoonBit registry/dispatcher/core-rewrite locations instead of only high-level module-pass framing.
- [`remove-unused-module-elements/index.md`](remove-unused-module-elements/index.md) - Refreshed module-pass dossier with a landing page plus Binaryen strategy, implementation/test map, roots/reference-only/nullification notes, WAT-shape, retention/rewrite, parity pages, and an immutable raw primary-source capture; the folder now also makes explicit that the current Starshine side has an exact MoonBit registry/dispatcher/liveness/rewrite/type-cleanup code map instead of only high-level module-pass framing.
- [`memory-packing/index.md`](memory-packing/index.md) - Expanded module-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test-map, segment-op/trap, WAT-shape, current Starshine gap, and parity pages; the folder now makes explicit that upstream `memory-packing` rewrites passive segment users and preserves trap/drop semantics rather than only trimming active zero runs.
- [`once-reduction/index.md`](once-reduction/index.md) - Expanded module-pass dossier with a landing page plus Binaryen strategy, implementation/test-map, dominance/propagation/cycle-safety, WAT-shape, Starshine strategy, and parity pages; the folder now makes explicit that upstream `once-reduction` is a narrow once-bit plus direct-call pass with a four-owner implementation split (`Scanner`, top-level fixed-point driver, `Optimizer`, and final wrapper cleanup), conservative after-merge reasoning, trivial wrapper cleanup, and an upstream-only idempotent-annotation fast path, while current Starshine is a narrower recursive boundary-array subset rather than the full CFG/dominator engine.
- [`global-refining/index.md`](global-refining/index.md) - Refreshed module-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test-map, export/public-type/retagging notes, WAT-shape, Starshine strategy, and parity pages; the folder now also makes explicit that the real `version_129` contract lives in a tiny `GlobalRefining.cpp` + `lubs.h` + `public-type-validator.h` + `global-refining.wast` surface, while current Starshine is a narrower private-global subset rather than a full exported-boundary port.
- [`global-struct-inference/index.md`](global-struct-inference/index.md) - Expanded module-pass dossier with a landing page plus Binaryen strategy, closed-world-analysis/unnesting, WAT-shape, Starshine strategy, and parity pages; the folder now makes explicit that upstream `gsi` is layered open-world direct-global optimization plus closed-world candidate-global reasoning, while current Starshine is a narrower closed-world direct-global fold, and records that current `main` still matches the tagged `version_129` source except for comment typo fixes while the dedicated lit file is unchanged.
- [`reorder-locals/index.md`](reorder-locals/index.md) - Refreshed module-pass dossier with a landing page plus Binaryen strategy, names/roundtrip/porting, WAT-shape, Starshine strategy/code-map, parity, and multivalue-call scope pages; the folder now makes explicit that upstream `reorder-locals` is a stable frequency sorter plus zero-access body-local trim rather than coalescing, while current Starshine is a boundary-owned module pass because parameter lookup and local-name/raw-name repair cross module metadata surfaces in this repo.

## Active Hot Passes

- [`ssa-nomerge/index.md`](ssa-nomerge/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, Starshine strategy/code-map, merge-policy/canonical-slot, WAT-shape, and parity pages; the folder now makes explicit that upstream `ssa-nomerge` is shared `SSAify` logic with merges disabled rather than full phi SSA, while current Starshine is a HOT-SSA roundtrip plus raw-fallback family that may materialize predecessor copies through local SSA destruction.
- [`vacuum/index.md`](vacuum/index.md) - Refreshed folder with a landing page plus Binaryen strategy, source-confirmed implementation/test map, effect/TNH, WAT-shape, an immutable raw primary-source manifest, and a refreshed Starshine code-map page; the dossier now also makes explicit that Binaryen `version_129` `vacuum` is concentrated in `Vacuum.cpp`, that visible helper ownership lives in `branch-hints.h` and `drop.h`, that the reviewed official `version_129` release page on 2026-04-22 showed publish date **2026-04-01**, and that current Starshine still lives in a tiny `pass_manager.mbt` recursive-`nop` subset plus pipeline-level validation/writeback hygiene.
- [`dead-code-elimination/index.md`](dead-code-elimination/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test map, control-type / EH-repair notes, WAT-shape, and current Starshine HOT-strategy pages; the dossier now corrects the older over-broad local story by making explicit that upstream `version_129` `dce` is a small `TypeUpdater`-centered unreachable-shape pass, not a general effect-driven dead-result simplifier.
- [`remove-unused-names/index.md`](remove-unused-names/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test map, control-name / implicit-block / delegate notes, WAT-shape catalog, the new Starshine HOT-strategy page, and the existing parser-gap note; the folder now also makes explicit that Binaryen `version_129` `remove-unused-names` is concentrated in `RemoveUnusedNames.cpp`, that `branch-utils.h` and `shared-constants.h` own the generic scope-target and caller-delegate helper surface, and that current Starshine is a narrower HOT subset centered on same-typed block peeling and dead-label loop demotion rather than the full generic label-cleanup pass.
- [`remove-unused-brs/index.md`](remove-unused-brs/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, upstream implementation/test map, true WAT-shape catalog, the existing local family and bailout pages, parity notes, and an immutable raw primary-source manifest; the folder now also makes explicit that the reviewed official Binaryen `version_129` release page on 2026-04-22 showed publish date **2026-04-01**, and that the local Starshine page now carries an exact MoonBit registry/preset/raw-dispatch/helper-cluster/test code map instead of only high-level HOT framing.
- [`optimize-instructions/index.md`](optimize-instructions/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test map, GC/call_ref/trap-sensitive notes, WAT-shape catalog, an exact Starshine code-map page, and an immutable raw primary-source manifest; the folder now also makes explicit that the official Binaryen `version_129` release page re-checked on 2026-04-22 showed publish date **2026-04-01**, that upstream `visitIf()` is still mostly boolean / duplicated-arm cleanup rather than generic constant-if folding, and that the retired slot-16 / slot-44 generated-artifact failures were HOT-lower/writeback issues rather than a still-open pass-local corruption family.
- [`heap-store-optimization/index.md`](heap-store-optimization/index.md) - Refreshed folder with a landing page plus Binaryen strategy, swap/control-flow safety, WAT-shape, current Starshine HOT-strategy, and an immutable raw primary-source manifest; the dossier now also makes explicit that the reviewed official Binaryen `version_129` release page on 2026-04-22 showed publish date **2026-04-01**, and that the local Starshine page now carries an exact MoonBit registry/dispatcher/helper-cluster/reduced-test/CLI-replay code map instead of only high-level HOT framing.
- [`heap2local/index.md`](heap2local/index.md) - Refreshed folder with a landing page plus Binaryen strategy, validation/special-case, WAT-shape, current Starshine HOT-strategy, parity pages, and an immutable raw primary-source manifest; the dossier now also makes explicit that upstream `heap2local` is conservative GC scalarization for nonescaping exclusive allocations rather than a generic heap-to-stack pass, that the official `version_129` release page reviewed on 2026-04-22 showed publish date **2026-04-01**, and that the local Starshine page now carries an exact MoonBit registry/dispatcher/candidate-analysis/rewrite/test code map while keeping the missing nondefaultable-local fixup layer explicit.
- [`pick-load-signs/index.md`](pick-load-signs/index.md) - Refreshed folder with a landing page plus Binaryen strategy, source-confirmed implementation/test-map, WAT-shape, current Starshine HOT-strategy, parity pages, and an immutable raw primary-source manifest; the dossier now also makes explicit that upstream `version_129` is concentrated in `PickLoadSigns.cpp`, that `properties.h` owns the real sign/zero-extension matcher surface, that the tiny dedicated lit file and neighboring `optimize-instructions-sign_ext.wast` split keep the broader i64 sign-extension story from being misattributed here, and that the local Starshine page now carries an exact MoonBit registry/dispatcher/raw-skip/rewrite/test code map while keeping the missing dedicated local i64 proof cases explicit.
- [`tuple-optimization/index.md`](tuple-optimization/index.md) - Refreshed hot-pass dossier; the landing page now points at both the upstream implementation/test-map page and the new Starshine implementation-map page, the Starshine strategy page now carries exact MoonBit code locations for the main helper clusters and test lanes, the folder keeps explicit that upstream `tuple-optimization` is a conservative tuple-local splitter rather than a generic multivalue optimizer, and the checked current-`main` upstream surfaces still match the tagged `version_129` core pass and dedicated lit file.
- [`precompute/index.md`](precompute/index.md) - Refreshed hot-pass dossier with a landing page plus Binaryen strategy, source-confirmed implementation/test map, propagation/partial-precompute/GC-identity, WAT-shape, and current Starshine-gap pages; the folder now also makes explicit that plain `precompute` is the shared `Precompute.cpp` semantic-evaluator core in its non-propagating public mode, that `pass.cpp` / `opt-utils.h` own the public and nested-rerun split from `precompute-propagate`, and that the real proof surface is the broad `precompute*.wast` family rather than one tiny dedicated lit file.
- [`simplify-locals/index.md`](simplify-locals/index.md) - Refreshed hot-pass dossier with a rewritten landing page plus upstream `version_129` strategy, implementation/test-map, variant/scheduler, WAT-shape, a new structure-result/carrier bridge page, and the existing Starshine-local implementation/parity pages; the folder now makes explicit that Binaryen `simplify-locals` is a staged five-variant family with a first-cycle single-use sink phase, later tee-aware sinking, optional structure formation, late equivalent-copy cleanup, and pass-runner nondefaultable-local fixups, while current `main` only differs from the tagged source by tiny container cleanup on the checked surfaces.

## Tracking

- [`tracker.md`](tracker.md) - Canonical progress tracker for which Binaryen passes are already implemented, which ones already have dedicated living wiki coverage, and which missing `-O4z` / no-DWARF parity passes still need both implementation and docs.

## Tracked Unimplemented `-O4z` / no-DWARF Pass Research

- [`flatten/index.md`](flatten/index.md) - Working dossier for the upstream aggressive flat-IR preparation pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `9`, the formal Flat IR contract from `flat.h`, the postorder `preludes` plus named-target-temp algorithm, the EH pop-fixup step, and the current `BrOn*` / `TryTable` unsupported boundary a future port must preserve explicitly.
- [`simplify-locals-notee-nostructure/index.md`](simplify-locals-notee-nostructure/index.md) - Working dossier for the upstream aggressive no-tee/no-structure locals pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `10`, the exact `SimplifyLocals<false, false, true>` variant surface, the shared direct-single-use-sink plus late-cleanup story, and the fact that this post-`flatten` slot still allows ordinary nesting even though it refuses new tees and new control-flow result structure.
- [`merge-locals/index.md`](merge-locals/index.md) - Working dossier for the upstream higher-aggression alias-local cleanup pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `27`, the single-set-plus-simple-source LocalGraph contract, the direct-source-versus-fresh-temp canonical-slot split, and the main beginner-friendly WAT shapes a future port must preserve.
- [`code-pushing/index.md`](code-pushing/index.md) - Working dossier for the upstream early control-dependent sinking pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `20`, the contiguous-suffix plus `if`-sink algorithm, the effect/control barrier model, and the main beginner-friendly WAT shapes a future port must preserve.
- [`code-folding/index.md`](code-folding/index.md) - Working dossier for the upstream late tail-sharing pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `38`, the split between expression-exit and function-terminating folding, the movement-safety helpers, and the main beginner-friendly WAT shapes a future port must preserve.
- [`merge-blocks/index.md`](merge-blocks/index.md) - Working dossier for the upstream late cleanup pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slots `39` and `42`, the whole-function same-name shadow bailout, the named-child effect barrier, and the key beginner-friendly WAT shapes a future port must preserve.
- [`optimize-casts/index.md`](optimize-casts/index.md) - Working dossier for the upstream mid-function GC cast-reuse pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `28`, the strict earlier-motion vs looser later-reuse split, and the fact that Binaryen `version_129` only handles `ref.cast` plus `ref.as_non_null` here today.
- [`local-subtyping/index.md`](local-subtyping/index.md) - Working dossier for the upstream GC/local narrowing pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `29`, the LUB-based collection over concrete `local.get` / `local.set` / `ref.as_non_null` traffic, the dominance-aware `LocalUpdater` rewrite contract, and the main beginner-friendly WAT shapes a future port must preserve.
- [`coalesce-locals/index.md`](coalesce-locals/index.md) - Working dossier for the upstream late local-slot coalescing pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slots `30` and `35`, the value-aware interference plus greedy-coloring contract, and the main beginner-friendly WAT shapes a future port must preserve.
- [`local-cse/index.md`](local-cse/index.md) - Working dossier for the upstream local tree-reuse pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slots `11` and `31`, the scan/check/apply request model, the limited basic-block-plus-adjacent-window semantics, and the main beginner-friendly WAT shapes a future port must preserve.
- [`simplify-locals-nostructure/index.md`](simplify-locals-nostructure/index.md) - Working dossier for the upstream early no-structure locals pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `22`, the exact `SimplifyLocals<true, false, true>` variant surface, the fact that teeing and nesting still remain enabled, and the main beginner-friendly WAT shapes a future port must preserve.
- [`rse/index.md`](rse/index.md) - Working dossier for the upstream late local-redundancy pass that still remains removed in Starshine; the folder records the saved generated-artifact `-O4z` slot `46`, the locals-only scope, the CFG/value-tracking plus liveness story, the deliberate loop conservatism, and the main beginner-friendly WAT shapes a future port must preserve.
- [`dae-optimizing/index.md`](dae-optimizing/index.md) - Working dossier for the upstream late boundary-rewrite pass that still remains boundary-only in Starshine; the folder records the saved generated-artifact `-O4z` slot `48`, the relation between plain `dead-argument-elimination` and `dae-optimizing`, the closed-world direct-call and escape rules, the nested `optimizeAfterInlining` cleanup contract, and the main beginner-friendly WAT shapes a future port must preserve.
- [`inlining-optimizing/index.md`](inlining-optimizing/index.md) - Working dossier for the upstream late boundary-and-callgraph rewrite pass that still remains boundary-only in Starshine; the folder records the saved generated-artifact `-O4z` slot `49`, the whole-module planning and size heuristics, the partial-inlining splitter strategy, the direct-call-based `version_129` planner surface, and the nested `optimizeAfterInlining` cleanup contract.
- [`duplicate-import-elimination/index.md`](duplicate-import-elimination/index.md) - Refreshed dossier for the upstream late import-alias cleanup pass that still remains boundary-only in Starshine; the folder now corrects the older over-broad all-import story by source-confirming that Binaryen `version_129` only deduplicates imported functions here, with `(module, base)` buckets, exact function-type equality, first-import-wins canonicalization, and `call` / `ref.func` / start / export / module-code function-reference rewrites.
- [`simplify-globals-optimizing/index.md`](simplify-globals-optimizing/index.md) - Working dossier for the upstream late global cleanup pass that still remains boundary-only in Starshine; the folder records the saved generated-artifact `-O4z` slot `52`, the split between single-use global folding, dead/`read-only-to-write` set removal, startup-versus-runtime constant propagation, and the nested default-function rerun without prepended `precompute-propagate`.
- [`string-gathering/index.md`](string-gathering/index.md) - Refreshed dossier for the upstream late string-hoisting pass that still remains unimplemented in Starshine; the folder now also has a source-confirmed implementation/test-map page making explicit the shared `StringLowering.cpp` owner-file structure, the exact-slot `StringConst` scan plus separate `walkModuleCode(...)` coverage, the reusable immutable-global rules, the validity-first defining-global reorder before `reorder-globals`, and the current local registry-bookkeeping caveat.
- [`reorder-globals/index.md`](reorder-globals/index.md) - Working dossier for the upstream late global-layout pass that still remains boundary-only in Starshine; the folder records the saved generated-artifact `-O4z` slot `55`, the count-plus-dependency search, the public under-`128` no-op rule, the `reorder-globals-always` variant, and the main beginner-friendly WAT shapes a future port must preserve.
- [`reorder-globals-always/index.md`](reorder-globals-always/index.md) - Refreshed dossier for the upstream small-module/test/internal-fixup sibling of `reorder-globals`; the folder now also has a dedicated source-confirmed proof page that makes the exact `< 128` cutoff removal, the exact `1.0 + (i / 128.0)` smooth scoring formula, the strongest `reorder-globals.wast` / `reorder-globals-real.wast` proof split, the nested `GlobalStructInference` caller, and the current-`main` no-drift result explicit.
- [`directize/index.md`](directize/index.md) - Refreshed dossier for the upstream final indirect-call cleanup pass that still remains boundary-only in Starshine; the folder now also has a source-confirmed implementation/test-map page making explicit the real `version_129` owner split across `Directize.cpp`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, and the three dedicated `directize*` lit files, alongside the existing table-entry analysis gate, constant-vs-`select` rewrite split, trap-to-`unreachable` rule, and `directize-initial-contents-immutable` mode.

## Additional upstream-only pass research

- [`ssa/index.md`](ssa/index.md) - Working dossier for the public upstream full-`ssa` sibling of the implemented `ssa-nomerge` pass; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but it still deserves a folder because the deep `ssa-nomerge` dossier repeatedly depends on teaching the exact sibling split. The folder records the shared `SSAify.cpp` engine, the LocalGraph-driven merge-local plus incoming-`tee` algorithm, the parameter-entry prepend rule, the direct `ssa.wast` test surface, and the exact split from `ssa-nomerge`.
- [`type-ssa/index.md`](type-ssa/index.md) - New upstream-only dossier for Binaryen's public GC heap-type-precision pass `type-ssa`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but it now deserves a folder because the existing `type-merging` docs already depended on contrasting against it without giving it a canonical home. The folder records the `createdTypes` map, the tiny constructor/cast seed surface, the exact `block` / `if` / `try` carried-value rules, the explicit loop exclusion, the call/return retagging surface, and the direct `type-ssa.wast` proof file.
- [`dae2/index.md`](dae2/index.md) - New upstream-only dossier for Binaryen's public experimental DAE reimplementation `dae2`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but it now deserves a folder because the neighboring plain-`dae` docs already had to keep warning that `dae2.wast` belongs to a different pass. The folder records the used-vs-forwarded parameter lattice, the reverse fixed-point graph over direct calls and root function-type trees, the `--closed-world` + GC referenced-function rewrite gate, the replacement-type mechanism, and the effect-preserving expression-removal boundaries.
- [`souperify/index.md`](souperify/index.md) - New upstream-only dossier for Binaryen's public Souper trace-emission family `souperify` / `souperify-single-use`; this pass family is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but it now deserves a folder because the neighboring `flatten`, `simplify-locals-nonesting`, and `dataflow-optimization` docs already depended on teaching where Souper extraction fits without giving it a canonical home. The folder records the hard `Flat::verifyFlatness` precondition, the DataFlow-IR plus `LocalGraph` extraction structure, the bounded trace-growth plus `Var` replacement rules, the `if`-only `pc` / `blockpc` surface, and the exact single-use child-truncation split.
- [`alignment-lowering/index.md`](alignment-lowering/index.md) - Refreshed dossier for the upstream scalar misalignment-legalization pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and has a small official `version_129` implementation plus a dedicated lit file. The folder now also has a dedicated mechanics page for the exact helper byte-width/alignment matrix, the largest-legal-chunk selection rule, and the operand-preserving unreachable rewrite contract, alongside the narrow ordinary-`Load` / `Store` scope and the single-evaluation temp-local story a future port must preserve.
- [`dealign/index.md`](dealign/index.md) - New upstream-only dossier for the public opposite-direction sibling of `alignment-lowering`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but the neighboring alignment docs now depend on keeping the split explicit. The folder records the tiny module-pass `align -> 1` rewrite contract, the `Load` / `Store` / `SIMDLoad` / `SIMDStore` visitor surface, the no-memory bailout, and the explicit split from scalar chunk-lowering legalization.
- [`string-lowering/index.md`](string-lowering/index.md) - New upstream-only dossier for the public `string-gathering` sibling / superset that lowers the supported wasm-string surface to externref-based globals, imports, helper calls, and optional metadata; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but the neighboring string docs now depend on keeping the split from plain gathering explicit. The folder records the inherited gathering phase, the ABI-visible `string` -> `extern` type rewrite, the `string.consts` JSON-vs-magic-import contract, the narrow helper/op coverage, and the singleton-public-function-type rewrite limit.
- [`remove-unused-non-function-elements/index.md`](remove-unused-non-function-elements/index.md) - Working dossier for the upstream sibling pass published as `remove-unused-nonfunction-module-elements` and tracked locally as `remove-unused-non-function-elements`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and deserves its own folder because the real `version_129` contract is shared-engine RUME plus one `rootAllFunctions` toggle, not a mere spelling alias or a literal “never touch anything function-related” promise. The folder records the local-vs-upstream naming split, the shared `RemoveUnusedModuleElements.cpp` engine, the defined-vs-imported function boundary, and the dedicated all-features test surface.
- [`remove-unused/index.md`](remove-unused/index.md) - Working dossier for the local boundary-only registry alias `remove-unused`; this dossier exists because the short local name still hid a real historical Binaryen lineage problem after the main parity queues were already covered. The folder records that old upstream Binaryen exposed `remove-unused-functions` as a function-only direct-call reachability pass, that Binaryen replaced it in 2016 with `remove-unused-module-elements`, and that current `version_129` exposes no public `remove-unused` spelling at all.
- [`remove-unused-types/index.md`](remove-unused-types/index.md) - Working dossier for the upstream closed-world GC/type cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits directly beside the current `global-refining` / `remove-unused-module-elements` / `gsi` cluster. The folder records the closed-world plus GC gate, the public-vs-private heap-type split, the `GlobalTypeRewriter` module rewrite contract, and the main beginner-friendly module-shape families a future port must preserve.
- [`type-refining/index.md`](type-refining/index.md) - Working dossier for the upstream closed-world pre-`global-refining` type-tightening pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and is the first pass in Binaryen's closed-world GC/type cluster before `signature-pruning`, `signature-refining`, and `global-refining`. The folder records the normal-vs-`type-refining-gufa` split, the direct-struct-traffic versus GUFA inference boundary, the public-type freeze, the read/write fixup contract, and the main beginner-friendly field-shape families a future port must preserve.
- [`type-generalizing/index.md`](type-generalizing/index.md) - Working dossier for the upstream experimental family published as `experimental-type-generalizing` and `experimental-type-generalizing-with-optimizing-casts`, tracked locally under the boundary-only alias `type-generalizing`; this pass family is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local registry and fills a real teaching gap between `type-refining`, `gufa`, and `gufa-cast-all`. The folder records the local-vs-upstream naming split, the tiny `ContentOracle`-driven rewrite surface over `struct.get`, `struct.set`, `call_ref`, and optional cast tightening, and the one-signature `call_ref` boundary a future port must preserve.
- [`type-finalizing/index.md`](type-finalizing/index.md) - Working dossier for the upstream GC type-state cleanup pass `type-finalizing`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and fills a real teaching gap between the more ambitious GC/type rewrites and the smaller open/final-state cleanup that can follow them. The folder records the GC-only gate, the private-vs-public boundary, the leaf-only finalization rule, and the sibling split from local `type-un-finalizing` / upstream `type-unfinalizing` that a future port must preserve.
- [`type-un-finalizing/index.md`](type-un-finalizing/index.md) - Working dossier for the local boundary-only alias `type-un-finalizing`, which Binaryen publishes publicly as `type-unfinalizing`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local registry and deserves its own folder because the shared `TypeFinalizing.cpp` engine gives the sibling a broader private-type reopening contract than `type-finalizing`'s private-leaf-only rule. The folder records the local-vs-upstream naming split, the private-only boundary, the lack of a leaf restriction, and the shared-engine split from `type-finalizing`.
- [`gufa/index.md`](gufa/index.md) - Working dossier for the upstream whole-program contents-oracle pass family that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and already matters to the existing `type-refining` GUFA notes. The folder records the `ContentOracle` contract, the plain-vs-optimizing-vs-cast-all split, the closed-world whole-program inference boundary, and the main beginner-friendly expression-shape families a future port must preserve.
- [`gufa-optimizing/index.md`](gufa-optimizing/index.md) - Working dossier for the upstream cleanup-owning sibling of `gufa` that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and deserves its own folder because Binaryen publishes it as a separate public pass with a dedicated lit file. The folder records the shared `ContentOracle` rewrite engine, the exact nested `dce` + `vacuum` cleanup contract, the changed-functions-only rerun scope, and the explicit split from `gufa-cast-all`.
- [`gufa-cast-all/index.md`](gufa-cast-all/index.md) - Working dossier for the upstream cast-materializing sibling of `gufa` that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and deserves its own folder because Binaryen publishes it as a separate public pass with a dedicated lit file. The folder records the shared `ContentOracle` rewrite engine, the exact `optimizing = false, castAll = true` split, the post-refinalize `addNewCasts` contract, and the feature-sensitive exactness boundary.
- [`signature-pruning/index.md`](signature-pruning/index.md) - Working dossier for the upstream closed-world pre-`signature-refining` signature-cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits between `type-refining` and `signature-refining` in Binaryen's closed-world GC/type cluster. The folder records the heap-type-level DAE contract, constant-actual promotion, delayed `ChildLocalizer` reruns, public/tag/continuation/subtyping boundaries, and the main beginner-friendly call-shape families a future port must preserve.
- [`signature-refining/index.md`](signature-refining/index.md) - Working dossier for the upstream closed-world pre-`global-refining` signature-tightening pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits between `signature-pruning` and `global-refining` in Binaryen's closed-world GC/type cluster. The folder records the heap-type-wide param-LUB plus result-LUB contract, the public-vs-params-only blocker split, the `call.without.effects` special handling, and the main beginner-friendly call-and-return-shape families a future port must preserve.
- [`global-type-optimization/index.md`](global-type-optimization/index.md) - Working dossier for the upstream closed-world post-`global-refining` struct-layout cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry, is registered upstream under the `gto` shorthand, and sits between `global-refining` and the later `remove-unused-module-elements` / `remove-unused-types` / `cfp` / `gsi` cluster. The folder records the hard `--closed-world` requirement, the mutability-vs-removal split, the subtype-safe field-reorder contract, the JS-descriptor prototype keepalive rule, and the trap-preserving rewrite story a future port must preserve.
- [`constant-field-propagation/index.md`](constant-field-propagation/index.md) - Working dossier for the upstream closed-world struct-field constant-read pass family that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry, uses the upstream public aliases `cfp` / `cfp-reftest`, and sits between `remove-unused-types` and `gsi` in Binaryen's closed-world GC/type cluster. The folder records the hard `--closed-world` requirement, the copy-fixed-point analysis, the narrow `ref.test` sibling-variant story, and the atomic / packed-field boundaries a future port must preserve.
- [`constant-field-null-test-folding/index.md`](constant-field-null-test-folding/index.md) - Refreshed dossier for the upstream CFP sibling published as `cfp-reftest` and tracked locally as `constant-field-null-test-folding`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and fills the local-name gap left by the parent CFP dossier. The folder now also has a dedicated matcher-mechanics page making the exact `values.size() == 2` gate, one-classifier-bucket-per-side rule, subtype-partition proof, nullable-base `ref.as_non_null` repair, and feature-sensitive nonnullable-`ref.test` bailout explicit alongside the narrow `select(ref.test(...))` field-read rewrite surface.
- [`global-struct-inference-desc-cast/index.md`](global-struct-inference-desc-cast/index.md) - Refreshed dossier for the upstream descriptor-cast sibling published as `gsi-desc-cast` and tracked locally as `global-struct-inference-desc-cast`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and fills the variant gap left by the broader `global-struct-inference` folder. The refreshed folder now source-confirms that the real `version_129` desc-cast rewrite is a target-descriptor-singleton rule inside shared `GlobalStructInference.cpp`, corrects the older cast-input-origin overstatement, and maps the dedicated `gsi-to-desc-cast.wast` plus neighboring `gsi-desc.wast` proof surface.
- [`abstract-type-refining/index.md`](abstract-type-refining/index.md) - Working dossier for the upstream closed-world post-`gsi` abstract-struct cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits directly between `gsi` and `unsubtyping` in Binaryen's late GC/type cluster. The folder records the hard `--closed-world` requirement, the TNH-only singleton-child refinement rule, the always-on never-created-to-bottom rewrite, the descriptor/exact-cast preoptimization contract, and the fact that declared subtype-edge cleanup is intentionally left to later `unsubtyping`.
- [`unsubtyping/index.md`](unsubtyping/index.md) - Working dossier for the upstream closed-world post-`gsi` relation-pruning pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits after optional `abstract-type-refining` in Binaryen's late GC/type cluster. The folder records the hard `--closed-world` requirement, the minimal subtype-plus-descriptor fixed-point contract, the exact-vs-ordinary cast distinction, the JS-prototype keepalive rules, and the descriptor-allocation trap-preservation story a future port must preserve.
- [`minimize-rec-groups/index.md`](minimize-rec-groups/index.md) - Working dossier for the upstream GC-only type-section shrink pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and the living late-pass notes. The folder records that upstream `version_129` treats it as an explicit pass rather than a default preset slot, that its real contract is SCC splitting plus identity preservation rather than dead-type cleanup, that it uses permutations before compact brand types, and that public-group conflicts plus feature-sensitive exactness are part of the real rewrite surface.
- [`type-merging/index.md`](type-merging/index.md) - Working dossier for the upstream late closed-world GC type-compaction pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits directly beside `type-refining`, `remove-unused-types`, and `unsubtyping`. The folder records the hard closed-world+GC gate, the cast-aware supertype-then-sibling DFA refinement algorithm, the descriptor-chain rewrite contract, and the refinalization boundary a future port must preserve.
- [`reorder-types/index.md`](reorder-types/index.md) - Refreshed dossier for the upstream type-layout pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and the living late-pass notes. The folder now source-confirms that Binaryen `version_129` is a GC-only, `--closed-world` private-type layout pass over supertype/described-type predecessor edges, with a 21-factor successor-weight search and a `GlobalTypeRewriter` single-large-private-rec-group rebuild rather than the old working-contract guesswork.
- [`reorder-functions/index.md`](reorder-functions/index.md) - Refreshed dossier for the upstream function-section layout pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits beside `reorder-globals`, `reorder-locals`, and `reorder-types`. The folder now also source-confirms the pre-seeded parallel direct-call scan, the serial start/export/element bump phase, the declaration-only `requiresNonNullableLocalFixups() == false` boundary, the raw-size-vs-gzip tradeoff note, and the explicit `ref.func` / declaration TODO omissions alongside the split from the lexical debugging sibling `reorder-functions-by-name`.
- [`reorder-functions-by-name/index.md`](reorder-functions-by-name/index.md) - Refreshed dossier for the upstream lexical function-ordering sibling that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and has its own public registration plus dedicated lit file. The folder now also has a focused lexical-order proof page making explicit the exact `a->name < b->name` comparator, the declaration-only `requiresNonNullableLocalFixups() == false` boundary, the four lit-backed `$a/$b/$c` permutation proofs, and the current-`main` no-drift result alongside the split from count-based `reorder-functions`.
- [`dead-argument-elimination/index.md`](dead-argument-elimination/index.md) - Refreshed dossier for the upstream plain direct-call boundary rewrite pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and the existing `dae-optimizing` folder depends on understanding it. The folder now also has a source-confirmed implementation/test-map page that makes the shared `DeadArgumentElimination.cpp` owner split from `pass.cpp` and optimizing-only `opt-utils.h` explicit, and corrects the proof-surface story to the distributed `dae_tnh` + `dae-gc*` family rather than one fictional dedicated `dae.wast`.
- [`simplify-globals/index.md`](simplify-globals/index.md) - Refreshed dossier for the upstream plain late global cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and is the plain sibling of the existing `simplify-globals-optimizing` folder. The folder now also has a source-confirmed implementation/test-map page that makes the shared `SimplifyGlobals.cpp` owner-file structure, the public registration split in `pass.cpp`, the pass-runner boundary in `pass.h`, and the clustered lit proof surface explicit alongside the existing startup-vs-runtime propagation split, `read-only-to-write` safety rules, practical-immutability and `drop(value)` rewrite contract, and the deliberate absence of the optimizing variant's nested default-function rerun.
- [`inlining/index.md`](inlining/index.md) - Refreshed dossier for the upstream plain late whole-module inliner that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and is the plain sibling of the existing `inlining-optimizing` folder. The folder now also source-confirms the split between preserved `@metadata.code.inline` bytes and the real `no-inline` / `no-full-inline` / `no-partial-inline` function flags, plus `ModuleUtils::copyFunction` clone survival for those flags alongside the existing direct-call-only planner, layered heuristics, Pattern A / Pattern B partial-inlining rules, and plain-vs-optimizing scheduler split.
- [`inline-main/index.md`](inline-main/index.md) - Working dossier for the upstream special-case wrapper-cleanup pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits directly beside `inlining` and `monomorphize` while solving a much narrower problem. The folder records the exact `main` / `__original_main` name-based scope, the exact-one-direct-call rule, the imported-endpoint and multi-call bailouts, and the shared-helper inline repair story a future port must preserve.
- [`merge-similar-functions/index.md`](merge-similar-functions/index.md) - Working dossier for the upstream late shrink-oriented near-duplicate function parameterization pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and Batch 4 map and sits directly beside `duplicate-function-elimination`, `duplicate-import-elimination`, and `simplify-globals*`. The refreshed folder now also source-confirms the exact same-hash-vs-same-class split, the lockstep `DeepValueIterator` diff-derivation walk, the local-index-shifting helper rewrite, and the original-name-preserving thunk replacement that a future port must preserve.
- [`monomorphize/index.md`](monomorphize/index.md) - Refreshed dossier for the upstream whole-module callsite-context specialization pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and sits conceptually beside `inlining` and `inline-main` without being ordinary inlining. The folder now also has a dedicated clone-mechanics page making explicit that Binaryen derives specialized signatures from surviving `LocalGet`s in transformed context operands, turns old params into locals, shifts old vars by the param-count delta, removes returns for dropped-result clones, and repairs caller-side `(drop (call ...))` wrappers in the accepted rewrite path.
- [`monomorphize-always/index.md`](monomorphize-always/index.md) - Working dossier for the upstream testing/debugging sibling of `monomorphize` that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and deserves its own folder because Binaryen publishes it as a separate public pass that keeps legal nontrivial contextual specializations without the default usefulness rejection. The folder records the sibling identity, the shared `Monomorphize.cpp` engine, the exact usefulness-gate split, and the weak-benefit type/context shapes a future port must preserve.
- [`propagate-globals-globally/index.md`](propagate-globals-globally/index.md) - Working dossier for the upstream startup-only late-global propagation pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and already appears as a meaningful neighboring source clue in the `simplify-globals*` and `string-gathering` folders. The refreshed folder now corrects the old source-layout mistake by documenting that `version_129` implements the pass inside shared `SimplifyGlobals.cpp`, records the startup-global-chain plus active data/elem offset rewrite contract, and keeps the exact startup-only split from full `simplify-globals*` explicit.
- [`global-effects/index.md`](global-effects/index.md) - Working dossier for the upstream metadata-producing pass published as `generate-global-effects` and tracked locally as `global-effects`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and already matters to the neighboring `simplify-locals` and `vacuum` folders. The folder records the local-vs-upstream naming split, the shallow-plus-fixed-point call-graph algorithm, the `Function.effects` metadata contract, and the downstream consumer story a future port must preserve.
- [`instrument-locals/index.md`](instrument-locals/index.md) - New upstream-only dossier for Binaryen's public local-access instrumentation pass; this pass sits outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but it still deserved expansion because the tracker had no remaining `none` queue while Binaryen exposes a compact real pass whose helper-import injection, partial-type-support, and effect-invalidation contract matters to neighboring `global-effects` / `vacuum` docs. The folder records postwalk `local.get` / `local.set` wrapping, shared call-id numbering, unconditional scalar helper-import injection, the current `i64` / general-ref / typed-funcref / `pop` boundaries, and the downstream effect-analysis interaction.
- [`instrument-memory/index.md`](instrument-memory/index.md) - New upstream-only dossier for Binaryen's public memory/heap-access instrumentation pass; this pass sits outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but the neighboring `instrument-locals` dossier already depended on keeping the sibling split explicit and Binaryen's own help text understates the real surface. The folder records postwalk `load` / `store` / `memory.grow` plus GC `struct.*` / `array.*` wrapping, unconditional-vs-GC helper import injection, exact filter strings, non-uniform shared ID numbering, memory64 address widening, and the downstream effect-analysis interaction.
- [`optimize-added-constants/index.md`](optimize-added-constants/index.md) - Refreshed dossier for the upstream direct memory-address offset-folding pass that still remains removed in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry, is easy to mis-teach from its name alone, and is the plain sibling of the newer `optimize-added-constants-propagate` dossier. The folder now also has a dedicated low-memory / overflow safety page making explicit the strict `1023`-yes / `1024`-no threshold, the merged-offset rule, and the separate memory32-vs-memory64 constant-pointer normalization proof alongside the hard `--low-memory-unused` gate, the direct load/store pointer rewrite surface, and the beginner-friendly WAT-shape families.
- [`optimize-added-constants-propagate/index.md`](optimize-added-constants-propagate/index.md) - Working dossier for the upstream local-propagating load/store-offset pass that still remains removed in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and is a real public sibling of the plain pass rather than a hidden mode. The folder records the `LazyLocalGraph`-based local-pair propagation contract, the SSA-vs-helper-local split, the same hard low-memory safety gate, and the main beginner-friendly WAT-shape families a future port must preserve.
- [`simplify-locals-notee/index.md`](simplify-locals-notee/index.md) - Working dossier for the upstream `simplify-locals` family variant that still remains removed in Starshine under the local alias `simplify-locals-no-tee`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and is easy to mis-teach from the stricter neighboring `simplify-locals-notee-nostructure` dossier. The folder records the exact `SimplifyLocals<false, true, true>` identity, the upstream-vs-local naming split, the still-enabled structured block/`if`/loop result rewrites, and the main beginner-friendly WAT-shape families a future port must preserve.
- [`simplify-locals-nonesting/index.md`](simplify-locals-nonesting/index.md) - Working dossier for the upstream flatness-preserving locals-family variant published as `simplify-locals-nonesting` and tracked locally as `simplify-locals-no-nesting`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and fills the remaining dedicated-folder gap for the strongest `SimplifyLocals<false, false, false>` sibling. The folder records the upstream-vs-local naming split, the exact no-tee/no-structure/no-nesting identity, the flat copy-retargeting rule, and the real `flatten` / `dfo` / `souperify` pipeline role a future port must preserve.
- [`precompute-propagate/index.md`](precompute-propagate/index.md) - Working dossier for the upstream aggressive / nested-rerun sibling of `precompute` that still remains removed in Starshine; this pass is outside the current open-world no-DWARF top-level path, but it is already named in the local removed registry and matters to the nearby `precompute`, `dae-optimizing`, and `inlining-optimizing` scheduler notes. The folder now records not just the real public pass split from plain `precompute`, the `LazyLocalGraph`-based extra propagation phase, and the `optimizeAfterInlining(...)` scheduler role, but also the exact `propagateLocals(...)` contract: fallthrough-value analysis for sets, all-reaching-sets consensus for gets, defaultable-versus-param entry handling, and the one-extra-rerun stopping rule.
- [`avoid-reinterprets/index.md`](avoid-reinterprets/index.md) - Working dossier for the upstream load/reinterpret cleanup pass that still remains removed in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and Batch 1 pass-port map and is easy to mis-teach as generic type canonicalization. The folder records the full-width-only load scope, the single-load-chain plus fallthrough-wrapper proof, the helper-local duplication strategy, and the memory64 pointer-temp boundary a future port must preserve.
- [`loop-invariant-code-motion/index.md`](loop-invariant-code-motion/index.md) - Working dossier for the upstream loop-hoisting pass published as `licm` and tracked locally as `loop-invariant-code-motion`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and local Batch 3 pass-port map. The folder records the local-vs-upstream naming split, the fixed-point temp-local hoisting strategy, the effect-plus-child-hoistability proof, and the main loop/memory/trap bailout families a future port must preserve.
- [`i64-to-i32-lowering/index.md`](i64-to-i32-lowering/index.md) - Working dossier for the upstream whole-module i64 ABI/legalization pass that still remains boundary-only in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local boundary-only registry and whole-module transform roster. The refreshed folder records the flat-input requirement, the split-local/signature plus synthetic-high-bits-global contract, and now also has a dedicated ABI/opcode coverage page clarifying which families are directly pair-lowered, helper-backed, fallback-only, or explicitly unsupported.
- [`legalize-js-interface/index.md`](legalize-js-interface/index.md) - New upstream-only dossier for Binaryen's public JS-boundary ABI legalization family pass; this pass sits outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but the existing `i64-to-i32-lowering` docs already depended on teaching where earlier JS-boundary `i64` legalization happens. The folder records the `legalstub$` / `legalimport$` / `legalfunc$` wrapper split, temp-ret helper selection, `export-originals` and `exported-helpers` mode flags, and the extra pruning sibling `legalize-and-prune-js-interface`.
- [`legalize-and-prune-js-interface/index.md`](legalize-and-prune-js-interface/index.md) - New upstream-only dossier for Binaryen's public prune-mode JS-boundary sibling of `legalize-js-interface`; this pass sits outside the current open-world no-DWARF / saved-`-O4z` queue and is not currently named in the local registry, but the new plain-family dossier already depended on keeping explicit that this is a real second public pass name. The folder records the subclass-in-shared-file structure, the import/export/global prune matrix, the defaultable-vs-nondefaultable stub split, the post-prune `ReFinalize()` step, and the exact relation to plain `legalize-js-interface`.
- [`dataflow-optimization/index.md`](dataflow-optimization/index.md) - Working dossier for the upstream flat-IR side-graph pass published as `dfo` and tracked locally as `dataflow-optimization`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and sits directly beside the existing `flatten` / `simplify-locals*` aggressive-pipeline docs. The folder records the local-vs-upstream naming split, the hard flatness precondition, the integer-local DataFlow-IR scope, the tiny actual rewrite surface, and the loop/EH/unsupported-op boundaries a future port must preserve.
- [`const-hoisting/index.md`](const-hoisting/index.md) - Refreshed dossier for the upstream repeated-literal size pass that still remains removed in Starshine; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and is easy to mis-teach as generic constant folding or CSE. The folder now also records the exact `Literal` bit-identity grouping rule for float values, the `+0.0` / `-0.0` and NaN-payload bucket split, and the tiny stale `f64` threshold comment in the official lit file alongside the byte-size profitability rule, fresh-local entry-prelude rewrite shape, unsupported `v128` boundary, and raw-size-versus-gzip-size warning.
- [`rereloop/index.md`](rereloop/index.md) - Working dossier for the upstream control-flow reoptimizer published as `rereloop` and tracked locally as `re-reloop`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and also appears in upstream `pass.cpp` as an explicit future neighbor of the flatten-era `-O4` cluster. The folder records the local-vs-upstream naming split, the hard flatness precondition, the task-based CFG builder, the helper-label-local render protocol, and the EH unsupported boundary a future port must preserve.
- [`de-nan/index.md`](de-nan/index.md) - Working dossier for the upstream NaN-determinization pass published as `denan` and tracked locally as `de-nan`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and is easy to mis-teach as ordinary floating optimization. The folder records the local-vs-upstream naming split, the runtime NaN-to-zero helper contract, the entry-param sanitization phase, the `local.get` / fallthrough skip rules, and the global-context plus SIMD boundaries a future port must preserve.
- [`untee/index.md`](untee/index.md) - Working dossier for the upstream tee-desugaring pass `untee`; this pass is outside the current open-world no-DWARF / saved-`-O4z` queue, but it is already named in the local removed registry and fills a real teaching gap between `code-pushing` and the `simplify-locals*` family. The folder records the tee-only rewrite surface, the explicit `local.set` plus `local.get` contract, the unreachable fast-path deletion, and the split from `simplify-locals-notee` that a future port must preserve.
- [`binaryen/passes/simplify-locals-nonesting/binaryen-strategy.md`](binaryen/passes/simplify-locals-nonesting/binaryen-strategy.md) - Upstream `version_129` `simplify-locals-nonesting` strategy: exact `SimplifyLocals<false, false, false>` identity, the nonesting gate in `optimizeLocalGet(...)`, shared late equivalent-set and dead-set cleanup, and the real split from `flatten` and the looser reduced variants.
- [`binaryen/passes/simplify-locals-nonesting/implementation-structure-and-tests.md`](binaryen/passes/simplify-locals-nonesting/implementation-structure-and-tests.md) - Upstream file/test map for `simplify-locals-nonesting`: what `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, the dedicated `simplify-locals-nonesting` pass pair, and the `flatten_simplify-locals-nonesting_*` combo files each prove about the variant's public contract and pipeline role.
- [`binaryen/passes/simplify-locals-nonesting/flatness-variant-boundaries.md`](binaryen/passes/simplify-locals-nonesting/flatness-variant-boundaries.md) - Focused guide to what Binaryen really means by `preserves flatness`, why this is stricter than `-notee-nostructure`, and why the pass is a flatten-neighbor cleanup rather than a synonym for `flatten`.
- [`binaryen/passes/simplify-locals-nonesting/wat-shapes.md`](binaryen/passes/simplify-locals-nonesting/wat-shapes.md) - Beginner-friendly `simplify-locals-nonesting` shape catalog covering flat copy-retargeting positives, direct-sink-into-set shapes, preserved tee/structure/nesting negatives, and the main effect/EH/pipeline bailouts.
- [`monomorphize/binaryen-strategy.md`](monomorphize/binaryen-strategy.md) - Upstream `version_129` `monomorphize` strategy: direct-call candidate scanning, effect-safe call-context extraction, specialized-signature rebuilding, dropped-result return removal, nested `-O3` usefulness measurement, and the `MaxParams = 20` hard bailout.
- [`monomorphize/implementation-structure-and-tests.md`](monomorphize/implementation-structure-and-tests.md) - Upstream file/test map for `monomorphize`: what `Monomorphize.cpp`, `pass.cpp`, the real helper headers, and the dedicated `monomorphize*` plus `no-inline-monomorphize-inlining` lit roster each prove.
- [`monomorphize/call-context-benefit-and-boundaries.md`](monomorphize/call-context-benefit-and-boundaries.md) - Focused guide to what Binaryen really means by callsite context, including effect-safe movement, trivial-context rejection, dropped-result and return-call boundaries, and the empirical usefulness threshold story.
- [`monomorphize/wat-shapes.md`](monomorphize/wat-shapes.md) - Beginner-friendly `monomorphize` shape catalog covering constant and refined-ref positives, dropped-call rewrites, movable-allocation context, too-many-param bailouts, and usefulness-gated non-specializations.
- [`reorder-globals-always/binaryen-strategy.md`](reorder-globals-always/binaryen-strategy.md) - Upstream `version_129` `reorder-globals-always` strategy: same `ReorderGlobals.cpp` engine as `reorder-globals`, removed `< 128` bailout, smooth synthetic scoring, and the explicit internal-fixup role after `GlobalStructInference` adds helper globals.
- [`reorder-globals-always/implementation-structure-and-tests.md`](reorder-globals-always/implementation-structure-and-tests.md) - Upstream file/test map for `reorder-globals-always`: what `ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `reorder-globals.wast`, and `reorder-globals-real.wast` each prove about the sibling's public identity and small-module role.
- [`reorder-globals-always/wat-shapes.md`](reorder-globals-always/wat-shapes.md) - Beginner-friendly `reorder-globals-always` shape catalog covering tiny-module positives, dependency-preserving reorders, `global.set` heat, import-first negatives, and the fresh-helper-global repair story.

## Tail Dispatch Notes

- [`late-pipeline-dispatch.md`](late-pipeline-dispatch.md) - Compact `-O4z` / `shrink` tail roster and dispatch note, now also recording that Debian/docs.rs/README package surfaces are all incomplete in different ways, that official GitHub release pages through `version_129` plus the Chromium refs listing still anchor the current public release horizon, that the current `main` changelog on both Chromium and official GitHub still lacks a newer documented optimization-pass addition, and that the living ordered-audit summary now records the full retirement of the generated-artifact hard-corruption set instead of an open late-pass blocker cluster.

## Current Maintenance Rule

- Every implemented pass should now have a stable folder landing page, even when detailed subpages are still missing.
- Unimplemented upstream passes may also get stable folders once the research is substantial enough, but those folders must say they are unimplemented clearly and must not be mixed into the active-pass lists above.
- Now that the main no-DWARF / saved-`-O4z` queue is dossier-covered, future campaign work may also expand into upstream-only registry passes such as `souperify`, `alignment-lowering`, `dealign`, `remove-unused-non-function-elements`, `remove-unused`, `remove-unused-types`, `type-refining`, `type-generalizing`, `type-finalizing`, `type-un-finalizing`, `gufa`, `gufa-optimizing`, `gufa-cast-all`, `signature-pruning`, `signature-refining`, `global-type-optimization`, `constant-field-propagation`, `constant-field-null-test-folding`, `global-struct-inference-desc-cast`, `abstract-type-refining`, `unsubtyping`, `minimize-rec-groups`, `type-merging`, `reorder-types`, `reorder-functions`, `reorder-functions-by-name`, `reorder-globals-always`, `dead-argument-elimination`, `simplify-globals`, `inlining`, `inline-main`, `merge-similar-functions`, `monomorphize`, `monomorphize-always`, `propagate-globals-globally`, `global-effects`, `optimize-added-constants`, `optimize-added-constants-propagate`, `simplify-locals-notee`, `simplify-locals-nonesting`, `precompute-propagate`, `avoid-reinterprets`, `loop-invariant-code-motion`, `i64-to-i32-lowering`, `legalize-js-interface`, `dataflow-optimization` / `dfo`, `const-hoisting`, `rereloop`, `de-nan`, and `untee`; keep that expansion in its own section so the main parity queue above stays readable. That first obvious upstream-only expansion queue, plus the newer `souperify`, `alignment-lowering`, `dealign`, `remove-unused-non-function-elements`, `remove-unused`, `type-generalizing`, `type-finalizing`, `type-un-finalizing`, `gufa`, `gufa-optimizing`, `gufa-cast-all`, `constant-field-propagation` / `cfp`, `constant-field-null-test-folding` / `cfp-reftest`, `global-struct-inference-desc-cast` / `gsi-desc-cast`, `type-merging`, `reorder-functions`, `reorder-functions-by-name`, `reorder-globals-always`, `dead-argument-elimination`, `simplify-globals`, `inlining`, `inline-main`, `merge-similar-functions`, `monomorphize`, `monomorphize-always`, `propagate-globals-globally`, `global-effects`, `optimize-added-constants`, `optimize-added-constants-propagate`, `simplify-locals-notee`, `simplify-locals-nonesting`, `precompute-propagate`, `avoid-reinterprets`, `loop-invariant-code-motion`, `i64-to-i32-lowering`, `legalize-js-interface`, `dataflow-optimization` / `dfo`, `const-hoisting`, `rereloop`, `de-nan`, and `untee` additions, is now dossier-covered too, so future threads should justify a major-gap fallback or add another source-backed upstream-only registry candidate explicitly instead of assuming another remaining `none` entry still exists.
- Keep [`tracker.md`](tracker.md) current whenever a pass changes implementation state or gains its first dedicated living wiki page.
- Expand folders from scaffold to full multi-entry form in priority order, starting from passes that already have artifact parity work or active backlog slices.
- The last implemented landing-only folder (`remove-unused-names`) is now deepened too, so future implemented-pass wiki work should justify a substantial gap instead of assuming another obvious scaffold remains.
- The old RUB major-gap fallback is now closed too: that folder now has a dedicated upstream implementation/test-map page and a real WAT-shape page, so future threads should not treat its former official-source or shape-catalog gap as still open.
- The old simplify-locals major-gap fallback is now closed too: that folder now has a dedicated upstream implementation/test-map page, an explicit public-variant/scheduler page, a raw primary-source manifest, and a compact structure-result/carrier bridge page, so future threads should not treat its former `version_129` source-structure, provenance, or variant-surface gaps as still open.
- The old `precompute-propagate` major-gap fallback is now closed too: that folder now has a dedicated local-worklist / fallthrough / merge-boundary page plus an expanded implementation/test map, so future threads should not treat its former vague-extra-phase teaching gap as still open.
- The 2026-04-18 terminology check in [`late-pipeline-dispatch.md`](late-pipeline-dispatch.md) found the current upstream-facing pass names still aligned with this folder map; the direct Debian manpage evidence explicitly covers `--heap-store-optimization` and `--remove-unused-brs`, the official GitHub/Chromium `main` changelog still shows no newer documented optimization-pass addition relevant to this catalog, and the published `wasm_opt::Pass` enum page is incomplete enough that it omits `HeapStoreOptimization` entirely. Keep treating Binaryen `Dce` versus the wiki's `dead-code-elimination` label and Binaryen `precompute-propagate` versus the repo's `precompute` landing page as aliases, not as rename pressure.
- That same check now has directly reachable official GitHub release pages and Chromium-hosted mirror notes through `version_129`. The pass-addition story visible in those public notes is still the same one already tracked here: `--minimize-rec-groups` is present by `version_119`, `--string-lifting` and `TypeRefiningGUFA` by `version_124`, `ReorderTypes` by `version_125`, and `--remove-relaxed-simd` plus `--strip-toolchain-annotations` by `version_126`.
- The Debian manpage, docs.rs surfaces, and bundled README overview remain useful terminology aids, but they are incomplete in different directions: Debian already shows some upstream-only passes such as `--minimize-rec-groups` and `--string-lowering`; the docs.rs enum explicitly says its exposed variants follow command-line pass names with Rust capitalization conventions, but it still only surfaces older entries like `RemoveUnusedTypes` and omits newer names such as `HeapStoreOptimization`, `MinimizeRecGroups`, and `StringLowering`; the docs.rs crate overview now overstates coverage by saying `Pass` represents or exposes all Binaryen passes even though the linked enum page still omits those names; and the README overview currently misspells `RemoveUnusedBrs` as `RemoveUnsedBrs`. Keep relying on the Chromium-hosted release-note trail when the question is "was a newer upstream-only pass added or removed?" rather than just "does this older public package still know this pass name?".
- The newer reachable `version_127` / `version_128` / `version_129` changelog sections did not surface another optimization-pass addition relevant to this catalog, and the current `main` changelog on both Chromium and official GitHub still leads with `MemorySegment` -> `DataSegment` API rename notes rather than a new pass addition. That is still only a narrow freshness check, but it strengthens the current conservative reading that no newer documented optimization-pass addition is visible in the currently reachable changelog surfaces.
- When newer upstream commits or release notes show behavior drift or newly added upstream passes without a local implementation, record that here or on the owning pass page instead of silently updating older `version_129`-backed summaries as if they were current-trunk facts.
