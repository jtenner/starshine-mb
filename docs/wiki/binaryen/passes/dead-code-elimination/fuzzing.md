---
kind: workflow
status: working
last_reviewed: 2026-06-19
sources:
  - ../../../tooling/pass-fuzz-compare.md
  - ../../../../../scripts/lib/pass-fuzz-compare-task.ts
  - ../../../../../src/validate/gen_valid.mbt
  - ../../../../../src/validate/gen_valid_tests.mbt
---

# `dead-code-elimination` Fuzzing Profile

Recommended general lane: run the ordinary mixed-generator compare-pass lane for this pass:

```sh
bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dead-code-elimination --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe
```

Dedicated GenValid profile: use `--gen-valid-profile dce` for DCE-positive unreachable-shape modules:

```sh
bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dead-code-elimination-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe
```

The profile intentionally emits Binaryen v130 `DeadCodeElimination.cpp` shape families that the generic mixed lane may hit sparsely:

- `dce-non-control-unreachable-child`: a non-control numeric expression where the first unreachable operand makes later operands dead while earlier operands must be preserved as drops.
- `dce-drop-unreachable`: `drop` around `unreachable`.
- `dce-block-dead-suffix`: block suffixes after `br`, `br_table`, or literal `unreachable`.
- `dce-block-has-break-guard`: a concrete-result block whose dead tail follows a break targeting that block, covering Binaryen's `TypeUpdater::hasBreaks` guard.
- `dce-if-unreachable-condition`: `if` with an unreachable condition.
- `dce-if-both-arms-unreachable`: result `if` with both arms literally unreachable.
- `dce-loop-literal-unreachable-body`: loop whose body is literally `unreachable`.
- `dce-try-table-unreachable`: `try_table` whose body is literally `unreachable`.

Legacy `try` and stack-switching handler-label liveness remain limited/tooling-boundary coverage for now. GenValid currently emits `try_table` but does not have a dedicated legacy-try or stack-switching template in this DCE profile. Starshine's WAST lowering represents legacy `try` as a synthetic sequential check block rather than a real `Try` node, so focused local tests cover only the two observed DCE reachability cases and keep a conservative raw skip for reachable synthetic arms. Starshine still does not represent stack-switching `cont` / `resume` handler labels, so that surface remains an unsupported input with reopening criteria.

For targeted batch generation outside compare-pass, the same labels can be used with `--require-feature`, for example `--require-feature dce-block-has-break-guard`.

## Latest slice evidence

2026-06-19 multi-pop memory bulk tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs memory.fill prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs memory.copy prefix before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs memory.init prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where bulk memory side effects are evaluated before a later dead prefix value and a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model `memory.fill`, `memory.copy`, and `memory.init` as three-input/zero-result side-effecting prefixes only when their operands fit the modeled prefix stack and a later dead prefix result remains to drop. Repair replays the memory mutation in original order, replays and drops the later dead prefix result, stores each catch payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: all three focused fixtures failed at WAST/test-fixture admission while bulk memory stack effects were guarded out of the specialized legacy-pop prefix scanner; with the two pre-existing memory-prefix tests included, the focused filter reported `5` total, `2` passed, `3` failed. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*memory.*prefix*' --test-failure-json` passed `5/5`; `moon fmt`; focused DCE file test passed `200/200`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2748/2748`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6105/6105`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-memory-bulk-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-memory-bulk-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes beyond these modeled bulk memory mutations, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop global.set tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs global.set prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a mutable-global write is evaluated before a later dead prefix value and a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model `global.set` as a one-input/zero-result side-effecting prefix only when its operand fits the modeled prefix stack and a later dead prefix result remains to drop. Repair replays the global write in original order, replays and drops the later dead prefix result, stores each catch payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: the focused fixture failed at WAST/test-fixture admission while `global.set` stack effects were guarded out of the specialized legacy-pop prefix scanner. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*global.set*prefix*' --test-failure-json` passed `1/1`; `moon fmt`; focused DCE file test passed `197/197`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2745/2745`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6102/6102`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-globalset-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-globalset-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes beyond this modeled `global.set` slice, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop table mutation tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs table.set prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs table.fill prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs table.copy prefix before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs table.init prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where void table mutation side effects are evaluated before a later dead prefix value and a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model only `table.set` as a two-input/zero-result mutation and `table.fill` / `table.copy` / `table.init` as three-input/zero-result mutations when their operands fit the modeled prefix stack. Repair replays the mutation, preserves its side effect/trap ordering, replays and drops the later dead prefix result, stores each catch payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: all four focused fixtures failed at WAST/test-fixture admission while table mutation stack effects were guarded out of the specialized legacy-pop prefix scanner; with the three pre-existing table-prefix tests included, the focused filter reported `7` total, `3` passed, `4` failed. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*table.*prefix*' --test-failure-json` passed `7/7`; `moon fmt`; focused DCE file test passed `196/196`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2744/2744`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6101/6101`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-table-bulk-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-table-bulk-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes beyond these modeled void table mutations, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop table size/grow tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs table.size prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs table.grow prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where a dynamic table-size result or table-grow side effect/result is evaluated before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model only `table.size` as a zero-input/one-result prefix and `table.grow` as a two-input/one-result prefix when the table reference value and i32 delta operands fit the modeled prefix stack, then repair replays the table instruction, drops the dead i32 result, stores each payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: both focused fixtures failed at WAST/test-fixture admission while table size/grow stack effects were guarded out of the specialized legacy-pop prefix scanner. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*table.*prefix*' --test-failure-json` passed `3/3` (including the pre-existing table-prefix fixture); `moon fmt`; focused DCE file test passed `192/192`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2740/2740`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6097/6097`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-table-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-table-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes beyond the modeled table/memory/string write prefixes, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop memory size/grow tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs memory.size prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs memory.grow prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where a dynamic memory-size result or memory-grow side effect/result is evaluated before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model only `memory.size` as a zero-input/one-result prefix and `memory.grow` as a one-input/one-result prefix when the i32 delta operand fits the modeled prefix stack, then repair replays the memory instruction, drops the dead i32 result, stores each payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: both focused fixtures failed at WAST/test-fixture admission while memory size/grow stack effects were guarded out of the specialized legacy-pop prefix scanner. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*memory.*prefix*'` passed `2/2`; `moon fmt`; focused DCE file test passed `190/190`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2738/2738`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6095/6095`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-memory-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-memory-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop `string.encode_*_array` tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs string.encode_utf8_array prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs string.encode_wtf16_array prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs string.encode_lossy_utf8_array prefix before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs string.encode_wtf8_array prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where modeled string encode-array writes consume a stringref, a mutable i8/i16 array reference, and an i32 start offset, then produce a dead i32 byte/unit count before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model only these four encode-array opcodes as three-input, one-result prefixes when the operands fit the modeled prefix stack, then repair replays the operands and write, drops the dead i32 result, stores each payload in a fresh local, replays locals as direct tail-call params, and emits the direct `return_call`.
- Red-first evidence: all four focused fixtures failed at WAST/test-fixture admission while string encode-array stack effects were guarded out of the specialized legacy-pop prefix scanner. After implementation, focused `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt --filter '*string.encode*array*'` passed `4/4`; `moon fmt`; focused DCE file test passed `188/188`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2736/2736`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6093/6093`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-string-encode-array-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-string-encode-array-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, broad descriptor movement beyond the modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop struct allocation tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs struct.new prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs struct.new_desc prefix before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs struct.new_default_desc prefix before catch multi-pop parameterized tail call text` cover DCE legacy-pop admission/repair gaps where explicit-field, descriptor-bearing explicit-field, and descriptor-bearing default GC struct allocations produce a dead non-null reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model only constructors whose resolved field/descriptor operands fit the modeled prefix stack, then replay the allocation, drop the produced dead struct reference, store each payload in a fresh local, replay locals as direct tail-call params, and emit the direct `return_call`.
- Red-first evidence: focused `struct.new_desc` / `struct.new_default_desc` fixtures failed before implementation while descriptor allocations were still guarded out of this prefix family; the focused `struct.new` fixture also failed before the explicit-field constructor stack effect was modeled. After implementation, each focused test passed `1/1`; `moon fmt`; `moon test src/wast` passed `414/414`; `moon test src/passes` passed `2732/2732`; `moon info` passed with existing GenValid warnings; full `moon test` passed `6089/6089`; native `moon build --target native --release src/cmd` passed with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-struct-allocation-prefix-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-struct-allocation-prefix-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `23` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`; dedicated wasm-smith `5000/0`, Binaryen `9977/0`, Binaryen failures `23/0`. Broader arbitrary/effectful multi-payload prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, broad descriptor movement beyond this modeled direct-tail allocation family, and final DCE parity remain open.

2026-06-19 multi-pop `array.new_elem` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs array.new_elem prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a passive element segment array allocation consumes modeled i32 source-offset and length operands and produces a dead non-null array reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST opcode/parser/lowering surface now recognizes `array.new_elem`, the WAST ingress scanner and raw repair model only `array.new_elem` as a two-input, one-result prefix when the modeled prefix stack has both operands, and repair replays the offset, length, and allocation, immediately drops the dead array reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves element-segment allocation/trap ordering without admitting array writes, descriptor allocations, string encode-array writes, or broader unmodeled array stack effects.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while the high-level WAST opcode surface did not recognize `array.new_elem`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; focused `moon test src/passes --filter "dead-code-elimination repairs array.new_elem prefix before catch multi-pop parameterized tail call text"` `1/1`; `moon test src/passes` `2729/2729`; `moon info` with existing GenValid warnings; full `moon test` `6086/6086`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-array-new-elem-prefix-10000` compared `7605/10000`, with `7605` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-array-new-elem-prefix-genvalid-dce-10000` compared `8492/10000`, with `8492` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3813/0`, Binaryen `7605/0`, Binaryen failures `20/0`; dedicated wasm-smith `4256/0`, Binaryen `8492/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `array.new_data` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs array.new_data prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a passive data segment array allocation consumes modeled i32 source-offset and length operands and produces a dead non-null array reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST opcode/parser/lowering surface now recognizes `array.new_data`, the WAST ingress scanner and raw repair model only `array.new_data` as a two-input, one-result prefix when the modeled prefix stack has both operands, and repair replays the offset, length, and allocation, immediately drops the dead array reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves data-segment allocation/trap ordering without admitting `array.new_elem`, array writes, descriptor allocations, string encode-array writes, or broader unmodeled array stack effects.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while the high-level WAST opcode surface did not recognize `array.new_data`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; focused `moon test src/passes --filter "dead-code-elimination repairs array.new_data prefix before catch multi-pop parameterized tail call text"` `1/1`; `moon test src/passes` `2728/2728`; `moon info` with existing GenValid warnings; full `moon test` `6085/6085`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-array-new-data-prefix-10000` compared `7602/10000`, with `7602` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-array-new-data-prefix-genvalid-dce-10000` compared `8497/10000`, with `8497` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3811/0`, Binaryen `7602/0`, Binaryen failures `20/0`; dedicated wasm-smith `4259/0`, Binaryen `8497/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, `array.new_elem`, descriptor/default descriptor allocation prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `array.new_fixed` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs array.new_fixed prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a fixed-length array allocation consumes exactly the declared element-value count and produces a dead non-null array reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST opcode/parser/lowering surface now recognizes `array.new_fixed`, the WAST ingress scanner and raw repair model only `array.new_fixed` as an `n`-input, one-result prefix when the modeled prefix stack has at least `n` operands, and repair replays the element values plus allocation, immediately drops the dead array reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves allocation/trap ordering without admitting `array.new_elem`, array writes, descriptor allocations, or broader unmodeled array stack effects.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while the high-level WAST opcode surface did not recognize `array.new_fixed`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2727/2727`; `moon info` with existing GenValid warnings; full `moon test` `6084/6084`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-array-new-fixed-prefix-10000` compared `7607/10000`, with `7607` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-array-new-fixed-prefix-genvalid-dce-10000` compared `8497/10000`, with `8497` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`; dedicated wasm-smith `4259/0`, Binaryen `8497/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, `array.new_elem`, descriptor/default descriptor allocation prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `array.new` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs array.new prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where an array allocation consumes a modeled element value plus i32 length and produces a dead non-null array reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST opcode/parser/lowering surface now recognizes `array.new`, the WAST ingress scanner and raw repair model only `array.new` as a two-input, one-result prefix in this slot, and repair replays the element, length, and allocation, immediately drops the dead array reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves allocation/trap ordering without admitting `array.new_fixed`, `array.new_data`, `array.new_elem`, array writes, descriptor allocations, or broader unmodeled array stack effects.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while both the high-level WAST opcode surface and specialized multi-pop tail-prefix scanner did not model `array.new`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2726/2726`; `moon info` with existing GenValid warnings; full `moon test` `6083/6083`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-array-new-prefix-10000` compared `7605/10000`, with `7605` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-array-new-prefix-genvalid-dce-10000` compared `8494/10000`, with `8494` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3813/0`, Binaryen `7605/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8494/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, remaining array constructors, descriptor/default descriptor allocation prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `array.new_default` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs array.new_default prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where an array default allocation consumes a modeled i32 length and produces a dead non-null array reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST opcode/parser/lowering surface now recognizes `array.new_default`, the WAST ingress scanner and raw repair model only `array.new_default` as a one-input, one-result prefix in this slot, and repair replays the length plus allocation, immediately drops the dead array reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves allocation/trap ordering without admitting `array.new`, `array.new_fixed`, `array.new_data`, `array.new_elem`, array writes, descriptor allocations, or broader unmodeled array stack effects.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while both the high-level WAST opcode surface and specialized multi-pop tail-prefix scanner did not model `array.new_default`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2725/2725`; `moon info` with existing GenValid warnings; full `moon test` `6082/6082`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-array-new-default-prefix-10000` compared `7606/10000`, with `7606` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-array-new-default-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3813/0`, Binaryen `7606/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, remaining array constructors, descriptor/default descriptor allocation prefixes, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `string.new_*_array` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs string.new_utf8_array prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a string array decode prefix consumes a modeled concrete array reference plus start/end offsets and produces a dead string reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner and raw repair model `string.new_utf8_array` / `string.new_wtf16_array` / `string.new_lossy_utf8_array` / `string.new_wtf8_array` as three-input, one-result prefixes only when the complete prefix stack is available; repair replays the array reference and offsets, replays the decode, drops the dead string result, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves decode/trap ordering without admitting string encode-array writes, arbitrary string array operands, array constructors, or broad unmodeled string stack effects.
- Evidence: the focused `string.new_utf8_array` test failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model string array decode stack effects, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2724/2724`; `moon info` with existing GenValid warnings; full `moon test` `6081/6081`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-string-new-array-prefix-10000` compared `7608/10000`, with `7608` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-string-new-array-prefix-genvalid-dce-10000` compared `8493/10000`, with `8493` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7608/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8493/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, descriptor/default descriptor allocation prefixes, array constructors, string encode-array stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop `struct.new_default` tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs struct.new_default prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a defaultable GC struct allocation produces a dead non-null reference before a multi-payload parameterized direct tail call. The admitted shape is deliberately narrow: the WAST ingress scanner models only `struct.new_default` as a zero-input/one-result prefix in this slot, and raw repair replays the allocation, immediately drops the dead reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. This preserves the allocation/trap side-effect ordering without admitting arbitrary GC constructors, `struct.new_desc` / `struct.new_default_desc`, `array.new*`, or broader allocation-prefix movement.
- Evidence: the focused test failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model `struct.new_default`, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2723/2723`; `moon info` with existing GenValid warnings; full `moon test` `6080/6080`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-struct-default-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-struct-default-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, descriptor/default descriptor allocation prefixes, array constructors, string array encode/decode stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop reference-conversion tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs any.convert_extern prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs extern.convert_any prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where `any.convert_extern` / `extern.convert_any` consume a modeled reference and produce a dead converted reference before a multi-payload parameterized direct tail call. The admitted shape remains bounded: the scanner must have enough modeled prefix stack depth for the reference operand, then raw repair replays `ref.null extern; any.convert_extern` or `ref.null any; extern.convert_any`, drops the dead converted reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `any.convert_extern` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model the conversion stack effect.
- Evidence: focused `any.convert_extern` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; focused `extern.convert_any` sibling passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2722/2722`; `moon info` with existing GenValid warnings; full `moon test` `6079/6079`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-ref-convert-prefix-10000` compared `7608/10000`, with `7608` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-ref-convert-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7608/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, string array encode/decode stack effects, unmodeled GC/reference/table/string stack effects beyond the listed modeled prefix operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop i31.get tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs i31.get_s prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs i31.get_u prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where `i31.get_s` / `i31.get_u` consume a modeled i31 reference and produce a dead i32 before a multi-payload parameterized direct tail call. The admitted shape remains bounded: the scanner must have enough modeled prefix stack depth for the i31 operand, then raw repair replays `i32.const; ref.i31; i31.get_*`, drops the dead i32, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `i31.get_s` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model the i31 get stack effect.
- Evidence: focused `i31.get_s` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; focused `i31.get_u` sibling passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2720/2720`; `moon info` with existing GenValid warnings; full `moon test` `6077/6077`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-i31-get-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-i31-get-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, string array encode/decode stack effects, unmodeled GC/reference/table/string stack effects beyond the listed modeled prefix operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop ref.i31 tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs ref.i31 prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where `ref.i31` consumes a modeled i32 and produces a dead i31 reference before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `ref.i31` is modeled as one-value-in/one-value-out in the same prefix stack-effect family as other unary/reference operators. Raw repair replays `i32.const; ref.i31`, drops the dead i31ref, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model `ref.i31`.
- Evidence: focused `ref.i31` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2718/2718`; `moon info` with existing GenValid warnings; full `moon test` `6075/6075`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-ref-i31-prefix-10000` compared `7609/10000`, with `7609` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-ref-i31-prefix-genvalid-dce-10000` compared `8491/10000`, with `8491` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3815/0`, Binaryen `7609/0`, Binaryen failures `20/0`; dedicated wasm-smith `4256/0`, Binaryen `8491/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, string array encode/decode stack effects, unmodeled GC/reference/table/string stack effects beyond the listed modeled prefix operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop string.const tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs string.const prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a string constant produces a dead value before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `string.const` is modeled as a pure zero-input, one-result prefix value alongside the existing simple constants/locals/refs. Raw repair replays the string constant to preserve the evaluated dead value, drops it, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model `string.const` as a simple value.
- Evidence: focused `string.const` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; `moon test src/wast` `414/414`; `moon test src/passes` `2717/2717`; `moon info` with existing GenValid warnings; `moon fmt`; full `moon test` `6074/6074`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-string-const-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-string-const-prefix-genvalid-dce-10000` compared `8497/10000`, with `8497` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4259/0`, Binaryen `8497/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, string array encode/decode stack effects, unmodeled GC/reference/table/string stack effects beyond the listed modeled prefix operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop GC accessor tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs struct.get prefix before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a GC field accessor produces a dead value before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `struct.get` / `struct.get_s` / `struct.get_u` consume one modeled struct reference and produce one field value, preserving null-trap/read behavior by replaying the accessor before dropping its dead result. The raw repair then stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `struct.get` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model struct accessor stack effects.
- Evidence: focused `struct.get` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; `moon test src/wast` `414/414`; `moon test src/passes` `2716/2716`; `moon info` with existing GenValid warnings; `moon fmt`; full `moon test` `6073/6073`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-struct-get-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-struct-get-prefix-genvalid-dce-10000` compared `8494/10000`, with `8494` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8494/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, unmodeled GC/reference/table stack effects beyond the listed modeled prefix operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop descriptor/ref-cast tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs ref.get_desc prefix before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs ref.test_desc prefix before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs ref.cast_desc_eq prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where descriptor reference prefixes produce a dead value before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `ref.get_desc` consumes one modeled described reference and produces one descriptor reference, `ref.test_desc` consumes one modeled reference and produces one i32, and `ref.cast_desc_eq[_null]` consumes the modeled inspected reference (plus the legacy optional descriptor operand when present) and produces the cast reference. The same raw support admits ordinary binary `ref.test` / `ref.cast` opcodes when they arrive from binary/IR inputs. The raw repair replays the prefix, drops the produced dead i32/reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `ref.get_desc` and `ref.test_desc` fixtures failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model descriptor reference stack effects.
- Evidence: focused `ref.get_desc` DCE test failed red-first at WAST/test-fixture admission, then passed `1/1`; focused `ref.test_desc` DCE test failed red-first at WAST/test-fixture admission in the prior descriptor slice, then focused `ref.test_desc` and `ref.cast_desc_eq` DCE tests passed `1/1` each; `moon test src/wast` `414/414`; `moon test src/passes` `2715/2715`; `moon info` with existing GenValid warnings; `moon fmt`; full `moon test` `6072/6072`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-ref-get-desc-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-ref-get-desc-prefix-genvalid-dce-10000` compared `8493/10000`, with `8493` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8493/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, unmodeled reference/table/GC stack effects beyond the listed one-value-in/one-value-out reference operators, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop ref/table unary tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs ref.as_non_null prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs table.get prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where additional one-value-in/one-value-out modeled prefixes produce a dead reference before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `ref.as_non_null` consumes one modeled ref and produces one non-null ref; `table.get` consumes one modeled i32 table index and produces the table reference type. The raw repair replays the prefix, drops the produced dead reference, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `ref.as_non_null` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model this reference unary stack effect.
- Evidence: focused `ref.as_non_null` DCE test failed red-first at WAST/test-fixture admission, then focused `ref.as_non_null` and `table.get` DCE tests passed `1/1` each; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2712/2712`; `moon info` with existing GenValid warnings; full `moon test` `6069/6069`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-ref-table-prefix-10000` compared `7607/10000`, with `7607` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-ref-table-prefix-genvalid-dce-10000` compared `8494/10000`, with `8494` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8494/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, other reference/table/GC stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop reference-operator tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs ref.is_null prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs ref.eq prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where modeled reference operators produce a dead value before a multi-payload parameterized direct tail call. The admitted shape remains bounded: `ref.is_null` is modeled as one ref value consumed and one i32 produced; `ref.eq` is modeled as two ref values consumed and one i32 produced. The raw repair replays the reference prefix, drops the produced dead i32, stores each catch payload into carrier locals, replays those locals as direct tail-call params, and emits the direct `return_call`. The `ref.is_null` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model reference-operator stack effects.
- Evidence: focused `ref.is_null` DCE test failed red-first at WAST/test-fixture admission, then focused `ref.is_null` and `ref.eq` DCE tests passed `1/1` each; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2710/2710`; `moon info` with existing GenValid warnings; full `moon test` `6067/6067`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-ref-prefix-10000` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-ref-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, other reference/table/GC stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop select tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs select prefix before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs typed select prefix before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where untyped and typed `select` prefixes produce a modeled dead value before a multi-payload parameterized direct tail call. The admitted shape remains bounded: the scanner must have enough modeled prefix stack depth for the two selected values plus the i32 condition, typed selects must declare a nonempty result vector, and all produced select results are replayed and dropped before catch payloads are stored into carrier locals and replayed as direct tail-call params. The untyped fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model `select` stack effects.
- Evidence: focused untyped DCE test failed red-first at WAST/test-fixture admission, then focused untyped and typed DCE tests passed `1/1` each; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2708/2708`; `moon info` with existing GenValid warnings; full `moon test` `6065/6065`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-select-tail-prefix-10000` compared `7607/10000`, with `7607` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-select-tail-prefix-genvalid-dce-10000` compared `8499/10000`, with `8499` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`; dedicated wasm-smith `4260/0`, Binaryen `8499/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop multi-result call tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs multi-result direct call before catch multi-pop parameterized tail call text`, `dead-code-elimination repairs multi-result call_indirect before catch multi-pop parameterized tail call text`, and `dead-code-elimination repairs multi-result call_ref before catch multi-pop parameterized tail call text` cover the DCE legacy-pop admission/repair gap where modeled direct/indirect/ref call prefixes produce multiple results before a multi-payload parameterized direct tail call. The admitted shape remains bounded: direct calls must have enough modeled stack depth for params; indirect/ref calls must also have an already-modeled table-index/function-reference operand; all call results are replayed and dropped before catch payloads are stored into carrier locals and replayed as direct tail-call params. The direct-call fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner still required exactly one call result; indirect/ref siblings cover the same generalized stack-effect rule.
- Evidence: focused DCE tests `1/1` for each new fixture; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2706/2706`; `moon info` with existing GenValid warnings; full `moon test` `6063/6063`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-multiresult-call-tail-prefix-10000` compared `7605/10000`, with `7605` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multiresult-call-tail-prefix-genvalid-dce-10000` compared `8494/10000`, with `8494` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3813/0`, Binaryen `7605/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8494/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop multi-parameter direct-call tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs multi-param direct call arg before catch multi-pop parameterized tail call text` covers the DCE legacy-pop admission/repair gap where a single-result direct `call` prefix itself consumes modeled value parameters before a multi-payload parameterized direct tail call. The admitted shape remains bounded: the scanner must have enough modeled stack depth for all direct-call params, the direct call must produce exactly one result to replay and drop, and the following complete LIFO catch-payload `pop` prefix must exactly match the direct tail callee params. The fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner still admitted only zero-parameter direct producer calls, then passed after WAST ingress and raw DCE repair applied the same param-consuming stack-effect rule used for indirect/ref producer calls.
- Evidence: focused DCE test `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2703/2703`; `moon info` with existing GenValid warnings; full `moon test` `6060/6060`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-multiparam-directcall-tail-prefix-10000` compared `7607/10000`, with `7607` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multiparam-directcall-tail-prefix-genvalid-dce-10000` compared `8497/10000`, with `8497` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`; dedicated wasm-smith `4259/0`, Binaryen `8497/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop multi-parameter call_indirect/call_ref tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs multi-param call_indirect arg before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs multi-param call_ref arg before catch multi-pop parameterized tail call text` cover the next DCE legacy-pop admission/repair gap where a single-result `call_indirect` or `call_ref` prefix itself consumes modeled value parameters plus its already-modeled table-index/function-reference operand before a multi-payload parameterized direct tail call. The admitted shape remains bounded: the scanner must have enough modeled stack depth for all call params plus the operand, the typed indirect/ref call must produce exactly one result to replay and drop, and the following complete LIFO catch-payload `pop` prefix must exactly match the direct tail callee params. The `call_indirect` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner still admitted only zero-parameter indirect/ref producer calls, then both focused fixtures passed after WAST ingress and raw DCE repair applied the same param-consuming stack-effect rule.
- Evidence: focused DCE tests `1/1` for each new fixture; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2702/2702`; `moon info` with existing GenValid warnings; full `moon test` `6059/6059`; native `moon build --target native --release src/cmd` with existing pass-manager warnings; `git diff --check`; `.mbti` diff empty. Direct `.tmp/pass-fuzz-dce-multiparam-callref-indirect-tail-prefix-10000` compared `7608/10000`, with `7608` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multiparam-callref-indirect-tail-prefix-genvalid-dce-10000` compared `8493/10000`, with `8493` normalized matches, `0` cleanup-normalized/compare-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7608/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8493/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop call_indirect/call_ref tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs call_indirect arg before catch multi-pop parameterized tail call text` and `dead-code-elimination repairs call_ref arg before catch multi-pop parameterized tail call text` cover a narrow DCE legacy-pop admission/repair gap where a no-parameter, single-result `call_indirect` or `call_ref` is part of the modeled single-result prefix before a multi-payload parameterized direct tail call. The admitted shape remains bounded: an already-produced table index or function reference is consumed by the typed call, exactly one value result remains to be replayed and dropped, and the following complete LIFO catch-payload `pop` prefix exactly matches the tail params. The `call_indirect` fixture failed red-first at WAST/test-fixture admission while the specialized multi-pop tail-prefix scanner did not model call-indirect/ref producer stack effects, then both focused fixtures passed after WAST ingress and raw DCE repair admitted only no-param/single-result indirect/ref prefix producers with an available operand.
- Evidence: focused DCE tests `1/1` for each new fixture; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2700/2700`; `moon info` with existing GenValid warnings; full `moon test` `6057/6057`; native `moon build --target native --release src/cmd` with existing pass-manager warnings; `git diff --check`; `.mbti` diff empty. Direct `.tmp/pass-fuzz-dce-multipop-callref-indirect-tail-prefix-10000` compared `7602/10000`, with `7602` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-callref-indirect-tail-prefix-genvalid-dce-10000` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3811/0`, Binaryen `7602/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, unmodeled indirect/ref stack effects, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop local.tee tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs local tee arg before catch multi-pop parameterized tail call text` covers a narrow DCE legacy-pop admission/repair gap where `local.tee` is part of the modeled single-result prefix before a multi-payload parameterized direct tail call. The admitted shape remains bounded: a previously-produced single value consumed and reproduced by `local.tee`, followed by a complete LIFO catch-payload `pop` prefix whose pop order exactly matches the tail params. The test failed red-first at WAST/test-fixture admission while `local.tee` was not in the legacy-pop stack-effect scanner, then passed after WAST ingress and raw DCE repair both treated `local.tee` as a one-value-in/one-value-out prefix operation that must be replayed before its result is dropped and payloads route through carrier locals.
- Evidence: focused DCE test `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2698/2698`; `moon info` with existing GenValid warnings; full `moon test` `6055/6055`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-multipop-localtee-tail-prefix-10000` compared `7601/10000`, with `7601` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-localtee-tail-prefix-genvalid-dce-10000` compared `8499/10000`, with `8499` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3811/0`, Binaryen `7601/0`, Binaryen failures `20/0`; dedicated wasm-smith `4260/0`, Binaryen `8499/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, call_indirect/call_ref prefix producers, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop conversion tail-prefix parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs conversion arg before catch multi-pop parameterized tail call text` covers a narrow DCE legacy-pop admission/repair gap where a numeric conversion/sign-extension instruction is part of the modeled single-result prefix before a multi-payload parameterized direct tail call. The admitted shape remains bounded: a modeled one-value prefix including WebAssembly unary numeric conversion/sign-extension/reinterpret operators, followed by a complete LIFO catch-payload `pop` prefix whose pop order exactly matches the tail params. The test failed red-first at WAST/test-fixture admission while conversion prefixes were not in the legacy-pop stack-effect scanner, then passed after WAST ingress and raw DCE repair both treated the conversion family as one-value-in/one-value-out prefix operations that must be replayed and dropped before routing payloads through carrier locals.
- Evidence: focused DCE test `1/1`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2697/2697`; `moon info` with existing GenValid warnings; full `moon test` `6054/6054`; native `moon build --target native --release src/cmd` with existing pass-manager warnings; `git diff --check`. Direct `.tmp/pass-fuzz-dce-multipop-conversion-tail-prefix-10000` compared `7602/10000`, with `7602` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-conversion-tail-prefix-genvalid-dce-10000` compared `8492/10000`, with `8492` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3811/0`, Binaryen `7602/0`, Binaryen failures `20/0`; dedicated wasm-smith `4256/0`, Binaryen `8492/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, call_indirect/call_ref prefix producers, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop nop-interleaved tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs nop-prefixed const arg before catch multi-pop parameterized tail call text` and `... return_call_ref text` cover a narrow DCE legacy-pop admission/repair gap where a stack-neutral `nop` appears before the modeled single-result tail-prefix value. The admitted shape remains bounded: stack-neutral `nop` plus a modeled single-result value before a complete LIFO catch-payload `pop` prefix whose pop order exactly matches the tail params, with the already-present function-reference operand for the `return_call_ref` case. Red-first attempts with folded `nop`/conversion/call-indirect shapes exposed WAST-ingress boundaries; the final flat stack-style `nop` fixtures now pass after WAST ingress and raw DCE repair both learned to tolerate `nop` without changing the modeled prefix stack depth. Broader arbitrary/effectful multi-payload prefixes remain open.
- Evidence: `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `148/148`; `moon fmt`; `moon test src/wast` `414/414`; `moon test src/passes` `2696/2696`; `moon info` with existing GenValid warnings; full `moon test` `6053/6053`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-multipop-nop-tail-prefix-10000` compared `7604/10000`, with `7604` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-nop-tail-prefix-genvalid-dce-10000` compared `8491/10000`, with `8491` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7604/0`, Binaryen failures `20/0`; dedicated wasm-smith `4256/0`, Binaryen `8491/0`, Binaryen failures `20/0`. Broader arbitrary/effectful multi-payload prefixes, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop non-simple tail-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs unary arg before catch multi-pop parameterized tail call text`, `... load arg ... return_call_indirect text`, and `... binary arg ... return_call_ref text` cover the next DCE legacy-pop behavior gap for multi-payload non-prefix dead-call remnants before parameterized direct, indirect, and ref tail calls. The admitted shape is still bounded: a modeled single-result evaluated prefix before a complete LIFO catch-payload `pop` prefix whose pop order exactly matches the tail params, plus the already-present simple table-index/function-reference operand for indirect/ref tails. The tests failed red-first in raw DCE repair after WAST/validation admitted the raw shapes, then passed after the repair learned to replay the modeled prefix, drop its result, store each payload in fresh locals, replay the locals as tail-call params, replay the operand when needed, and emit the tail call.
- Evidence: `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `146/146`; `moon test src/wast` `414/414`; `moon fmt`; `moon test src/passes` `2694/2694`; full `moon test` `6051/6051`; `moon info` with existing GenValid warnings; native `moon build --target native --release src/cmd` with existing pass-manager warnings; `git diff --check`. Direct `.tmp/pass-fuzz-dce-multipop-nonsimple-tail-prefix-10000` compared `7607/10000`, with `7607` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-nonsimple-tail-prefix-genvalid-dce-10000` compared `8496/10000`, with `8496` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3814/0`, Binaryen `7607/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8496/0`, Binaryen failures `20/0`. Broader arbitrary payload movement, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop operand-tail parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs const arg before catch multi-pop parameterized return_call_indirect text` and `... return_call_ref text` cover the next narrow DCE legacy-pop behavior gap for multi-payload non-prefix dead-call remnants before parameterized operand tail calls. The admitted shape remains conservative: one simple evaluated value before a complete LIFO catch-payload `pop` prefix, the pop order exactly matching the tail-call params, and a modeled simple table-index or function-reference operand immediately before `return_call_indirect` / `return_call_ref`. The tests failed red-first after WAST/validation admission preserved the raw shape but raw DCE repair still lacked multi-pop operand-tail replay, then passed after the repair learned to drop the earlier value, store each payload in a fresh carrier local, replay those locals as tail-call params, replay the already-present operand, and emit the operand tail call.
- Evidence: `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `143/143`; `moon test src/wast` `414/414`; `moon fmt`; `moon test src/passes` `2691/2691`; full `moon test` `6048/6048`; `moon info` with existing GenValid warnings; native `moon build --target native --release src/cmd` with existing pass-manager warnings; `git diff --check`. Direct `.tmp/pass-fuzz-dce-multipop-operand-tail-10000` compared `7604/10000`, with `7604` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-operand-tail-genvalid-dce-10000` compared `8499/10000`, with `8499` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7604/0`, Binaryen failures `20/0`; dedicated wasm-smith `4260/0`, Binaryen `8499/0`, Binaryen failures `20/0`. Non-simple multi-payload prefixes, broader arbitrary payload movement, branch-alternative payload flow, stack-switching, and final DCE parity remain open.

2026-06-19 multi-pop direct parameterized tail-call pop slice:

- Focused test: `dead-code-elimination repairs const arg before catch multi-pop parameterized tail call text` covers the next narrow DCE legacy-pop behavior gap for multi-payload non-prefix dead-call remnants before a parameterized direct `return_call`. The admitted shape is intentionally conservative: one simple evaluated value before a complete LIFO catch-payload `pop` prefix, and direct callee params exactly matching the pop order. The test failed red-first at WAST/validation admission, then passed after WAST ingress learned to validate multi-payload LIFO pop order for this tail-param gate and raw DCE repair learned to drop the earlier evaluated value, store each payload in a fresh carrier local, replay those locals as tail-call params, and emit the direct `return_call`.
- Evidence: `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `141/141`; `moon test src/wast` `414/414`; `moon fmt`; `moon test src/passes` `2689/2689`; full `moon test` `6046/6046`; `moon info` with existing GenValid warnings; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-multipop-direct-tail-10000` compared `7609/10000`, with `7609` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-multipop-direct-tail-genvalid-dce-10000` compared `8494/10000`, with `8494` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3815/0`, Binaryen `7609/0`, Binaryen failures `20/0`; dedicated wasm-smith `4257/0`, Binaryen `8494/0`, Binaryen failures `20/0`. Multi-payload indirect/ref operand tails, non-simple multi-payload prefixes, stack-switching, and broad legacy-pop movement remain open.

2026-06-19 broad binary non-prefix parameterized tail-call pop slice:

- Focused tests: `dead-code-elimination repairs non-add binary arg before catch pop parameterized tail call text`, `... non-i32 binary arg ... return_call_indirect ...`, and `... float binary arg ... return_call_ref ...` cover the next DCE legacy-pop behavior gap for non-prefix dead-call remnants before parameterized direct/indirect/ref `return_call*` tails. Red-first, all three fixtures failed at WAST admission while only the prior narrow `i32.add` binary remnant was accepted. The fix widens WAST ingress and raw DCE repair from only `i32.add` with `i32.const` operands to the modeled numeric/comparison binary-operator family over simple value operands, while preserving the existing single-payload, simple-operand, tail-param matching gates.
- Evidence: `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `140/140`; `moon test src/wast` `414/414`; `moon fmt`; `moon test src/passes` `2688/2688`; `moon info` with existing GenValid warnings; full `moon test` `6045/6045`; native `moon build --target native --release src/cmd` with existing pass-manager warnings. Direct `.tmp/pass-fuzz-dce-broad-binary-nonprefix-param-tail-pop-10000-rerun2` compared `7603/10000`, with `7603` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 19, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-broad-binary-nonprefix-param-tail-pop-genvalid-dce-10000-rerun2` compared `8495/10000`, with `8495` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation/property/generator failures, and `20` cached Binaryen/tool command failures (`binaryen-rec-group-zero` 17, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters: direct wasm-smith `3812/0`, Binaryen `7603/0`, Binaryen failures `20/0`; dedicated wasm-smith `4258/0`, Binaryen `8495/0`, Binaryen failures `20/0`.

2026-06-16 select unreachable-false-value slice:

- Focused test: `dead-code-elimination trims select tail after unreachable false value` covers the Binaryen v130 `dce_all-features.wast` `select` family where the second value operand is the first unreachable child. Red-first Starshine trimmed the dead suffix but left the already-evaluated first value operand on the stack without a preserving `drop`; the fix rewrites the raw RPN `value unreachable value select drop` suffix to drop the first value before the preserved `unreachable` and remove the later value/select/drop tail.
- Evidence: `moon fmt`; `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `65/65`; `moon test src/passes` `2519/2519`; `moon info` with existing GenValid warnings; full `moon test` `5832/5832`; native `moon build --target native --release src/cmd` passed with existing pass-manager unused warnings. Direct `.tmp/pass-fuzz-dce-select-unreachable-false-value-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-select-unreachable-false-value-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters in both lanes: wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 select unreachable-condition slice:

- Focused test: `dead-code-elimination preserves select values before unreachable condition` covers the Binaryen v130 `dce_all-features.wast` `select` family where the condition is the first unreachable child. Red-first Starshine removed the `select` wrapper but left the two already-evaluated value operands on the stack without `drop`s; the fix rewrites the raw RPN `value value unreachable select drop` suffix to `drop` both earlier values before the preserved `unreachable`.
- Evidence: `moon fmt`; `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `64/64`; `moon test src/passes` `2518/2518`; `moon info` with existing GenValid warnings; full `moon test` `5831/5831`; native `moon build --target native --release src/cmd` passed with existing pass-manager unused warnings. Direct `.tmp/pass-fuzz-dce-select-unreachable-condition-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-select-unreachable-condition-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters in both lanes: wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 result-`if` non-control wrapper slice:

- Focused test: `dead-code-elimination trims global.set wrapper around unreachable value` reduces the Binaryen v130 `dce_all-features.wast` `global.set` family where a result `if` has both arms literally `unreachable` and therefore makes the consuming `global.set` dead. Red-first Starshine kept the `global.set`; the fix lets the raw DCE pretrim path voidify literal-unreachable result `if` nodes and trim the following non-control wrapper/suffix, while keeping return-valued typed `if` bodies on the HOT lift path.
- Evidence: `moon fmt`; `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `63/63`; `moon test src/passes` `2517/2517`; `moon info` with existing GenValid warnings; full `moon test` `5830/5830`; native `moon build --target native --release src/cmd` passed with existing pass-manager unused warnings. Direct `.tmp/pass-fuzz-dce-global-set-unreachable-if-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-global-set-unreachable-if-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters in both lanes: wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 non-control unreachable-child slices:

- Focused test: `dead-code-elimination trims non-control operands after unreachable child` reduces the Binaryen `dce_all-features.wast` `note-loss-of-non-control-flow-children` family to a dropped `i32.add` whose first operand is a result block ending in `unreachable` and whose later operand is a branch. Red-first Starshine left the later branch after trimming the operator/drop tail; the fix voidifies the branchless nonfallthrough result block and removes the branch/operator/drop suffix on the raw DCE pretrim path while preserving the existing terminal void-`if` plus trailing-`unreachable` cleanup guard.
- Focused test: `dead-code-elimination preserves branch child when trimming unary wrapper` covers the Binaryen `replace-unary-with-br-child` family. Red-first Starshine left the dead `i32.ctz`; the fix recognizes nonfallthrough value blocks consumed only by a unary operator plus `drop`, keeps the block value, and replaces the unary wrapper with a direct `drop` of that preserved child.
- Branch-operand evidence: `moon fmt`; `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `61/61`; `moon test src/wast` `384/384`; `moon test src/passes` `2515/2515`; `moon info` with existing GenValid warnings; full `moon test` `5828/5828`; native `moon build --target native --release src/cmd` passed. Direct `.tmp/pass-fuzz-dce-noncontrol-unreachable-child-10000` and dedicated `.tmp/pass-fuzz-dce-noncontrol-unreachable-child-genvalid-dce-10000` each compared `9977/10000` with `0` mismatches and `23` Binaryen/tool command failures.
- Unary-wrapper evidence: `moon fmt`; `moon test --package jtenner/starshine/passes --file dead_code_elimination_test.mbt` `62/62`; `moon test src/passes` `2516/2516`; full `moon test` `5829/5829`; native `moon build --target native --release src/cmd`; `moon info` with existing GenValid warnings. Direct `.tmp/pass-fuzz-dce-unary-unreachable-child-rerun-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Dedicated `.tmp/pass-fuzz-dce-unary-unreachable-child-rerun-genvalid-dce-10000` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache counters in both lanes: wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 legacy `Try` / `pop` feasibility boundary:

- Feasibility result: real Binaryen legacy `Try` / `pop` DCE parity is not a small DCE-only implementation in the current local representation. `@lib.Instruction` still has no real legacy `Try` or `pop`; binary decode/encode, validation/typechecking, WAST lowering, and HOT lower do not carry Binaryen catch payload `pop` flow. The existing HOT `Try` helper is not a public `@lib` surface and cannot lower to a real legacy `try`, so using it would not close parity.
- Focused tests: `dead-code-elimination documents unsupported legacy pop boundary` requires a Binaryen v130 `call-pop-catch`-style fixture with `(pop i32)` to fail closed with an explicit diagnostic naming `legacy`, `pop`, `real legacy Try`, and `catch payload`. Follow-up `src/wast` tests require `wast_text_binary_roundtrip` to use the same explicit legacy-pop and stack-switching boundary diagnostics instead of generic parser/type errors.
- Binaryen v130 probe: `wasm-opt --version` recorded `wasm-opt version 130 (version_130)` in `.tmp/dce-legacy-pop-boundary/wasm-opt-version.txt`; `wasm-opt --all-features --dce -S .tmp/dce-legacy-pop-boundary/call-pop-catch.wat -o .tmp/dce-legacy-pop-boundary/call-pop-catch.dce.wat` shows Binaryen preserving the legacy `try`, extracting `(pop i32)` into a local, and keeping the repaired nested blocks.
- Local evidence: the new focused DCE test failed red-first on the old generic `expected instruction` diagnostic, then passed after `wast_to_binary_module` gained the explicit legacy-pop precheck. The roundtrip follow-up tests then failed red-first on generic `expected instruction` / `expected type body` diagnostics and passed after `wast_text_binary_roundtrip` reused both unsupported-surface prechecks. Legacy-pop boundary validation passed: `moon fmt`; focused `dead_code_elimination_test.mbt` `59/59`; `moon test src/wast` `382/382`; `moon info` with existing GenValid warnings; `moon test src/passes` `2513/2513`; full `moon test` `5824/5824`; native `moon build --target native --release src/cmd` with existing pass-manager unused warnings. Roundtrip follow-up validation passed: `moon fmt`; `moon test src/wast` `384/384`; focused `dead_code_elimination_test.mbt` `59/59`.
- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-legacy-pop-boundary-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-legacy-pop-boundary-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Reopening criteria: add real legacy `Try` and Binaryen `pop` / catch-payload representation across `@lib`, WAST/text, binary decode+encode, validation/typechecking, HOT lift/lower, and then implement/source-test the DCE `try` type-collapse rule plus the exact `hasPop && addedBlock` nested-pop repair trigger. Do not use the synthetic legacy-try lowering as closure evidence.

2026-06-16 stack-switching fail-closed WAST boundary:

- Focused DCE and WAST coverage now require stack-switching fixtures to fail with an explicit diagnostic naming `cont`, `resume`, `resume_throw`, and `on` handler labels. This does not implement Binaryen's stack-switching DCE behavior; it makes the tooling blocker exact and fail-closed until local `@lib` / WAST representation exists.
- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-stack-switching-boundary-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-stack-switching-boundary-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 residual no-candidate and EH/tooling boundary classification:

- Focused tests in `src/passes/dead_code_elimination_test.mbt` now lock a true straight-line `no-dce-candidates` raw skip (`(func nop)`) separately from the literal-unreachable block case that must not skip. The same file adds legacy-try coverage for the current local lowering surface: when a lowered legacy synthetic try has a reachable arm, DCE skips the raw function with `legacy-synthetic-try-reachable-arm-dce-noop` so following roots stay reachable; when the lowered legacy try body and all catches are unreachable, DCE still trims the following root. The stack-switching `cont` / `resume` handler-label fixture remains rejected by the current WAST/lib type surface.
- Binaryen v130 comparison probes under `.tmp/dce-legacy-stack-boundary/` confirmed the oracle behavior to reopen against: `wasm-opt version 130 (version_130)` with `wasm-opt --all-features --dce -S` keeps code after a legacy `try` when either the body or catch can finish, removes the following root when the legacy `try` body and all catches are unreachable, and keeps stack-switching handler block result types live under a surrounding `drop` in `dce-stack-switching.wast`. These are not audit-close evidence; they narrow the remaining blockers to missing local real-`Try`/stack-switching representation and document the conservative synthetic-try guard.
- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-legacy-boundary-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-legacy-boundary-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 guarded and no-candidate raw-skip narrowing for explicit dead suffixes:

- Focused tests added in `src/passes/dead_code_elimination_test.mbt` lock Binaryen v130 behavior for explicit dead suffixes before the remaining raw skips: nested dead suffixes in functions that still need Starshine's load/call/set and loop-outer-branch HOT hazard guards, literal-unreachable block/loop collapse before trimming the containing suffix, and a literal-unreachable block that previously took the broader `no-dce-candidates` skip. The fixtures failed red-first by taking the old raw no-op paths and preserving dead code. Local `wasm-opt version 130 (version_130)` with `wasm-opt --all-features --dce -S` removes the same nested suffixes and collapses the no-candidate literal-unreachable block.
- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-explicit-dead-suffix-no-candidate-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-explicit-dead-suffix-no-candidate-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 `try_table` body-fallthrough fix:

- Focused tests added in `src/passes/dead_code_elimination_test.mbt` lock Binaryen v130 modern-EH behavior for `try_table` whose body is unreachable: a void `try_table` makes following roots dead, and a result `try_table` collapses through the following `drop` so later roots are trimmed. The first fixture failed red-first because Starshine treated the `try_table` catch-list region as if it were a normal fallthrough path.
- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-trytable-fallthrough-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-trytable-fallthrough-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `5000` hits / `0` misses, Binaryen `9977` hits / `0` misses, Binaryen failures `23` hits / `0` misses.

2026-06-16 guarded raw-skip narrowing for root dead suffixes:

- Direct mixed lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-code-elimination --out-dir .tmp/pass-fuzz-dce-guard-root-dead-suffix-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 22, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `500` hits / `4500` misses, Binaryen `1041` hits / `8936` misses, Binaryen failures `2` hits / `21` misses.
- Dedicated DCE GenValid lane: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x551a --pass dead-code-elimination --gen-valid-profile dce --out-dir .tmp/pass-fuzz-dce-guard-root-dead-suffix-genvalid-dce-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 100 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` cleanup-normalized matches, `0` mismatches, `0` validation failures, `0` property failures, `0` generator failures, and `23` Binaryen/tool command failures (`binaryen-rec-group-zero` 20, `binaryen-invalid-tag-index` 1, `binaryen-table-index-out-of-range` 1, `binaryen-bad-section-size` 1). Cache: `.tmp/pass-fuzz-cache`, wasm-smith `500` hits / `4500` misses, Binaryen `1055` hits / `8922` misses, Binaryen failures `3` hits / `20` misses.
