---
kind: concept
status: active
last_reviewed: 2026-05-25
sources:
  - ./index.md
  - ./binaryen-strategy.md
  - ./starshine-strategy.md
  - ./starshine-port-readiness-and-validation.md
  - ./linear-traces-read-only-to-write-and-reruns.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ../../../raw/research/0573-2026-05-19-sgo-v010-signoff.md
  - ../../../raw/research/0673-2026-05-25-sgo003-call-breadth-closeout.md
  - ../../../raw/research/0674-2026-05-25-sgo-full-parity-backlog-reactivation.md
  - ../../../raw/research/0675-2026-05-25-sgo-fact-table-source-audit.md
  - ../../../raw/research/0676-2026-05-25-sgo-same-init-expression-guardrails.md
  - ../../../raw/research/0677-2026-05-25-sgo-loop-independent-global-set-prefix.md
  - ../../../raw/research/0678-2026-05-25-sgo-loop-independent-local-set-prefix.md
  - ../../../raw/research/0679-2026-05-25-sgo-loop-independent-store-prefix.md
  - ../../../raw/research/0680-2026-05-25-sgo-loop-independent-i64-store-prefix.md
  - ../../../raw/research/0681-2026-05-25-sgo-loop-independent-subword-store-prefix.md
  - ../../../raw/research/0682-2026-05-25-sgo-loop-independent-float-store-prefix.md
  - ../../../raw/research/0683-2026-05-25-sgo-loop-independent-table-set-prefix.md
  - ../../../raw/research/0684-2026-05-25-sgo-loop-independent-memory-fill-prefix.md
  - ../../../../../src/passes/simplify_globals_optimizing.mbt
  - ../../../../../src/passes/simplify_globals_optimizing_test.mbt
related:
  - ../simplify-globals/index.md
  - ../propagate-globals-globally/index.md
---

# `simplify-globals-optimizing` parity matrix

This page separates two claims that must not be conflated:

- **v0.1.0 supported-surface signoff:** [`0573`](../../../raw/research/0573-2026-05-19-sgo-v010-signoff.md) remains valid for the accepted direct-pass / nested-runtime / late-tail scheduling surface.
- **Full Binaryen `SimplifyGlobals.cpp` breadth:** still incomplete and active again as a product-goal backlog after [`0674`](../../../raw/research/0674-2026-05-25-sgo-full-parity-backlog-reactivation.md). The previous [`0673`](../../../raw/research/0673-2026-05-25-sgo003-call-breadth-closeout.md) decision still closes the already-enumerated call-breadth queue; future breadth must proceed through explicit child slices with Binaryen-positive evidence, not an implicit open queue.

Status labels:

- **implemented**: Starshine has focused tests and code for the family.
- **partially implemented**: Starshine covers useful subfamilies but not the complete Binaryen behavior.
- **missing**: source-backed Binaryen family has no local implementation yet.
- **intentionally conservative**: Starshine preserves shape for safety/type/performance reasons pending a later proof.
- **unknown / needs fixture**: source suggests breadth, but Starshine needs a local reduced fixture or Binaryen probe before coding.

## Matrix

| Binaryen SGO family | Current Starshine status | Evidence / next action |
| --- | --- | --- |
| Module-wide global traffic facts | **partially implemented / `[SGO]003A` accepted** | As of [`0675`](../../../raw/research/0675-2026-05-25-sgo-fact-table-source-audit.md), `sgo_collect_global_infos(...)` records one source-shaped row with imports, exports, mutability, value type, init, total reads/writes, function read/write counts, global/table/data/element-offset reads, and element item-expression reads. Existing same-init and read-only-to-write plan flags are now recorded on the same row after their analyses run. Direct SGO fuzz preserved zero mismatches and zero validation failures. This is fact-table alignment, not full Binaryen `nonInitWritten` / `readOnlyToWrite` behavior parity; later behavior breadth remains split across explicit child slices. |
| Practical immutability / private never-written globals | **implemented** | Private mutable globals with no writes, or removable writes, become immutable; covered by focused SGO tests. |
| Single-use global initializer folding | **implemented for global-initializer uses** | Multi-instruction initializer folding into later globals is covered. The 0584 through 0588 single-use GC lit regressions pin the official startup-only `struct.new(ref.i31(i32.const 42))` fold into a later mutable global initializer, the nested `struct.new` operand variant, multiple independent folds in one module, multiple source globals folded into one later `struct.new` initializer, and the related copy-chain fold through nested global initializers without triggering nested cleanup. The 0589 negatives pin conservatism for second global use, function-code second use, imported source, and function-code-only single use. Broader object-identity/refinalization-sensitive cases stay conservative. |
| Same-as-init write removal | **partially implemented / `[SGO]003B` evidence-gated** | Direct literal, `ref.null`, `ref.func`, and several canonicalized alias/init cases are covered, including one-run/two-run guardrails. The 0594/0595/0596 non-init lit regressions pin the official scalar direct-literal same-init positive, changed-write negative, and imported-initializer negative for repeated writes equal to initial values versus non-initial, unknown, or imported-provenance writes. The 0658 closeout and 0676 guardrail slice confirm that broad expression equivalence is not currently an implementation target: block/result wrappers, alias one-shot removal, arithmetic expressions, and `select` expressions all need fresh exact Binaryen-positive evidence before any broadening. |
| Dead or redundant `global.set` rewrite to `drop(value)` | **implemented for current facts** | Never-read, same-as-init, and supported read-only-to-write writes are rewritten as `drop`, preserving operand evaluation. Broader Binaryen fact breadth can expose more cases. |
| `read-only-to-write` | **partially implemented** | Starshine covers direct, `eqz`, compare-const, many pure-condition, pure `select` self-guards with the global in any select operand, block-yielded, transparent, nested, and exact `if return; set` forms, with the 0597 lit guardrails preserving the official trailing-code, `else`-arm, and non-`return` body negatives. As of 2026-05-23 it also has a conservative stack/value-flow scanner for source-backed side-effecting safe conditions where clean `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s, independent `local.set` / `local.tee` / non-candidate `global.set`, scalar memory-load, `table.get`, or clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` operands or operandless `elem.drop` / `data.drop` are preserved and the actual `global.get` flows through non-trapping pure ops, `select`, and `i32.eqz` to the final branch; nested-`if (result i32)` arm-flow positives with independent direct-call/`call_indirect`/`call_ref`/pure/local-tee conditions, including clean direct, indirect, or reference-call operands, independent arm-local calls, clean arm-local `local.set` / non-candidate `global.set` / `local.tee`, scalar memory-load, `table.get`, or clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` / `elem.drop` / `data.drop` effects, transparent arm value blocks, supported post-if `select` value use, supported pure/ref/numeric post-if consumers (`ref.is_null`, ordinary `ref.test`, `ref.eq`, `ref.i31`, fact-sensitive `ref.i31`-fed `ref.as_non_null` / `i31.get_s` / `i31.get_u` including typed `select` results whose value operands are both proven `ref.i31`, `extern.convert_any`, `any.convert_extern`, nontrapping int-to-float conversions, float demote/promote, saturating float-to-int truncations, float div, clean `memory.size` / `table.size` nullary size queries, and selected nontrapping SIMD consumers (`i8x16.splat` / `add` / `popcnt` / `bitmask` / `all_true`, `i16x8.splat` / `abs` / `neg` / `eq` / `all_true` / `bitmask`, `i32x4.splat` / `neg` / `add` / `eq` / `all_true` / `bitmask` / `extract_lane`, `i64x2.splat` / `neg` / `eq` / `bitmask` / `all_true`, `f32x4.splat` / `abs` / `neg` / `sqrt` / `div` / `ne` / `i32x4.all_true`, and `f64x2.splat` / `abs` / `neg` / `add` / `ne` / `i64x2.all_true` chains, plus lane extracts for `i8x16` / `i16x8` / `i64x2` / `f32x4` / `f64x2`, all six `*.replace_lane` forms, integer SIMD shifts for `i8x16` / `i16x8` / `i32x4` / `i64x2`, all remaining parser-backed SIMD comparisons for `i8x16`, `i16x8`, `i32x4`, `i64x2`, `f32x4`, and `f64x2`, basic nontrapping SIMD arithmetic/unary ops (integer lane abs/neg, saturating add/sub, sub/mul/min/max/avgr, `i64x2.add`/`sub`/`mul`, f32/f64 ceil/floor/trunc/nearest/sqrt and add/sub/mul/div/min/max/pmin/pmax), core v128/shuffle ops (`v128.not`, `v128.any_true`, `v128.and`/`andnot`/`or`/`xor`, `i8x16.shuffle`, `i8x16.swizzle`, and ternary `v128.bitselect`), and parser-backed SIMD extension/conversion/narrow/extmul/dot/extadd-pairwise ops (`extend_low/high`, `f32x4.demote_f64x2_zero`, `f64x2.promote_low_f32x4`, saturating vector truncation, integer-to-float vector conversion, `narrow`, `extmul`, `i32x4.dot_i16x8_s`, and `i16x8` / `i32x4` `extadd_pairwise`))) before the outer branch, flat or nested transparent result blocks plus non-branching value-loop wrappers around direct/pure supported FlowScanner condition chains, value loops yielding a direct candidate read to adjacent `i32.eqz`, compare-const, or reverse compare-const, and value loops yielding a simple nontrapping pure condition chain to adjacent `i32.eqz`, compare-const, or reverse compare-const, block-yielded supported FlowScanner chains in exact `if return; set` bodies, clean value-producing `if` wrappers inside block conditions and exact `if return; set` block conditions when the wrapper condition is independent and one arm yields a supported size-query/SIMD chain, and one or more transparent result blocks or a non-branching transparent result loop wrapping the nested-if plus supported pure/select post-consumers; and the official recursive nested-pattern carveout for two- and three-layer no-else same-global patterns. No-catch `try_table` wrappers are now also covered for direct/`i32.eqz`/compare condition guards, exact `if return; set` variants, reference-specific `ref.is_null` condition / `if return; set`, and supported external pure-condition chains from 0603 through 0607; catch-bearing wrappers remain conservative. Full Binaryen `FlowScanner` behavior remains incomplete. |
| Side-effecting-but-safe condition value-flow cases | **partially implemented** | First slices landed for official-style `local.set` / non-candidate `global.set` / `local.tee` / `i32.load` / `global.get + const` / `select` / `i32.eqz` positives plus clean `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s, clean `local.set`, non-candidate `global.set`, clean `table.set`, clean `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` / `elem.drop` / `data.drop`, scalar memory-load, and `table.get` probes plus extra pure-op chains between the global-derived value and final branch, lit-style nested-if arm-flow cases where the global is not the nested condition, direct, indirect, or reference-call operands are clean, and independent arm-local calls do not consume the global-derived value, and recursive nested-pattern positives where inner no-else self-guards precede the final same-global condition read. The nested-if arm-flow subset now also allows clean arm-local `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s / `local.set` / non-candidate `global.set` / `local.tee`, scalar memory-load, `table.get`, or clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` / `elem.drop` / `data.drop` effects, transparent arm value blocks, post-if `select` value use, nested-if results that flow through supported pure post-if consumers such as `i32.eqz`, nontrapping reference/GC conversions such as `ref.is_null`, ordinary `ref.test`, `ref.eq`, `ref.i31`, fact-sensitive `ref.i31`-fed `ref.as_non_null` / `i31.get_s` / `i31.get_u` including typed `select` results whose value operands are both proven `ref.i31`, `extern.convert_any`, and `any.convert_extern`, nontrapping numeric conversions/division such as int-to-float, float demote/promote, saturating float-to-int truncation, float div, clean `memory.size` / `table.size` nullary size queries, and selected nontrapping SIMD consumers (`i8x16.splat` / `add` / `popcnt` / `bitmask` / `all_true`, `i16x8.splat` / `abs` / `neg` / `eq` / `all_true` / `bitmask`, `i32x4.splat` / `neg` / `add` / `eq` / `all_true` / `bitmask` / `extract_lane`, `i64x2.splat` / `neg` / `eq` / `bitmask` / `all_true`, `f32x4.splat` / `abs` / `neg` / `sqrt` / `div` / `ne` / `i32x4.all_true`, and `f64x2.splat` / `abs` / `neg` / `add` / `ne` / `i64x2.all_true` chains, plus lane extracts for `i8x16` / `i16x8` / `i64x2` / `f32x4` / `f64x2`, all six `*.replace_lane` forms, integer SIMD shifts for `i8x16` / `i16x8` / `i32x4` / `i64x2`, all remaining parser-backed SIMD comparisons for `i8x16`, `i16x8`, `i32x4`, `i64x2`, `f32x4`, and `f64x2`, basic nontrapping SIMD arithmetic/unary ops (integer lane abs/neg, saturating add/sub, sub/mul/min/max/avgr, `i64x2.add`/`sub`/`mul`, f32/f64 ceil/floor/trunc/nearest/sqrt and add/sub/mul/div/min/max/pmin/pmax), core v128/shuffle ops (`v128.not`, `v128.any_true`, `v128.and`/`andnot`/`or`/`xor`, `i8x16.shuffle`, `i8x16.swizzle`, and ternary `v128.bitselect`), and parser-backed SIMD extension/conversion/narrow/extmul/dot/extadd-pairwise ops (`extend_low/high`, `f32x4.demote_f64x2_zero`, `f64x2.promote_low_f32x4`, saturating vector truncation, integer-to-float vector conversion, `narrow`, `extmul`, `i32x4.dot_i16x8_s`, and `i16x8` / `i32x4` `extadd_pairwise`)) before the outer branch, flat or nested transparent result blocks plus non-branching value-loop wrappers around direct/pure supported FlowScanner condition chains, block-yielded supported FlowScanner chains in exact `if return; set` bodies, clean value-producing `if` wrappers inside block conditions and exact `if return; set` block conditions when the wrapper condition is independent and one arm yields a supported size-query/SIMD chain, and one or more transparent value blocks, the matching non-branching value loop whose result is the outer branch condition, or a non-branching value loop whose direct result feeds an adjacent `i32.eqz`, compare-const, or reverse compare-const branch condition, plus a non-branching value loop whose simple nontrapping pure result feeds adjacent `i32.eqz`, compare-const, or reverse compare-const. Guardrails keep trapping memory loads and `table.get`s whose address/index is global-derived, tainted `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` operands or operandless `elem.drop` / `data.drop`, tainted `local.tee` / `drop`, multiple same-global reads in one condition, nested conditions steered by the global, post-if call operands, `call_indirect` / `call_ref` operands, or trapping post-if loads that consume the global-derived value, nested arm blocks where the global feeds a trapping load, block-wrapped post-if loads that consume the global-derived value, nullable or non-`ref.i31`-proven `i31.get_s`/`i31.get_u`, trapping regular float-to-int truncations, effectful `memory.grow` / `table.grow` operands fed by the global, SIMD values feeding post-consumer calls (including lane/shift/replace-lane/compare/arithmetic/core-bitwise/extadd-pairwise/extension/conversion results) or global-derived SIMD loads, unsupported SIMD/parser forms not yet source-backed by Starshine WAT lowering, nested patterns with `else`, and extra dropped same-global reads. Remaining side-effecting lit families need separate fixtures and broader value-flow accounting. |
| Immutable copy-chain canonicalization | **implemented for exact type** | Exact-type immutable ancestor rewrites are covered in globals and function bodies. The 0581 prefer-earlier lit regression pins the official imported-root copy chain so later global initializers and live function-body reads retarget to the earliest equivalent global. Subtyping/refinalization-sensitive copy-chain widening is intentionally not implemented. |
| Startup propagation into later globals / element offsets / data offsets / table initializers | **partially implemented** | Supported constants are propagated into later global initializers, table initializers, active elem offsets, and data offsets. The 0580 offsets-lit regression pins defined immutable offsets in active data/elem segments, imported offset preservation, passive data tolerance, and no nested cleanup for startup-only segment/global-initializer rewrites. The 0582 nested-GC lit regression pins defined immutable propagation into nested `struct.new` global-initializer children while preserving the imported middle child. Reference-typed element item expressions are intentionally preserved until exact type/refinalization safety is proven. |
| Runtime linear-trace propagation | **partially implemented** | Straight-line, top-level noise, adjacent/nested plain blocks, no-else, then-body, else-local same-arm, loop-local same-body, and `try_table` body-local shapes, imported/exported same-trace writes, and reference/null facts are covered. The 0598 direct function-effects slice preserves runtime facts across ordinary direct calls for globals the callee does not syntactically/transitively set, while keeping mutated globals conservative; 0599 carries the same effect-filtered facts through plain blocks whose nested barriers are only ordinary direct calls or similarly carryable plain blocks; 0600 rewrites facts created inside `else` arms without importing pre-`if` facts or joining after the `if`; 0601 rewrites facts created inside loops without importing pre-loop facts or carrying facts out; 0602 rewrites facts inside `try_table` bodies while clearing facts after the exception-control join. Broader `ConstantGlobalApplier` adjacency remains open. |
| Call/control/write barriers | **implemented for current runtime model** | Imported calls, indirect calls, `call_ref`, dynamic return-call targets, branches, returns, throws, else joins, post-if/post-loop joins, post-`try_table` joins, and non-constant writes to the same tracked global clear or block facts in tests. The 0591/0592/0593 function-effects guardrails pin that calls to `$get`/`$set` and wrong-global condition reads paired with such calls are not counted as local syntactic read-only-to-write events; 0598 narrows ordinary direct-call runtime fact invalidation with a module-local global-set summary, 0599 only lets that narrowed invalidation survive plain-block wrapping, 0600 keeps pre-`if` facts out of else arms while allowing else-local facts, 0601 keeps pre-loop facts out of loop bodies while allowing loop-local facts, and 0602 permits `try_table` body-local facts while still clearing after the `try_table`. The 0634 read-summary study found a Binaryen positive for a direct no-read/no-write call in a read-only-to-write condition, and 0671 implements the first summary-backed direct-call subset: fixed-point per-global reads plus mutates summaries allow zero-param/one-result direct calls that neither read nor mutate the candidate global, including wrong-global reads, while candidate-derived operands, imported/dynamic calls, and callee candidate reads remain conservative. Needs comparison against every Binaryen `LinearExecutionWalker` corner and any future standalone generated-effects analysis before full parity. |
| Reference typed replacements | **partially implemented** | Function-body `ref.null`, externref-null, and `ref.func` replacements are covered; element item expressions remain conservative. |
| GC/refinalization-sensitive replacements | **intentionally conservative / partial** | Some function-body replacements validate through the normal pipeline. Broader type-changing replacement/refinalization surfaces, especially around typed elements or more precise refs, remain open. The 0583 `simplify-globals-gc.wast` probe pins the less-refined alias negative and records the `ref.cast(ref.func-global)` positive as blocked on normal `ref.cast` WAT support and/or focused validation/refinalization work. |
| Typed element item-expression preservation | **intentionally conservative** | Starshine keeps reference `global.get`s in typed element item expressions to avoid invalid type/refinalization drift; this is a known difference from broader Binaryen replacement surfaces. |
| Optimizing wrapper touched-function tracking | **implemented for current scheduler** | Code rewrites mark touched functions; startup-only rewrites do not. Nested cleanup is function-filtered. |
| Nested default cleanup sequence | **partially implemented / artifact-tuned** | Starshine uses an accepted pruned nested list without `precompute-propagate`; Binaryen runs the full default function optimization pipeline per changed function. Exact scheduler parity remains `[SGO]004`, not `[SGO]003`. |
| Plain `simplify-globals` / `propagate-globals-globally` shared-engine relationship | **missing as a shared local engine** | `simplify-globals-optimizing` owns the current active implementation. Plain `simplify-globals` and `propagate-globals-globally` remain boundary-only; a future shared substrate should expose the same core with variant-specific wrappers. |

## 2026-05-20 implementation slices

Target: **safe `select`-mediated `read-only-to-write` value-flow**, narrowed to source/probe-backed Binaryen-shaped positives.

Safety rule for the landed subset:

- accept pure `select` self-guard stack shapes where two operands are constants and the `global.get $g` appears as the select condition, first selected value, or second selected value before an outer no-else same-global constant `global.set`,
- accept side-effecting stack shapes where clean `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s, independent `local.set` / `local.tee` / non-candidate `global.set`, scalar memory-load, `table.get`, or clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` operands or operandless `elem.drop` / `data.drop` plus a single actual `global.get $g` flow through supported non-trapping pure ops, `select`, optional `i32.eqz`, and finally an outer no-else same-global constant `global.set`,
- support the global-derived value as the `select` condition input, the first selected value input, or the second selected value input, including extra pure operators such as `i32.xor` after `global.get $g; const; i32.add`,
- preserve clean `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s, independent `local.set` / `local.tee`, non-candidate `global.set`, scalar memory-load, `table.get`, and clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` / `elem.drop` / `data.drop` effects while removing only fake global state; later nested cleanup may simplify pure `select` / `i32.eqz` residue when the global replacement makes it constant,
- require the outer body to be the already-supported same-global constant `global.set`,
- preserve negatives where `global.get $g` feeds a trapping memory load or `table.get`, escapes through `local.set` / `local.tee` / `drop`, flows into `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy`, is stored through another `global.set`, appears multiple times in the same condition, or otherwise reaches a non-condition consumer.

Follow-up in the same slice also accepts source-backed nested-if arm-flow positives:

- `call $foo; if (result i32) ... else (global.get $g) end; if (then global.set $g const)`, plus ordinary `call_ref` with clean operands, where the call may have effects but clean operands and the condition do not depend on the candidate global,
- pure/local-set/global-set/table-set/local-tee/scalar-load/`table.get` refinements of that independent nested condition,
- arm-local calls, including result calls with supported pure consumers, when they are independent of the candidate global,
- clean arm-local `nop` / clean void blocks / clean void loops / clean void `if`s / clean result blocks / clean result loops / clean result `if`s / `local.set` / non-candidate `global.set` / `local.tee`, scalar memory-load, `table.get`, or clean `table.set` / `i32.store` / `i64.store` / `i32.store8` / `i32.store16` / `i64.store8` / `i64.store16` / `i64.store32` / `f32.store` / `f64.store` / `memory.fill` / `memory.copy` / `memory.init` / `table.fill` / `table.init` / `table.copy` / `elem.drop` / `data.drop` effects and transparent arm value blocks that do not consume the candidate global,
- a nested-if arm result used as a post-if `select` value with clean sibling operands,
- a nested-if arm result followed by a supported pure consumer such as `i32.eqz` before the outer same-global write guard,
- one or more transparent result blocks wrapping the nested-if plus supported pure/select post-consumers when the final block result is the outer branch condition,
- clean value-producing `if` wrappers inside block conditions and exact `if return; set` block conditions when the wrapper condition is independent and a candidate arm flows through an already-supported size-query/SIMD chain,
- a clean value-producing `if` wrapper around another supported nested-if arm-flow result, including supported pure post-consumers such as `i32.eqz` and the clean-sibling `select` value form,
- reference-typed nested-if arm results through nontrapping `ref.is_null`, ordinary `ref.test`, `ref.eq`, `ref.i31`, fact-sensitive `ref.i31`-fed `ref.as_non_null` / `i31.get_s` / `i31.get_u` including typed `select` results whose value operands are both proven `ref.i31`, `extern.convert_any`, and `any.convert_extern` post-consumers, including transparent result-block wrappers around the same flow,
- numeric nested-if arm results through nontrapping int-to-float conversions, float demote/promote, saturating float-to-int truncations, and float div before the outer branch,
- preserve the neighboring negatives where `global.get $g` is the nested `if` condition and therefore can decide whether the call runs, where the nested-if result becomes an operand to a post-if call, where a post-if `select` result feeds a trapping load, where a block-wrapped post-if load or post-loop `i32.eqz` / pure-`i32.eqz` / compare / reverse-compare load consumes the global-derived value, where the global-derived value is stored through `local.tee`, where a transparent arm block feeds it to a trapping load, where a clean `if` wrapper result feeds a trapping load, where a clean condition `if` wrapper arm feeds it to a trapping load, where the candidate read steers an inner if-wrapper branch, where a nontrapping reference-test/result feeds a post-consumer call, where the reference op may trap such as nullable `i31.get_s`, or where the numeric op may trap such as regular float-to-int truncation.

The next recursive nested-pattern slice adds the Binaryen lit carveout where a condition block may contain another no-else same-global read-only-to-write pattern before the final condition read. Starshine now covers the two-layer `$nested-pattern` shape and the three-layer `$nested-pattern-thrice` shape, while preserving the official near-miss negatives for an inner `if` with `else` and for an extra dropped same-global read that makes total reads exceed safe pattern reads. Because the resulting value-block/control shape currently exposes a HOT nested-cleanup verification frontier, the SGO wrapper conservatively filters touched functions with value blocks containing `if` control out of the nested cleanup lane and returns the valid SGO core rewrite instead of forcing the cleanup sequence.

The 2026-05-23 loop self-guard inventory records the current non-branching value-loop subset as a deliberate stopping point rather than an invitation to reuse the whole block FlowScanner inside loops. Already-covered no-op const/drop prefixes before a yielded candidate read come through the shared direct/pure condition matchers. The [`0677`](../../../raw/research/0677-2026-05-25-sgo-loop-independent-global-set-prefix.md) follow-up covers the source-backed direct loop prefix where clean constant writes to a different global precede the yielded candidate read, [`0678`](../../../raw/research/0678-2026-05-25-sgo-loop-independent-local-set-prefix.md) adds the matching Binaryen-positive constant `local.set` prefix, [`0679`](../../../raw/research/0679-2026-05-25-sgo-loop-independent-store-prefix.md) adds a narrow constant-operand `i32.store` prefix, [`0680`](../../../raw/research/0680-2026-05-25-sgo-loop-independent-i64-store-prefix.md) adds the matching `i64.store` prefix, [`0681`](../../../raw/research/0681-2026-05-25-sgo-loop-independent-subword-store-prefix.md) adds the matching integer subword store family, [`0682`](../../../raw/research/0682-2026-05-25-sgo-loop-independent-float-store-prefix.md) adds the matching `f32.store` / `f64.store` family with paired candidate-derived store negatives, [`0683`](../../../raw/research/0683-2026-05-25-sgo-loop-independent-table-set-prefix.md) adds the clean `table.set` loop prefix with a candidate-derived table-index negative, and [`0684`](../../../raw/research/0684-2026-05-25-sgo-loop-independent-memory-fill-prefix.md) adds the three-constant `memory.fill` loop prefix with a candidate-derived destination negative, all while preserving the same narrow direct-loop contract. Future loop broadening should require a fresh Binaryen probe and focused negative for each exact shape, especially around branch/control bodies, `br_if`, returns, trapping global-derived loads, memory/table growth, atomics, SIMD memory operations, relaxed SIMD, broad unprobed bulk operations, and calls whose operands consume the candidate value.

The follow-up probe inventory in [`0574`](../../../raw/research/0574-2026-05-23-sgo-next-breadth-probe-inventory.md) downranks broad same-init expression work for now: Binaryen preserved read-present block-wrapped same-init writes, alias-init/direct-write one-shot removal, else-arm runtime reads, and block-wrapped runtime-set operands in focused probes. The follow-up dominance-lit regressions in [`0575`](../../../raw/research/0575-2026-05-23-sgo-dominance-lit-regression.md) and [`0590`](../../../raw/research/0590-2026-05-23-sgo-dominance-post-call-else-guardrails.md) map the official `simplify-globals-dominance.wast` runtime trace against local tests and pin the missing source-shaped positive plus boundaries: a dominated then-body read is rewritten before a recursive call barrier, while value-observable reads after calls, else-arm reads, and post-if joins remain conservative. The same-init/runtime guardrail slice in [`0576`](../../../raw/research/0576-2026-05-23-sgo-same-init-runtime-guardrails.md) pins alias-init/direct-literal one-shot behavior and block-wrapped runtime-set operands as conservative boundaries. The official-lit breadth inventory in [`0577`](../../../raw/research/0577-2026-05-23-sgo-official-lit-breadth-inventory.md) maps `simplify-globals-read_only_to_write.wast` and `simplify-globals-non-init.wast` against current local coverage; it found no obvious low-risk uncovered positive implementation slice in those files, only useful exact source-shaped guardrails for `$additional-set`, `$side-effects-in-body`, and imported-initializer/non-init-write conservatism. The follow-up guardrail slice in [`0578`](../../../raw/research/0578-2026-05-23-sgo-official-lit-guardrails.md) landed those three local regressions; the `$additional-set` test guards semantic removal of fake global traffic rather than Binaryen's raw dropped-constant text shape. The non-init same-init, changed-write, and imported-initializer lit regressions in [`0594`](../../../raw/research/0594-2026-05-23-sgo-non-init-same-init-lit-regression.md), [`0595`](../../../raw/research/0595-2026-05-23-sgo-non-init-changed-write-negative-lit.md), and [`0596`](../../../raw/research/0596-2026-05-23-sgo-non-init-imported-initializer-lit.md) now pin all three parser-supported `simplify-globals-non-init.wast` direct SGO modules without broadening expression equivalence or imported-provenance facts. The if-return negative guardrails in [`0597`](../../../raw/research/0597-2026-05-23-sgo-if-return-negative-lit-guardrails.md) pin the parser-supported `simplify-globals-read_only_to_write.wast` trailing-code, `else`, and non-`return` body negatives without broadening branch/return/control-transfer matching. The remaining-lit inventory in [`0579`](../../../raw/research/0579-2026-05-23-sgo-remaining-lit-inventory.md) ranked exact offsets source-alignment and prefer-earlier copy-chain regressions as the next low-risk traceability slices, with startup-only nested GC and narrow `ref.cast` refinalization as separate higher-risk follow-ups. The offsets regression in [`0580`](../../../raw/research/0580-2026-05-23-sgo-offsets-lit-regression.md), prefer-earlier regression in [`0581`](../../../raw/research/0581-2026-05-23-sgo-prefer-earlier-lit-regression.md), nested-GC initializer regression in [`0582`](../../../raw/research/0582-2026-05-23-sgo-nested-gc-lit-regression.md), single-use GC initializer regression in [`0584`](../../../raw/research/0584-2026-05-23-sgo-single-use-gc-lit-regression.md), nested single-use GC initializer regression in [`0585`](../../../raw/research/0585-2026-05-23-sgo-nested-single-use-gc-lit-regression.md), multiple single-use GC initializer regression in [`0586`](../../../raw/research/0586-2026-05-23-sgo-multiple-single-use-gc-lit-regression.md), multi-input single-use GC initializer regression in [`0587`](../../../raw/research/0587-2026-05-23-sgo-multi-input-single-use-gc-lit-regression.md), single-use copy-chain regression in [`0588`](../../../raw/research/0588-2026-05-23-sgo-single-use-chain-lit-regression.md), and single-use negative guardrails in [`0589`](../../../raw/research/0589-2026-05-23-sgo-single-use-negative-lit-guardrails.md) landed those parser-supported traceability slices without implementation changes; [`0583`](../../../raw/research/0583-2026-05-23-sgo-gc-refcast-probe.md) separately pinned the less-refined GC-alias negative and recorded the blocked `ref.cast` positive.

The function-effects call-negative guardrails in [`0591`](../../../raw/research/0591-2026-05-23-sgo-function-effects-call-negatives.md), wrong-global guardrail in [`0592`](../../../raw/research/0592-2026-05-23-sgo-function-effects-wrong-global-negative.md), and generated-effects boundary guardrail in [`0593`](../../../raw/research/0593-2026-05-23-sgo-function-effects-generated-effects-boundary.md) first pinned get-call, set-call, get+set-call, wrong-global read-only-to-write, and generated-effects-dependent unaffected-global conservatism from `simplify-globals_func-effects.wast`. The follow-up implementation in [`0598`](../../../raw/research/0598-2026-05-24-sgo-direct-function-effects-runtime-facts.md) adds a narrow direct-function global-set summary so ordinary direct calls clear only globals the callee may mutate during runtime trace propagation. The plain-block follow-up in [`0599`](../../../raw/research/0599-2026-05-24-sgo-block-carried-function-effects.md) carries those effect-filtered facts through block bodies whose only nested barriers are ordinary direct calls or similarly carryable plain blocks. Calls still do not count as syntactic read-only-to-write events, and imported/indirect/reference/control-transfer barriers remain conservative.

The else-local runtime slice in [`0600`](../../../raw/research/0600-2026-05-24-sgo-else-local-runtime-facts.md) adds a fresh-fact rewrite for facts created inside an `else` arm, while preserving the existing pre-`if` else-read and post-`if` join guardrails. The loop-local runtime slice in [`0601`](../../../raw/research/0601-2026-05-24-sgo-loop-local-runtime-facts.md) applies the same fresh-fact approach inside loop bodies without carrying pre-loop facts into the loop or loop facts out through the join/backedge. The `try_table` runtime slice in [`0602`](../../../raw/research/0602-2026-05-24-sgo-try-table-local-runtime-facts.md) rewrites facts available inside `try_table` bodies, but still clears facts after the exception-control join.

The `try_table` condition read-only-to-write slice in [`0603`](../../../raw/research/0603-2026-05-24-sgo-try-table-condition-read-only-to-write.md) treats no-catch `try_table (result i32)` wrappers around already-supported condition reads as transparent self guards, while catch-bearing `try_table` conditions remain conservative. The follow-up in [`0604`](../../../raw/research/0604-2026-05-24-sgo-try-table-eqz-compare-read-only-to-write.md) extends that no-catch transparent wrapper through adjacent `i32.eqz` and compare-with-const self guards. The next follow-up in [`0605`](../../../raw/research/0605-2026-05-24-sgo-try-table-if-return-read-only-to-write.md) extends the no-catch wrapper through exact `if return; set` direct, `i32.eqz`, compare, reverse-compare, and block-wrapped-set guards. The `ref.is_null` follow-up in [`0606`](../../../raw/research/0606-2026-05-24-sgo-try-table-ref-is-null-read-only-to-write.md) handles no-catch `try_table (result funcref)` wrappers feeding `ref.is_null` condition and exact `if return; set` guards, [`0616`](../../../raw/research/0616-2026-05-24-sgo-try-table-ref-is-null-compare.md) extends that direct `ref.is_null` condition family through adjacent compare-with-const and leading-constant reverse-compare guards, [`0617`](../../../raw/research/0617-2026-05-24-sgo-try-table-ref-is-null-compare-if-return.md) fills the normal compare-with-const exact `if return; set` tail, and [`0618`](../../../raw/research/0618-2026-05-24-sgo-try-table-ref-is-null-compare-pure.md) routes normal compare-with-const results through supported pure post-consumers such as `i32.eqz`, and [`0619`](../../../raw/research/0619-2026-05-24-sgo-try-table-ref-is-null-reverse-compare-pure.md) adds the leading-constant counterpart. The refactor-only shared-matcher slice in [`0620`](../../../raw/research/0620-2026-05-24-sgo-try-table-shared-ref-is-null-compare-matcher.md) preserves the 0616-0619 behavior while centralizing the no-catch `try_table`/`ref.is_null` prefix, normal/reverse compare matching, optional pure-post-consumer scan, and direct/if-return tail application. The pure-chain follow-up in [`0607`](../../../raw/research/0607-2026-05-24-sgo-try-table-pure-chain-read-only-to-write.md) routes no-catch `try_table` wrappers through the existing supported external pure-condition scanner and exact pure-chain `if return; set` tails; the refactor-only shared applier slice in [`0623`](../../../raw/research/0623-2026-05-24-sgo-shared-block-try-table-guard-appliers.md) preserves block and no-catch `try_table` external-pure behavior while centralizing the wrapper-body extractor plus direct/if-return tail appliers, and [`0624`](../../../raw/research/0624-2026-05-24-sgo-shared-condition-guard-dispatch.md) extends that structural sharing to the direct, `i32.eqz`, compare, reverse-compare, and exact `if return; set` dispatchers without broadening loop or caught-`try_table` behavior. The refactor-only dispatch split in [`0625`](../../../raw/research/0625-2026-05-24-sgo-read-only-to-write-dispatch-split.md) groups the top-level block/no-catch-`try_table` read-only-to-write dispatcher into shared external-pure, shared tail, and no-catch-`try_table`-specific guard groups while leaving block-only nested-if-arm-flow, loop, and caught-`try_table` boundaries unchanged. The refactor-only guard-applier unification in [`0626`](../../../raw/research/0626-2026-05-24-sgo-try-table-guard-applier-unification.md) routes no-catch `try_table` select and `ref.is_null` guard families through one direct/if-return body applier without changing matcher boundaries. The refactor-only select-position shared matcher in [`0627`](../../../raw/research/0627-2026-05-24-sgo-select-position-shared-matcher.md) unifies the three select operand placements and leading-constant reverse-compare positions for no-catch `try_table` and `try_table` + `ref.is_null` values without broadening the accepted family. The refactor-only immediate `ref.is_null` guard applier unification in [`0628`](../../../raw/research/0628-2026-05-24-sgo-ref-is-null-guard-applier-unification.md) routes the no-catch `try_table; ref.is_null` direct and exact `if return; set` family through the same shared direct/if-return body applier. The refactor-only optional guard-index applier in [`0629`](../../../raw/research/0629-2026-05-24-sgo-optional-try-table-guard-applier.md) centralizes `Some`/`None` dispatch for no-catch `try_table` immediate, compare, select, and reverse-compare guard families while preserving each matcher boundary. The refactor-only guard-dispatch inline slice in [`0630`](../../../raw/research/0630-2026-05-24-sgo-try-table-guard-dispatch-inline.md) removes redundant one-line family counter wrappers by passing each matcher result directly to that shared optional applier. The refactor-only if-return tail index slice in [`0631`](../../../raw/research/0631-2026-05-24-sgo-if-return-tail-idx-refactor.md) centralizes target-index handling for exact `if return; set` and block-wrapped-set tails while preserving exact tail placement and wrapper boundaries. The refactor-only block/loop guarded-read slice in [`0632`](../../../raw/research/0632-2026-05-24-sgo-block-loop-guarded-read-refactor.md) centralizes block/no-catch wrapper extraction and loop fallback for direct, `i32.eqz`, compare, and reverse-compare guards while preserving the broader direct self-guard scanner and narrower `i32.eqz`/compare pure-loop behavior. The select follow-up in [`0608`](../../../raw/research/0608-2026-05-24-sgo-try-table-select-read-only-to-write.md) adds no-catch `try_table` wrappers whose result feeds a pure-constant-sibling `select` guard, including the exact select `if return; set` tail; catch-bearing wrappers stay conservative. The pure-post-select follow-up in [`0609`](../../../raw/research/0609-2026-05-24-sgo-try-table-select-pure-read-only-to-write.md) routes that select result through the existing supported pure-condition scanner and exact pure-post-select `if return; set` tail; [`0622`](../../../raw/research/0622-2026-05-24-sgo-try-table-select-shared-matcher.md) preserves those select behaviors while consolidating immediate/pure-post-select matching and direct/if-return tail handling. The reverse-compare follow-up in [`0610`](../../../raw/research/0610-2026-05-24-sgo-try-table-select-reverse-compare-read-only-to-write.md) adds the probed leading-constant compare form for a select whose condition is the no-catch `try_table` result, and [`0615`](../../../raw/research/0615-2026-05-24-sgo-try-table-select-value-reverse-compare.md) broadens that leading-constant reverse-compare helper to first- and second-selected-value positions; [`0621`](../../../raw/research/0621-2026-05-24-sgo-try-table-select-reverse-compare-pure.md) routes those leading-constant compare results through supported pure post-consumers such as `i32.eqz`, including exact pure-post-compare `if return; set` tails. The `ref.is_null` select follow-up in [`0611`](../../../raw/research/0611-2026-05-24-sgo-try-table-ref-is-null-select-read-only-to-write.md) covers no-catch `try_table (result funcref)` through `ref.is_null` into a pure-constant-sibling select condition. The pure-post-select follow-up in [`0612`](../../../raw/research/0612-2026-05-24-sgo-try-table-ref-is-null-select-pure-read-only-to-write.md) routes that select result through the existing supported pure-condition scanner and exact pure-post-select `if return; set` tail. The reverse-compare follow-up in [`0613`](../../../raw/research/0613-2026-05-24-sgo-try-table-ref-is-null-select-reverse-compare.md) adds the leading-constant compare form for that `ref.is_null` select condition. The value-position follow-up in [`0614`](../../../raw/research/0614-2026-05-24-sgo-try-table-ref-is-null-select-value-positions.md) broadens the same no-catch `ref.is_null` select helper to first- and second-selected-value positions, including supported post-select pure and leading-constant reverse-compare tails; [`0621`](../../../raw/research/0621-2026-05-24-sgo-try-table-select-reverse-compare-pure.md) also covers supported pure post-consumers after those `ref.is_null` select leading-constant reverse-compare tails.

Remaining value-flow work should add one Binaryen lit shape at a time with guardrail negatives for broader nested effectful control, wrong target global, additional body effects, non-branch consumers, calls with operands, broader trapping/effectful operations, runtime trace cases beyond the official dominance-lit subset, same-init expression equivalence beyond the pinned direct-literal/ref/null/ref.func and alias guardrails, and nested-pattern variants beyond the source-backed no-else/direct/eqz subset. The exact 0577, non-init same-init/changed-write/imported-initializer, offsets, prefer-earlier, nested-GC, single-use GC including nested-use, multiple-independent, multi-input, copy-chain, and compact negative variants, and less-refined GC-alias source-alignment guardrails are now covered; the next behavior slice should start from a different focused Binaryen-positive family, not broad inference from those lit files. For GC breadth, resolve the narrow `ref.cast(ref.func-global)` parser/validation/refinalization blocker before attempting that positive, and keep object-identity `struct.new_default` duplication separate.

The post-0632 inventory refresh in [`0633`](../../../raw/research/0633-2026-05-25-sgo-next-slice-probe-inventory-refresh.md) ranked narrow transparent wrapper composition for `[SGO]003B`, no-catch `try_table` wrapper composition for `[SGO]003G`, and a research-only callee global-read/write summary design for `[SGO]003I` before any call-shaped read-only-to-write implementation. The `[SGO]003I` study in [`0634`](../../../raw/research/0634-2026-05-25-sgo-function-effect-read-summary-study.md) completed that dependency by recommending fixed-point read/write summaries before call-shaped positives. The `[SGO]003E` boundary study in [`0635`](../../../raw/research/0635-2026-05-25-sgo-call-effect-boundary-study.md) found Binaryen-positive direct, imported, and indirect call probes but deferred behavior to a narrower direct-call read/write-summary implementation. The behavior-bearing follow-up in [`0671`](../../../raw/research/0671-2026-05-25-sgo-direct-call-read-summary.md) adds that first fixed-point read/write summary slice for zero-param/one-result direct calls that neither read nor mutate the candidate global. The guardrail follow-up in [`0672`](../../../raw/research/0672-2026-05-25-sgo-direct-call-const-arg-guardrails.md) pins that independent direct calls with clean constant operands are already accepted and candidate-derived direct-call operands remain conservative. The closeout in [`0673`](../../../raw/research/0673-2026-05-25-sgo003-call-breadth-closeout.md) marks the previously enumerated call breadth queue evidence-gated: imported/indirect/generated-effects, callee-write positives, richer non-constant operands, and broader call placements need a new explicit child slice before implementation. [`0674`](../../../raw/research/0674-2026-05-25-sgo-full-parity-backlog-reactivation.md) reactivates full Binaryen SGO parity as a backlog/product goal, with `[SGO]003A` source/fact-table audit as the next active child; it does not reopen those closed call shapes without fresh evidence. The `[SGO]003B` guardrail slice in [`0636`](../../../raw/research/0636-2026-05-25-sgo-transparent-block-wrapper-guardrails.md) confirmed nested transparent `block (result i32)` wrappers around a candidate read are Binaryen-positive and already covered by Starshine, while a branchy wrapper remains conservative. The `[SGO]003G` guardrail slice in [`0637`](../../../raw/research/0637-2026-05-25-sgo-try-table-block-wrapper-guardrails.md) confirmed no-catch `try_table` plus transparent block-body composition is already covered and caught wrappers stay conservative. The `[SGO]003F` boundary study in [`0638`](../../../raw/research/0638-2026-05-25-sgo-loop-self-guard-boundary-study.md) found a Binaryen-positive independent-effect value-loop prefix, but kept Starshine conservative after a naive broad loop FlowScanner reuse regressed existing coverage; branchy loops and trapping candidate consumers remain guardrails. The `[SGO]003N` frontier note in [`0639`](../../../raw/research/0639-2026-05-25-sgo-nested-cleanup-wrapper-frontier.md) documents the current nested cleanup structural filter for touched value-producing `block`/`if` wrappers with nested `if` control as scheduler/HOT-cleanup safety, not a matcher gap; lifting it should move to a future `[SGO]004` verifier/scheduler slice. The `[SGO]003P` direct rebaseline in [`0640`](../../../raw/research/0640-2026-05-25-sgo003p-direct-rebaseline.md) refreshed the standard 10k evidence with `9975` compared matches, `0` mismatches, and `0` validation failures. The refactor-only `[SGO]003O` clean-pop helper slice in [`0641`](../../../raw/research/0641-2026-05-25-sgo-flow-pop-clean-refactor.md) centralized repeated FlowScanner pop/taint checks without changing accepted matcher behavior. The `[SGO]003C` arm-placement guardrail slice in [`0642`](../../../raw/research/0642-2026-05-25-sgo-nested-if-arm-placement-guardrails.md) pins first-arm and second-arm single-read positives plus the both-arm-read negative without implementation broadening. The follow-up refactor-only `[SGO]003O` call-result helper slice in [`0643`](../../../raw/research/0643-2026-05-25-sgo-flow-call-result-refactor.md) centralizes FlowScanner call-boundary stack handling without changing call-shaped matcher boundaries. The narrow `[SGO]003D` grow-independence guardrail slice in [`0644`](../../../raw/research/0644-2026-05-25-sgo-grow-independence-guardrails.md) pins independent `memory.grow` / `table.grow` prefixes as covered while candidate-derived grow operands remain conservative. The refactor-only `[SGO]003O` clean-pop continuation in [`0645`](../../../raw/research/0645-2026-05-25-sgo-flow-clean-pop-continuation-refactor.md) extends shared FlowScanner pop/taint helpers across arm-result and nested-if arm-flow scanners without behavior broadening. `[SGO]003` is active again after 0674 as a coordinated full-parity backlog, but behavior changes still require new explicit child slices; the first active child is `[SGO]003A` source/fact-table audit.
