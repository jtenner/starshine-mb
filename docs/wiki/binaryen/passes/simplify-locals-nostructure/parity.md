---
kind: concept
status: working
last_reviewed: 2026-07-11
sources:
  - ../../../raw/research/1399-2026-06-30-slns-v130-source-refresh-and-tee-gap.md
  - ../../../raw/binaryen/2026-05-04-simplify-locals-nostructure-current-main-recheck.md
  - ../../../../../src/passes/simplify_locals.mbt
  - ../../../../../src/passes/simplify_locals_nostructure_test.mbt
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./starshine-strategy.md
  - ./starshine-port-readiness-and-validation.md
---

# `simplify-locals-nostructure` parity checklist

This page is the active audit checklist for bringing Starshine's direct `simplify-locals-nostructure` behavior to Binaryen `version_130` parity or to narrow evidence-backed Starshine-win / non-goal decisions.

## Current baseline

- Local oracle: `wasm-opt version 130 (version_130)`.
- Source refresh: [`../../../raw/research/1399-2026-06-30-slns-v130-source-refresh-and-tee-gap.md`](../../../raw/research/1399-2026-06-30-slns-v130-source-refresh-and-tee-gap.md).
- Direct pass status: active hot pass under canonical spelling `simplify-locals-nostructure`; compatibility alias `simplify-locals-no-structure` exists separately.
- Important correction from the tee slices: canonical SLNS no longer skips every function merely because a live `local.tee` already exists. Binaryen is tee-enabled for this variant. Starshine allows a root dropped existing tee in a function with later structured control, the no-tee sibling cleans straight-line unrelated locals despite an existing dropped tee, and the 2026-07-01 protected-root slices no longer skip the whole function for embedded-control dropped tees when unrelated root local traffic can be cleaned. The protected path now also runs later root main cycles plus root-only dead cleanup for deferred multi-use root locals, and a follow-up protected child cleanup fixes the bounded generated `ssa-nomerge-smoke` embedded-tee residual family at 1000- and 10000-case random all-profiles scale. The no-local-writes raw fallback also fixes the former `case-009332-wasm-smith` no-normal result-block/control-debris residual, and the typed result-loop const/nop raw fallback fixes the former `case-003694-wasm-smith` residual. This is still not full parity closure because performance acceptance/exception remains open even though the latest required wasm-smith lane has no Starshine mismatches.
- Bounded post-fix smokes: `.tmp/pass-fuzz-slns-slice-genvalid-200` compared `200/200` with `200` normalized matches and zero failures; `.tmp/pass-fuzz-slns-tee-guard2-genvalid-500` compared `500/500` with `500` normalized matches and zero failures, both using `_build/native/release/build/cmd/cmd.exe`.
- Current fuzz evidence after the subphase-timer performance change: red-first `simplify-locals-nostructure keeps high-frequency subphase timers off the trace hot path` failed while SLNS emitted per-cycle `count-local-gets`, `scan-root-region`, main/delete, dead-cleanup, and dead-delete timer lines inside the outer pass timer, then passed after the high-frequency subphase timers were accumulated without per-cycle trace emission. The transform is unchanged; `detail:simplify-locals:main-cycle` remains visible for cycle-count invariants. Focused SLNS file passes `79/79`; `moon fmt`, `moon test src/passes` (`3736/3736`), and native `src/cmd` build pass with pre-existing warnings. Bounded regular `.tmp/pass-fuzz-slns-genvalid-1000-after-subphase-timer-accumulate` is normalized-green (`1000/1000`, zero failures), random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-1000-after-subphase-timer-accumulate` is normalized-green (`1000/1000`, zero failures), dedicated `.tmp/pass-fuzz-slns-genvalid-all-1000-after-subphase-timer-accumulate` remains `897/1000` normalized with the same `103` failure IDs as the previous annotation-only baseline, and classifier `.tmp/slns-genvalid-all-1000-after-subphase-timer-accumulate-result-type-classification.txt` validates all `412` residual artifacts as `103/103` annotation-only. The post-subphase required-size matrix was refreshed: regular `.tmp/pass-fuzz-slns-genvalid-100000-after-subphase-timer-accumulate` compared and normalized `100000/100000` with zero failures; explicit wasm-smith `.tmp/pass-fuzz-slns-wasm-smith-10000-after-subphase-timer-accumulate` compared/normalized `9956/10000` with zero mismatches and `44` Binaryen/oracle command failures (`rec-group-zero=39`, `bad-section-size=3`, `invalid-tag-index=1`, `table-index-out-of-range=1`); dedicated `.tmp/pass-fuzz-slns-genvalid-all-10000-after-subphase-timer-accumulate` compared `10000/10000`, normalized `9069`, left `931` raw mismatches, and classifier `.tmp/slns-genvalid-all-10000-after-subphase-timer-accumulate-result-type-classification.txt` validates all `3724` residual artifacts as `931/931` annotation-only; random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-after-subphase-timer-accumulate` compared and normalized `10000/10000` with zero failures. After the later deferred-set code change, this matrix became historical; the current refreshed matrix is recorded in the latest deferred-set paragraph below. A follow-up repeat probe of the ten originally `>2x` timing cases (`.tmp/slns-perf-probe-subphase-timer-accumulate-slow10-repeat-current`) found `78/100` repeat runs within `<=2x` and a repeated slow-case median ratio of `1.8269x`, but maxima still reached `2.6625x`. A broader repeat probe over all 100 largest inputs (`.tmp/slns-perf-probe-subphase-timer-accumulate-100-largest-repeat3-current`) found `242/300` repeat runs within `<=2x`, repeat-run median/mean/p75/max `1.7930x` / `1.7871x` / `1.9412x` / `2.7598x`, and per-case median ratio median/mean/p75/max `1.8043x` / `1.7879x` / `1.8837x` / `2.3251x`. Existing-trace attribution in `.tmp/slns-perf-probe-subphase-timer-accumulate-tail-attribution-current/summary.md` shows all `300/300` repeat traces have exactly four SLNS pass invocations and eight visible main-cycle events; all 100 sampled cases select `simplify-locals-nostructure-effect-order`; the `14/100` weak-tail cases differ from the stronger `86/100` primarily by about `42.5us` median Starshine pass-local time while Binaryen medians are effectively identical (`133.9us` in both groups). Structural attribution in `.tmp/slns-perf-probe-subphase-timer-accumulate-structural-attribution-current/summary.md` found no clean weak-vs-strong structural separator. Function-slot attribution in `.tmp/slns-perf-probe-subphase-timer-accumulate-function-slot-attribution-current/summary.md` then parsed the same traces and found the weak-tail cases are slower across all four SLNS function-pass slots rather than one isolated position; the largest descriptive correlations are slot-2 non-main/pass/main time (`r=0.464` / `0.450` / `0.396`). A temporary source-instrumented subphase-total probe then emitted accumulated `count-local-gets`, `scan-root-region`, delete-main, `dead-cleanup`, and delete-dead totals after each SLNS pass timer for 14 weak-tail and 14 previously strong cases, wrote `.tmp/slns-perf-probe-temp-subphase-totals-weak14-strong14/summary.md`, reverted the instrumentation, and rebuilt the native binary. The one-run sample did not robustly reproduce the previous ratio split (`1.6716x` weak median vs `1.6783x` strong median), which reinforces timing noise; absolute Starshine pass medians were still higher for the weak sample (`254us` vs `225us`), and the measured subphase delta was mostly `scan-root-region` (`112us` vs `101us`) rather than count/delete/dead-cleanup. This supports a bounded/noisy performance exception discussion or targeted scan-region attribution, not closeout: central tendency is within the floor, but `14/100` cases passed fewer than two of three repeats and one passed none.
- Latest deferred-set recording cleanup after the temporary subphase probe: red-first white-box `simplify-locals deferred follow-up records only scanned first-cycle sets` first failed to compile against the intended scan API, then passed after first-cycle multi-use `local.set` nodes were recorded during the actual region scan and `simplify_locals_has_deferred_multi_use_local_set` stopped rescanning every live HOT node. This avoids protected root follow-up cycles caused only by multi-use local traffic inside embedded-control tee child regions that the protected root-only path deliberately skipped. Validation: focused white-box `11/11`, focused SLNS `79/79`, `moon fmt`, `moon test src/passes` `3737/3737`, and native `src/cmd` build pass with pre-existing warnings. Bounded post-change fuzz normalized regular `1000/1000`, wasm-smith `997/1000` with `3` Binaryen/oracle command failures, dedicated `897/1000` with the same `103` annotation-only effect-order IDs, and random all-profiles `1000/1000`. The required-size four-lane matrix is now refreshed after this latest code change with `_build/native/release/build/cmd/cmd.exe`: regular `.tmp/pass-fuzz-slns-genvalid-100000-after-deferred-set-record` compared/normalized `100000/100000` with zero failures and selected `binaryen-oracle-portable=100000`; explicit wasm-smith `.tmp/pass-fuzz-slns-wasm-smith-10000-after-deferred-set-record` compared/normalized `9956/10000` with zero mismatches and `44` Binaryen/oracle command failures (`binaryen-rec-group-zero=39`, `binaryen-bad-section-size=3`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`); dedicated `.tmp/pass-fuzz-slns-genvalid-all-10000-after-deferred-set-record` compared `10000/10000`, normalized `9069`, left `931` raw mismatches with zero validation/generator/property/command failures, selected straight-line `4290`, tee-control `2885`, and effect-order `2825`, and classifier `.tmp/slns-genvalid-all-10000-after-deferred-set-record-result-type-classification.txt` validates all `3724` residual artifacts as `931/931` annotation-only with `0` non-annotation residuals; random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-after-deferred-set-record` compared/normalized `10000/10000` with zero failures and selected pass-fuzz-stress `2062`, coverage-forced-portable `2037`, ssa-nomerge-smoke `1973`, ssa-nomerge-parity `1970`, and binaryen-oracle-portable `1958`. This refresh removed the stale-fuzz blocker after the deferred-set code change. The later 2026-07-01 closeout refresh below reran the same required matrix after explicit user approval of the pass-local timing caveat.
- Focused local test baseline after the main-cycle fast paths: the no-change/no-deferred perf-invariant failed red-first before SLNS skipped a forced second scan without deferred multi-use local.set work. A follow-up changed/no-deferred invariant then failed red-first on a straight-line single-use sink (`2 != 1`) and now passes after the first-cycle loop only forces a follow-up scan when actual deferred multi-use local.set work remains. After the embedded-control root-only follow-up, focused SLNS now passes `72/72`; `moon fmt`, `moon test src/passes` `3729/3729`, and native `src/cmd` build pass with pre-existing warnings. Fresh one-case replays after the prior dead `catch_ref` refinalization-guard fix normalize `case-001895` and `case-002945`; `case-001994` and `case-005696` are annotation-only. Stale partial replay/classifier `.tmp/pass-fuzz-slns-replay-partial10000-after-dead-catchref-refctx-guard-full959` / `.tmp/slns-replay-partial10000-after-dead-catchref-refctx-guard-full959-result-type-classification.txt` replays `959/959`, normalizes `64`, leaves `895` raw mismatches, and classifies all `895/895` residuals annotation-only with `0` non-annotation residuals. Completed 1000 aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-skip-changed-no-deferred-cycle` compares `1000/1000`, normalizes `897`, leaves `103` raw mismatches with zero failures, and classifier `.tmp/slns-genvalid-all-1000-after-skip-changed-no-deferred-cycle-result-type-classification.txt` validates all `412` residual artifacts and finds `103/103` annotation-only. Fresh completed dedicated aggregate `.tmp/pass-fuzz-slns-genvalid-all-10000-after-dead-catchref-refctx-guard-fresh` compares `10000/10000`, normalizes `9069`, leaves `931` raw mismatches, has zero validation/generator/property/command failures, and classifier `.tmp/slns-genvalid-all-10000-after-dead-catchref-refctx-guard-fresh-result-type-classification.txt` validates all `3724` residual artifacts and finds `931/931` annotation-only with `0` non-annotation residuals. Bounded timing probe `.tmp/slns-perf-probe-dedicated-10` is whole-command Starshine-faster on all ten tiny sampled inputs, but pass-local Starshine misses `<= 2x Binaryen` on eight of ten cases (median ratio `3.30x`). The broader `.tmp/slns-perf-probe-dedicated-100-even` timing sample is whole-command Starshine-faster on `100/100` even-spaced dedicated inputs, but pass-local Starshine meets the ordinary floor on only `2/100` cases (median ratio `3.18x`, mean `3.13x`). The no-change-only fast path rerun `.tmp/slns-perf-probe-dedicated-100-even-after-skip-redundant-main-cycle` still missed the floor on `100/100` cases (median ratio `3.27x`). The changed/no-deferred fast path rerun `.tmp/slns-perf-probe-dedicated-100-even-after-skip-changed-no-deferred-cycle` improves median main-cycle/scan event counts from `6` to `3` and median pass ratio to `2.22x`, but still misses the pass-local floor on `100/100` cases. A less-tiny follow-up over the 100 largest completed dedicated inputs, `.tmp/slns-perf-probe-dedicated-100-largest-after-skip-changed-no-deferred-cycle`, completes `100/100`, is whole-command Starshine-faster on only `55/100`, and misses the pass-local floor on `100/100` with median pass ratio `3.94x`; trace counts show four pass invocations and twelve main/dead scan events per selected module, i.e. about three cycles per function. Performance remains an explicit blocker. This is pass-specific aggregate plus bounded performance evidence, not final four-lane signoff or performance acceptance.
- Random all-profiles GenValid seed `0x5555` is now green after the embedded-tee cleanup, superseding the earlier blocker evidence: the first required-size attempt `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-seed5555` timed out after `2400s` before `result.json`; partial `cases.jsonl` had `6168` records (`4897` matches, `1271` mismatches), all mismatches from `ssa-nomerge-smoke`. Pre-fix completed bounded probe `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-1000-seed5555` compared `1000/1000`, normalized `795`, left `205` raw mismatches, and had zero failures; all mismatches selected `ssa-nomerge-smoke`, and classifier `.tmp/slns-random-all-profiles-1000-seed5555-result-type-classification.txt` found `0/205` annotation-only. The full residual trace sweep `.tmp/slns-random-all-profiles-1000-seed5555-guard-trace-summary.md` showed every completed residual case (`205/205`) hit `skip-hot reason=control-local-tee-hazard-noop`. Minimal probe `.tmp/slns-embedded-tee-unrelated-local.wat` reduced the issue to an embedded-control dropped tee plus unrelated root local traffic. The first root-only protection fix validates and cleans that reduced probe, with trace `protect-hot reason=control-local-tee-hazard-root-only`; the follow-up protected-root-cycle fix also validates `.tmp/slns-root-multiuse-embedded-tee.after-root-cycle.wasm` and removes deferred multi-use root carriers while keeping the embedded-control path protected. A follow-up protected child cleanup fixes the two remaining representative instruction-shape gaps from post-root-cycle `case-000002`: dead pure stack-style `local.set` inside a dropped result `if`, and embedded `drop(local.tee)` lowered to `local.set`. Fresh replay `.tmp/pass-fuzz-slns-random-case000002-after-embedded-tee-dead-result-if` now normalizes `1/1` with zero failures. Refreshed bounded random all-profiles lane `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-1000-after-embedded-tee-dead-result-if` now compares `1000/1000`, normalizes `1000`, and has zero mismatches or validation/generator/property/command failures with selected profiles `coverage-forced-portable=215`, `ssa-nomerge-smoke=205`, `pass-fuzz-stress=205`, `ssa-nomerge-parity=193`, and `binaryen-oracle-portable=182`. This closes the previously bounded `205/1000` random all-profiles embedded-control tee residual set. The required-size refresh `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-after-embedded-tee-dead-result-if` now compares `10000/10000`, normalizes `10000`, and has zero mismatches or validation/generator/property/command failures with selected profiles `pass-fuzz-stress=2062`, `coverage-forced-portable=2037`, `ssa-nomerge-smoke=1973`, `ssa-nomerge-parity=1970`, and `binaryen-oracle-portable=1958`; Binaryen cache `6435/3565`. This completes the random all-profiles lane, but it is not final closeout because wasm-smith residuals and performance remain open.
- Dedicated GenValid aggregate profile `simplify-locals-nostructure-all` now exists with straight-line, tee/control, and effect/order leaves; see [`./fuzzing.md`](./fuzzing.md). Initial 100-case smoke `.tmp/pass-fuzz-slns-genvalid-all-smoke-100` compared `100/100`, normalized `71`, found `29` mismatches, and had zero validation/generator/command failures. Follow-up tracing classified all 29 residuals as effect-order cases where Starshine hit `skip-raw reason=root-local-set-stack-hazard-noop`; straight-line and tee-control 100-case leaf smokes are normalized-green, while an effect-order 30-case leaf smoke is `30/30` mismatches for the same raw stack/local-set call-hazard guard. A later 2026-06-30 slice narrowed the initial raw/HOT guard to call-valued local writes while keeping the broader lowered-cleanup guard for the json-as stack-root `local.tee` hazard; the aggregate smoke improved to `.tmp/pass-fuzz-slns-genvalid-all-smoke-100-after-narrow-stack-guard` with `91/100` normalized and zero failures, and the effect-order leaf improved to `.tmp/pass-fuzz-slns-effect-order-smoke-30-after-narrow-stack-guard` with `20/30` normalized and zero failures. The remaining effect-order residuals no longer trace to skip guards. A follow-up classifier found all 10 effect-order leaf residuals and all 9 aggregate residuals are type-annotation-only refinalization/output-shape drift: Binaryen and Starshine WAT become identical after erasing structured-control result annotations, and both optimized wasm outputs validate. The call-valued overwrite, fallthrough-block, EH-catch, and unreachable-dead-set smokes leave those 100-case counts unchanged. A completed 1000-case aggregate lane after the unreachable-dead-set slice compared `1000/1000`, normalized `874`, found `126` mismatches and zero failures; local classification found `120/126` are the same annotation-only class, while five have Binaryen-only unreachable debris after throwing `try_table` arms and one was a Binaryen tee-form rewrite of a pure local value into an imported-call argument after a value-producing block call argument. The value-block slice fixed that last tee-form gap with a red-first test; `.tmp/pass-fuzz-slns-genvalid-all-1000-after-value-block` now compares `1000/1000`, normalizes `875`, leaves `125` mismatches and zero failures, and classifies as `120/125` annotation-only plus the same five EH/unreachable-debris residuals. The follow-up classifier artifact `.tmp/slns-eh-unreachable-debris-classification.txt` reduces those five residuals to result-typed `try_table` bodies that cannot produce a normal value (`throw` / `unreachable` body exiting through `catch_ref` / `catch_all_ref`): Binaryen sometimes removes the `try_table` result and may emit explicit `unreachable` debris, while Starshine preserved the result-typed EH spelling. The dropped-block slice fixed a narrow dropped-block subcase with red-first coverage, and the dead-if-arm slice then added red-first coverage for a constant-dead `if` arm containing a no-normal `catch_ref` `try_table`. The dead-if-arm rewrite deliberately requires a preceding constant condition proving that arm unreachable and tag-specific `catch_ref` payloads; an intermediate broader arm rewrite also rewrote live arms and introduced a new residual (`case-000488`), so it was narrowed before keeping. The post-fix aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-dead-if-arm-catch-ref-eh` still compares `1000/1000`, normalizes `875`, and leaves the same `125` residual count, with zero failures. A follow-up classifier `.tmp/slns-genvalid-all-1000-after-dead-if-arm-catch-ref-eh-result-type-classification.txt` showed `122/125` annotation-only residuals; `case-000194` and `case-000997` moved into that class, leaving `case-000251`, `case-000692`, and `case-000770` as non-annotation EH result/unreachable-debris residuals. A broad live result-block rewrite trial was rejected because `.tmp/pass-fuzz-slns-genvalid-all-1000-after-live-block-eh` regressed to `874/1000` normalized with `126` mismatches by adding `case-000305`, where Binaryen keeps a live `catch_all_ref` block result-typed. Follow-up oracle probes under `.tmp/slns-eh-probes/` showed the live `case-000692`-style voiding is tied to Binaryen's function-level refinalization after a type-changing local-get replacement: `.tmp/slns-eh-probes/case692-live-callarg-reduced.opt.wat` voids the live no-normal `catch_all_ref` `try_table` only in a function that also simplifies a `ref.func` local set/get pair, while no-refinalization live call-argument probes keep the result annotation. Starshine now has paired coverage for that discriminator and a narrow original-function proxy (`ref.func; local.set N; local.get N`), so `.tmp/slns-genvalid-all-1000-after-refinalized-live-catchall2-result-type-classification.txt` finds `124/125` annotation-only residuals and leaves only `case-000251` non-annotation. A later explicit-block both-arm `catch_all_ref` probe added paired no-refinalization/refinalization-triggered tests and a narrow block-body canonicalizer, but `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-both-arm3` still compares `1000/1000`, normalizes `875`, and leaves `case-000251` as the only non-annotation residual. A follow-up generated-case slice recognizes the broader `ref.func; local.set; local.get; call_ref` refinalization proxy and retargets zero-depth `catch_all_ref` labels from the voided `if` label to the enclosing non-null `(ref exn)` block, so direct generated `case-000251` replay no longer skips with `catch_all_ref label must expect exnref` and the output validates. The aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-label-retarget2` remains `1000/1000` compared, `875` normalized, `125` mismatches, zero failures, so surrounding `call_ref` operand/type/local spelling remains open. A follow-up exact-if probe added white-box lowered-cleanup coverage for Binaryen's `(ref null (exact $ft))` refinalization of `ref.func` / `ref.null func` if arms, but valid rebuilt 1000-case aggregate runs `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-exact-if2`, `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-exact-if3`, and `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-callref-context` still compare `1000/1000`, normalize `875`, leave `125` mismatches, and have zero failures; current `case-000251` remains open with local cleanup/directization, exact-if, and EH result-voiding drift. A later red-first white-box lowered-cleanup test covers directizing a `ref.func; local.set; local.get; call_ref` wrapper, but the valid rebuilt aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-local-callref-directize3` is also unchanged at `1000/1000` compared, `875` normalized, `125` mismatches, zero validation/generator/property/command failures, selected profiles `423/276/301`, and Binaryen cache `1000/0`; generated `case-000251` still prints the local wrapper, indicating the remaining path is outside that lowered helper. A follow-up direct-lib pipeline regression then showed the generated path was blocked by `skip-invalid-lower reason=writeback-validate:type mismatch` after exact-if refinalization of a `funcref` `if` feeding `table.set`. The first pragmatic fix suppressed that exact result only in immediate `table.set` value context, so generated replay no longer skipped invalid lower and the local-call-ref/EH cleanup applied, but `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-table-set-exact-suppress` remained unchanged at `875/1000` normalized with `125` mismatches. A later red-first validation hardening slice proved the actual writeback issue was the `ref.null func` arm not being rewritten when the if result was narrowed to `(ref null (exact $ft))`; Starshine now rewrites that null arm to the exact nullable function type and removes the table-set suppression. Focused lowered-cleanup and direct-lib pipeline tests validate the exact-if/table-set form, generated `case-000251` no longer appears in the latest failure set, and `.tmp/pass-fuzz-slns-genvalid-all-1000-after-exact-ref-null-arm` improves to `1000/1000` compared, `893` normalized, `107` mismatches, zero validation/generator/property/command failures, selected profiles `423/276/301`, Binaryen cache `1000/0`. Fresh classifier `.tmp/slns-genvalid-all-1000-after-exact-ref-null-arm-result-type-classification.txt` finds all `107/107` residuals in the effect-order leaf become identical after erasing only structured-control result annotations, and `wasm-tools validate --features all` accepted all `428` Binaryen/Starshine raw and canonical wasm artifacts. This is agent-classified as narrow annotation-only/refinalization output drift, not final signoff or broad refinalization closure. A fresh post-exact-null-arm attempt at the required 10000-case aggregate timed out after `2400s` at `.tmp/pass-fuzz-slns-genvalid-all-10000-after-exact-ref-null-arm-fresh` before writing `result.json`; partial `cases.jsonl` contains `9707` completed records (`8748` matches, `959` mismatches) with selected profiles straight-line `4152`, tee-control `2826`, effect-order `2729`, and all `959` failure dirs are from effect-order. Classifier `.tmp/slns-genvalid-all-10000-after-exact-ref-null-arm-fresh-partial-result-type-classification.txt` finds `934/959` partial residuals are annotation-only after erasing structured-control result annotations, but `25` are non-annotation residuals: `24` show EH `try_table` result/unreachable-debris/refinalization spelling drift and one (`case-008144`) shows function type-index/call_ref type reference spelling plus exact-if drift. `wasm-tools validate --features all` accepted all `3836` residual raw/canonical Binaryen and Starshine wasm artifacts. Treat this as a scaled partial blocker, not signoff. A follow-up reduced `case-001740` to a live constant-then `catch_all_ref` arm under the same refinalization trigger and added red-first coverage; replay of all `959` stale partial mismatch inputs at `.tmp/pass-fuzz-slns-replay-partial10000-after-single-arm-catchall` plus classifier `.tmp/slns-replay-partial10000-after-single-arm-catchall-result-type-classification.txt` improves the partial set to `936/959` annotation-only and `23` non-annotation residuals. The replay validates all `3836` residual wasm artifacts and moves `case-001740` plus `case-008369` into the annotation-only class, but the replay is not signoff because it reuses a stale timed-out partial lane and still leaves `23` non-annotation residuals open. A later classification-only slice inspected representative `case-001895` and `case-001994`: both are generated call/call_ref operand contexts where Binaryen keeps a dead-else `catch_ref` no-normal `try_table` result-typed and Starshine emits void-plus-unreachable. Smaller Binaryen probes show no-refinalization dead-arm shapes stay result-typed and local-refinalization-triggered shapes can void without Starshine's unreachable debris; focused Starshine boundary tests pass but do not reduce the stale partial replay. The next classification slice replayed `case-008144` alone at `.tmp/pass-fuzz-slns-replay-case008144-current` with the current native binary and still got `1/1` mismatch, zero failures. That residual is a distinct type-index/type-refinement output-shape gap: Binaryen preserves source-order surviving function type indices and exact-narrows the table-set `funcref` if under a same-function `ref.func` local-call_ref trigger, while Starshine emits equivalent reordered type references and leaves the if result as `funcref`. Probes under `.tmp/slns-type-probes/case008144-*` show no-trigger and already-direct-call_ref shapes stay `funcref`; a green direct-lib Starshine boundary covers the imported-function trigger but does not reproduce the generated mismatch. The next `case-002342` slice replayed that EH residual fresh at `.tmp/pass-fuzz-slns-replay-case002342-current` and still got `1/1` mismatch with zero failures. It belongs to the EH/refinalization bucket but differs from the simple dead-else representatives: Binaryen localizes the no-normal `catch_all_ref` cleanup inside the `call_ref` operand block and narrows nearby `funcref` annotations, while Starshine uses a whole-function exnref wrapper plus broader `funcref` spelling; all fresh residual wasm artifacts validate, and raw Starshine output is larger (`677B` vs Binaryen `643B`), so it remains an open parity gap rather than accepted drift. A follow-up `case-003158` slice replayed another EH/call_ref operand residual fresh at `.tmp/pass-fuzz-slns-replay-case003158-current` and still got `1/1` mismatch with zero failures. The fresh artifacts validate; Binaryen canonical output is one byte larger (`727B` vs Starshine `726B`), but Starshine raw output is much larger (`766B` vs Binaryen `727B`), so this slice also does not establish a Starshine win. Reduced Binaryen probes under `.tmp/slns-eh-probes/case3158-callref-catchall-else-*` show the same refinalization-gated pattern: no-trigger keeps the result-typed `try_table`, while a same-function `ref.func; local.set; local.get; call_ref` trigger voids the `catch_all_ref` try_table inside the call_ref operand block; Starshine still keeps result-typed EH in a wrapper block. A later `case-002945` slice replayed a generated call-argument dead-then `catch_ref` residual fresh at `.tmp/pass-fuzz-slns-replay-case002945-current` and still got `1/1` mismatch with zero failures; all fresh artifacts validate. Binaryen keeps the no-normal dead-then `catch_ref` `try_table` result-typed in the generated call argument, while Starshine emits void-plus-`unreachable`; fresh sizes are not a Starshine win (`573B`/`574B` normalized Binaryen/Starshine, `573B`/`608B` raw Binaryen/Starshine). Reduced probes under `.tmp/slns-eh-probes/case2945-callarg-dead-then-catchref-*` show no-trigger and already-direct-call_ref reduced forms keep the result-typed try_table, while a same-function local-call_ref refinalization trigger makes Binaryen void the dead-arm try_table. The reduced no-trigger/direct-call_ref forms do not reproduce the full generated Starshine mismatch, so this remains an open generated-context EH result/refinalization parity gap in the `catch_ref` dead-arm bucket. A later `case-004065` slice replayed a generated both-arm `catch_all_ref` call_ref operand residual fresh at `.tmp/pass-fuzz-slns-replay-case004065-current` and still got `1/1` mismatch with zero failures; all fresh artifacts validate. Binaryen localizes and voids both no-normal `catch_all_ref` arms inside the operand block under a same-function local-call_ref refinalization trigger, while Starshine keeps/result-prints broader result-typed operand spelling in the full generated case; fresh raw size is larger for Starshine (`624B` Binaryen vs `633B` Starshine), despite a slightly smaller normalized Starshine artifact (`624B`/`618B`). Reduced probes under `.tmp/slns-eh-probes/case4065-callarg-botharms-catchall-*` show no-trigger and already-direct-call_ref forms keep result-typed try_tables, while a local-call_ref trigger makes Binaryen void the operand try_tables; current Starshine validates the reduced trigger form but still leaves explicit unreachable debris, and the reduced form does not fully reproduce the generated wider result-typed spelling. This remains an open generated-context both-arm `catch_all_ref` EH block-placement/refinalization parity gap. A later `case-004556` slice fixed a reduced prefix-plus-tail `catch_all_ref` block under the same local-call_ref refinalization discriminator with red-first public WAT coverage, but generated replay `.tmp/pass-fuzz-slns-replay-case004556-after-tail-catchall` still mismatches and the stale partial replay/classifier `.tmp/pass-fuzz-slns-replay-partial10000-after-tail-catchall` / `.tmp/slns-replay-partial10000-after-tail-catchall-result-type-classification.txt` remains `936/959` annotation-only with the same `23` non-annotation residuals after validating all `3836` artifacts. Completed 1000 aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-tail-catchall` is unchanged at `1000/1000` compared, `893` normalized, `107` mismatches, zero failures. A follow-up `case-004933` classification replayed a related generated function-tail `catch_all_ref` residual fresh at `.tmp/pass-fuzz-slns-replay-case004933-current`; it still mismatches `1/1` with zero failures, all four artifacts validate, and raw Starshine is larger (`699B` vs Binaryen `683B`) despite a smaller normalized artifact (`681B` vs `683B`). Reduced function-tail probes under `.tmp/slns-eh-probes/case4933-function-tail-catchall-*` show no-trigger and already-direct-call_ref shapes keep the result-typed tail try_table, while local-refinalization trigger shapes already match in reduced form; the reduced probes do not reproduce the generated mismatch. The `23` stale partial non-annotation residuals remain open.
- Previous stale partial replay after the dead-then `catch_all_ref` fix: `.tmp/pass-fuzz-slns-replay-partial10000-after-dead-then-catchall` replayed `959/959` timed-out partial mismatches with zero validation/generator/property/command failures and Binaryen cache `959/0`; the harness still reports `959` raw mismatches because result annotations are not normalized away. Classifier `.tmp/slns-replay-partial10000-after-dead-then-catchall-result-type-classification.txt` validates all `3836` residual artifacts and improves the stale set to `946/959` annotation-only with `13` non-annotation residuals: `case-001895`, `001994`, `002945`, `004065`, `004556`, `004933`, `005696`, `007606`, `007639`, `007797`, `007981`, `008144`, and `009315`. Compared to the previous `.tmp/slns-replay-partial10000-after-live-catchref-result-type-classification.txt` count of `942/959` annotation-only with `17` non-annotation residuals, `case-002342`, `case-003158`, `case-007321`, and `case-009494` moved into annotation-only. This is not a completed `10000` lane because it replays stale inputs from a timed-out run.
- Earlier one-case recheck for three then-remaining stale residuals: `.tmp/pass-fuzz-slns-replay-case002945-current`, `.tmp/pass-fuzz-slns-replay-case004065-current`, and `.tmp/pass-fuzz-slns-replay-case004556-current` still each compare `1/1` with `1` mismatch, zero validation/generator/property/command failures, and all twelve raw/canonical artifacts validating. Summary `.tmp/slns-recheck-cases-002945-004065-004556-current.txt` records unchanged open classifications: `case-002945` generated dead-then `catch_ref` call-argument over-voiding, `case-004065` generated both-arm `catch_all_ref` call_ref-operand block-placement/refinalization drift, and `case-004556` generated tail `catch_all_ref` block drift. All three have raw size against Starshine, so none is an accepted Starshine win. A quick public-WAT dead-then `catch_ref` no-refinalization boundary was green and removed rather than kept because it did not reproduce `case-002945`.
- Current one-case recheck for the representative dead-else `catch_ref` residuals: `.tmp/pass-fuzz-slns-replay-case001895-current` and `.tmp/pass-fuzz-slns-replay-case001994-current` still each compare `1/1` with `1` mismatch, zero validation/generator/property/command failures, and all eight raw/canonical artifacts validating. Summary `.tmp/slns-recheck-cases-001895-001994-current.txt` keeps both open: `case-001895` remains a generated imported-call argument where Binaryen keeps the no-normal `catch_ref` `try_table` result-typed while Starshine emits void-plus-`unreachable` (`512B/530B` raw Binaryen/Starshine), and `case-001994` remains the related `call_ref` operand context with nearby exact-if/call_ref annotation drift plus the same void-plus-`unreachable` over-canonicalization (`674B/704B` raw). This is current one-case evidence only and was later superseded for the stale residual count by the mixed-arm, reverse-mixed, and function-tail replay refreshes; it remains useful raw-size/classification evidence, not signoff.
- Current stale partial replay refresh after the function-tail `catch_all_ref` fix: the previous `case-007981` mixed-arm fix normalized generated `case-007981` and moved `case-004065` into annotation-only in `.tmp/slns-replay-partial10000-after-mixed-ref-operands-result-type-classification.txt` (`947/958` annotation-only, `11` non-annotation residuals). The next red-first public coverage for `case-009315`'s reverse `catch_ref`/`catch_all_ref` no-normal arms failed before implementation because Starshine kept the operand wrapper nullable/result-typed after already voiding the arms. Lowered SLNS now recognizes already-voided ref-payload arms for both-arm operand wrapping and unwraps a single nested non-null exn block into the enclosing operand block. Fresh generated replay `.tmp/pass-fuzz-slns-replay-case009315-after-reverse-mixed` still raw-mismatches `1/1` with zero failures, but the remaining diff is structured result annotation drift (`nullref`/`anyref`) and all four artifacts validate. The later function-tail `catch_all_ref` fix adds red-first public coverage for the function-tail sibling bucket and makes generated `case-004556` and `case-004933` normalize in one-case replays. The refreshed stale partial replay `.tmp/pass-fuzz-slns-replay-partial10000-after-function-tail-catchall` reuses the timed-out partial inputs and compares `959/959`, with `3` normalized matches, `956` raw mismatches, zero validation/generator/property/command failures, and Binaryen cache `959/0`. Classifier `.tmp/slns-replay-partial10000-after-function-tail-catchall-result-type-classification.txt` validates all `3824` remaining residual artifacts and improves stale partial evidence to `951/956` annotation-only with `5` non-annotation residuals: `case-001895`, `case-001994`, `case-002945`, `case-005696`, and `case-008144`. Compared to the reverse-mixed classifier, `case-004556` and `case-004933` normalized while `case-007606` and `case-007797` moved into annotation-only. This refreshed replay is still stale partial evidence, not completed `10000` signoff. Fresh one-case replays for all five remaining IDs in `.tmp/pass-fuzz-slns-replay-case{001895,001994,002945,005696,008144}-fresh-five-residuals` still each mismatch `1/1` with zero failures; summary `.tmp/slns-five-residuals-fresh-replay-summary.txt` validates all twenty raw/canonical artifacts and keeps the same four `catch_ref` dead-arm over-voiding plus one type-index/call_ref spelling classifications open. Raw size remains against Starshine for all five (`512/530`, `674/704`, `573/608`, `658/696`, and `479/501` raw Binaryen/Starshine bytes), so none is accepted as a Starshine win.
- Current `case-008144` one-case fix after the fresh five-residual refresh: red-first direct-lib coverage `simplify-locals-nostructure refinalizes table-set if across intervening imported call` failed on the generated shape where the table-set value is a `funcref` if with transparent nested funcref block arms, then an imported call, then a same-function `ref.func; local.set; local.get; call_ref` trigger. Lowered SLNS now unwraps those transparent block arms while exact-refinalizing the if and rewrites the `ref.null func` arm to `ref.null (ref null exact $ft)`, so fresh replay `.tmp/pass-fuzz-slns-replay-case008144-after-nested-ref-func-if` compares `1/1` with one normalized match, zero mismatches, zero failures, and Binaryen cache `1/0`. Focused `*intervening imported call*`, focused lowered-cleanup `*refinalizes ref.func null if result*`, focused SLNS `68/68`, `moon fmt`, and native `src/cmd` build pass with pre-existing warnings where applicable. The stale partial replay/classifier was later refreshed at `.tmp/pass-fuzz-slns-replay-partial10000-after-nested-ref-func-if`: it replayed `959/959` stale timed-out partial mismatch inputs, normalized `62`, left `897` raw mismatches, and had zero validation/generator/property/command failures with Binaryen cache `959/0`; classifier `.tmp/slns-replay-partial10000-after-nested-ref-func-if-result-type-classification.txt` validates all `3588` remaining residual artifacts and updates stale partial evidence to `893/897` annotation-only with `4` non-annotation residuals (`case-001895`, `case-001994`, `case-002945`, `case-005696`). This removes the distinct `case-008144` type-index/type-refinement residual from both current one-case and stale partial non-annotation sets, leaving four generated `catch_ref` dead-arm over-voiding residuals as current non-annotation gaps. A follow-up classification-only slice refreshed those four as current one-case replays under `.tmp/pass-fuzz-slns-replay-case{001895,001994,002945,005696}-fresh-dead-arm-catchref`: each still compares `1/1`, normalizes `0`, reports one mismatch, and has zero validation/generator/property/command failures with Binaryen cache `1/0`; validation accepted `input.wasm` plus raw/canonical Binaryen and Starshine outputs for all four. Summary `.tmp/slns-four-catchref-residuals-fresh-summary.md` keeps the common classification open: generated `catch_ref` dead-arm/no-normal over-voiding where Binaryen keeps the result-typed `catch_ref` `try_table` and Starshine emits void-plus-`unreachable`. Raw size remains against Starshine (`512/530`, `674/704`, `573/608`, `658/696` raw Binaryen/Starshine bytes), so none is accepted as a Starshine win. A red-first follow-up then reduced `case-001895` to `simplify-locals-nostructure keeps dead else catch_ref if only local cleanup happens`: the test failed while lowered SLNS over-voided a local-cleanup-only dead `catch_ref` arm, and now passes after dead-arm `catch_ref` canonicalization was gated on the existing refinalization discriminator. Fresh one-case replays after the fix show `case-001895` and `case-002945` normalize, while `case-001994` and `case-005696` now differ only by structured result annotations. Full stale partial replay `.tmp/pass-fuzz-slns-replay-partial10000-after-dead-catchref-refctx-guard-full959` compares `959/959`, normalizes `64`, leaves `895` raw mismatches, and has zero validation/generator/property/command failures with Binaryen cache `959/0`; classifier `.tmp/slns-replay-partial10000-after-dead-catchref-refctx-guard-full959-result-type-classification.txt` validates all `3580` residual artifacts and finds all `895/895` residuals annotation-only with `0` non-annotation residuals. Completed 1000 aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-dead-catchref-refctx-guard` compares `1000/1000`, normalizes `897`, leaves `103` raw mismatches, has zero failures, selected profiles `423/276/301`, Binaryen cache `1000/0`, and classifier `.tmp/slns-genvalid-all-1000-after-dead-catchref-refctx-guard-result-type-classification.txt` validates all `412` residual artifacts and finds `103/103` annotation-only. This removes the currently known non-annotation stale partial residuals. A follow-up completed the pass-specific dedicated 10000 aggregate `.tmp/pass-fuzz-slns-genvalid-all-10000-after-dead-catchref-refctx-guard-fresh`: compared `10000/10000`, normalized `9069`, left `931` raw mismatches, had zero failures, and classified all residuals annotation-only with validated artifacts. Final four-lane signoff and performance acceptance remain open.
- Earlier classification-only follow-up for two of the previously listed 17 residuals: `.tmp/pass-fuzz-slns-replay-case007321-current` and `.tmp/pass-fuzz-slns-replay-case007606-current` each still compare `1/1` with `1` mismatch and zero validation/generator/property/command failures, and all eight checked artifacts validate. `case-007321` is an open generated nested call-operand `catch_all_ref` dead-arm/no-normal gap where Binaryen voids the dead inner arm and leaves `unreachable` while Starshine keeps a result-typed broader wrapper; sizes are not a raw-size win (`531B`/`530B` normalized Binaryen/Starshine, `531B`/`545B` raw). `case-007606` joins the open generated function-tail `catch_all_ref` bucket with `case-004933`; Binaryen uses a non-null `(ref exn)` block plus void tail `try_table`/`unreachable`, while Starshine keeps nullable/result-typed tail spelling; sizes are not a raw-size win (`597B`/`595B` normalized, `597B`/`622B` raw). No code fix or residual-count reduction landed.
- Fresh classification-only follow-up for `case-007639` and `case-007797`: `.tmp/pass-fuzz-slns-replay-case007639-current` and `.tmp/pass-fuzz-slns-replay-case007797-current` each still compare `1/1` with `1` mismatch and zero validation/generator/property/command failures, and all eight checked artifacts validate. `case-007639` is an open generated imported-call operand/local-directization EH gap: Binaryen voids a mixed `catch_all_ref`/`catch_ref` no-normal `if` inside the operand and directizes nearby `ref.func` call_ref traffic, while Starshine keeps broader local traffic and result-typed EH spelling. A reduced public WAT probe for just the mixed-arm EH form was already green, so no red-first implementable fixture landed; the generated context remains unreduced. Sizes are not a raw-size win (`528B`/`534B` normalized Binaryen/Starshine, `528B`/`568B` raw). `case-007797` joins the generated function-tail `catch_all_ref` bucket with `case-004933`/`case-007606`; Binaryen wraps in non-null `(ref exn)`, voids the tail `try_table`, and appends `unreachable`, while Starshine keeps nullable/result-typed tail spelling plus table-set if annotation drift. It is not a raw-size win (`504B` Binaryen vs `518B` Starshine raw; normalized `504B`/`500B`). No code fix, aggregate rerun, stale replay rerun, completed `10000`, final signoff, or performance lane landed.
- Fresh classification-only follow-up for `case-007981`, `case-009315`, and `case-009494`: `.tmp/pass-fuzz-slns-replay-case007981-current`, `.tmp/pass-fuzz-slns-replay-case009315-current`, and `.tmp/pass-fuzz-slns-replay-case009494-current` each still compare `1/1` with `1` mismatch and zero validation/generator/property/command failures, and all twelve checked artifacts validate. `case-007981` is another mixed `catch_all_ref`/`catch_ref` generated call_ref-operand EH/refinalization gap: Binaryen voids both no-normal arms inside a non-null `(ref exn)` block, while Starshine keeps the operand block/result EH typed as nullable `exnref`; raw size is not a Starshine win (`712B` Binaryen vs `737B` Starshine; normalized `712B`/`708B`). A temporary public WAT reduction using `call_ref` syntax was parser-blocked and removed. `case-009315` broadens the mixed catch_ref/catch_all_ref call-operand bucket with surrounding local cleanup/directization and `nullref`/`anyref` annotation drift; sizes are not a Starshine win (`579B`/`585B` normalized, `579B`/`592B` raw). The initial `case-009494` replay combined annotation drift with a generated imported-call single-arm `catch_all_ref` block where Binaryen uses `nullexnref` and voids the no-normal arm while Starshine keeps result-typed `exnref`; raw size was not a Starshine win (`626B` Binaryen vs `657B` Starshine; normalized `626B`/`625B`). A follow-up red-first reduced public fixture now covers the dead-then `catch_all_ref` direction and fresh generated replay `.tmp/pass-fuzz-slns-replay-case009494-after-dead-then-label-fix` reduces that generated case to result-annotation drift (`exnref` vs `nullexnref`) with all four artifacts validating and canonical sizes equal (`626B`/`626B`), though raw Starshine remains larger (`658B` vs `626B`). Completed aggregate `.tmp/pass-fuzz-slns-genvalid-all-1000-after-dead-then-catchall` stays `1000/1000` compared, `893` normalized, `107` mismatches, zero failures, selected profiles `423/276/301`, Binaryen cache `1000/0`. Fresh stale partial replay/classifier after that fix reduces the stale non-annotation set to `13` by moving `case-002342`, `case-003158`, `case-007321`, and `case-009494` into annotation-only, but this is still stale partial evidence rather than a completed `10000` lane. No completed `10000`, final signoff, or performance lane landed.
- Final closeout refresh after explicit performance-caveat acceptance: the user explicitly accepted the remaining SLNS pass-local timing differences on 2026-07-01, so the prior draft exception is now an accepted narrow performance caveat rather than a blocker. Strict timing evidence remains what it was before acceptance: `.tmp/slns-perf-probe-subphase-timer-accumulate-100-largest-current` has whole-command Starshine faster on `99/100`, pass-local `90/100` within `<=2x`, and Starshine/Binaryen pass medians `0.2335ms` / `0.1348ms`; repeat3 `.tmp/slns-perf-probe-subphase-timer-accumulate-100-largest-repeat3-current` has `242/300` repeats within `<=2x`, repeat-run median/mean/p75/max `1.7930x` / `1.7871x` / `1.9412x` / `2.7598x`, and per-case median max `2.3251x`. This is not strict performance signoff; it is user-approved closeout with a documented caveat and reopening criteria. Final validation/closeout commands then passed: `moon info`, `moon fmt`, focused SLNS `79/79`, focused simplify-locals white-box `11/11`, `moon test src/passes` `3737/3737`, `moon test` `7122/7122`, and native `src/cmd` build (`no work to do`). The final required fuzz matrix used `_build/native/release/build/cmd/cmd.exe`, `--jobs auto`, default `.tmp/pass-fuzz-cache`, `--max-failures 2000`, and `--keep-going-after-command-failures`: regular GenValid `.tmp/pass-fuzz-slns-genvalid-100000-closeout-accepted-perf` compared/normalized `100000/100000` with zero failures and selected `binaryen-oracle-portable=100000`; explicit wasm-smith `.tmp/pass-fuzz-slns-wasm-smith-10000-closeout-accepted-perf` compared/normalized `9956/10000` with zero mismatches and `44` Binaryen/oracle command failures (`binaryen-rec-group-zero=39`, `binaryen-bad-section-size=3`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`); dedicated aggregate `.tmp/pass-fuzz-slns-genvalid-all-10000-closeout-accepted-perf` compared `10000/10000`, normalized `9069`, left `931` raw mismatches with zero validation/generator/property/command failures, selected straight-line `4290`, tee-control `2885`, and effect-order `2825`, and classifier `.tmp/slns-genvalid-all-10000-closeout-accepted-perf-result-type-classification.txt` validates all `3724` residual artifacts as `931/931` annotation-only with `0` non-annotation residuals; random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-closeout-accepted-perf` compared/normalized `10000/10000` with zero failures and selected pass-fuzz-stress `2062`, coverage-forced-portable `2037`, ssa-nomerge-smoke `1973`, ssa-nomerge-parity `1970`, and binaryen-oracle-portable `1958`. Agent classification: no known Starshine validation, generator, property, command, or true semantic mismatches remain; the only raw dedicated residual family is structured-control result-annotation output drift backed by classifier and validation evidence. Reopen for any Starshine validation/generator/property/semantic mismatch, new non-annotation residual, repeatable central-tendency pass-local regression above `2x`, or a concrete safe scan-region/tail optimization owner.

## Accepted soft performance caveat

The user explicitly accepted the remaining SLNS pass-local timing differences on 2026-07-01. This converts the former draft exception packet into an accepted narrow closeout caveat. It does **not** mean strict performance signoff was achieved; report the caveat and the timing evidence below when summarizing closeout.

Evidence supporting the accepted narrow caveat:

- Correctness/fuzz evidence is current through the closeout refresh after the deferred-set recording cleanup: regular GenValid `100000/100000` normalized, explicit wasm-smith `9956/10000` normalized with only `44` Binaryen/oracle command failures, dedicated aggregate `10000/10000` compared with `931/931` residuals classified annotation-only and all residual artifacts validating, and random all-profiles `10000/10000` normalized.
- The current performance issue is bounded to pass-local timing, not whole-command wall time: the 100-largest current probe has whole-command Starshine faster on `99/100` cases, but pass-local `90/100` within `<=2x` in a single run.
- Repeat timing shows noisy sub-millisecond tail behavior rather than stable catastrophic misses: the all-100 repeat3 probe has `242/300` runs within `<=2x`, repeat-run median/mean/p75 `1.7930x` / `1.7871x` / `1.9412x`, but max `2.7598x`; `86/100` cases pass at least two repeats, while `14/100` pass fewer than two and one case passes none.
- Tail, structural, and function-slot attribution show a fixed-shape effect-order sample: all `300/300` repeat traces have four SLNS pass invocations and eight visible main-cycle events; all 100 sampled inputs come from `simplify-locals-nostructure-effect-order`; structural counters overlap heavily; weak cases are slower across all four function-pass slots rather than one isolated position; and no safe input-family-specific or function-position-specific code change was identified.
- Prior code-level performance fixes were exhausted for obvious owners in this slice sequence: redundant follow-up scans were removed and high-frequency subphase timer trace emission was taken off the measured hot path, reducing the 100-largest median ratio from the post-const/nop `3.7990x` to `1.7590x`.

Reasons not to claim strict performance signoff despite the accepted caveat:

- The ordinary repo floor is per-pass `starshine_time <= 2 * binaryen_time`; repeat tails still exceed it, with max `2.7598x` and per-case median max `2.3251x`.
- The evidence is from a bounded 100-largest dedicated effect-order sample, not a general performance signoff over every supported input family.
- Current ordinary traces intentionally omit accumulated subphase detail events, and function-slot attribution still splits the tail between visible main-cycle and inferred non-main time. A temporary subphase-total probe points weak absolute time mostly at `scan-root-region`, but its one-run weak/strong ratio split was noisy and it was not kept as stable instrumentation, so a real code fix still needs targeted low-overhead counters or a red-first performance invariant before changing behavior.

Accepted exception wording: SLNS `version_130` closes with a narrow performance caveat because current correctness/fuzz evidence is green or annotation-only, whole-command timing is faster on the bounded 100-largest sample, pass-local central tendency is within the ordinary floor, and the remaining fixed-shape sub-millisecond tail lacks an identified safe code fix after redundant-cycle and trace-overhead optimizations. Reopening criteria: any Starshine validation/generator/property/semantic mismatch; a repeatable pass-local regression with central tendency above `2x`; a stable structural/subphase owner that suggests a safe optimization; or user reversal of the accepted caveat.

## Transform-family checklist

| Family | Binaryen `version_130` contract | Starshine audit status | Evidence / next action |
| --- | --- | --- | --- |
| Template identity | `SimplifyLocals<true, false, true>`: teeing and nesting enabled; structure-building disabled. | **Covered but keep active.** | Runner calls `simplify_locals_run_with_options(ctx, func, false, true)`. |
| Get counting | `LocalGetCounter` drives single-use, tee, and dead-set decisions. | **Partial.** | Existing sink/dead tests cover basics. Needs dedicated GenValid profile coverage. |
| First-cycle single-use sinks | First cycle refuses multi-use tee creation and Binaryen's implementation schedules a second cycle for deferred tee work. | **Covered for closeout, including focused straight-line/drop-first-use shapes and final generated stress.** Starshine now has internal performance fast paths that skip the second main/dead-cleanup scan whenever the first cycle has no deferred multi-use local.set work, including changed single-use sinks. This is not an output-shape divergence. | Binaryen probes under `.tmp/slns-tee-cycle-audit/` show a dropped first use avoids fresh tee creation and collapses to the final constant carrier; Starshine test `simplify-locals-nostructure avoids tees when the first use is dropped` matches. Perf-invariant tests `simplify-locals-nostructure skips redundant second main cycle without deferred writes` and `simplify-locals-nostructure skips changed first-cycle follow-up without deferred writes` protect the no-deferred fast paths. Include in scaled dedicated profile signoff. |
| Later-cycle teeing | Later cycles may convert a sunk multi-use set into `local.tee`. | **Covered for closeout for straight-line, branch-free value-block call-argument, and audited embedded-control root/protected-child shapes.** | Binaryen probe `.tmp/slns-tee-cycle-audit/multi-use-tee.opt.wat` creates `local.tee` for canonical SLNS, while `.tmp/slns-tee-cycle-audit/multi-use-tee-notee.opt.wat` keeps the local carrier. The scaled `case-000606` residual reduced to a pure multi-use const whose first use follows a branch-free value block used as an earlier call argument; Binaryen emits `local.tee`, and Starshine now matches via red-first `simplify-locals-nostructure carries pure tee candidate across value block call argument`. Continue embedded-control tee work only after the HOT child-ownership blocker is reduced. |
| Existing `local.tee` should not block unrelated optimization | Binaryen optimizes unrelated local traffic in functions containing existing tees. | **Covered by final signoff for the audited generated embedded-control residual family.** | Red-first canonical straight-line test, red-first canonical root-tee-with-later-control test, red-first no-tee straight-line test, red-first embedded-control/root-unrelated cleanup test, and red-first embedded-control/deferred-root cleanup test now pass. Binaryen probes include `.tmp/slns-existing-tee.opt.wat`, `.tmp/slns-structured-existing-tee.opt.wat`, `.tmp/slns-existing-tee-notee.opt.wat`, `.tmp/slns-embedded-tee-unrelated-local.wat`, and `.tmp/slns-root-multiuse-embedded-tee.wat`. Current validation after the protected child cleanup: focused embedded-control tests pass `4/4`; focused `*tee*` `23/23`; focused SLNS `75/75`; `moon fmt`; `moon test src/passes` `3732/3732`; native `src/cmd` build with pre-existing warnings. Direct reduced replay now reports `protect-hot reason=control-local-tee-hazard-root-only`, validates, and cleans root local traffic including deferred multi-use root carriers. The follow-up protected child cleanup makes representative `case-000002` normalize, the refreshed bounded random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-1000-after-embedded-tee-dead-result-if` is normalized-green (`1000/1000`, zero failures), and the completed random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-after-embedded-tee-dead-result-if` is also normalized-green (`10000/10000`, zero failures). Continue with the remaining wasm-smith `case-003694` and performance blockers rather than accepting old drift. |
| `drop(local.tee)` cleanup | `visitDrop` lowers dropped tee to set; final cleanup may remove the set. | **Improved / covered for the audited generated embedded-control residual.** | Existing-tee tests cover root/single-use dead dropped-tee cleanup for canonical straight-line, canonical with later control, no-tee straight-line shapes, and the minimal embedded-control/root-unrelated cleanup probe. The protected child cleanup now lowers the representative embedded `drop(local.tee)` to `local.set` without broad child-control deletion, and the random all-profiles 10000 refresh is normalized-green. Keep this active for new nested tee shapes until final matrix/performance closeout. |
| Overwritten pending-set cleanup | Later pending writes convert the previous pending set to `drop(oldValue)`, preserving old-value effects/traps while allowing the later value to feed the use. | **Covered for load/trap-shaped straight-line canonical/no-tee cases and adjacent call-valued overwrite/final-use canonical/no-tee cases; broader stack-effect local-write hazards remain guarded.** | Binaryen probes `.tmp/slns-overwrite-load-load.opt.wat` and `.tmp/slns-overwrite-load-load-notee.opt.wat` produce `drop(i32.load 0)`, `nop`, `i32.load 4`. Binaryen probes `.tmp/slns-call-overwrite-audit/overwrite-call-call.opt.wat` and `overwrite-call-call-notee.opt.wat` produce `drop(call $a)`, `nop`, `call $b`. Starshine regression tests `simplify-locals-nostructure drops overwritten pending load set`, `simplify-locals-notee-nostructure drops overwritten pending load set`, `simplify-locals-nostructure drops overwritten pending call set`, and `simplify-locals-notee-nostructure drops overwritten pending call set` pass. Reopen broader call-valued overwrite work for `call_ref` / `call_indirect`, non-adjacent call-valued carriers with intervening effects, or shapes that still hit the stack-effecting later-read guard. |
| Effect/order invalidation | Directional `effects.orderedAfter(info.effects)` invalidates unsafe motion. Distinct global reads/writes are not automatically ordered; same-global read/write is ordered. Read-only dynamic loads may commute across other read-only loads, but memory/table writes remain barriers. Current `version_130` probes keep `table.get $t0` before both same-table and distinct-table `table.set`; local direct calls remain barriers even for pure-looking callees; nullable-local `ref.as_non_null` can sink across a read-only dynamic load. | **Partial / improved for distinct globals, dynamic read-only loads, direct-call barriers, one null-trap positive, pure pre-call local traffic, and adjacent overwritten call writes; remaining profile residuals are now non-skip annotation drift.** | Binaryen probes under `.tmp/slns-effect-audit/`: `global-get-set.opt.wat` sinks `global.get $g` across `global.set $h`; `global-get-set-same.opt.wat` keeps the carrier across `global.set $g`; `load-load.opt.wat` commutes read-only dynamic loads; `load-store.opt.wat`, `store-value-load.opt.wat`, `table-get-set.opt.wat`, `table-get-set-same.opt.wat`, and `table-get-set-distinct.opt.wat` keep carriers across memory/table writes. Follow-up probes under `.tmp/slns-call-audit/` keep const/load carriers across direct/imported calls; `.tmp/slns-null-audit/ref-as-non-null-load-nullable-local.opt.wat` sinks `ref.as_non_null` across a dropped read-only load; `.tmp/slns-call-overwrite-audit/overwrite-call-call*.opt.wat` proves the adjacent overwritten call final-use positive. Starshine coverage now includes exact global read/write effects, red-first dynamic load movement with dependent-load and atomic barriers preserved, table same/distinct barriers, direct-call barrier coverage, nullable-local null-trap positive coverage, red-first guard-reduction coverage for pure local traffic before a later root call, and red-first adjacent call-overwrite cleanup. The raw/HOT `root-local-set-stack-hazard-noop` guard is now limited to stack-effect-valued local writes that have a later stack-effecting read before the next write; the broader lowered-cleanup guard remains for the historical stack-root local.tee artifact. Dedicated-profile residuals dropped from `29/29` effect-order mismatches to `10/30` on the leaf and `9/100` on the aggregate, and trace replays show no residual skip reason. A follow-up classifier shows those smoke residuals are exactly structured-control result annotation drift (`all_residuals_type_annotation_only=True`) with both Binaryen and Starshine outputs validating, so they are evidence-backed behavior-parity matches despite output/refinalization drift for this narrow family. Continue with shared memory/atomic source/probe audit, broader `call_ref` / `call_indirect` / tail-call precision, nondefaultable/refinalization fixups beyond annotation-only drift, and EH/catch helper-diff surfaces. |
| `try` / `try_table` throwing barriers | Pending sinkables whose values may throw are dropped before entering EH bodies; nonthrowing values may still sink into EH bodies. Catch regions are fresh sinking regions: pre-`try` sinkables do not move into catches, but local traffic produced and consumed wholly inside a catch can still optimize. | **Improved for `try_table` body, direct-HOT legacy `try` body, catch/catch-list fresh-region boundaries, live refinalization-triggered single-arm `catch_all_ref` and `catch_ref` result cases; EH result/payload breadth remains open.** | Binaryen probes `.tmp/slns-try-table-nonthrowing-sink.opt.wat` and `.tmp/slns-try-table-throwing-barrier.opt.wat` show pure values sinking into a `try_table` body while call-valued pending sets stay outside the handler. Plain legacy probes `.tmp/slns-legacy-try-audit/nonthrowing.opt.wat` and `throwing.opt.wat` show the same body barrier. Catch probes under `.tmp/slns-eh-catch-audit/` show Binaryen keeps pre-`try` const/call carriers out of catch bodies, while optimizing catch-local `i32.const 7; local.set 0; local.get 0` to `nop; i32.const 7`. Starshine mirrors `try_table` through public WAT tests and has direct-HOT `HotOp::Try` coverage because public WAT desugars legacy `try` before HOT. Red-first direct-HOT catch tests exposed and fixed the following-root inliner crossing into `Try`/`TryTable` child slot `1`; catch-list tests are Starshine HOT representation coverage because public Wasm `try_table` catch clauses are immediates. The five non-annotation residuals from the post-value-block 1000-case aggregate are reduced/classified as EH result canonicalization drift around result-typed `try_table` bodies that only `throw` / `unreachable`. Starshine now matches the narrow dropped-block subcase with red-first `simplify-locals-nostructure canonicalizes no-normal try_table result` coverage, one constant-dead `if`-arm `catch_ref` subcase with red-first `simplify-locals-nostructure canonicalizes no-normal if-arm try_table result` coverage, and the refinalization-triggered live `catch_all_ref` block subcase with a paired `case-000305` boundary test. It deliberately does not rewrite live arms or `catch_all_ref` arms after aggregate replay showed a broader arm rewrite introduced an extra residual (`case-000488`) without reducing the lane. The dedicated 1000-case aggregate stayed `875/1000` normalized with `125` mismatches through the early `case-000251` sub-fixes; post-dead-if classification moved `case-000194` and `case-000997` into the annotation-only class, and the refinalization-triggered live `catch_all_ref` fix moved `case-000692` and `case-000770` into the annotation-only class. The explicit-block both-arm `catch_all_ref` discriminator is now covered, but generated `case-000251` still remained non-annotation after `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-both-arm3`. A later lowered local-call-ref directizer test and helper also leave `.tmp/pass-fuzz-slns-genvalid-all-1000-after-case251-local-callref-directize3` unchanged at `875/1000` normalized with `125` mismatches and zero failures; generated `case-000251` still shows the local call_ref wrapper and EH result drift, so the unresolved path is likely raw/HOT or another canonicalization entry point. A broad live block rewrite was rejected because it added `case-000305`, proving Binaryen sometimes keeps live `catch_all_ref` blocks result-typed; the kept fixes use same-function refinalization proxies instead. The later exact-null-arm writeback fix removes generated `case-000251` from the latest failure set, and classifier `.tmp/slns-genvalid-all-1000-after-exact-ref-null-arm-result-type-classification.txt` finds no non-annotation residuals in that 1000-case lane. The stale partial-10000 replay still has EH/refinalization residuals; fresh one-case replays classified `case-002342`, `case-003158`, `case-004065`, `case-007321`, `case-007639`, `case-007981`, and `case-009315` as generated `call_ref`/call operand block-placement, mixed `catch_all_ref`/`catch_ref`, or dead-arm refinalization gaps where Binaryen localizes or voids EH cleanup inside the operand block while Starshine uses broader wrapper/result-typed spelling, keeps surrounding local traffic, or emits extra unreachable debris. After the dead-then `catch_all_ref` fix, the refreshed stale replay moved `case-002342`, `case-003158`, `case-007321`, and `case-009494` into annotation-only; after the mixed `catch_all_ref`/`catch_ref` fix, `case-007981` normalized and `case-004065` moved into annotation-only; after the reverse mixed-arm fix, `case-007639` and `case-009315` moved into annotation-only; after the function-tail `catch_all_ref` fix, `case-004556` and `case-004933` normalize while `case-007606` and `case-007797` classify as annotation-only, leaving `5` stale non-annotation residuals. The `case-008144` nested table-set exact-if fix then removed the distinct type-index/type-refinement/call_ref spelling case, and the refreshed stale replay/classifier now leaves `4` non-annotation residuals. Fresh one-case replays for `case-001895`, `case-001994`, `case-002945`, and `case-005696` still each mismatch `1/1` with zero failures and validated artifacts; the current remaining stale non-annotation set is exactly that generated `catch_ref` dead-arm bucket where Binaryen keeps the result-typed no-normal `catch_ref` `try_table` and Starshine emits void-plus-`unreachable`. Keep the broader EH/refinalization family open until a completed 10000 dedicated lane and final signoff confirm no new instruction-order, label, local-traffic, validation, or performance gaps. Reopen/implement generated `ref.func`/`call_ref` neighborhoods, call-argument/live if-arm block contexts, typed catch payloads, branch-bearing catch regions, and scaled EH-heavy fuzzing only with focused discriminators. |
| Unreachable/refinalization/nondefaultable fixups | Rewrites can request `ReFinalize`; pass runner fixups remain in scope. Binaryen `version_130` removes zero-read stack-style `local.set` after `unreachable` in both canonical and no-tee no-structure variants; it sinks initialized nondefaultable struct locals to the result while preserving a validating unused nondefaultable local declaration. | **Partial / improved for one bottom zero-read cleanup; direct nondefaultable locals are tooling-blocked.** | Red-first `simplify-locals-nostructure removes zero-read stack set after unreachable` failed before the no-structure raw cleanup and now passes; no-tee coverage also passes. The 2026-06-30 effect-order residual classifier proved the current 30-case leaf and 100-case aggregate residuals are only structured-control result type spelling differences and both outputs validate. Direct nondefaultable-local probe still fails in Starshine final validation with `locals_error: local type has no default value`, so it remains a validation/tooling boundary, not a behavior claim. A bottom set/get probe still has Binaryen tee-form vs Starshine stack-form unreachable-code drift. The post-dead-if classifier narrowed the five 1000-lane EH residuals to three non-annotation cases, and the refinalization-triggered live `catch_all_ref` fix narrows them further: `case-000194`, `case-000692`, `case-000770`, and `case-000997` are now annotation-only, while `case-000251` still shows `try_table` result/refinalization drift around bottom/throwing `catch_all_ref` bodies plus surrounding `ref.func`/`call_ref` local-traffic differences. A later exact-if white-box subcase covers Binaryen's refined `(ref null (exact $ft))` spelling for `ref.func` / `ref.null func` if arms, and a follow-up white-box lowered-cleanup subcase covers direct `ref.func; local.set; local.get; call_ref` target wrapper removal. Both rebuilt aggregate checks remain unchanged at `875/1000`; generated `case-000251` still has broader local cleanup/directization and EH result-voiding drift, including the local call_ref wrapper. The dropped-block, one constant-dead `if`-arm `catch_ref`, refinalization-triggered live `catch_all_ref`, explicit-block both-arm `catch_all_ref`, exact-if, lowered local-call-ref, and exact-null-arm subcases are covered/fixed so far. The latest 1000-case aggregate improves to `893/1000`, and the remaining `107` residuals classify as structured-control result-annotation-only under `.tmp/slns-genvalid-all-1000-after-exact-ref-null-arm-result-type-classification.txt`; the follow-up single-arm `catch_all_ref`, function-tail `catch_ref`, live single-arm `catch_ref`, dead-then `catch_all_ref`, mixed `catch_all_ref`/`catch_ref`, and reverse mixed-arm fixes keep the completed 1000-lane counts unchanged and improve replay classification of the stale partial 10000 residual set from `25` to `9` non-annotation cases. Nevertheless, recheck GC/refined refs, nondefaultable params/locals after tooling support, bottom-typed values in structured/EH result carriers, and any residual with instruction-order or local-traffic differences before claiming closure. |
| Late equivalent-get cleanup | No-structure canonicalizes equivalent gets but uses `removeEquivalentSets = allowStructure`, so it does not remove equivalent set carriers in that phase. | **Covered for direct copy/store/load get canonicalization and simple copy-chain cleanup; broader refinalization still partial.** | Binaryen probe `.tmp/slns-equivalent-audit/equiv-store-load.opt.wat` rewrites both copied-local uses back to the original param and final dead-set cleanup removes the copy set. New dead-copy probe `.tmp/slns-dead-set-audit/copy-dead-after-use.opt.wat` shows canonical SLNS rewrites the surviving copy use back to the source param and removes both copy carriers. Starshine tests `simplify-locals-nostructure canonicalizes late equivalent gets` and `simplify-locals-nostructure removes dead copy after equivalent-get cleanup` match. The no-tee sibling is intentionally stronger on the pure copy-chain fixture: Starshine also canonicalizes the remaining effect-free local copy back to the param without creating a tee or synthesizing structure, while Binaryen no-tee leaves that carrier. Treat this as a narrow Starshine-win cleanup; reopen for effects, traps, GC/nondefaultable/refined refs, embedded control, or any tee/structure synthesis. |
| Final dead-set cleanup | `UnneededSetRemover` removes zero-read sets after late cleanup while preserving side effects and traps as dropped values. It also removes bottom-valued zero-read stack sets after `unreachable`. | **Covered for focused pure, trapping, call-valued, overwritten, late-equivalent-created, and bottom/unreachable zero-read sets; scaled lanes still open.** | Binaryen probes under `.tmp/slns-dead-set-audit/` show pure zero-read sets become inert debris in both variants, trapping load-valued zero-read sets become `drop(i32.load ...)`, call-valued zero-read sets become `drop(call $side)`, and canonical copy-chain cleanup removes equivalent dead copy carriers. Probes under `.tmp/slns-nondefaultable-audit/` show both no-structure variants remove `unreachable; local.set 0` when Local 0 has zero reads. Starshine coverage now includes `simplify-locals-nostructure removes pure zero-read sets`, `simplify-locals-notee-nostructure removes pure zero-read sets`, both trapping zero-read variants, both call-valued zero-read variants, both unreachable stack-set variants, and both dead-copy tests. Include in scaled dedicated profile signoff and keep GC/nondefaultable/refinalization surfaces open. |
| Disabled structure synthesis | Block/if/loop return-building helpers are gated by `allowStructure` and disabled here. | **Covered for closeout across focused if, loop, one-armed-if, branch-created block, branch-free fallthrough block, branch-free value-block expression boundaries, and final fuzz evidence.** | Binaryen probes under `.tmp/slns-structure-audit/` show branch-free void blocks may still sink local traffic without synthesizing block results, while loops, one-armed ifs, and branch-created blocks keep local carriers. Starshine now has red-first `simplify-locals-nostructure sinks across fallthrough block without block result` plus red-first `simplify-locals-nostructure carries pure tee candidate across value block call argument`; the latter keeps pure pending locals live across a branch-free value block in an earlier call argument without synthesizing a block result. Reopen for branch-bearing block precision, multivalue/typed branch payloads, EH result carriers, or scaled residuals. |
| No-tee variant spelling | `simplify-locals-notee-nostructure` is a separate sibling with tee creation disabled. | **Partial, with straight-line multi-use boundary now refreshed.** | Red-first 2026-06-30 test proves the sibling no longer bails out on straight-line existing dropped tees and can still sink unrelated single-use locals without creating new tees. The first-cycle/later-cycle slice refreshed the Binaryen no-tee multi-use boundary: no fresh tee is created and the local carrier remains. Continue with structured-control embedded-tee boundaries and large-module artifact hazard reduction. |
| Dedicated GenValid profile | Final signoff needs a pass-owned profile that produces SLNS-relevant opportunities. | **Closed for `version_130` with current required-size closeout matrix.** | `simplify-locals-nostructure-all` samples straight-line, tee/control, and effect/order leaves and records selected-profile manifest metadata. Final dedicated closeout `.tmp/pass-fuzz-slns-genvalid-all-10000-closeout-accepted-perf` compared `10000/10000`, normalized `9069`, left `931` raw mismatches, and had zero validation/generator/property/command failures. Selected profiles were straight-line `4290`, tee-control `2885`, effect-order `2825`; all mismatches selected effect-order. Classifier `.tmp/slns-genvalid-all-10000-closeout-accepted-perf-result-type-classification.txt` validates all `3724` residual raw/canonical artifacts and finds `931/931` residuals annotation-only with `0` non-annotation residuals. Final regular GenValid `.tmp/pass-fuzz-slns-genvalid-100000-closeout-accepted-perf` is `100000/100000` normalized, final random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-closeout-accepted-perf` is `10000/10000` normalized, and final explicit wasm-smith `.tmp/pass-fuzz-slns-wasm-smith-10000-closeout-accepted-perf` is `9956/10000` compared/normalized with zero mismatches and `44` Binaryen/oracle command failures. |
| Performance target | Repo floor is `starshine_time <= 2 * binaryen_time`; stricter targets require explicit acceptance. | **Accepted caveat, not strict timing signoff.** | The user explicitly accepted the remaining pass-local timing differences on 2026-07-01. Keep reporting the caveat: latest subphase-timer accumulation probe `.tmp/slns-perf-probe-subphase-timer-accumulate-100-largest-current-summary.md` improves to Starshine/Binaryen pass medians `0.2335ms` / `0.1348ms`, median ratio `1.7590x`, and `90/100` cases within `<=2x`, with whole-command Starshine faster on `99/100`; repeat3 `.tmp/slns-perf-probe-subphase-timer-accumulate-100-largest-repeat3-current` has `242/300` repeats within `<=2x`, repeat-run median/mean/p75/max `1.7930x` / `1.7871x` / `1.9412x` / `2.7598x`, and per-case median max `2.3251x`. Tail/structural/function-slot attribution found no safe targeted fix. Reopen if central-tendency pass-local timing regresses above `2x`, if a stable scan-region/subphase owner appears, or if the user reverses the caveat. |

## Current final status

This `version_130` audit is **closed with an accepted performance caveat**. Final validation passed: `moon info`, `moon fmt`, focused SLNS `79/79`, focused simplify-locals white-box `11/11`, `moon test src/passes` `3737/3737`, `moon test` `7122/7122`, and native `src/cmd` build (`no work to do`). Final required-size fuzz is current after the latest deferred-set recording code change and after the explicit performance-caveat acceptance: regular GenValid `.tmp/pass-fuzz-slns-genvalid-100000-closeout-accepted-perf` (`100000/100000` compared and normalized, zero failures), explicit wasm-smith `.tmp/pass-fuzz-slns-wasm-smith-10000-closeout-accepted-perf` (`9956/10000` compared and normalized, zero mismatches, `44` Binaryen/oracle command failures), dedicated aggregate `.tmp/pass-fuzz-slns-genvalid-all-10000-closeout-accepted-perf` (`10000/10000` compared, `9069` normalized, `931` raw mismatches), and random all-profiles `.tmp/pass-fuzz-slns-genvalid-random-all-profiles-10000-closeout-accepted-perf` (`10000/10000` compared and normalized, zero failures). The dedicated classifier `.tmp/slns-genvalid-all-10000-closeout-accepted-perf-result-type-classification.txt` validates all `3724` residual artifacts and finds `931/931` annotation-only with `0` non-annotation residuals. Agent classification: no known Starshine validation, generator, property, command, or true semantic mismatches remain; the remaining dedicated raw residuals are structured-control result-annotation output drift, not harness-proven semantic safety. Performance is closed only by explicit user acceptance of the documented caveat, not by strict timing signoff.
