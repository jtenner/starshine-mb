---
kind: concept
status: supported
last_reviewed: 2026-07-24
sources:
  - ./index.md
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/passes/dae_optimizing_test.mbt
  - ../../../../../src/passes/registry_test.mbt
  - ../../../../../src/cmd/fuzz_harness_wbtest.mbt
  - ../../../ir2/registry-map.md
  - ../../no-dwarf-default-optimize-path.md
  - ../dae-optimizing/starshine-strategy.md
  - ../../../../../agent-todo.md
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ./starshine-port-readiness-and-validation.md
  - ../dae-optimizing/starshine-strategy.md
  - ../dae2/index.md
  - ../signature-pruning/index.md
---

# Starshine `dead-argument-elimination` strategy and status

For first-slice implementation order and validation details, use [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md). This page is the current status/code-map summary; the readiness page is the historical future-port checklist plus the current DAE-A through DAE-O implementation-family matrix.

## Current status

Starshine has a **behavior- and lifecycle-complete module-pass surface** for the represented Binaryen-v131 plain `dead-argument-elimination` / upstream `dae` boundary family. The authoritative current evidence is in [`completion-matrix.md`](./completion-matrix.md); unsupported proposal surfaces continue to fail closed. Release completion is blocked only by the dense-call artifact-scale performance item `[O4Z-DAE-PERF]001`.

The important local naming split is now:

- upstream Binaryen exposes the short public pass name `dae`;
- Starshine registers both `dead-argument-elimination` and `dae` as module-pass entries that route through [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt) with the optimizing suffix disabled;
- Starshine also registers the optimizing sibling as both `dae-optimizing` and `dead-argument-elimination-optimizing`.

Because [`run_hot_pipeline_expand_passes(...)`](../../../../../src/passes/optimize.mbt) does exact lookup before dispatch, current intended behavior is:

- `dead-argument-elimination` is known and accepted as a module pass;
- `dae` is a local alias for the same plain core;
- plain DAE is still not expanded by the `optimize` or `shrink` presets.

### Historical pre-closure checkpoints

Before the 2026-07-21 closure, the direct-pass wiring and focused tests had Moon validation, but direct plain DAE was not signed off: the dedicated profile remains raw-red at 10000 and full random-all remains raw-red on known cleanup mismatch families. A 2026-07-06 red-first simple function-signature reuse fix removes the convergence raw-positive size caveat in saved-mismatch replays, but all replayed direct dedicated/random-all cases still mismatch raw. Direct regular GenValid is green at closeout scale (`.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705`: `100000/100000` normalized, zero failures), and the previous direct wasm-smith `case-009332` size-losing unreachable-control-debris gap is fixed (`.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705`: `9956/9956` normalized, zero mismatches, `44` Binaryen/oracle command failures). Refreshed direct evidence remains raw-red after generic signature-finalization consolidation: `.tmp/pass-fuzz-dae-direct-closeout-10000-after-generic-signature-finalization-final-20260710` has `5000/10000` normalized with `5000` mismatches, and `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-generic-signature-finalization-final-20260710` has `9207/10000` normalized with `793` mismatches; both compared all `10000`, had zero failures, used Binaryen cache `10000/0`, and set `reduceMismatches=false`. The paired normalized optimizing random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-10000-after-generic-signature-finalization-final-20260710` remains `9538/10000` normalized with `462` mismatches and the same zero-failure/cache facts. The superseded 2026-07-05 random-all lane remains preserved at `9004` normalized / `996` mismatches. A 2026-07-06 stratified runtime replay over one saved mismatch from each residual class strengthened the cleanup classification for sampled cases but did not make the raw-red lane green. A later direct DAE+RUME raw-positive coverage scan under `.tmp/dae-raw-positive-analysis-20260706` found all `59` raw-positive coverage cases are canonical/WAT-negative and have no scanned side-effecting keywords before the first terminal `br_table` in input/Binaryen/Starshine raw WAT. A follow-up source review ties that terminal to `gen_valid_append_core_control_ops()` in the coverage prelude before later effectful templates, and a bounded `wasm-tools shrink` attempt on `case-007094` accepted the raw-positive/canonical-negative original but found no smaller module in 200 attempts; this supports but does not close the pure-prefix/unreachable-tail cleanup classification. A 2026-07-06 red-first argument-refinement follow-up now covers the two-caller direct-call GC LUB subset where distinct leaf allocation operands share a non-final supertype and Binaryen narrows the broad parameter to that common non-null type. Later typeidx block/if/loop operand follow-ups cover the zero-param `(block (type $carrier))`, `(if (type $carrier) ...)`, and `(loop (type $carrier) ...)` actual subsets where Binaryen narrows the broad parameter from the carrier type's non-null leaf result. Typed-select operand follow-ups cover the bounded `select (result (ref $leaf))` actual subset where Binaryen rewrites the select result type while narrowing the helper parameter to exact leaf, plus the direct-call-result operand subset where two typed helper calls feed `select (result (ref null $base))` and Binaryen narrows to their common non-null supertype. A later returned-value LUB follow-up covers a focused terminal-`if` helper body where distinct returned leaf allocations share a non-final supertype; Binaryen narrows the broad helper result and terminal `if` block type to non-null `(ref $mid)`, and Starshine now matches that focused shape. A subsequent explicit-return LUB follow-up covers a terminal void `if` whose arms each return distinct leaf allocations and leave trailing `unreachable`; Binaryen narrows the broad helper result to non-null `(ref $mid)`, and Starshine now matches that focused shape too. Later focused probes cover loop-carried and typed-control explicit-return result evidence, zero-param typeidx block bodies, zero-param typeidx terminal `if` carriers, static `local.get` returned-value evidence, plus flat, droppable-prefix, simple block-carrier/simple loop-carrier multi-result helpers and branch-free zero-param block-carrier multi-result `if` arms, explicit-return block-carrier multi-result `if` arms, and bounded typed-select operands in flat multi-result suffixes where result operands include a `struct.new_default` leaf alongside scalar results; block-condition, pure-condition, and call-condition typed-select probes now cover branch-free/effect-free zero-input blocks, branch-free/effect-free multi-instruction stack-local slices, and branch-free direct calls with scalar operands as select conditions. The typeidx and typed-select argument-operand slices are bounded to zero-param controls or simple typed selects with stack-local conditions/direct-call result operands; a new returned-value slice also covers non-tail-lane parametric typeidx `block`/`if`/`loop` controls with drop-only consumed params and flat GC-plus-scalar result bodies. Richer parametric typeidx boundaries with branchful/non-drop operand reconstruction and broader select reconstruction remain open. Starshine now matches those source-backed subsets. Broader arbitrary returned-value/control/candidate unification remains open, including richer typeidx block/if boundaries and non-flat multi-result carriers beyond the simple block-carrier, simple loop-carrier, simple `if`-arm, typed-select operand, parametric drop-only terminal typeidx control, block-carrier `if`-arm, and explicit-return block-carrier `if`-arm subsets. The optimizing sibling's broader no-DWARF post-pass suffix context (`dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize`) is green through 10000 on the `dae-optimizing` profile, but that is scheduling evidence for the optimizing profile, not direct plain DAE signoff.

## Exact local code map

## Registry and request behavior

- [`src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
  - `pass_registry_entries()` registers `dead-argument-elimination` and `dae` as module passes using `dae_summary()`.
  - `pass_registry_entries()` registers `dae-optimizing` and `dead-argument-elimination-optimizing` as module passes using `dae_optimizing_summary()`.
  - `pass_registry_boundary_only_names()` no longer owns the plain `dead-argument-elimination` spelling.
  - `optimize_preset_passes(...)` and `shrink_preset_passes(...)` continue to list `dae-optimizing`, not plain DAE.

- [`src/passes/pass_manager.mbt`](../../../../../src/passes/pass_manager.mbt)
  - `run_hot_pipeline_apply_module_pass(...)` dispatches `dead-argument-elimination` and `dae` to `dae_run_module_pass(mod_)`.
  - `dae-optimizing` and `dead-argument-elimination-optimizing` still route through the optimizing wrapper.

- [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt)
  - Focused tests now assert that plain `dead-argument-elimination`, `dae`, `dae-optimizing`, and `dead-argument-elimination-optimizing` are active module-pass names.
  - Adjacent public-pipeline tests exercise `dead-argument-elimination` and `dae` on a private unused-scalar-parameter fixture.
  - These tests are green in the 2026-07-06 focused file run (`284/284` after the three-parameter call_ref/call_indirect select returned-value follow-up; 286/286 after the four-parameter follow-up; 288/288 after the five-parameter follow-up; 290/290 after the six-parameter follow-up; 292/292 after the seven-parameter follow-up; 294/294 after the eight-parameter follow-up; 296/296 after the nine-parameter follow-up; 298/298 after the ten-parameter stack-slice follow-up); the typed-select focused select run passed (`68/68` after the three-parameter returned-value coverage), focused `*typed-call select returned-value*` passes (`1/1`) after failing red for direct-call operands in returned-value typed selects, focused `*call_ref select returned-value*` passes (`11/11`) after failing red for zero-arg, single-parameter, two-parameter, three-parameter, four-parameter, five-parameter, six-parameter, seven-parameter, eight-parameter, nine-parameter, and ten-parameter `ref.func; call_ref` operands in returned-value typed selects, focused `*parameterized call_ref select returned-value*` passes (`1/1`) after failing red for the one-argument direct-target call_ref slice, focused `*two-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the two-argument direct-target call_ref slice, focused `*three-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the three-argument direct-target call_ref slice, focused `*four-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the four-argument direct-target call_ref slice, focused `*five-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the five-argument direct-target call_ref slice, focused `*six-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the six-argument direct-target call_ref slice, focused `*seven-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the seven-argument direct-target call_ref slice, focused `*eight-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the eight-argument direct-target call_ref slice, focused `*nine-parameter call_ref select returned-value*` passes (`1/1`) after failing red for the nine-argument direct-target call_ref slice, focused `*call_indirect select returned-value*` passes (`10/10`) after failing red for the one-, two-, three-, four-, five-, six-, seven-, eight-, nine-, and ten-argument indirect-call slices, focused `*call-postop select GC results*` passes (`1/1`) for the branch-free call-plus-scalar-post-op condition coverage slice, focused `*memory-condition select GC results*` passes (`1/1`) for the branch-free memory-load condition coverage slice, focused `*branchful select-condition*` passes (`1/1`) after failing red for the bounded non-escaping `if` returned-value condition slice, focused `*branchful typed-select*` passes (`1/1`) for the matching argument-refinement coverage slice, focused `*typed-call select*` passes (`1/1`) after failing red for the typed-call select-operand argument-refinement slice, focused `*non-tail-lane multi-result select*` passes (`1/1`) for the select-first/scalar-second multi-result coverage slice, focused `*non-tail-lane parametric typeidx*` passes (`3/3`) for the drop-only consumed-param block/if/loop returned-value slice, focused `*non-drop parametric typeidx*` passes (`3/3`) for the stack-safe nontrapping pure-prefix parametric typeidx returned-value slice, focused `*effectful parametric typeidx*` passes (`3/3`) for the ordinary direct-call consumed-param block/if/loop returned-value slice, focused `*trapping-load parametric typeidx*` passes (`3/3`) for the potentially trapping numeric-load consumed-param block/if/loop returned-value slice, focused `*typeidx*` passes (`25/25`), focused `*select*` passes (`82/82`), full `dae_optimizing_test.mbt` passes (`298/298`), focused `*parameters*` now passes (`18/18`), `moon test src/passes` passes (`4331/4331`), full `moon test` passes (`7770/7770`), and native CLI build passes with pre-existing warnings, and those direct DAE compare lanes were still raw-red/open at that historical checkpoint. The final 2026-07-21 matrix and `253/253`, `347/347`, `6217/6217`, `9696/9696` totals supersede these intermediate counts.

## Planning and backlog context

- [research note 0063](../../../ir2/registry-map.md)
  - Lists `dead-argument-elimination` and `dead-argument-elimination-optimizing` in the boundary-only name family.
  - This is the older registry-map proof that both descriptive local names are intentionally known.

- [`docs/wiki/binaryen/no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
  - Names only `dae-optimizing` in the canonical no-DWARF post-pass phase.
  - This means plain DAE is a registry/API coverage pass, not the current parity-blocking default-path slot.

- [`agent-todo.md`](../../../../../agent-todo.md)
  - Keeps the active `DAE` backlog family under `[O4Z-AUDIT-DAE]`.
  - The current audit tracks plain DAE profile coverage, direct-pass wiring, direct compare counters, and the optimizing sibling's cleanup evidence together while keeping their behaviors distinct.

## Strategy implication for active Starshine work

Further Starshine implementation should still avoid two extremes:

- do not port plain DAE as an isolated local peephole that only deletes trivially unused parameters;
- do not accidentally give plain DAE the optimizing sibling's nested cleanup behavior.

The practical design should be a shared module-boundary core plus a scheduling flag, mirroring Binaryen's source split:

1. **Call-boundary ownership analysis**
   - collect direct calls by callee;
   - record dropped direct calls;
   - record tail-call relationships;
   - treat exports, imports, `ref.func`, and indirect/call-ref unknowns as signature-rewrite boundaries.

2. **Boundary rewrite core**
   - remove parameters whose incoming values are never used;
   - materialize all-equal constant actuals inside the callee before deleting the param;
   - refine live GC reference params from direct-call operand least-upper-bound evidence;
   - refine result types from returned-value evidence;
   - remove dropped results only when all known direct calls drop them and tail-call constraints allow it.

3. **Callsite and body repair**
   - rewrite every affected direct call when a signature changes;
   - preserve evaluation order for removed operands;
   - localize hard operands into temporaries before retrying if Binaryen would need localization;
   - preserve unreachable / uninhabited-result behavior when call results disappear.

4. **Plain-vs-optimizing split**
   - keep plain `dead-argument-elimination` / `dae` as the shared core with no nested cleanup rerun;
   - keep optimizing DAE as the same core plus the nested `optimizeAfterInlining(...)`-style cleanup scheduler documented in [`../dae-optimizing/starshine-strategy.md`](../dae-optimizing/starshine-strategy.md).

5. **Name compatibility decision**
   - current intent is to support both descriptive and upstream spellings for plain and optimizing DAE;
   - keep registry tests and pass catalogs aligned with that decision.

## Current closure, 2026-07-24

Plain DAE represented behavior and lifecycle ownership are closed through `676b90f68c2993899c1f5432a2d4253dead50774`. Native SHA-256 `e6d703492f7008bd3265569e8620cfb6514cd1604e533e4bf497ed03bf8c066e` passes DAE whitebox `395/395`, public DAEO `342/342`, pass-manager `306/306`, full Moon `9908/9908`, and the full CI-profile wasm-gc gate.

The final explicit-v131 plain matrix is regular `100000/100000` exact; dedicated `5000` normalized plus `5000` smaller residuals totaling `-18,750` canonical bytes; random-all `8471` normalized plus `219` cleanup-normalized and `1310` residuals totaling `-59,506`; and wasm-smith `9951` normalized plus `2` cleanup-normalized and `3` smaller residuals across `9956` comparable cases with only `44` Binaryen-only failures. No plain residual is larger. The `105` equal-size random-all cases each remove exactly one redundant local.

Focused case-515 plain DAE has a seven-run median of `20.351ms`, below the repository's one-second representative target. Release completion nevertheless remains blocked by a new dense-call artifact-scale probe: a `3,597,499`-byte, `5,374`-definition module with roughly `251,000` rendered direct calls exceeds the `1500`-second Starshine bound, while Binaryen v131 `--dae` completes in `0.356s`. Historical retained-artifact validity/idempotence and `-9,670` canonical delta remain useful checkpoints, but they do not close this current performance blocker.

## Scanner consolidation inventory and refactor plan, 2026-07-07

This inventory is source-review planning for [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt). It does not make direct plain DAE green: after selected singleton-result application was added above shared core-result application, typed scratch-local reuse, and the shared callsite rewrite application, the latest direct random-all lane remains raw-red (`9207/10000` normalized and `793` raw mismatches in `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-selected-result-application-20260710`). The superseded 2026-07-05 lane recorded `9004` normalized / `996` mismatches; the current lane removes the `dae-effectful-args=136` and `dae-localization=67` families from direct output while `coverage-forced-portable=259`, `dae-unused-params=214`, `dae-constant-args=197`, `dae-tail-call-boundary=63`, and `dae-convergence=60` remain raw-red. All five current direct residual families are strongly classified by source/diff/replay/size/performance evidence, but classification does not make raw output green.

| Binaryen strategy bucket | Current Starshine surfaces | Consolidation problem | Target abstraction |
| --- | --- | --- | --- |
| Ownership / call-boundary analysis | `DaeCurrentCallFacts`, `DaeCallBoundaryFacts`, `dae_collect_current_call_facts(...)`, `DaeAdjacentParamForwardEvidence`, `dae_adjacent_param_forward_evidence(...)`, escape scans, direct caller maps, `return_call` counting, dropped-call counters, and the larger original/dead-suffix summary family from `DaeOriginalBoundarySnapshot` / `dae_collect_original_boundary_snapshot(...)` through the `original_dead_suffix_*` helpers | The boundary core is already module-shaped, but it now carries many historical summary arrays and special-case preservation predicates. The reusable boundary carriers now group escape, tail-call, direct-caller, direct-call, dropped-call, imported/defined/total, type-index, and current/boundary fact snapshots for several core and selected lanes, and original imported/defined/escape/param/call-summary facts for the core setup; many original-vs-current legality decisions still flow through parallel arrays and selected residual cleanup guards. | A larger `DaeBoundaryFacts` / candidate record that owns visibility/escape status, direct callers, dropped calls, tail-call constraints, original-vs-current call summaries, and candidate legality. `dae_run_core_once(...)` should consume this record instead of a long parameter list of parallel arrays. |
| Value / stack-slice recovery | `DaeCallShape`, `DaeValueSlice`, `dae_instr_stack_effect(...)`, `dae_find_value_slice(...)`, `dae_collect_callsite_value_slices(...)`, `DaeSelectConditionLocalizationEvidence`, condition-carrying `DaeSelectValueSlices`, `DaeGcSelectValueSlicesEvidence`, `dae_select_condition_localization_evidence(...)`, `dae_gc_select_value_slices_evidence(...)`, `dae_gc_select_value_slices_candidate_evidence(...)`, `dae_gc_result_candidate_slice(...)`, and the shared `call_ref` / `call_indirect` returned-value operand evidence | This is the most promising reusable layer: callsite operand repair, typed-select actual recognition, returned-value recognition, and parametric typeidx returned-value recognition all need the same answer: which instruction range produces this value, what stack inputs it consumes, and whether replaying or dropping it preserves effects/traps. Several older bounded recognizers still encode length/cap assumptions rather than asking a shared value-slice API. | A `DaeValueSlice` evidence API with start/end, input/result arity, purity/effect/branch flags, direct call/ref/indirect target metadata, and optional candidate type evidence. Argument refinement, result refinement, and removed-operand repair should reuse it. |
| GC type evidence / LUB and refinement | `dae_type_idx_is_subtype_of(...)`, `dae_common_supertype_idx(...)`, `dae_lub_gc_candidate_ref_type(...)`, `dae_refined_gc_candidate_ref_type(...)`, block-type result candidate helpers, local-get evidence, direct-call result evidence, typed-select LUB, explicit-return and terminal-control result evidence | The LUB logic is increasingly centralized, but evidence producers remain scattered: allocation, `local.get`, direct call, typed/typeidx block/if/loop, select operands, explicit returns, flat multi-results, block carriers, and parametric controls each have their own recognizer shape. | A `DaeGcTypeEvidence` layer that separates evidence production from evidence combination. Producers return candidate ref type plus rewrite hook; the shared combiner handles nullability, exactness, nearest common supertype, old-type admissibility, and rewritten block/select type repair. |
| Operand/result repair | `dae_rewrite_callsite_at(...)`, `dae_rewrite_refined_gc_actual_slice(...)`, `dae_rewrite_refined_gc_actuals_instrs(...)`, `DaeSingleBodyControlShapeEvidence`, `DaeSingleBodyControlRewriteEvidence`, `dae_apply_single_body_control_rewrite_evidence(...)`, `dae_try_update_signature_only(...)`, `dae_update_defined_func_type(...)`, dropped-result call/drop repair, dead-suffix repair, and uninhabited/unreachable cleanup | Signature/callsite repair is Binaryen-shaped, but result-type rewrite hooks are embedded in candidate recognizers and callsite actual rewrites. This couples evidence gathering to mutation and makes it easy to add another narrow scanner instead of widening a common repair path. | A repair planner that receives boundary facts plus value/type evidence and produces explicit edits: signature update, callsite operand rewrite, result expression type rewrite, and effect-preserving dropped-operand statements. |
| Localization and effect preservation | `DaeOperandDisposition`, `DaeOperandRewriteDisposition`, `DaeOperandRemovalPlan`, `DaeCallsiteRewritePlan`, `DaeCallsiteRewriteApplicationEvidence`, `DaeCallsiteLocalizationContext`, `dae_operand_rewrite_disposition(...)`, `dae_stack_slice_localization_evidence_from_request(...)`, `dae_callsite_operand_removal_plan(...)`, `dae_callsite_rewrite_plan(...)`, `dae_apply_callsite_rewrite_plan(...)`, `dae_localization_scratch_slot(...)`, value-slice purity, typed scratch-local reconstruction, callsite replay/drop preservation, and the `dae-localization` / `dae-effectful-args` residual classifications | The shared disposition distinguishes direct drop, effect-preserving replay/drop, localize-and-retry eligibility, and escaping-branch rejection. Slice 161 derives one contiguous whole-argument plan and applies prefix/operands/call/suffix reconstruction once for ordinary replay/drop repair and typed localization. Slice 162 keeps the scratch pool caller-owned but reuses one DAE-owned local for every exact defaultable type across sequential and nested callsites; mixed types remain separate and non-null references still use nullable declarations. Evaluation/effect/trap order and call placement are unchanged. Explicit boundary-candidate rescheduling remains open. | Continue widening the shared plan/application only where another production owner or parity gap exists: preserve ambient stack/evaluation order/effects/traps across broader value slices and make retry/frontier scheduling explicit. Plain DAE may localize/retry as part of the core; optimizing cleanup remains a separate scheduler suffix. |
| Convergence / fixpoint | `dae_run_core(...)`, `DaeCoreResultApplication`, `dae_apply_core_result(...)`, `dae_apply_selected_core_result(...)`, `dae_apply_primary_selected_core_result(...)`, the bounded core iteration loop, `DaeBodyRewriteFixpointEvidence`, `dae_apply_body_rewrite_until_stable(...)`, `dae_apply_body_rewrite_until_stable_or_original(...)`, selected reverse exact-literal revisit, wrapper/forwarding-chain helpers, direct-return-call repair, and the `dae-convergence` profile | Local bounded body rewrites share one evidence-backed fixpoint driver. Slice 163 centralizes productive `DaePassResult` installation, touched adoption/OR merge, aggregate changed state, and primary-definition propagation; slice 164 adds the exact consumed-singleton selection clearing shared by five early selected-shape producers. Break/continue rules, selection setting, producer calls, current-fact refresh, iteration limits, trace labels, raw cleanup, and final touched policy remain lane-owned. | Continue toward an explicit phase scheduler only where phase/revisit ownership really overlaps. Selected artifact-only cleanups, raw cleanup, and final return-suffix handling should either move to named applications with matching touched semantics or stay visibly isolated. |
| Cleanup scheduling | `dae_cleanup_dropped_unreachable_debris(...)`, `dae_strip_module_nops_and_dropped_pure_leaf_once(...)`, selected Func237/Func287/Func288/Func327 cleanup families, `dae_run_nested_cleanup(...)`, and the `optimize` flag | The plain-vs-optimizing split is explicit at `dae_run_module_pass(... optimize?)` and `dae_run_nested_cleanup(...)`, but plain DAE still has raw cleanup needed for current parity fixes. Optimizing-only cleanup must not leak into plain DAE, and direct raw-red lanes must stay reported raw-red. | Name cleanup layers by ownership: mandatory validity/semantic repair in the shared core, direct-DAE raw parity cleanup with explicit evidence, and optimizing-only nested cleanup. Tests should keep `dead-argument-elimination` / `dae` distinct from `dae-optimizing`. |

Concrete refactor plan before broad code motion:

1. Introduce small evidence abstractions first, not a scheduler rewrite. The first one hundred sixty-four implementation slices extracted reusable typed-select GC evidence for returned-value slices and argument actuals, shared indirect-call result-slice evidence for `call_ref` / `call_indirect`, shared direct-call GC result signature/refinement evidence, a shared `DaeGcTypeEvidence` / `dae_gc_result_slot_evidence(...)` carrier for candidate/refinement/rewrite hooks in flat result slots, a first `DaeValueSlice` / `dae_find_select_value_slices(...)` carrier for typed-select operand/condition bounds and effect/branch disposition, `DaeSelectConditionLocalizationEvidence` / `dae_select_condition_localization_evidence(...)` for exact singleton-producer versus multi-instruction stack-slice condition evidence shared by select bound recovery plus argument-side and returned-value GC select evidence, a `DaeControlResultSlice` / `dae_find_multi_result_control_slice(...)` carrier for flat, block-carried, and terminal-return multi-result arm bounds, `DaeFlatMultiResultSuffixEvidence` / `dae_flat_multi_result_suffix_evidence(...)` for shared flat and terminal-return result-suffix proof under the carrier-chain helper, `DaeLocalReferenceExtentEvidence` / `dae_local_reference_extent_evidence(...)` for shared recursive local-reference maximum and high-local limit proof under cleanup-local preservation gates, `DaeControlResultGcEvidence` / `dae_control_result_slice_gc_evidence(...)` for per-lane GC evidence over recovered control slices, `DaeControlResultPairGcEvidence` / `dae_control_result_pair_gc_evidence(...)` for paired terminal-arm LUB/refinement and selective arm rewrites, `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)` for shared fallthrough and terminal-return if-pair slice/evidence/LUB/refinement carriers, `dae_parametric_typeidx_multi_result_arm_gc_evidence(...)` for parametric typeidx arm prefix/result evidence, `DaeGcActualShapeDiscoveryEvidence` / `dae_gc_actual_shape_discovery_evidence(...)` plus `DaeGcActualTypeEvidence` / `dae_gc_actual_type_evidence(...)` for target-independent direct/control/select actual-shape discovery followed by current-target candidate/refinement/repair planning, `DaeOperandDisposition` / `dae_operand_disposition_from_range(...)` for shared droppable/effect/branch range classification plus callsite removed-operand preservation, `DaeOperandRewriteAction` / `DaeOperandRewriteDisposition` for explicit direct-drop, preserve-effects-and-drop, localize-and-retry, or reject-escaping-branch decisions, `DaeOperandRemovalPlan` plus `dae_callsite_operand_removal_plan(...)` for consistency-checked keep/replay/drop callsite repair planning, `DaeValueSlice.disposition` / `dae_value_slice_from_operand_disposition(...)` so select/control value slices carry the same repair carrier, `dae_collect_callsite_value_slices(...)` / `dae_gc_actual_value_slice_shape_evidence(...)` so callsite removal, actual-refinement repair, and direct GC actual collection consume the same value-slice/disposition carrier before target-specific GC evaluation, `DaeCallsiteValueSliceEvidence` / `dae_callsite_value_slice_evidence(...)` so callsite-wide cursor, argument count, entry-stack context, value slices, and repair dispositions are passed as one evidence record through callsite removal, GC refinement, uniform-constant collection, direct GC actual collection, and the selected global-suffix repair path, `dae_value_slice_single_instruction_index(...)` and `DaeCallsiteSingleInstructionActualEvidence` / `dae_callsite_single_instruction_actual(...)` for reusable single-instruction actual evidence consumed by uniform-constant actual collection and selected global-suffix repair, `DaeCallBoundaryFacts` for escape/tail/direct-caller gates in recursive uniform-constant forwarding and uniform-constant collection entrypoints, `DaeModuleBoundarySnapshot` / `dae_collect_module_boundary_snapshot(...)` for imported/defined/total counts, type indices, current call facts, and boundary facts consumed by the core and selected boundary lanes, `DaeOriginalBoundarySnapshot` / `dae_collect_original_boundary_snapshot(...)` for original imported/defined/total counts, escapes, param counts, and call summaries consumed by core setup, `DaeOriginalResultPreservationEvidence` / `dae_original_result_preservation_evidence_from_row(...)` for original-vs-current result-preservation guards consumed by the core and reverse exact-literal loops, `dae_original_result_preservation_blocks_argument_side_result_repair(...)` for shared argument-side result-repair/drop-result blocking over that carrier, `DaeDroppedResultRemovalLegalityEvidence` / `dae_dropped_result_removal_legality_evidence(...)` for common result-removal blockers across selected and full-module dropped-result cleanup loops, `DaeDroppedResultRepairPlan` / `dae_apply_dropped_result_repair_plan(...)` for explicit all-caller vs no-param dead-suffix repair plus dropped-call-drop cleanup decisions across selected and full-module result-removal paths, `DaeDroppedResultModuleContext` / `dae_dropped_result_module_context(...)` for shared imported/defined type-section, function-section, selected/full dropped-result scope, params/results, legality setup, and selected-lane perf-timer-compatible setup across full-module plus selected dropped-result cleanup paths, `DaeOriginalResultPreservationContext` / `dae_original_result_preservation_context(...)` plus `DaeOriginalResultPreservationRow` / `dae_original_result_preservation_row_from_context(...)` / `dae_original_result_preservation_evidence_from_row(...)` for shared original-vs-current result-preservation input arrays and per-definition indexed guard facts consumed by the main core zero-param lane, main current-param lane, reverse exact-literal lane, no-param dropped-caller guard, and parameterized self-side-chain guard, `DaeControlEntryStackEvidence` / `dae_control_entry_stack_evidence(...)` plus `dae_control_entry_stack_values(...)` for nested-control entry/result stack evidence and shared child entry-stack extraction consumed by recursive callsite/value-stack scanners, `DaeNestedControlEntryEvidence` / `dae_nested_control_entry_evidence(...)` for instruction-level block/loop/`try_table`/`if` entry-stack evidence consumed by refined-GC actual rewriting, callsite removal, uniform-constant actual collection, and direct-GC actual collection, `DaeControlBlockTypeSignatureEvidence` / `dae_control_block_type_signature_evidence(...)` for shared block/loop/if/try_table type-signature resolution consumed by block-type stack effects, control-entry evidence, and parametric typeidx terminal-control refinement, `DaeStackOperandWindowEvidence` / `dae_stack_operand_window_evidence(...)` plus `DaeStackEffectEvidence` / `dae_stack_effect_evidence(...)`, `dae_stack_single_value_producer_evidence(...)`, `dae_control_single_result_block_type_evidence(...)`, and `dae_stack_bool_*` / `dae_stack_int_*` helpers for stack operand provenance window lookups, single-value producer proof, block-type single-result proof, and instruction stack-effect pop/push updates consumed by original dead-suffix/result-preservation, value-slice recovery, typed-select condition, indirect-call result-slice, direct GC result control candidates, and multi-result refinement scanners, `DaeDeadSuffixEvidence` / `dae_dead_suffix_evidence(...)` for shared unreachable-root and suffix-tail recovery, `DaeTailCallBoundaryEvidence` / `dae_tail_call_boundary_evidence(...)` for shared direct-vs-indirect tail-call blocker facts, `DaeDirectCallTargetEvidence` / `dae_direct_call_target_evidence(...)` for shared target-specific direct-call counts/drop/undropped/live-prefix facts, `DaeDeadSuffixDirectCallEvidence` / `dae_dead_suffix_direct_call_evidence(...)` for shared dead-suffix dropped/undropped direct-call target facts consumed by full-module dropped-result cleanup gates, `DaeDeadSuffixDirectTargetEvidence` / `dae_dead_suffix_direct_target_evidence(...)` for shared dead-suffix direct/dropped/undropped target lists consumed by original-boundary target collectors, `DaeDroppedResultScopeDirectCallEvidence` / `dae_dropped_result_scope_direct_call_evidence(...)` for scoped selected-vs-all caller direct-call count/drop/undropped/live-prefix facts consumed by dropped-result removal legality, `DaeOriginalNoParamDroppedCallerGuardEvidence` / `dae_original_no_param_dropped_caller_guard_evidence(...)` for original no-param dropped-caller guard facts consumed by result-preservation evidence and param-operand side-call collection, `DaeOriginalParameterizedUndroppedSelfSideChainEvidence` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` for the parameterized undropped self-side-chain guard consumed by original result-preservation and current/unobserved core checks, `DaeDroppedResultCalleeRewriteEvidence` / `dae_dropped_result_callee_rewrite_evidence(...)` for shared dropped-result callee suffix/simplification/result-removal-blocker facts consumed by the full-module unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup gates, `DaeEscapedResultDirectCallEvidence` / `dae_escaped_result_direct_call_evidence(...)` for shared dropped/undropped escaped-result direct-call facts consumed by dead-suffix preservation collectors, `dae_rewrite_dropped_result_calls_with_policy_instrs(...)` for shared dropped-result call/drop repair traversal with explicit unreachable-suffix policy, `DaeDroppedResultScope` / `dae_dropped_result_scope_from_optional(...)` for selected-vs-all and full-module dropped-result legality/repair scoping, `DaeDroppedResultCandidate` plus `dae_dropped_result_candidate_from_boundary_facts(...)` / `dae_dropped_result_candidate_from_current_call_facts(...)` for dropped-result direct-call/drop-count/caller/owned eligibility facts consumed by selected candidate collection and per-def removal attempts, `dae_record_uniform_const_candidate(...)` for uniform-constant actual candidate merge/invalidation, and `DaeCallBoundaryCandidate` / `dae_call_boundary_candidate(...)` for owned-callee count/caller/self-recursion eligibility facts consumed by core and selected candidate loops; the remaining target is a broader value-slice/helper layer that can cover typeidx control beyond final actuals/terminal arms, localization/retry disposition, broader boundary/result-removal legality beyond the first shared blocker/direct-tail/direct-target/dead-suffix target carriers, original-vs-current legality beyond the first snapshot, result-preservation evidence, no-param dropped-caller guard, parameterized self-side-chain guard, argument-side repair gate, preservation-input context carriers, and original result-removal prune-row evidence plus dropped-only/mixed result-side prune predicates and the first prune-evidence aggregate for params-absent cleanup and uncalled-after-param-cleanup blockers, `DaeNoEscapeSliceEvidence` / `dae_no_escaping_branch_copied_range_evidence(...)` for localization/control gates that need both branch-escape proof and an accepted copied range, with `dae_no_escaping_branch_copied_prefix_evidence(...)` and `dae_no_escaping_branch_copied_suffix_evidence(...)` projections for prefix/suffix consumers that also need the accepted copied slice, and `DaeTerminalIfNoEscapeCarrierEvidence` / `dae_terminal_if_no_escape_carrier_evidence(...)` for terminal zero-result `if` carriers that share branch-safe condition-prefix and optional else-body evidence across Func237/Func256 localization-control loops, plus `DaeMixedMultiResultReturnArmGcEvidence` / `dae_mixed_multi_result_return_arm_gc_evidence(...)` for mixed return/fallthrough multi-result `if` arms that share return-arm candidate/LUB rejection and effect-free fallthrough acceptance across then/else paths, and `DaeSingleResultControlPairGcEvidence` / `dae_single_result_control_pair_gc_evidence(...)` for single-result `if` arms that share then/else candidate LUB and rewritten-body selection across implicit and explicit-return result paths, plus `DaeMixedMultiResultReturnArmRewriteEvidence` / `dae_apply_mixed_multi_result_return_arm_refinements(...)` for mixed return/fallthrough multi-result `if` arms that share refined-slot rewrite application across then/else returned arms, and `DaeControlResultSingleRefinementApplicationEvidence` / `dae_apply_control_result_single_refinements(...)` for single-arm refined result-vector application now consumed by parametric typeidx terminal control and flat multi-result direct-GC body refinement, plus `DaeTerminalNoEscapePrefixEvidence` / `dae_terminal_no_escape_prefix_evidence(...)` for terminal-instruction bodies with copied no-escape prefixes now consumed by unreachable-after-if-else folding and leading terminal-`br 0` block splicing, and `DaeMixedSingleResultReturnArmGcEvidence` / `dae_mixed_single_result_return_arm_gc_evidence(...)` for mixed return/fallthrough single-result `if` arms now consumed by both then/else paths, plus `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` for shared immediate-copy and used-temp-copy gap range policy in Func237 cleanup-localization subflows, `DaeLocalCopyTargetEvidence` / `dae_local_copy_target_evidence(...)` for protected temp/target distinctness and parameter-local rejection shared by Func237 immediate-copy, used-temp copy, and store-carrier copy cleanup-localization, `DaeTargetCallsiteEvidence` / `dae_target_callsite_evidence(...)` for direct-only versus direct-or-`return_call` target-callee matching across call-boundary and callsite-repair consumers, `DaeAdjacentParamForwardEvidence` / `dae_adjacent_param_forward_evidence(...)` for target-independent exact adjacent `local.get 0 .. local.get n-1` operand-range proof across local-use suppression, self-call origin handling, and dead-suffix full-forward detection, and `DaeDroppedPureLeafCleanupEvidence` / `dae_dropped_pure_leaf_cleanup_evidence_from_stack(...)` / `dae_dropped_pure_leaf_cleanup_evidence_from_leaf(...)` for shared raw-cleanup preservation policy over stack-truncated and adjacent dropped pure leaves, and `DaeLiveLocalCopyCleanupEvidence` / `dae_live_local_copy_cleanup_evidence(...)` for shared raw-cleanup live-local-copy chain replacement across the five-instruction, three-instruction, and two-instruction copy-chain branches over the existing droppable pure-stack carrier, and `DaeRedundantAccessCleanupEvidence` / `dae_redundant_access_cleanup_evidence(...)` for shared adjacent local/global access-pair cleanup rewrites, and `DaeDroppedLocalTeeSuffixCleanupEvidence` / `dae_dropped_local_tee_suffix_cleanup_evidence(...)` for shared raw-cleanup local-tee/drop and pure-suffix drop rewrites, and `DaeSingleBodyControlShapeEvidence` / `DaeSingleBodyControlRewriteEvidence` / `dae_single_body_control_shape_evidence(...)` / `dae_apply_single_body_control_rewrite_evidence(...)` for shared block/loop identity, original/rewritten body selection, and replacement block-type reconstruction across explicit-return void repair and refined single-result GC repair, with `DaeSingleResultControlGcCandidateEvidence` retaining the candidate-specific GC facts, and `DaeResultRewriteApplicationEvidence` / `dae_apply_result_rewrites_to_body(...)` for shared masked result-slot rewrite application across flat control-result GC evidence, paired control-result LUB evidence, mixed returned-arm repair, and mixed terminal-result repair, and `DaeSelectValueRewriteApplicationEvidence` / `dae_apply_select_value_rewrites_to_body(...)` for shared typed-select left/right/select rewrite application across single-result returned-value select repair, flat multi-result typed-select operand repair, and argument-side refined-actual repair, and `DaeSingleResultControlPairApplicationEvidence` / `dae_apply_single_result_control_pair_gc_evidence(...)` for shared single-result `if` rewrite application across value-result and explicit-return void-result paths, and `DaeIndexedInstructionRewriteApplicationEvidence` / `dae_apply_indexed_instruction_rewrites_to_body(...)` for shared indexed instruction rewrite application under masked result-slot repair and all three typed-select repair paths, and `DaeMultiResultOperandSlotEvidence` / `dae_multi_result_operand_slot_prefix_evidence(...)` / `dae_multi_result_operand_slot_single_producer_evidence(...)` for shared multi-result suffix slot prefix and single-producer fallback evidence under the single-`if` and typed-select operand scanners, and `DaeStackSliceLocalizationPolicy` / `dae_stack_slice_localization_evidence_from_policy(...)` for shared select-condition and parametric typeidx prefix localization policy projection, `DaeSelectConditionLocalizationEvidence` / `dae_select_condition_localization_evidence(...)` for singleton-producer versus multi-instruction stack-slice condition evidence shared by select bound recovery and both argument/result GC select consumers, `dae_apply_first_instruction_rewrite_to_body(...)` for shared first-matching body repair scans consumed by Func237 cleanup-repair loops, `dae_try_rewrite_first_control_child_body(...)` for shared recursive control-child repair application consumed by Func237 cleanup-repair paths, and `dae_apply_first_current_or_control_child_body_rewrite(...)` for shared current-body-then-recursive-child body-rewrite application now consumed by dropped-result branch-carrier, unreachable-after-if-else, immediate-local-copy, and used-temp-copy Func237 cleanup-repair paths, and `DaeRecursiveBodyRewriteApplicationEvidence` / `dae_apply_first_recursive_body_rewrite(...)` for shared recursive body-rewrite application evidence now consumed by that compatibility wrapper plus the Func237 inner-iteration loop wrapper and i32-param-loop spill recursive repair paths, and `DaeBodyRewriteFixpointEvidence` / `dae_apply_body_rewrite_until_stable(...)` plus `dae_apply_body_rewrite_until_stable_or_original(...)` for bounded body-rewrite convergence evidence now consumed by Func237 inner-iteration loop wrapping, selected zero-tee/set splitting, three Func237 local-copy cleanup families, and Func256 mapped-loop-condition repair instead of each carrying its own fixed-point loop, plus `dae_apply_first_recursive_instruction_body_rewrite(...)` / `dae_apply_first_recursive_control_child_body_rewrite(...)` for shared first-matching recursive instruction rewrite evidence now consumed by the control-child projection, Func237 leading-retain hoisting, and Func237 terminal-`br 0` block flattening, and `dae_apply_first_recursive_instruction_body_rewrite_to_body(...)` for the evidence-backed array-return projection now consumed by Func237 nested branch-result flattening, leading-retain hoisting, terminal-`br 0` block flattening, and zero-branch flattening, plus `DaeStackSliceLocalizationEvidence.copied_slice` / `dae_stack_slice_localization_policy_accepts(...)` for copied no-escape repair evidence and a shared policy-acceptance projection consumed by typed-select condition recovery and parametric typeidx prefix-consumption, and `dae_control_block_type_signature_evidence_from_type_sec(...)`, `dae_control_block_type_stack_shape_evidence(...)`, and `dae_control_block_type_exact_signature_accepts(...)` for shared type-section-backed control block shape/signature proof now consumed by control-entry evidence plus raw cleanup block-type predicates, and `dae_control_block_type_zero_input_evidence(...)` for shared zero-input control block proof now consumed by direct GC explicit-return block/loop/if candidate checks, `dae_control_block_type_zero_input_output_count_evidence(...)` for shared zero-input/output-count control block proof now consumed by single-block result-carrier recovery, mixed if/fallthrough GC candidates, terminal-return block carriers, and multi-result control refinement carrier gates, and `DaeControlBlockTypeGcResultEvidence` / `dae_control_block_type_non_null_concrete_gc_result_evidence(...)` / `dae_control_block_type_non_null_concrete_gc_result_slot_evidence(...)` / `dae_control_block_type_single_non_null_concrete_gc_result_evidence(...)` / `dae_control_block_type_last_non_null_concrete_gc_result_evidence(...)` for shared non-null concrete GC result-slot proof now consumed by direct GC actual and result-shape GC actual recovery, with single-result and trailing-result wrappers sharing the same slot selector and signature-to-result projection, plus `DaeEmptyZeroResultControlCleanupEvidence` / `dae_empty_zero_result_control_cleanup_evidence(...)` / `dae_rebuild_cleaned_single_body_control_or_drop(...)` for shared raw-cleanup zero-result block/loop/`try_table` empty-body drop decisions after child cleanup, `DaeI32ParamZeroResultLoopEvidence` / `dae_i32_param_zero_result_loop_evidence(...)` plus `dae_control_block_type_exact_signature_evidence(...)` for shared type-section-backed single-`i32`-param/zero-result loop proof now consumed by both explicit-carrier and iteration-local typed-loop spill paths, `DaeTerminalReturnBodyEvidence` / `dae_terminal_return_body_evidence(...)` for shared terminal-return body proof now consumed by terminal-return block carriers and nested block-carrier unwrapping, `DaeEffectFreeBodyEvidence` / `dae_effect_free_body_evidence(...)` for shared side-effect-free body proof now consumed by single-block result-carrier recovery, nested block carrier unwrapping, and mixed-arm fallthrough evidence, and `DaeEffectFreeFallthroughEvidence` / `dae_effect_free_fallthrough_evidence(...)` for shared mixed-arm side-effect-free fallthrough proof in single-result and multi-result return/fallthrough refinement; remaining paired-arm repair disposition still extends beyond these fallthrough/terminal-return GC helper subsets, and cleanup scheduling is still scanner-heavy beyond these local preservation and empty-control/effect-free-body/fallthrough carriers.
2. Keep code changes behavior-preserving where practical, then add one red-first behavior case that proves the abstraction handles a previously scanner-specific shape. Good next candidates are parametric typeidx/control result evidence, argument-side typeidx evidence, broader paired-arm repair disposition, or a broader stack-slice carrier for repair paths, because current code already has tests and many near-duplicate bounded recognizers.
3. Keep plain and optimizing scheduling split in every test: shared-core evidence may affect both variants, but nested cleanup and touched-function post-optimization must remain behind `optimize=true`.
4. Do not close or green-report direct DAE from this refactor. Any compare-pass update must report raw mismatches, cleanup-normalized counts, validation/command failures, cache counters, and agent-classified residual families separately for direct `dead-argument-elimination` and `dae-optimizing`.

Acceptance criteria for the first consolidation:

- at least two existing scanner surfaces call the new reusable evidence helper;
- focused tests fail before any behavior-widening implementation and pass after it;
- `moon test src/passes` is run for behavior changes, while docs-only inventory slices may use `git diff --check` and source review;
- docs and `agent-todo.md` record which scanners were consolidated and which remain scanner-heavy.

### Typeidx control body-installation consolidation, 2026-07-12

Slice 186 source-reviewed the three production paths that rebuild typeidx controls and immediately install them into a copied function body: parametric terminal block/loop/paired-`if` refinement, zero-input terminal paired-`if` refinement, and preserved multi-result loop carriers. Their evidence, selected control index, active `TypeSec` selection, inner bodies, and result-refinement policy differ, but destination validation, control rebuild, outer-body copy, slot replacement, and rollback are identical.

`DaeTypeidxControlBodyRewriteApplication` and `dae_rebuild_typeidx_control_in_body(...)` now own that exact transaction above the existing typeidx-control rebuild. All three consumers supply their caller-selected active type section and destination slot, then receive the updated type section plus installed body. Candidate/LUB production, arm repair, result projection, touched policy, final module application, and scheduling remain caller-owned; plain DAE receives no optimizing-only cleanup or phase.

The red-first whitebox contract failed while the body-installation helper was unbound, then passed while proving surrounding-body preservation, active equivalent-type reuse, and invalid-slot rejection before mutation. Validation reached DAE whitebox `200/200`, public DAE `303/303`, pass suites `5208/5208`, `moon info` with 9 warnings, full tests `8660/8660`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches, zero failures, and Binaryen cache `10000/0`. Existing source/diff/replay/size/performance classifications are unchanged; direct DAE remains explicitly raw-red.

### Refined result-vector application consolidation, 2026-07-12

Slice 185 source-reviewed paired terminal-arm LUB application and sparse single-arm refinement application. Their evidence production remains distinct: paired evidence computes a per-slot LUB and rewrites both arms, while single evidence carries optional refined candidates and one rewritten body. Their result-vector mutation was identical, including exact length admission, optional mirrored `refined_results` updates, sparse slot application, no-refinement rejection, and transactional mismatch behavior.

`DaeOptionalRefinedResultSlotsApplicationEvidence` and `dae_apply_optional_refined_result_slots(...)` now own that shared application. `dae_apply_control_result_pair_refinements(...)` projects the pair's fully refined vector into optional slots; `dae_apply_control_result_single_refinements(...)` supplies its sparse candidates directly. Pair/single evidence, body rewrites, control reconstruction, active `TypeSec` timing, touched policy, and scheduling remain caller-owned.

The red-first whitebox contract failed while the helper was unbound, then passed while proving sparse mirrored updates and mismatch rollback. Validation reached DAE whitebox `199/199`, public DAE `303/303`, pass suites `5207/5207`, `moon info` with 9 warnings, full tests `8659/8659`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero failures. Existing residual classifications are unchanged; direct DAE remains explicitly raw-red and plain scheduling is unchanged.

### Verified uniform-constant candidate consolidation, 2026-07-12

Slice 183 adds `dae_try_rewrite_verified_uniform_const_candidate(...)` above the existing uniform-constant evidence and generic removed-parameter candidate application. Func867 default-argument specialization and the forwarded Func3765 suffix specialization are the two production consumers. The shared boundary projects uniform evidence only for removed parameters, verifies each selected actual against the exact expected materialized constant, and then delegates caller repair, callee local/constant reconstruction, cleanup, signature/code finalization, touched metadata, and rollback to the generic candidate path. Func867 retains its current caller discovery and seven-parameter type gate. Func3765 retains its exact Func3737 caller/count and one-`i32` suffix contract, but now runs after the optimizing-only Func3737 folded-multivalue rewrite so the forwarded `1024` is current evidence instead of an assumed constant. The pre-wrapper Func3737 scanner remains separate because its folded multivalue caller is still outside generic value-slice recovery.

The red-first whitebox contract failed because the shared helper was unbound, then passed while proving multi-instruction kept operands, exact removed-constant evidence, shared caller/callee rewrite, signature/touched results, forwarded-local recovery, and mismatch rollback. Public DAE initially exposed the old Func3765-before-Func3737 evidence timing; after making that optimizing-only dependency explicit, validation reached whitebox `197/197`, public DAE `303/303`, pass suites `5205/5205`, `moon info` with 9 warnings, full tests `8657/8657`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches, zero failures, and Binaryen cache `10000/0`. Residual families and classifications are unchanged. Direct DAE remains explicitly raw-red, and plain DAE receives no optimizing-only schedule or cleanup.

### Current-section module code finalization consolidation, 2026-07-12

Slice 182 adds `dae_finalize_module_code_rewrite(...)` above the existing signature context, code-finalization plan, and application layers. Direct GC result/body refinement and the selected Func323 nine-parameter caller-plus-callee product are the two production consumers. The shared boundary resolves the current `FuncSec`/`CodeSec`/`TypeSec` only after each producer has completed its caller-owned evidence and code product, rejects a code array whose length no longer matches the current code section, constructs the params/results/touched plan, and applies signature plus code replacement transactionally. Direct GC passes its optionally pre-updated active `TypeSec`; Func323 passes the current module and retains its narrower control/add-zero cleanup. GC candidate/LUB evidence, caller repair, result-body ownership, count verification, traces, and scheduling remain outside.

The red-first whitebox contract failed because `dae_finalize_module_code_rewrite(...)` was unbound, then passed while proving current equivalent-type reuse without discarding the active type section, code installation, changed-function touched metadata, and malformed-code rollback. Validation reached whitebox `196/196`, public DAE `303/303`, pass suites `5204/5204`, `moon info` with 9 warnings, full tests `8656/8656`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches, zero failures, and Binaryen cache `10000/0`. Residual families and their source/diff/replay/size/performance classifications are unchanged. Direct DAE remains explicitly raw-red, and the optimizing-only Func323 schedule is not imported into plain DAE.

### Direct-caller scope application consolidation, 2026-07-12

Slice 181 adds `DaeDirectCallerRewriteEvidence`, `DaeDirectCallerScopeApplication`, and `dae_apply_direct_caller_scope_rewrite(...)` above the direct GC actual-repair and generic removed-parameter callsite-repair products. The shared boundary owns selected direct-caller iteration, invalid-definition skipping, current rewritten-function delivery, exact rewrite-count accumulation, changed-definition tracking, function replacement, no-op behavior, and transactional rollback when a later callback rejects. Direct GC keeps its body-only refined-actual traversal and self-caller body synchronization; removed-parameter repair keeps current caller-signature lookup, typed scratch-local declaration repair, and localized-parameter tracking. Both consumers retain caller-owned exact count verification, callee repair/finalization, touched policy, and type timing.

The red-first whitebox contract failed because the application helper and records were unbound, then passed while proving productive plus no-op traversal, invalid/out-of-scope preservation, exact counts, changed-definition metadata, and rollback after a later failure. Validation reached whitebox `195/195`, public DAE `303/303`, pass suites `5203/5203`, `moon info` with 9 warnings, full tests `8655/8655`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches, zero failures, and Binaryen cache `10000/0`. Residual families are unchanged. Direct DAE remains explicitly raw-red, and plain DAE receives no optimizing-only scheduling or cleanup.

### Dropped-result scope application consolidation, 2026-07-12

Slice 180 adds `dae_rewrite_dropped_result_calls_in_scope_with_policy(...)` above the recursive call/drop rewrite policy. Ordinary pre-signature dropped-result call rewriting and post-repair anywhere call/drop cleanup are the two production consumers with the same scope iteration, bounds handling, function replacement, no-op behavior, and rewrite counting. Each caller still selects whether an unreachable root truncates the traversal: the count-verified signature rewrite uses the live-prefix policy, while cleanup scans anywhere after the caller-selected repair ordering. The avoidable live/anywhere body wrappers and per-function cleanup wrapper are removed.

The red-first whitebox contract failed because the scoped policy helper was unbound, then passed while proving live-prefix truncation, anywhere dead-suffix cleanup, exact rewrite counts, and out-of-scope preservation. Validation reached whitebox `194/194`, public DAE `303/303`, pass suites `5202/5202`, `moon info` with 9 warnings, full tests `8654/8654`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches, zero failures, and Binaryen cache `10000/0`. Residual families are unchanged. Direct DAE remains explicitly raw-red, and plain DAE receives no optimizing-only scheduling or cleanup.

### Dropped-result callee application consolidation, 2026-07-12

Slice 179 adds `dae_apply_dropped_result_callee_rewrite(...)` above the existing dropped-result repair plan and generic module code finalizer. Ordinary dropped-result removal and the unreachable-callee result-removal application are the two production consumers with the same post-evidence contract: execute the caller-owned repair plan, install the caller-produced callee, preserve optional repair/type performance timer labels, and finalize signature/code sections with the caller-selected touched policy. Result-removal legality, direct-call rewrite/count verification, selected/full scope, repair ordering, callee construction, retained params, and scheduling remain caller-owned. The three unreachable-callee decisions continue to share their narrower wrapper/plan.

The red-first whitebox contract failed because `dae_apply_dropped_result_callee_rewrite(...)` was unbound, then passed while proving no-param dead-suffix repair, dropped-call-drop cleanup, caller/callee touched widening, callee installation, and result-signature removal. Validation reached whitebox `193/193`, public DAE `303/303`, pass suites `5201/5201`, `moon info` with 9 warnings, full tests `8653/8653`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`; every lane has zero cleanup-normalized matches/failures and Binaryen cache `10000/0`. Residual classifications are unchanged. Direct DAE remains intentionally raw-red, and plain-vs-optimizing scheduling is unchanged.

### Removed-parameter callee application consolidation, 2026-07-12

Slice 178 adds `dae_apply_removed_param_callee_rewrite(...)` above the removed-parameter evidence and generic module code finalizer. Generic direct-candidate rewriting and the selected Func3737 wrapper-suffix product are the two production consumers with an exact application contract: recompute written locals after parameter projection, run default-zero/control/add-zero cleanup, collapse an unobservable prefix to `unreachable`, install the callee, widen touched metadata to every changed function, and finalize the current signature/code sections. Candidate discovery, direct-call and suffix verification, callsite repair, constant selection, current-fact lifetime, and scheduling remain caller-owned. The selected Func323 nine-parameter product remains separate because its callee cleanup omits the default-zero-high-guard stage.

The red-first whitebox contract failed because `dae_apply_removed_param_callee_rewrite(...)` was unbound, then passed while proving cleanup, caller/callee touched accounting, code installation, and signature finalization. Validation reached whitebox `192/192`, public DAE `303/303`, pass suites `5200/5200`, `moon info` with 9 warnings, full tests `8652/8652`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`; every lane has zero cleanup-normalized matches/failures and Binaryen cache `10000/0`. Existing source/diff/replay/size/performance classifications are unchanged. Direct DAE remains intentionally raw-red, and plain-vs-optimizing scheduling is unchanged.

### Typeidx control rebuild application consolidation, 2026-07-12

Slice 177 adds `dae_rebuild_typeidx_control(...)` above the existing `DaeTypeidxControlRewritePlan` / application layers. Parametric terminal typeidx block/loop/if refinement, zero-input terminal multi-result `if` refinement, and preserved multi-result loop-carrier refinement are the three production consumers. The shared boundary now owns plan admission, equivalent simple function-type reuse or append, and typed control reconstruction in one operation. Later consumers use the active rewritten `TypeSec`, so a second control rebuild cannot discard an earlier type-section product. Candidate discovery, GC LUB/refinement, consumed-param stack policy, result/body selection, code finalization, and scheduling remain caller-owned.

The red-first whitebox contract failed because `dae_rebuild_typeidx_control(...)` was unbound, then passed while proving first append, second equivalent-type reuse from the active type section, block/loop reconstruction, and invalid paired-`if` rejection before type mutation. Validation reached whitebox `191/191`, public DAE `303/303`, pass suites `5199/5199`, `moon info` with 9 warnings, full tests `8651/8651`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`; every lane has zero cleanup-normalized matches/failures and Binaryen cache `10000/0`. Direct DAE remains intentionally raw-red and plain-vs-optimizing scheduling is unchanged.

### Multi-result operand-sequence evidence consolidation, 2026-07-12

Slice 176 adds `DaeMultiResultOperandSequenceEvidence` / `dae_multi_result_operand_sequence_evidence(...)` as a reusable value/control evidence boundary for the single-result-`if` and typed-select scanners inside flat multi-result result refinement. The shared walker owns terminal-return-aware suffix bounds, trivially dropped prefix recovery, ordered result-slot traversal, ordinary single-producer fallback, exact operand/value indices, and the requirement that at least one specialized slot is present. The two production consumers retain their distinct candidate callbacks, GC LUB/refinement evidence, and rewrite application. Red-first whitebox locks one specialized slot followed by one fallback slot and rejection of an all-fallback sequence. Validation reached whitebox `190/190`, public DAE `303/303`, pass suites `5198/5198`, `moon info` with 9 warnings, full tests `8650/8650`, and a native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, all with zero failures and Binaryen cache `10000/0`. Direct DAE remains intentionally raw-red; plain-vs-optimizing scheduling is unchanged.

### Generic module-signature boundary consolidation, 2026-07-10

Slice 157 extracted `DaeModuleSignatureContext`, `dae_module_signature_context(...)`, and `dae_apply_module_signature_finalization(...)` from the overlap between `dae_try_update_signature_only(...)`, dropped-result finalization, and `dae_try_remove_dead_suffix_only_unreachable_params(...)`. The context owns only the definition/type/function section state needed to replace a function signature; it does not collect dropped-result scope or legality. The finalizer updates type/function sections, strips stale names, and returns callee-only changed/primary metadata while preserving the code section, so code reconstruction remains explicit in the two body-mutating consumers. `DaeDroppedResultModuleContext` now nests this generic boundary and keeps result-specific scope/legality beside it.

The red-first whitebox contract failed on the missing generic helpers, then passed while proving signature replacement does not silently rebuild code. Focused/full validation reached DAE whitebox `171/171`, pass-manager `189/189`, public DAE `302/302`, `moon test src/passes` `4441/4441`, `moon info` with 9 pre-existing warnings, full `moon test` `7880/7880`, and a native CLI build. Fresh no-reduction dedicated direct, direct random-all, and normalized optimizing random-all lanes remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families and zero failures. This is generic boundary/finalization ownership only: selected/full dropped-result policy, repair ordering, touched widening, plain-vs-optimizing scheduling, and explicit direct raw-red reporting are unchanged.

### Generic module code-finalization consolidation, 2026-07-10

Slice 158 source-reviewed the apparent code-reconstruction overlap rather than widening the signature helper blindly. `DaeModuleCodeFinalizationPlan`, `DaeModuleCodeTouchedPolicy`, `dae_module_code_finalization_plan(...)`, and `dae_apply_module_code_finalization(...)` now sit directly above the generic signature finalizer. They own only copied next params/results, signature replacement, code-section installation, and explicit callee-only versus changed-function touched accounting.

Four honest production surfaces consume the generic application: direct GC refinement when caller or callee bodies changed, selected dropped-result append-`drop` removal, full-module dropped-result unreachable-callee application, and dead-suffix-only unreachable parameter removal. Signature-only GC refinement still stops at `dae_apply_module_signature_finalization(...)`. Call-count verification, optional pre-updated control type sections, result legality, repair/drop ordering, callee-body construction, and selected/full policy remain caller-owned; direct GC and dead-suffix paths retain callee-only touched masks, while only selected dropped-result removal requests changed-function widening.

The red-first whitebox contract failed on the missing plan/application helpers, then passed while proving code installation, signature replacement, and explicit changed-function touched widening. Focused/full validation reached DAE whitebox `172/172`, pass-manager `189/189`, public DAE `302/302`, `moon test src/passes` `4442/4442`, `moon info` with 9 pre-existing warnings, full `moon test` `7881/7881`, and a native CLI build. Fresh no-reduction dedicated direct, direct random-all, and normalized optimizing random-all lanes remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families, zero failures, and Binaryen cache `10000/0`. This is generic application ownership, not raw parity or a scheduler change.

### Typeidx control reconstruction consolidation, 2026-07-10

Slice 159 source-reviewed type-section installation and control reconstruction after slice 158 kept optional type-section pre-update caller-owned. `DaeTypeidxControlRewritePlan`, `DaeTypeidxControlRewriteApplication`, `dae_typeidx_control_rewrite_plan(...)`, and `dae_apply_typeidx_control_rewrite(...)` now own only copied control params/results/bodies, simple function-type reuse-or-append, and reconstruction of typeidx `block`, `loop`, or paired-arm `if` instructions. The application rejects unknown control kinds, missing `if` else arms, and zero-result plans.

Three honest production branches consume the shared application: parametric terminal multi-result typeidx block/loop/if refinement, zero-input terminal multi-result `if` refinement, and preserved multi-result loop-carrier refinement. Candidate discovery, GC LUB/refinement, consumed-param stack safety, terminal position, original-versus-rewritten arm choice, and final function/module code installation remain caller-owned. The old zero-param-only type helper is gone.

The red-first whitebox contract failed because the plan/application helpers were unbound, then passed for block/loop/if reconstruction, equivalent-type reuse, and invalid-shape rejection. Validation reached DAE whitebox `173/173`, pass-manager `189/189`, public DAE `302/302`, `moon test src/passes` `4443/4443`, `moon info` with 9 pre-existing warnings, full `moon test` `7882/7882`, and a native CLI build. Fresh no-reduction lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches; all three have zero failures and Binaryen cache `10000/0`. Direct residual profiles remain coverage `259`, unused params `214`, constants `197`, tail `63`, and convergence `60`; optimizing residuals remain coverage `259`, effectful args `136`, and localization `67`. This is typeidx application ownership, not raw parity or a scheduler change.

### Result cleanup-preservation decision consolidation, 2026-07-10

Slice 160 source-reviewed the unobserved-unreachable and zero-call result-side gates without manufacturing a universal zero-call fact bag. `DaeOriginalResultCleanupPreservationEvidence`, `dae_original_result_cleanup_preservation_evidence(...)`, and `dae_original_result_cleanup_preservation_any(...)` now project exactly two shared original-phase decisions from the existing boundary row: mixed-self suffix preservation and the result-side-chain cleanup summary. `DaeOriginalResultBoundaryEvidence` derives that projection once.

Two honest production decisions consume the shared projection: unobserved-function removal blocks when either shared preservation fact is present, and zero-call uncalled-result cleanup uses the same combined decision while its dead-suffix-only parameter cleanup reads the exact result-side-chain field. Unique prior-call, same-operand, escaped-undropped suffix, parameterized dropped-self-caller, current simplified-body, current self-call, and current module-call facts remain explicit. Plain scheduling is unchanged and no optimizing-only cleanup moved into the core.

The red-first whitebox contract failed because the carrier and helpers were unbound, then passed after implementation. Validation reached DAE whitebox `174/174`, pass-manager `189/189`, public DAE `302/302`, `moon test src/passes` `4444/4444`, `moon info` with 9 pre-existing warnings, full `moon test` `7883/7883`, and a native CLI build. Fresh no-reduction lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All three had zero validation/generator/property/command failures and Binaryen cache `10000/0`; only optimizing used `drop-consts` and `unreachable-control-debris`. Direct DAE remains explicitly raw-red.

### Callsite whole-argument rewrite application consolidation, 2026-07-10

Slice 161 source-reviewed the ordinary removed-operand repair and typed localization branches and found a real duplicate application: both copied the pre-argument prefix, replayed or replaced every recovered operand in order, re-emitted the call, copied the suffix, and returned the new call index. `DaeCallsiteRewritePlan`, `DaeCallsiteRewriteApplicationEvidence`, `dae_callsite_rewrite_plan(...)`, and `dae_apply_callsite_rewrite_plan(...)` now own that contiguous whole-argument reconstruction once.

Two honest production modes consume the shared application. Ordinary callsite repair uses `dae_callsite_operand_removal_plan(...)` to preserve hard operand effects with replay plus `drop`; typed localization derives scratch types for hard removed operands and emits ordered `local.set`s. Both modes reject escaping branches, preserve ambient-stack-derived slice boundaries and exact evaluation/call order, and continue through the same recursive direct-`call` / `return_call` traversal. Scratch-local allocation across callers, localized removed-parameter projection, current mutation state, retry/frontier scheduling, and caller touched accounting remain outside the plan.

The red-first whitebox contract failed because the plan/application helpers were unbound, then passed for both replay/drop and typed localization. Validation reached DAE whitebox `175/175`, pass-manager `189/189`, public DAE `302/302`, `moon test src/passes` `4445/4445`, `moon info` with 9 pre-existing warnings, full `moon test` `7884/7884`, and a native CLI build. Fresh no-reduction lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero validation/generator/property/command failures and Binaryen cache `10000/0`; only optimizing uses the documented cleanup normalizers. Direct DAE remains explicitly raw-red, and plain scheduling remains distinct from the optimizing suffix.

### Typed scratch-local reuse, 2026-07-10

Slice 162 resolves the caller-allocation question without widening the shared removal plan. `dae_localization_scratch_slot(...)` selects an existing DAE-owned scratch declaration by exact defaultable type or appends one when no exact match exists. `dae_apply_callsite_rewrite_plan(...)` now receives the caller's current scratch-type pool and returns the updated pool; the recursive callsite traversal installs that pool once per caller instead of appending one local per localized operand occurrence.

The red-first public plain-DAE fixture failed with two `i32` locals for sequential and nested localized calls, then passed with one local and two writes to the same slot. Whitebox coverage proves exact `i32` reuse, `f64` separation, nullable-reference allocation, and repeated nullable-reference reuse. Reuse never borrows pre-existing caller locals, never crosses callers, and never aliases different types. Localized values are consumed by `local.set` and are not read, so sequential and nested reuse does not introduce a live-range overlap; evaluation/effect/trap order, ambient stack values, call placement, callee removed-slot projection, recursive direct/`return_call` traversal, and touched accounting are unchanged.

Validation reached DAE whitebox `176/176`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4447/4447`, `moon info` with 9 pre-existing warnings, full `moon test` `7886/7886`, and a native CLI build. Fresh no-reduction lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero validation/generator/property/command failures and Binaryen cache `10000/0`; only optimizing uses the documented cleanup normalizers. Direct DAE remains explicitly raw-red, and plain scheduling remains distinct from the optimizing suffix.

### Core result-application consolidation, 2026-07-10

Slice 163 source-reviewed `DaePassResult` consumption across the fixed core, restored-operand pass, reverse exact-literal paths, low forwarded-constant and immutable-global revisits, low callee loops, and the boundary wrapper/callee lane. `DaeCoreResultApplication` plus `dae_apply_core_result(...)` now own only the repeated scheduler-state application: productive module installation, first-result touched adoption or later OR merge, aggregate changed-state update, an explicit `applied` decision, and primary-definition propagation.

These are honest production consumers with different producers and revisit modes. Their phase-local semantics remain outside the helper: no-change break versus continue behavior, selected-bit clearing, candidate order, current call-fact collection or reuse, iteration limits, trace labels, and frontier membership are unchanged. The module raw cleanup and final return-suffix cleanup deliberately stay outside because they retain the existing touched bitmap rather than consuming an ordinary returned touched set. Plain `dead-argument-elimination` / `dae` still runs the shared core without the optimizing-only nested cleanup suffix.

The red-first whitebox contract failed because `dae_apply_core_result(...)` was unbound, then passed for changed-module installation, touched adoption, touched OR merge, no-change preservation, aggregate changed preservation, and primary-definition propagation. Validation reached DAE whitebox `177/177`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4448/4448`, `moon info` with 9 pre-existing warnings, full `moon test` `7887/7887`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures, with Binaryen cache `10000/0`. Direct DAE remains explicitly raw-red.

### Selected singleton result-application consolidation, 2026-07-10

Slice 164 source-reviewed the selected one-shot result consumers immediately after the boundary wrapper/callee lane and found an exact lifecycle overlap: set one singleton selection, invoke a producer, clear the consumed bit for both productive and no-op outcomes, then apply ordinary module/touched/changed/primary state. `dae_apply_selected_core_result(...)` now owns only the clearing step plus delegation to `dae_apply_core_result(...)`.

Five honest production producers consume the helper in the first bounded cluster: Func236 local retargeting, Func256 loop-carrier retargeting, and Func237 parameter-loop spilling, default-carrier folding, and raw-debris stripping. Selection setting, producer invocation, candidate order, no-change control flow, trace labels, later revisit decisions, fact timing, and iteration limits remain caller-owned. The helper does not create a frontier or cache current evidence. Raw module cleanup and final return-suffix cleanup remain separate because their touched semantics still differ, and plain `dead-argument-elimination` / `dae` still omits the optimizing suffix.

The red-first whitebox contract failed because `dae_apply_selected_core_result(...)` was unbound, then passed while proving consumed-bit clearing for productive and no-op results plus ordinary module/touched/aggregate-changed/primary semantics. Validation reached DAE whitebox `178/178`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4449/4449`, `moon info` with 9 pre-existing warnings, full `moon test` `7888/7888`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures, Binaryen cache `10000/0`, and unchanged residual families. Direct DAE remains explicitly raw-red.

### Primary-selected result-application consolidation, 2026-07-10

Slice 165 source-reviewed the two bounded unread-parameter loops that consume multi-definition selection bitmaps. Both loops had the same productive lifecycle: apply an ordinary core result, stop on no-op, and clear exactly the productive result's `primary_def` before the next iteration. `dae_apply_primary_selected_core_result(...)` now delegates ordinary module/touched/aggregate-changed/primary state to `dae_apply_core_result(...)` and clears that primary bit only when application succeeds.

The low-callee and low-wrapper-callee loops are the two honest production consumers. Their fixed current call facts, candidate construction and order, `14`/`21` iteration limits, no-change break behavior, trace labels, and current-module timing remain explicit. This helper is deliberately distinct from `dae_apply_selected_core_result(...)`, which clears an already-consumed singleton bit even for a no-op result. Neither helper owns selection discovery, producer invocation, a generic frontier, raw/final cleanup touched policy, or the optimizing suffix.

The red-first whitebox contract failed because `dae_apply_primary_selected_core_result(...)` was unbound, then passed while proving productive primary-bit clearing, unrelated selection preservation, and no-op non-clearing plus ordinary result application. Validation reached DAE whitebox `179/179`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4450/4450`, `moon info` with 9 pre-existing warnings, full `moon test` `7889/7889`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures with Binaryen cache `10000/0`. Direct DAE remains explicitly raw-red.

### Selected function-body module-boundary consolidation, 2026-07-10

Slice 166 moves five Func237 early-shape producers off duplicated selected-function module scanners. `dae_try_apply_selected_def_body_rewrite_once(...)` now owns the exact common boundary: reject an invalid or unselected definition, read the current code body and current type section, invoke one caller-supplied body rewrite, reject absent or unchanged output, preserve the function's locals and every unrelated body, install the rewritten code section, strip stale names, mark exactly the selected definition touched, and return that definition as primary.

The outer-loop carrier, nested branch-result, zero-branch, leading-retain-call, and inner-iteration-loop producers are the honest production consumers. Their body recognizers remain separate and source-specific. Scheduler selection setting/clearing, ordinary result application, phase order, the terminal-br0 two-iteration loop, the late inner-loop revisit, the productive-only post-Func867 revisit, traces, and raw/final cleanup policy remain explicit. Each invocation reads the current module, so no body or type evidence crosses mutation phases. Plain `dead-argument-elimination` / `dae` still omits the optimizing suffix.

The red-first whitebox contract failed because `dae_try_apply_selected_def_body_rewrite_once(...)` was unbound, then passed while proving selected-only application, current type-section delivery, unrelated-body/local preservation, exact touched/primary metadata, stale-name removal, and unchanged-body rejection. Validation reached DAE whitebox `180/180`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4451/4451`, `moon info` with 9 pre-existing warnings, full `moon test` `7890/7890`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures with Binaryen cache `10000/0`. The existing source/diff/replay/size/performance classifications remain unchanged; direct DAE is still explicitly raw-red.

### Selected full-function module-boundary consolidation, 2026-07-10

Slice 167 extends the selected current-module boundary from body-only rewrites to caller-owned full `Func` rewrites. `dae_try_apply_selected_def_func_rewrite_once(...)` now owns selected-definition/current-code gating, current function plus type-section delivery, absent-or-equal rejection, unrelated-function preservation, code installation, stale-name removal, and exact touched/primary metadata. The body-only helper delegates through it, retaining local preservation as a narrower policy.

Five locals-and-body producers are direct honest consumers: Func287 setup-local compaction, Func288 switch-carrier compaction, Func299 inverted-result-`if` cleanup, Func236 local retargeting, and Func246 builder folding. The previously identified Func237 terminal-br0, dropped-result branch-carrier, and unreachable-after-`if` body producers now use the body boundary, bringing that narrower consumer set to eight. This removes 149 net source lines while leaving every recognizer, caller selection lifecycle, ordinary result application, terminal-br0 two-iteration loop, late/Func867-dependent revisit, trace, type/FuncSec mutation, multi-function rewrite, raw/final cleanup, and plain-vs-optimizing schedule explicit.

The red-first whitebox contract failed because `dae_try_apply_selected_def_func_rewrite_once(...)` was unbound, then passed while proving locals-and-body replacement at exactly one selected definition, current type-section delivery, unrelated-function preservation, equal-function rejection, stale-name removal, and exact result metadata. Validation reached DAE whitebox `181/181`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4452/4452`, `moon info` with 9 pre-existing warnings, full `moon test` `7891/7891`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures with Binaryen cache `10000/0`; residual profile counts are identical. Direct DAE remains explicitly raw-red.

### Selected wrapper boundary consolidation, 2026-07-10

Slice 168 routes three more exact wrappers through the current-module boundaries and fixes one real selection bug. `dae_try_flatten_selected_func288_return_wrapper_once(...)` now uses the full-function boundary so the wrapper's exact three-local gate remains caller-owned while invalid, out-of-range, and **unselected** definitions reject centrally. Before the change, an in-range Func288 return-wrapper shape was rewritten even when `selected_defs[288]` was false. Red-first whitebox coverage now locks the unselected no-op plus the productive selected rewrite, locals, unrelated functions, stale-name removal, touched mask, and primary definition.

Func237 raw-debris cleanup is the second new full-function consumer because it can trim locals after resolving the current function signature. Func237 parameter-loop spilling is the ninth body-boundary consumer because it preserves locals and changes only the body after its existing local-0 safety gate. The full-function boundary now has seven direct consumers and the body projection has nine. This slice removes 59 net source lines without changing recognizers, signature fallback, type evidence timing, singleton selection/result lifecycle, phase order, traces, revisits, raw/final cleanup, FuncSec/type-changing or multi-function producers, or the plain-vs-optimizing schedule.

Validation reached DAE whitebox `182/182`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4453/4453`, `moon info` with 9 pre-existing warnings, full `moon test` `7892/7892`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures with Binaryen cache `10000/0`; residual profile counts are identical. Direct DAE remains explicitly raw-red.

### Dynamic first-productive selected scan consolidation, 2026-07-10

Slice 169 adds `dae_try_apply_first_selected_func_rewrite_once(...)` above the fixed-definition application boundary. It scans selected definitions in ascending order, asks a wrapper-owned callback for a full current-function rewrite, rejects absent or equal output through `dae_try_apply_selected_def_func_rewrite_once(...)`, and stops after the first productive application. The shared path therefore owns selected scan order, current function/type-section delivery, first-productive stopping, unrelated-function preservation, code installation, stale-name removal, and exact touched/primary metadata without duplicating the fixed application contract.

Four existing production scanners now consume it: selected nop removal, Func408 pair-`if` carrier repair, selected i32-constant local-set inlining, and selected terminal-call local-set inlining. Their recursive/body recognizers, current callee and target signature resolution, minimum protected-local counts, trailing-local trimming, and caller scheduling remain wrapper-owned. Type indices and imported counts are recomputed for each wrapper invocation against its current module; no scan fact crosses a mutation phase. The refactor removes 26 net source lines.

The red-first whitebox contract failed because the dynamic helper was unbound, then passed while proving an earlier selected no-op, a later selected productive rewrite, ascending visitation `[0, 1]`, no visit after productivity, current type-section/function delivery, unrelated-function preservation, stale-name removal, and exact touched/primary metadata. Validation reached DAE whitebox `183/183`, pass-manager `189/189`, public DAE `303/303`, `moon test src/passes` `4454/4454`, `moon info` with 9 pre-existing warnings, full `moon test` `7893/7893`, and a native CLI build. Fresh reduction-disabled lanes remain unchanged: dedicated direct `5000/10000` normalized with `5000` mismatches, direct random-all `9207/10000` normalized with `793` mismatches, and normalized optimizing random-all `9538/10000` normalized with `462` mismatches. All have zero cleanup-normalized matches and zero validation/generator/property/command failures with Binaryen cache `10000/0`. Direct DAE remains explicitly raw-red and all prior classifications remain unchanged.

The next consolidation decision should fully source-review Func311 and Func313 trace-heavy partial/full products before sharing any application boundary. Func298's FuncSec timing, selected const-if multi-function application, Func323 caller-plus-callee/signature mutation, dropped-result rewrites, specialized touched policy, raw/final cleanup, and scheduler revisits remain separate unless exact multi-consumer overlap is proved. If Func311/313 do not share an exact product and trace contract, return to broader value/control evidence rather than forcing an abstraction. Do not cache current facts as original evidence or import optimizing-only cleanup into plain DAE.

### Selected typed full-function application consolidation, 2026-07-10

Slice 170 adds `dae_try_apply_selected_def_typed_func_rewrite_once(...)` as the current-signature projection over the fixed selected full-function boundary. The projection preserves selected/current-code gating, resolves the selected definition's signature from the current module only after the fixed boundary admits the callback, supplies the current `Func`, resolved `FuncType`, and current type section to a wrapper-owned rewrite, then delegates absent/equal rejection, unrelated-function preservation, code installation, stale-name removal, and exact touched/primary metadata to `dae_try_apply_selected_def_func_rewrite_once(...)`.

Func311 and Func313 terminal-call argument rewriting are the two honest production consumers. Func311 retains its exact four-`i32`/one-`i32` signature gate, constant folding, bounded swaps and local inlining, high-local trace, trimmed-locals partial product, and separate one-`i64`-local full product. Func313 retains its seven-`i32`/one-`i32` signature gate, shape/trace policy, bounded swaps, terminal-call local inlining, suffix-staging unwrapping, and trimmed-local product. Their two-iteration scheduler loops, trace labels, selection setting/clearing, and result application remain explicit. Func298 FuncSec timing, Func323 caller-plus-callee/signature mutation, selected multi-function const-`if` application, specialized touched policy, raw/final cleanup, and revisit scheduling remain outside the boundary.

The red-first whitebox contract failed because the typed helper was unbound, then passed while proving unselected callback suppression, current resolved signature and type-section delivery, locals-and-body replacement, unrelated-function preservation, stale-name removal, and exact touched/primary metadata. Post-rebase validation reached DAE whitebox `184/184`, pass-manager `216/216`, public DAE `303/303`, `moon test src/passes` `5192/5192`, `moon info` with 9 pre-existing warnings, full `moon test` `8644/8644`, and a native release CLI build. Fresh reduction-disabled lanes completed after the rebase: dedicated direct remains `5000/10000` normalized with `5000` mismatches and the same four residual families; the changed local-main random-all composition is `9136/10000` normalized with `864` mismatches across the same five direct residual families sampled in that composite; normalized optimizing random-all is `9633/10000` with `367` mismatches across sampled coverage and effectful-argument cleanup families. Every lane has zero cleanup-normalized matches and zero validation/generator/property/command failures. Direct DAE remains explicitly raw-red; prior family classifications remain evidence-backed but do not imply raw output parity.

The next consolidation should source-review Func298, Func323, selected multi-function const-`if`, dropped-result, specialized touched, and raw/final cleanup application scopes before choosing another boundary. Prefer a real two-consumer value/control or module-application overlap over widening the typed one-function helper into type-changing or multi-function mutation.

### Selected FuncSec/code application consolidation, 2026-07-12

Slice 171 adds `dae_try_apply_selected_def_funcsec_func_rewrite_once(...)` for the exact selected one-function application product shared by Func256 and Func298 loop-carrier compaction. The helper admits only a selected current definition with concrete `FuncSec`, `CodeSec`, and `TypeSec`, resolves its current `FuncType`, supplies the current `Func`, signature, and type section to a wrapper-owned rewrite, rejects absent or equal products, reuses the first existing function type with the same params/results, installs the one changed function plus FuncSec index, strips stale names, and returns exact touched/primary metadata. A function-equal callback remains a full no-op even if an earlier equivalent type exists, preserving the previous type-index timing.

Func256 and Func298 retain their exact two-`i32`/void gate, local availability checks, i32 param-loop spill, local remap, mapped-loop-condition rewrite, void-loop wrapping, dropped-pure/nop cleanup, high-local rejection, and trailing-local trimming. Func298 additionally retains zero-before-return cleanup and every trace gate. The shared helper does not append or mutate `TypeSec`, change multiple functions, widen touched scope, or own scheduler selection/result lifecycle. Func323 and Func3737 caller-plus-callee/type-changing products therefore remain outside.

The red-first whitebox contract failed while the helper was unbound, then passed while proving unselected callback suppression, current resolved signature and type-section delivery, first-equivalent FuncSec reuse, locals/body replacement, unrelated-function preservation, stale-name removal, equal-product rejection, and exact touched/primary metadata. Existing public Func256/Func298 tests passed `2/2`; full validation reached DAE whitebox `185/185`, public DAE `303/303`, pass suites `5193/5193`, `moon info` with 9 pre-existing warnings, full tests `8645/8645`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, current-composition direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero validation/generator/property/command failures. Residual family counts are unchanged, and direct DAE remains explicitly raw-red.

The next consolidation should source-review Func323 and Func3737 together by caller/callee mutation, current call facts, constant-param substitution, caller suffix repair, `TypeSec`/`FuncSec` update timing, touched widening, traces, and primary semantics. Extract only an exact two-consumer type-changing application contract. Otherwise partition selected multi-function const-`if`, dropped-result/specialized touched paths, raw/final cleanup, and revisits, or return to broader value/control evidence.

### Bounded singleton-selected scheduling consolidation, 2026-07-12

Slice 175 source-reviewed terminal-br0, all four Func237 inner-loop placements, Func867 default-argument specialization, and the bounded Func267/268 const-`if` loops by producer signature, selection lifetime, touched merge, no-op behavior, trace timing, iteration policy, and dependency gates. The one-shot sites now use `dae_run_singleton_selected_core_rewrite(...)`; Func867 still gates its follow-on Func237 revisit on a productive specialization, and the unconditional late Func237 revisit remains in its original phase.

`dae_run_bounded_singleton_selected_core_rewrite(...)` adds only the exact loop lifecycle shared by the two-iteration terminal-br0 lane and the separate eight-iteration Func267/268 const-`if` lanes. It threads the current module, touched accumulator, and aggregate changed state through the existing singleton lifecycle, breaks on the first no-op, and invokes a caller-owned callback immediately after each productive application so trace labels and iteration numbers retain their original timing. Bounds, phase placement, producer choice, and follow-on dependencies remain caller-owned.

Red-first whitebox `dae bounded singleton-selected core rewrite stops after first no-op` failed while the helper was unbound, then passed for two productive module handoffs followed by a no-op, exact touched accumulation, selection restoration, and productive callback order. Full validation reached DAE whitebox `189/189`, public DAE `303/303`, pass suites `5197/5197`, `moon info` with 9 pre-existing warnings, full tests `8649/8649`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero validation/generator/property/command failures; residual family counts and prior classifications are unchanged. Direct DAE remains explicitly raw-red, and plain `dead-argument-elimination` / `dae` receives none of this optimizing-only schedule.

Remaining direct work should return to broader reusable value/control evidence or a source-backed residual closure slice. Do not route fixed-current-fact selected fallbacks, dynamic selected scans, specialized touched policies, or type-changing multi-function products through singleton machinery unless their evidence lifetime and mutation scope are exact.

### Cleanup-result application consolidation, 2026-07-12

Slice 174 source-reviewed the optimizing-only module raw cleanup and final return-suffix cleanup by scanner scope, evidence lifetime, returned touched policy, scheduler placement, and follow-on dependencies. Their scanning is not identical: raw cleanup traverses current code with the scheduler touched bitmap controlling live-local-copy cleanup and requests `return_touched=false`, while final return-suffix cleanup performs its own wrapper/terminal-return scan after all post-raw selected lanes. Those policies remain separate.

`dae_apply_cleanup_result_preserving_touched(...)` now owns only their exact shared scheduler-state application. It converts either cleanup result into ordinary core-result application without cleanup-returned touched metadata, so productive results install the returned module and primary definition while retaining the caller-owned touched bitmap, and no-op results preserve current module, touched state, and aggregate changed state. Both production consumers assign the returned application state and retain their own trace labels and placement.

The red-first whitebox contract failed while the helper was unbound, then passed for productive and no-op results whose returned touched arrays deliberately differ from scheduler state. Focused plain-DAE scheduling coverage passed `4/4`, confirming no optimizing cleanup was imported. Full validation reached DAE whitebox `188/188`, public DAE `303/303`, pass suites `5196/5196`, `moon info` with 9 pre-existing warnings, full tests `8648/8648`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero validation/generator/property/command failures. Dedicated residuals remain constant arguments `1875`, unused parameters `1875`, convergence `625`, and tail boundary `625`; direct random-all remains coverage `243`, constant arguments `244`, unused parameters `126`, convergence `133`, and tail `118`; optimizing remains coverage `243` and effectful arguments `124`. Existing evidence classifications are unchanged, and direct DAE remains explicitly raw-red.

Terminal-br0 iteration, the late Func237 revisit, and the Func867-productive conditional revisit still have distinct placement and dependency semantics even though each uses singleton selected execution. The next slice should route them through existing singleton lifecycle machinery only where producer invocation and result application are exact, without hiding the two-iteration break, unconditional late placement, or productive-Func867 gate. Otherwise return to broader value/control evidence. Plain `dead-argument-elimination` / `dae` receives none of the optimizing cleanup or revisit schedule.

### Singleton-selected const-`if` lifecycle consolidation, 2026-07-12

Slice 173 source-reviewed `dae_try_simplify_selected_const_if_defs_once(...)` and all four scheduler call families. The producer can rewrite every selected function in one module copy, but current production scheduling always activates exactly one definition: Func233 once, Func267/268 in separate bounded loops, Func327 once before wrapper cleanup, and Func1794 once before terminal-call/default propagation. No honest multi-selection scheduler consumer exists, so the producer's broader bitmap capability is not promoted into a new multi-function application abstraction.

`dae_run_singleton_selected_core_rewrite(...)` now owns the exact repeated lifecycle: mark one definition selected for producer execution, invoke against the current module, clear that singleton for both productive and no-op results, then delegate module installation, touched adoption/merge, aggregate changed state, applied/no-op distinction, and primary propagation through `dae_apply_selected_core_result(...)`. Phase placement, Func267/268's eight-iteration bound, break behavior, trace labels, and Func327/Func1794 follow-on dependencies remain caller-owned.

The red-first whitebox contract failed while the helper was unbound, then passed while proving that the selection is active during the callback, unrelated selections survive, productive and no-op callbacks both clear the consumed singleton, and result application preserves touched/changed/primary semantics. Focused public const-`if` coverage passed `2/2`, the Func260 helper-literal product passed `1/1`, and full validation reached DAE whitebox `187/187`, public DAE `303/303`, pass suites `5195/5195`, `moon info` with 9 pre-existing warnings, full tests `8647/8647`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero validation/generator/property/command failures. Direct DAE remains explicitly raw-red.

Selected/full dropped-result source review found no new exact application boundary beyond the existing shared `DaeDroppedResultModuleContext`, `DaeDroppedResultRepairPlan`, unreachable-callee application, and module code finalization. The fact-driven loop refreshes current call facts per iteration and stops on first no-op; the selected fallback retains a fixed current-fact snapshot plus candidate trace; full and selected scopes choose different repair kinds/order and touched policies. Merging those lifecycles would hide evidence timing and scheduler semantics rather than remove a scanner. Raw cleanup, final cleanup, terminal-br0/late-Func237/productive-Func867 revisits, and broader value/control evidence remain the next partitioning targets. Plain `dead-argument-elimination` / `dae` receives none of these optimizing-only selected const-`if` schedules.

### Removed-parameter callee application consolidation, 2026-07-12

Slice 172 partitions Func323 before extraction: its seven-parameter branch remains caller-only cleanup, its eight-parameter branch still delegates to the generic candidate path, and only its nine-parameter branch performs the selected caller-plus-callee/type-changing product. Func3737 has the same final removed-parameter callee projection and changed-function module finalization, but different caller verification, suffix repair, constants, body cleanup, traces, and scheduler lifecycle.

`DaeRemovedParamCalleeRewriteEvidence` and `dae_removed_param_callee_rewrite_evidence(...)` now centralize the exact common callee product for three honest consumers: generic `dae_try_rewrite_candidate(...)`, selected Func323 nine-parameter specialization, and selected Func3737 wrapper specialization. The evidence owns kept-parameter projection, written/localized removed-parameter body-local materialization, constant substitution before local remapping, old-local remapping, and final local-list construction. Each caller still owns candidate legality, callsite repair, localized-param discovery, body cleanup, and trace decisions. All three consumers now finish through `dae_module_signature_context(...)` plus `dae_apply_module_code_finalization(...)` with `DaeTouchModuleCodeChangedFuncs`, so `TypeSec`, `FuncSec`, code installation, stale-name removal, touched widening, and primary metadata share one current-module boundary.

The red-first whitebox contract failed while `dae_removed_param_callee_rewrite_evidence(...)` was unbound, then passed while proving kept signature projection, a written removed-parameter local, constant substitution, and old-local remapping. Public Func323 and Func3737 coverage passed `1/1` and `2/2`; full validation reached DAE whitebox `186/186`, public DAE `303/303`, pass suites `5194/5194`, `moon info` with 9 pre-existing warnings, full tests `8646/8646`, and native build. Fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, with zero cleanup-normalized matches and zero validation/generator/property/command failures. Residual counts and classifications are unchanged; direct DAE remains explicitly raw-red.

Plain `dead-argument-elimination` / `dae` continues to use the generic candidate core without any optimizing suffix scheduling or cleanup. The next consolidation should source-review selected multi-function const-`if` application, dropped-result/specialized touched application, raw/final cleanup, and revisit scheduling by exact evidence lifetime and mutation scope; otherwise return to a broader value/control evidence overlap. Do not merge Func323/Func3737 caller recognizers or traces merely because their callee finalization now shares the core.

### First consolidation implemented, 2026-07-07

The first code consolidation added `DaeGcSelectValueEvidence` plus the helper now named `dae_gc_select_value_slices_evidence(...)` in [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt). The helper centralizes typed-`select` GC value-slice evidence: it consumes recovered select slices, asks the existing returned-value candidate API for both value operands, combines them through `dae_lub_gc_candidate_ref_type(...)`, records optional `dae_refined_gc_candidate_ref_type(...)`, and carries operand rewrite hooks for block/local/call evidence repair.

Two scanner surfaces now call the shared evidence helper instead of open-coding the same candidate/LUB/refinement chain:

- `dae_direct_gc_result_select_candidate_body(...)` uses it for single-result typed-select returned-value refinement and result-select repair.
- `dae_try_refine_multi_result_select_operand_body(...)` uses it for both candidate detection and rewrite of flat multi-result typed-select operands.

A follow-up code consolidation added the helper now named `dae_gc_select_actual_value_slices_evidence(...)`, sharing the same `DaeGcSelectValueEvidence` carrier for argument-side typed-`select` actuals. It centralizes nullable `ref.null` operand handling, direct actual candidate extraction, LUB/refinement, stack-local condition checks, and rewrite hooks for retargeting `ref.null` plus the typed `select`. Candidate collection and effect-preserving callsite repair now use that helper instead of separate shape checks.

A third code consolidation added `DaeIndirectGcResultSliceEvidence` plus `dae_indirect_call_gc_result_slice_evidence(...)`. The helper centralizes shared `call_ref` / `call_indirect` returned-value operand evidence: function-signature candidate extraction, parameter-count checks, stack-local argument/table operand validation, side-effect/branch rejection, and the refined GC result candidate returned to `dae_gc_result_candidate_slice(...)`. The two former duplicate scanner surfaces, `dae_call_ref_gc_result_candidate_slice(...)` and `dae_call_indirect_gc_result_candidate_slice(...)`, now only verify their call opcode/target shape and delegate operand evidence to the shared helper.

A fourth behavior-preserving consolidation added `DaeDirectGcCallResultEvidence`, `dae_direct_call_gc_result_signature_evidence(...)`, and `dae_direct_call_gc_result_refinement_evidence(...)`. Direct-call GC result signature extraction is now shared between `dae_direct_gc_actual_candidate_type(...)` (argument-side call result candidates) and the `@lib.Call` arm of `dae_direct_gc_result_candidate_instr(...)` (returned-value refinement), so direct calls no longer open-code separate function-signature and old-result refinement checks. This is a small step toward a common direct/indirect call evidence layer; it deliberately stops short of scheduler or cleanup policy changes.

A fifth behavior-preserving consolidation added `DaeGcTypeEvidence` and `dae_gc_result_slot_evidence(...)`. This helper wraps a single result-producing instruction's GC candidate, optional old-result refinement, and optional rewrite hook. It replaced separate candidate/refinement/rewrite open-coding in `dae_try_refine_multi_result_single_if_operand_body(...)`, the parametric typeidx arm result path, and the flat multi-result carrier tail in `dae_try_refine_direct_gc_types(...)`. The parametric typeidx/control recognizer is still shape-specific, but its flat result slots now share the same evidence carrier as the non-parametric multi-result recognizers.

A sixth behavior-preserving consolidation added `DaeValueSlice`, `DaeSelectValueSlices`, and `dae_find_select_value_slices(...)`. This is the first step beyond slot-only GC evidence: it centralizes typed-select left/right value-slice recovery, stack-local condition bounds, optional left-start and droppable-prefix constraints, and effect/branch disposition for the recovered operands. `dae_direct_gc_result_select_candidate_body(...)` and `dae_try_refine_multi_result_select_operand_body(...)` now delegate select operand/condition bound recovery to that helper instead of carrying separate backward `cond_start` scans and bounded left/right length scanners; after the one hundred thirty-eighth consolidation, GC candidate/LUB/refinement flows through `dae_gc_select_value_slices_evidence(...)` with the recovered carrier.

A seventh behavior-preserving consolidation added `DaeControlResultSlice` and `dae_find_multi_result_control_slice(...)`. The helper centralizes multi-result arm/control result-span recovery for flat arms, flattenable block carriers, terminal-return arms, and block-plus-`return` carriers, including result start/end, result lane count, carrier kind, flattening state, terminal-return state, and effect/branch disposition via `DaeValueSlice`. The old `dae_multi_result_if_arm_candidate_body(...)` and `dae_multi_result_if_arm_return_candidate_body(...)` scanners now delegate to this shared carrier instead of maintaining separate flat/block/return unwrapping loops. This slice deliberately does not add scheduler behavior, optimizing cleanup, or new compare classification; GC candidate/refinement for recovered slots still flows through the existing slot/control evidence helpers.

An eighth behavior-preserving consolidation added `DaeControlResultGcEvidence` and `dae_control_result_slice_gc_evidence(...)`. The helper combines a recovered `DaeControlResultSlice` with `dae_gc_result_slot_evidence(...)` to collect per-lane candidates, optional old-type refinements, rewrite hooks, and body-change state in one reusable carrier. The parametric typeidx arm result path now builds a control slice and delegates the per-lane GC evidence loop, and the flat/block/loop multi-result carrier tail in `dae_try_refine_direct_gc_types(...)` now uses the same helper for require-refined lanes instead of open-coding another slot scan. This still does not change scheduler behavior or import optimizing cleanup into plain DAE; it just removes another pair of duplicate result-lane loops.

A ninth behavior-preserving consolidation added `DaeControlResultPairGcEvidence` and `dae_control_result_pair_gc_evidence(...)`. The helper consumes paired `DaeControlResultGcEvidence` carriers, computes per-lane then/else GC LUBs, applies old-result refinements, and selectively applies only the rewrite hooks for lanes whose paired LUB refined the signature. The two direct terminal-`if` paths in `dae_try_refine_direct_gc_types(...)` now recover then/else `DaeControlResultSlice` carriers, collect per-arm GC evidence, and delegate paired LUB/refinement/rewrite handling to the shared helper instead of open-coding duplicate then/else candidate loops. This slice removed the now-unused non-return arm wrapper and kept the terminal-return wrapper only where the mixed return/fallthrough recognizer still needs it; it does not change scheduler behavior, compare classification, or the plain-vs-optimizing split.

A tenth behavior-preserving consolidation added `dae_parametric_typeidx_multi_result_arm_gc_evidence(...)`. The helper wraps the parametric typeidx arm prefix-consumption check, recovered result `DaeControlResultSlice`, and per-lane `DaeControlResultGcEvidence` into one arm carrier. `dae_try_refine_parametric_typeidx_multi_result_terminal_control_body(...)` now uses that carrier for block/loop/single-arm refinement and delegates paired `if` then/else LUB/refinement/rewrite handling through `dae_control_result_pair_gc_evidence(...)` instead of keeping a separate parametric then/else candidate loop. This intentionally leaves scheduling unchanged and does not import optimizing cleanup into plain DAE.

An eleventh behavior-preserving consolidation added the original combined `DaeGcActualShapeEvidence` / `dae_gc_actual_shape_evidence(...)` carrier for argument-side direct, final control, and typed-select actuals. The one hundred forty-fourth consolidation supersedes that combined carrier with separate shape-discovery and current-target type-evidence phases, described below. This still does not add localization/retry, scheduler behavior, or optimizing cleanup to plain DAE.

A twelfth behavior-preserving consolidation added `DaeOperandDisposition`, `dae_operand_disposition_from_range(...)`, and `dae_operand_disposition_from_call_shape(...)` with an initial disposition-based append wrapper that the thirty-eighth slice later superseded with `DaeOperandRemovalPlan`. The helper centralizes droppable/effect/branch range classification for trivial-droppable checks and callsite repair while preserving existing callsite semantics by letting `dae_rewrite_callsite_at(...)` reuse call-shape disposition to either omit removed pure operands or replay effectful removed operands followed by `drop`. This is the first shared repair-disposition carrier; it does not add localization/retry, does not reconstruct non-final control operands, and does not change the plain-vs-optimizing scheduler split.

A thirteenth behavior-preserving consolidation threaded that repair-disposition carrier into `DaeValueSlice`. `DaeValueSlice` now stores a `DaeOperandDisposition`, and `dae_value_slice_from_operand_disposition(...)` / `dae_value_slice_from_operand_disposition_with_purity(...)` build value slices for the select and control-slice surfaces while preserving the previous distinction between effect-free range purity and trivially droppable call-shape purity. This removes the now-redundant branch/effect range wrapper functions and gives typed-select and multi-result control carriers direct access to the same effect-preserving repair facts that callsite removed-operand repair uses. It still does not add localization/retry, arbitrary repair edits, scheduler behavior, or optimizing cleanup to plain DAE.

A one hundred fourth behavior-preserving consolidation added `DaeRecursiveBodyRewriteApplicationEvidence` and `dae_apply_first_recursive_body_rewrite(...)`. The helper records whether a recursive body repair applied at the current body or through the first eligible block/loop/`if`/`try_table` child, with target/rewrite/body-changed evidence layered over the existing indexed sparse edit helper. The prior `dae_apply_first_current_or_control_child_body_rewrite(...)` compatibility wrapper now delegates through that evidence carrier, and two additional recursive repair paths consume the same application planner: Func237 inner-iteration loop wrapping and i32-param-loop spill repair. This is repair-application consolidation only; it preserves root-before-child order, first-child traversal order, plain-vs-optimizing scheduling, current Func237/loop-spill shape policies, and direct DAE raw-red status.

A one hundred fifth behavior-preserving consolidation added `DaeBodyRewriteFixpointEvidence` and `dae_apply_body_rewrite_until_stable(...)`. The helper records max-iteration, iteration-count, limit, and body-change evidence for bounded body-rewrite convergence loops. Func237 inner-iteration loop wrapping and selected zero-tee/set splitting now consume the same fixpoint helper instead of maintaining separate bounded loops. This preserves existing loop bounds, cleanup scheduling, plain-vs-optimizing scheduling, and direct DAE raw-red status.

A one hundred sixth behavior-preserving consolidation added `dae_apply_first_recursive_instruction_body_rewrite(...)` and split `dae_apply_first_recursive_control_child_body_rewrite(...)` out as a projection over it. The helper centralizes first-matching recursive instruction rewrite evidence for control-child recursion and two Func237 recursive cleanup scans: leading retain-call hoisting and terminal `br 0` block flattening. This removes more duplicate scan/copy loops while preserving the prior instruction-level traversal semantics, plain-vs-optimizing scheduling, and direct DAE raw-red status.

A one hundred seventh behavior-preserving consolidation added `dae_apply_first_recursive_instruction_body_rewrite_to_body(...)` as the evidence-backed array-return projection over recursive instruction rewrite application. Func237 nested branch-result flattening and zero-branch flattening now consume the same recursive instruction helper as leading-retain hoisting and terminal `br 0` block flattening instead of keeping separate first-match scan/copy loops or ad hoc evidence unpacking. This preserves the existing instruction traversal order, Func237 shape policies, cleanup scheduling, plain-vs-optimizing scheduling, and direct DAE raw-red status.

A one hundred eighth behavior-preserving consolidation widened `DaeStackSliceLocalizationEvidence` with copied no-escape slice evidence and added `dae_stack_slice_localization_policy_accepts(...)` as the shared boolean projection over policy-backed localization evidence. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption now share the same policy-acceptance helper and carry copied accepted slices alongside transition, branch, disposition, and removal-plan facts instead of each open-coding evidence-to-boolean unpacking. This is stack-slice localization/effect-preservation consolidation only; it does not add true localize-then-retry, arbitrary repair edits, non-final typeidx/control reconstruction, scheduler changes, optimizing cleanup to plain DAE, or any direct-green claim.

A one hundred ninth behavior-preserving consolidation split type-section-backed control block signature recovery out as `dae_control_block_type_signature_evidence_from_type_sec(...)` and added `dae_control_block_type_stack_shape_evidence(...)`, `dae_control_block_type_stack_shape_accepts(...)`, and `dae_control_block_type_exact_signature_accepts(...)`. The module-backed control-entry stack path, zero-param single-result/zero-result raw cleanup predicates, and the single-`i32`-param zero-result Func237 cleanup gate now share the same block-type signature carrier instead of each resolving typeidx function signatures locally. This is control-type evidence and cleanup-policy consolidation only; it preserves cleanup scheduling, the plain-vs-optimizing split, direct DAE raw-red status, and does not add non-final control/typeidx operand reconstruction.

A one hundred eleventh behavior-preserving consolidation added `dae_control_block_type_zero_input_evidence(...)` as the shared zero-input projection over the type-section-backed control block signature carrier. Single-block result-carrier recovery and direct GC explicit-return block/loop/`if` candidate checks now consume this helper instead of independently checking `input_count == 0`, matching `VoidBlockType`/`ValTypeBlockType`, or unpacking `Some((0, _))` stack-effect tuples locally. This keeps cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries unchanged.

A one hundred twelfth behavior-preserving consolidation added `dae_control_block_type_zero_input_output_count_evidence(...)` as the shared zero-input plus exact-output-count projection over the same type-section-backed control block signature carrier. Single-block result-carrier recovery, mixed single- and multi-result if/fallthrough GC candidates, terminal-return block carriers, and multi-result `if`/block/loop carrier gates in direct GC type refinement now consume this helper instead of unpacking `Some((0, 0))` or `Some((0, outputs)) if outputs == results.length()` stack-effect tuples locally. This is control-type/value-slice evidence consolidation only; it preserves cleanup scheduling, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A fourteenth behavior-preserving consolidation added `dae_collect_callsite_value_slices(...)`, `dae_call_shape_from_value_slice(...)`, the then-combined `dae_gc_actual_value_slice_evidence(...)`, `dae_rewrite_refined_gc_actual_slice(...)`, and `dae_direct_gc_actual_candidate_type_for_value_slice(...)`; the one hundred forty-fourth slice later replaced the combined actual-evidence entrypoint with `dae_gc_actual_value_slice_shape_evidence(...)` plus current-target evaluation. Callsite shape collection now builds reusable `DaeValueSlice` carriers once. `dae_rewrite_callsite_at(...)` reuses `slice.disposition` directly for keep/remove repair, `dae_rewrite_refined_gc_actuals_instrs(...)` rewrites refined actuals through value slices, and `dae_collect_direct_struct_default_actuals_instrs(...)` collects direct GC actual candidates through the same value-slice evidence layer. This removes another round of shape-to-disposition recomputation and starts moving callsite-boundary loops toward one stack-slice carrier, while keeping scheduler behavior, localization/retry, and optimizing cleanup unchanged.

A fifteenth behavior-preserving consolidation added `dae_value_slice_single_instruction_index(...)` and moved the remaining callsite-wide shape-wrapper users to `DaeValueSlice`. The uniform-constant actual collector and the selected Func313/Func323 global-suffix repair path now ask the value-slice carrier for reusable single-instruction actual evidence instead of collecting legacy `DaeCallShape` tuples and open-coding `end == start + 1` checks. The old private `dae_collect_callsite_shapes(...)` compatibility wrapper is gone, leaving callsite argument recovery on the shared value-slice/disposition carrier. This still does not add localization/retry, arbitrary repair edits, scheduler behavior, or optimizing cleanup to plain DAE.

A sixteenth behavior-preserving consolidation added `DaeCallBoundaryFacts`, `dae_make_call_boundary_facts(...)`, `dae_call_boundary_allows_forwarding(...)`, and `dae_call_boundary_direct_callers(...)`. Recursive uniform-constant forwarding now passes one boundary carrier through `dae_collect_uniform_const_actuals_with_facts(...)`, `dae_collect_uniform_const_actuals_visited(...)`, `dae_collect_uniform_const_actuals_instrs(...)`, `dae_resolve_uniform_const_candidate(...)`, and `dae_resolve_forwarded_uniform_const_local_get(...)` instead of threading separate escape, tail-call, and direct-caller arrays through each loop. This starts the ownership/call-boundary bucket without changing legality, convergence scheduling, localization/retry, or the plain-vs-optimizing split.

A seventeenth behavior-preserving consolidation added `dae_record_uniform_const_candidate(...)`. Recursive uniform-constant actual collection now uses the helper for both direct materializable constants and local-set-carried constants, so the `seen` / `invalid` / previous-candidate merge policy lives in one reusable candidate recorder instead of two open-coded loops. This keeps uniform-constant forwarding legality and scheduling unchanged, and it does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.

An eighteenth behavior-preserving consolidation widened `DaeCallBoundaryFacts` with direct-call and dropped-call counts, added `dae_call_boundary_facts_from_current(...)`, and added `DaeCallBoundaryCandidate` / `dae_call_boundary_candidate(...)` as the first owned-callee boundary candidate carrier. The main DAE core loop and the reverse exact-literal loop now both consume that carrier for owned-callee gating plus current direct-call counts, dropped-call counts, and direct-caller lists instead of immediately re-indexing separate call-fact arrays for those decisions. This deliberately stays short of full candidate legality, result-removal legality, imported/defined summaries, convergence scheduling, or optimizing cleanup.

A nineteenth behavior-preserving consolidation widened `DaeCallBoundaryCandidate` with self-direct-caller and nonself-direct-rewrite eligibility facts. The selected unread-parameter rewrite lane and selected exact-literal rewrite lane now consume the boundary candidate for owned-callee, positive direct-call, no-self-recursion gating and for the direct-call count/caller evidence passed into `dae_try_rewrite_candidate(...)`, instead of each open-coding escape/tail/count/self-caller checks over separate call-fact arrays. This still does not own imported/defined summaries, dropped-result legality, result-removal policy, convergence scheduling, localization/retry, or optimizing cleanup.

A twentieth behavior-preserving consolidation added `dae_collect_uniform_const_actuals_from_boundary_facts(...)` as the public-internal entrypoint for uniform-constant actual collection over `DaeCallBoundaryFacts`. The main core uniform-constant lanes, reverse exact-literal lane, selected exact-literal lane, and selected forwarded-wrapper lane now pass one boundary carrier into uniform-constant collection instead of rebuilding or rethreading separate escape/tail/direct-caller arrays at each entrypoint. The recursive local-get forwarding collector was already carrier-based; this slice closes the remaining entrypoint gap without changing forwarding legality, localization/retry, convergence scheduling, or the plain-vs-optimizing split.

A twenty-first behavior-preserving consolidation added `DaeModuleBoundarySnapshot` plus `dae_collect_module_boundary_snapshot(...)` as the first imported/defined/current-call/type-index boundary summary. `dae_run_core_once(...)`, the reverse exact-literal core, selected Func323 global-suffix repair, selected Func867 default-argument specialization, the selected single-`i32` suffix default-argument specialization, and selected Func3737 wrapper-suffix removal now collect module counts, type indices, current call facts, and `DaeCallBoundaryFacts` through one snapshot instead of rebuilding those adjacent boundary facts locally. This still does not own original-vs-current summaries, dropped-result legality, localization/retry, convergence scheduling, or optimizing cleanup.

A twenty-second behavior-preserving consolidation added `DaeOriginalBoundarySnapshot` plus `dae_collect_original_boundary_snapshot(...)` as the first original-boundary setup summary. `dae_run_core(...)` now collects original imported/defined/total counts, original escapes, original param counts, and the original call summary through one carrier, and `dae_collect_original_call_summary_with_context(...)` reuses the already-collected boundary context instead of rebuilding imported/defined/escape facts internally. This still does not own dropped-result legality, result-removal policy, broader original dead-suffix summaries, localization/retry, convergence scheduling, or optimizing cleanup.

A twenty-third behavior-preserving consolidation added `dae_rewrite_dropped_result_calls_with_policy_instrs(...)` as the shared dropped-result call/drop repair traversal. The older top-level dropped-result rewrite and anywhere dropped-result cleanup wrappers now pass an explicit unreachable-root stop policy into one recursive implementation instead of maintaining two near-identical scanners over blocks, loops, `try_table`, and `if` arms. This centralizes result-repair traversal shape and makes the dead-suffix policy visible, but it does not change dropped-result legality, result-removal candidate selection, dead-suffix repair, localization/retry, convergence scheduling, or optimizing cleanup.

A twenty-fourth behavior-preserving consolidation added `DaeDroppedResultScope`, `dae_dropped_result_scope_from_optional(...)`, and scope-consuming helpers for dropped-result undropped-call checks, call/drop rewriting, dropped-call cleanup, and no-param dead-suffix repair. The main dropped-result removal path now builds one selected-vs-all-caller scope and passes it through those legality/repair phases instead of repeatedly matching `caller_defs` and open-coding caller loops at each phase. This centralizes repair scope selection for the dropped-result subflow while preserving existing invalid-caller skipping and all-vs-selected behavior; it still does not change dropped-result candidate legality, result-removal policy, non-final operand reconstruction, localization/retry, convergence scheduling, or optimizing cleanup.

A twenty-fifth behavior-preserving consolidation added `dae_repair_dead_suffix_calls_in_scope_except(...)` and moved the remaining full-module dropped-result cleanup callers onto `DaeDroppedResultScope`. Unobserved-unreachable and no-param dropped-result cleanup paths now build one full scope and use the same scoped undropped-call legality, dead-suffix repair, no-param dead-suffix repair, and call/drop cleanup helpers as the selected dropped-result path instead of keeping separate whole-module helpers and manual loops. This removes another duplicated all-functions repair policy while preserving the existing skip-def, invalid-caller, no-param-only repair, result-removal, localization/retry, convergence, and plain-vs-optimizing scheduling behavior.

A twenty-sixth behavior-preserving consolidation added `DaeOriginalResultPreservationEvidence` and `dae_original_result_preservation_any(...)` (now built through `dae_original_result_preservation_evidence_from_row(...)`). The main core zero-param/result-removal lane, the main core current-param lane, and the reverse exact-literal lane now share one original-vs-current result-preservation carrier for the common same-self-result, undropped-self-caller, escaped-result-operand-chain, mixed self-result, escaped-block self-result, param-operand side-callee, no-param dropped-caller, dropped escaped-result, and parameterized self-side-chain guards instead of open-coding the same indexed summary predicates in each loop. This deliberately does not change dropped-result candidate legality, result-removal policy, localization/retry, convergence scheduling, or optimizing cleanup.

A twenty-seventh behavior-preserving consolidation added `DaeDroppedResultCandidate` and `dae_dropped_result_candidate_from_current_call_facts(...)`. The fact-selected dropped-result candidate collector and the per-def dropped-result removal attempt path now share one carrier for current direct-call count, dropped-call count, direct-caller list, escape/tail owned status, all-direct-calls-dropped status, and the owned dropped-result candidate predicate instead of open-coding adjacent `DaeCurrentCallFacts` indexing. This is intentionally only a candidate-fact consolidation: `dae_try_remove_dropped_results(...)` still owns result-removal policy, selected/full repair scope, dead-suffix repair, type signature update, localization/retry behavior, convergence scheduling, and the plain-vs-optimizing split.

A twenty-eighth behavior-preserving consolidation added `dae_dropped_result_candidate_from_boundary_facts(...)` and made the current-call helper delegate through `DaeCallBoundaryFacts` / `DaeCallBoundaryCandidate`. The fact-selected dropped-result collector now builds one boundary carrier and dropped-result candidate eligibility reuses the same owned-callee, direct-call count, dropped-call count, and direct-caller evidence as other boundary candidate loops instead of maintaining a parallel current-call indexing path. This remains a candidate-evidence refactor only: result-removal legality, selected/full repair scope, dead-suffix repair, type signature update, localization/retry behavior, convergence scheduling, and plain-vs-optimizing scheduling are unchanged.

A twenty-ninth behavior-preserving consolidation added `dae_original_result_preservation_blocks_argument_side_result_repair(...)`. The helper consumes `DaeOriginalResultPreservationEvidence` and centralizes the shared argument-side result-repair / dropped-result blocking predicate used by the main current-param lane and the reverse exact-literal lane, while preserving the existing difference where the main lane treats the parameterized self-side-chain guard as blocking and the reverse exact-literal lane does not. This reduces another pair of original-vs-current summary guard lists without changing dropped-result legality, localization/retry, convergence scheduling, or the plain-vs-optimizing split.

A thirtieth behavior-preserving consolidation added `DaeDroppedResultRemovalLegalityEvidence`, `dae_dropped_result_removal_legality_evidence(...)`, and `dae_dropped_result_removal_legality_blocks(...)`. The selected dropped-result removal path and the full-module unobserved-unreachable / dead-self-suffix / uncalled-result cleanup paths now consume one carrier for result-count-aware large-result cleanup skips, live tail-call blockers, and scoped undropped direct-call blockers instead of each open-coding the same result-removal guard sequence. This still does not change dropped-result candidate selection, selected/full repair scope, type signature updates, localization/retry, convergence scheduling, or optimizing cleanup.

A thirty-first behavior-preserving consolidation added the now-superseded `DaeOperandRemovalAction` plus helper predicates over `DaeOperandDisposition`; the one hundred forty-fifth slice replaces it with the four-outcome rewrite disposition described below. The shared disposition carrier now makes the removed-operand decision explicit as drop-without-repair, replay-for-removal, or reject-branching-removal; the thirty-eighth slice then consumed that action through `DaeOperandRemovalPlan` for callsite append/drop repair and through `dae_slice_is_trivially_droppable(...)` until the one hundred forty-fifth slice moved those consumers to the replacement rewrite disposition. This is a localization/effect-preservation evidence cleanup only: branch-bearing operands are still not broadly localized/retried, arbitrary repair edits are still absent, and the plain-vs-optimizing scheduler split is unchanged.

A thirty-second behavior-preserving consolidation added `DaeDroppedResultRepairPlan`, `DaeDroppedResultDeadSuffixRepairKind`, `dae_dropped_result_repair_plan(...)`, and `dae_apply_dropped_result_repair_plan(...)`. Selected dropped-result removal and the full-module unobserved-unreachable, dead-self-suffix, uncalled-result, and unreachable cleanup helpers now carry one explicit repair plan for all-caller vs no-param-only dead-suffix repair plus optional dropped-call-drop cleanup, instead of each pairing repair and cleanup loops by hand. This keeps result-removal legality, candidate selection, type updates, convergence scheduling, localization/retry, and the plain-vs-optimizing scheduler split unchanged.

A thirty-third behavior-preserving consolidation added `DaeCallsiteValueSliceEvidence`, `dae_callsite_value_slice_evidence(...)`, and `dae_callsite_value_slice_evidence_is_consistent(...)`. Callsite-wide argument recovery now has one explicit carrier for call index, argument count, entry-stack context, recovered value slices, cursor, and per-slice repair dispositions. `dae_rewrite_callsite_at(...)`, `dae_rewrite_refined_gc_actuals_instrs(...)`, `dae_collect_uniform_const_actuals_instrs(...)`, `dae_collect_direct_struct_default_actuals_instrs(...)`, and the selected Func313/Func323 global-suffix repair path consume that carrier instead of pairing raw `(slices, cursor)` tuples by hand. This still does not add localize-then-retry behavior, arbitrary repair edits, non-final typeidx/control operand reconstruction, convergence scheduling changes, or optimizing cleanup to plain DAE.

A thirty-fourth behavior-preserving consolidation added `DaeCallsiteSingleInstructionActualEvidence`, `dae_callsite_single_instruction_actual(...)`, and `dae_callsite_single_instruction_actual_is_consistent(...)`. The uniform-constant actual collector and selected Func313/Func323 global-suffix repair path now ask the callsite evidence carrier for a single-instruction actual record instead of each indexing into `evidence.slices[p]` and rechecking single-instruction bounds by hand. This extends the callsite evidence layer without changing candidate legality, operand reconstruction, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.

A thirty-fifth behavior-preserving consolidation added `DaeDroppedResultModuleContext` and `dae_dropped_result_module_context(...)`. The unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup paths now share one carrier for imported count, defined-function index, type indices, copied function/type sections, full dropped-result scope, and result-removal legality instead of each rebuilding the same module setup and legality facts by hand. This extends the dropped-result legality/repair evidence layer without changing result-removal policy, selected/full repair semantics, type updates, localization/retry, convergence scheduling, or optimizing cleanup to plain DAE.

A thirty-sixth behavior-preserving consolidation widened `DaeDroppedResultModuleContext` with params/results and optional selected-lane perf timer preservation, then rewired the selected dropped-result removal path to consume the same module context as the full-module cleanup paths. Selected dropped-result removal now shares imported count, defined-function index, type indices, copied function/type sections, selected scope construction, result-removal legality, params/results, and section/scope setup timing instead of rebuilding that context separately. This keeps dropped-result result-removal policy, repair plan semantics, type updates, localization/retry, convergence scheduling, and optimizing cleanup unchanged.

A thirty-seventh behavior-preserving consolidation added `DaeOriginalResultPreservationContext`, `dae_original_result_preservation_context(...)`, and `dae_original_result_preservation_evidence_from_context(...)`. The main core zero-param/result-removal lane, the main core current-param lane, and the reverse exact-literal lane now build one preservation-input carrier and reuse it when deriving `DaeOriginalResultPreservationEvidence`, instead of repeating the same long original summary array list at each preservation check. This keeps the preservation predicates, argument-side repair gate, result-removal policy, localization/retry behavior, convergence scheduling, and optimizing cleanup unchanged.

A thirty-eighth behavior-preserving consolidation added `DaeOperandRemovalPlan`, `dae_operand_removal_plan(...)`, `dae_callsite_operand_removal_plan(...)`, and `dae_append_operand_with_removal_plan(...)`. Callsite removal now asks the callsite evidence carrier for an explicit per-operand keep/replay/drop plan, and trivial-droppable checks use the same plan predicate instead of interpreting the action booleans independently. This replaces the older disposition append wrapper with a repair-plan carrier consumed by both callsite rewriting and droppability checks while preserving the existing direct-removal semantics: pure removed operands are omitted, effectful removed operands are replayed and dropped, branch-bearing direct removals still remain a conservative non-localization boundary. It does not add true Binaryen localize-then-retry, arbitrary repair edits, non-final typeidx/control operand reconstruction, convergence changes, or optimizing cleanup to plain DAE.

A thirty-ninth behavior-preserving consolidation added `DaeControlEntryStackEvidence` and `dae_control_entry_stack_evidence(...)`. Nested block/loop/`try_table`/`if` recursion in callsite removal, refined GC actual rewriting, uniform-constant actual collection, and direct GC actual collection now derives child entry-stack context through the shared control-entry carrier instead of calling the block-type stack-effect helper directly at each loop. This is a value/stack-slice evidence cleanup only: it records both input and result stack counts for the control type, preserves the existing entry-stack value passed to callsite evidence, and does not add non-final control operand reconstruction, localization/retry, convergence changes, or optimizing cleanup to plain DAE.

A fortieth behavior-preserving consolidation added shared stack-provenance helper routines: `dae_stack_bool_has_true_operand(...)`, `dae_stack_bool_apply_effect(...)`, `dae_stack_int_has_operand_value(...)`, `dae_stack_int_has_known_operand(...)`, and `dae_stack_int_apply_effect(...)`. Original-boundary dead-suffix/result-preservation scanners now consume the same operand-slice lookup and stack push/pop effect helpers in `dae_mark_dead_suffix_escaped_result_operand_targets(...)`, `dae_mark_dead_suffix_same_result_operand_targets(...)`, `dae_dead_suffix_call_to_has_same_result_operand(...)`, and `dae_instrs_have_undropped_call_to_with_param_operand(...)` instead of open-coding parallel bool/int stack loops. This is another value/stack-slice evidence cleanup only: it centralizes source-provenance stack updates for existing scanners without adding Binaryen-style localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE.

A forty-first behavior-preserving consolidation added `DaeDeadSuffixEvidence` and `dae_dead_suffix_evidence(...)`. Dead-suffix repair, direct-call target/count scanners, dropped/undropped escaped-result scanners, self-call param-operand preservation, self-recursive side-call detection, and private-result side-call checks now recover the unreachable-root index and suffix tail through the shared evidence carrier instead of each copying or walking the tail after an unreachable root by hand. This is a value/stack-slice and effect-preservation cleanup only: it does not change what counts as an unreachable root, does not add localization/retry or arbitrary repair edits, and does not move optimizing-only cleanup into plain DAE.

A forty-second behavior-preserving consolidation added `DaeStackOperandWindowEvidence`, `dae_stack_operand_window_evidence(...)`, and `dae_stack_operand_window_evidence_is_consistent(...)`. The bool/int stack-provenance helpers now ask one top-of-stack operand-window carrier for the bounded operand range instead of each recomputing `stack.length() - arg_count + p` and underfull-stack checks by hand. This widens the fortieth stack-provenance extraction inside the value/stack-slice bucket only: it preserves existing bool/int operand-source semantics, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-third behavior-preserving consolidation added `DaeStackEffectEvidence`, `dae_stack_effect_evidence(...)`, `dae_stack_bool_apply_instr_effect(...)`, and `dae_stack_int_apply_instr_effect(...)`. The bool/int dead-suffix operand-target and self-call-param scanners now share the same instruction stack-effect carrier before applying provenance push/pop updates instead of each matching `dae_instr_stack_effect(...)` and replaying inputs/outputs by hand. This is another value/stack-slice evidence cleanup only: it preserves unknown-stack-effect permissiveness by leaving the provenance stack unchanged when no effect evidence is available, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-fourth behavior-preserving consolidation added `dae_control_entry_stack_values(...)` as the shared child entry-stack extraction helper over `DaeControlEntryStackEvidence`. Recursive callsite removal, refined-GC actual rewriting, uniform-constant actual collection, and direct GC actual collection now ask the same helper for block/loop/`try_table`/`if` entry-stack values instead of each matching the control-entry evidence record and projecting `entry_stack_values` by hand. This is a small value/stack-slice evidence cleanup only: it preserves existing rejection when block-type stack evidence is unavailable, does not reconstruct non-final typeidx/control operands, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-fifth behavior-preserving consolidation added `DaeControlBlockTypeSignatureEvidence` and `dae_control_block_type_signature_evidence(...)` as the shared block-type signature resolver for `void`, `valtype`, and type-indexed control. `dae_instr_stack_effect_from_block_type(...)`, `dae_control_entry_stack_evidence(...)`, and `dae_try_refine_parametric_typeidx_multi_result_terminal_control_body(...)` now consume the same params/results/input/output/typeidx evidence instead of separately resolving block-type stack effects and typeidx function signatures. This is a GC/control evidence cleanup only: it preserves the typeidx-only gate for parametric terminal-control refinement, does not reconstruct non-final typeidx/control operands, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-sixth behavior-preserving consolidation added `DaeSingleBlockResultCarrierEvidence` and `dae_single_block_result_carrier_evidence(...)` as a shared result-carrier proof for a single branch-free/effect-free block with zero inputs and the expected multi-result output arity. `dae_find_multi_result_control_slice(...)` now uses that carrier for its non-return flattenable block case, block-plus-`return` carrier case, and nested flattening loop instead of resolving block stack effects and scanning for observable effects in three adjacent shape scanners. This is a value/stack-slice recovery cleanup only: it preserves existing flat-tail and terminal-return semantics, does not add non-final typeidx/control operand reconstruction, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-seventh behavior-preserving consolidation added `DaeNestedControlEntryEvidence`, `dae_nested_control_entry_evidence(...)`, and a consistency check as the instruction-level control-entry carrier for block/loop/`try_table`/`if` recursion. Refined-GC actual rewriting, callsite operand removal, uniform-constant actual collection, and direct GC actual collection now consume that carrier instead of each resolving child entry-stack context from block types directly. This is another value/stack-slice cleanup only: it preserves existing rejection when nested control entry evidence is unavailable, does not reconstruct non-final typeidx/control operands, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-eighth behavior-preserving consolidation added `DaeTailCallBoundaryEvidence` and `dae_tail_call_boundary_evidence(...)` as the shared direct-vs-indirect tail-call boundary carrier. Direct-call parameter/constant rewrite legality now consumes the carrier's indirect-tail blocker, and dropped-result removal legality consumes the same carrier's any-tail result-removal blocker, instead of keeping separate recursive direct-tail and indirect-tail scanner loops. This is a boundary/result-removal legality cleanup only: it preserves the existing difference where indirect tail calls block callsite signature repair while any live tail call blocks dropped-result removal, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A forty-ninth behavior-preserving consolidation added `DaeDirectCallTargetEvidence` and `dae_direct_call_target_evidence(...)` as a shared target-specific direct-call evidence carrier. Direct-call containment, direct-call counting, undropped direct-call detection, and live-undropped direct-call detection now consume the same recursive count/drop/live-prefix facts instead of maintaining separate block/loop/`try_table`/`if` scanners. This is an ownership/call-boundary cleanup only: it preserves direct `call` counting separately from `return_call` undropped-use evidence, keeps post-`unreachable` calls visible to anywhere counts while excluding them from live-undropped evidence, does not add localization/retry or arbitrary repair edits, and does not change convergence or plain-vs-optimizing scheduling.

A fiftieth behavior-preserving consolidation added `DaeDeadSuffixDirectCallEvidence` and `dae_dead_suffix_direct_call_evidence(...)` as the shared dead-suffix direct-call target carrier. The dropped-suffix and undropped-suffix target predicates now consume one helper, and the full-module unobserved-unreachable and dead-self-suffix dropped-result cleanup gates consume the carrier directly instead of asking separate dead-suffix target predicates. This is a dead-suffix/result-removal legality cleanup only: it preserves the existing top-level `call`+`drop` dropped-suffix policy, reuses recursive target evidence for undropped uses, does not add localization/retry, arbitrary repair edits, convergence changes, or optimizing cleanup to plain DAE, and does not change direct raw-red status.

A fifty-first behavior-preserving consolidation added `DaeDroppedResultScopeDirectCallEvidence` and `dae_dropped_result_scope_direct_call_evidence(...)` as the scoped selected-vs-all caller direct-call target carrier. Dropped-result removal legality now asks the scope carrier for direct-call counts, dropped-call counts, undropped-use facts, and live-prefix undropped-call blockers instead of open-coding a caller loop with a separate live-undropped predicate. Existing selected/full-scope tests now consume the same carrier. This is a result-removal legality cleanup only: it preserves invalid-caller skipping, `skip_def` exclusion, live-prefix blocking, tail-call blocking, large-result cleanup blocking, and the plain-vs-optimizing scheduler split, and it does not change direct raw-red status.

A fifty-second behavior-preserving consolidation added `DaeOriginalNoParamDroppedCallerGuardEvidence`, `dae_original_no_param_dropped_caller_guard_evidence(...)`, and `dae_should_preserve_undropped_self_result_from_no_param_dropped_caller_evidence(...)` as an indexed original-boundary guard carrier for the no-param dropped-caller preservation case. The existing `dae_should_preserve_undropped_self_result_from_no_param_dropped_caller(...)` predicate now consumes the carrier, which keeps both original result-preservation evidence and the param-operand self-result side-callee count collector on the same safe indexed facts instead of rechecking the long original summary array list directly. This is an original-vs-current boundary summary cleanup only: it preserves out-of-range false/zero defaults, the active/nonself/parameterized count gates, self-result/param-operand/result-other requirements, mutual-call rejection, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A fifty-third behavior-preserving consolidation added `DaeOriginalParameterizedUndroppedSelfSideChainEvidence` and `dae_should_preserve_parameterized_undropped_self_side_chain_evidence(...)` as an indexed original-boundary guard carrier for the parameterized undropped self-side-chain case. Its original context projection was later superseded by the one hundred fiftieth boundary-row consolidation: current production consumers call `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` on the already-indexed per-definition preservation row instead of re-reading the context. This is another original-vs-current boundary summary cleanup only: it preserves out-of-range false/zero defaults, the parameterized nonself/self-call/result-other/escaped-dead-suffix gates, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A fifty-fourth behavior-preserving consolidation added `DaeDroppedResultCalleeRewriteEvidence`, `dae_dropped_result_callee_rewrite_evidence(...)`, and carrier predicates for missing dropped/undropped dead-suffix calls. The full-module unobserved-unreachable, dead-self-suffix, and uncalled-result dropped-result cleanup gates now consume one callee rewrite carrier for param/result counts, original-unreachable status, simplified-unreachable status, dropped/undropped dead-suffix target facts, and result-removal blocker status instead of each pairing dead-suffix, simplification, and legality facts locally. This is a dropped-result result-removal legality cleanup only: it preserves the existing required dropped vs undropped suffix checks, simplification policy, result-removal blocker policy, repair-plan behavior, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A fifty-fifth behavior-preserving consolidation added `DaeEscapedResultDirectCallEvidence`, `dae_escaped_result_direct_call_evidence(...)`, and `dae_dead_suffix_escaped_result_direct_call_evidence(...)` as the shared dropped/undropped escaped-result direct-call carrier. The original-boundary dead-suffix preservation collectors now consume the same recursive evidence for dropped and undropped escaped-result calls instead of maintaining parallel block/loop/`try_table`/`if` scanners. This is an original-boundary value/stack-slice evidence cleanup only: it preserves the existing escaped-result, result-count, dropped-vs-undropped, and dead-suffix policies, keeps localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status unchanged, and does not add optimizing cleanup to plain DAE.

A fifty-sixth behavior-preserving consolidation added `DaeDeadSuffixDirectTargetEvidence`, `dae_dead_suffix_direct_target_evidence(...)`, and `dae_collect_dead_suffix_direct_target_lists(...)` as the shared dead-suffix direct-target list carrier. The original-boundary direct, dropped-direct, and undropped-direct dead-suffix target collectors now consume one dead-suffix projection for direct `call` targets, top-level dropped `call`+`drop` targets, and recursive undropped `call`/`return_call` targets instead of each recovering the unreachable suffix and applying its own list policy. This is an original-boundary ownership/value-slice evidence cleanup only: it preserves the existing direct vs `return_call`, top-level dropped-call, recursive undropped-target, localization/retry, convergence, plain-vs-optimizing scheduling, and direct raw-red policies unchanged, and does not add optimizing cleanup to plain DAE.

A fifty-seventh behavior-preserving consolidation added `dae_stack_single_value_producer_evidence(...)` as the shared zero-input/one-output instruction stack-effect proof and moved additional tuple-shaped stack-effect users onto the existing `DaeStackEffectEvidence` carrier. Indirect-call GC result-slice operand validation, typed-select stack-local condition checks, multi-result `if`/typed-select suffix consumers, `dae_find_value_slice(...)`, parametric typeidx prefix stack accounting, and self-call operand-origin propagation now consume reusable stack-effect evidence instead of matching raw `(inputs, outputs)` tuples in each local scanner. This is a value/stack-slice evidence cleanup only: it preserves effect/branch rejection, unknown-stack-effect rejection or permissiveness at the original callsites, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add optimizing cleanup to plain DAE.

A fifty-eighth behavior-preserving consolidation added `dae_control_single_result_block_type_evidence(...)` as the shared zero-input/one-output block-type proof over `DaeControlBlockTypeSignatureEvidence`. Direct GC result block, loop, and `if` candidate paths plus a multi-result suffix consumer now ask the shared block-type signature carrier for single-result eligibility instead of matching `@lib.ValTypeBlockType` or raw `Some((0, 1))` stack-effect tuples locally. This is a control/value evidence cleanup only: it preserves typeidx rejection for missing/out-of-range signatures, void/control multi-result bailouts, rewrite shape, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add optimizing cleanup to plain DAE.

A fifty-ninth behavior-preserving consolidation added `DaeNestedControlChildrenEvidence` and `dae_nested_control_children_evidence(...)` as the reusable child-body projection over the existing nested-control entry carrier. Recursive uniform-constant actual collection and direct `struct.new_default` actual collection now consume one control-children carrier for block/loop/`try_table`/`if` primary and optional secondary bodies plus shared entry-stack context instead of maintaining separate block/loop/`try_table`/`if` arm walkers and local entry-stack projections. This is a value/stack-slice traversal cleanup only: it preserves rejection when nested-control entry evidence is unavailable, branch/arm traversal order, localization/retry behavior, convergence, plain-vs-optimizing scheduling, and direct raw-red status unchanged, and does not add optimizing cleanup to plain DAE.

A sixtieth behavior-preserving consolidation added `dae_rebuild_nested_control_children(...)` as the reusable reconstruction helper for the nested-control child evidence carrier. Refined-GC actual rewriting and callsite operand removal now consume `DaeNestedControlChildrenEvidence` plus the rebuild helper for block/loop/`try_table`/`if` primary and optional secondary bodies instead of maintaining separate reconstruction arms with local entry-stack projections. This is a value/stack-slice traversal and effect-preserving repair cleanup only: it preserves rejection when nested-control evidence is unavailable, preserves `try_table` catches and if-with-else shape, does not add non-final typeidx/control operand reconstruction, localization/retry behavior, convergence changes, plain-vs-optimizing scheduling changes, or optimizing cleanup to plain DAE, and leaves direct raw-red status unchanged.

A sixty-first behavior-preserving consolidation added `DaeMultiResultSuffixBoundsEvidence` and `dae_multi_result_suffix_bounds_evidence(...)` as a reusable suffix-bound carrier for multi-result refinement scans. The single-`if` operand refinement path and typed-select operand refinement path now share terminal-return mode selection, minimum-length checks, operand-end calculation, suffix-start iteration, and trivially-dropped-prefix filtering instead of each carrying the same mode/suffix loop before their shape-specific `if` or `select` recognizers. This is a value/stack-slice recovery cleanup only: it preserves the existing scan order including non-terminal mode before terminal-return mode, keeps the shape-specific candidate/refinement logic local to each path, does not add localization/retry behavior, convergence changes, plain-vs-optimizing scheduling changes, or optimizing cleanup to plain DAE, and leaves direct raw-red status unchanged.

A sixty-second behavior-preserving consolidation added `DaeStackSliceSimulationEvidence` and `dae_stack_slice_simulation_evidence(...)` as a reusable bounded stack-slice simulator with policy hooks. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks now share one stack-underflow/effect/maximum-stack/final-stack evidence carrier instead of each walking instructions and applying stack effects locally. This is a value/stack-slice recovery cleanup only: it preserves the single-instruction typed-select fast path, preserves the existing allowed-instruction policies for condition slices and parametric typeidx prefixes, does not add localization/retry behavior, non-final typeidx/control reconstruction, convergence changes, plain-vs-optimizing scheduling changes, or optimizing cleanup to plain DAE, and leaves direct raw-red status unchanged.

A sixty-third behavior-preserving consolidation added `DaeEscapingBranchEvidence`, `dae_escaping_branch_evidence(...)`, and `dae_no_escaping_branch_evidence(...)` as reusable branch-escape facts for localization/control cleanup gates. The existing escaping-branch predicate now derives from the carrier, and three Func237 carrier-localization gates consume the no-escape evidence directly instead of treating branch-safety as an ad hoc boolean. This is a localization/effect-preservation cleanup only: it preserves the exact depth-based branch-target policy, keeps broader localize-then-retry and non-final typeidx/control reconstruction out of scope, does not change convergence or plain-vs-optimizing scheduling, does not import optimizing cleanup into plain DAE, and leaves direct raw-red status unchanged.

A sixty-fourth behavior-preserving consolidation added `dae_multi_result_control_carrier_chain_slice(...)` as a reusable multi-result block-carrier chain recovery helper. `dae_find_multi_result_control_slice(...)` now uses the same helper for fallthrough multi-result carriers, terminal-return carrier chains, terminal block bodies, and block-plus-`return` carriers instead of maintaining separate unwrapping loops around `DaeSingleBlockResultCarrierEvidence`. This is a value/control stack-slice cleanup only: it preserves the existing carrier kind, flattening, appended-`return`, and terminal-return policies, does not add non-final typeidx/control reconstruction or localization/retry, does not change convergence or plain-vs-optimizing scheduling, does not import optimizing cleanup into plain DAE, and leaves direct raw-red status unchanged.

A sixty-fifth behavior-preserving consolidation added `DaeOriginalResultPreservationRow`, `dae_original_result_preservation_row_from_context(...)`, `dae_original_no_param_dropped_caller_guard_evidence_from_row(...)`, `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)`, and `dae_original_result_preservation_evidence_from_row(...)` as one per-definition original-boundary row for result-preservation guard facts. `dae_original_result_preservation_evidence_from_context(...)` and the parameterized self-side-chain context helper now project through that row, so the no-param dropped-caller guard, parameterized self-side-chain guard, and result-preservation evidence share one safe indexed original-summary projection instead of each carrying its own array lookup wrapper. This is an original-vs-current boundary summary cleanup only: it preserves out-of-range zero/false defaults, same-self/result-from-undropped/escaped-chain/mixed-self/escaped-block/param-operand/no-param/dropped-escaped/parameterized-side-chain predicates, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A sixty-sixth behavior-preserving consolidation added `DaeOriginalResultRemovalPruneRow`, `dae_should_prune_later_mutual_dropped_result_from_row(...)`, and `dae_should_prune_dropped_self_result_with_void_side_callers_from_row(...)` as one original-boundary result-removal prune carrier. Its original direct parallel-array row constructor was superseded in the one hundred fiftieth slice by `dae_original_result_removal_prune_row_from_preservation_row(...)`, which derives shared fields from the already-indexed preservation row. The unobserved cleanup lane and the zero-param current core lane now build one row per definition and use it for both later-mutual dropped-result pruning and dropped-self-result-with-void-side-callers pruning instead of each predicate re-indexing overlapping original summary arrays. This is an original-vs-current result-removal legality cleanup only: it preserves out-of-range zero/false defaults, later-mutual and void-side-caller prune predicates, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A sixty-seventh behavior-preserving consolidation widened `DaeOriginalResultRemovalPruneRow` with private-result, result-other, no-param-result-caller, and mixed dropped/undropped caller facts plus row-derived dropped-only and mixed-caller prune predicates. The unobserved cleanup lane and zero-param current core lane now consume the row for mixed dropped/undropped result pruning and their dropped-only result-side caller pruning variants instead of each loop re-indexing the same original summary arrays. This is still an original-vs-current result-removal legality cleanup only: it preserves out-of-range zero/false defaults, mixed/dropped-only/later-mutual/void-side-caller predicates, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A sixty-eighth behavior-preserving consolidation widened `DaeOriginalResultRemovalPruneRow` with block-undropped-self and same-self-result-operand facts plus row-derived mixed-self suffix, same-self result-operand, and escaped-block self-result preservation predicates. The unobserved cleanup lane now uses the row for mixed-self, same-self-operand, and escaped-block self-result guards, and the zero-direct-call current core lane uses the same row-derived predicates/fields for its mixed-self, same-self-operand, and escaped-block result guards instead of re-indexing those original summary arrays separately. This is still an original-vs-current result-removal legality cleanup only: it preserves out-of-range zero/false defaults, all existing prune/preserve predicates, result-removal policy, localization/retry behavior, convergence scheduling, the plain-vs-optimizing scheduler split, and direct raw-red status.

A sixty-ninth behavior-preserving consolidation added `DaeStackSliceTransitionEvidence` and `dae_stack_slice_transition_evidence(...)` as a final-stack expectation wrapper over the bounded stack-slice simulator. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks now share the same transition carrier for initial-stack, allowed-instruction, simulation, and expected-final-stack acceptance instead of each matching the simulator and comparing `final_stack_values` locally. This is a value/stack-slice recovery cleanup only: it preserves the existing typed-select single-instruction fast path, allowed-instruction policies, underflow/effect/max-stack evidence, non-final typeidx/control reconstruction gaps, localization/retry behavior, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventieth behavior-preserving consolidation widened `DaeEscapingBranchEvidence` with range bounds and added `dae_escaping_branch_range_evidence(...)` / `dae_no_escaping_branch_range_evidence(...)` so prefix/range localization gates can prove branch escape safety without first copying a temporary instruction array. The full-array helpers and predicate wrappers now delegate through the range-aware carrier, and two Func237 carrier-localization paths consume the range helper before copying their accepted prefixes. This is still a localization/effect-preservation cleanup only: it preserves the existing depth policy, branch-escape rejection, localization/retry behavior, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-first behavior-preserving consolidation added `dae_no_escaping_branch_prefix_evidence(...)` and `dae_no_escaping_branch_suffix_evidence(...)` as bounded projections over the range-aware escaping-branch carrier. Several Func237 and Func256 localization/control cleanup gates now prove accepted prefixes or suffixes with those helpers before copying temporary arrays or rebuilding loop carriers. This keeps the same branch-depth policy, rejection behavior, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-second behavior-preserving consolidation added `DaeDroppedOperandPairSkipEvidence` / `dae_dropped_operand_pair_skip_evidence(...)` for the shared "skip already-dropped trivial operand" subflow inside multi-result suffix operand scanners. The single-`if` and typed-select multi-result refinement loops now consume one bounds-checked carrier for start/end/next/skipped-pair evidence instead of open-coding the same pure-operand-plus-`drop` loop. This is still a value/stack-slice recovery cleanup only: it preserves suffix bounds, candidate selection, repair/rewrite behavior, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-third behavior-preserving consolidation added `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)` as the shared paired-`if` control-result refinement carrier. The fallthrough terminal-`if` path and the `if`-plus-`unreachable` terminal-return path in `dae_try_refine_direct_gc_types(...)` now delegate then/else slice recovery, per-arm GC evidence, paired LUB/refinement, and if-body-change evidence to one helper instead of open-coding the same slice/evidence/pair loop twice. This is still a control/GC evidence cleanup only: it preserves the existing block-type rewrite difference between value-producing fallthrough ifs and zero-result terminal-return ifs, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-fourth behavior-preserving consolidation added `DaeControlResultPairRefinementApplicationEvidence` / `dae_apply_control_result_pair_refinements(...)` as the shared paired-GC result-refinement application carrier. Parametric typeidx terminal-control refinement and both direct multi-result `if` refinement paths now apply paired LUB/refined result vectors through one helper instead of open-coding result-index loops over `next_results` and optional local refined-result vectors. This is still a control/GC result-repair evidence cleanup only: it preserves the existing fallthrough type-section rewrite, terminal-return body-change policy, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-fifth behavior-preserving consolidation added `DaeControlResultUndroppedDirectCallEvidence` / `dae_control_result_undropped_direct_call_evidence(...)` as the shared control-result direct-call blocker carrier. The dead-suffix block-undropped self-call scanner now uses one helper for block, loop, `try_table`, `if` with else, and single-arm `if` result-arity-plus-undropped-call evidence instead of open-coding the result arity and arm scans per control shape. This is still a dropped-result/result-preservation legality cleanup only: it preserves conservative unknown typeidx result handling, void-result nonblocking behavior, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-sixth behavior-preserving consolidation added `DaeOriginalDeadSuffixSideCallEvidence` / `dae_original_dead_suffix_side_call_evidence(...)` as the shared original-boundary dead-suffix side-call cleanup gate carrier. The zero-direct-call current core now consumes one carrier for void-side cleanup preservation, recursive result-side chain preservation, and same-self result-operand preservation in both the dead-suffix-only parameter cleanup gate and the later uncalled-result skip gate instead of open-coding the same policy predicates twice. This is still an original-boundary result-removal legality cleanup only: it preserves the existing side-call/self-recursion predicates, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-seventh behavior-preserving consolidation widened `DaeOriginalResultPreservationEvidence` with `result_side_chain_cleanup_preservation`, a shared summary over the no-param dropped-caller, dropped-escaped-result-under-self-caller, and parameterized undropped self-side-chain preservation gates. The unobserved-unreachable cleanup gate, dead-suffix-only parameter cleanup gate, and later uncalled-result skip gate now consume the result-preservation carrier for that common side-chain cleanup decision instead of rechecking the same original-boundary predicates locally. This is still an original-boundary result-preservation/result-removal cleanup only: it preserves the existing escaped-block and same-self result-operand differences, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-eighth behavior-preserving consolidation added `DaeOriginalResultRemovalPruneEvidence` / `dae_original_result_removal_prune_evidence_from_row(...)` plus aggregate predicates for params-absent dropped-result cleanup and uncalled-after-param-cleanup blockers. The zero-direct-call current core and zero-param current core now derive later-mutual, mixed dropped/undropped, dropped-only private/no-param, and dropped-self-with-void-side-caller prune decisions once from the original-boundary prune row and consume the aggregate evidence in multiple cleanup/skip gates instead of rechecking those row predicates locally. This is still an original-boundary result-removal legality cleanup only: it preserves the existing private-vs-no-param dropped-only distinction, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

A seventy-ninth behavior-preserving consolidation added `DaeNoEscapeSliceEvidence` / `dae_no_escaping_branch_copied_range_evidence(...)` as a reusable carrier for accepted no-escaping branch ranges plus the copied instruction slice. Two Func237 localization/control paths now consume that carrier where they previously proved no-escape and then copied the same accepted prefix separately: `dae_func237_outer_loop_then_split_next_carrier(...)` and `dae_try_wrap_func237_outer_loop_carrier_instr(...)`. This is still a localization/effect-preservation cleanup only: it preserves the existing branch-depth policy, accepted range bounds, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

An eightieth behavior-preserving consolidation widened `DaeNoEscapeSliceEvidence` with `dae_no_escape_slice_evidence_from_branch(...)`, `dae_no_escaping_branch_copied_prefix_evidence(...)`, and `dae_no_escaping_branch_copied_suffix_evidence(...)`. Dropped-result branch-carrier folding, unreachable-after-if-else folding, and leading terminal-`br 0` block splicing now consume copied prefix/suffix no-escape carriers instead of separately proving accepted prefix/suffix branch safety and copying the same slice. This is still a localization/effect-preservation cleanup only: it preserves the existing branch-depth policy, accepted prefix/suffix bounds, localization/retry gaps, convergence scheduling, plain-vs-optimizing scheduler split, and direct raw-red status.

An eighty-first behavior-preserving consolidation added `DaeTerminalIfNoEscapeCarrierEvidence`, `dae_no_escaping_branch_copied_evidence(...)`, and `dae_terminal_if_no_escape_carrier_evidence(...)` as the shared terminal zero-result `if` carrier for localization/control loops. Func237 inner-iteration wrapping and Func256 void-loop carrier wrapping now consume one carrier for branch-safe copied condition prefixes, zero-result `if` validation, then-body projection, and optional copied no-escape else-body evidence instead of open-coding prefix branch proof, prefix copying, `if` shape matching, and else-body branch checks in each scanner. This keeps the same branch-depth policy, no-else allowance for Func256, Func237's required-else gate, convergence behavior, direct raw-red status, and plain-vs-optimizing scheduling split.

An eighty-second behavior-preserving consolidation added `DaeMixedMultiResultReturnArmGcEvidence` / `dae_mixed_multi_result_return_arm_gc_evidence(...)` as the shared mixed return/fallthrough multi-result `if` arm carrier. The mixed fallthrough/refinement path now consumes the same helper for then and else arms instead of open-coding return-arm body/start/flattening recovery, per-lane return-candidate extraction, candidate/current LUB rejection, and effect-free fallthrough acceptance separately for each arm. This preserves mixed return/fallthrough refinement policy, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-third behavior-preserving consolidation added `DaeSingleResultControlPairGcEvidence` / `dae_single_result_control_pair_gc_evidence(...)` as the shared single-result `if` arm LUB/rewrite carrier. The implicit returned-value `if` path and the explicit-return zero-result `if` path now both delegate then/else candidate LUB and rewritten-arm body selection to this helper instead of open-coding the same candidate/LUB/rewrite selection loop twice. This is a GC type evidence cleanup only: it preserves existing block-type rewrite differences between value-producing and explicit-return `if` forms, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-fourth behavior-preserving consolidation added `DaeMixedMultiResultReturnArmRewriteEvidence` / `dae_apply_mixed_multi_result_return_arm_refinements(...)` as the shared refined-slot rewrite application carrier for mixed return/fallthrough multi-result `if` arms. The then-return and else-return application paths now consume the same helper after candidate/LUB/refinement has selected lanes, instead of each copying an arm, replaying the refined-lane candidate scan, applying optional rewrites, and tracking flattened-body changes separately. This is paired-arm repair evidence cleanup only: it preserves the existing mixed return/fallthrough candidate policy, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-fifth behavior-preserving consolidation added `DaeControlResultSingleRefinementApplicationEvidence` / `dae_apply_control_result_single_refinements(...)` as the shared single-arm refined-result application carrier over `DaeControlResultGcEvidence`. The parametric typeidx terminal-control single-arm path and the flat multi-result direct-GC body refinement path now consume one helper for applying `refined_candidates` into `next_results` plus optional local refined-result vectors instead of each open-coding result-index repair loops. This is GC result-repair evidence cleanup only: it preserves existing candidate policy, body rewrite selection, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-sixth behavior-preserving consolidation added `DaeTerminalNoEscapePrefixEvidence` / `dae_terminal_no_escape_prefix_evidence(...)` as a reusable terminal-body carrier that pairs the final instruction with a copied no-escape prefix. Unreachable-after-if-else branch folding and leading terminal-`br 0` block splicing now consume the same helper instead of separately recovering the last instruction, proving prefix no-escape, and copying that prefix. This is localization/effect-preservation evidence cleanup only: it preserves existing branch-depth policy, terminal-instruction checks, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-seventh behavior-preserving consolidation added `DaeMixedSingleResultReturnArmGcEvidence` / `dae_mixed_single_result_return_arm_gc_evidence(...)` as the shared mixed return/fallthrough single-result `if` arm carrier. The single-result mixed fallthrough/refinement path now consumes the same helper for then and else arms instead of open-coding return-arm candidate extraction, candidate/current LUB rejection, optional returned-arm rewrite propagation, and effect-free fallthrough acceptance separately for each arm. This is GC/LUB plus localization/effect-preservation evidence cleanup only: it preserves existing simple mixed return/fallthrough policy, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-eighth behavior-preserving consolidation added `DaeStackSliceLocalizationEvidence` / `dae_stack_slice_localization_evidence(...)` as the first combined stack-slice localization/repair carrier. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks now consume one helper that pairs expected-final stack-slice transition evidence, no-escaping-branch range evidence, operand disposition, and a keep/remove repair plan instead of each stopping at the transition carrier and leaving localization/repair facts separate. This is localization/effect-preservation plus stack-slice repair evidence cleanup only: it preserves existing condition/prefix instruction policies, single-instruction typed-select behavior, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

An eighty-ninth behavior-preserving consolidation added `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` as a shared local-copy gap carrier for two Func237 cleanup-localization subflows. Immediate single-use temp-copy folding and used-temp copy retargeting now both route their gap range checks through one mode-tagged helper that centralizes temp/target local interference, branch/throw/return rejection, call-with-try-table preservation, and the used-temp recursive nested-control policy instead of maintaining separate range loops. This is optimizing-cleanup evidence consolidation only: it preserves existing immediate-vs-used-temp policy, root `try_table` call preservation, localization/retry gaps in the DAE core, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninetieth behavior-preserving consolidation added `DaeTargetCallsiteMode`, `DaeTargetCallsiteEvidence`, and `dae_target_callsite_evidence(...)` as a small call-boundary carrier for matching a target callee at a callsite. Self-forwarded-parameter local-use suppression, refined-GC actual rewriting, parameter-removal callsite rewriting, recursive uniform-constant actual collection, and direct `struct.new_default` GC actual collection now share one direct-call vs direct-or-`return_call` target policy instead of each open-coding callee-index matching. This is ownership/call-boundary and callsite-repair evidence cleanup only: it preserves direct-only collection where the old loops ignored `return_call`, preserves tail-aware matching where the old rewrite loops accepted `return_call`, does not add localization/retry or convergence scheduling, does not import optimizing cleanup into plain DAE, and does not change direct raw-red status.

A ninety-first behavior-preserving consolidation added `DaeDroppedPureLeafCleanupEvidence` / `dae_dropped_pure_leaf_cleanup_evidence_from_stack(...)` / `dae_dropped_pure_leaf_cleanup_evidence_from_leaf(...)` as a shared raw-cleanup preservation carrier for dropped pure leaves. Stack-suffix truncation after `dae_find_droppable_pure_stack_expr_start(...)` and adjacent pure-leaf-plus-`drop` stripping now consume one evidence carrier for range eligibility, integer-constant debris preservation, local-get-after-same-set preservation, and local-get-after-effect preservation instead of open-coding those gates in two cleanup paths. This is localization/effect-preservation and cleanup-policy evidence consolidation only: it preserves raw cleanup modes, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninety-second behavior-preserving consolidation added `DaeSingleResultControlGcCandidateEvidence` and `dae_single_result_control_gc_candidate_evidence(...)` as a shared single-result control GC candidate carrier. The direct GC result `block` and `loop` candidate arms delegated single-result block-type proof, body candidate recovery, optional rewritten-body selection, and valtype rewrite wrapping through one candidate-specific path instead of each open-coding the same block/loop body scanner and wrapper construction. The one hundred forty-third consolidation later replaced the dedicated apply wrapper with the broader single-body control reconstruction carrier described below. This remains GC type/refinement and result-repair evidence consolidation only: it preserves existing block/loop candidate policy, leaves `if` LUB handling on `DaeSingleResultControlPairGcEvidence`, does not add localization/retry or convergence scheduling, does not import optimizing cleanup into plain DAE, and does not change direct raw-red status.

A ninety-third behavior-preserving consolidation added `DaeResultRewriteApplicationEvidence` and `dae_apply_result_rewrites_to_body(...)` as a shared masked result-slot rewrite application carrier. Flat control-result GC evidence, paired then/else control-result LUB evidence, mixed return/fallthrough returned-arm repair, and mixed terminal-result repair now delegate bounds checks, active-lane masks, optional rewrite application, and body-changed counting through the same helper instead of open-coding parallel rewritten-body loops. This is operand/result repair consolidation only: it preserves active-lane policy, flattened-arm body-changed reporting, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninety-fourth behavior-preserving consolidation added `DaeSelectValueRewriteApplicationEvidence` and `dae_apply_select_value_rewrites_to_body(...)` as a shared typed-select rewrite application carrier. The single-result returned-value select path and the flat multi-result typed-select operand path now delegate left operand rewrite, right operand rewrite, select-instruction rewrite, bounds checks, select-slot validation, and body-changed counting through one helper instead of open-coding parallel repair blocks. This is operand/result repair consolidation only: it preserves existing typed-select candidate policy, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninety-fifth behavior-preserving consolidation added `DaeSingleResultControlPairApplicationEvidence` and `dae_apply_single_result_control_pair_gc_evidence(...)` as a shared single-result `if` rewrite-application carrier. The implicit returned-value `if` GC refinement path and the explicit-return zero-result `if` refinement path now delegate value-result vs void-result block-type selection, rewritten then/else body insertion, and application evidence through one helper instead of open-coding two parallel `if` reconstruction blocks. This is operand/result repair consolidation only: it preserves existing single-result arm candidate/LUB policy, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninety-sixth behavior-preserving consolidation added `DaeStackSliceLocalizationRequest` and `dae_stack_slice_localization_evidence_from_request(...)` as an explicit request layer for bounded stack-slice localization evidence. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption now build the same request carrier for body/range, initial/final stack expectations, branch depth, instruction policy, and keep/remove repair intent before deriving transition, no-escape, disposition, and removal-plan evidence. This moves the earlier combined localization helper toward a reusable localization/retry plan interface without adding true Binaryen localize-then-retry, arbitrary repair edits, non-final typeidx/control reconstruction, convergence changes, optimizing cleanup to plain DAE, or any direct-green claim.

A ninety-seventh behavior-preserving consolidation added `DaeSingleResultControlPairRepairEvidence` and `dae_single_result_control_pair_repair_evidence(...)` as a broader single-result `if` repair carrier. The implicit returned-value `if` GC refinement path and the explicit-return zero-result `if` refinement path now share arm candidate gathering through a callback, then/else LUB selection, optional arm-rewrite selection, and value-result vs void-result `if` rewrite application instead of separately nesting candidate extraction and application around the lower-level pair/application helpers. This is operand/result repair and GC LUB/refinement consolidation only: it preserves existing implicit-vs-explicit arm candidate policy, does not add broader localization/retry, arbitrary repair edits, convergence changes, optimizing cleanup to plain DAE, or any direct-green claim.

A ninety-eighth behavior-preserving consolidation added `DaeIndexedInstructionRewriteApplicationEvidence` and `dae_apply_indexed_instruction_rewrites_to_body(...)` as a lower-level body-edit carrier for sparse instruction rewrites. The masked result-slot helper and the typed-select left/right/select helper now both delegate duplicate-target rejection, out-of-range rejection, optional rewrite application, unchanged-body preservation, and rewrite-count/body-changed evidence to the shared indexed helper instead of maintaining separate body-copy/counting loops. This is operand/result repair application consolidation only: it preserves existing result-slot mask policy, typed-select slot ordering/select-slot validation, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A ninety-ninth behavior-preserving consolidation added `DaeMultiResultOperandSlotEvidence`, `dae_multi_result_operand_slot_prefix_evidence(...)`, and `dae_multi_result_operand_slot_single_producer_evidence(...)` as a shared multi-result suffix slot carrier. The multi-result single-`if` operand scanner and flat typed-select operand scanner now share dropped-pair skipping, exhausted-suffix rejection, operand-start projection, and the zero-input/one-output single-producer fallback instead of each pairing `DaeDroppedOperandPairSkipEvidence` with its own local fallback producer check. This is value/stack-slice recovery consolidation only: it preserves existing single-`if` vs typed-select candidate policies, localization/retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A one hundredth behavior-preserving consolidation added `DaeStackSliceLocalizationPolicy`, `dae_select_condition_localization_policy(...)`, `dae_parametric_typeidx_prefix_localization_policy(...)`, and `dae_stack_slice_localization_evidence_from_policy(...)` as a shared stack-slice localization policy layer above the existing request/evidence carrier. Multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption now project their initial/final stack expectations, branch depth, instruction admissibility, and keep-operand repair intent through reusable policy constructors instead of open-coding request construction at each scanner. This is localization/effect-preservation plus value/stack-slice repair policy consolidation only: it preserves existing select-condition and parametric typeidx instruction policies, true localize-then-retry gaps, convergence scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A one hundred first behavior-preserving consolidation added `dae_apply_first_instruction_rewrite_to_body(...)` as a shared first-matching body repair helper above `DaeIndexedInstructionRewriteApplicationEvidence`. Three Func237 cleanup-repair body scanners now delegate their repeated scan-copy-rewrite loops through that helper: outer-loop carrier wrapping, nested branch-result flattening, and recursive dropped-result branch-carrier folding. This is operand/result repair-application consolidation only: it preserves existing Func237 shape policies, single-instruction rewrite semantics, cleanup scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A one hundred second behavior-preserving consolidation added `dae_try_rewrite_first_control_child_body(...)` as a shared recursive control-child body-rewrite application helper. Func237 dropped-result branch-carrier folding and unreachable-after-if-else folding now delegate block/loop/`if`/`try_table` child reconstruction through the helper instead of each open-coding the same child order and reconstruction cases; the unreachable-after-if-else recursive body scan also uses `dae_apply_first_instruction_rewrite_to_body(...)` for its first matching instruction replacement. This is recursive operand/result repair-application consolidation only: it preserves the existing Func237 shape policies, child traversal order, cleanup scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

A one hundred third behavior-preserving consolidation added `dae_apply_first_current_or_control_child_body_rewrite(...)` as the shared current-body-then-child recursive body-rewrite application helper. Func237 dropped-result branch-carrier folding, unreachable-after-if-else folding, immediate local-copy folding, and used-temp-copy retargeting now delegate their current-body attempt plus first recursive control-child rewrite through the same helper instead of mixing shared child reconstruction with separate body scan/copy loops. This is another operand/result repair-application consolidation only: it preserves existing Func237 shape policies, root-before-child order, child traversal order, cleanup scheduling, direct raw-red status, and the plain-vs-optimizing scheduler split.

Red-first whitebox coverage first failed to compile on the missing helpers, then passed after each extraction: `dae GC select value evidence computes reusable LUB and refined candidates` covers returned-value select evidence, `dae GC select actual evidence centralizes nullable refinement and rewrites` covers argument-side nullable actual evidence, `dae indirect GC result slice evidence centralizes call_ref and call_indirect operands` covers the shared indirect-call operand evidence and effectful-table rejection, `dae direct call GC result evidence centralizes signature and refinement` covers shared direct-call signature/refinement evidence plus the unchanged-result rejection, `dae GC result slot evidence centralizes candidate refinement and rewrite` covers the shared slot-level candidate/refinement/rewrite carrier, `dae select value slices centralize operand and condition bounds` covers select value-slice bounds and required-left-start rejection, `dae control result slices centralize flat and return arm bounds` covers the block-carried and terminal-return multi-result control slice carrier, `dae control result GC evidence centralizes lane candidates and rewrites` covers the per-lane control-slice GC carrier, `dae control result pair GC evidence centralizes terminal arm LUB rewrites` covers paired then/else LUB/refinement and selective rewrites, `dae parametric typeidx arm evidence reuses control result GC carrier` covers the parametric typeidx arm evidence wrapper, `dae GC actual shape evidence centralizes control and select repairs` covers shared argument actual-shape evidence for final control actuals plus nullable typed-select repair hooks, `dae operand disposition centralizes removed operand repair` covers the new shared droppable/effectful range carrier plus removed-operand replay/drop emission, `dae value slices carry reusable operand disposition` covers value slices carrying that disposition while preserving effect-free computed-range purity separately from trivial droppability, `dae callsite value slices expose reusable repair disposition` covers callsite-wide value-slice recovery plus replay/drop repair through the shared disposition carrier, `dae callsite value slice exposes reusable single-instruction evidence` covers the single-instruction actual evidence hook consumed by both uniform-constant and selected global-suffix callsite loops, `dae call boundary facts expose reusable forwarding gates` covers the boundary carrier's escape/tail forwarding gates plus direct-caller lookup, `dae uniform const candidate recorder centralizes merge invalidation` covers the shared candidate recorder's first-candidate, equal-candidate, differing-candidate invalidation, and independent-lane behavior, `dae call boundary candidate centralizes owned callee counts` covers the widened boundary carrier's owned-callee candidate, direct-call count, dropped-call count, direct-caller list, escaped/tail rejection, self-direct-caller rewrite ineligibility, nonself direct-rewrite eligibility, and out-of-range behavior, and `dae uniform const boundary facts carry direct caller evidence` covers the boundary-carrier uniform-constant collection entrypoint over direct-caller evidence, `dae module boundary snapshot carries counts facts and type indices` covers the imported/defined/total, type-index, current-call, and boundary-candidate snapshot entrypoint, and `dae original boundary snapshot carries original counts escapes and call summaries` covers the original-boundary setup carrier, `dae dropped result call rewrite policy centralizes dead suffix handling` covers the shared dropped-result call/drop rewrite traversal with explicit stop-at-unreachable vs anywhere policy, `dae dropped result rewrite scope centralizes selected caller handling` covers selected-vs-all caller scope construction plus scoped undropped-call scanning and scoped call/drop rewriting, and `dae dropped result full scope centralizes unreachable cleanup repair` covers the full-scope dead-suffix repair and dropped-call cleanup helpers used by the remaining unobserved-unreachable/no-param cleanup paths, and `dae original result preservation evidence centralizes shared guards` covers the shared original-vs-current result-preservation carrier consumed by the core and reverse exact-literal loops, `dae dropped result candidate centralizes call facts and owned eligibility` covers the shared dropped-result candidate facts consumed by selected candidate collection and per-def removal attempts, `dae dropped result candidate reuses boundary candidate facts` covers the boundary-based candidate entrypoint over the shared ownership/count/caller carrier, and `dae original result preservation repair gate reuses shared evidence` covers the shared argument-side result-repair gate while preserving the main-lane vs reverse-lane parameterized self-side-chain difference, and `dae dropped result removal legality centralizes result blockers` covers the shared result-removal blocker carrier across result-present selected/full-module cleanup and result-free nonblocking cases, and `dae operand disposition centralizes removed operand repair` now also covers the explicit drop/replay/reject removal action helpers, and `dae dropped result repair plan centralizes dead suffix cleanup` covers all-caller vs no-param repair plan modes plus optional dropped-call-drop cleanup, and `dae callsite value evidence centralizes cursor and operand dispositions` covers the callsite-wide evidence carrier's cursor, argument count, entry-stack context, value slices, and repair dispositions, and `dae callsite single instruction actual evidence centralizes slice lookup` covers the reusable single-instruction actual carrier over that callsite evidence, `dae dropped result module context centralizes section scope and legality facts` covers the shared dropped-result setup carrier including params/results, and `dae original result preservation evidence centralizes shared guards` now also covers the shared preservation-input context carrier, and `dae callsite removal plan centralizes keep replay and drop repair` covers the explicit callsite operand removal-plan carrier after first failing on the missing helper, and `dae control entry stack evidence centralizes nested block inputs` covers the shared nested-control entry/result stack evidence carrier after first failing on the missing helper, and `dae stack operand evidence centralizes source slices and updates` covers the shared top-of-stack operand-window carrier plus bool/int stack operand-source lookup and stack-effect update helpers after first failing on the missing helper names, `dae stack effect evidence centralizes provenance updates` covers the shared instruction stack-effect carrier and bool/int instruction-effect application helpers after first failing on the missing helper names, `dae control entry stack evidence centralizes nested block inputs` now also covers the shared `dae_control_entry_stack_values(...)` projection helper and `dae_control_block_type_signature_evidence(...)` signature carrier after first failing on each missing helper name, `dae control result slices centralize flat and return arm bounds` now also covers the shared `dae_single_block_result_carrier_evidence(...)` single-block carrier after first failing on the missing helper name, `dae control entry stack evidence centralizes nested block inputs` now also covers `dae_nested_control_entry_evidence(...)` after first failing on the missing helper name, and `dae dead suffix evidence centralizes unreachable tails` covers the shared dead-suffix root/tail evidence carrier after first failing on the missing helper name. The forty-eighth slice adds `dae tail call boundary evidence centralizes rewrite and result blockers`, covering shared direct-tail vs indirect-tail boundary facts after first failing on the missing helper name. The forty-ninth slice adds `dae direct call target evidence centralizes count and live undropped facts`, covering shared target-specific direct-call counts, dropped-call facts, undropped uses, and live-prefix blockers after first failing on the missing helper name. The fiftieth slice adds `dae dead suffix direct call evidence centralizes dropped and undropped facts`, covering the shared dead-suffix dropped/undropped direct-call target carrier after first failing on the missing helper name. The fifty-first slice adds `dae dropped result scope direct call evidence centralizes selected caller facts`, covering the scoped caller direct-call count/drop/undropped/live-prefix carrier after first failing on the missing helper name. The fifty-second slice adds `dae original no-param dropped caller guard centralizes indexed facts`, covering the shared no-param dropped-caller original-boundary guard carrier, safe out-of-range defaults, and carrier-level preservation predicate after first failing on the missing helper names. The fifty-third slice adds `dae original parameterized self side-chain guard centralizes indexed facts`, covering the shared parameterized undropped self-side-chain original-boundary guard carrier, safe out-of-range defaults, and carrier-level preservation predicate after first failing on the missing helper names. The fifty-fourth slice adds `dae dropped result callee rewrite evidence centralizes suffix and legality facts`, covering the shared dropped-result callee rewrite carrier for suffix, simplification, params/results, and result-removal blocker evidence after first failing on the missing helper name. The fifty-fifth slice adds `dae escaped result direct call evidence centralizes dropped and undropped suffix facts`, covering the shared dropped/undropped escaped-result direct-call carrier and dead-suffix projection after first failing on the missing helper names. The fifty-sixth slice adds `dae dead suffix direct target evidence centralizes dropped and undropped target lists`, covering the shared direct/dropped/undropped dead-suffix target-list carrier after first failing on the missing helper name. The fifty-seventh slice widens `dae stack effect evidence centralizes provenance updates` with `dae_stack_single_value_producer_evidence(...)`, covering shared zero-input/one-output producer evidence after first failing on the missing helper name. The fifty-eighth slice widens `dae control entry stack evidence centralizes nested block inputs` with `dae_control_single_result_block_type_evidence(...)`, covering shared zero-input/one-output block-type evidence after first failing on the missing helper name. The fifty-ninth slice widens the same test with `dae_nested_control_children_evidence(...)`, covering reusable primary/secondary child-body evidence for nested controls after first failing on the missing helper name. The sixtieth slice widens the same test with `dae_rebuild_nested_control_children(...)`, covering nested-control reconstruction for `try_table` catches and if-with-else shape preservation after first failing on the missing helper name. The sixty-first slice adds `dae multi-result suffix bounds centralize terminal result scans`, covering shared suffix-start/operand-end/terminal-return bounds for multi-result `if` and typed-select operand scanners after first failing on the missing helper name. The sixty-second slice adds `dae stack slice simulation centralizes condition and typeidx scans`, covering shared bounded stack-slice simulation for multi-instruction typed-select conditions and parametric typeidx prefix consumption after first failing on the missing helper name. The sixty-third slice adds `dae escaping branch evidence centralizes localization blockers`, covering reusable depth-based escaping-branch evidence and the no-escape carrier for localization/control gates after first failing on the missing helper names. The sixty-fourth slice adds `dae multi result carrier chain evidence centralizes fallthrough and return slices`, covering shared fallthrough, terminal-return, and appended-return carrier-chain recovery after first failing on the missing helper name. The sixty-fifth slice adds `dae original result preservation row centralizes indexed guard facts`, covering the shared original-boundary row projection, row-level result-preservation evidence, and row-derived no-param dropped-caller plus parameterized self-side-chain guard evidence after first failing on the missing helper names. The sixty-sixth slice adds `dae original result removal prune row centralizes indexed guard facts`, covering the shared original-boundary prune row plus later-mutual and void-side-caller result-removal prune predicates after first failing on the missing helper names. The sixty-seventh slice widens the same test with mixed dropped/undropped caller and dropped-only result-side prune predicates from the row after first failing on the missing fields/helpers. The sixty-eighth slice widens the same test with mixed-self suffix, same-self result-operand, and escaped-block self-result row predicates after first failing on the widened row arity and missing helper names. The sixty-ninth slice widens `dae stack slice simulation centralizes condition and typeidx scans` with `DaeStackSliceTransitionEvidence` / `dae_stack_slice_transition_evidence(...)`, covering shared expected-final-stack acceptance for typed-select condition and parametric typeidx prefix scans after first failing on the missing helper name. The seventieth slice widens `dae escaping branch evidence centralizes localization blockers` with range-aware branch-escape evidence, covering accepted no-escape prefixes, escaping subranges, and invalid ranges after first failing on missing fields and helper names. The seventy-first slice widens the same escaping-branch test with prefix/suffix no-escape projections over the range carrier, covering accepted prefixes, escaping suffix rejection, and invalid prefix bounds after first failing on the missing helper names. The seventy-second slice widens `dae stack slice simulation centralizes condition and typeidx scans` with `DaeDroppedOperandPairSkipEvidence` / `dae_dropped_operand_pair_skip_evidence(...)`, covering reusable dropped trivial-operand pair skipping for multi-result suffix scanners after first failing on the missing helper name. The seventy-third slice widens `dae control result pair GC evidence centralizes terminal arm LUB rewrites` with `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)`, covering shared fallthrough and terminal-return if-pair slice/evidence/LUB/refinement carriers after first failing on the missing helper name. The seventy-fourth slice widens the same test with `DaeControlResultPairRefinementApplicationEvidence` / `dae_apply_control_result_pair_refinements(...)`, covering shared application of paired refined result vectors to direct and parametric typeidx refinement paths after first failing on the missing helper name. The seventy-fifth slice adds `dae control-result undropped direct-call evidence centralizes suffix block scans`, covering shared block/loop/`try_table` and then/else control-result direct-call blocker evidence after first failing on the missing helper name. The seventy-sixth slice adds `dae original dead-suffix side-call evidence centralizes cleanup gates`, covering shared void-side cleanup preservation, recursive result-side chain preservation, and same-self result-operand preservation gates after first failing on the missing helper name. The seventy-seventh slice widens `dae original result preservation evidence centralizes shared guards`, covering shared result-side chain cleanup preservation over no-param dropped-caller, dropped-escaped-result-under-self-caller, and parameterized undropped self-side-chain gates after first failing on the missing evidence field. The seventy-eighth slice widens `dae original result removal prune row centralizes indexed guard facts`, covering `DaeOriginalResultRemovalPruneEvidence` aggregate cleanup/skip blockers after first failing on the missing helper name. The seventy-ninth slice widens `dae escaping branch evidence centralizes localization blockers`, covering `DaeNoEscapeSliceEvidence` copied no-escape ranges after first failing on the missing helper name. The eightieth slice widens the same test with copied prefix/suffix no-escape projections after first failing on the missing helper names. The eighty-first slice widens the same test with `DaeTerminalIfNoEscapeCarrierEvidence` for terminal zero-result `if` carriers, first failing on the missing helper name. The eighty-second slice adds `dae mixed multi-result return arm evidence centralizes LUB and effect-free arms`, covering `DaeMixedMultiResultReturnArmGcEvidence` / `dae_mixed_multi_result_return_arm_gc_evidence(...)` for shared mixed return/fallthrough multi-result arm candidate/LUB rejection and effect-free fallthrough acceptance after first failing on the missing helper name. The eighty-third slice adds `dae single-result control pair GC evidence centralizes LUB rewrites`, covering `DaeSingleResultControlPairGcEvidence` / `dae_single_result_control_pair_gc_evidence(...)` for shared single-result implicit/explicit-return `if` arm LUB and rewritten-body selection after first failing on the missing helper name. The eighty-fourth slice adds `dae mixed return-arm rewrite evidence centralizes refined slot repairs`, covering `DaeMixedMultiResultReturnArmRewriteEvidence` / `dae_apply_mixed_multi_result_return_arm_refinements(...)` for shared mixed return/fallthrough returned-arm refined-slot rewrite application after first failing on the missing helper name. The eighty-fifth slice adds `dae control result single refinement application centralizes next-result repair`, covering `DaeControlResultSingleRefinementApplicationEvidence` / `dae_apply_control_result_single_refinements(...)` for shared single-arm refined-result application after first failing on the missing helper name. The eighty-sixth slice widens `dae escaping branch evidence centralizes localization blockers` with `DaeTerminalNoEscapePrefixEvidence` / `dae_terminal_no_escape_prefix_evidence(...)`, covering shared terminal-instruction plus copied no-escape prefix evidence after first failing on the missing helper name. The eighty-seventh slice adds `dae mixed single-result fallthrough arm evidence centralizes LUB and effect-free arms`, covering `DaeMixedSingleResultReturnArmGcEvidence` / `dae_mixed_single_result_return_arm_gc_evidence(...)` for shared mixed return/fallthrough single-result arm candidate/LUB rejection, optional returned-arm rewrite propagation, and effect-free fallthrough acceptance after first failing on the missing helper name. The eighty-eighth slice adds `dae stack slice localization evidence carries transition escape and repair facts`, covering `DaeStackSliceLocalizationEvidence` / `dae_stack_slice_localization_evidence(...)` for shared transition, no-escape, operand-disposition, and removal-plan facts after first failing on the missing helper name. The eighty-ninth slice adds `dae func237 local copy gap evidence shares immediate and used-temp policy`, covering `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` for shared immediate-copy and used-temp-copy gap range policy after first failing on the missing helper name. The ninetieth slice adds `dae target callsite evidence centralizes direct and return-call matching`, covering `DaeTargetCallsiteEvidence` / `dae_target_callsite_evidence(...)` for direct-only and direct-or-`return_call` target matching after first failing on the missing helper name. The ninety-first slice adds `dae dropped pure leaf cleanup evidence centralizes stack and leaf preservation`, covering `DaeDroppedPureLeafCleanupEvidence` stack/leaf constructors and preservation predicates after first failing on the missing helper names. The one hundred twenty-second slice adds `dae dropped pure leaf subject evidence shares stack and leaf preservation proofs`, covering `DaeDroppedPureLeafSubjectEvidence` stack/leaf constructors plus shared singleton, prefix-effect, integer-const debris, same-set local-get, and effect-after-local-get preservation predicates after first failing on the missing helper names. The ninety-second slice adds `dae single-result control GC evidence centralizes block and loop rewrites`, covering `DaeSingleResultControlGcCandidateEvidence` / `dae_single_result_control_gc_candidate_evidence(...)` for shared block/loop candidate-body recovery and rewrite wrapping after first failing on the missing helper names; the one hundred forty-third slice later routes its reconstruction through the broader single-body control rewrite carrier. The ninety-third slice adds `dae result rewrite application evidence centralizes masked slot repairs`, covering `DaeResultRewriteApplicationEvidence` / `dae_apply_result_rewrites_to_body(...)` for unmasked and active-mask result-slot rewrites plus rejected mask/window bounds after first failing on the missing helper name. The ninety-fourth slice adds `dae select value rewrite application evidence centralizes select repairs`, covering `DaeSelectValueRewriteApplicationEvidence` / `dae_apply_select_value_rewrites_to_body(...)` for left/right/select rewrite slots, unchanged applications, unsorted slot rejection, and out-of-range rejection after first failing on the missing helper name. The ninety-fifth slice adds `dae single-result control pair application centralizes if rewrites`, covering `DaeSingleResultControlPairApplicationEvidence` / `dae_apply_single_result_control_pair_gc_evidence(...)` for value-result and void-result `if` rewrite application after first failing on the missing helper name. The ninety-sixth slice widens `dae stack slice localization evidence carries transition escape and repair facts`, covering `DaeStackSliceLocalizationRequest` / `dae_stack_slice_localization_evidence_from_request(...)` for typed-select condition and parametric typeidx-prefix localization requests after first failing on the missing helper names. The ninety-seventh slice adds `dae single-result control pair repair evidence gathers arm candidates`, covering `DaeSingleResultControlPairRepairEvidence` / `dae_single_result_control_pair_repair_evidence(...)` for shared implicit returned-value and explicit-return zero-result `if` arm candidate gathering, LUB, and rewrite application after first failing on the missing helper name. The ninety-eighth slice adds `dae indexed instruction rewrite evidence centralizes shared body edits`, covering `DaeIndexedInstructionRewriteApplicationEvidence` / `dae_apply_indexed_instruction_rewrites_to_body(...)` for shared sparse body rewrite application, duplicate-target rejection, out-of-range rejection, and rewrite-count/body-changed evidence after first failing on the missing helper name. The ninety-ninth slice adds `dae multi-result operand slot evidence centralizes suffix scan fallback`, covering `DaeMultiResultOperandSlotEvidence` / `dae_multi_result_operand_slot_prefix_evidence(...)` / `dae_multi_result_operand_slot_single_producer_evidence(...)` for dropped-pair prefix projection, exhausted/out-of-range rejection, and single-producer fallback evidence after first failing on the missing helper names. The one hundredth slice widens `dae stack slice localization evidence carries transition escape and repair facts`, covering `DaeStackSliceLocalizationPolicy`, select-condition and parametric typeidx prefix policy constructors, and `dae_stack_slice_localization_evidence_from_policy(...)` after first failing on the missing helper names. The one hundred first slice adds `dae first instruction rewrite evidence centralizes body repair scans`, covering `dae_apply_first_instruction_rewrite_to_body(...)` and its shared indexed-rewrite evidence after first failing on the missing helper name. The one hundred second slice adds `dae control child rewrite helper centralizes recursive body repair`, covering `dae_try_rewrite_first_control_child_body(...)` for then-before-else child rewrites and non-control rejection after first failing on the missing helper name. The one hundred third slice adds `dae recursive body rewrite helper centralizes current and child repair scans`, covering `dae_apply_first_current_or_control_child_body_rewrite(...)` for current-body precedence plus first recursive child-body rewrites after first failing on the missing helper name. The one hundred fourth slice adds `dae recursive body rewrite application evidence centralizes repair planning`, covering current-body evidence, child-body evidence, and control-child-only recursive repair evidence after first failing on missing helper names. The one hundred fifth slice adds `dae body rewrite fixpoint evidence centralizes bounded convergence`, covering bounded body-rewrite convergence evidence after first failing on the missing helper name. The one hundred sixth slice widens `dae recursive body rewrite application evidence centralizes repair planning` with `dae_apply_first_recursive_control_child_body_rewrite(...)`, covering control-child-only recursive body rewrite application after first failing on the missing helper name. The one hundred seventh slice widens the same test with `dae_apply_first_recursive_instruction_body_rewrite_to_body(...)`, covering the evidence-backed instruction-rewrite array projection after first failing on the missing helper name. The one hundred eighth slice widens `dae stack slice localization evidence carries transition escape and repair facts` with copied-slice evidence on `DaeStackSliceLocalizationEvidence` plus `dae_stack_slice_localization_policy_accepts(...)`, covering shared typed-select and parametric typeidx policy acceptance after first failing on missing field/helper names. These are intentionally behavior-preserving consolidations: they do not add a new compare-pass classification, do not change the plain-vs-optimizing scheduler split, and do not make direct DAE green. Direct plain DAE remains raw-red under the saved direct random-all and dedicated-profile evidence until the remaining `dae-effectful-args`, `dae-localization`, `dae-convergence`, and `coverage-forced-portable` families are fixed or fully proved as measured Starshine wins.

Latest red-first whitebox coverage also includes `dae GC actual shape discovery separates target refinement` for direct/control/select shape kinds, candidate-only versus target-refined evidence, nullable select rewrite planning, `require_refined` rejection, and stale-body/wrong-target-type rejection, plus `dae single-body control rewrite evidence centralizes reconstruction` for shared block/loop identity, original-versus-rewritten body selection, refined-valtype-versus-void replacement types, and unsupported-control rejection, plus the widened `dae droppable pure stack evidence centralizes local-free cleanup proof` case for unconditional same-target local-copy repair policy, `dae dropped local tee suffix cleanup evidence centralizes pure suffix rewrites` for shared local-tee/drop and local-tee pure-suffix drop cleanup evidence, `dae redundant access cleanup evidence centralizes adjacent pair rewrites` for shared adjacent local/global cleanup-pair rewrite evidence, `dae local-get range evidence centralizes cleanup local proof` for shared suffix local-get/local-free cleanup range evidence, `dae droppable pure control value evidence centralizes control cleanup proof` for shared type-section-backed pure-control value cleanup evidence, `dae i32-param zero-result loop evidence centralizes typed-loop shape` for shared type-section-backed exact loop signature and iteration-local carrier evidence, `dae control block input evidence centralizes zero-input control checks` for shared zero-input and zero-input/output-count control block proof, `dae control block GC result evidence centralizes single and trailing slots` for shared non-null concrete GC result-slot proof, `dae empty zero-result control cleanup evidence centralizes body drop` for the raw-cleanup empty-control carrier and its widened `if` cleanup evidence, `dae terminal-return body evidence centralizes return-carrier guards` for shared terminal-return body proof under terminal block-carrier and nested unwrapping consumers, `dae effect-free body evidence centralizes carrier and fallthrough guards` for shared side-effect-free body proof under single-block carrier and mixed-arm fallthrough consumers, `dae single-result control GC evidence centralizes block and loop rewrites` for the instruction-level block/loop dispatch helper, `dae escaping branch evidence centralizes localization blockers` for shared copied no-escape slice evidence, and `dae multi-result operand slot evidence centralizes suffix scan fallback` for shared dropped-prefix evidence; each first failed to compile on missing helper names or fields before passing after extraction.

A one hundred fourteenth behavior-preserving consolidation added `DaeEmptyZeroResultControlCleanupEvidence`, `dae_empty_zero_result_control_cleanup_evidence(...)`, and `dae_rebuild_cleaned_single_body_control_or_drop(...)` for raw-cleanup zero-result control body cleanup. The block, loop, and `try_table` arms in `dae_strip_nops_and_dropped_pure_leaf_instrs(...)` now share one type-section-backed zero-input/zero-output empty-body drop decision and one rebuild helper instead of repeating the same child-cleanup plus empty-control predicate in three arms. This is cleanup-evidence consolidation only: it does not move optimizing-only nested cleanup into plain DAE, and direct DAE remains raw-red.

A one hundred fifteenth behavior-preserving consolidation added `dae_single_result_control_gc_candidate_instr_evidence(...)` for instruction-level block/loop direct-GC result candidate dispatch. The `@lib.Block` and `@lib.Loop` arms in `dae_direct_gc_result_candidate_instr(...)` now delegate through one helper over the existing `DaeSingleResultControlGcCandidateEvidence` body recovery and reconstruction path instead of maintaining the same candidate/apply sequence in both arms. The one hundred forty-third consolidation later moved that reconstruction onto the generic single-body control rewrite carrier. This is GC type/control repair consolidation only: it preserves localization/retry gaps, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status.

A one hundred sixteenth behavior-preserving consolidation added `dae_no_escaping_branch_copied_slice_evidence(...)` as the shared copied no-escape slice projection over range-aware branch evidence. Copied range, copied prefix, and copied whole-body helpers now delegate through one accepted-slice carrier instead of each matching no-escape branch evidence and copying accepted instructions separately. This is localization/value-slice evidence consolidation only: it preserves branch-depth policy, accepted-slice copying, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status.

A one hundred seventeenth behavior-preserving consolidation added `dae_trivially_dropped_prefix_evidence(...)` as the shared full-prefix projection over `DaeDroppedOperandPairSkipEvidence`. Typed-select value-slice recovery, flat multi-result result start recovery, flat multi-return result start recovery, multi-result suffix-bound enumeration, and the direct-GC flat result fallback now consume one dropped-prefix carrier instead of a separate boolean-only even-pair scan. This is value/stack-slice recovery consolidation only: it preserves dropped-pair policy, result/refinement behavior, cleanup scheduling, plain-vs-optimizing scheduling, and direct raw-red status.

A one hundred eighteenth behavior-preserving consolidation added `DaeLocalUsageEvidence` plus `dae_local_usage_evidence(...)` / range and single-get-write projections. Func237 immediate-copy and used-temp copy cleanup-localization, local-constant inlining, selected Func311 terminal-call argument rewriting, and high-local trace reporting now consume one recursive local get/write summary instead of paired get/write scanners; the evidence also carries `try_table` presence for future local effect/preservation consumers. This is localization/cleanup evidence consolidation only: it does not add true Binaryen localize-then-retry, broaden plain DAE cleanup scheduling, change optimizing cleanup boundaries, or make direct DAE green.

A one hundred nineteenth behavior-preserving consolidation added `DaeFunc237LocalCopyCandidateEvidence` plus `dae_func237_local_copy_candidate_evidence(...)`. The helper centralizes Func237 local-copy candidate eligibility over `DaeLocalUsageEvidence`: parameter-local rejection, mode-specific temp get/write counts, and root `try_table` preservation are now produced once and consumed by both immediate-copy and used-temp cleanup-localization loops. This removes the duplicate recursive `try_table`-only scanner from the root call-gap path and makes the local-usage `has_try_table` fact an active local-preservation input. It does not add Binaryen-style localize-then-retry, alter plain-vs-optimizing scheduling, rerun compare-pass, or make direct DAE green.

A one hundred twentieth behavior-preserving consolidation added `DaeLocalCopyTargetEvidence` plus `dae_local_copy_target_evidence(...)`. Func237 immediate-copy, used-temp copy, and store-carrier copy cleanup-localization now share the same protected-local target proof for temp/target distinctness and parameter-local rejection, while the store-carrier path also consumes `dae_local_copy_target_evidence_instr_independent(...)` for the preserved operand's temp/target independence. This moves another local-preservation predicate out of the one-off shape scanners without changing cleanup scheduling, adding Binaryen-style localize-then-retry, rerunning compare-pass, or making direct DAE green.

A one hundred twenty-first behavior-preserving consolidation added `DaeDroppablePureStackEvidence` plus `dae_droppable_pure_stack_evidence(...)`. The raw cleanup live-local-copy folding branches now share a single trailing pure-stack proof that records start/end bounds, local-get presence, and the local-free policy instead of repeatedly asking a boolean helper in each copy-chain recognizer. This is cleanup/localization evidence consolidation only: it preserves Binaryen's current rule that local-get-sourced temp copy chains are not stackified, does not add true localize-then-retry, does not move optimizing cleanup into plain DAE, and does not make direct DAE green.

A one hundred twenty-second behavior-preserving consolidation added `DaeDroppedPureLeafSubjectEvidence` plus stack and leaf constructors for raw dropped pure-leaf cleanup. The stack-suffix truncation path and adjacent leaf-plus-`drop` path now share one subject carrier for the dropped instruction, prefix bounds, singleton status, prefix-observable-effect fact, integer-constant debris preservation, local-get-after-same-set preservation, and local-get-after-effect preservation instead of keeping separate stack-vs-leaf predicates. This is cleanup-preservation evidence consolidation only: it preserves existing raw cleanup behavior, does not broaden localization/retry, does not move optimizing cleanup into plain DAE, and does not make direct DAE green.

A one hundred twenty-third behavior-preserving consolidation added `DaeFlatMultiResultSuffixEvidence` plus `dae_flat_multi_result_suffix_evidence(...)`. The multi-result carrier-chain helper now asks one suffix carrier for both flat result tails and terminal-`return` result tails instead of switching between separate flat-start and multi-return-start scanners; the carrier records start/end bounds, result count, and terminal-return state before `DaeControlResultSlice` construction. This is value/stack-slice result-bound recovery consolidation only: it preserves dropped-prefix policy, control-slice behavior, cleanup scheduling, the plain-vs-optimizing split, and direct DAE raw-red status.

A one hundred twenty-fourth behavior-preserving consolidation added `DaeLocalReferenceExtentEvidence` plus `dae_local_reference_extent_evidence(...)`. The recursive local-reference scan now produces one carrier with the maximum referenced local slot and whether any reference crosses a caller-provided limit. The old high-local predicate and max-local helper now project from this carrier, and the selected Func311 high-local trace path consumes the single carrier directly instead of rescanning for the maximum after a high-local hit. This is localization/cleanup-preservation evidence consolidation only: it preserves local trimming policy, selected cleanup scheduling, the plain-vs-optimizing split, and direct DAE raw-red status.

A one hundred twenty-fifth behavior-preserving consolidation added `DaeLiveLocalCopyCleanupEvidence` plus `dae_live_local_copy_cleanup_evidence(...)`. The raw cleanup live-local-copy folding path now asks one replacement-plan carrier for the five-instruction triple-copy, three-instruction double-copy, two-instruction set/get, and tee/set copy-chain rewrites over the existing local-free droppable pure-stack proof instead of open-coding those branches separately. This is cleanup/localization evidence consolidation only: it preserves local-get-sourced copy-chain retention, does not add Binaryen-style localize-then-retry, does not move optimizing cleanup into plain DAE, and does not make direct DAE green.

A one hundred twenty-sixth behavior-preserving consolidation widened `DaeEmptyZeroResultControlCleanupEvidence` with primary/secondary body lengths and body-count evidence plus `dae_empty_zero_result_if_cleanup_evidence(...)`. Raw cleanup now uses the same zero-input/zero-output empty-control drop carrier for block/loop/`try_table` single-body cleanup and empty zero-result `if` condition stripping instead of leaving the `if` arm with an inline zero-result/empty-then/empty-else predicate. This is cleanup-scheduling evidence consolidation only: it preserves current condition stripping, keeps optimizing-only nested cleanup out of plain DAE, reruns no compare lane, and does not make direct DAE green.

A one hundred twenty-seventh behavior-preserving consolidation added `DaeEffectFreeFallthroughEvidence` plus `dae_effect_free_fallthrough_evidence(...)`. Mixed single-result and mixed multi-result return/fallthrough arm scanners now share one side-effect guard for accepting fallthrough arms that have no observable effect or branch, instead of each open-coding `dae_instrs_have_observable_effect_or_branch(...)` rejection before building its own fallthrough record. This is localization/effect-preservation evidence consolidation only: it does not add Binaryen-style localize-then-retry, alter GC LUB behavior, change cleanup scheduling, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred twenty-eighth behavior-preserving consolidation added `DaeEffectFreeBodyEvidence` plus `dae_effect_free_body_evidence(...)`. Single-block multi-result carrier recovery, nested block-carrier unwrapping in direct GC refinement, and mixed return/fallthrough fallthrough evidence now share one side-effect-free body proof instead of calling `dae_instrs_have_observable_effect_or_branch(...)` directly at each body-acceptance site. This is localization/effect-preservation evidence consolidation only: it does not add Binaryen-style localize-then-retry, alter GC LUB behavior, change cleanup scheduling, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred twenty-ninth behavior-preserving consolidation added `DaeTerminalReturnBodyEvidence` plus `dae_terminal_return_body_evidence(...)`. Terminal-return block-carrier recovery in `dae_find_multi_result_control_slice(...)` and nested block-carrier unwrapping in direct GC refinement now share one terminal-`return` body proof instead of using a boolean-only scanner for one path and an inline effect-free-or-terminal-return guard for the other. This is localization/effect-preservation and value/control carrier evidence consolidation only: it does not add Binaryen-style localize-then-retry, alter GC LUB behavior, change cleanup scheduling, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred thirtieth behavior-preserving consolidation added `dae_control_block_type_exact_signature_evidence(...)` plus `DaeI32ParamZeroResultLoopEvidence` / `dae_i32_param_zero_result_loop_evidence(...)`. The typed-loop spill paths for explicit carrier locals and iteration-local carriers now share exact single-`i32`-param/zero-result control signature proof, loop-body length evidence, and optional first-`local.tee` iteration-local evidence instead of independently matching loop block types and first-instruction local carriers. This is control-block-shape/localization evidence consolidation only: it preserves the existing narrow i32-loop spill policy, does not add Binaryen-style localize-then-retry, alter cleanup scheduling, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred thirty-first behavior-preserving consolidation added `DaeDroppablePureControlValueEvidence` plus `dae_droppable_pure_control_value_evidence(...)`. Raw cleanup now shares one type-section-backed zero-input/single-result pure-control value carrier for block/loop/`try_table` bodies and pure value `if` arms, recording condition arity, body lengths, and catch count before projecting into dropped pure-value arity and pure-`if` condition-stripping checks. This removes the separate non-`if` and `if` pure-control scanners while preserving catch-free `try_table` policy, pure-body requirements, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A one hundred thirty-second behavior-preserving consolidation added `DaeLocalGetRangeEvidence` plus `dae_local_get_range_evidence(...)`. Droppable pure-stack cleanup evidence and dropped pure-leaf stack-subject evidence now share one suffix local-get/local-free carrier for start/end bounds, singleton status, and the local-free-or-singleton cleanup allowance instead of independently scanning for local gets and deriving cleanup eligibility. This is raw cleanup/local-preservation evidence consolidation only: it preserves local-get-sourced copy-chain retention, dropped pure-leaf singleton cleanup behavior, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A one hundred thirty-third behavior-preserving consolidation added `DaeRedundantAccessCleanupEvidence` plus `dae_redundant_access_cleanup_evidence(...)`. Raw cleanup now shares one adjacent access-pair rewrite carrier for local-get/local-set removal, local-get/local-tee skipping, global-get/global-set removal, and local-tee/local-set replacement instead of keeping four neighboring pair recognizers inside the cleanup loop. This is localization/effect-preserving raw cleanup consolidation only: it preserves the existing pair rewrites, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A one hundred thirty-fourth behavior-preserving consolidation added `DaeDroppedLocalTeeSuffixCleanupEvidence` plus `dae_dropped_local_tee_suffix_cleanup_evidence(...)`. Raw cleanup now shares one local-tee suffix replacement carrier for direct `local.tee`/`drop`, `local.tee` plus non-trapping unary-drop, single non-trapping binary-suffix drop, and paired non-trapping binary-suffix drop rewrites instead of keeping separate neighboring suffix scanners in the cleanup loop. This is localization/effect-preserving raw cleanup consolidation only: it preserves the existing `local.set` replacement, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A one hundred thirty-fifth behavior-preserving consolidation widened `DaeLiveLocalCopyCleanupEvidence` with an explicit `requires_local_free_stack` policy fact. The existing helper now also owns the unconditional redundant `local.set x; local.get x; local.set x` collapse, while distinct-target copy stackification still requires the shared local-free droppable-stack evidence. The raw cleanup loop no longer keeps a separate same-target three-instruction scanner and invokes the shared repair-plan carrier even when touched-function stackification is disabled. This is localization/effect-preserving raw cleanup consolidation only: it preserves the same-target `local.set` result, local-get-sourced copy retention, touched-function cleanup gating, plain-vs-optimizing scheduling, direct raw-red status, and optimizing cleanup boundaries.

A one hundred thirty-sixth behavior-preserving consolidation added `DaeDroppedPureExpressionCleanupEvidence` plus `dae_dropped_pure_expression_cleanup_evidence(...)`. The raw cleanup loop now asks one adjacent-expression repair-plan carrier to recognize and skip pure leaf-plus-`drop`, leaf/unary-plus-`drop`, and two-leaf/binary-plus-`drop` ranges instead of retaining three sequential scanners. The carrier records source bounds, next cursor, operand/operator counts, cleanup family, and whether the existing dropped-leaf preservation policy applies; it preserves effect-adjacent integer leaf debris, the existing unary/binary cleanup rules, and the local-get-left binary bailout. This is localization/effect-preserving raw cleanup consolidation only: plain-vs-optimizing scheduling, direct raw-red status, cleanup scheduling, and optimizing cleanup boundaries are unchanged.

A one hundred thirty-seventh behavior-preserving consolidation added `DaeSelectConditionLocalizationEvidence` plus `dae_select_condition_localization_evidence(...)`. The carrier preserves the prior singleton-vs-multi-instruction acceptance split: singleton conditions record trivial-droppability and optional zero-input/single-result producer evidence, while longer ranges carry the existing policy-backed `DaeStackSliceLocalizationEvidence` with copied no-escape slice, transition, disposition, and keep-operand repair facts. `dae_find_select_value_slices(...)`, `dae_gc_select_actual_value_slices_evidence(...)`, and the returned-value GC select path now consume the shared evidence helper instead of re-running a boolean condition gate, and the old singleton/boolean wrappers are removed. This is value/stack-slice and localization evidence consolidation only; it does not widen accepted condition shapes, add scratch-local localize-then-retry, alter GC LUB/refinement, change convergence or cleanup scheduling, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred thirty-eighth behavior-preserving consolidation widened `DaeSelectValueSlices` to retain the already-proven `DaeSelectConditionLocalizationEvidence` and renamed the returned-value GC entrypoint to `dae_gc_select_value_slices_evidence(...)`. Single-result and multi-result typed-select refinement now pass the recovered carrier instead of decomposing it and rescanning condition bounds. The multi-result path also replaces four parallel left/right/condition/select index arrays with one optional `DaeSelectValueSlices` array, preserving the relation between operands, condition evidence, and select instruction through candidate discovery and repair. Red-first whitebox coverage requires both singleton and multi-instruction condition carriers and exercises GC LUB/refinement through the shared slices entrypoint. This remains behavior-preserving value/stack-slice and GC-evidence consolidation: accepted shapes, LUB/refinement, repair output, plain-vs-optimizing scheduling, cleanup scheduling, compare evidence, and direct raw-red status are unchanged.

A one hundred thirty-ninth behavior-preserving consolidation added `DaeGcSelectValueSlicesEvidence` plus `dae_gc_select_value_slices_candidate_evidence(...)`, pairing recovered select slices with their already-computed candidate/LUB/refinement and rewrite evidence. Single-result returned-value select refinement and multi-result typed-select operand discovery now share the discovery-plus-GC entrypoint. The multi-result path retains the complete carrier per result lane and consumes it during rewrite instead of storing slices and recomputing GC candidate evidence from the original body. Red-first whitebox coverage first failed on the missing carrier entrypoint, then passed with a multi-instruction condition while asserting both recovered bounds and GC LUB/refinement. This remains behavior-preserving value/stack-slice and GC-evidence threading: accepted shapes, LUB/refinement, indexed rewrite ordering, plain-vs-optimizing scheduling, cleanup scheduling, compare evidence, and direct raw-red status are unchanged.

A one hundred fortieth behavior-preserving consolidation replaced the raw `left_idx` / `right_idx` / `condition_start` / `select_idx` interface of argument-side typed-select GC evidence with `dae_gc_select_actual_value_slices_evidence(...)`. The then-combined `dae_gc_actual_shape_evidence(...)` recovered the existing condition-carrying `DaeSelectValueSlices`, requires the historical singleton left/right actuals, and receives one `DaeGcSelectValueSlicesEvidence` carrier containing both shape and GC candidate/refinement/rewrite facts. Candidate collection and refined-actual repair still traverse independently and may use different old/rewrite target types, but both now consume the same reusable shape abstraction rather than reconstructing condition evidence from raw indices. Red-first whitebox coverage failed on the missing carrier entrypoint, then passed with a multi-instruction stack-local condition and nullable `ref.null` rewrite evidence. This remains behavior-preserving value/stack-slice and GC-evidence consolidation: accepted operand shapes, GC LUB/refinement, callsite repair, plain-vs-optimizing scheduling, cleanup scheduling, compare evidence, and direct raw-red status are unchanged.

A one hundred forty-first behavior-preserving consolidation stopped flattening that argument-side `DaeGcSelectValueSlicesEvidence` back into raw left/right rewrite fields on the then-combined `DaeGcActualShapeEvidence`. That carrier retained an optional complete select branch before the one hundred forty-fourth slice separated shape discovery from target-specific type evidence, and `dae_rewrite_refined_gc_actual_slice(...)` delegates its left/right/select edits to `dae_apply_select_value_rewrites_to_body(...)`. This makes argument-side typed-select repair the third production consumer of the shared indexed select-rewrite application path, after single-result returned-value repair and flat multi-result typed-select operand repair, and removes the direct `slice.start`, `slice.start + 1`, and `slice.end - 1` select edit sequence. Candidate collection and rewrite traversal still recompute target-dependent evidence separately, so no body-indexed carrier crosses a mutation phase or assumes the same old/rewrite target type. Red-first whitebox coverage first failed because `DaeGcActualShapeEvidence` had no retained `select` branch, then passed with singleton left/right values and a three-instruction stack-local condition. Accepted shapes, GC LUB/refinement, effect handling, convergence, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status remain unchanged.

A one hundred forty-second behavior-preserving consolidation added `DaeAdjacentParamForwardEvidence` plus `dae_adjacent_param_forward_evidence(...)` for the exact adjacent `local.get 0 .. local.get n-1` operand range ending at a call index. Self-forwarded local-use suppression, self-call origin handling, and dead-suffix dropped-self-call full-forward detection now share that target-independent shape proof instead of maintaining three copies of the same prefix loop. Target-callee matching stays in the callers, dead-suffix detection still requires the recovered range to begin after the unreachable root, zero-parameter handling remains caller-specific, and ignored-local marking covers exactly the recovered operand range. Red-first whitebox coverage first failed because the evidence helper was missing, then passed for a valid nonzero-offset range, wrong/reordered locals, insufficient prefix, empty-prefix compatibility, exact ignored-range marking, and the dead-suffix floor. This is ownership/call-boundary and value-range consolidation only; it does not widen accepted forwarding shapes, change result/GC repair, alter convergence, import optimizing cleanup into plain DAE, rerun compare-pass, or make direct DAE green.

A one hundred forty-third behavior-preserving consolidation added `DaeSingleBodyControlShapeEvidence` and `DaeSingleBodyControlRewriteEvidence` plus `dae_single_body_control_shape_evidence(...)`, `dae_single_body_control_rewrite_evidence(...)`, and `dae_apply_single_body_control_rewrite_evidence(...)`. Explicit-return block/loop repair and refined single-result GC block/loop repair now share control-kind recovery, original versus rewritten body selection, replacement block-type ownership, and block/loop reconstruction instead of maintaining separate void and refined-valtype wrappers. `DaeSingleResultControlGcCandidateEvidence` now retains the generic rewrite carrier beside its candidate type, while paired-arm `if` repair stays on the existing pair-specific path. The explicit-return consumer still emits no rewrite for an already-void unchanged control, preserving the prior changed-instruction contract. Red-first whitebox coverage first failed because the new helpers were unbound, then passed for block and loop identity, unchanged and rewritten body selection, refined-valtype and void replacement types, unsupported `if` and non-control rejection, and both production consumers. This is GC/result repair reconstruction consolidation only; zero-input and candidate/refinement gates, accepted shapes, effects, localization/retry, convergence, cleanup scheduling, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.

A one hundred forty-fourth behavior-preserving consolidation replaced the combined argument-side actual carrier with `DaeGcActualShapeKind`, `DaeGcActualShapeDiscoveryEvidence`, and `DaeGcActualTypeEvidence`. `dae_gc_actual_shape_discovery_evidence(...)` now identifies direct, block, loop, `if`, or typed-select shape without an old or rewrite target type; `dae_gc_actual_type_evidence(...)` re-reads the current body and computes candidate, refinement, nullable-select LUB, and rewrite planning for the current target. Candidate collection and refined-actual rewriting each perform fresh discovery plus target evaluation through `dae_gc_actual_value_slice_shape_evidence(...)`, so body-indexed select evidence never crosses mutation phases and wrong-target or changed-terminal evidence rejects instead of being reused. Red-first whitebox coverage first failed because the new helpers and shape kinds were unbound, then passed for direct/control/select distinction, candidate-only evidence, `require_refined` rejection, nullable select rewrite planning, and stale-body/wrong-target-type avoidance. This is behavior-preserving value/stack-slice and GC-evidence phase separation only; accepted shapes, effects, localization/retry, convergence, cleanup scheduling, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status are unchanged.

A one hundred forty-fifth behavior-preserving consolidation replaces the removal-only `DaeOperandRemovalAction` with `DaeOperandRewriteAction` and `DaeOperandRewriteDisposition`. The shared carrier explicitly distinguishes direct drop, effect-preserving replay plus value drop, localize-and-retry eligibility, and escaping-branch rejection. `dae_callsite_operand_removal_plan(...)` now derives and consistency-checks a direct-removal disposition before replay/drop emission, while `dae_stack_slice_localization_evidence_from_request(...)` carries the same disposition after stack-transition and no-escape proof; typed-select condition and parametric typeidx prefix consumers therefore share the localization decision instead of treating a keep-operand removal plan as localization evidence. `DaeOperandRemovalPlan` now records `requires_localization_retry` separately from direct rejection, so localize-and-retry is not silently collapsed into replay/drop. Red-first whitebox coverage first failed because the new disposition and consistency helpers were unbound, then passed for pure drop, effectful replay/drop, localize-and-retry, escaping-branch rejection, repair-plan consistency, and both production paths. This still does not allocate scratch locals or perform the retry; accepted shapes, repair output, convergence, cleanup scheduling, plain-vs-optimizing scheduling, compare evidence, and direct raw-red status remain unchanged.

A one hundred forty-sixth behavior-preserving consolidation adds `dae_apply_body_rewrite_until_stable_or_original(...)` as the unchanged-body projection over `DaeBodyRewriteFixpointEvidence` / `dae_apply_body_rewrite_until_stable(...)`. The three Func237 immediate-local-copy, used-temp-copy, and store-carrier-copy cleanup loops now use that shared bounded driver with their existing `32`, `64`, and `8` iteration limits, while Func256 mapped-loop-condition repair uses the evidence-returning helper directly with its existing limit of `4`. This removes four scanner-local convergence loops without changing their one-rewrite-at-a-time recognizers, limits, rewrite ordering, or no-change return contracts. Red-first whitebox coverage first failed because the projection helper was unbound, then passed for changed, unchanged, and iteration-limited results; focused Func237/Func256 coverage, `moon test src/passes` (`4429/4429`), `moon info`, and full `moon test` (`7868/7868`) passed. The module-boundary scheduler, cleanup selection, plain-vs-optimizing split, compare evidence, and direct raw-red status remain unchanged.

A one hundred forty-seventh behavior-widening consolidation turns the four-outcome operand disposition into a reusable call-boundary localization application. The now-superseded `DaeLocalizedCallsiteRewriteEvidence` (renamed by slice 161 to `DaeCallsiteRewriteApplicationEvidence`), `DaeCallsiteLocalizationContext`, `dae_localization_scratch_type(...)`, and `dae_rewrite_callsite_with_localization_at(...)` consume recovered `DaeCallsiteValueSliceEvidence`, reject escaping removed operands, emit each proven hard removed operand into a defaultable typed scratch local, and aggregate which old parameters were localized. Recursive callsite repair threads one context through block/loop/`if`/`try_table` traversal for both direct `call` and `return_call`; caller locals are extended once per real scratch write, and `dae_build_local_map(...)` now preserves corresponding removed callee slots as body locals so local numbering and Binaryen output shape remain coherent. Non-null reference parameter types are widened only for scratch declaration defaultability. Red-first whitebox coverage first failed because the application helper was unbound, then passed for typed scratch writes, localized-parameter evidence, defaultable reference scratch types, kept operands, and escaping-branch rejection. Public plain-DAE direct and `return_call` tests prove effect/trap order, surviving arguments, scratch/callee local reconstruction, validation, and that the plain pass still stops without the optimizing cleanup suffix. Focused/full validation passed. The direct `dae-localization` and `dae-effectful-args` singleton profiles are now each normalized-green at `10000/10000` with zero mismatches or failures in `.tmp/pass-fuzz-dae-localization-10000-scratch-local-callee-projection-20260709` and `.tmp/pass-fuzz-dae-effectful-args-10000-scratch-local-callee-projection-20260709` (Binaryen cache `10000/0` each, no normalizers, `reduceMismatches=false`). The refreshed direct aggregate `.tmp/pass-fuzz-dae-direct-closeout-10000-after-scratch-localization-20260709` remains raw-red at `5000/10000` normalized and `5000` mismatches, now only `dae-unused-params=1875`, `dae-constant-args=1875`, `dae-convergence=625`, and `dae-tail-call-boundary=625`. The refreshed direct random-all lane remains raw-red at `9207/10000` normalized and `793` mismatches; the normalized `dae-optimizing` random-all rerun remains unchanged at `9538/10000` normalized and `462` mismatches. This fixes two focused direct leaves and reduces direct raw-red evidence; it does not make either direct aggregate lane green.

A one hundred forty-eighth behavior-preserving consolidation adds `DaeRemovedParamLocalProjectionEvidence` plus `dae_removed_param_local_projection_evidence(...)` as the shared decision for whether a removed parameter disappears entirely or survives as a typed body local. The core candidate rewrite, `dae_build_local_map(...)`, selected Func323 global-suffix rewrite, and selected Func3737 wrapper-suffix rewrite now consume the same written/localized/type projection instead of separately deciding which old slots and local types to retain. Unread, unwritten, unlocalized parameters are deliberately elided; written parameters retain their declared type; localized non-defaultable references retain the nullable scratch declaration required by Wasm local defaultability. Red-first whitebox coverage failed while the helper was unbound, then passed for all three outcomes. The focused convergence fixture now also locks that the wrapper and leaf each converge without an unread replacement local and validates the module. This consolidates four production consumers without changing the bounded module scheduler, callsite localization behavior, cleanup selection, plain-vs-optimizing split, or raw-red compare counts.

The same projection makes the refreshed `dae-convergence` residual strongly classifiable rather than an open phase-convergence gap. Exhaustive analysis in `.tmp/dae-convergence-residual-classification-20260709.json` covers all `625` dedicated and `60` random-all convergence mismatches: both tools perform the same two-step leaf-then-wrapper boundary convergence and differ only by Binaryen's two generic unread replacement locals. Tagged Binaryen `ParamUtils::removeParameters(...)` adds a local for every removed param before rewriting remaining gets; Starshine omits the slot only when there is no surviving write or localization requirement. Removing those two Binaryen local declarations makes every canonical WAT pair equal. All size deltas favor Starshine with no positive cases, full Node replay reports `685/685` equal results with zero failures, and an alternating output benchmark is runtime-neutral within noise (`4.713` versus `4.735 ns/call` medians). Agent classification: the generated convergence family is a measured Starshine-win local-declaration cleanup. Direct DAE remains raw-red because the classified output-shape mismatches are intentionally retained.

A one hundred forty-ninth correctness and consolidation slice adds `DaeInstructionObservabilityEvidence` as the shared recursive carrier for observable effects, branch control, conservative trap/unknown status, and pre-unreachable erasability. Existing effect-only and effect-or-branch production consumers now project from that carrier, while `dae_simplify_to_unreachable_when_prefix_unobservable(...)` additionally requires no escaping branch and either a known nontrapping instruction or an explicit constant-safe integer-div/rem or float-to-int conversion proof. Red-first whitebox coverage failed while the carrier was unbound, and a public plain-DAE fixture plus a pre-change native replay proved that the old scanner erased a reachable `i32.div_s` zero-divisor trap; the implemented path retains the trap while still removing the unused parameter and dead post-unreachable suffix. Self-contained depth-zero `br_table` blocks remain erasable, but `return`, escaping branches, side effects, unknown traps, zero divisors, signed overflow, and out-of-range conversions fail closed. Plain and optimizing scheduling remain distinct.

The same proof closes the prior `coverage-forced-portable=259` classification gap without normalizing away raw evidence. `.tmp/dae-coverage-forced-portable-classification-20260710.json` exhaustively covers all `259` refreshed direct random-all cases and `936` defined-function prefixes. Every prefix has one identical numeric instruction sequence, no preterminal call/load/store/memory/table/global/allocation/exception/atomic effect, constant-safe nonzero integer div/rem operands, finite in-range float-to-int inputs, and only a self-contained `br_table 0 0` before the following `unreachable`. Binaryen retains that pure prefix; Starshine removes it and reaches the same terminal trap. Every size delta favors Starshine (`-1288603` raw, `-1291704` canonical, `-8864535` WAT aggregate; zero positive cases). Runtime replay was attempted for all `259` cases and was blocked by unsupported imports in every case, so no unsupported-runtime result is presented as semantic proof. An import-free reduced output probe instead validated both outputs and, over 21 alternating rounds, measured median Node compile time `1205.46 ns` Binaryen versus `1007.45 ns` Starshine (`-16.43%`) and caught-trap call time `5441.45 ns` versus `2872.66 ns` (`-47.21%`). Starshine pass-local timing on representative case `000487` was `1928us` median across 101 traced runs; Binaryen exposes no comparable pass-local timer here, so no optimizer-speed claim is made. Agent classification: the full generated coverage residual is a source/diff/trap-proofed, size- and output-performance-measured Starshine win. Current direct aggregate/random-all lanes remain intentionally raw-red at `5000/10000` and `9207/10000`; all five current direct residual families are now strongly classified rather than normalized away.

A one hundred fiftieth behavior-preserving consolidation adds `DaeOriginalResultBoundaryContext`, `DaeOriginalResultBoundaryRow`, and `DaeOriginalResultBoundaryEvidence`. The main core now indexes the shared original parameter/call/self/escape/dead-suffix facts once into `DaeOriginalResultPreservationRow`, derives `DaeOriginalResultRemovalPruneRow` from that row plus the prune-only facts, and computes both preservation and prune evidence together. Unobserved-unreachable cleanup, zero-call dead-suffix/result-side cleanup, zero-parameter dropped-result removal, and current-parameter argument-side result repair all consume the same per-definition boundary projection; the parameterized self-side-chain gate also projects from the same preservation row instead of re-indexing the context. Red-first whitebox coverage failed while the boundary context/row/evidence helpers were unbound, then passed with shared-field equality and out-of-range fail-closed defaults. Focused DAE tests, `moon test src/passes` (`4436/4436`), `moon info`, full `moon test` (`7875/7875`), and the native build passed. Fresh dedicated direct, direct random-all, and normalized optimizing random-all lanes retained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families and zero failures. This removes a production parallel-array indexing surface without changing any result-removal predicate, mutation ordering, plain-vs-optimizing scheduling, or raw-red reporting.

A one hundred fifty-first behavior-preserving consolidation adds `DaeOriginalDeadSuffixEscapedResultCallFacts` and `dae_collect_original_dead_suffix_escaped_result_call_facts_with_escaped(...)`. One module traversal now computes both dropped and undropped escaped-result calls in dead suffixes, replacing two whole-module collectors and the undropped-only predicate wrapper. The shared fact carrier is threaded through the main core and reverse exact-literal core; dropped facts feed preservation/result-removal decisions, while undropped facts feed unobserved, zero-call, and reverse-rewrite gates, so the abstraction has multiple honest production consumers rather than merely joining `call` and `return_call` arms. Red-first whitebox coverage failed while the collector was unbound, then passed for mixed dropped/undropped evidence and no-code fail-closed defaults. DAE whitebox passed `167/167`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4437/4437`, `moon info`, full `moon test` `7876/7876`, and the native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all counts remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families and zero failures. Plain-vs-optimizing scheduling, result-removal policy, mutation order, and explicit direct raw-red reporting are unchanged.

A one hundred fifty-second behavior-preserving consolidation adds `DaeOriginalEscapedResultOperandCallFacts` and `dae_collect_original_escaped_result_operand_call_facts_with_escaped(...)`. The original setup now reuses the slice-151 dropped escaped-result suffix facts and performs one caller traversal to collect both escaped-caller dead-suffix operand counts and private-result caller counts gated by dropped escaped-result callees. This replaces two whole-module operand collectors and removes the private collector's duplicate callee suffix scan. The main core receives one carrier instead of parallel arrays; preservation and reverse setup project the dead-suffix counts, while unobserved and zero-call result-side decisions also consume the private-result counts. Red-first whitebox coverage failed while the combined collector was unbound, then passed for both fact families and no-code defaults. DAE whitebox passed `168/168`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4438/4438`, `moon info` with 9 warnings, full `moon test` `7877/7877`, and the native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all counts remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families, zero failures, reduction disabled, and Binaryen cache `10000/0`. Result policy, mutation order, cleanup scheduling, plain-vs-optimizing scheduling, and explicit direct raw-red reporting are unchanged.

A one hundred fifty-third behavior-preserving consolidation adds `DaeOriginalResultEscapeRow` to `DaeOriginalResultBoundaryRow`. The row projects two escape-only original arrays—private-result escaped-operand call counts and undropped escaped-result suffix flags—beside six exact facts already owned by `DaeOriginalResultPreservationRow`. Both the unobserved-unreachable decision and the zero-call/uncalled-result decision now consume this one per-definition original-phase row instead of independently indexing the same arrays and preservation facts. Their branch-specific predicates remain separate, current direct-call/body facts remain current, and dropped versus undropped escaped-result suffix ownership stays explicit. Red-first whitebox coverage failed because the boundary context did not accept the two arrays and the row had no `escape` field, then passed with shared-field equality, distinct dropped/undropped assertions, and out-of-range fail-closed defaults. DAE whitebox passed `168/168`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4438/4438`, `moon info` with 9 warnings, full `moon test` `7877/7877`, and the native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all counts remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families, zero failures, reduction disabled, and Binaryen cache `10000/0`. Result-removal policy, mutation order, cleanup scheduling, plain-vs-optimizing scheduling, and explicit direct raw-red reporting are unchanged.

A one hundred fifty-fourth behavior-preserving consolidation adds `DaeOriginalResultSideCallRow` to the same per-definition boundary. The row projects three side-call-only original arrays—other-call presence plus recursive void-side and result-side call flags—beside preservation-owned result-other and mutual-call facts. The existing `dae_original_dead_suffix_side_call_evidence(...)` helper now consumes the row, while zero-call no-self, private-result-side-chain, unreachable-param-cleanup, and uncalled-result predicates reuse its fields. The same zero-call branch now reads original parameter, call, self, active nonself, parameterized nonself/result, nonself-undropped, private-result dead-suffix, and dropped-self facts from the existing preservation/prune rows instead of directly indexing their source arrays. Prior-call counts, private-result same-operand counts, escaped-undropped suffix counts, parameterized dropped-self-caller counts, current body simplification, current self calls, and current module call counts remain explicit because they are phase-specific or lack a second honest owner. Red-first whitebox coverage first failed because the boundary context still accepted 10 rather than 13 arguments and the row had no `side_call` field, then passed for shared-field equality and fail-closed defaults. Full Moon validation and fresh compare lanes retained `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families and zero failures. This removes avoidable zero-call parallel-array extraction without changing policy, mutation ordering, cleanup selection, or the plain-vs-optimizing split.

A one hundred fifty-fifth behavior-preserving consolidation adds `DaeDroppedResultUnreachableCalleeRewritePlan`, `dae_dropped_result_unreachable_callee_rewrite_plan(...)`, and `dae_apply_dropped_result_unreachable_callee_rewrite(...)`. The unobserved-unreachable, dead-self-suffix-result, and uncalled-result branches retain their distinct current/original legality predicates, but now share the same post-decision application: selected/full dead-suffix repair, dropped-call `drop` cleanup, empty-local unreachable callee replacement, function-type update, module reconstruction, and touched/primary result. Each consumer still selects all-caller versus no-param-caller repair and whether parameters survive. Red-first whitebox failed while the plan/application helpers were unbound, then passed for caller repair, callee rewrite, signature removal, and result metadata. Validation passed DAE whitebox `169/169`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4439/4439`, `moon info` with 9 warnings, full `moon test` `7878/7878`, and native build. Fresh direct aggregate, direct random-all, and normalized optimizing random-all lanes stayed exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families, zero failures, reduction disabled, and Binaryen cache `10000/0`. This is shared module-boundary application machinery with three honest production consumers; it does not change result legality, mutation order, cleanup selection, raw-red reporting, or the plain-vs-optimizing scheduler boundary.

A one hundred fifty-sixth behavior-preserving consolidation extracted the smaller honest overlap between selected append-`drop` result removal and the full-module unreachable-callee application. Its result-specific `DaeDroppedResultModuleFinalizationPlan`, touched policy, and finalizer names were later superseded by slice 158's generic module code-finalization layer; at this slice they owned function-signature replacement, type/function/code section reconstruction, name stripping, primary-definition metadata, and explicit touched accounting. Selected removal uses `DaeTouchDroppedResultChangedFuncs` so every rewritten caller plus the callee remains scheduled; unreachable-callee removal uses `DaeTouchDroppedResultCalleeOnly`, preserving its existing callee-only mask. The two paths still diverge before finalization: selected removal verifies exact rewritten-call count, removes adjacent call `drop`s before no-param repair, appends `drop` to the existing callee body, simplifies it, and retains params; the unreachable path repairs first, then installs empty-local `unreachable` with branch-selected params. Red-first whitebox failed while the finalization plan/application were unbound, then passed for rewritten bodies, signature removal, changed-caller touched marking, and primary definition. Validation passed DAE whitebox `170/170`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4440/4440`, `moon info` with 9 warnings, full `moon test` `7879/7879`, native build, and three fresh 10000-case compare lanes. Counts remain exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families, zero failures, reduction disabled, and Binaryen cache `10000/0`. This shares result-boundary finalization without merging selected/full legality, repair timing, callee-body policy, or plain-vs-optimizing scheduling.

A one hundred eighty-fourth consolidation closed the concrete pre-wrapper Func3737 value-slice gap without weakening the general callsite slicer. At that historical checkpoint, `dae_try_rewrite_verified_materialized_const_suffix_candidate(...)` verified exact adjacent materialized suffix operands through a count-checked direct-caller scope while preserving ambient stack provenance.

**Superseded on 2026-07-22.** Verified immediate suffixes and selected ref-null compatibility producers now build and apply the canonical `DaeRewritePlan`, including original ownership, current epochs, exact caller/count identity, callee projection, signature/type finalization, producer effects, module validation, and rollback. The former `dae_apply_removed_param_candidate_after_caller_scope(...)` and standalone `dae_apply_removed_param_callee_rewrite(...)` tails are deleted. The original slice-184 tests remain provenance; current exposed-boundary and scalar-finalization tests exercise the live transaction.

Scanner-heavy surfaces that remain after these slices include broader parametric typeidx/control returned-value recognizers beyond the terminal arm carrier, repair application beyond the shared four-outcome operand rewrite disposition, consistency-checked callsite removal plan, and typed scratch-local application, especially whole-argument-segment localization/reload, broader value-slice rewriting, and explicit boundary rescheduling after a localize-and-retry decision; exact-type caller scratch reuse is implemented, while cross-type or pre-existing-local borrowing remains intentionally unsupported. Paired-arm repair beyond the fallthrough/terminal-return GC, single-result control-pair LUB, mixed multi-result return/fallthrough arm candidate/LUB, mixed single-result return/fallthrough arm candidate/LUB, mixed returned-arm rewrite application, shared paired refined-result application, and shared single-arm refined-result application carriers, result-preservation control-result direct-call blocker evidence beyond the first dead-suffix block/loop/`try_table`/`if` carrier, original-boundary side-call/result-preservation cleanup policy beyond the first shared dead-suffix side-call gate and result-side chain cleanup summary carriers, argument-side actual-shape repair beyond the separated direct/control/select discovery carrier, current-target GC evaluation, retained condition-carrying select slices-plus-GC evidence, and shared indexed select-rewrite application, broader candidate/rewrite carriers beyond the returned-value and argument-side select slices-plus-GC records, single-instruction actual-evidence subsets beyond the callsite evidence helper now consumed by uniform-constant and selected global-suffix loops, direct-call target policy beyond the first `DaeTargetCallsiteEvidence` helper now consumed by self-forwarded local-use suppression, refined-GC actual rewriting, parameter-removal callsite rewriting, recursive uniform-constant collection, and direct `struct.new_default` actual collection, adjacent forwarded-operand shape beyond the exact full-parameter `DaeAdjacentParamForwardEvidence` range now shared by local-use suppression, self-call origin handling, and dead-suffix full-forward detection, control/block-type signature context beyond the first `DaeControlEntryStackEvidence` carrier, `dae_control_entry_stack_values(...)` projection helper, `DaeNestedControlEntryEvidence` instruction-level control-entry carrier, `DaeNestedControlChildrenEvidence` primary/secondary body carrier now consumed by uniform-constant and direct `struct.new_default` actual collection plus `dae_rebuild_nested_control_children(...)` now consumed by refined-GC actual rewriting and callsite removal, `DaeMultiResultSuffixBoundsEvidence` now consumed by the multi-result single-`if` and typed-select operand suffix scanners, `DaeFlatMultiResultSuffixEvidence` now consumed by the multi-result carrier-chain helper for shared flat and terminal-return result-tail proof, `DaeDroppedOperandPairSkipEvidence` now consumed by both suffix scanners for dropped trivial-operand pair skipping plus `dae_trivially_dropped_prefix_evidence(...)` now consumed by typed-select recovery, flat/terminal result-suffix recovery, suffix-bound enumeration, and direct-GC flat result fallback, `DaeMultiResultOperandSlotEvidence` now consumed by both suffix scanners for dropped-pair prefix projection and single-producer fallback evidence, `DaeRedundantAccessCleanupEvidence` now consumed by adjacent local/global access-pair cleanup for local-get/local-set, local-get/local-tee, global-get/global-set, and local-tee/local-set rewrites, `DaeDroppedLocalTeeSuffixCleanupEvidence` now consumed by raw-cleanup local-tee/drop, unary-suffix drop, single binary-suffix drop, and paired binary-suffix drop rewrites, `DaeDroppedPureLeafCleanupEvidence` plus `DaeDroppedPureLeafSubjectEvidence` now consumed by both raw-cleanup dropped-stack-suffix truncation and adjacent leaf-plus-`drop` stripping to share dropped-instruction, prefix-effect, integer-const, and local-get preservation gates, `DaeLocalReferenceExtentEvidence` now consumed by high-local cleanup guards, max-local projection, and the selected Func311 high-local trace path to share recursive local-reference extent proof, `DaeSingleBodyControlShapeEvidence` and `DaeSingleBodyControlRewriteEvidence` now consumed by explicit-return and refined single-result GC block/loop repair to share control identity, body selection, replacement type, and reconstruction while `DaeSingleResultControlGcCandidateEvidence` retains candidate-specific facts, `DaeResultRewriteApplicationEvidence` now consumed by flat control-result GC evidence, paired control-result LUB evidence, mixed returned-arm repair, and mixed terminal-result repair to share masked rewrite-window application, `DaeSelectValueRewriteApplicationEvidence` now consumed by single-result returned-value select repair, flat multi-result typed-select operand repair, and argument-side refined-actual repair to share left/right/select rewrite application, `DaeSingleResultControlPairApplicationEvidence` now consumed by implicit returned-value and explicit-return zero-result `if` refinement paths to share value/void `if` rewrite application, `DaeSingleResultControlPairRepairEvidence` now consumed by those same paths to share arm candidate gathering, LUB, and rewrite application, `DaeIndexedInstructionRewriteApplicationEvidence` now consumed by masked result-slot and all three typed-select rewrite application paths to share sparse indexed body-edit accounting and bounds/duplicate-target rejection, `DaeStackSliceSimulationEvidence` plus `DaeStackSliceTransitionEvidence`, the combined `DaeStackSliceLocalizationEvidence` carrier with copied no-escape slice evidence, the request-backed `DaeStackSliceLocalizationRequest` layer, `DaeStackSliceLocalizationPolicy` projection, and `dae_stack_slice_localization_policy_accepts(...)` now consumed by multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks, plus `dae_apply_first_instruction_rewrite_to_body(...) now consumed by first-matching sparse body-edit application paths, `dae_try_rewrite_first_control_child_body(...)` now consumed by Func237 cleanup-repair recursive child scanners for shared block/loop/`if`/`try_table` child-body reconstruction, and `dae_apply_first_current_or_control_child_body_rewrite(...)` now consumed by four Func237 cleanup-repair paths for shared current-body-then-control-child recursive application, range-aware `DaeEscapingBranchEvidence` plus `DaeNoEscapeSliceEvidence` now consumed by branch-escape predicates, prefix/suffix projections, copied accepted-range/prefix/suffix evidence, terminal no-escape prefix evidence, and selected localization/control no-escape gates, `DaeControlBlockTypeSignatureEvidence` resolver now consumed by callsite removal/refined-GC/uniform-constant/direct-GC recursion, block-type stack effects, and parametric terminal-control refinement, and `DaeSingleBlockResultCarrierEvidence` now consumed by multi-result block-carrier flattening through the shared `dae_multi_result_control_carrier_chain_slice(...)` fallthrough/terminal/appended-return helper, stack-provenance scanning beyond the first operand-window, instruction stack-effect, single-value producer, and block-type single-result carriers plus bool/int operand lookup and stack-effect update helpers now consumed by original dead-suffix/result-preservation, value-slice recovery, typed-select condition, indirect-call result-slice, and multi-result refinement scanners, dead-suffix legality beyond the first shared unreachable-tail evidence carrier, dead-suffix direct-call target carrier, dead-suffix direct target-list carrier, and escaped-result direct-call carrier, direct-vs-indirect tail-call boundary blocker evidence, target-specific direct-call count/undropped/live-prefix evidence, and scoped dropped-result direct-call legality evidence, the now-shared dropped-result call/drop traversal policy, explicit selected/full repair plan mode, selected/full-module dropped-result context carrier, and three-consumer unreachable-callee rewrite application, broader original-boundary escaped-result classification beyond the shared dropped/undropped call and escaped-result operand carriers, broader boundary candidate construction beyond forwarding gates, boundary-carried uniform-constant entry collection, owned-callee count/caller/self-recursion eligibility, the first module boundary snapshot, the first original-boundary snapshot plus shared result-preservation guards, no-param dropped-caller guard, parameterized self-side-chain guard, preservation-input context, per-definition preservation row, result-removal prune row plus dropped-only/mixed result-side and mixed-self/same-operand/escaped-block predicates plus the first aggregate prune-evidence cleanup/skip blockers, and argument-side repair gate, dropped-result legality beyond the first boundary-based candidate carrier, shared result-removal blocker evidence, all/no-param repair-plan carrier, selected/full-module dropped-result context carrier, and dropped-result callee rewrite evidence carrier, remaining dropped-result result-removal policy, broader localization/retry for hard operands, module-core phase convergence scheduling beyond the now-shared local body-rewrite fixpoint driver, and optimizing-only cleanup selection. The next consolidation target should start from source review and select the smallest duplicated original-vs-current boundary, result-removal legality, control/typeidx evidence scanner, or frontier/touched result-application surface with at least two honest production consumers. Do not force whole-argument localization/reload or explicit frontier scheduling merely to create an abstraction; reopen them only when two real consumers or a newly exposed parity gap require them. Keep optimizing-only cleanup out of plain DAE.

## Validation plan when the port starts

Use [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md) as the implementation ladder: analyzer-only first, scalar direct-call parameter deletion second, then constant actuals, GC refinements, dropped-result removal, localization, and plain-vs-optimizing sibling split checks.

Use the existing Binaryen dossier as the behavior checklist:

- [`./binaryen-strategy.md`](./binaryen-strategy.md) for phase order, data ownership, and plain-vs-optimizing split.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md) for the upstream owner-file and proof-surface map.
- [`./wat-shapes.md`](./wat-shapes.md) for beginner-friendly positive and negative cases to convert into tests.
- [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md) for Starshine's first safe slices, exact code surfaces, and Binaryen oracle lanes.
- [`../dae-optimizing/starshine-strategy.md`](../dae-optimizing/starshine-strategy.md) for the optimizing-only scheduler delta.

Concrete tests should cover at least:

- known-boundary dead-param removal;
- recursive direct-call cycles whose incoming param is never otherwise used;
- all-callers-same-constant materialization;
- GC parameter and result refinement;
- export / import / `ref.func` bailouts;
- hard operand localization and retry;
- dropped-return removal and tail-call bailouts;
- exact local spelling behavior for `dead-argument-elimination`, `dae`, `dae-optimizing`, and `dead-argument-elimination-optimizing`.

## Current non-goals

- Do not claim Starshine has Binaryen `dae` parity until required direct/profile compare lanes are green and reviewed; focused Moon tests, direct regular GenValid, and post-fix direct wasm-smith are green, but direct DAE dedicated/random-all lanes remain raw-red and focused owned-direct `struct.new_default`, static `local.get` result, typed-block-operand argument, zero-param, parametric, and multi-result typeidx block/if/loop operand argument, typed-select operand argument including typed-call result operands, branch-free effectful call, and bounded branchful non-escaping `if` conditions, nullable `ref.null` select LUB, and nullable typed-select common-LUB, typed-block-body result, two-caller argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select LUB, multi-result typed-select operand LUB including a non-tail-lane select-first/scalar-second shape, non-tail-lane drop-only parametric typeidx block/if/loop returned-value refinement, explicit-return `if` returned-value LUB, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, zero-param typeidx block body, zero-param typeidx terminal `if`, fallthrough typed/typeidx loop, zero-param multi-result fallthrough loop, simple multi-result `if`-arm returned-value refinement, and flat implicit/explicit plus droppable-prefix/simple-and-nested block-carrier/simple loop-carrier/simple `if`-arm/tail/leading/explicit-return/multiple/interleaved-debris single-result `if` operands/typed-select operands/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value refinement subsets of live GC argument/result refinement are implemented; broader arbitrary unification remains open.
- Do not treat `dae2` as a test bucket for plain DAE; it is a separate upstream pass.
- Do not merge this page into the optimizing sibling: the whole point is to keep the shared boundary core and the optimizing-only nested rerun split readable.
