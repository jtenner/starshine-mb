---
kind: entity
status: supported
last_reviewed: 2026-07-09
sources:
  - ../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md
  - ../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md
  - ../../../raw/research/0435-2026-05-04-dead-argument-elimination-current-main-recheck.md
  - ../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md
  - ../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md
  - ../../../raw/research/0293-2026-04-24-dead-argument-elimination-primary-sources-and-starshine-followup.md
  - ../../../raw/research/0159-2026-04-21-dead-argument-elimination-binaryen-research.md
  - ../../../raw/research/0230-2026-04-21-dead-argument-elimination-implementation-followup.md
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/dead_argument_elimination.mbt
  - ../../../../../src/validate/gen_valid.mbt
  - ./fuzzing.md
  - ../../no-dwarf-default-optimize-path.md
  - ../tracker.md
  - ../../../../../agent-todo.md
related:
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ./starshine-strategy.md
  - ./starshine-port-readiness-and-validation.md
  - ../dae-optimizing/index.md
  - ../signature-pruning/index.md
  - ../tracker.md
---

# `dead-argument-elimination`

## Role

- `dead-argument-elimination` is an upstream Binaryen boundary pass.
- The public upstream CLI alias is `dae`.
- It now has an active Starshine implementation surface in [`../../../../../src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt) and is wired as a module pass under both `dead-argument-elimination` and the upstream alias `dae`. Focused Moon tests for the direct-pass registry/dispatcher path are green, but direct DAE compare lanes still have many raw mismatches and are not signed off.
- The current repo no-DWARF default optimize path uses the related later pass `dae-optimizing`, not this plain variant.
- The two passes share the same core engine in upstream `version_129`; `dae-optimizing` is the plain DAE algorithm plus one extra nested cleanup rerun.

## Why it matters

- Even though plain `dae` is outside the current no-DWARF parity queue, it is already a named local registry entry, so it is a real future implementation surface rather than speculative upstream trivia.
- The existing `dae-optimizing` dossier is easier to understand once plain DAE has its own home.
- This pass teaches the part that is easiest to blur together:
  - the core boundary rewrite algorithm itself,
  - versus the extra scheduler behavior that the optimizing suffix adds.
- `agent-todo.md` now tracks the plain DAE profile/direct-pass work inside `[O4Z-AUDIT-DAE]`, while this dossier remains the durable place to explain the non-optimizing pass separately from `dae-optimizing`.

## Beginner summary

A good beginner mental model is:

- gather all direct callers of a function,
- refuse boundaries that can escape or be observed indirectly,
- tighten argument and result types when all known calls agree,
- turn always-the-same actuals into local callee constants,
- delete dead parameters and dead dropped returns,
- localize hard operands and iterate if necessary,
- then stop.

That last point is the big difference from `dae-optimizing`: **plain DAE does not do the extra post-change cleanup rerun**.

## Current durable takeaways

- Plain DAE is a **whole-module direct-call boundary pass**, not a hot local cleanup pass; the direct `dead-argument-elimination` / `dae` registry and dispatcher entries now route to the shared core with the optimizing suffix disabled.
- It can do more than delete unused parameters:
  - constant actual materialization,
  - GC parameter refinement,
  - result type refinement,
  - dropped-return elimination,
  - operand localization and retry.
- Exports and `ref.func` escapes count as unseen calls, so Binaryen refuses signature-changing rewrites there.
- Tail calls and tail-callee relationships still matter for dropped-return legality, but owned direct `return_call` sites can participate in unused-parameter signature/operand repair when every observable caller is known.
- The implementation is iterative and can perform a localization-only round so a later removal becomes legal.
- The plain pass shares the same upstream file and much of the same lit neighborhood as `dae-optimizing`, but it deliberately omits the final `optimizeAfterInlining(...)` rerun.
- There is no single dedicated plain `dae.wast`; the real proof surface is split across `dae_tnh.wast`, `dae-gc.wast`, `dae-gc-refine-params.wast`, and `dae-gc-refine-return.wast`, with neighboring optimizing files acting as sibling-boundary evidence rather than the main plain-pass oracle.
- The 2026-05-04 current-main recheck found no teaching-relevant drift from the tagged source story, and it keeps the future Starshine implementation ladder explicit: analyzer-only candidate classification first, scalar direct-call parameter deletion second, and constant actual / GC refinement / dropped-result / localization families later.
- The Starshine status/readiness pages now include a first source-reviewed DAE-A through DAE-O implementation-status matrix. That matrix is not closeout evidence: it classifies many scalar/call-boundary families as source-covered/focused-green but direct-compare-open, records that the owned-direct `struct.new_default` subset of live GC argument/result refinement plus focused static `local.get` result, typed-block-operand argument, zero-param typeidx block/if/loop and typed-select operand argument including typed-call result operands, branch-free effectful call, plus bounded branchful non-escaping `if` conditions, nullable common-LUB including nullable `ref.null` select LUB, typed-block-body result, zero-param typeidx block body and terminal `if` result, fallthrough typed/typeidx loop result, zero-param multi-result fallthrough loop result, simple multi-result `if`-arm result, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm result, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands in multi-result bodies, two-caller direct-call argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, multi-result typed-select operand plus block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets are fixed, records the 2026-07-05 exact-reference refinement follow-up that makes allocation-derived signatures match Binaryen in the profile lanes, records the 2026-07-06 typed-block operand/result, argument-LUB, terminal returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, multi-result typed-select operand plus block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, and explicit-return returned-value LUB follow-ups, and records the 2026-07-05 focused direct `return_call` unused-parameter repair, and records the 2026-07-07 scanner-consolidation extractions where `DaeGcSelectValueEvidence` / `dae_gc_select_value_slice_evidence(...)` now shares typed-select GC evidence between single-result and multi-result result-refinement scanners, `dae_gc_select_actual_value_evidence(...)` shares nullable typed-select actual evidence between candidate collection and callsite repair, `dae_indirect_call_gc_result_slice_evidence(...)` shares `call_ref` / `call_indirect` result-slice operand evidence, `dae_direct_call_gc_result_signature_evidence(...)` / `dae_direct_call_gc_result_refinement_evidence(...)` share direct-call result signature and old-result refinement evidence between argument-side and returned-value scanners, `DaeGcTypeEvidence` / `dae_gc_result_slot_evidence(...)` shares candidate/refinement/rewrite evidence across flat multi-result, single-`if` operand, and parametric typeidx control result slots, `DaeValueSlice` / `dae_find_select_value_slices(...)` now shares typed-select operand/condition bound recovery between single-result and flat multi-result returned-value scanners, `DaeGcActualShapeEvidence` / `dae_gc_actual_shape_evidence(...)` shares final actual-shape candidate/refinement/repair evidence across direct/control actual candidates and typed-select callsite repair, and `DaeOperandDisposition` / `dae_operand_disposition_from_range(...)` centralizes droppable/effect/branch range classification plus callsite removed-operand replay/drop repair. Later 2026-07-05 diff/source reviews classify sampled residual dirs as Starshine-win local/temp/effect-drop cleanup, transitive producer-return cleanup, or multi-result tuple/local-spill avoidance. A follow-up red-first GenValid slice superseded the two-result `dae-unused-returns` trigger with a single-result DAE-F positive leaf: the focused leaf is normalized-green at 100 cases; the direct aggregate is raw-red at 100 (`32` normalized / `68` mismatches), 1000 (`312` normalized / `688` mismatches), and closeout-scale 10000 (`3125` normalized / `6875` mismatches) with no new failing leaf families; and the optimizing sibling dedicated profile is green at 10000 cases. The optimizing sibling also has green regular GenValid at 100000 and post-imported-tag-fix explicit wasm-smith zero-mismatch evidence (`9956` compared, `9955` normalized, `1` cleanup-normalized, `44` Binaryen/oracle command failures); the imported-tag fix repairs DAE type pruning for function types referenced only by imported tags. Paired probes show direct DAE plus `vacuum` keeps the same raw local/temp residuals at the older 1000-case scale, while `dae-optimizing` plus `vacuum` and `dae-optimizing + inlining-optimizing + remove-unused-module-elements` are green at 10000; `dae-optimizing + inlining-optimizing` alone remains raw-red at 10000 because it needs the surrounding RUME cleanup, with Starshine retaining a private unreachable helper/type that Binaryen deletes. Broader arbitrary type-refinement (including local value-flow beyond static local type evidence, richer typeidx block/if/loop boundaries with params/control flow beyond the new drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load terminal returned-value subsets, mixed return/fallthrough beyond the simple single-result and flat multi-result void-if arm subsets, select beyond the bounded typed two-value single-result plus nullable-null/branchful-if-condition argument, block/pure/call-condition/call-post-op-condition/memory-condition/branchful-if-condition, non-tail-lane multi-result select-first/scalar-second, drop-only/effectful parametric typeidx block/if/loop terminal controls, and flat multi-result suffixes, and non-flat multi-result carriers beyond the current droppable-prefix flat tail and simple/nested block-carrier, simple loop-carrier, simple `if`-arm, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands, simple/nested block-carrier `if`-arm, and simple/nested/void-block explicit-return block-carrier `if`-arm subsets), direct raw-red dedicated/random-all, non-profile ordered scheduler/integration, and final closeout lanes remain open, though a 2026-07-06 optimizing-profile full post-pass suffix lane (`dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize`) normalized `10000/10000` with zero failures; direct regular GenValid is green at closeout scale (`.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705` normalized `100000/100000` with zero failures), and the previous direct wasm-smith 10000 size-losing `case-009332` unreachable-control-debris mismatch is fixed by red-first raw dropped-unreachable cleanup (`.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705` requested `10000`, compared `9956`, normalized `9956`, mismatches `0`, `44` Binaryen/oracle command failures). Direct random-all remains raw-red: the pre-fix 500 lane had `443` normalized / `57` known direct/coverage cleanup mismatches, the post-fix 500 rerun timed out without `result.json`, the post-fix 100-case smoke had `87` normalized / `13` mismatches, the post-fix 200-case smoke `.tmp/pass-fuzz-dead-argument-elimination-random-all-200-post-debris-cleanup-20260705` had `176` normalized / `24` known direct/coverage cleanup mismatches, and the post-fix 300-case lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-300-post-debris-cleanup-20260705` had `262` normalized / `38` known direct/coverage cleanup mismatches with validating artifacts. A subsequent `dae-optimizing` random-all retry (`.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705`) also timed out without `result.json` after `1037` records (`1035` compared, `985` normalized, `50` mismatches, `2` Starshine command failures). The mismatches stayed in the prior cleanup/scheduling classes (`coverage-forced-portable`, `dae-localization`, `dae-effectful-args`), but the command failures exposed a tail-call result-mismatch blocker: red-first focused fixtures now cover both the direct-tail subset and an unreachable-suffix tail-call subset, native CLI replays of `case-000433` and `case-000434` now succeed, and compare replays no longer command-fail. Both replay mismatches are sampled `coverage-forced-portable` pure/unreachable cleanup with deletion-only canonical WAT diffs and validating artifacts. A completed bounded post-tail random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-500-post-tail2-20260705` requested/compared `500/500`, normalized `474`, mismatched `26`, and had zero failures; a higher `.tmp/pass-fuzz-dae-optimizing-random-all-1000-post-tail2-20260705` lane requested/compared `1000/1000`, normalized `952`, cleanup-normalized `0`, mismatched `48`, and had zero failures. Mismatches stayed in known cleanup/scheduling classes (`coverage-forced-portable=30`, `dae-effectful-args=9`, `dae-localization=9`) with validating raw/canonical artifacts and aggregate Starshine size wins. A completed 1282-case lane `.tmp/pass-fuzz-dae-optimizing-random-all-1282-post-tail2-20260705` requested/compared `1282/1282`, normalized `1227`, cleanup-normalized `0`, mismatched `55`, and had zero validation/generator/property/command failures; mismatches stayed in the same cleanup/scheduling classes with validating artifacts. The ordinary 2000-case retry `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-20260705` timed out after 7200s without `result.json` during mismatch reduction, but the no-reduction follow-up `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` completed full random-all requested/compared `10000/10000`, normalized `9538`, mismatched `462`, had zero validation/generator/property/command failures, and kept all mismatches in the classified cleanup/scheduling classes (`coverage-forced-portable`, `dae-effectful-args`, `dae-localization`) with validating artifacts. Direct random-all also now has full raw-red evidence in `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705`: requested/compared `10000/10000`, normalized `9004`, mismatched `996`, zero failures, known direct/coverage cleanup families only. A 2026-07-06 stratified runtime replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706` sampled one saved mismatch from each residual class: six import-free direct DAE leaf samples had equal Node results, the `coverage-forced-portable` sample was import-blocked for runtime, all seven replays had zero validation/generator/property/command failures, and WAT/source review kept the sampled classes in measured cleanup territory rather than new semantic gaps. A follow-up red-first simple function-signature reuse fix removes the convergence raw-positive size caveat in saved-mismatch replays (`dae-convergence=60` direct random-all is now `-240` raw/canonical; `dae-convergence=625` dedicated-profile is now `-2500` raw/canonical), but both replay sets remain raw mismatches. Later red-first typed-block operand and typed-block result fixtures pass and replays over the saved direct random-all mismatches still leave `996/996` raw mismatches with zero failures, confirming that the known raw-red families are not hidden type-refinement misses. A cleanup-normalized replay of the same saved set (`.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-cleanup-normalized-20260706`) also left `996/996` raw mismatches with zero cleanup-normalized matches and zero failures while validating all `4980` input/raw/canonical wasm artifacts; the residual aggregate is measured Starshine-smaller (`-593155` raw / `-1294602` canonical / `-8900446` WAT total), but it is still not raw-green. A direct `dead-argument-elimination + remove-unused-module-elements` replay over those residuals (`.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`) also stayed fully raw-red (`996/996` mismatches, zero failures), with Starshine-smaller aggregate canonical/WAT output but `59` raw-positive `coverage-forced-portable` cases. 2026-07-07 residual analyses now fully classify three saved direct families without making direct DAE green: `.tmp/dae-tail-call-boundary-residual-classification-20260707.json` covers all `63` `dae-tail-call-boundary` direct random-all and post-tail-trim DAE+RUME residuals where Binaryen keeps one unused replacement local after removing the same unused parameter and pure `return_call` operands while Starshine omits that local (`-126` raw/canonical and `-1071` WAT bytes in each lane, no positive cases, representative Node-equal case `104`), `.tmp/dae-unused-params-residual-classification-20260707.json` covers all `214` `dae-unused-params` residuals where Binaryen keeps two unused replacement locals while Starshine omits them (`-428` raw/canonical and `-7276` WAT bytes in each lane, no positive cases, representative Node-equal case `20`), and `.tmp/dae-constant-args-residual-classification-20260707.json` covers all `197` `dae-constant-args` residuals where Binaryen materializes the uniform `i32.const 77` replacement through a fresh helper local while Starshine substitutes the same pure constant directly (`-1182` raw/canonical and `-10835` WAT bytes in each lane, no positive cases, representative Node-equal case `7`). A targeted follow-up on the largest raw-positive sample, `case-007094`, stayed a raw mismatch with zero failures, was runtime-blocked by imports under Node, validated its five input/raw/canonical wasm artifacts, and measured `+1006` raw but `-4123` canonical / `-28236` WAT bytes for Starshine; scanning all `59` raw-positive coverage cases under `.tmp/dae-raw-positive-analysis-20260706` quantified the subset as raw-positive (`+30761` total, `+348..+1006` per case) but all canonical/WAT-negative (`-186785` canonical / `-1280032` WAT total), with no scanned pre-terminal side-effecting keywords in input/Binaryen/Starshine raw WAT. A follow-up source review ties the first terminal to `gen_valid_append_core_control_ops()` in the coverage prelude before later effectful coverage templates, and a bounded `wasm-tools shrink` attempt on `case-007094` accepted the raw-positive/canonical-negative original but found no smaller module in 200 attempts (`41526` bytes, `0.00%` reduction); this supports but does not prove a pure-prefix/unreachable-tail cleanup classification for that subset. Direct DAE remains raw-red overall; `dae-optimizing` has its required random-all lane but the broader audit remains open on direct raw-red families, returned-value LUB/type-refinement breadth, and broader no-DWARF ordered scheduling/integration. The 2026-07-06 simple/nested block-carrier, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, interleaved droppable-debris single-result `if` operands, bounded typed-select multi-result operands, pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select operands, non-tail-lane multi-result typed-select operands, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop terminal returned values, and explicit-return block-carrier multi-result `if`-arm plus simple mixed single- and multi-result return/fallthrough `if` slices added focused source-backed coverage only and did not rerun compare-pass; direct DAE remains raw-red. A 2026-07-06 non-profile regular-GenValid full-suffix attempt is explicitly raw-red (`.tmp/pass-fuzz-dae-optimizing-full-postpass-context-regular-genvalid-10000-20260706`: `2015/10000` compared before max-failure cutoff, `0` normalized, `2015` mismatches, zero failures), and a no-DAE suffix control also mismatched `1000/1000`, so this is currently broader post-pass/portable-oracle output-shape drift rather than DAE-owned green evidence.
- The latest scanner-consolidation slice adds `DaeStackSliceLocalizationRequest` / `dae_stack_slice_localization_evidence_from_request(...)`, so multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption now share one request-backed localization evidence path for range, stack expectation, branch-depth, instruction-policy, and keep/remove repair intent. This is behavior-preserving value/stack-slice localization consolidation only; plain-vs-optimizing scheduling is unchanged and direct DAE remains raw-red.
- The first 2026-07-04 audit slice adds pass-owned GenValid profile plumbing: aggregate `dead-argument-elimination` with aliases `dae`, `dae-all`, and `dae-closeout`; focused leaves for unused params, effectful args, constant args, unused returns, argument/return type refinement, unseen-call boundary, tail-call boundary, localization, and convergence; plus the separate `dae-optimizing` profile/closeout alias. Follow-up template slices give `dae-unused-params` a deterministic private-helper/direct-caller module, `dae-effectful-args` a mutable-global side-effect producer whose result is passed as an unused helper actual, `dae-constant-args` a same-second-constant direct-caller module, `dae-unused-returns` a private single-result helper whose direct callers immediately drop the helper result, `dae-arg-type-refinement` and `dae-return-type-refinement` small GC modules with an empty base struct plus final leaf subtype for parameter/result type tightening, `dae-tail-call-boundary` a private unused-parameter-looking helper reached only through `return_call`, `dae-unseen-call-boundary` a private unused-parameter-looking helper escaped through `ref.func` in an active element segment plus an indirect caller, `dae-convergence` a private wrapper/leaf helper chain where pruning the leaf's unused parameter makes the wrapper's forwarded parameter removable on a later iteration, `dae-localization` a private unused-parameter-looking helper whose unused actual is a value-producing block around an effectful producer call, and `dae-optimizing` a five-function helper/caller module where a removable unused param unlocks a touched helper body with identity-add cleanup debris. This slice also promotes plain `dead-argument-elimination` and `dae` from boundary-only names to active module-pass registry/dispatcher entries with focused pipeline tests added. The aggregate now has concrete templates for all ten direct leaves; the optimizing sibling has a nested-cleanup trigger; focused Moon validation is green; direct DAE compare counters remain mismatch-heavy while the small `dae-optimizing` profile smoke is green. See [`./fuzzing.md`](./fuzzing.md).

## Page map

- [`./binaryen-strategy.md`](./binaryen-strategy.md)
  Deep dive into the actual Binaryen `version_129` implementation: phases, data structures, helper dependencies, legality gates, and the exact plain-vs-optimizing split.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md)
  Source-confirmed file/test map for `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `lubs.h`, `type-updating.h`, `return-utils.h`, and the split plain-vs-optimizing-vs-`dae2` lit surface.
- [`./wat-shapes.md`](./wat-shapes.md)
  Beginner-friendly before/after shape catalog for the main positive, bailout, preserved, and easy-to-misunderstand rewrite families.
- [`./starshine-strategy.md`](./starshine-strategy.md)
  Starshine status and port strategy, now refreshed with the 2026-07-07 scanner-consolidation inventory plus shared typed-select returned-value, argument-actual GC, indirect-call result-slice, direct-call result signature/refinement, flat GC result-slot, typed-select `DaeValueSlice` bound-recovery, multi-result `DaeControlResultSlice` arm/control bound-recovery, per-lane `DaeControlResultGcEvidence`, paired terminal-arm `DaeControlResultPairGcEvidence`, shared fallthrough/terminal-return if-pair GC refinement evidence via `DaeMultiResultIfPairGcRefinementEvidence`, parametric typeidx arm GC-evidence extraction, `DaeGcActualShapeEvidence` actual-shape candidate/repair extraction, `DaeOperandDisposition` callsite removed-operand repair classification, `DaeValueSlice`-carried operand disposition, callsite-wide `DaeValueSlice` collection/GC actual evidence, value-slice single-instruction actual evidence, `DaeCallBoundaryFacts` forwarding gates plus the `dae_collect_uniform_const_actuals_from_boundary_facts(...)` entrypoint for uniform-constant call-boundary traversal, `dae_record_uniform_const_candidate(...)` candidate merge/invalidation, `DaeCallBoundaryCandidate` owned-callee count/caller and self-recursion rewrite-eligibility evidence, `DaeModuleBoundarySnapshot` imported/defined/type-index/current-call boundary summaries consumed by core and selected rewrite loops, `DaeOriginalBoundarySnapshot` original imported/defined/escape/param/call-summary setup facts consumed by the core, the `dae_rewrite_dropped_result_calls_with_policy_instrs(...)` dropped-result call/drop repair traversal policy, `DaeDroppedResultScope` selected-vs-all plus full-module dropped-result repair scoping, `DaeDroppedResultCandidate` boundary/current-call dropped-result candidate facts, and `DaeOriginalResultPreservationEvidence` / `dae_original_result_preservation_evidence_from_row(...)` original-vs-current result-preservation guard sharing, plus the 2026-07-08 `dae_dropped_result_candidate_from_boundary_facts(...)` follow-up that routes dropped-result candidate eligibility through `DaeCallBoundaryFacts` / `DaeCallBoundaryCandidate`, the `dae_original_result_preservation_blocks_argument_side_result_repair(...)` follow-up that shares argument-side result-repair blocking over the original result-preservation carrier, and the `DaeDroppedResultRemovalLegalityEvidence` follow-up that shares result-count-aware tail-call / undropped-call / large-result cleanup blockers across selected and full-module dropped-result cleanup loops, the `DaeOperandRemovalAction` follow-up that makes disposition decisions explicit as drop/replay/reject for callsite and value-slice repair, the `DaeOperandRemovalPlan` / `dae_callsite_operand_removal_plan(...)` follow-up that centralizes per-callsite keep/replay/drop repair planning over the callsite evidence carrier, and the `DaeDroppedResultRepairPlan` follow-up that carries all-caller vs no-param dead-suffix repair plus optional dropped-call-drop cleanup decisions across selected and full-module dropped-result repair paths, and the `DaeCallsiteValueSliceEvidence` follow-up that carries call index, argument count, entry-stack context, cursor, recovered value slices, and repair dispositions through callsite removal/refinement/constant/GC/global-suffix loops, plus the `DaeCallsiteSingleInstructionActualEvidence` follow-up that centralizes single-instruction actual lookup over that callsite carrier for uniform-constant and selected global-suffix loops, and the `DaeDroppedResultModuleContext` follow-up that centralizes imported/type/function section setup, params/results, selected/full dropped-result scope, result-removal legality, and selected-lane setup timing for selected dropped-result removal plus unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup paths, and the `DaeOriginalResultPreservationContext` follow-up that centralizes original result-preservation input arrays for the main core zero-param/current-param lanes and reverse exact-literal lane, plus the `DaeOriginalResultPreservationRow` follow-up that centralizes per-definition original-boundary guard fact projection for result-preservation, no-param dropped-caller, and parameterized self-side-chain evidence, and the `DaeOriginalResultRemovalPruneRow` follow-up that centralizes later-mutual, void-side-caller, mixed dropped/undropped caller, dropped-only result-side, mixed-self suffix, same-self result-operand, and escaped-block self-result predicates, and the `DaeOperandRemovalPlan` follow-up that shares explicit callsite removed-operand repair plans between callsite rewriting and trivial-droppable checks, plus the `DaeControlEntryStackEvidence` / `dae_control_entry_stack_values(...)` follow-up that centralizes nested control entry/result stack context and child entry-stack projection for callsite removal, refined-GC actual, uniform-constant actual, and direct-GC actual recursive loops, the `DaeNestedControlEntryEvidence` / `dae_nested_control_entry_evidence(...)` follow-up that carries instruction-level block/loop/`try_table`/`if` entry-stack facts through the same recursive loops, and the `DaeControlBlockTypeSignatureEvidence` / `dae_control_block_type_signature_evidence(...)` follow-up that centralizes block-type params/results/input/output/typeidx evidence for block-type stack effects, control-entry evidence, and parametric typeidx terminal-control refinement, the `DaeSingleBlockResultCarrierEvidence` / `dae_single_block_result_carrier_evidence(...)` follow-up that centralizes effect-free single-block multi-result carrier evidence for block flattening, block-plus-`return`, and nested carrier unwrapping inside `dae_find_multi_result_control_slice(...)`, and the `dae_stack_bool_*` / `dae_stack_int_*` helper follow-up that centralizes original dead-suffix/result-preservation stack operand-source lookup and push/pop effect updates across bool/int provenance scanners, plus the `DaeDeadSuffixEvidence` / `dae_dead_suffix_evidence(...)` follow-up that centralizes unreachable-root and suffix-tail recovery across dead-suffix repair, target/count, escaped-result, self-call-param, and side-call scanners, the `DaeTailCallBoundaryEvidence` / `dae_tail_call_boundary_evidence(...)` follow-up that centralizes direct-vs-indirect tail-call blocker facts for direct-call rewrite legality and dropped-result removal legality, the `DaeDirectCallTargetEvidence` / `dae_direct_call_target_evidence(...)` follow-up that centralizes target-specific direct-call counts plus dropped, undropped, and live-prefix undropped-call facts for containment/count/legality scanners, the `DaeDeadSuffixDirectCallEvidence` / `dae_dead_suffix_direct_call_evidence(...)` follow-up that centralizes dead-suffix dropped/undropped target facts for full-module dropped-result cleanup gates, the `DaeDeadSuffixDirectTargetEvidence` / `dae_dead_suffix_direct_target_evidence(...)` follow-up that centralizes direct/dropped/undropped dead-suffix target lists for original-boundary target collectors, the `DaeDroppedResultScopeDirectCallEvidence` / `dae_dropped_result_scope_direct_call_evidence(...)` follow-up that centralizes scoped selected-vs-all caller direct-call count/drop/undropped/live-prefix facts for dropped-result removal legality, the `DaeOriginalNoParamDroppedCallerGuardEvidence` / `dae_original_no_param_dropped_caller_guard_evidence(...)` follow-up that centralizes safe indexed no-param dropped-caller original-boundary guard facts for result-preservation and param-operand side-call collection, the `DaeOriginalParameterizedUndroppedSelfSideChainEvidence` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_context(...)` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` follow-up that centralizes safe indexed parameterized self-side-chain guard facts for result-preservation and unobserved/current core checks, the `DaeDroppedResultCalleeRewriteEvidence` / `dae_dropped_result_callee_rewrite_evidence(...)` follow-up that centralizes callee suffix/simplification/result-removal-blocker evidence for full-module dropped-result cleanup gates, the `DaeEscapedResultDirectCallEvidence` / `dae_escaped_result_direct_call_evidence(...)` follow-up that centralizes dropped/undropped escaped-result direct-call evidence for original dead-suffix preservation collectors, and the `DaeStackOperandWindowEvidence` / `dae_stack_operand_window_evidence(...)` follow-up that centralizes top-of-stack operand-window bounds for bool/int provenance scanners, plus the `DaeStackEffectEvidence` / `dae_stack_effect_evidence(...)` follow-up that centralizes instruction stack-effect evidence before bool/int provenance push/pop updates and the `dae_stack_single_value_producer_evidence(...)` follow-up that shares zero-input/one-output producer proof across value-slice recovery, typed-select conditions, indirect-call result-slice validation, and multi-result refinement suffix consumers, and the `dae_control_single_result_block_type_evidence(...)` follow-up that shares zero-input/one-output block-type proof across direct GC result block/loop/`if` candidate paths and a multi-result suffix consumer, plus the `DaeNestedControlChildrenEvidence` / `dae_nested_control_children_evidence(...)` follow-up that centralizes primary/secondary nested-control child-body evidence for uniform-constant and direct `struct.new_default` actual collection and the `dae_rebuild_nested_control_children(...)` follow-up that centralizes nested-control reconstruction for refined-GC actual rewriting and callsite removal, plus the `DaeMultiResultSuffixBoundsEvidence` / `dae_multi_result_suffix_bounds_evidence(...)` follow-up that centralizes terminal/non-terminal suffix bounds for the multi-result single-`if` and typed-select operand scanners, and the `DaeStackSliceSimulationEvidence` / `dae_stack_slice_simulation_evidence(...)` follow-up that centralizes bounded stack-slice simulation for multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks, the `DaeStackSliceTransitionEvidence` / `dae_stack_slice_transition_evidence(...)` follow-up that centralizes expected-final-stack acceptance over those same two scanners, the `DaeStackSliceLocalizationEvidence` / `dae_stack_slice_localization_evidence(...)` follow-up that combines transition, no-escape, disposition, and removal-plan evidence for those same two scanners, the `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` follow-up that shares immediate-copy and used-temp-copy gap range policy, the `DaeTargetCallsiteEvidence` / `dae_target_callsite_evidence(...)` follow-up that shares direct-only vs direct-or-`return_call` target-callee matching across call-boundary and callsite-repair consumers, the `DaeDroppedPureLeafCleanupEvidence` / `dae_dropped_pure_leaf_cleanup_evidence_from_stack(...)` / `dae_dropped_pure_leaf_cleanup_evidence_from_leaf(...)` follow-up that shares raw-cleanup pure-leaf preservation policy across stack-truncation and adjacent leaf-drop paths, plus the `DaeSingleResultControlGcCandidateEvidence` / `dae_single_result_control_gc_candidate_evidence(...)` / `dae_apply_single_result_control_gc_candidate_evidence(...)` follow-up that shares direct GC result block/loop candidate-body recovery and rewrite wrapping, plus the `DaeResultRewriteApplicationEvidence` / `dae_apply_result_rewrites_to_body(...)` follow-up that shares masked result-slot rewrite application across flat control-result GC evidence, paired control-result LUB evidence, mixed returned-arm repair, and mixed terminal-result repair, plus the `DaeSelectValueRewriteApplicationEvidence` / `dae_apply_select_value_rewrites_to_body(...)` follow-up that shares typed-select left/right/select rewrite application across single-result returned-value select repair and flat multi-result typed-select operand repair, plus the `DaeSingleResultControlPairApplicationEvidence` / `dae_apply_single_result_control_pair_gc_evidence(...)` follow-up that shares value-result vs void-result `if` rewrite application across implicit returned-value and explicit-return zero-result refinement paths, plus the `DaeDroppedOperandPairSkipEvidence` / `dae_dropped_operand_pair_skip_evidence(...)` follow-up that centralizes dropped trivial-operand pair skipping for the multi-result single-`if` and typed-select suffix scanners, the `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)` follow-up that centralizes paired fallthrough/terminal-return if GC refinement evidence, the `DaeControlResultPairRefinementApplicationEvidence` / `dae_apply_control_result_pair_refinements(...)` follow-up that centralizes paired refined-result vector application for direct and parametric typeidx refinement paths, the `DaeControlResultUndroppedDirectCallEvidence` / `dae_control_result_undropped_direct_call_evidence(...)` follow-up that centralizes block/loop/`try_table`/`if` result-arity plus undropped-direct-call blocker evidence for dead-suffix result-preservation scans, and the range-aware `DaeEscapingBranchEvidence` / `dae_escaping_branch_range_evidence(...)` / `dae_no_escaping_branch_range_evidence(...)` follow-up plus `dae_no_escaping_branch_prefix_evidence(...)` / `dae_no_escaping_branch_suffix_evidence(...)` projections and copied range/prefix/suffix `DaeNoEscapeSliceEvidence` helpers plus the terminal-if no-escape, mixed return/fallthrough arm, and single-result control-pair evidence helpers that centralize branch-escape and return/control-arm LUB evidence for localization/control/refinement gates, plus the `dae_multi_result_control_carrier_chain_slice(...)` follow-up that centralizes fallthrough, terminal-return, and appended-return multi-result carrier-chain recovery inside `dae_find_multi_result_control_slice(...)`, and the `DaeOriginalResultPreservationRow` / `dae_original_result_preservation_row_from_context(...)` follow-up that centralizes per-definition original-boundary result-preservation guard fact projection, plus the `DaeOriginalResultRemovalPruneRow` / `dae_original_result_removal_prune_row(...)` follow-up that centralizes original-boundary result-removal prune evidence for later-mutual, void-side-caller, mixed dropped/undropped caller, dropped-only result-side, mixed-self suffix, same-self result-operand, and escaped-block self-result cleanup gates, plus the `DaeOriginalDeadSuffixSideCallEvidence` / `dae_original_dead_suffix_side_call_evidence(...)` follow-up that shares void-side cleanup, recursive result-side chain, and same-self result-operand preservation gates across two zero-direct-call cleanup decisions, plus the `DaeOriginalResultRemovalPruneEvidence` / `dae_original_result_removal_prune_evidence_from_row(...)` follow-up that aggregates params-absent cleanup and uncalled-after-param-cleanup blockers over the original-boundary prune row, while still pointing to remaining scanner-heavy broader typeidx/control evidence, boundary candidate legality beyond forwarding gates plus count/caller/self-recursion eligibility and the first module/original snapshots, no-param dropped-caller guard, parameterized self-side-chain guard, result-preservation input carrier, and per-definition result-preservation/prune rows, repair disposition beyond explicit drop/replay/reject value-slice/callsite evidence/paired GC/final actual-shape/single-instruction/control-entry projection/nested-control entry carrier/block-type signature/single-block result-carrier/carrier-chain/stack-window/stack-slice-simulation/expected-final-transition repair, dropped-result legality beyond the first boundary-based candidate, result-removal blocker, tail-call boundary blocker, direct-call target and dead-suffix target carriers, dead-suffix direct target-list carrier, all/no-param repair-plan, and selected/full-module context carriers plus dropped-result callee rewrite evidence, remaining dropped-result result-removal policy, localization, convergence, and cleanup scheduling work.
- [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md)
  First-slice and validation bridge for a future Starshine port: no-rewrite analyzer, scalar direct-call parameter deletion, follow-up rewrite families, exact local code surfaces, Binaryen `--dae` oracle checks, and the guardrail that plain DAE must not run the optimizing sibling's nested cleanup replay.

## Current maintenance rule

- Treat [`../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md`](../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md) as the immutable tagged primary-source manifest for this folder.
- Treat [`../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md`](../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md) as the focused current-main / Starshine-readiness freshness layer above the 2026-04-26 bridge.
- Treat this folder as the canonical home for future `dead-argument-elimination` / `dae` research and port planning.
- Do not rely on the older **unimplemented** wording in this dossier; treat it as superseded by the local active implementation and keep this page open for a full status refresh.
- Keep the relationship to [`../dae-optimizing/index.md`](../dae-optimizing/index.md) explicit instead of silently folding plain DAE facts into the optimizing dossier.

## Sources

- [`../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md`](../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md)
- [`../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md`](../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md)
- [`../../../raw/research/0435-2026-05-04-dead-argument-elimination-current-main-recheck.md`](../../../raw/research/0435-2026-05-04-dead-argument-elimination-current-main-recheck.md)
- [`../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md`](../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md)
- [`../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md`](../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md)
- [`../../../raw/research/0293-2026-04-24-dead-argument-elimination-primary-sources-and-starshine-followup.md`](../../../raw/research/0293-2026-04-24-dead-argument-elimination-primary-sources-and-starshine-followup.md)
- [`../../../raw/research/0159-2026-04-21-dead-argument-elimination-binaryen-research.md`](../../../raw/research/0159-2026-04-21-dead-argument-elimination-binaryen-research.md)
- [`../../../raw/research/0230-2026-04-21-dead-argument-elimination-implementation-followup.md`](../../../raw/research/0230-2026-04-21-dead-argument-elimination-implementation-followup.md)
- [`../../../../../src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
- [`../../no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
- [`../tracker.md`](../tracker.md)
- [`../../../../../agent-todo.md`](../../../../../agent-todo.md)
- Binaryen `version_129` implementation and test sources:
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/DeadArgumentElimination.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/pass.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/opt-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/param-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/return-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/lubs.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/type-updating.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae_tnh.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc-refine-params.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc-refine-return.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-optimizing.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-refine-params-and-optimize.wast>
