---
kind: entity
status: supported
last_reviewed: 2026-07-21
sources:
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/dead_argument_elimination.mbt
  - ../../../../../src/validate/gen_valid.mbt
  - ./fuzzing.md
  - ./completion-matrix.md
  - ./source-case-map.md
  - ./de-artifacting-inventory.md
  - ../../no-dwarf-default-optimize-path.md
  - ../tracker.md
  - ../../../../../agent-todo.md
related:
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ./starshine-strategy.md
  - ./starshine-port-readiness-and-validation.md
  - ./completion-matrix.md
  - ./source-case-map.md
  - ./de-artifacting-inventory.md
  - ../dae-optimizing/index.md
  - ../signature-pruning/index.md
  - ../tracker.md
---

# `dead-argument-elimination`

## Role

- `dead-argument-elimination` is an upstream Binaryen boundary pass.
- The public upstream CLI alias is `dae`.
- It has an active-partial Starshine implementation in [`../../../../../src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt), wired under both `dead-argument-elimination` and `dae`. The July 21–22 deterministic and four-lane results remain regression baselines, not current release closure, while the reopened lifecycle work remains active.
- The current repo no-DWARF default optimize path uses the related later pass `dae-optimizing`, not this plain variant.
- The two passes share the same core engine in upstream `version_131`; `dae-optimizing` is the plain DAE algorithm plus one extra nested cleanup rerun. The `version_130...version_131` source comparison leaves `DeadArgumentElimination.cpp` and its helper owners unchanged; v131 updates the expected unreachable local write from `local.tee` to `local.set` in `dae-gc.wast` and `dae2.wast`.
- [`source-case-map.md`](./source-case-map.md) maps every represented Binaryen-v131 source fixture to current focused coverage and identifies the missing TNH option path. [`completion-matrix.md`](./completion-matrix.md) remains the detailed release ledger, and [`de-artifacting-inventory.md`](./de-artifacting-inventory.md) records removed gates plus remaining optional phase/profitability budgets.
- The 2026-07-21 artifact closeout fixes a real mixed-type local-projection bug: a removed slot that the exact body still reads must have constant replacement evidence or a typed local projection. Plain DAE now validates and is byte-idempotent at `2,991,169` bytes, SHA-256 `f7bbacf174d6b1edacddc3f60abef4a107b385b14f86fc5373d7c3e6ac025c72`, with `85.329s` productive and `64.277s` idempotent medians; canonical Starshine is `9,670` bytes smaller than Binaryen v131.

## Why it matters

- Even though plain `dae` is outside the current no-DWARF parity queue, it is already a named local registry entry, so it is a real future implementation surface rather than speculative upstream trivia.
- The existing `dae-optimizing` dossier is easier to understand once plain DAE has its own home.
- This pass teaches the part that is easiest to blur together:
  - the core boundary rewrite algorithm itself,
  - versus the extra scheduler behavior that the optimizing suffix adds.
- `agent-todo.md` now tracks the plain DAE profile/direct-pass work inside `[O4Z-AUDIT-DAE]`, while this dossier remains the durable place to explain the non-optimizing pass separately from `dae-optimizing`.

## Beginner summary

A good beginner mental model is:

- gather all direct callers of a function,
- refuse boundaries that can escape or be observed indirectly,
- tighten argument and result types when all known calls agree,
- turn always-the-same actuals into local callee constants,
- delete dead parameters and dead dropped returns,
- localize hard operands and iterate if necessary,
- then stop.

That last point is the big difference from `dae-optimizing`: **plain DAE does not do the extra post-change cleanup rerun**.

## Current durable takeaways

- Plain DAE is a **whole-module direct-call boundary pass**, not a hot local cleanup pass; the direct `dead-argument-elimination` / `dae` registry and dispatcher entries now route to the shared core with the optimizing suffix disabled.
- It can do more than delete unused parameters:
  - constant actual materialization,
  - GC parameter refinement,
  - result type refinement,
  - dropped-return elimination,
  - operand localization and retry.
- Exports and `ref.func` escapes count as unseen calls, so Binaryen refuses signature-changing rewrites there.
- Tail calls and tail-callee relationships still matter for dropped-return legality, but owned direct `return_call` sites can participate in unused-parameter signature/operand repair when every observable caller is known.
- A stable `DaeDecisionReason` vocabulary classifies boundary, tail, operand, GC-refinement, stale-fact, rollback, profitability, and budget outcomes. Boundary candidates and operand dispositions carry primary reasons. `DaeDiagnostics` now aggregates the canonical core worklist and emits deterministic plain-DAE, DAEO, and cleanup-reentry trace records containing every counter and all stable reason codes; selected, nested, and remaining specialized-loop counters stay open under `DAE-H4`.
- `DaeRewritePlan` now provides the first epoch-bearing immutable transaction across caller edits, callee/local projection, replacement signatures, exact type/function section consequences, metadata policy, touched reporting, dependency invalidation, validation, and rollback. Ordinary private scalar unread-parameter removal uses it; constant, localization, GC, result, control, and selected paths remain to migrate.
- The implementation is iterative and can perform a localization-only round so a later removal becomes legal.
- All immutable original-module legality inputs now live in one `DaeOriginalBoundarySnapshot`: module counts, original type indices, visibility, params, unreachable prefixes, call summaries, and result/dead-suffix evidence are collected once before mutation and passed to the core as one record. `DaeCurrentBoundaryGraph` separately owns resolved current signatures, exact bodies, call facts, and per-definition signature/body plus module/call epochs. The fixed core refreshes after every commit and rejects stale copied wave facts; later specialized phases and the unified worklist still need epoch tokens.
- Original result-preservation and result-removal prune policy now share one per-definition `DaeOriginalResultBoundaryRow`: common original call/self/escape/dead-suffix facts are indexed once, while unobserved cleanup, zero-call cleanup, dropped-result removal, and argument-side repair consume preservation/prune projections without changing plain-vs-optimizing scheduling.
- Original dead-suffix escaped-result classification now uses one `DaeOriginalDeadSuffixEscapedResultCallFacts` module scan for both dropped and undropped facts. The main and reverse cores consume the shared carrier in distinct preservation/removal and rewrite gates; the old parallel whole-module collectors are gone.
- Original escaped-result operand classification now uses one `DaeOriginalEscapedResultOperandCallFacts` caller scan for escaped-caller dead-suffix targets and private-result caller targets gated by the shared dropped-suffix facts. The old two operand collectors and the private collector's duplicate callee scan are gone; preservation/reverse and unobserved/zero-call decisions retain distinct projections.
- Unobserved-unreachable and zero-call/uncalled-result policy now share one `DaeOriginalResultEscapeRow` per definition for their exact original-phase overlap. The row reuses preservation facts, adds only the private-result escaped-operand count and undropped escaped-result suffix flag, keeps branch-specific predicates separate, and does not mix current call/body evidence into the original snapshot.
- Zero-call side-call policy now consumes `DaeOriginalResultSideCallRow` plus the existing preservation/prune rows. Other-call and recursive void/result-side facts are projected once, result-other and mutual-call facts are reused, and the branch no longer reindexes its original parameter/call/self/nonself/private-result baseline; prior-call and current body/call facts remain explicit.
- Unobserved and zero-call result cleanup now share `DaeOriginalResultCleanupPreservationEvidence`, derived once under the boundary evidence from exactly mixed-self suffix preservation and the result-side-chain cleanup summary. The combined decision has two production consumers, while zero-call-only original facts and all current body/self/module-call facts remain explicit. Slice-160 validation reached DAE whitebox `174/174`, pass suites `4444/4444`, full tests `7883/7883`, and unchanged raw-red direct/optimizing compare counts.
- Ordinary callsite replay/drop repair and typed scratch-local localization now share `DaeCallsiteRewritePlan`, `DaeCallsiteRewriteApplicationEvidence`, `dae_callsite_rewrite_plan(...)`, and `dae_apply_callsite_rewrite_plan(...)`. One contiguous application preserves prefix/operand/call/suffix order for both modes. Slice 162 adds `dae_localization_scratch_slot(...)`, so each caller reuses DAE-owned locals by exact defaultable type across sequential and nested callsites while mixed types remain separate and non-null references use nullable declarations. Removed-parameter local projection, recursive traversal, touched policy, and retry/frontier scheduling remain caller-owned. Validation reached DAE whitebox `176/176`, pass suites `4447/4447`, full tests `7886/7886`, and unchanged raw-red direct/optimizing compare counts.
- Productive core and revisit results now share `DaeCoreResultApplication` plus `dae_apply_core_result(...)` for module installation, first touched-bitmap adoption or later OR merge, aggregate changed state, applied/no-op distinction, and primary-definition propagation. The fixed core, restored-operand, reverse exact-literal, low revisit, low callee, and boundary wrapper/callee phases consume it while keeping break/continue rules, fact refresh, iteration limits, traces, raw cleanup, and final return-suffix touched policy explicit. Slice-163 validation reached DAE whitebox `177/177`, pass suites `4448/4448`, full tests `7887/7887`, and unchanged intentionally raw-red compare counts.
- Selected one-shot lanes now also share `dae_apply_selected_core_result(...)` for exactly one consumed singleton-bit clear followed by ordinary core-result application. Func236 local retargeting, Func256 loop-carrier retargeting, and three Func237 early-shape producers are the first bounded consumers; selection setting, producer calls, order, no-change policy, facts, traces, and iteration ownership remain explicit. Slice-164 validation reached DAE whitebox `178/178`, pass suites `4449/4449`, full tests `7888/7888`, and unchanged raw-red dedicated/direct/optimizing counts of `5000/10000`, `9207/10000`, and `9538/10000` normalized.
- Multi-selection bounded loops now share `dae_apply_primary_selected_core_result(...)` for ordinary result application followed by productive-primary clearing only. The low-callee and low-wrapper-callee unread-parameter loops are the two consumers; no-op results preserve all selections, and fixed facts, order, limits, breaks, traces, and cleanup ownership remain phase-local. Slice-165 validation reached DAE whitebox `179/179`, pass suites `4450/4450`, full tests `7889/7889`, and unchanged reduction-disabled counts of `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures. Direct DAE remains raw-red.
- Selected locals-and-body producers now share `dae_try_apply_selected_def_func_rewrite_once(...)` for selected/current-function gating, current type-section delivery, no-op rejection, unrelated-function preservation, code installation, stale-name removal, exact touched metadata, and primary reporting. Func287 setup compaction, Func288 switch-carrier compaction, Func288 return-wrapper flattening, Func237 raw-debris/local trimming, Func299 inverted-result-`if` cleanup, Func236 local retargeting, and Func246 builder folding are seven direct consumers; the body-only boundary delegates through it. Slice 168 fixes the Func288 wrapper's missing selected-bit rejection while preserving its three-local gate and keeps Func237 raw cleanup's current-signature local trimming. Validation reached DAE whitebox `182/182`, pass suites `4453/4453`, full tests `7892/7892`, and unchanged reduction-disabled counts of `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures. Recognizers, FuncSec/type-changing and multi-function rewrites, scheduler lifecycle, cleanup, and the plain-vs-optimizing split remain explicit. Direct DAE remains raw-red.
- Dynamic selected full-function scanners now share `dae_try_apply_first_selected_func_rewrite_once(...)`, which scans selected definitions in ascending order and delegates first-productive installation through the fixed full-function boundary. Selected nop removal, Func408 pair-`if` repair, selected i32-constant local-set inlining, and selected terminal-call local-set inlining consume it while retaining recognizers, current signature/target discovery, protected-local counts, local trimming, traces, and scheduling. Slice-169 validation reached DAE whitebox `183/183`, pass suites `4454/4454`, full tests `7893/7893`, and unchanged reduction-disabled dedicated/direct/optimizing counts of `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures. Direct DAE remains raw-red.
- Selected full-function rewrites that require the current resolved signature now share `dae_try_apply_selected_def_typed_func_rewrite_once(...)`, a projection over the fixed full-function boundary. Func311 and Func313 terminal-call argument rewrites are the two consumers; exact type/shape traces, Func311 partial/full local products, Func313 suffix staging, and bounded scheduler loops remain wrapper-owned. Slice-170 post-rebase validation reached DAE whitebox `184/184`, pass-manager `216/216`, public DAE `303/303`, pass suites `5192/5192`, and full tests `8644/8644`. Fresh reduction-disabled lanes are dedicated direct `5000/10000`, changed-composition direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, all with zero failures. Direct DAE remains raw-red.
- Selected one-function products that both rewrite code and reuse an existing equivalent current signature now share `dae_try_apply_selected_def_funcsec_func_rewrite_once(...)`. Func256 and Func298 loop-carrier compaction are the two consumers; current section admission, current `FuncType` delivery, equivalent FuncSec type reuse, equal-product rejection, installation, stale-name removal, and exact metadata are shared while body/trace policy remains wrapper-owned. Slice-171 validation reached DAE whitebox `185/185`, public DAE `303/303`, pass suites `5193/5193`, and full tests `8645/8645`; fresh reduction-disabled lanes remain dedicated direct `5000/10000`, direct random-all `9136/10000`, and normalized optimizing random-all `9633/10000`, all with zero failures. Direct DAE remains raw-red, and TypeSec-changing or multi-function products remain separate.
- Parametric terminal typeidx controls, zero-input terminal paired-`if` refinement, and preserved multi-result loop carriers now share `DaeTypeidxControlBodyRewriteApplication` / `dae_rebuild_typeidx_control_in_body(...)` for destination admission, existing control/type rebuild, copied outer-body installation, active-type reuse, and invalid-slot rollback. Evidence, inner-body products, active `TypeSec` choice, result policy, touched semantics, and scheduling remain separate. Slice-186 validation reached DAE whitebox `200/200`, public DAE `303/303`, pass suites `5208/5208`, full tests `8660/8660`, and unchanged reduction-disabled counts `5000/10000`, `9136/10000`, and `9633/10000`; direct DAE remains raw-red and plain scheduling is unchanged.
- Paired terminal-arm and sparse single-arm GC result refinement now share `DaeOptionalRefinedResultSlotsApplicationEvidence` / `dae_apply_optional_refined_result_slots(...)` for exact vector-length admission, sparse slot mutation, optional mirrored updates, no-refinement rejection, and mismatch rollback. Pair/single evidence, body reconstruction, type timing, touched policy, and scheduling remain separate. Slice-185 validation reached DAE whitebox `199/199`, public DAE `303/303`, pass suites `5207/5207`, full tests `8659/8659`, and unchanged reduction-disabled counts `5000/10000`, `9136/10000`, and `9633/10000`; direct DAE remains raw-red and plain scheduling is unchanged.
- Exact immediate materialized suffix candidates enter `DaeRewritePlan` through `dae_try_rewrite_verified_materialized_const_suffix_candidate(...)`. The entry reconstructs original/current facts, verifies exact callers/counts and the current signature, preserves kept prefix/ambient-stack provenance through the scalar callsite builder, and commits atomically with callee projection, type finalization, effects, validation, and rollback. Selected ref-null compatibility uses the same transaction. The former count-checked post-caller callee application fallback is deleted; current bounded evidence is DAE white-box `345/345`, public DAE/DAEO `330/330`, and pass-manager `305/305`.
- Func867 default-argument specialization and the forwarded Func3765 suffix specialization share `dae_try_rewrite_verified_uniform_const_candidate(...)`: collect uniform evidence only for removed parameters and require exact expected materialized constants. Immediate suffixes now use the slice-184 suffix transaction; non-immediate shapes retain generic caller/callee value-slice application. Func3765 still runs after optimizing-only Func3737 repair so `1024` is current evidence. Slice-183 validation reached DAE whitebox `197/197`, public DAE `303/303`, pass suites `5205/5205`, full tests `8657/8657`, and unchanged reduction-disabled counts; direct DAE remains raw-red.
- Direct GC result/body refinement and the historical Func323 nine-parameter caller-plus-callee product still share `dae_finalize_module_code_rewrite(...)` for candidate construction. Production ownership now differs: result-only GC refinement projects its complete candidate into explicit `DaeRefineResult` actions and commits through `DaeRewritePlan`, while ordinary combined GC parameter/result work uses the same action plan and only the optimizing direct-GC batch remains family-plan-backed and the identity-free late global-suffix lane wraps its complete custom candidate in `DaeFamilyRewritePlan`, validates immutable original ownership/current epochs/exact call identities, emits parameter/materialization/local/type effects, and merges them into DAEO worth selection. Current white-box coverage rejects an originally element-exposed target and passes at `345/345`.
- Direct GC actual repair and generic removed-parameter callsite repair now share `dae_apply_direct_caller_scope_rewrite(...)` for selected caller traversal, invalid-definition skipping, current rewritten-function delivery, exact rewrite counts, changed-definition tracking, function replacement, no-op behavior, and transactional rollback. Their body-only versus typed scratch-local products, caller signature/localization state, count verification, callee finalization, touched policy, and type timing remain separate. Slice-181 validation reached DAE whitebox `195/195`, public DAE `303/303`, pass suites `5203/5203`, full tests `8655/8655`, and unchanged fresh raw-red counts.
- Dropped-result scoped call/drop application now shares `dae_rewrite_dropped_result_calls_in_scope_with_policy(...)` between count-verified live-prefix call rewriting and post-repair anywhere cleanup. Scope traversal, bounds handling, function replacement, no-op behavior, and rewrite accumulation are centralized while unreachable-suffix policy, count verification, repair ordering, legality, and scheduling remain caller-owned. Slice-180 validation reached DAE whitebox `194/194`, public DAE `303/303`, pass suites `5202/5202`, full tests `8654/8654`, and unchanged fresh raw-red counts.
- Dropped-result callee application now shares `dae_apply_dropped_result_callee_rewrite(...)` between ordinary dropped-result removal and the unreachable-callee result-removal application. Repair-plan execution, caller-produced callee installation, optional repair/type timer labels, and touched-policy-aware signature/code finalization are centralized; legality, direct-call rewrite/count verification, selected/full scope, repair ordering, callee construction, retained params, and scheduling remain consumer-owned. Slice-179 validation reached DAE whitebox `193/193`, public DAE `303/303`, pass suites `5201/5201`, full tests `8653/8653`, and unchanged fresh raw-red counts.
- The historical slice-178 standalone removed-parameter callee application has been superseded by scalar-plan ownership. `DaeRemovedParamCalleeRewriteEvidence` remains the projection carrier, but caller edits, callee cleanup/projection, signature/type finalization, touched/effects, validation, and rollback commit together through `DaeRewritePlan`; `dae_apply_removed_param_callee_rewrite(...)` is deleted.
- Typeidx control reconstruction now also shares `dae_rebuild_typeidx_control(...)` across parametric terminal controls, zero-input terminal multi-result `if` refinement, and preserved multi-result loop carriers. Plan admission, active-`TypeSec` reuse/append timing, and block/loop/paired-`if` reconstruction are centralized while candidate/LUB evidence, consumed-param stack policy, body selection, finalization, and scheduling remain caller-owned. Slice-177 validation reached DAE whitebox `191/191`, public DAE `303/303`, pass suites `5199/5199`, and full tests `8651/8651`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and plain DAE receives no optimizing scheduling.
- Flat multi-result single-`if` and typed-select result scanners now share `DaeMultiResultOperandSequenceEvidence` / `dae_multi_result_operand_sequence_evidence(...)` for terminal-return-aware suffix recovery, dropped-prefix skipping, ordered slot traversal, ordinary single-producer fallback, and exact operand/value indices. Candidate recognition, GC refinement, and rewrite application remain consumer-owned. Slice-176 validation reached DAE whitebox `190/190`, public DAE `303/303`, pass suites `5198/5198`, and full tests `8650/8650`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and scheduling is unchanged.
- Bounded singleton-selected scheduling now shares `dae_run_bounded_singleton_selected_core_rewrite(...)` across Func237 terminal-br0 and the separate Func267/268 const-`if` loops. Current module/touched/changed handoff, productive callback timing, selection restoration, and break-on-first-no-op are centralized while bounds, traces, phases, and dependencies remain caller-owned. Func867 specialization and all four Func237 inner-loop placements use the one-shot singleton lifecycle, preserving the productive-Func867 gate and unconditional late revisit. Slice-175 validation reached DAE whitebox `189/189`, public DAE `303/303`, pass suites `5197/5197`, and full tests `8649/8649`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and plain DAE receives no optimizing scheduling.
- Optimizing-only module raw cleanup and final return-suffix cleanup now share `dae_apply_cleanup_result_preserving_touched(...)` for productive module/primary application, no-op aggregate-state preservation, and exact scheduler touched ownership. Their distinct scanners, touched evidence, phase placement, and result construction remain explicit. Slice-174 validation reached DAE whitebox `188/188`, public DAE `303/303`, pass suites `5196/5196`, and full tests `8648/8648`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and plain DAE receives no optimizing cleanup.
- Singleton-selected const-`if` scheduling now shares `dae_run_singleton_selected_core_rewrite(...)` across Func233, the bounded Func267/268 helper loop, Func327, and Func1794. Selection activation during producer execution, productive/no-op clearing, and core result application are centralized; phase order, loop limits, traces, dependencies, and the producer's unused multi-selection capability remain explicit. Slice-173 validation reached DAE whitebox `187/187`, public DAE `303/303`, pass suites `5195/5195`, and full tests `8647/8647`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and plain DAE receives no optimizing scheduling.
- `DaeRemovedParamCalleeRewriteEvidence` / `dae_removed_param_callee_rewrite_evidence(...)` remains the shared callee projection across scalar and older specialized candidate construction. Generic/verified scalar paths commit through `DaeRewritePlan`; the nine-parameter late global-suffix product commits through `DaeFamilyRewritePlan`; no production path uses the deleted standalone removed-parameter callee application helper. Historical consumers included generic candidate rewriting, Func323's nine-param caller-plus-callee specialization, and Func3737's wrapper specialization. Kept params, replacement locals, constant substitution, local remapping, and local reconstruction are shared, followed by the existing changed-function module finalizer. Func323's 7/8-param branches, caller repair, call verification, cleanup, traces, and scheduling remain explicit. Slice-172 validation reached DAE whitebox `186/186`, public DAE `303/303`, pass suites `5194/5194`, and full tests `8646/8646`; fresh reduction-disabled lanes remain `5000/10000`, `9136/10000`, and `9633/10000` normalized with zero failures. Direct DAE remains raw-red, and plain DAE receives no optimizing cleanup.
- Selected function-body producers now share `dae_try_apply_selected_def_body_rewrite_once(...)` for selected/current-body gating, current type-section delivery, no-op rejection, local and unrelated-body preservation, code installation, stale-name removal, exact touched metadata, and primary reporting. Nine Func237 producers consume it: outer-loop carrier, nested branch-result, zero-branch, leading-retain-call, inner-iteration-loop, terminal-br0, dropped-result branch-carrier, unreachable-after-`if`, and parameter-loop spilling. Body recognizers, local-0 safety, and all caller-side scheduling remain explicit. Slice-168 validation reached DAE whitebox `182/182`, pass suites `4453/4453`, full tests `7892/7892`, and unchanged reduction-disabled counts of `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures. Direct DAE remains raw-red.
- Unobserved-unreachable, dead-self-suffix-result, and uncalled-result removal now share `DaeDroppedResultUnreachableCalleeRewritePlan` plus one application helper for caller repair, unreachable callee replacement, signature update, and module reconstruction. Branch legality and retained-parameter choices remain distinct, and fresh compare counts are unchanged.
- `DaeModuleSignatureContext`, `dae_module_signature_context(...)`, and `dae_apply_module_signature_finalization(...)` form the generic signature/type/function boundary beneath signature-only and code-applying rewrites. The signature finalizer preserves the existing code section and returns callee-only metadata.
- `DaeModuleCodeFinalizationPlan`, `DaeModuleCodeTouchedPolicy`, `dae_module_code_finalization_plan(...)`, and `dae_apply_module_code_finalization(...)` add caller-owned code installation plus explicit callee-only versus changed-function touched accounting. Direct GC body/caller refinement, selected append-`drop` result removal, the full-module unreachable-callee application, and dead-suffix-only unreachable parameter removal consume it; signature-only GC refinement still stops before code reconstruction. Exact rewritten-call verification, optional type-section pre-update, result legality, repair timing, callee-body rewriting, retained params, and the plain-vs-optimizing scheduler split remain branch-owned. Slice-158 validation reached DAE whitebox `172/172`, pass suites `4442/4442`, full tests `7881/7881`, and unchanged raw-red compare counts.
- `DaeTypeidxControlRewritePlan`, `DaeTypeidxControlRewriteApplication`, `dae_typeidx_control_rewrite_plan(...)`, and `dae_apply_typeidx_control_rewrite(...)` centralize simple function-type reuse/append plus typeidx block/loop/paired-if reconstruction. Parametric terminal multi-result controls, zero-input terminal multi-result `if`, and preserved multi-result loop carriers consume the application; candidate/LUB legality, stack safety, body selection, and module finalization remain caller-owned. Slice-159 validation reached DAE whitebox `173/173`, pass suites `4443/4443`, full tests `7882/7882`, and unchanged direct/optimizing raw-red counts and residual profiles.
- The plain pass shares the same upstream file and much of the same lit neighborhood as `dae-optimizing`, but it deliberately omits the final `optimizeAfterInlining(...)` rerun.
- There is no single dedicated plain `dae.wast`; the real proof surface is split across `dae_tnh.wast`, `dae-gc.wast`, `dae-gc-refine-params.wast`, and `dae-gc-refine-return.wast`, with neighboring optimizing files acting as sibling-boundary evidence rather than the main plain-pass oracle.
- The 2026-05-04 current-main recheck found no teaching-relevant drift from the tagged source story, and it keeps the future Starshine implementation ladder explicit: analyzer-only candidate classification first, scalar direct-call parameter deletion second, and constant actual / GC refinement / dropped-result / localization families later.
- The Starshine status/readiness pages now include a first source-reviewed DAE-A through DAE-O implementation-status matrix. That matrix is not closeout evidence: it classifies many scalar/call-boundary families as source-covered/focused-green but direct-compare-open, records that the owned-direct `struct.new_default` subset of live GC argument/result refinement plus focused static `local.get` result, typed-block-operand argument, zero-param typeidx block/if/loop and typed-select operand argument including typed-call result operands, branch-free effectful call, plus bounded branchful non-escaping `if` conditions, nullable common-LUB including nullable `ref.null` select LUB, typed-block-body result, zero-param typeidx block body and terminal `if` result, fallthrough typed/typeidx loop result, zero-param multi-result fallthrough loop result, simple multi-result `if`-arm result, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm result, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands in multi-result bodies, two-caller direct-call argument-LUB, terminal-`if` returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, multi-result typed-select operand plus block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, and simple `if`-arm/simple-and-nested block-carrier `if`-arm/simple-and-nested/void-block explicit-return block-carrier `if`-arm multi-result returned-value subsets are fixed, records the 2026-07-05 exact-reference refinement follow-up that makes allocation-derived signatures match Binaryen in the profile lanes, records the 2026-07-06 typed-block operand/result, argument-LUB, terminal returned-value LUB, typed-select returned-value LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, multi-result typed-select operand plus block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select LUB, non-tail-lane multi-result typed-select LUB, and explicit-return returned-value LUB follow-ups, and records the 2026-07-05 focused direct `return_call` unused-parameter repair, and records the 2026-07-07 scanner-consolidation extractions where `DaeGcSelectValueEvidence` / `dae_gc_select_value_slices_evidence(...)` now shares typed-select GC evidence between single-result and multi-result result-refinement scanners, `dae_gc_select_actual_value_slices_evidence(...)` shares condition-carrying typed-select actual slices plus nullable GC evidence between candidate collection and callsite repair, `dae_indirect_call_gc_result_slice_evidence(...)` shares `call_ref` / `call_indirect` result-slice operand evidence, `dae_direct_call_gc_result_signature_evidence(...)` / `dae_direct_call_gc_result_refinement_evidence(...)` share direct-call result signature and old-result refinement evidence between argument-side and returned-value scanners, `DaeGcTypeEvidence` / `dae_gc_result_slot_evidence(...)` shares candidate/refinement/rewrite evidence across flat multi-result, single-`if` operand, and parametric typeidx control result slots, `DaeValueSlice` / `dae_find_select_value_slices(...)` now shares typed-select operand/condition bound recovery between single-result and flat multi-result returned-value scanners, `DaeGcActualShapeKind` / `DaeGcActualShapeDiscoveryEvidence` plus `DaeGcActualTypeEvidence` separate final direct/control/select actual discovery from current-target candidate/refinement/LUB/repair evidence across candidate collection and typed-select callsite repair, and `DaeOperandDisposition` / `dae_operand_disposition_from_range(...)` centralizes droppable/effect/branch range classification plus callsite removed-operand replay/drop repair. Later 2026-07-05 diff/source reviews classify sampled residual dirs as Starshine-win local/temp/effect-drop cleanup, transitive producer-return cleanup, or multi-result tuple/local-spill avoidance. A follow-up red-first GenValid slice superseded the two-result `dae-unused-returns` trigger with a single-result DAE-F positive leaf: the focused leaf is normalized-green at 100 cases; the historical direct aggregate was raw-red at 100 (`32` normalized / `68` mismatches), 1000 (`312` normalized / `688` mismatches), and closeout-scale 10000 (`3125` normalized / `6875` mismatches), while the refreshed 2026-07-09 aggregate is still raw-red but improved to `5000/10000` normalized with `5000` mismatches after `dae-localization` and `dae-effectful-args` went green; and the optimizing sibling dedicated profile is green at 10000 cases. The optimizing sibling also has green regular GenValid at 100000 and post-imported-tag-fix explicit wasm-smith zero-mismatch evidence (`9956` compared, `9955` normalized, `1` cleanup-normalized, `44` Binaryen/oracle command failures); the imported-tag fix repairs DAE type pruning for function types referenced only by imported tags. Paired probes show direct DAE plus `vacuum` keeps the same raw local/temp residuals at the older 1000-case scale, while `dae-optimizing` plus `vacuum` and `dae-optimizing + inlining-optimizing + remove-unused-module-elements` are green at 10000; `dae-optimizing + inlining-optimizing` alone remains raw-red at 10000 because it needs the surrounding RUME cleanup, with Starshine retaining a private unreachable helper/type that Binaryen deletes. Broader arbitrary type-refinement (including local value-flow beyond static local type evidence, richer typeidx block/if/loop boundaries with params/control flow beyond the new drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load terminal returned-value subsets, mixed return/fallthrough beyond the simple single-result and flat multi-result void-if arm subsets, select beyond the bounded typed two-value single-result plus nullable-null/branchful-if-condition argument, block/pure/call-condition/call-post-op-condition/memory-condition/branchful-if-condition, non-tail-lane multi-result select-first/scalar-second, drop-only/effectful parametric typeidx block/if/loop terminal controls, and flat multi-result suffixes, and non-flat multi-result carriers beyond the current droppable-prefix flat tail and simple/nested block-carrier, simple loop-carrier, simple `if`-arm, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands, simple/nested block-carrier `if`-arm, and simple/nested/void-block explicit-return block-carrier `if`-arm subsets), direct raw-red dedicated/random-all, non-profile ordered scheduler/integration, and final closeout lanes remain open, though a 2026-07-06 optimizing-profile full post-pass suffix lane (`dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize`) normalized `10000/10000` with zero failures; direct regular GenValid is green at closeout scale (`.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705` normalized `100000/100000` with zero failures), and the previous direct wasm-smith 10000 size-losing `case-009332` unreachable-control-debris mismatch is fixed by red-first raw dropped-unreachable cleanup (`.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705` requested `10000`, compared `9956`, normalized `9956`, mismatches `0`, `44` Binaryen/oracle command failures). Direct random-all remains raw-red: the refreshed 2026-07-09 no-reduction lane has `9207/10000` normalized and `793` mismatches after removing the localization/effectful-argument families; historically, the pre-fix 500 lane had `443` normalized / `57` known direct/coverage cleanup mismatches, the post-fix 500 rerun timed out without `result.json`, the post-fix 100-case smoke had `87` normalized / `13` mismatches, the post-fix 200-case smoke `.tmp/pass-fuzz-dead-argument-elimination-random-all-200-post-debris-cleanup-20260705` had `176` normalized / `24` known direct/coverage cleanup mismatches, and the post-fix 300-case lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-300-post-debris-cleanup-20260705` had `262` normalized / `38` known direct/coverage cleanup mismatches with validating artifacts. A subsequent `dae-optimizing` random-all retry (`.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705`) also timed out without `result.json` after `1037` records (`1035` compared, `985` normalized, `50` mismatches, `2` Starshine command failures). The mismatches stayed in the prior cleanup/scheduling classes (`coverage-forced-portable`, `dae-localization`, `dae-effectful-args`), but the command failures exposed a tail-call result-mismatch blocker: red-first focused fixtures now cover both the direct-tail subset and an unreachable-suffix tail-call subset, native CLI replays of `case-000433` and `case-000434` now succeed, and compare replays no longer command-fail. Both replay mismatches are sampled `coverage-forced-portable` pure/unreachable cleanup with deletion-only canonical WAT diffs and validating artifacts. A completed bounded post-tail random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-500-post-tail2-20260705` requested/compared `500/500`, normalized `474`, mismatched `26`, and had zero failures; a higher `.tmp/pass-fuzz-dae-optimizing-random-all-1000-post-tail2-20260705` lane requested/compared `1000/1000`, normalized `952`, cleanup-normalized `0`, mismatched `48`, and had zero failures. Mismatches stayed in known cleanup/scheduling classes (`coverage-forced-portable=30`, `dae-effectful-args=9`, `dae-localization=9`) with validating raw/canonical artifacts and aggregate Starshine size wins. A completed 1282-case lane `.tmp/pass-fuzz-dae-optimizing-random-all-1282-post-tail2-20260705` requested/compared `1282/1282`, normalized `1227`, cleanup-normalized `0`, mismatched `55`, and had zero validation/generator/property/command failures; mismatches stayed in the same cleanup/scheduling classes with validating artifacts. The ordinary 2000-case retry `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-20260705` timed out after 7200s without `result.json` during mismatch reduction, but the no-reduction follow-up `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` completed full random-all requested/compared `10000/10000`, normalized `9538`, mismatched `462`, had zero validation/generator/property/command failures, and kept all mismatches in the classified cleanup/scheduling classes (`coverage-forced-portable`, `dae-effectful-args`, `dae-localization`) with validating artifacts. Direct random-all also now has full raw-red evidence in `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705`: requested/compared `10000/10000`, normalized `9004`, mismatched `996`, zero failures, known direct/coverage cleanup families only. A 2026-07-06 stratified runtime replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706` sampled one saved mismatch from each residual class: six import-free direct DAE leaf samples had equal Node results, the `coverage-forced-portable` sample was import-blocked for runtime, all seven replays had zero validation/generator/property/command failures, and WAT/source review kept the sampled classes in measured cleanup territory rather than new semantic gaps. A follow-up red-first simple function-signature reuse fix removes the convergence raw-positive size caveat in saved-mismatch replays (`dae-convergence=60` direct random-all is now `-240` raw/canonical; `dae-convergence=625` dedicated-profile is now `-2500` raw/canonical), but both replay sets remain raw mismatches. Later red-first typed-block operand and typed-block result fixtures pass and replays over the saved direct random-all mismatches still leave `996/996` raw mismatches with zero failures, confirming that the known raw-red families are not hidden type-refinement misses. A cleanup-normalized replay of the same saved set (`.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-cleanup-normalized-20260706`) also left `996/996` raw mismatches with zero cleanup-normalized matches and zero failures while validating all `4980` input/raw/canonical wasm artifacts; the residual aggregate is measured Starshine-smaller (`-593155` raw / `-1294602` canonical / `-8900446` WAT total), but it is still not raw-green. A direct `dead-argument-elimination + remove-unused-module-elements` replay over those residuals (`.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`) also stayed fully raw-red (`996/996` mismatches, zero failures), with Starshine-smaller aggregate canonical/WAT output but `59` raw-positive `coverage-forced-portable` cases. 2026-07-07 residual analyses now fully classify three saved direct families without making direct DAE green: `.tmp/dae-tail-call-boundary-residual-classification-20260707.json` covers all `63` `dae-tail-call-boundary` direct random-all and post-tail-trim DAE+RUME residuals where Binaryen keeps one unused replacement local after removing the same unused parameter and pure `return_call` operands while Starshine omits that local (`-126` raw/canonical and `-1071` WAT bytes in each lane, no positive cases, representative Node-equal case `104`), `.tmp/dae-unused-params-residual-classification-20260707.json` covers all `214` `dae-unused-params` residuals where Binaryen keeps two unused replacement locals while Starshine omits them (`-428` raw/canonical and `-7276` WAT bytes in each lane, no positive cases, representative Node-equal case `20`), and `.tmp/dae-constant-args-residual-classification-20260707.json` covers all `197` `dae-constant-args` residuals where Binaryen materializes the uniform `i32.const 77` replacement through a fresh helper local while Starshine substitutes the same pure constant directly (`-1182` raw/canonical and `-10835` WAT bytes in each lane, no positive cases, representative Node-equal case `7`). A targeted follow-up on the largest raw-positive sample, `case-007094`, stayed a raw mismatch with zero failures, was runtime-blocked by imports under Node, validated its five input/raw/canonical wasm artifacts, and measured `+1006` raw but `-4123` canonical / `-28236` WAT bytes for Starshine; scanning all `59` raw-positive coverage cases under `.tmp/dae-raw-positive-analysis-20260706` quantified the subset as raw-positive (`+30761` total, `+348..+1006` per case) but all canonical/WAT-negative (`-186785` canonical / `-1280032` WAT total), with no scanned pre-terminal side-effecting keywords in input/Binaryen/Starshine raw WAT. A follow-up source review ties the first terminal to `gen_valid_append_core_control_ops()` in the coverage prelude before later effectful coverage templates, and a bounded `wasm-tools shrink` attempt on `case-007094` accepted the raw-positive/canonical-negative original but found no smaller module in 200 attempts (`41526` bytes, `0.00%` reduction); this supports but does not prove a pure-prefix/unreachable-tail cleanup classification for that subset. Direct DAE remains raw-red overall; `dae-optimizing` has its required random-all lane but the broader audit remains open on direct raw-red families, returned-value LUB/type-refinement breadth, and broader no-DWARF ordered scheduling/integration. The 2026-07-06 simple/nested block-carrier, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, interleaved droppable-debris single-result `if` operands, bounded typed-select multi-result operands, pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select operands, non-tail-lane multi-result typed-select operands, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop terminal returned values, and explicit-return block-carrier multi-result `if`-arm plus simple mixed single- and multi-result return/fallthrough `if` slices added focused source-backed coverage only and did not rerun compare-pass; direct DAE remains raw-red. A 2026-07-06 non-profile regular-GenValid full-suffix attempt is explicitly raw-red (`.tmp/pass-fuzz-dae-optimizing-full-postpass-context-regular-genvalid-10000-20260706`: `2015/10000` compared before max-failure cutoff, `0` normalized, `2015` mismatches, zero failures), and a no-DAE suffix control also mismatched `1000/1000`, so this is currently broader post-pass/portable-oracle output-shape drift rather than DAE-owned green evidence.
- The earlier localize-and-retry slice introduced the application now represented by `DaeCallsiteRewriteApplicationEvidence`: `DaeCallsiteLocalizationContext`, `dae_localization_scratch_type(...)`, and `dae_rewrite_callsite_with_localization_at(...)` turn proven hard removed operands into typed scratch-local writes, aggregate localized parameter slots, reject escaping branches, and extend both caller locals and corresponding removed callee slots through the shared recursive direct/`return_call` rewrite traversal. Slice 161 now routes this mode and ordinary replay/drop repair through one whole-argument plan/application. Non-null reference scratch declarations use the nullable form for defaultability. Red-first whitebox and public plain-DAE direct/`return_call` coverage pass. The direct `dae-localization` and `dae-effectful-args` singleton lanes are each normalized-green at `10000/10000` with zero mismatches/failures and Binaryen cache `10000/0`. Refreshed direct evidence remains raw-red: `.tmp/pass-fuzz-dae-direct-closeout-10000-after-scratch-localization-20260709` has `5000` normalized / `5000` mismatches, and `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-scratch-localization-20260709` has `9207` normalized / `793` mismatches; the `dae-optimizing` random-all rerun remains `9538` normalized / `462` mismatches. Whole-argument reload and frontier/touched scheduling remain open; exact-type caller scratch reuse is now implemented, plain-vs-optimizing scheduling is unchanged, and direct DAE remains raw-red.
- The 2026-07-09 convergence close read strongly classifies the deterministic generated `dae-convergence` residual as a measured Starshine win rather than a phase-convergence gap. All `625` refreshed dedicated-profile and `60` refreshed random-all cases perform the same leaf-then-wrapper signature convergence; every canonical WAT pair becomes equal after removing Binaryen's two unread replacement locals. Source review ties those locals to generic `ParamUtils::removeParameters(...)` allocation, while Starshine's shared projection keeps a former parameter slot only for a surviving write or localization. The lanes are uniformly Starshine-smaller, full Node replay is `685/685` equal, output-call timing is neutral within noise, and direct DAE remains raw-red because the classified representation difference is retained.
- The 2026-07-10 observability slice replaces duplicate recursive effect/branch scanners with `DaeInstructionObservabilityEvidence` and makes pre-unreachable prefix elision fail closed for effects, escaping control, unknown traps, zero divisors, signed overflow, and unsafe conversions. Red-first public coverage proves a reachable `i32.div_s` trap is retained. Exhaustive `.tmp/dae-coverage-forced-portable-classification-20260710.json` analysis covers all `259` current coverage residuals and `936` prefixes: one constant-safe numeric sequence, zero preterminal effects/unproved traps, self-contained `br_table 0 0`, uniformly smaller Starshine output, import-blocked full runtime replay, and a validating import-free reduced probe with faster Node compile and caught-trap medians. That full family is now classified as a measured Starshine win. Direct DAE remains intentionally raw-red at `9207/10000` because classified output differences are not normalized away, but every current direct random-all residual family now has strong evidence.
- The preceding scanner-consolidation slice separates target-independent argument actual discovery from target-specific GC evidence. `DaeGcActualShapeKind` / `DaeGcActualShapeDiscoveryEvidence` identify direct, block, loop, `if`, and typed-select shapes without an old or rewrite target; `DaeGcActualTypeEvidence` / `dae_gc_actual_type_evidence(...)` re-read the current body and compute candidate/refinement/LUB/rewrite facts for the current target. Candidate collection and refined-actual repair each rediscover per traversal, stale changed-terminal or wrong-target evidence rejects, scheduling is unchanged, and direct DAE remains raw-red.
- The preceding scanner-consolidation slice adds `DaeSingleBodyControlShapeEvidence` and `DaeSingleBodyControlRewriteEvidence` plus shared shape, rewrite-plan, application, and changed-instruction helpers. Explicit-return block/loop void repair and refined single-result GC block/loop repair now share control identity, original-versus-rewritten body selection, replacement block type, and reconstruction; paired-arm `if` repair remains separate, scheduling is unchanged, and direct DAE remains raw-red.
- The preceding scanner-consolidation slice adds `DaeAdjacentParamForwardEvidence` / `dae_adjacent_param_forward_evidence(...)` and routes self-forwarded local-use suppression, self-call origin handling, and dead-suffix dropped-self-call full-forward detection through one target-independent exact adjacent `local.get 0 .. local.get n-1` range proof. Target matching and the dead-suffix unreachable-root floor remain caller-owned, zero-parameter behavior is preserved per caller, ignored-local marking covers exactly the recovered range, direct DAE remains raw-red, and scheduling is unchanged. The preceding slice retained the complete argument-side `DaeGcSelectValueSlicesEvidence` branch inside `DaeGcActualShapeEvidence` and routed refined typed-select actual edits through `dae_apply_select_value_rewrites_to_body(...)`, making argument repair the third production consumer of the shared indexed left/right/select rewrite path instead of writing `slice.start`, `slice.start + 1`, and `slice.end - 1` directly. Candidate and rewrite traversals still recompute target-dependent evidence separately and singleton left/right acceptance is unchanged. The slice before that rewired argument-side typed-select actual discovery through `dae_gc_select_actual_value_slices_evidence(...)`; the slice before that added `dae_gc_select_value_slices_candidate_evidence(...)` for returned-value discovery and retained multi-result rewrite evidence. The preceding slice widened `DaeSelectValueSlices` with the already-proven `DaeSelectConditionLocalizationEvidence`, routed single-result and multi-result GC refinement through `dae_gc_select_value_slices_evidence(...)`, and replaced four parallel select-index arrays with one optional select-slice carrier array. The preceding slice added `DaeDroppedPureExpressionCleanupEvidence` plus `dae_dropped_pure_expression_cleanup_evidence(...)`, replacing the adjacent pure leaf-, unary-, and binary-expression-plus-`drop` scanners with one bounds/count/policy repair-plan carrier while preserving leaf debris policy and the local-get-left binary bailout. The preceding slice widened `DaeLiveLocalCopyCleanupEvidence` with explicit local-free-stack policy and moved the unconditional same-target `local.set`/`local.get`/`local.set` collapse into the shared copy-chain repair carrier. The preceding slice added `DaeDroppedLocalTeeSuffixCleanupEvidence` plus `dae_dropped_local_tee_suffix_cleanup_evidence(...)` so raw cleanup shares one local-tee suffix rewrite carrier for direct local-tee/drop, unary-suffix drop, single binary-suffix drop, and paired binary-suffix drop cleanup; direct DAE remains raw-red and scheduling is unchanged. The preceding slice added `DaeRedundantAccessCleanupEvidence` plus `dae_redundant_access_cleanup_evidence(...)` so raw cleanup shares one adjacent access-pair rewrite carrier for local-get/local-set removal, local-get/local-tee skipping, global-get/global-set removal, and local-tee/local-set replacement; direct DAE remains raw-red and scheduling is unchanged. The preceding slice added `DaeLocalGetRangeEvidence` plus `dae_local_get_range_evidence(...)` so droppable pure-stack cleanup evidence and dropped pure-leaf stack-subject evidence share suffix local-get/local-free cleanup range proof; direct DAE remains raw-red and scheduling is unchanged. The preceding slice added `DaeDroppablePureControlValueEvidence` plus `dae_droppable_pure_control_value_evidence(...)` so raw cleanup shares one type-section-backed pure block/loop/`try_table`/`if` value carrier for dropped pure-value arity and pure-`if` condition stripping; direct DAE remains raw-red and scheduling is unchanged. The preceding slice added `DaeI32ParamZeroResultLoopEvidence` plus `dae_i32_param_zero_result_loop_evidence(...)` and the exact-signature evidence projection so both typed-loop spill paths share single-`i32`-param/zero-result control signature, loop body, and optional iteration-local proof; direct DAE remains raw-red and scheduling is unchanged. The preceding slice added `DaeTerminalReturnBodyEvidence` plus `dae_terminal_return_body_evidence(...)` so terminal-return block-carrier recovery and nested block-carrier unwrapping share one terminal-`return` body proof. The preceding slice added `DaeEffectFreeBodyEvidence` plus `dae_effect_free_body_evidence(...)` so single-block result-carrier recovery, nested block-carrier unwrapping, and mixed return/fallthrough fallthrough evidence share one side-effect-free body proof. The preceding slice widened `DaeEmptyZeroResultControlCleanupEvidence` with primary/secondary body lengths and adds `dae_empty_zero_result_if_cleanup_evidence(...)` so raw cleanup block/loop/`try_table` empty-control dropping and zero-result `if` condition stripping consume one empty-control evidence carrier. The earlier slice added `DaeLiveLocalCopyCleanupEvidence` plus `dae_live_local_copy_cleanup_evidence(...)` so the raw cleanup five-instruction, three-instruction, and two-instruction live-local-copy chain branches share one replacement-plan carrier over the droppable pure-stack evidence. The earlier slice added `DaeLocalReferenceExtentEvidence` plus `dae_local_reference_extent_evidence(...)` so high-local cleanup guards, max-local projection, and the selected Func311 high-local trace path share one recursive local-reference extent carrier. The earlier slice added `DaeFlatMultiResultSuffixEvidence` plus `dae_flat_multi_result_suffix_evidence(...)` so the multi-result carrier-chain helper shares flat and terminal-`return` result-tail proof instead of switching between separate flat-start and multi-return-start scanners. The preceding local cleanup slice added `DaeDroppablePureStackEvidence` plus `dae_droppable_pure_stack_evidence(...)` so raw cleanup live-local-copy folding branches share trailing pure-stack bounds, local-get presence, and the local-free copy-stackification policy instead of repeatedly asking a boolean helper in each copy-chain recognizer. These are behavior-preserving value-slice/localization/cleanup evidence consolidations; plain-vs-optimizing scheduling is unchanged and direct DAE remains raw-red. The prior slice added `dae_trivially_dropped_prefix_evidence(...)` for shared full-prefix proof over `DaeDroppedOperandPairSkipEvidence`: typed-select value-slice recovery, flat multi-result and multi-return result-start recovery, multi-result suffix-bound enumeration, and the direct-GC flat result fallback consume one dropped-prefix carrier instead of a separate boolean-only scan. Earlier slices added `dae_no_escaping_branch_copied_slice_evidence(...)` for shared copied no-escape slice recovery over range-aware branch evidence; copied range, copied prefix, and copied whole-body helpers share one accepted-slice carrier. Earlier slices added `dae_single_result_control_gc_candidate_instr_evidence(...)` for shared instruction-level block/loop direct-GC result candidate dispatch over the existing `DaeSingleResultControlGcCandidateEvidence` body recovery and rewrite wrapper; `DaeEmptyZeroResultControlCleanupEvidence`, `dae_empty_zero_result_control_cleanup_evidence(...)`, and `dae_rebuild_cleaned_single_body_control_or_drop(...)` for shared raw-cleanup zero-result control body cleanup; `DaeControlBlockTypeGcResultSlot` and `dae_control_block_type_non_null_concrete_gc_result_slot_evidence(...)` for shared single-slot/trailing-slot non-null concrete GC result-slot proof, `dae_control_block_type_zero_input_evidence(...)` and `dae_control_block_type_zero_input_output_count_evidence(...)` for zero-input/exact-output-count proof, and `DaeControlBlockTypeGcResultEvidence` plus indexed non-null concrete GC result-slot evidence for direct GC actual and result-shape GC actual recovery.
- The first 2026-07-04 audit slice adds pass-owned GenValid profile plumbing: aggregate `dead-argument-elimination` with aliases `dae`, `dae-all`, and `dae-closeout`; focused leaves for unused params, effectful args, constant args, unused returns, argument/return type refinement, unseen-call boundary, tail-call boundary, localization, and convergence; plus the separate `dae-optimizing` profile/closeout alias. Follow-up template slices give `dae-unused-params` a deterministic private-helper/direct-caller module, `dae-effectful-args` a mutable-global side-effect producer whose result is passed as an unused helper actual, `dae-constant-args` a same-second-constant direct-caller module, `dae-unused-returns` a private single-result helper whose direct callers immediately drop the helper result, `dae-arg-type-refinement` and `dae-return-type-refinement` small GC modules with an empty base struct plus final leaf subtype for parameter/result type tightening, `dae-tail-call-boundary` a private unused-parameter-looking helper reached only through `return_call`, `dae-unseen-call-boundary` a private unused-parameter-looking helper escaped through `ref.func` in an active element segment plus an indirect caller, `dae-convergence` a private wrapper/leaf helper chain where pruning the leaf's unused parameter makes the wrapper's forwarded parameter removable on a later iteration, `dae-localization` a private unused-parameter-looking helper whose unused actual is a value-producing block around an effectful producer call, and `dae-optimizing` a five-function helper/caller module where a removable unused param unlocks a touched helper body with identity-add cleanup debris. This slice also promotes plain `dead-argument-elimination` and `dae` from boundary-only names to active module-pass registry/dispatcher entries with focused pipeline tests added. The aggregate now has concrete templates for all ten direct leaves; the optimizing sibling has a nested-cleanup trigger; focused Moon validation is green; direct DAE compare counters remain mismatch-heavy while the small `dae-optimizing` profile smoke is green. See [`./fuzzing.md`](./fuzzing.md).

## Page map

- [`./binaryen-strategy.md`](./binaryen-strategy.md)
  Deep dive into the actual Binaryen `version_129` implementation: phases, data structures, helper dependencies, legality gates, and the exact plain-vs-optimizing split.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md)
  Source-confirmed file/test map for `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `lubs.h`, `type-updating.h`, `return-utils.h`, and the split plain-vs-optimizing-vs-`dae2` lit surface.
- [`./wat-shapes.md`](./wat-shapes.md)
  Beginner-friendly before/after shape catalog for the main positive, bailout, preserved, and easy-to-misunderstand rewrite families.
- [`./starshine-strategy.md`](./starshine-strategy.md)
  Starshine status and port strategy, now refreshed with the scanner-consolidation inventory plus shared single-body block/loop reconstruction, typed-select returned-value, argument-actual GC, indirect-call result-slice, direct-call result signature/refinement, flat GC result-slot, typed-select `DaeValueSlice` bound-recovery, multi-result `DaeControlResultSlice` arm/control bound-recovery, per-lane `DaeControlResultGcEvidence`, paired terminal-arm `DaeControlResultPairGcEvidence`, shared fallthrough/terminal-return if-pair GC refinement evidence via `DaeMultiResultIfPairGcRefinementEvidence`, parametric typeidx arm GC-evidence extraction, separated `DaeGcActualShapeDiscoveryEvidence` / `DaeGcActualTypeEvidence` actual discovery and current-target candidate/repair extraction, `DaeOperandDisposition` range facts plus `DaeOperandRewriteDisposition` four-outcome callsite/localization classification, `DaeValueSlice`-carried operand disposition, callsite-wide `DaeValueSlice` collection/GC actual evidence, value-slice single-instruction actual evidence, `DaeCallBoundaryFacts` forwarding gates plus the `dae_collect_uniform_const_actuals_from_boundary_facts(...)` entrypoint for uniform-constant call-boundary traversal, `dae_record_uniform_const_candidate(...)` candidate merge/invalidation, `DaeCallBoundaryCandidate` owned-callee count/caller and self-recursion rewrite-eligibility evidence, `DaeModuleBoundarySnapshot` imported/defined/type-index/current-call boundary summaries consumed by core and selected rewrite loops, `DaeOriginalBoundarySnapshot` original imported/defined/escape/param/call-summary setup facts consumed by the core, the `dae_rewrite_dropped_result_calls_with_policy_instrs(...)` dropped-result call/drop repair traversal policy, `DaeDroppedResultScope` selected-vs-all plus full-module dropped-result repair scoping, `DaeDroppedResultCandidate` boundary/current-call dropped-result candidate facts, and `DaeOriginalResultPreservationEvidence` / `dae_original_result_preservation_evidence_from_row(...)` original-vs-current result-preservation guard sharing, plus the 2026-07-08 `dae_dropped_result_candidate_from_boundary_facts(...)` follow-up that routes dropped-result candidate eligibility through `DaeCallBoundaryFacts` / `DaeCallBoundaryCandidate`, the `dae_original_result_preservation_blocks_argument_side_result_repair(...)` follow-up that shares argument-side result-repair blocking over the original result-preservation carrier, and the `DaeDroppedResultRemovalLegalityEvidence` follow-up that shares result-count-aware tail-call / undropped-call / large-result cleanup blockers across selected and full-module dropped-result cleanup loops, the superseding `DaeOperandRewriteAction` / `DaeOperandRewriteDisposition` follow-up that makes direct-drop, effect-preserving-drop, localize-and-retry, and escaping-branch-reject decisions explicit across callsite removal and stack-slice localization, the `DaeOperandRemovalPlan` / `dae_callsite_operand_removal_plan(...)` follow-up that centralizes consistency-checked per-callsite keep/replay/drop repair planning over the callsite evidence carrier, and the `DaeDroppedResultRepairPlan` follow-up that carries all-caller vs no-param dead-suffix repair plus optional dropped-call-drop cleanup decisions across selected and full-module dropped-result repair paths, and the `DaeCallsiteValueSliceEvidence` follow-up that carries call index, argument count, entry-stack context, cursor, recovered value slices, and repair dispositions through callsite removal/refinement/constant/GC/global-suffix loops, plus the `DaeCallsiteSingleInstructionActualEvidence` follow-up that centralizes single-instruction actual lookup over that callsite carrier for uniform-constant and selected global-suffix loops, and the `DaeDroppedResultModuleContext` follow-up that centralizes imported/type/function section setup, params/results, selected/full dropped-result scope, result-removal legality, and selected-lane setup timing for selected dropped-result removal plus unobserved-unreachable, dead-self-suffix, and uncalled-result cleanup paths, and the `DaeOriginalResultPreservationContext` follow-up that centralizes original result-preservation input arrays for the main core zero-param/current-param lanes and reverse exact-literal lane, plus the `DaeOriginalResultPreservationRow` follow-up that centralizes per-definition original-boundary guard fact projection for result-preservation, no-param dropped-caller, and parameterized self-side-chain evidence, and the `DaeOriginalResultRemovalPruneRow` follow-up that centralizes later-mutual, void-side-caller, mixed dropped/undropped caller, dropped-only result-side, mixed-self suffix, same-self result-operand, and escaped-block self-result predicates, and the `DaeOperandRemovalPlan` follow-up that shares explicit callsite removed-operand repair plans between callsite rewriting and trivial-droppable checks, plus the `DaeControlEntryStackEvidence` / `dae_control_entry_stack_values(...)` follow-up that centralizes nested control entry/result stack context and child entry-stack projection for callsite removal, refined-GC actual, uniform-constant actual, and direct-GC actual recursive loops, the `DaeNestedControlEntryEvidence` / `dae_nested_control_entry_evidence(...)` follow-up that carries instruction-level block/loop/`try_table`/`if` entry-stack facts through the same recursive loops, and the `DaeControlBlockTypeSignatureEvidence` / `dae_control_block_type_signature_evidence(...)` follow-up that centralizes block-type params/results/input/output/typeidx evidence for block-type stack effects, control-entry evidence, and parametric typeidx terminal-control refinement, the `DaeSingleBlockResultCarrierEvidence` / `dae_single_block_result_carrier_evidence(...)` follow-up that centralizes effect-free single-block multi-result carrier evidence for block flattening, block-plus-`return`, and nested carrier unwrapping inside `dae_find_multi_result_control_slice(...)`, and the `dae_stack_bool_*` / `dae_stack_int_*` helper follow-up that centralizes original dead-suffix/result-preservation stack operand-source lookup and push/pop effect updates across bool/int provenance scanners, plus the `DaeDeadSuffixEvidence` / `dae_dead_suffix_evidence(...)` follow-up that centralizes unreachable-root and suffix-tail recovery across dead-suffix repair, target/count, escaped-result, self-call-param, and side-call scanners, the `DaeTailCallBoundaryEvidence` / `dae_tail_call_boundary_evidence(...)` follow-up that centralizes direct-vs-indirect tail-call blocker facts for direct-call rewrite legality and dropped-result removal legality, the `DaeDirectCallTargetEvidence` / `dae_direct_call_target_evidence(...)` follow-up that centralizes target-specific direct-call counts plus dropped, undropped, and live-prefix undropped-call facts for containment/count/legality scanners, the `DaeDeadSuffixDirectCallEvidence` / `dae_dead_suffix_direct_call_evidence(...)` follow-up that centralizes dead-suffix dropped/undropped target facts for full-module dropped-result cleanup gates, the `DaeDeadSuffixDirectTargetEvidence` / `dae_dead_suffix_direct_target_evidence(...)` follow-up that centralizes direct/dropped/undropped dead-suffix target lists for original-boundary target collectors, the `DaeDroppedResultScopeDirectCallEvidence` / `dae_dropped_result_scope_direct_call_evidence(...)` follow-up that centralizes scoped selected-vs-all caller direct-call count/drop/undropped/live-prefix facts for dropped-result removal legality, the `DaeOriginalNoParamDroppedCallerGuardEvidence` / `dae_original_no_param_dropped_caller_guard_evidence(...)` follow-up that centralizes safe indexed no-param dropped-caller original-boundary guard facts for result-preservation and param-operand side-call collection, the `DaeOriginalParameterizedUndroppedSelfSideChainEvidence` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` / `dae_original_parameterized_undropped_self_side_chain_evidence_from_row(...)` follow-up that centralizes safe indexed parameterized self-side-chain guard facts for result-preservation and unobserved/current core checks, the `DaeDroppedResultCalleeRewriteEvidence` / `dae_dropped_result_callee_rewrite_evidence(...)` follow-up that centralizes callee suffix/simplification/result-removal-blocker evidence for full-module dropped-result cleanup gates, the `DaeEscapedResultDirectCallEvidence` / `dae_escaped_result_direct_call_evidence(...)` follow-up that centralizes dropped/undropped escaped-result direct-call evidence for original dead-suffix preservation collectors, and the `DaeStackOperandWindowEvidence` / `dae_stack_operand_window_evidence(...)` follow-up that centralizes top-of-stack operand-window bounds for bool/int provenance scanners, plus the `DaeStackEffectEvidence` / `dae_stack_effect_evidence(...)` follow-up that centralizes instruction stack-effect evidence before bool/int provenance push/pop updates and the `dae_stack_single_value_producer_evidence(...)` follow-up that shares zero-input/one-output producer proof across value-slice recovery, typed-select conditions, indirect-call result-slice validation, and multi-result refinement suffix consumers, and the `dae_control_single_result_block_type_evidence(...)` follow-up that shares zero-input/one-output block-type proof across direct GC result block/loop/`if` candidate paths and a multi-result suffix consumer, plus the `DaeNestedControlChildrenEvidence` / `dae_nested_control_children_evidence(...)` follow-up that centralizes primary/secondary nested-control child-body evidence for uniform-constant and direct `struct.new_default` actual collection and the `dae_rebuild_nested_control_children(...)` follow-up that centralizes nested-control reconstruction for refined-GC actual rewriting and callsite removal, plus the `DaeMultiResultSuffixBoundsEvidence` / `dae_multi_result_suffix_bounds_evidence(...)` follow-up that centralizes terminal/non-terminal suffix bounds for the multi-result single-`if` and typed-select operand scanners, and the `DaeStackSliceSimulationEvidence` / `dae_stack_slice_simulation_evidence(...)` follow-up that centralizes bounded stack-slice simulation for multi-instruction typed-select condition recovery and parametric typeidx prefix-consumption checks, the `DaeStackSliceTransitionEvidence` / `dae_stack_slice_transition_evidence(...)` follow-up that centralizes expected-final-stack acceptance over those same two scanners, the `DaeStackSliceLocalizationEvidence` / `dae_stack_slice_localization_evidence(...)` follow-up that combines transition, no-escape, copied-slice, raw disposition, and four-outcome rewrite disposition evidence for those same two scanners, the `DaeFunc237LocalCopyGapEvidence` / `dae_func237_local_copy_gap_evidence(...)` follow-up that shares immediate-copy and used-temp-copy gap range policy, the `DaeFunc237LocalCopyCandidateEvidence` / `dae_func237_local_copy_candidate_evidence(...)` follow-up that shares parameter-local rejection, mode-specific local-use counts, and root `try_table` preservation over `DaeLocalUsageEvidence` for those same local-copy loops, the `DaeTargetCallsiteEvidence` / `dae_target_callsite_evidence(...)` follow-up that shares direct-only vs direct-or-`return_call` target-callee matching across call-boundary and callsite-repair consumers, the `DaeDroppedPureLeafCleanupEvidence` / `dae_dropped_pure_leaf_cleanup_evidence_from_stack(...)` / `dae_dropped_pure_leaf_cleanup_evidence_from_leaf(...)` follow-up that shares raw-cleanup pure-leaf preservation policy across stack-truncation and adjacent leaf-drop paths, plus the `DaeSingleResultControlGcCandidateEvidence` / `dae_single_result_control_gc_candidate_evidence(...)` / `dae_apply_single_result_control_gc_candidate_evidence(...)` follow-up that shares direct GC result block/loop candidate-body recovery and rewrite wrapping, plus the `DaeResultRewriteApplicationEvidence` / `dae_apply_result_rewrites_to_body(...)` follow-up that shares masked result-slot rewrite application across flat control-result GC evidence, paired control-result LUB evidence, mixed returned-arm repair, and mixed terminal-result repair, plus the `DaeSelectValueRewriteApplicationEvidence` / `dae_apply_select_value_rewrites_to_body(...)` follow-up that shares typed-select left/right/select rewrite application across single-result returned-value select repair and flat multi-result typed-select operand repair, plus the `DaeSingleResultControlPairApplicationEvidence` / `dae_apply_single_result_control_pair_gc_evidence(...)` follow-up that shares value-result vs void-result `if` rewrite application across implicit returned-value and explicit-return zero-result refinement paths, plus the `DaeIndexedInstructionRewriteApplicationEvidence` / `dae_apply_indexed_instruction_rewrites_to_body(...)` follow-up that shares sparse indexed body-edit application under masked result-slot and typed-select repair helpers, plus the `DaeMultiResultOperandSlotEvidence` / `dae_multi_result_operand_slot_prefix_evidence(...)` / `dae_multi_result_operand_slot_single_producer_evidence(...)` follow-up that shares multi-result suffix slot prefix and fallback evidence under single-`if` and typed-select operand scanners, plus the `DaeStackSliceLocalizationPolicy` / `dae_stack_slice_localization_evidence_from_policy(...)` follow-up and `dae_stack_slice_localization_policy_accepts(...)` projection that share select-condition and parametric typeidx prefix localization policy acceptance with copied no-escape slice evidence, plus the `dae_control_block_type_signature_evidence_from_type_sec(...)` / `dae_control_block_type_stack_shape_evidence(...)` / `dae_control_block_type_exact_signature_accepts(...)` follow-up that shares type-section-backed control block signature proof between control-entry stack evidence and raw cleanup block-type predicates, plus the `dae_control_block_type_zero_input_evidence(...)` follow-up that shares zero-input control block proof between single-block result-carrier recovery and direct GC explicit-return control candidate checks, plus the `dae_control_block_type_zero_input_output_count_evidence(...)` follow-up that shares zero-input/output-count control block proof between single-block result-carrier recovery, mixed if/fallthrough GC candidates, terminal-return block carriers, and multi-result control refinement carrier gates, plus the `DaeControlBlockTypeGcResultEvidence` / `dae_control_block_type_non_null_concrete_gc_result_evidence(...)` / `dae_control_block_type_non_null_concrete_gc_result_slot_evidence(...)` / `dae_control_block_type_single_non_null_concrete_gc_result_evidence(...)` / `dae_control_block_type_last_non_null_concrete_gc_result_evidence(...)` follow-up that shares non-null concrete GC result-slot proof between direct GC actual candidate recovery and result-shape GC actual recovery, with single-result and trailing-result wrappers sharing the same slot selector and signature-to-result projection, plus the `DaeEmptyZeroResultControlCleanupEvidence` / `dae_empty_zero_result_control_cleanup_evidence(...)` / `dae_rebuild_cleaned_single_body_control_or_drop(...)` follow-up that shares zero-result block/loop/`try_table` empty-body cleanup decisions after raw child cleanup, plus the `dae_single_result_control_gc_candidate_instr_evidence(...)` follow-up that shares instruction-level block/loop direct-GC result candidate dispatch over the existing single-result control GC body/rewrite carrier, plus the `dae_apply_first_instruction_rewrite_to_body(...)` follow-up that shares first-matching sparse body repair scans across three Func237 cleanup-repair loops and the unreachable-after-if-else recursive body scan, plus the `dae_try_rewrite_first_control_child_body(...)` follow-up that shares recursive control-child body-repair reconstruction across Func237 dropped-result branch-carrier folding and unreachable-after-if-else folding, plus the `dae_apply_first_current_or_control_child_body_rewrite(...)` follow-up that shares current-body-then-control-child recursive rewrite application across four Func237 cleanup-repair paths, plus the `DaeRecursiveBodyRewriteApplicationEvidence` / `dae_apply_first_recursive_body_rewrite(...)` follow-up that records current-body versus first-control-child recursive body-rewrite application evidence and is now consumed by that wrapper plus Func237 inner-iteration loop wrapping and i32-param-loop spill repair, plus the `dae_apply_first_recursive_instruction_body_rewrite(...)` follow-up that shares first-matching recursive instruction rewrite evidence across the control-child projection and Func237 leading-retain/terminal-`br 0` recursive scans, plus the `DaeDroppedOperandPairSkipEvidence` / `dae_dropped_operand_pair_skip_evidence(...)` follow-up that centralizes dropped trivial-operand pair skipping for the multi-result single-`if` and typed-select suffix scanners, the `DaeMultiResultIfPairGcRefinementEvidence` / `dae_multi_result_if_pair_gc_refinement_evidence(...)` follow-up that centralizes paired fallthrough/terminal-return if GC refinement evidence, the `DaeControlResultPairRefinementApplicationEvidence` / `dae_apply_control_result_pair_refinements(...)` follow-up that centralizes paired refined-result vector application for direct and parametric typeidx refinement paths, the `DaeControlResultUndroppedDirectCallEvidence` / `dae_control_result_undropped_direct_call_evidence(...)` follow-up that centralizes block/loop/`try_table`/`if` result-arity plus undropped-direct-call blocker evidence for dead-suffix result-preservation scans, and the range-aware `DaeEscapingBranchEvidence` / `dae_escaping_branch_range_evidence(...)` / `dae_no_escaping_branch_range_evidence(...)` follow-up plus `dae_no_escaping_branch_prefix_evidence(...)` / `dae_no_escaping_branch_suffix_evidence(...)` projections and copied range/prefix/suffix `DaeNoEscapeSliceEvidence` helpers plus the terminal-if no-escape, mixed return/fallthrough arm, and single-result control-pair evidence helpers that centralize branch-escape and return/control-arm LUB evidence for localization/control/refinement gates, plus the `dae_multi_result_control_carrier_chain_slice(...)` follow-up that centralizes fallthrough, terminal-return, and appended-return multi-result carrier-chain recovery inside `dae_find_multi_result_control_slice(...)`, plus the `DaeFlatMultiResultSuffixEvidence` / `dae_flat_multi_result_suffix_evidence(...)` follow-up that centralizes flat and terminal-return result-tail proof under that carrier-chain helper, plus the `DaeLocalReferenceExtentEvidence` / `dae_local_reference_extent_evidence(...)` follow-up that centralizes high-local limit and max-local proof for cleanup-local preservation gates, plus the `DaeLiveLocalCopyCleanupEvidence` / `dae_live_local_copy_cleanup_evidence(...)` follow-up that centralizes raw cleanup live-local-copy chain replacement across the five-instruction, three-instruction, and two-instruction copy-chain branches over the droppable pure-stack carrier, the widened `DaeEmptyZeroResultControlCleanupEvidence` / `dae_empty_zero_result_if_cleanup_evidence(...)` follow-up that shares empty zero-result control cleanup evidence between block/loop/`try_table` body dropping and zero-result `if` condition stripping, the `DaeEffectFreeFallthroughEvidence` / `dae_effect_free_fallthrough_evidence(...)` follow-up that shares mixed-arm side-effect-free fallthrough proof between single-result and multi-result return/fallthrough refinement, the `DaeEffectFreeBodyEvidence` / `dae_effect_free_body_evidence(...)` follow-up that shares side-effect-free body proof between single-block result-carrier recovery, nested block-carrier unwrapping, and mixed-arm fallthrough evidence, and the `DaeOriginalResultPreservationRow` / `dae_original_result_preservation_row_from_context(...)` follow-up that centralizes per-definition original-boundary result-preservation guard fact projection, plus the `DaeOriginalResultRemovalPruneRow` / `dae_original_result_removal_prune_row_from_preservation_row(...)` follow-up that centralizes original-boundary result-removal prune evidence for later-mutual, void-side-caller, mixed dropped/undropped caller, dropped-only result-side, mixed-self suffix, same-self result-operand, and escaped-block self-result cleanup gates, plus the `DaeOriginalDeadSuffixSideCallEvidence` / `dae_original_dead_suffix_side_call_evidence(...)` follow-up that shares void-side cleanup, recursive result-side chain, and same-self result-operand preservation gates across two zero-direct-call cleanup decisions, plus the `DaeOriginalResultRemovalPruneEvidence` / `dae_original_result_removal_prune_evidence_from_row(...)` follow-up that aggregates params-absent cleanup and uncalled-after-param-cleanup blockers over the original-boundary prune row, while still pointing to remaining scanner-heavy broader typeidx/control evidence, boundary candidate legality beyond forwarding gates plus count/caller/self-recursion eligibility and the first module/original snapshots, no-param dropped-caller guard, parameterized self-side-chain guard, result-preservation input carrier, and per-definition result-preservation/prune rows, repair application beyond the shared four-outcome operand rewrite disposition and consistency-checked callsite plan, including scratch-local allocation and actual retry scheduling across value-slice/callsite evidence/paired GC/final actual-shape/single-instruction/control-entry projection/nested-control entry carrier/block-type signature/single-block result-carrier/carrier-chain/stack-window/stack-slice-simulation/expected-final-transition repair, dropped-result legality beyond the first boundary-based candidate, result-removal blocker, tail-call boundary blocker, direct-call target and dead-suffix target carriers, dead-suffix direct target-list carrier, all/no-param repair-plan, and selected/full-module context carriers plus dropped-result callee rewrite evidence, shared bounded body-rewrite fixpoint evidence plus the unchanged-body projection now drive Func237 inner-loop wrapping, selected zero-tee/set splitting, three Func237 local-copy cleanup families, and Func256 mapped-loop-condition repair; remaining work includes dropped-result result-removal policy, true localization/retry, module-core convergence scheduling beyond that local driver, and cleanup scheduling.
- [`./starshine-port-readiness-and-validation.md`](./starshine-port-readiness-and-validation.md)
  First-slice and validation bridge for a future Starshine port: no-rewrite analyzer, scalar direct-call parameter deletion, follow-up rewrite families, exact local code surfaces, Binaryen `--dae` oracle checks, and the guardrail that plain DAE must not run the optimizing sibling's nested cleanup replay.

## Current maintenance rule

Treat the retained 2026-05-04 current-main research digest and the 2026-04-26 readiness digest as the retained tagged-source/current-main and Starshine implementation bridges.
- Treat this folder as the canonical home for future `dead-argument-elimination` / `dae` research and port planning.
- Do not rely on the older **unimplemented** wording in this dossier; treat it as superseded by the local active implementation and keep this page open for a full status refresh.
- Keep the relationship to [`../dae-optimizing/index.md`](../dae-optimizing/index.md) explicit instead of silently folding plain DAE facts into the optimizing dossier.

## Sources

- research note 0435
- research note 0406
- research note 0293
- research note 0159
- research note 0230
- [`../../../../../src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
- [`../../no-dwarf-default-optimize-path.md`](../../no-dwarf-default-optimize-path.md)
- [`../tracker.md`](../tracker.md)
- [`../../../../../agent-todo.md`](../../../../../agent-todo.md)
- Binaryen `version_129` implementation and test sources:
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/DeadArgumentElimination.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/pass.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/opt-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/param-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/return-utils.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/lubs.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/type-updating.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae_tnh.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc-refine-params.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-gc-refine-return.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-optimizing.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/dae-refine-params-and-optimize.wast>
