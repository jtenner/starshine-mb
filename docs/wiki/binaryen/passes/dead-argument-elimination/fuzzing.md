---
kind: workflow
status: working
last_reviewed: 2026-07-10
sources:
  - ../../../tooling/pass-fuzz-compare.md
  - ../../../../../scripts/lib/pass-fuzz-compare-task.ts
  - ../../../../../src/validate/gen_valid.mbt
  - ../../../../../src/validate/gen_valid_tests.mbt
  - ../../../../../src/validate/pkg.generated.mbti
---

# `dead-argument-elimination` Fuzzing Profile

This page tracks the pass-owned GenValid lanes for the plain `dead-argument-elimination` / `dae` audit and the related `dae-optimizing` closeout lane.

## Dedicated profiles

The DAE profile registry now has a replayable aggregate plus focused leaf names:

- Aggregate: `dead-argument-elimination`
- Aliases: `dae`, `dae-all`, `dae-closeout`, `dead-argument-elimination-all`, `dead-argument-elimination-closeout`, `pass-dae-closeout`
- Leaves:
  - `dae-unused-params`
  - `dae-effectful-args`
  - `dae-constant-args`
  - `dae-unused-returns`
  - `dae-arg-type-refinement`
  - `dae-return-type-refinement`
  - `dae-unseen-call-boundary`
  - `dae-tail-call-boundary`
  - `dae-localization`
  - `dae-convergence`
- Optimizing sibling: `dae-optimizing`
- Optimizing alias: `dae-optimizing-closeout`

The aggregate currently samples the ten direct-DAE leaves with weights biased toward the core positive rewrite families: unused params and constant args have weight `3`, effectful args and unused returns have weight `2`, and the refinement/boundary/localization/convergence leaves have weight `1` each. The optimizing sibling remains a separate profile so direct DAE signoff and nested-cleanup signoff can be reported independently.

## Generator envelope

`gen_valid_dead_argument_elimination_profile_config(profile)` keeps DAE cases small and direct-call oriented:

- no imports by default,
- 3 defined functions for most leaves,
- 4 defined functions for the effectful-argument and localization leaves,
- 5 defined functions for convergence and optimizing contexts,
- scalar-only value types by default,
- GC/subtyping enabled only for argument/return type-refinement leaves,
- tail calls enabled only for `dae-tail-call-boundary`,
- table/element/indirect-call surfaces enabled only for `dae-unseen-call-boundary`,
- globals enabled only for `dae-effectful-args` and `dae-localization`.

The first slice wired the profile names, aliases, composite membership, config helper, public API snapshot, and registry tests. Follow-up slices filled the body-template layer:

- `dae-unused-params` bypasses the generic body generator and emits a deterministic three-function module where private helper function `1` has three `i32` parameters, uses only parameter `0`, and is called directly from the exported entry and an extra private caller. This gives the direct DAE lane a stable unused-parameter rewrite trigger.
- `dae-effectful-args` bypasses the generic body generator and emits a deterministic four-function module with one mutable private `i32` global. Private helper function `1` has signature `(i32, i32) -> i32`, uses only parameter `1`, and both direct callers supply parameter `0` from private producer function `2`, whose body performs `global.set` before returning an `i32`. This gives the direct DAE lane a stable effectful-unused-actual preservation trigger.
- `dae-constant-args` now also bypasses the generic body generator and emits a deterministic three-function module where private helper function `1` has signature `(i32, i32) -> i32`, uses both parameters, and both direct callers pass the same second `i32.const 77` actual while varying the first actual. This gives the direct DAE lane a stable same-constant-argument materialization/removal trigger.
- `dae-unused-returns` now bypasses the generic body generator and emits a deterministic three-function module where private helper function `1` has signature `(i32, i32) -> i32`, both direct callers immediately `drop` the helper result before returning their own constants, and the helper computes one `i32` result from its parameters. This is the generated single-result positive DAE-F leaf: Binaryen and Starshine both remove the dropped private helper result in the refreshed focused lane. The prior two-result template is superseded as a profile trigger but remains recorded below as a sampled multi-result tuple/local-spill avoidance residual from `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`.
- `dae-arg-type-refinement` now bypasses the generic body generator and emits a deterministic three-function GC module with a non-final empty base struct type, a final leaf subtype, and a private helper whose parameter is typed as the broad nullable base while both owned direct callers pass `struct.new_default` values of the leaf subtype. The helper consumes the parameter with `ref.is_null`, so this leaf targets argument type refinement rather than unused-parameter deletion. A 2026-07-05 generator fix made the base non-final after `wasm-tools` rejected the subtype leaf when the base used the shorthand final `comp_type` form.
- `dae-return-type-refinement` now bypasses the generic body generator and emits a deterministic three-function GC module with the same non-final base/final leaf subtype pair. A private helper is declared as returning the broad nullable base type but always produces `struct.new_default` of the leaf subtype, and both owned direct callers immediately consume the helper result with `ref.is_null`. This gives the direct DAE lane a stable return type-refinement trigger.
- `dae-tail-call-boundary` now bypasses the generic body generator and emits a deterministic three-function module where private helper function `1` has signature `(i32, i32) -> i32`, uses only parameter `1`, and is reached from the exported entry plus an extra private caller only through `return_call`. This originally targeted a presumed tail-call bailout; the 2026-07-05 direct compare instead showed Binaryen rewrites this return-call-only shape. A later 2026-07-05 red-first pass fixture fixed Starshine's direct `return_call` call-fact collection and callsite operand repair; remaining sampled tail-leaf compare drift is only Binaryen retaining one unused body local after the signature prune while Starshine omits it.
- `dae-unseen-call-boundary` now bypasses the generic body generator and emits a deterministic three-function module with one `funcref` table and one active element segment containing `ref.func 1`. The exported entry directly calls private helper function `1`, private function `2` invokes the same helper signature through `call_indirect`, and the helper uses only parameter `1`. This gives the direct DAE lane a stable unseen-call/ref.func/table/indirect-call bailout trigger that otherwise looks like an unused-parameter rewrite candidate.
- `dae-convergence` now bypasses the generic body generator and emits a deterministic five-function module where private wrapper function `1` forwards both parameters to private leaf function `2`, while the leaf uses only parameter `0`. The wrapper's second parameter is initially syntactically used only as the leaf's dead actual, so pruning the leaf signature unlocks a later wrapper-signature prune. This gives the direct DAE lane a stable convergence/stale-analysis retry trigger without tail-call or unseen-call boundaries.
- `dae-localization` now bypasses the generic body generator and emits a deterministic four-function module with one mutable private `i32` global. Private helper function `1` has signature `(i32, i32) -> i32`, uses only parameter `1`, and both direct callers supply parameter `0` from a value-producing block that calls private side-effect producer function `2`, whose body performs `global.set` before returning an `i32`. This gives the direct DAE lane a stable hard effectful operand that must be localized or otherwise preserved before the unused-parameter rewrite can safely complete.
- `dae-optimizing` now also bypasses the generic body generator and emits a deterministic five-function module where private helper function `1` has signature `(i32, i32) -> i32`, uses only parameter `1`, and leaves an identity `local.get 1; i32.const 0; i32.add` tail in the touched helper body while four direct callers make parameter `0` removable. This gives the optimizing sibling a stable nested-cleanup trigger after the core DAE signature rewrite changes the helper.

All ten direct-DAE leaves plus the optimizing sibling now have DAE-specific body templates and shape facts. Plain `dead-argument-elimination` and `dae` are also wired as active module-pass names so the dedicated direct-DAE compare lane has a local execution target. A 2026-07-05 continuation compiled the profile tests and confirmed the two focused direct-DAE GC type-refinement tests red before implementing narrow owned-direct `struct.new_default` argument/result refinement; a later continuation did the same for the direct `return_call` unused-parameter shape. A 2026-07-06 follow-up broadened live GC argument refinement one step beyond bare allocation operands: a Binaryen v130 probe showed `--dae` narrows a broad nullable base parameter when every owned direct caller passes a non-null leaf typed-block operand, and red-first `dae_optimizing_test.mbt` coverage now passes after Starshine accepts non-null type-indexed block-result operands while preserving candidate exactness/nullability. A later 2026-07-06 result-refinement follow-up probed a typed-block helper body, confirmed Binaryen narrows the broad result to exact leaf and flattens the wrapper, then added red-first coverage that now passes after Starshine recognizes the wrapper body and rewrites it to the exact allocation expression when tightening the result signature. The focused tests now pass, and the `dae-unused-returns` profile now provides generated single-result DAE-F coverage. A later 2026-07-06 argument-LUB follow-up probed a two-caller Binaryen v130 shape where one owned direct caller passes `struct.new_default $leaf1`, another passes `struct.new_default $leaf2`, and both leaves share a non-final `$mid` subtype; `wasm-opt --dae` narrows the live broad `(ref null $base)` helper parameter to non-null `(ref $mid)`. Red-first focused Starshine coverage failed while the parameter stayed `(ref null $base)`, then passed after DAE accumulated a nearest common supertype for non-null direct-call GC actual candidates instead of rejecting differing leaf candidates. A later 2026-07-06 returned-value LUB follow-up probed a helper body whose terminal `if` returns distinct leaf allocations through a broad `(ref null $base)` result; Binaryen v130 narrows the helper result and the terminal `if` block type to non-null `(ref $mid)`, and red-first Starshine coverage now passes after DAE computes the returned-arm LUB and rewrites the terminal result instruction type. A subsequent explicit-return LUB slice probed the same two leaf arms when each branch performs `struct.new_default; return` and leaves a trailing `unreachable`; Binaryen v130 again narrows the helper result to non-null `(ref $mid)`, and red-first Starshine coverage now passes after DAE recognizes terminal void `if` arms whose explicit return operands have a common GC LUB. A later loop-carried explicit-return slice probed a terminal void `loop` whose body creates a leaf allocation, explicitly returns it, and leaves trailing `unreachable`; Binaryen v130 narrows the helper result to exact leaf, and red-first Starshine coverage now passes after DAE admits void loop bodies with explicit-return result evidence. A follow-up typed-control explicit-return slice probed typed terminal `if`, `block`, and `loop` variants whose value exits through `return`; Binaryen v130 rebuilds those terminal controls as void while narrowing the helper result to a common non-null `$mid` for the typed `if` and exact leaf for the typed block/loop, and red-first Starshine coverage now passes after DAE accepts `ValTypeBlockType` explicit-return evidence and rebuilds those terminal controls as void. A later flat multi-result slice probed helpers returning `(result i32 (ref null $base))` whose flat result operands include `i32.const` and `struct.new_default $leaf`, both as implicit fallthrough and with an explicit trailing `return`; Binaryen v130 narrows the reference result to exact leaf while preserving the scalar result, and red-first Starshine coverage now passes after DAE refines per-result flat operand candidates. Follow-up probes showed Binaryen applies the same narrowing through a trivially dropped prefix and through simple zero-input multi-result block carriers, both with fallthrough and explicit `return`; red-first Starshine coverage now passes after DAE accepts trivially dropped prefixes and flattens those simple block carriers while tightening the function signature. A later simple multi-result `if`-arm probe showed Binaryen narrows `(result i32 (ref null $base))` helpers to `(result i32 (ref $mid))` when fallthrough or explicit-return arms produce distinct leaf allocations with a common non-final `$mid`; red-first Starshine coverage now passes after DAE computes per-result LUBs across those simple arms. A subsequent typeidx terminal `if` probe showed Binaryen also narrows single-result helpers through zero-param type-indexed terminal `if` carriers, both for fallthrough and explicit `return`; red-first Starshine coverage now passes after DAE accepts zero-param typeidx `if` block types for returned-value and explicit-return LUB recognition. A later fallthrough-loop probe showed Binaryen narrows broad single-result helpers when a zero-param typed or type-indexed terminal `loop` falls through with a leaf allocation; red-first Starshine coverage now passes after DAE accepts zero-param single-result loop carriers and rewrites them to narrowed value loop types while tightening the helper result. A subsequent local-result probe showed Binaryen narrows a broad nullable base result to nullable leaf when a helper returns `local.get` of a narrower local; red-first Starshine coverage now passes after DAE threads static function-local type evidence into result-candidate recognition and allows nullable candidates only when the old result is nullable. Later nested block-carrier probes showed Binaryen narrows through nested zero-input multi-result blocks for both fallthrough and explicit-return operands; red-first Starshine coverage now passes after DAE recursively unwraps branch-free/effect-free nested block carriers and also admits a terminal function `return` as the only branch after branch-free/effect-free result operands. This is still not broad result-LUB or full DAE closeout: arbitrary returned-value/control/candidate unification beyond the focused terminal-`if` allocation, explicit-return `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, static local.get evidence, zero-param typeidx block/terminal-`if` carriers, fallthrough typed/typeidx loop carriers, and flat/droppable-prefix/block-carrier/nested-block-carrier/simple-`if`-arm multi-result allocation shapes remains open, and the direct DAE aggregate compare remains raw-red at 10000 dedicated-profile cases even though the residual selected-profile families are the same measured Starshine-win cleanup families seen at smaller scales.

## Recommended lanes

Direct DAE dedicated-profile smoke:

```sh
bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dead-argument-elimination --gen-valid-profile dead-argument-elimination --out-dir .tmp/pass-fuzz-dead-argument-elimination-dae-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures
```

Optimizing sibling dedicated-profile smoke with DAE cleanup normalizers:

```sh
bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass dae-optimizing --gen-valid-profile dae-optimizing --normalize drop-consts --normalize unreachable-control-debris --out-dir .tmp/pass-fuzz-dae-optimizing-profile-10000 --jobs auto --starshine-bin _build/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures
```

Closeout still requires the full pass matrix from [`../../../tooling/pass-fuzz-compare.md`](../../../tooling/pass-fuzz-compare.md): regular GenValid `100000`, explicit `--wasm-smith` `10000`, dedicated DAE profile `10000`, and random all-profiles `10000`, with direct DAE and `dae-optimizing` reported separately.

## Current blockers

- 2026-07-10 original-result boundary-row consolidation: red-first whitebox `dae original result boundary row shares preservation and prune facts` failed because `dae_original_result_boundary_context(...)`, `dae_original_result_boundary_row_from_context(...)`, and `dae_original_result_boundary_evidence_from_row(...)` were unbound. The implemented `DaeOriginalResultBoundaryContext` indexes the shared original parameter/call/self/escape/dead-suffix facts once through `DaeOriginalResultPreservationRow`, derives `DaeOriginalResultRemovalPruneRow` from that row plus prune-only facts, and produces preservation/prune evidence together. The main core reuses the projection across unobserved cleanup, zero-call cleanup, zero-parameter dropped-result removal, current-parameter result repair, and the parameterized self-side-chain guard. This is behavior-preserving; no legality predicate or scheduler changed. Focused whitebox passed `166/166`, public DAE tests passed `302/302`, `moon test src/passes` passed `4436/4436`, `moon info` passed with 9 pre-existing warnings, full `moon test` passed `7875/7875`, and native build passed. Fresh lanes are unchanged and intentionally raw-red: `.tmp/pass-fuzz-dae-direct-closeout-10000-after-boundary-row-20260710` is `5000` normalized / `5000` mismatches with `dae-unused-params=1875`, `dae-constant-args=1875`, `dae-convergence=625`, and `dae-tail-call-boundary=625`; `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-boundary-row-20260710` is `9207` / `793` with `coverage-forced-portable=259`, `dae-unused-params=214`, `dae-constant-args=197`, `dae-tail-call-boundary=63`, and `dae-convergence=60`; normalized `.tmp/pass-fuzz-dae-optimizing-random-all-10000-after-boundary-row-20260710` is `9538` / `462` with `coverage-forced-portable=259`, `dae-effectful-args=136`, and `dae-localization=67`. All compared `10000/10000`, had zero validation/generator/property/command failures, and used Binaryen cache `10000/0`; only the optimizing lane used the documented cleanup normalizers.
- 2026-07-10 `coverage-forced-portable` observability fix and full residual classification: red-first whitebox `dae instruction observability evidence blocks effects branches and traps` failed because `dae_instruction_observability_evidence(...)` was unbound. The public `dead-argument-elimination preserves trapping prefixes before unreachable cleanup` fixture and a pre-change native replay then showed the old prefix scanner incorrectly collapsed a reachable `i32.div_s` by zero to bare `unreachable`. `DaeInstructionObservabilityEvidence` now serves the existing effect-only and effect-or-branch consumers plus unreachable-prefix application; application fails closed for observable effects, escaping control, unknown traps, zero divisors, signed overflow, and unsafe float-to-int conversion, while retaining explicit constant-safe numeric proofs and self-contained branch blocks. Focused tests passed after red, full `dae_optimizing_test.mbt` passed `302/302`, whitebox passed `165/165`, `moon test src/passes` passed `4435/4435`, `moon info` passed with 9 pre-existing warnings, full `moon test` passed `7874/7874`, and native build passed. `.tmp/dae-coverage-forced-portable-classification-20260710.json` exhaustively covers all `259` refreshed direct random-all coverage residuals and `936` defined-function prefixes: one identical preterminal numeric sequence, zero preterminal observable effects or unproved traps, constant-safe div/rem and float-to-int inputs, and only self-contained `br_table 0 0` before `unreachable`. Binaryen retains the pure prefix while Starshine removes it. Aggregate deltas are `-1288603` raw / `-1291704` canonical / `-8864535` WAT with zero positive cases. Runtime replay `.tmp/pass-fuzz-dae-random-all-residual-replay-runtime-after-observability-20260710` compared all `793` saved residuals with zero failures; all `259` coverage cases were runtime-blocked by unsupported imports, so that replay is not treated as semantic proof. The import-free reduced probe measured 21-round median Node compile time `1205.46 ns` Binaryen versus `1007.45 ns` Starshine and caught-trap call time `5441.45 ns` versus `2872.66 ns`; representative Starshine pass-local time was `1928us` median over 101 traces, with no comparable Binaryen timer and therefore no optimizer-speed claim. Agent classification: all `259` coverage residuals are measured Starshine-win pure-prefix elimination. Fresh current-binary lanes remain intentionally raw-red and unchanged: dedicated direct `.tmp/pass-fuzz-dae-direct-closeout-10000-after-observability-20260710` is `5000` normalized / `5000` mismatches; direct random-all `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-observability-20260710` is `9207` / `793`; normalized optimizing random-all `.tmp/pass-fuzz-dae-optimizing-random-all-10000-after-observability-20260710` is `9538` / `462`. All compared `10000/10000`, had zero validation/generator/property/command failures, used all-hit Binaryen caches, and set `reduceMismatches=false`; only the optimizing lane used `drop-consts` and `unreachable-control-debris`. Every current direct random-all residual family is now strongly classified, but direct DAE remains explicitly raw-red.
- 2026-07-09 `dae-localization` typed scratch-local parity fix: red-first whitebox `dae callsite localization application replaces hard removed operands with typed scratch writes` first failed because `dae_rewrite_callsite_with_localization_at(...)` was unbound, then passed after the shared application consumed recovered callsite value slices, rejected escaping branches, emitted hard removed operands into defaultable typed scratch locals, and recorded localized parameter slots. Public plain-DAE tests cover both direct `call` and `return_call` callers, preserve the effectful producer before surviving arguments, retain the corresponding old callee slot as a body local, validate the rewritten module, and do not invoke optimizing-only nested cleanup. The first 1000-case no-normalizer lane `.tmp/pass-fuzz-dae-localization-1000-scratch-local-20260709` stayed raw-red solely because Starshine initially omitted Binaryen's unused replacement callee local; after threading localized-parameter evidence through `dae_build_local_map(...)`, `.tmp/pass-fuzz-dae-localization-1000-scratch-local-callee-projection-20260709` normalized `1000/1000`, and the scaled `.tmp/pass-fuzz-dae-localization-10000-scratch-local-callee-projection-20260709` normalized `10000/10000`. The sibling `dae-effectful-args` strategy uses the same application and is also green in `.tmp/pass-fuzz-dae-effectful-args-1000-scratch-local-callee-projection-20260709` (`1000/1000`) and `.tmp/pass-fuzz-dae-effectful-args-10000-scratch-local-callee-projection-20260709` (`10000/10000`). All four green lanes had zero cleanup-normalized matches, mismatches, validation/generator/property/command failures, used no normalizers, set `reduceMismatches=false`, and used all-hit Binaryen caches. Refreshed aggregate evidence remains explicitly raw-red: `.tmp/pass-fuzz-dae-direct-closeout-10000-after-scratch-localization-20260709` normalized `5000/10000` with `5000` mismatches (`dae-unused-params=1875`, `dae-constant-args=1875`, `dae-convergence=625`, `dae-tail-call-boundary=625`), while `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-scratch-localization-20260709` normalized `9207/10000` with `793` mismatches (`coverage-forced-portable=259`, `dae-unused-params=214`, `dae-constant-args=197`, `dae-tail-call-boundary=63`, `dae-convergence=60`). Both had zero failures, no normalizers, Binaryen cache `10000/0`, and `reduceMismatches=false`. The `dae-optimizing` random-all rerun with the documented normalizers remained unchanged at `9538/10000` normalized, `0` cleanup-normalized, `462` mismatches, zero failures, and Binaryen cache `10000/0`. This fixes the deterministic localization/effectful-argument leaves and removes those families from refreshed direct residuals; direct DAE remains raw-red overall. After the shared removed-parameter local projection landed, current-binary reruns confirmed unchanged counts: `.tmp/pass-fuzz-dae-direct-closeout-10000-after-local-projection-classification-20260709` remains `5000` normalized / `5000` mismatches with the same four selected families, `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-local-projection-classification-20260709` remains `9207` normalized / `793` mismatches with the same five selected families, and normalized `.tmp/pass-fuzz-dae-optimizing-random-all-10000-after-local-projection-classification-20260709` remains `9538` normalized / `462` mismatches. All three compared `10000/10000`, had zero validation/generator/property/command failures, used Binaryen cache `10000/0`, and set `reduceMismatches=false`; only the optimizing lane used the documented cleanup normalizers.
- 2026-07-09 `dae-convergence` residual classification: `.tmp/dae-convergence-residual-classification-20260709.json` exhaustively maps all `625` refreshed dedicated-profile convergence residuals and all `60` refreshed random-all convergence residuals. Each lane has one unique input/Binaryen/Starshine artifact hash and one diff shape: both tools converge the leaf signature first and the wrapper signature on a later core iteration, update the same calls and shared one-parameter type, and differ only because Binaryen retains one unread `i32` replacement local in each rewritten function while Starshine elides both. Removing exactly those two Binaryen local declarations makes every canonical WAT pair equal (`625/625` and `60/60`). This follows the source contract rather than validation alone: Binaryen `ParamUtils::removeParameters(...)` generically allocates a fresh local for every removed parameter before rewriting gets, while Starshine's `DaeRemovedParamLocalProjectionEvidence` preserves a former parameter slot only for surviving writes or proven caller-side localization. Size evidence is uniformly Starshine-favorable with no positive cases: dedicated `-2500` raw / `-2500` canonical / `-21250` WAT bytes and random-all `-240` raw / `-240` canonical / `-2040` WAT bytes. Full Node runtime replays `.tmp/pass-fuzz-dae-convergence-direct-runtime-replay-20260709` and `.tmp/pass-fuzz-dae-convergence-random-all-runtime-replay-20260709` checked all `685` cases with equal results, zero unsupported runtimes, zero semantic mismatches, and zero validation/generator/property/command failures while intentionally remaining raw mismatches; both used no normalizers, had `0` normalized / `0` cleanup-normalized matches, and used all-hit Binaryen caches (`625/0` and `60/0`). An alternating 21-round output microbenchmark over 3,000,000 exported `main` calls per artifact measured medians `4.713 ns/call` Starshine versus `4.735 ns/call` Binaryen (`-0.46%`, treated as runtime parity rather than a material speed claim); 101 traced Starshine runs measured median pass-local time `123us`, but no comparable Binaryen pass-local timer was available, so this slice makes no optimizer-speed claim. A fresh current-binary singleton `.tmp/pass-fuzz-dae-convergence-10000-local-projection-classified-20260709` compared `10000/10000` with `0` normalized / `0` cleanup-normalized matches, `10000` intentional raw mismatches, zero failures, no normalizers, Binaryen cache `10000/0`, and `reduceMismatches=false`; `.tmp/dae-convergence-singleton-10000-classification-20260709.json` confirms one unique shape, exact WAT equality after deleting the two Binaryen locals in all `10000` cases, aggregate `-40000` raw / `-40000` canonical / `-340000` WAT bytes, and no positive cases. Agent classification: the deterministic `dae-convergence` leaf is a measured Starshine-win local-declaration cleanup, not a module-boundary convergence gap. Direct DAE remains raw-red because the classified mismatch is intentionally preserved; the later 2026-07-10 observability slice separately classifies the full `coverage-forced-portable` residual.
- 2026-07-07 `dae-tail-call-boundary` residual classification: local analysis `.tmp/dae-tail-call-boundary-residual-classification-20260707.json` maps all `63` saved `dae-tail-call-boundary` residuals in both the direct random-all lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` and the post-tail-trim direct DAE+RUME replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-after-unreachable-tail-trim-20260707`. Every case has the same diff in its lane: Binaryen removes the unused first helper parameter and corresponding pure `i32.const` `return_call` operands but keeps one fresh unused helper local for the old parameter, while Starshine removes the same parameter/operands and omits the dead replacement local. The helper is reached only through owned direct `return_call` sites, reads only the surviving parameter, and the extra local is never read, written, exported, or addressable. The family is measured Starshine-win cleanup for this saved residual set (`-126` raw / `-126` canonical / `-1071` WAT bytes in each lane, with `0` raw/canonical/WAT-positive cases), and representative runtime evidence from `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706/case-104/result.json` shows equal Node results. This classifies only `dae-tail-call-boundary`; direct DAE remains raw-red overall while other residual families still need red-first fixes or family-by-family measured Starshine-win proof.
- 2026-07-07 `dae-unused-params` residual classification: local analysis `.tmp/dae-unused-params-residual-classification-20260707.json` maps all `214` saved `dae-unused-params` residuals in both the direct random-all lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` and the post-tail-trim direct DAE+RUME replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-after-unreachable-tail-trim-20260707`. Every case has the same diff in its lane: Binaryen removes unused helper parameters and call operands but keeps two fresh unused helper locals for the old parameters, while Starshine removes the same parameters/operands and omits the dead replacement locals. The helper body reads only the surviving parameter, the removed call operands are pure `i32.const` values, and the extra locals are never read, written, exported, or addressable. The family is measured Starshine-win cleanup for this saved residual set (`-428` raw / `-428` canonical / `-7276` WAT bytes in each lane, with `0` raw/canonical/WAT-positive cases), and representative runtime evidence from `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706/case-20/result.json` shows equal Node results. This classifies only `dae-unused-params`; direct DAE remains raw-red overall while other residual families still need red-first fixes or family-by-family measured Starshine-win proof.
- 2026-07-07 `dae-constant-args` residual classification: local analysis `.tmp/dae-constant-args-residual-classification-20260707.json` maps all `197` saved `dae-constant-args` residuals in both the direct random-all lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` and the post-tail-trim direct DAE+RUME replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-after-unreachable-tail-trim-20260707`. Every case has the same diff: Binaryen materializes the uniform removed-parameter replacement `i32.const 77` through a fresh helper local, while Starshine substitutes the same pure constant directly at the former `local.get` site. The family is measured Starshine-win cleanup for this saved residual set (`-1182` raw / `-1182` canonical / `-10835` WAT bytes in each lane, with `0` raw/canonical/WAT-positive cases), and representative runtime evidence from `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706/case-7/result.json` shows equal Node results. This classifies only `dae-constant-args`; direct DAE remains raw-red overall while other residual families still need red-first fixes or family-by-family measured Starshine-win proof.
- 2026-07-07 coverage unreachable-tail cleanup fix: a raw-positive coverage sample was manually reduced to `.tmp/dae-coverage-raw-positive-minisample-20260707.wat`, where Binaryen `wasm-opt --dae` trims effectful tail instructions after a block-wrapped terminal `unreachable` while Starshine kept the dead `call` / `return_call` suffix. Red-first `dead-argument-elimination trims unreachable tail debris from coverage-shaped functions` failed while Starshine retained the suffix, then passed after DAE recursively canonicalized block-wrapped unreachable roots to `unreachable` and truncated same-region tails after them. Validation: focused test passed after failing red (`1/1`); `moon fmt`; `moon test src/passes` (`4332/4332`); full `moon test` (`7771/7771`); `moon info` passed with pre-existing warnings; native `src/cmd` build passed with pre-existing warnings. Manual mini-replay now matches Binaryen raw size (`67` bytes). The saved direct DAE+RUME residual replay after the fix, `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-after-unreachable-tail-trim-20260707`, still compares `996/996` raw-red with `0` normalized, `0` cleanup-normalized, `996` mismatches, zero validation/generator/property/command failures, Binaryen cache `996/0`, and `reduceMismatches=false`; however the previous `coverage-forced-portable` raw-positive risk is removed in the selected-profile size audit `.tmp/dae-rume-residual-profile-size-summary-after-unreachable-tail-trim-20260707.json`: `coverage-forced-portable=259` is now `-1173747` raw / `-1175646` canonical / `-8042558` WAT with `0` raw/canonical/WAT-positive cases, and all other residual profiles remain raw/canonical/WAT non-positive (`dae-unused-params=214`, `dae-constant-args=197`, `dae-effectful-args=136`, `dae-localization=67`, `dae-tail-call-boundary=63`, `dae-convergence=60`). Direct DAE remains raw-red overall; this is a parity fix and measured cleanup-risk reduction, not direct closeout.
- 2026-07-06 ten-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-ten-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-ten-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two ten-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*ten-parameter call_ref select returned-value*` and `*ten-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE replaced the fixed eleven-instruction returned-value typed-select operand scan with stack-effect value-slice recovery and removed the nine-parameter cap for pure stack-local direct-target `call_ref` and `call_indirect` result candidates. The focused probes prove ten-parameter direct-target call operands and the implementation should admit longer pure stack-local single-output operand slices by construction, but eleven-plus parameters are not separately probe-covered and effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused ten-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`11/11`); focused `*call_indirect select returned-value*` (`10/10`); focused `*select*` (`82/82`); full `dae_optimizing_test.mbt` (`298/298`); `moon fmt`; `moon test src/passes` (`4331/4331`); `moon info` passed with pre-existing warnings; full `moon test` (`7770/7770`); native `src/cmd` build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run for this slice; direct DAE remains raw-red overall.
- 2026-07-06 nine-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-nine-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-nine-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two nine-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*nine-parameter call_ref select returned-value*` and `*nine-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most nine stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned eleven-instruction value slices. This fixed-cap note is superseded by the later ten-parameter stack-slice recognizer; effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused nine-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`10/10`); focused `*call_indirect select returned-value*` (`9/9`); `moon fmt`; focused `*select*` (`80/80`); full `dae_optimizing_test.mbt` (`296/296`); `moon test src/passes` (`4329/4329`); `moon info` passed with pre-existing warnings; full `moon test` (`7768/7768`); native `src/cmd` build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run for this slice; direct DAE remains raw-red overall.
- 2026-07-06 eight-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-eight-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-eight-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two eight-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*eight-parameter call_ref select returned-value*` and `*eight-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most eight stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned ten-instruction value slices. Nine-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused eight-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`9/9`); focused `*call_indirect select returned-value*` (`8/8`); `moon fmt`; focused `*select*` (`78/78`); full `dae_optimizing_test.mbt` (`294/294`); `moon test src/passes` (`4327/4327`); `moon info` passed with pre-existing warnings; full `moon test` (`7766/7766`); native `src/cmd` build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 seven-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-seven-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-seven-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two seven-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*seven-parameter call_ref select returned-value*` and `*seven-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most seven stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned nine-instruction value slices. Eight-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused seven-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`8/8`); focused `*call_indirect select returned-value*` (`7/7`); `moon fmt`; focused `*select*` (`76/76`); full `dae_optimizing_test.mbt` (`292/292`); `moon test src/passes` (`4325/4325`); `moon info` passed with pre-existing warnings; full `moon test` (`7764/7764`); native `src/cmd` build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 six-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-six-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-six-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two six-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*six-parameter call_ref select returned-value*` and `*six-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most six stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned eight-instruction value slices. Seven-parameter calls are covered by the later bounded slice; eight-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused six-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`7/7`); focused `*call_indirect select returned-value*` (`6/6`); `moon fmt`; focused `*select*` (`74/74`); full `dae_optimizing_test.mbt` (`290/290`); `moon test src/passes` (`4323/4323`); `moon info` passed with pre-existing warnings; full `moon test` (`7762/7762`); native `src/cmd` build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 five-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-five-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-five-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two five-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*five-parameter call_ref select returned-value*` and `*five-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most five stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned seven-instruction value slices. Six-parameter calls are covered by the later bounded slice; seven-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation so far: Binaryen probes validated/emitted narrowed WAT; both focused five-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`6/6`); focused `*call_indirect select returned-value*` (`5/5`). No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 four-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-four-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-four-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two four-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*four-parameter call_ref select returned-value*` and `*four-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most four stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned six-instruction value slices. Five-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation so far: Binaryen probes validated/emitted narrowed WAT; both focused four-parameter tests failed before implementation and passed after (`1/1` each); focused `*call_ref select returned-value*` (`5/5`); focused `*call_indirect select returned-value*` (`4/4`). No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 three-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-three-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-three-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two three-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*three-parameter call_ref select returned-value*` and `*three-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most three stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned five-instruction value slices. Four-parameter calls are covered by a later slice; five-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused three-parameter tests failed before implementation and passed after (`1/1` each); `moon fmt`; focused `*call_ref select returned-value*` (`4/4`); focused `*call_indirect select returned-value*` (`3/3`); focused `*select*` (`68/68`); full `dae_optimizing_test.mbt` (`284/284`); `moon test src/passes` (`4317/4317`); `moon info` with pre-existing warnings; full `moon test` (`7756/7756`); native `src/cmd` build with pre-existing warnings; `git diff --check` clean. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 two-parameter `call_ref`/`call_indirect` select returned-value refinement: Binaryen v130 probes `.tmp/dae-returned-select-call-ref-two-param-probe.wat` and `.tmp/dae-returned-select-call-indirect-two-param-probe.wat` validated and `wasm-opt --dae` narrowed a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two two-argument direct-target `ref.func; call_ref` or `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*two-parameter call_ref select returned-value*` and `*two-parameter call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE widened the bounded returned-value typed-select recognizer to at most two stack-local, non-branching, side-effect-free arguments (plus a direct `ref.func` target or stack-local table index) and scanned four-instruction value slices. Three-plus-parameter calls, effectful arguments/table indices, table/indirect `call_ref` targets, stores/local mutation, and broader target/value localization remain open. Validation: Binaryen probes validated/emitted narrowed WAT; both focused two-parameter tests failed before implementation and passed after (`1/1` each); `moon fmt`; focused `*call_ref select returned-value*` (`4/4`); focused `*call_indirect select returned-value*` (`3/3`); focused `*select*` (`68/68`); full `dae_optimizing_test.mbt` (`284/284`); `moon test src/passes` (`4317/4317`); `moon info` with pre-existing warnings; full `moon test` (`7756/7756`); native `src/cmd` build with pre-existing warnings; `git diff --check` clean. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 `call_indirect` select returned-value refinement: Binaryen v130 probe `.tmp/dae-returned-select-call-indirect-probe.wat` showed `wasm-opt --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two one-argument `call_indirect` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*call_indirect select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE recognizes at-most-one-argument, stack-local, non-branching, side-effect-free `call_indirect` result slices with a stack-local table index as returned-value GC candidates. Multi-parameter `call_indirect`, effectful arguments/table indices, and arbitrary stack localization remain open. Validation: Binaryen probe validated/emitted narrowed WAT; focused `*call_indirect select returned-value*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*select*` (`64/64`); full `dae_optimizing_test.mbt` (`280/280`); `moon test src/passes` (`4313/4313`); `moon info` with pre-existing warnings; full `moon test` (`7752/7752`); native `src/cmd` build with pre-existing warnings; `git diff --check` clean. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 single-parameter `call_ref` select returned-value refinement: Binaryen v130 probe `.tmp/dae-returned-select-call-ref-param-probe.wat` showed `wasm-opt --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two one-argument `ref.func; call_ref` slices returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*parameterized call_ref select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE recognizes at-most-one-argument, stack-local, non-branching, side-effect-free `call_ref` result slices with a direct `ref.func` target as returned-value GC candidates. Multi-parameter `call_ref`, `call_indirect`, table/indirect targets, effectful arguments, and arbitrary target/value reconstruction remain open. Validation so far: Binaryen probe emitted narrowed WAT; focused `*parameterized call_ref select returned-value*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*call_ref select returned-value*` (`2/2`); focused `*select*` (`63/63`). No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 zero-arg `call_ref` select returned-value refinement: Binaryen v130 probe `.tmp/dae-returned-select-call-ref-operands-probe.wat` showed `wasm-opt --all-features --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two zero-arg `call_ref` operands returning `$leaf1`/`$leaf2` feed `select (result (ref null $base))`. Red-first focused `*call_ref select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE recognizes bounded `ref.func; call_ref` result slices with zero function params as returned-value GC candidates and rewrites the select result type to the tightened LUB. The recognizer deliberately does not cover parameterized `call_ref`, `call_indirect`, `call_ref` target/value slices with extra stack operands, or arbitrary effect/localization reconstruction. Validation for this slice: prior Binaryen probe validated/emitted narrowed WAT; focused `*call_ref select returned-value*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*select*` passed (`62/62`); full `dae_optimizing_test.mbt` passed (`278/278`); `moon test src/passes` passed (`4311/4311`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7750/7750`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 typed-call select returned-value refinement: Binaryen v130 probe `.tmp/dae-returned-select-call-operands-probe.wat` showed `wasm-opt --all-features --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two direct calls returning `$leaf1`/`$leaf2` values feed `select (result (ref null $base))`. Red-first focused `*typed-call select returned-value*` failed while Starshine kept `(ref null $base)`, then passed after DAE accepts direct-call result types as returned-value GC candidates. Validation for this slice: Binaryen probe validated/emitted narrowed WAT; focused `*typed-call select returned-value*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*select*` passed (`61/61`); full `dae_optimizing_test.mbt` passed (`277/277`); `moon test src/passes` passed (`4310/4310`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7749/7749`); native CLI build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 typed-call select-operand argument refinement: Binaryen v130 probe `.tmp/dae-argument-select-call-operands-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad helper parameter from `(ref null $base)` to non-null `(ref $mid)` when two distinct direct calls returning `$leaf1`/`$leaf2` values feed `select (result (ref null $base))`. Red-first focused `*typed-call select*` failed while Starshine kept `(ref null $base)`, then passed after DAE accepts direct-call result types as GC actual candidates for typed-select operands and rewrites the select result type to the tightened LUB. Validation for this slice: Binaryen probe validated/emitted narrowed WAT; focused `*typed-call select*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*parameters*` passed (`18/18`); focused `*select*` passed (`60/60`); full `dae_optimizing_test.mbt` passed (`276/276`); `moon test src/passes` passed (`4309/4309`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7748/7748`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 branchful `if`-condition typed-select argument-refinement coverage: Binaryen v130 probe `.tmp/dae-argument-select-branchful-condition-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad helper parameter from `(ref null $base)` to non-null `(ref $mid)` when two distinct leaf allocations feed `select (result (ref null $base))` and the select condition is a branchful but non-escaping `if (result i32)`. Focused `*branchful typed-select*` passed through the existing stack-modeled `if` condition recognizer from the returned-value slice; no implementation changed and no compare-pass lane was run. Validation for this coverage slice: Binaryen probe validated/emitted narrowed WAT; `moon fmt`; focused `*branchful typed-select*` passed (`1/1`); focused `*parameters*` passed (`17/17`); focused `*select*` passed (`59/59`); full `dae_optimizing_test.mbt` passed (`275/275`); `moon test src/passes` passed (`4308/4308`); `git diff --check` clean. Direct DAE remains raw-red overall.
- 2026-07-06 branchful `if`-condition typed-select returned-value refinement: Binaryen v130 probe `.tmp/dae-result-select-branchful-condition-probe.wat` showed `wasm-opt --all-features --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two distinct leaf allocations feed `select (result (ref null $base))` and the select condition is a branchful but non-escaping `if (result i32)`. Red-first focused `*branchful select-condition*` failed while Starshine kept `(ref null $base)`, then passed after DAE allowed stack-modeled `if` instructions in select-condition slices while still rejecting escaping branch instructions, loops, try_table, and local set/tee there. Validation for this slice: Binaryen probe validated/emitted narrowed WAT; focused `*branchful select-condition*` failed before implementation and passed after (`1/1`); `moon fmt`; focused `*select*` passed (`58/58`); full `dae_optimizing_test.mbt` passed (`274/274`); `moon test src/passes` passed (`4307/4307`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7746/7746`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 trapping-load parametric typeidx block/if/loop returned-value refinement: Binaryen v130 probes `.tmp/dae-result-param-typeidx-load-block-probe.wat` and `.tmp/dae-result-param-typeidx-load-if-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows the first lane of a broad `(result (ref null $base) i32)` helper to exact non-null leaf when terminal parametric typeidx `block`/`if`/`loop` controls consume an `i32` through a potentially trapping numeric-load prefix plus `drop` and then produce the GC lane followed by a scalar lane. Red-first focused `*trapping-load parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE allowed numeric loads in the bounded stack-safe terminal-control prefix recognizer while still requiring the prefix to consume the control params and leave zero stack values before flat result operands. The recognition is intentionally bounded to stack-modeled loads whose effects/traps remain in place; it does not cover stores, local mutation, branchful control, indirect calls, or arbitrary stack localization. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; focused `*trapping-load parametric typeidx*` failed before implementation and passed after (`3/3`); `moon fmt`; focused `*typeidx*` passed (`25/25`); full `dae_optimizing_test.mbt` passed (`273/273`); `moon test src/passes` passed (`4306/4306`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7745/7745`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 effectful parametric typeidx block/if/loop returned-value refinement: Binaryen v130 probes `.tmp/dae-result-param-typeidx-effectful-block-probe.wat`, `.tmp/dae-result-param-typeidx-effectful-if-probe.wat`, and `.tmp/dae-result-param-typeidx-effectful-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows the first lane of a broad `(result (ref null $base) i32)` helper to exact non-null leaf when terminal parametric typeidx `block`/`if`/`loop` controls consume an `i32` through an ordinary direct-call prefix and then produce the GC lane followed by a scalar lane. Red-first focused `*effectful parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE allowed direct calls in the bounded stack-safe terminal-control prefix recognizer while still requiring the prefix to consume the control params and leave zero stack values before flat result operands. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; focused `*effectful parametric typeidx*` failed before implementation and passed after (`3/3`); `moon fmt`; focused `*typeidx*` passed (`22/22`); full `dae_optimizing_test.mbt` passed (`270/270`); `moon test src/passes` passed (`4303/4303`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7742/7742`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 non-drop parametric typeidx block/if/loop returned-value refinement: Binaryen v130 probes `.tmp/dae-result-param-typeidx-nondrop-block-probe.wat`, `.tmp/dae-result-param-typeidx-nondrop-if-probe.wat`, and `.tmp/dae-result-param-typeidx-nondrop-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows the first lane of a broad `(result (ref null $base) i32)` helper to exact non-null leaf when terminal parametric typeidx `block`/`if`/`loop` controls consume an `i32` with a stack-safe nontrapping pure `i32.eqz; drop` prefix and produce the GC lane followed by a scalar lane. Red-first focused `*non-drop parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE generalized its bounded terminal-control recognizer from direct `drop` prefixes to stack-safe nontrapping pure prefixes that consume the control params before flat result operands. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; focused `*non-drop parametric typeidx*` failed before implementation and passed after (`3/3`); `moon fmt`; focused `*typeidx*` passed (`19/19`); `moon test src/passes` passed (`4300/4300`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7739/7739`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 non-tail-lane parametric typeidx block/if/loop returned-value refinement: Binaryen v130 probes `.tmp/dae-result-param-typeidx-nontail-block-probe.wat`, `.tmp/dae-result-param-typeidx-nontail-if-probe.wat`, and `.tmp/dae-result-param-typeidx-nontail-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows the first lane of a broad `(result (ref null $base) i32)` helper to exact non-null leaf when terminal parametric typeidx `block`/`if`/`loop` controls consume an `i32`, drop it in the body/arms, and produce the GC lane followed by a scalar lane. Red-first focused `*non-tail-lane parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE added a bounded drop-only parametric typeidx terminal-control recognizer, computes per-lane GC result refinements from the control body/arms, and rewrites the control type to the same params plus narrowed results. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; focused `*non-tail-lane parametric typeidx*` failed before implementation and passed after (`3/3`); `moon fmt`; focused `*typeidx*` passed (`16/16`); `moon test src/passes` passed (`4297/4297`); `moon info` passed with pre-existing warnings; full `moon test` passed (`7736/7736`); native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- 2026-07-06 call-condition typed-select returned-value refinement: Binaryen v130 probe `.tmp/dae-result-select-call-arg-condition-probe.wat` showed `wasm-opt --all-features --dae` narrows a broad helper result from `(ref null $base)` to non-null `(ref $mid)` when two distinct leaf allocations feed `select (result (ref null $base))` and the select condition is a branch-free direct call with a scalar operand. Red-first focused `*call-condition select GC results*` failed while Starshine kept `(ref null $base)`, then passed after DAE threaded function type metadata through returned-value candidate recognition and used the branch-free stack-effect checker for result-select condition slices. A follow-up Binaryen probe `.tmp/dae-result-select-call-postop-condition-probe.wat` showed the same narrowing when the branch-free direct-call condition result is consumed by a scalar post-op (`i32.const; call $cond; i32.eqz`), and focused `*call-postop select GC results*` covers that already-supported stack-modeled slice. A later probe `.tmp/dae-result-select-memory-condition-probe.wat` showed Binaryen also narrows through a branch-free but potentially trapping memory-load condition (`i32.const 0; i32.load; i32.eqz`), and focused `*memory-condition select GC results*` now covers that already-supported stack-modeled slice without broadening to branchful/control-local reconstruction. A subsequent non-tail-lane multi-result probe `.tmp/dae-result-select-non-tail-multivalue-probe.wat` showed Binaryen narrows the first lane of `(result (ref null $base) i32)` when the typed select is followed by a scalar result; focused `*non-tail-lane multi-result select*` passed through the existing bounded flat multi-result select-operand recognizer. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; red-first focused `*call-condition select GC results*` failed before implementation and passed after (`1/1`); focused `*call-postop select GC results*` passed (`1/1`); focused `*memory-condition select GC results*` passed (`1/1`); focused `*non-tail-lane multi-result select*` passed (`1/1`); focused `*select GC results*` passed (`4/4` before the follow-up coverage tests); focused `*select*` passed (`57/57` after the non-tail-lane multi-result coverage test); `moon fmt`; full `dae_optimizing_test.mbt` (`258/258` before the follow-up coverage tests); `moon test src/passes` (`4291/4291`); `moon info` passed with pre-existing warnings; full `moon test` (`7730/7730`); native CLI build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run for these follow-up coverage slices; direct DAE remains raw-red overall.
- 2026-07-06 effectful typed-select condition argument refinement: Binaryen v130 probe `.tmp/dae-arg-select-call-condition-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad `(ref null $base)` helper parameter to exact non-null `(ref (exact $leaf))` when an owned direct caller passes two leaf allocations through `select (result (ref $leaf))` and the select condition is an ordinary direct call returning `i32`. Red-first focused `*effectful typed-select conditions*` failed while Starshine kept `(ref null $base)`, then passed after DAE relaxed select-condition recognition from effect-free to branch-free and threaded function type metadata into condition stack-effect checks. Validation for this slice: Binaryen probe validated/emitted narrowed WAT; red-first focused `*effectful typed-select conditions*` failed before implementation and passed after (`1/1`); focused `*typed-select*` passed (`2/2`); focused `*parameters*` passed (`16/16`); `moon fmt`; full `dae_optimizing_test.mbt` (`257/257`); `moon test src/passes` (`4290/4290`); `moon info` passed with pre-existing warnings; full `moon test` (`7729/7729`); native CLI build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run for this slice; direct DAE remains raw-red overall.
- 2026-07-06 multi-result typeidx block/if/loop argument refinement: Binaryen v130 probes `.tmp/dae-arg-multi-typeidx-block-probe.wat`, `.tmp/dae-arg-multi-typeidx-if-probe.wat`, and `.tmp/dae-arg-multi-typeidx-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad `(ref null $base)` helper parameter to non-null `(ref $leaf)` when owned direct callers pass a multi-result type-indexed `block`/`if`/`loop` actual whose last result lane is the consumed reference and an earlier scalar lane remains on the caller stack. Binaryen localizes the multi-result control through tuple scratch traffic; Starshine keeps the valid multi-result control shape and tightens the helper signature. Red-first focused `*multi-result typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE accepted the last non-null concrete GC result lane of final `block`/`if`/`loop` control actuals. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; red-first focused `*multi-result typeidx*` failed before implementation and passed after (`3/3`); focused `*parameters*` `15/15`; `moon fmt`; full `dae_optimizing_test.mbt` `256/256`; `moon test src/passes` `4289/4289`; `moon info` passed with pre-existing warnings; full `moon test` `7728/7728`; native CLI build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run yet for this slice; direct DAE remains raw-red overall.
- 2026-07-06 parametric typeidx block/if/loop argument refinement: Binaryen v130 probes `.tmp/dae-arg-param-typeidx-block-probe.wat`, `.tmp/dae-arg-param-typeidx-if-probe.wat`, and `.tmp/dae-arg-param-typeidx-loop-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad `(ref null $base)` helper parameter to non-null `(ref $leaf)` when owned direct callers pass single-result type-indexed `block`/`if`/`loop` actuals whose `$carrier` type consumes an `i32` and returns `(ref $leaf)`. Binaryen localizes the consumed control input into a scratch local before rewriting the controls to zero-input result carriers; Starshine keeps the valid parametric control shape but tightens the helper signature. Red-first focused `*parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE accepted single-result typeidx control candidates with params and recognized final `block`/`loop` controls at the end of multi-instruction argument slices. Validation for this slice: Binaryen probes validated/emitted narrowed WAT; red-first focused `*parametric typeidx*` failed before implementation and passed after (`3/3`); focused `*parameters*` `12/12`; `moon fmt`; full `dae_optimizing_test.mbt` `253/253`; `moon test src/passes` `4286/4286`; `moon info` passed with pre-existing warnings; full `moon test` `7725/7725`; native CLI build passed with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run yet for this slice; direct DAE remains raw-red overall.
- 2026-07-06 nullable typed-select common-LUB argument refinement: Binaryen v130 probe `.tmp/dae-arg-typed-select-null-field-sibling-lub-probe.wat` showed `wasm-opt --all-features --dae` narrows a live broad `(ref null $base)` helper parameter to nullable `(ref null $mid)` when two owned direct callers pass `ref.null` plus distinct leaf allocations through typed selects whose leaf types share non-final `$mid`. Red-first focused `*nullable typed-select actuals*` failed while Starshine kept `(ref null $base)`, then passed after `dae_lub_gc_candidate_ref_type(...)` preserved nullability when combining nullable GC actual candidates. Validation for this slice: `moon fmt`; focused `*nullable typed-select actuals*` `1/1`; focused `*parameters*` `9/9`; full `dae_optimizing_test.mbt` `250/250`; `moon test src/passes` `4283/4283`; `moon info` passed with pre-existing warnings; full `moon test` `7722/7722`; native CLI build passed with pre-existing warnings. No compare-pass lane was run; direct DAE remains raw-red overall.
- Focused Moon validation is now green for the DAE audit surface touched so far: red-first `moon test --package jtenner/starshine/validate --file gen_valid_tests.mbt --filter '*unused-returns*'` first failed while the generated helper still had two results, then passed after the profile was changed to a single dropped-result DAE-F trigger; `moon test --package jtenner/starshine/validate --file gen_valid_tests.mbt --filter '*dae*'` passes (`11/11`); `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*exact*'` (`11/11`, failed red before the exact-ref and post-cleanup exact-ref fixes), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*return_call sites*'` (`1/1`, failed red before the implementation), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*dead-argument-elimination*'` (`4/4`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*unused-return cleanup exposes dead params*'` (`1/1`, failed red before the post-cleanup nop-body DAE rerun), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*imported tag type references*'` (`1/1`, failed red before imported tag type liveness was included in DAE type pruning), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*tag*'` (`4/4`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*returned-value LUB*'` (`1/1`, failed red before the terminal-`if` result-LUB fix), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*explicit-return GC results*'` (`1/1`, failed red before the explicit-return result-LUB fix), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*loop-carried explicit-return GC results*'` (`1/1`, failed red before the loop-carried explicit-return result-refinement fix), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*typed-if explicit-return GC results*'` (`1/1`, failed red before the typed-if explicit-return result-refinement fix), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*typed-control explicit-return GC results*'` (`1/1`, failed red before the typed block/loop explicit-return result-refinement fix), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*flat multi-result bodies*'` (`2/2`, the explicit-return case failed red before flat multi-result result refinement), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*droppable multi-result prefix*'` failed before droppable-prefix multi-result refinement and now passes (`1/1`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*multi-result block carriers*'` failed before block-carrier multi-result refinement and now passes (`2/2`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*typeidx block bodies*'` failed before zero-param typeidx block result refinement and now passes (`2/2`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*typeidx if*'` failed before zero-param typeidx terminal-`if` result refinement and now passes (`2/2`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*fallthrough loop*'` failed before fallthrough typed/typeidx loop result refinement and now passes (`2/2`) after the multi-result loop follow-up, red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*block-carrier multi-result if arms*'` failed before branch-free zero-param block-carrier `if` arm flattening and now passes for the simple and nested block-carrier arm fixtures (`4/4` after this slice), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*explicit-return GC results through block-carrier*'` failed before explicit-return zero-param block-carrier `if` arm flattening and now passes (`1/1`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*fallthrough GC results to common LUB*'` failed for simple mixed single-result return/fallthrough `if` paths before the LUB fix and now passes (`2/2`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*mixed multi-result return and fallthrough*'` failed while the reference lane stayed broad before the multi-result LUB fix and now passes (`1/1`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*nested multi-result block carriers*'` failed while the reference lane stayed broad before the nested block-carrier unwrapping fix and now passes (`1/1`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*multi-result block carriers*'` later failed one nested explicit-return block-carrier fixture while the reference lane stayed broad and now passes (`4/4`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*nested block-carrier multi-result if arms*'` failed while the reference lane stayed broad before nested block-carrier `if` arm unwrapping and now passes (`2/2`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*void-block multi-result if arms*'` failed while the reference lane stayed broad before zero-output void-block explicit-return `if` arm unwrapping and now passes (`1/1`), red-first `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*single-result if operands*'` failed first while the tail reference operand stayed broad, later while the leading/non-tail reference operand stayed broad, later while the explicit-return middle-lane reference operand stayed broad, later while two flat-suffix reference lanes fed by separate single-result `if` operands stayed broad, and later while interleaved droppable debris before a single-result `if` operand kept the reference lane broad before bounded single-result `if` operand refinement and now passes (`5/5`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*multi-result if arms*'` passes (`7/7`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*multi-result*'` passes (`22/22`), `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt --filter '*GC results*'` now passes (`38/38`) after the typed-select returned-value slice, focused `*select*` passes (`56/56`) after the memory-condition typed-select returned-value coverage slice, full `dae_optimizing_test.mbt` passed (`244/244`) before the later typeidx argument-operand follow-ups, red-first `moon test src/passes` failed on `dae raw cleanup strips dropped unreachable debris before hard unreachable` before the direct DAE unreachable-debris cleanup fix and later failed on the simple type-reuse whitebox expectation before the signature-reuse fix; it passed (`4277/4277`) after the pure-condition typed-select slice, `moon info` passes with pre-existing warnings, native CLI build passes with pre-existing warnings, earlier `moon test src/validate` (`1687/1687`), full `moon test` passed (`7716/7716`) after the pure-condition typed-select slice. A later red-first typeidx block argument-operand slice failed before implementation while Starshine kept `(ref null $base)`, then passed after DAE accepted zero-param type-indexed block actual candidates. A follow-up Binaryen-positive typeidx `if`/`loop` argument-operand slice failed red before implementation while Starshine kept `(ref null $base)`, then passed after DAE accepts a candidate from the value-producing control instruction at the end of the argument slice; focused `*typeidx block operands*`, `*typeidx if operands*`, and `*typeidx loop operands*` each pass (`1/1`). A later typed-select operand slice probed Binaryen narrowing through `select (result (ref $leaf))`; red-first focused `*typed select operands*` failed while Starshine kept `(ref null $base)`, then passed after DAE computes the exact operand LUB and rewrites the select result type at callsites. A follow-up nullable typed-select operand slice probed Binaryen narrowing through `ref.null $leaf` plus `struct.new_default $leaf` feeding `select (result (ref null $leaf))`; red-first focused `*nullable select LUB*` failed while Starshine kept `(ref null $base)`, then passed after DAE treats null select operands as nullable LUB refinements and rewrites the `ref.null` actual plus select result type. Focused `*parameters*` later passes (`18/18`) after the typed-call select-operand argument slice; full `dae_optimizing_test.mbt` passes (`276/276`), `moon fmt` passes, focused `*typeidx*` passes (`25/25`), focused `*effectful parametric typeidx*` passes (`3/3`) after failing red, focused `*trapping-load parametric typeidx*` passes (`3/3`) after failing red, `moon test src/passes` passes (`4309/4309`), `moon info` passes with pre-existing warnings, full `moon test` passes (`7748/7748`), native CLI build passes with pre-existing warnings, and `git diff --check` is clean after this typed-call select-operand slice. No compare-pass lane was run for these focused slices, and direct DAE remains raw-red overall.
- Latest focused validation after the ten-parameter `call_ref`/`call_indirect` returned-value slice: focused `*ten-parameter call_ref select returned-value*` (`1/1`, failed red before implementation), focused `*ten-parameter call_indirect select returned-value*` (`1/1`, failed red before implementation), focused `*call_ref select returned-value*` (`11/11`), and focused `*call_indirect select returned-value*` (`10/10`). Validation after this slice: Binaryen probes validated/emitted narrowed WAT; `moon fmt`; focused `*select*` (`82/82`); full `dae_optimizing_test.mbt` (`298/298`); `moon test src/passes` (`4331/4331`); `moon info` with pre-existing warnings; full `moon test` (`7770/7770`); native `src/cmd` build with pre-existing warnings; `git diff --check` clean. No compare-pass lane has been run for this focused slice, and direct DAE remains raw-red overall.
- Direct DAE aggregate compare is **not** green. The superseded 2026-07-05 smoke `.tmp/pass-fuzz-dae-direct-smoke-100-after-refine` requested `100`, compared `60`, normalized `4`, cleanup-normalized `0`, mismatched `56`, hit `maxFailures`, had `8` generator failures from invalid final-super GC subtype templates, `0` validation/property/command failures, selected all ten direct leaves, and used Binaryen cache `60/0`. After fixing the GC templates, `.tmp/pass-fuzz-dae-direct-smoke-100-after-genfix` requested/compared `100/100`, normalized `6`, cleanup-normalized `0`, mismatched `94`, had `0` validation/generator/property/command failures, selected all ten direct leaves, and used Binaryen cache `98/2`. After the direct `return_call` repair, `.tmp/pass-fuzz-dae-direct-smoke-100-after-tailcall` requested/compared `100/100`, normalized `6`, cleanup-normalized `0`, mismatched `94`, had `0` validation/generator/property/command failures, selected all ten direct leaves, and used Binaryen cache `100/0`. After the exact-ref refinement fix, `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact` requested/compared `100/100`, normalized `18`, cleanup-normalized `0`, mismatched `82`, had `0` validation/generator/property/command failures, selected all ten direct leaves, and used Binaryen cache `100/0`.
- Current direct-DAE mismatch classification is superseded by the single-result `dae-unused-returns` profile refresh. The prior `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact` run requested/compared `100/100`, normalized `18`, cleanup-normalized `0`, mismatched `82`, had `0` validation/generator/property/command failures, selected all ten leaves, and used Binaryen cache `100/0`; all `82` raw mismatch dirs in that smoke were source/diff reviewed and agent-classified as sampled Starshine-win cleanup rather than active semantic parity gaps. After the single-result DAE-F profile refresh, `.tmp/pass-fuzz-dae-unused-returns-single-smoke-100` requested/compared `100/100`, normalized `100`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `100/0`, proving the focused generated unused-return leaf now matches. The refreshed aggregate `.tmp/pass-fuzz-dae-direct-smoke-100-single-return` requested/compared `100/100`, normalized `32`, cleanup-normalized `0`, mismatched `68`, had `0` validation/generator/property/command failures, selected the same ten leaves, and used Binaryen cache `100/0`; the follow-up `.tmp/pass-fuzz-dae-direct-smoke-1000-single-return` requested/compared `1000/1000`, normalized `312`, cleanup-normalized `0`, mismatched `688`, had zero failures, and used Binaryen cache `1000/0`. The closeout-scale dedicated profile lane `.tmp/pass-fuzz-dae-direct-closeout-10000-single-return` requested/compared `10000/10000`, normalized `3125`, cleanup-normalized `0`, mismatched `6875`, had `0` validation/generator/property/command failures, and used Binaryen cache `10000/0`; selected counts were `dae-constant-args=1875`, `dae-unused-params=1875`, `dae-effectful-args=1250`, `dae-unused-returns=1250`, and `dae-localization`/`dae-convergence`/`dae-arg-type-refinement`/`dae-unseen-call-boundary`/`dae-return-type-refinement`/`dae-tail-call-boundary=625` each. In the refreshed 1000 and 10000 aggregates, `dae-unused-returns`, `dae-unseen-call-boundary`, `dae-arg-type-refinement`, and `dae-return-type-refinement` have no failure dirs; the remaining raw residuals stay in the already-classified Starshine-win cleanup leaves. At 10000 cases their failure counts and size deltas are: `dae-unused-params` (`1875`, old-parameter local omission, `-3750` normalized/raw bytes), `dae-constant-args` (`1875`, direct constant substitution instead of temp materialization, `-11250` normalized/raw bytes), `dae-convergence` (`625`, iterative prune with omitted old-parameter locals, originally `-2500` canonical / `+625` raw before type reuse; post-fix mismatch replay is `-2500` raw/canonical), `dae-tail-call-boundary` (`625`, repaired `return_call` signatures/operands with omitted old local, `-1250` normalized/raw), `dae-localization` (`625`, direct `drop (call $producer)` instead of unused temp, `-5000` normalized / `-1250` raw), and `dae-effectful-args` (`1250`, effect-preserving transitive producer-return cleanup, `-7500` normalized/raw). Original total failure-delta evidence was `-31250` canonical and `-24375` raw bytes versus Binaryen; the post-type-reuse dedicated mismatch replay improves raw to `-27500` while preserving `-31250` canonical. The superseded two-result `dae-unused-returns` residual from `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact` remains documented as one sampled multi-result Starshine-win tuple/local-spill avoidance shape (`14` dirs, hash `21baa02654be`, `-280` normalized / `-336` raw bytes) after raw validation and Node replay. This classifies the current direct-profile residual families by selected profile, but it is not full DAE closeout: the direct dedicated-profile lane remains raw-red, broader type-refinement/scheduling/integration lanes still need evidence, and broad lanes are only at smoke scale. Direct regular GenValid smoke `.tmp/pass-fuzz-dead-argument-elimination-genvalid-1000-20260705` requested/compared `1000/1000`, normalized `1000`, mismatches/failures `0`, Binaryen cache `0/1000`; the closeout-scale regular lane `.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705` requested/compared `100000/100000`, normalized `100000`, mismatches/failures `0`, Binaryen cache `1314/98686`. Direct explicit wasm-smith smoke `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-1000-20260705` requested `1000`, compared `997`, normalized `997`, mismatches `0`, validation/generator/property failures `0`, command failures `3` all Binaryen/oracle classes (`binaryen-rec-group-zero=2`, `binaryen-invalid-tag-index=1`), cache `wasm-smith 1000/0`, `Binaryen 5/992`, `Binaryen failures 0/3`. The superseded closeout-scale direct wasm-smith lane `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-20260705` requested `10000`, compared `9956`, normalized `9955`, mismatches `1`, validation/generator/property failures `0`, command failures `44` all Binaryen/oracle classes (`binaryen-rec-group-zero=39`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`, `binaryen-bad-section-size=3`), cache `wasm-smith 10000/0`, `Binaryen 1098/8858`, `Binaryen failures 3/41`. The one direct wasm-smith mismatch (`case-009332`) was a size-losing unreachable-control-debris parity gap, not a Starshine win: Starshine retained `drop (unreachable)` before `unreachable` where Binaryen removed it; all input/raw/canonical artifacts validated. A red-first focused cleanup test now covers the reduced debris shape, direct DAE runs the shared raw dropped-unreachable cleanup, and replay `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-case-009332-replay-after-debris-cleanup-20260705` normalized `1/1` with zero failures. The post-fix closeout-scale direct wasm-smith lane `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705` requested `10000`, compared `9956`, normalized `9956`, mismatches `0`, validation/generator/property failures `0`, command failures `44` all Binaryen/oracle classes (`binaryen-rec-group-zero=39`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`, `binaryen-bad-section-size=3`), cache `wasm-smith 10000/0`, `Binaryen 9956/0`, `Binaryen failures 44/0`. Direct random-all `.tmp/pass-fuzz-dead-argument-elimination-random-all-500-20260705` requested/compared `500/500`, normalized `443`, mismatches `57`, failures `0`, Binaryen cache `205/295`; residual selected profiles are `coverage-forced-portable=17`, `dae-constant-args=12`, `dae-unused-params=9`, `dae-localization=5`, `dae-convergence=5`, `dae-tail-call-boundary=5`, and `dae-effectful-args=4`, with aggregate Starshine deltas `-36159` raw / `-84267` canonical / `-579159` WAT bytes and all raw/canonical failure artifacts validating. A post-fix direct random-all 500 rerun `.tmp/pass-fuzz-dead-argument-elimination-random-all-500-post-debris-cleanup-20260705` timed out after 1200s without `result.json`; the completed bounded post-fix smoke `.tmp/pass-fuzz-dead-argument-elimination-random-all-100-post-debris-cleanup-20260705` requested/compared `100/100`, normalized `87`, mismatched `13`, had zero validation/generator/property/command failures, and used Binaryen cache `100/0`. Its mismatch selected profiles were `dae-constant-args=4`, `coverage-forced-portable=3`, `dae-localization=2`, `dae-unused-params=2`, `dae-convergence=1`, and `dae-effectful-args=1`; all `65` input/raw/canonical failure artifacts validated. A larger completed direct random-all post-fix smoke `.tmp/pass-fuzz-dead-argument-elimination-random-all-200-post-debris-cleanup-20260705` requested/compared `200/200`, normalized `176`, mismatched `24`, and had zero validation/generator/property/command failures with Binaryen cache `200/0`. Its raw-red residuals stayed in known direct/coverage cleanup families: `dae-constant-args=7` (`-42` raw/canonical bytes), `coverage-forced-portable=4` (`-5241` raw / `-17920` canonical / `-123013` WAT bytes), `dae-effectful-args=3` (`-18` raw/canonical), `dae-localization=3` (`-6` raw / `-24` canonical), `dae-unused-params=3` (`-6` raw/canonical), `dae-convergence=2` (`+2` raw / `-8` canonical), and `dae-tail-call-boundary=2` (`-4` raw/canonical); all `120` input/raw/canonical failure artifacts validated. A new 300-case direct random-all lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-300-post-debris-cleanup-20260705` completed requested/compared `300/300`, normalized `262`, cleanup-normalized `0`, mismatched `38`, and had zero validation/generator/property/command failures with Binaryen cache `300/0`. A larger 400-case direct random-all lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-400-post-debris-cleanup-20260705` completed requested/compared `400/400`, normalized `350`, cleanup-normalized `0`, mismatched `50`, and had zero validation/generator/property/command failures with Binaryen cache `400/0`. The 400-case raw-red residuals again stayed in known families: `dae-constant-args=12` (`-72` raw/canonical), `coverage-forced-portable=12` (`-20906` raw / `-59246` canonical / `-406830` WAT), `dae-unused-params=8` (`-16` raw/canonical), `dae-convergence=5` (`+5` raw / `-20` canonical), `dae-tail-call-boundary=5` (`-10` raw/canonical), `dae-localization=4` (`-8` raw / `-32` canonical), and `dae-effectful-args=4` (`-24` raw/canonical); all `250` input/raw/canonical failure artifacts validated. Full direct random-all now has a raw-red no-reduction lane: `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` requested/compared `10000/10000`, normalized `9004`, cleanup-normalized `0`, mismatches `996`, validation/generator/property/command failures `0`, Binaryen cache `4950/5050`, and `reduceMismatches=false`. Selected-profile mismatches stayed in the known direct/coverage cleanup families: `coverage-forced-portable=259` (`-589614` raw / `-1291274` canonical / `-8864535` WAT), `dae-unused-params=214` (`-428` raw/canonical), `dae-constant-args=197` (`-1182` raw/canonical), `dae-effectful-args=136` (`-816` raw/canonical), `dae-localization=67` (`-134` raw / `-536` canonical), `dae-tail-call-boundary=63` (`-126` raw/canonical), and `dae-convergence=60` (originally `+60` raw / `-240` canonical; post-type-reuse replay is `-240` raw/canonical); all `4980` input/raw/canonical failure artifacts validated. A 2026-07-06 stratified replay with `--runtime-execution node` over one saved case from each direct random-all residual class (`case-000007`, `000018`, `000020`, `000031`, `000046`, `000050`, `000104`) is recorded under `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706`: the six import-free direct DAE leaf samples all produced equal Node results, the `coverage-forced-portable` sample was runtime-blocked by imports, and all seven replays had zero validation/generator/property/command failures while remaining raw mismatches. The inspected WAT shapes are the same measured cleanup families as the aggregate counters: direct constant substitution instead of a temp local, old-parameter local omission, direct `drop (call $producer)` or producer-return cleanup instead of unused temp storage, `return_call` old-local omission, convergence old-local omission, originally raw `+1` / canonical `-4` per sample before simple function-signature reuse removed the duplicate type, and coverage-profile pure nontrapping prefix deletion before the same terminal `br_table`/`unreachable`. A follow-up red-first simple function-signature reuse test failed before DAE reused an existing equivalent simple function type rather than appending a duplicate replacement type; focused `dae_optimizing_test.mbt` now passes `199/199`, `moon test src/passes` passes `4232/4232`, and native CLI rebuild passed with pre-existing warnings. Replaying the saved direct random-all mismatches under `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-type-reuse-20260706` compared `996/996`, left all `996` raw mismatches, had zero validation/generator/property/command failures, checked Node runtime for `737` import-free cases with zero semantic mismatches (`259` import-blocked), and improved aggregate size deltas to `-593155` raw / `-1294602` canonical / `-8900446` WAT bytes; `dae-convergence=60` is now `-240` raw/canonical / `-2040` WAT with no positive raw/canonical cases. A later typed-block argument-refinement follow-up replayed the same saved random-all mismatches under `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-20260706`; it compared `996/996`, left all `996` raw mismatches, and had zero validation/generator/property/command failures with Binaryen cache `996/0`. The subsequent typed-block result-refinement follow-up replayed that saved mismatch set again under `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-20260706`; it again compared `996/996`, left all `996` raw mismatches, and had zero validation/generator/property/command failures with Binaryen cache `996/0`. A direct residual closeout replay added the documented DAE cleanup normalizers over those saved mismatches under `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-cleanup-normalized-20260706`; it compared `996/996`, normalized `0`, cleanup-normalized `0`, left `996` mismatches, had zero validation/generator/property/command failures, used Binaryen cache `996/0`, and all `4980` input/raw/canonical wasm artifacts validated. The normalized replay confirms that the current direct residuals do not collapse under `drop-consts` / `unreachable-control-debris`; it stays raw-red but all measured per-profile aggregate deltas are non-positive for Starshine: `coverage-forced-portable=259` (`-590229` raw / `-1291274` canonical / `-8864535` WAT), `dae-unused-params=214` (`-428` raw/canonical / `-7276` WAT), `dae-constant-args=197` (`-1182` raw/canonical / `-10835` WAT), `dae-effectful-args=136` (`-816` raw/canonical / `-10200` WAT), `dae-localization=67` (`-134` raw / `-536` canonical / `-4489` WAT), `dae-tail-call-boundary=63` (`-126` raw/canonical / `-1071` WAT), and `dae-convergence=60` (`-240` raw/canonical / `-2040` WAT), for a total `-593155` raw / `-1294602` canonical / `-8900446` WAT bytes. Sampled shapes remain the inspected Starshine cleanup families: omitted old-parameter locals, direct constant substitution rather than Binaryen temp materialization, effect-preserving `drop (call $producer)` / producer-return cleanup instead of unused temp storage, repaired `return_call` with old-local omission, convergence old-local omission, and broad coverage-profile pure/unreachable prefix deletion. This strengthens measured-Starshine-win evidence for the sampled residuals but is not full acceptance or green signoff. A direct scheduling replay that added `remove-unused-module-elements` to the same saved residual set, `.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`, also stayed raw-red: `996/996` compared, `0` normalized, `0` cleanup-normalized, `996` mismatches, zero validation/generator/property/command failures, Binaryen cache `724/272`, `reduceMismatches=false`, and all `4980` input/raw/canonical artifacts validated. Aggregate deltas in this direct DAE+RUME context were `-505487` raw / `-1166714` canonical / `-8016600` WAT bytes; canonical and WAT deltas were Starshine-smaller for every residual profile, but `coverage-forced-portable` had `59` raw-positive cases while the other residual profiles remained raw/canonical/WAT smaller. A 2026-07-07 selected-profile size audit saved in `.tmp/dae-random-all-residual-profile-size-summary-20260707.json` maps the RUME replay back to the original random-all manifest: `coverage-forced-portable=259` (`-503105` raw / `-1164131` canonical / `-7987532` WAT, `59` raw-positive and `0` canonical-positive cases), `dae-unused-params=214` (`-428` raw/canonical), `dae-constant-args=197` (`-1182` raw/canonical), `dae-effectful-args=136` (`-272` raw/canonical), `dae-localization=67` (`-134` raw / `-335` canonical), `dae-tail-call-boundary=63` (`-126` raw/canonical), and `dae-convergence=60` (`-240` raw/canonical). This proves direct RUME scheduling does not collapse the direct raw residuals and is not direct green evidence; it only strengthens the measured cleanup evidence for non-coverage residuals and isolates the RUME raw-positive subset to coverage-generated unreachable-tail cases. A targeted raw-positive `coverage-forced-portable` follow-up then replayed the largest DAE+RUME raw-positive case, `case-007094`, into `.tmp/pass-fuzz-dead-argument-elimination-coverage-raw-positive-7094-rume-reduced-20260706`: `1/1` compared, `0` normalized, `0` cleanup-normalized, `1` mismatch, zero validation/generator/property/command failures, Binaryen cache `1/0`, `reduceMismatches=true` but no reduced-artifact metadata. A runtime retry under `.tmp/pass-fuzz-dead-argument-elimination-coverage-raw-positive-7094-rume-runtime-20260706` stayed raw-red with zero failures and `runtimeExecutionMatrix.outcome="blocked"` (`unsupported=1`) because the coverage sample imports host/table/memory/global surfaces; its five input/raw/canonical wasm artifacts validate. Size/section review of `case-007094` shows a raw-positive but canonical-negative shape: Binaryen raw/canonical is `4488` bytes with a `4178`-byte code section, Starshine raw is `5494` bytes with a `5176`-byte code section, but Starshine canonical falls to `365` bytes and WAT falls from `29731` to `1495` bytes after unreachable-tail canonicalization. A follow-up raw-positive subset scan under `.tmp/dae-raw-positive-analysis-20260706` quantified all `59` raw-positive `coverage-forced-portable` cases in the DAE+RUME replay: raw deltas are positive for Starshine (`+30761` total, `+348..+1006` per case), but every canonical and WAT delta is negative (`-186785` canonical and `-1280032` WAT total). The same scan found `0` side-effecting call/store/global/table/memory mutation/throw/atomic keywords before the first terminal `br_table` in the input, Binaryen raw, or Starshine raw WAT for every case; every input and Starshine raw sample had side effects only after that terminal, and the input first-terminal line stayed within `568..573`. A follow-up source review ties the first terminal to the coverage generator itself: `portable_coverage_forced_default()` inherits coverage-forced branch-heavy control plus calls/tables/mems/globals/atomics while disabling refs/tags, `gen_valid_append_coverage_prelude()` emits expanded scalar numeric coverage before `gen_valid_append_core_control_ops()`, and `gen_valid_append_core_control_ops()` emits the first `i32.const 1; br_table` block before the later effectful coverage templates. A bounded `wasm-tools shrink` attempt on `case-007094` under `.tmp/dae-shrink-raw-positive-7094-20260706` accepted the original with both a strict scan predicate and a size-only raw-positive/canonical-negative predicate, but 200 attempts found no smaller module (`41526` bytes, `0.00%` reduction, no output file created by the local shrink command). This is stronger sampled/scan/source-shape evidence for a pure-prefix/unreachable-tail cleanup subset, not full family acceptance or green signoff. These focused refinement, normalized-residual, raw-positive coverage, and direct RUME-context replays do not reduce the known raw-red cleanup families. Replaying the saved direct dedicated-profile mismatches under `.tmp/pass-fuzz-dae-direct-closeout-mismatch-replay-after-type-reuse-20260706` compared `6875/6875`, left all `6875` raw mismatches, had zero validation/generator/property/command failures, Node runtime all-equal, and improved aggregate size deltas to `-27500` raw / `-31250` canonical / `-334375` WAT bytes; `dae-convergence=625` is now `-2500` raw/canonical / `-21250` WAT with no positive raw/canonical cases. This improves direct wasm-smith and random-all matrix evidence and removes the convergence raw-positive caveat. Later flat, droppable-prefix, simple/nested block-carrier, nested explicit-return block-carrier, and simple loop-carrier multi-result, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, interleaved droppable debris before a single-result `if` operand, bounded typed `select` operands in multi-result bodies including the non-tail-lane select-first/scalar-second shape, and block-/pure-condition typed-select variants, simple/nested block-carrier plus simple/nested/void-block explicit-return block-carrier multi-result `if`-arm, simple mixed single- and multi-result return/fallthrough `if`, zero-param typeidx block body, zero-param typeidx terminal `if`, and fallthrough typed/typeidx loop refinement focused slices improve returned-value type-refinement breadth but did not rerun compare-pass and do not change raw status. Direct DAE remains raw-red because the dedicated-profile 10000 lane and the full random-all lane both still have raw mismatches.
- Dedicated profile aliases execute but do not imply parity: after-refine one-case smokes for `dae-all` and `dae-closeout` each compared `1/1` with `1` mismatch; `dae-optimizing-closeout` compared `1/1` with `1` normalized match.
- `dae-optimizing` dedicated-profile evidence is green through the 10000-case dedicated lane: superseded 100-case smokes all requested/compared `100/100`, normalized `100`, cleanup-normalized `0`, mismatches `0`, validation/generator/property/command failures `0`, and Binaryen cache `100/0` with `--normalize drop-consts --normalize unreachable-control-debris`; `.tmp/pass-fuzz-dae-optimizing-smoke-1000-single-return` requested/compared `1000/1000`, normalized `1000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `1000/0`, selected `dae-optimizing=1000`; `.tmp/pass-fuzz-dae-optimizing-closeout-10000-single-return` requested/compared `10000/10000`, normalized `10000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `10000/0`, selected `dae-optimizing=10000`. After the exact-non-null post-cleanup fix, `.tmp/pass-fuzz-dae-optimizing-smoke-1000-post-exact-ref-is-null` again requested/compared `1000/1000`, normalized `1000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `1000/0`.
- Broader `dae-optimizing` lanes now have regular GenValid and explicit wasm-smith evidence plus one classified random-all gap. Regular GenValid `.tmp/pass-fuzz-dae-optimizing-genvalid-100000-20260705` requested/compared `100000/100000`, normalized `100000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `314/99686`, selected `binaryen-oracle-portable=100000`. The first explicit wasm-smith attempt `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-20260705` exposed a Starshine command failure on a wasm-smith module whose only live type reference was an imported tag. A red-first focused test now covers that metadata/liveness bug, and the post-fix rerun `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-post-import-tag-20260705` requested `10000`, compared `9956`, normalized `9955`, cleanup-normalized `1`, mismatches `0`, validation/generator/property failures `0`, command failures `44` all in Binaryen/oracle classes (`binaryen-rec-group-zero=39`, `binaryen-invalid-tag-index=1`, `binaryen-table-index-out-of-range=1`, `binaryen-bad-section-size=3`), with cache `wasm-smith 10000/0`, `Binaryen 9955/1`, and `Binaryen failures 44/0`. Random-all attempts did not complete because they timed out without `result.json` while other Moon/fuzz work was contending in the workspace: `.tmp/pass-fuzz-dae-optimizing-random-all-10000-20260705` reached `408` compared cases (`382` matches / `26` mismatches) and `.tmp/pass-fuzz-dae-optimizing-random-all-1000-20260705` reached `398` compared cases (`373` matches / `25` mismatches). The partial 1000 selected-profile failure sample was `coverage-forced-portable=11`, `dae-localization=4`, `dae-effectful-args=4`, `dae-arg-type-refinement=4`, and `dae-unused-returns=2`; the `dae-arg-type-refinement` cases were a true post-cleanup convergence gap where exact non-null `ref.is_null` folded to `0` but Starshine did not rerun DAE to delete the now-dead parameter. A red-first focused test now covers that shape, the implementation reruns a guarded post-cleanup DAE core for touched exact-non-null zero bodies and strips the resulting dropped `struct.new_default` actuals; replay `.tmp/pass-fuzz-dae-optimizing-random-all-1000-replay-20260705` fixed those four arg-refinement cases (`25/25` compared, `4` normalized, `21` remaining mismatches, failures `0`, Binaryen cache `25/0`). A later red-first unused-return rerun test covered the remaining `dae-unused-returns` partial shape: after dropped-result cleanup reduced a helper body to `nop`, a guarded post-cleanup DAE rerun now deletes the newly dead helper params and pure actuals. Replay `.tmp/pass-fuzz-dae-optimizing-random-all-1000-replay-post-unused-return-20260705` compared `25/25`, normalized `6`, cleanup-normalized `0`, left `19` mismatches, had zero validation/generator/property/command failures, and used Binaryen cache `25/0`; the two unused-return cases now match. The remaining 19 partial mismatches are classified as non-closeout random-all cleanup/scheduling evidence rather than signoff: `coverage-forced-portable=11` is broad pure/unreachable debris cleanup from the coverage profile, with Starshine deleting large pure prefixes before the same terminal `br_table`/`unreachable` shape and aggregate size deltas `-5082` raw / `-254428` WAT bytes despite mixed per-case raw deltas; `dae-localization=4` is the already-classified effect-preserving direct `drop (call $producer)` vs Binaryen temp-local receive family (`0` raw / `-200` WAT bytes); `dae-effectful-args=4` is the already-classified transitive producer-return cleanup family (`-28` raw / `-352` WAT bytes). At that point full random-all remained missing. A later retry after the imported-tag and post-cleanup fixes, `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705`, was killed by the 7200s command timeout without `result.json` after reaching `1037` case records: `1035` compared cases, `985` normalized matches, `0` cleanup-normalized matches, `50` mismatches, `2` Starshine command failures, and no recorded generator/validation/property failures. Selected-profile counts in the partial were `binaryen-oracle-portable=123`, `ssa-nomerge-smoke=119`, `pass-fuzz-stress=118`, `ssa-nomerge-parity=113`, `coverage-forced-portable=104`, `local-subtyping-straight-line=68`, `local-subtyping-unreachable-tail=53`, `heap2local-struct=52`, `coalesce-locals-straight-line=47`, `coalesce-locals-loop-copy-through=33`, `heap2local-array=32`, `heap2local-ref=31`, `coalesce-locals-structured=28`, `dae-constant-args=23`, `dae-unused-params=21`, `dae-arg-type-refinement=12`, `dae-return-type-refinement=10`, `dae-localization=9`, `dae-effectful-args=9`, `dae-convergence=9`, `dae-unseen-call-boundary=8`, `dae-unused-returns=8`, and `dae-tail-call-boundary=7`. The `50` mismatches were `coverage-forced-portable=32`, `dae-localization=9`, and `dae-effectful-args=9`, matching the prior non-closeout cleanup/scheduling residual classes. The two command failures were `coverage-forced-portable` tail-call validation failures (`case-000433` and `case-000434`) with `final module validate: tail call result mismatch`. A red-first focused fixture first covered the reduced direct-tail result-preservation subset; a follow-up red-first fixture then covered tail-call result sites that appear after an unreachable root in the same function body, matching `case-000434`'s reduced failure mode. The implementation now treats any syntactically present direct/indirect/ref tail call as a conservative live result dependency for result-removal decisions, even when it is in an unreachable suffix. Native CLI replays of both `case-000433` and `case-000434` now succeed, and compare-harness command-failure replays for each case now record zero command/validation/generator/property failures. The two replay mismatches are sampled `coverage-forced-portable` pure/unreachable cleanup: Starshine's canonical WAT is a deletion-only subset of Binaryen's (`case-000433`: `0` additions / `2502` deletions, raw `-850` bytes, canonical `-4123` bytes; `case-000434`: `0` additions / `1668` deletions, raw `-1117` bytes, canonical `-2749` bytes), and all raw/canonical artifacts validate. A post-fix random-all smoke `.tmp/pass-fuzz-dae-optimizing-random-all-smoke100-post-tail2-20260705` requested/compared `100/100`, normalized `94`, mismatched `6` (`coverage-forced-portable=3`, `dae-localization=2`, `dae-effectful-args=1`), and had zero failures; a longer prefix attempt `.tmp/pass-fuzz-dae-optimizing-random-all-prefix500-post-tail2-20260705` timed out after `269` compared records with `258` normalized, `11` mismatches (`coverage-forced-portable=5`, `dae-localization=3`, `dae-effectful-args=3`), and zero command failures. The completed bounded random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-500-post-tail2-20260705` requested/compared `500/500`, normalized `474`, mismatched `26`, and had zero validation/generator/property/command failures; selected-profile mismatches were only the known cleanup/scheduling classes (`coverage-forced-portable=17`, `dae-localization=5`, `dae-effectful-args=4`). All raw/canonical failure artifacts validate. Aggregate Starshine deltas versus Binaryen were `coverage-forced-portable`: `-9617` raw / `-57678` canonical / `-395397` WAT bytes despite mixed per-case raw deltas and local-renumbering additions in one sample; `dae-localization`: `0` raw / `-30` canonical / `-250` WAT bytes; `dae-effectful-args`: `-28` raw/canonical / `-352` WAT bytes. A higher bounded random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-1000-post-tail2-20260705` now completes: requested/compared `1000/1000`, normalized `952`, cleanup-normalized `0`, mismatched `48`, and had zero validation/generator/property/command failures with Binaryen cache `1000/0`. Mismatches remained in the same known cleanup/scheduling classes: `coverage-forced-portable=30` (`-16994` raw / `-98893` canonical / `-677774` WAT bytes), `dae-effectful-args=9` (`-63` raw/canonical / `-792` WAT bytes), and `dae-localization=9` (`0` raw / `-54` canonical / `-450` WAT bytes); all `240` input/raw/canonical failure artifacts validated. A 2000-case retry `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-20260705` timed out after 7200s without `result.json` after `1287` case records: `1232` normalized matches, `55` mismatches, and no command failures. The partial mismatches remained in the same classes (`coverage-forced-portable=32`, `dae-effectful-args=14`, `dae-localization=9`) with aggregate Starshine deltas `-16097` raw / `-104538` canonical / `-717104` WAT bytes; all `275` input/raw/canonical failure artifacts validated. Adding the compare harness `--no-reduce-mismatches` option separated closeout counting from expensive GenValid byte-slice reduction and produced completed no-reduction random-all lanes. The 2000-case confirmation `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-no-reduce-20260705` requested/compared `2000/2000`, normalized `1919`, cleanup-normalized `0`, mismatches `81`, validation/generator/property/command failures `0`, Binaryen cache `1608/392`, and `reduceMismatches=false`; mismatches stayed in `coverage-forced-portable=48` (`-33350` raw / `-153853` canonical / `-1054367` WAT), `dae-effectful-args=22` (`-154` raw/canonical), and `dae-localization=11` (`0` raw / `-66` canonical), with all `405` input/raw/canonical failure artifacts validating. The required full random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` completed requested/compared `10000/10000`, normalized `9538`, cleanup-normalized `0`, mismatches `462`, validation/generator/property/command failures `0`, Binaryen cache `5766/4234`, and `reduceMismatches=false`. Its mismatches are fully classified in the same known cleanup/scheduling families: `coverage-forced-portable=259` (`-132113` raw / `-833893` canonical / `-5714125` WAT), `dae-effectful-args=136` (`-952` raw/canonical), and `dae-localization=67` (`0` raw / `-402` canonical); all `2310` input/raw/canonical failure artifacts validated. This closes the prior random-all command-failure blocker for `dae-optimizing`, with remaining mismatches classified as sampled Starshine-win cleanup/scheduling evidence rather than untriaged semantic gaps.
- Paired cleanup/scheduling probes are filed but not closeout. Plain `dead-argument-elimination + vacuum` on the DAE profile stayed raw-red at both 100 and 1000 cases: `.tmp/pass-fuzz-dae-vacuum-context-100-single-return` requested/compared `100/100`, normalized `32`, cleanup-normalized `0`, mismatched `68`, failures `0`, Binaryen cache `84/16`; `.tmp/pass-fuzz-dae-vacuum-context-1000-single-return` requested/compared `1000/1000`, normalized `312`, cleanup-normalized `0`, mismatched `688`, failures `0`, Binaryen cache `1000/0`, with the same six direct cleanup-leaf failure profiles, so `vacuum` alone does not erase the raw local/temp drift. `dae-optimizing + vacuum` on the optimizing profile is green at 100, 1000, and now 10000 cases: `.tmp/pass-fuzz-dae-optimizing-vacuum-context-100-single-return`, `.tmp/pass-fuzz-dae-optimizing-vacuum-context-1000-single-return`, and `.tmp/pass-fuzz-dae-optimizing-vacuum-context-10000-single-return-no-reduce-20260705` normalized all requested cases with zero failures; the 10000 lane used `reduceMismatches=false` and Binaryen cache `10000/0`. `dae-optimizing + inlining-optimizing` alone stayed raw-red at 100, 1000, and 10000 cases: `.tmp/pass-fuzz-dae-optimizing-inlining-context-100-single-return`, `.tmp/pass-fuzz-dae-optimizing-inlining-context-1000-single-return`, and `.tmp/pass-fuzz-dae-optimizing-inlining-context-10000-single-return-no-reduce-20260705` all had `0` normalized matches and all cases mismatched with zero failures. The 10000 lane used `reduceMismatches=false`, Binaryen cache `10000/0`, selected `dae-optimizing=10000`, aggregate Starshine deltas `+100000` raw / `+100000` canonical / `+1100000` WAT bytes, and all `50000` input/raw/canonical failure artifacts validated; sampled `case-000001` shows Starshine retaining a private unreachable helper and extra function type that Binaryen deletes. Adding `remove-unused-module-elements` normalized the 100-case, 1000-case, and 10000-case lanes (`.tmp/pass-fuzz-dae-optimizing-inlining-rume-context-100-single-return`, `.tmp/pass-fuzz-dae-optimizing-inlining-rume-context-1000-single-return`, `.tmp/pass-fuzz-dae-optimizing-inlining-rume-context-10000-single-return-no-reduce-20260705`) with zero failures; the 10000 lane used `reduceMismatches=false` and Binaryen cache `10000/0`. This classifies that sampled context as a scheduling-neighborhood cleanup requirement rather than a DAE core semantic bug or Starshine win. A broader no-DWARF post-pass suffix context is now also green on the optimizing profile: `dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize` normalized `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-1000-single-return-no-reduce-20260706` and `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-10000-single-return-no-reduce-20260706` (`1000/1000` and `10000/10000`) with zero validation/generator/property/command failures, zero mismatches, `reduceMismatches=false`, DAE cleanup normalizers, `--jobs auto`, and `_build/native/release/build/cmd/cmd.exe`; the 10000 lane used Binaryen cache `10000/0`. A first non-profile regular-GenValid attempt for that full suffix, `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-regular-genvalid-10000-20260706`, is raw-red and stopped at the mismatch cap: requested `10000`, compared `2015`, normalized `0`, cleanup-normalized `0`, mismatches `2015`, validation/generator/property/command failures `0`, selected `binaryen-oracle-portable=2015`, Binaryen cache `0/2015`, `reduceMismatches=false`, `maxFailuresHit=true`. The no-DAE control suffix `.tmp/pass-fuzz-postpass-context-regular-genvalid-no-dae-control-1000-20260706` also mismatched every case (`1000/1000`, normalized `0`, failures `0`, Binaryen cache `0/1000`), and sampled artifacts validate, so this lane is currently classified as a broader non-profile post-pass/portable-oracle output-shape gap rather than DAE-owned green or DAE-owned measured-win evidence. Future direct DAE mismatch families from larger or broader lanes must still be classified rather than hidden as representation drift; direct raw-red follow-up, broader returned-value LUB/type-refinement evidence, and non-profile ordered integration evidence remain open. A later direct `dead-argument-elimination + remove-unused-module-elements` replay over the saved direct random-all residuals (`.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`) also stayed fully raw-red (`996/996` mismatches, zero failures), so RUME is not sufficient to close the plain direct pass residuals even though aggregate canonical/WAT sizes still favor Starshine.
