---
kind: concept
status: supported
last_reviewed: 2026-07-10
sources:
  - ../../../raw/binaryen/2026-05-04-dead-argument-elimination-current-main-recheck.md
  - ../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md
  - ../../../raw/research/0435-2026-05-04-dead-argument-elimination-current-main-recheck.md
  - ../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md
  - ../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ./starshine-strategy.md
  - ../dae-optimizing/starshine-strategy.md
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/passes/dead_argument_elimination.mbt
  - ../../../../../src/passes/dae_optimizing_test.mbt
  - ../../../../../src/lib/types.mbt
  - ../../../../../src/validate/env.mbt
  - ../../../../../src/validate/typecheck.mbt
  - ../../../../../src/binary/decode.mbt
  - ../../../../../src/wast/lower_to_lib.mbt
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./wat-shapes.md
  - ./starshine-strategy.md
  - ../dae-optimizing/index.md
  - ../signature-pruning/index.md
---

# Starshine `dead-argument-elimination` port readiness and validation

This page began as the plain `dead-argument-elimination` implementation-readiness checklist. It is now partly superseded by the active partial Starshine implementation: `dead-argument-elimination` and `dae` route to the shared DAE core, while `dae-optimizing` / `dead-argument-elimination-optimizing` route to the optimizing wrapper.

Use it when reviewing remaining plain-DAE gaps or whether a plain-DAE change accidentally implements the optimizing sibling's scheduler behavior. Any old boundary-only wording below should be read as historical context unless restated as current.

## Current hold point

Starshine currently has:

- local module-pass names `dead-argument-elimination` and `dae` in [`src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt);
- dispatcher routing in [`src/passes/pass_manager.mbt`](../../../../../src/passes/pass_manager.mbt) from both names to `dae_run_module_pass(mod_)`;
- a shared owner file, [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt), also used by the optimizing sibling;
- focused green pipeline tests in [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt);
- no active preset role for the plain pass.

That is intentionally different from upstream Binaryen, where the public pass name is `dae` and `dae-optimizing` is a sibling wrapper over the same core engine.

## Why this must be a module pass

Plain DAE changes a function boundary and every owned caller together.
A HOT peephole cannot see enough of the module to do that correctly.

The minimum faithful local implementation needs all of these module facts before it mutates anything:

- function signatures and type-section users;
- imports and exports;
- direct `call` and `return_call` sites;
- function references and call-reference / indirect-call escape surfaces;
- tail-call relationships;
- all direct operands at each parameter position;
- whether each result is dropped by all owned callers;
- whether removed operands have effects that must remain evaluated.

## First safe slice: no-rewrite analyzer

Before deleting any parameter, add an analyzer-only pass or test helper that reports candidates without changing the module.

It should classify each defined function as one of:

- **closed direct-call boundary**: all relevant calls are owned direct calls and the signature may be considered;
- **visible boundary**: import/export/public/reference exposure means the signature must not change;
- **tail-call constrained**: dropped-result changes are blocked or limited by tail-call compatibility;
- **operand-localization needed**: a candidate exists, but call operands must first be localized or preserved explicitly.

Validation for this slice:

- assert no binary/text output changes;
- compare candidate classification against small Binaryen `--dae` fixtures from [`./wat-shapes.md`](./wat-shapes.md);
- add tests for exports, imports, `ref.func`, `call_ref`, `call_indirect`, and `return_call` even if the first mutating slice ignores most of them.

## First mutating slice: scalar dead-param deletion

The smallest useful mutating slice is narrower than Binaryen's full pass:

1. Only private defined functions.
2. Only direct `call` sites, not `call_ref` or `call_indirect` rewrites.
3. Only scalar parameters that are not read in the callee body.
4. Preserve removed actual operands as side-effecting statements in the caller when they are not trivially removable.
5. Repair the callee signature and every direct call in the same module rewrite.
6. Refuse recursive and multi-function cycles until the candidate graph is explicit enough to prove them.

This slice should already be a module pass because it changes declarations and callsites together.
It should not be implemented by locally deleting `local.get` or by editing only function bodies.

## Follow-up slices

After the scalar slice is green, port the remaining Binaryen families one at a time:

- **constant actual materialization**: if all owned callers pass the same constant, insert the callee-local value before removing the incoming parameter;
- **recursive and forwarding cycles**: remove parameters forwarded through direct-call cycles only when the entry value is never otherwise observed;
- **GC parameter refinement**: keep live parameters but narrow their reference type from call-operand least-upper-bound evidence;
- **result refinement**: narrow result types from returned-value evidence and repair call expression types;
- **dropped-result removal**: remove results only when all owned callers drop them and tail-call constraints allow it;
- **uninhabited-result preservation**: emit the needed `call; unreachable`-style repair when deleting an uninhabited result would otherwise lose control-flow knowledge;
- **operand localization and retry**: localize hard operands first, then rerun the boundary core when the localized form exposes a legal deletion;
- **unprofitable-chain throttle**: preserve Binaryen's one-caller chain throttle rather than making Starshine an unbounded signature-churn pass.

## Plain-vs-optimizing guardrail

Plain `dead-argument-elimination` must stop after the shared boundary rewrite core.
It must not run the nested cleanup replay documented for [`../dae-optimizing/starshine-strategy.md`](../dae-optimizing/starshine-strategy.md).

Practical test rule:

- `--pass dead-argument-elimination` may leave callee-local setup or other cleanup debris that remains valid;
- the optimizing sibling may clean that debris only when `dead-argument-elimination-optimizing` / a future `dae-optimizing` alias is requested.

If a future plain pass starts matching every optimizing golden, check whether it accidentally imported the sibling scheduler.

## Exact local code surfaces

Current reusable code surfaces:

- [`src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
  - registers `dead-argument-elimination`, `dae`, `dae-optimizing`, and `dead-argument-elimination-optimizing` as module-pass entries;
  - keeps `dae-optimizing`, not plain DAE, in the public `optimize` and `shrink` presets.
- [`src/passes/pass_manager.mbt`](../../../../../src/passes/pass_manager.mbt)
  - dispatches `dead-argument-elimination` / `dae` to the shared core with `optimize=false`;
  - dispatches `dae-optimizing` / `dead-argument-elimination-optimizing` to the optimizing wrapper.
- [`src/passes/dead_argument_elimination.mbt`](../../../../../src/passes/dead_argument_elimination.mbt)
  - owns the current call-boundary summary, parameter/constant/result rewrite helpers, cleanup helpers, core iteration, and optimizing-suffix flag.
- [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt)
  - contains the current focused direct DAE and optimizing sibling registry/pipeline tests.
- [`src/lib/types.mbt`](../../../../../src/lib/types.mbt)
  - `Instruction::call_ref(...)` and `Instruction::return_call_ref(...)` constructors;
  - direct call / tail-call instruction representation used by future caller repair.
- [`src/validate/env.mbt`](../../../../../src/validate/env.mbt)
  - `Env::resolve_functype(...)` and `Env::get_functype_by_funcidx(...)`, needed for signature checks and repaired-call validation.
- [`src/validate/typecheck.mbt`](../../../../../src/validate/typecheck.mbt)
  - type rules for `call`, `call_indirect`, `call_ref`, `return_call`, and `return_call_ref`; these are the post-rewrite correctness oracle inside Starshine.
- [`src/binary/decode.mbt`](../../../../../src/binary/decode.mbt)
  - binary support for `call_ref` / `return_call_ref`, useful for escape-surface tests.
- [`src/wast/lower_to_lib.mbt`](../../../../../src/wast/lower_to_lib.mbt)
  - text-fixture lowering for call and tail-call families.

Remaining audit surfaces for faithful parity:

- fresh Moon validation for the direct-pass wiring and all DAE GenValid templates;
- direct `dead-argument-elimination` / `dae` Binaryen-oracle compare lanes;
- direct `dae-optimizing` compare lanes with documented cleanup normalizers;
- evidence that any remaining output drift is a measured Starshine win or a narrow approved non-goal, not hidden parity debt.

## Active implementation-family audit, 2026-07-04

This source sweep supersedes the earlier generic "classify DAE-A through DAE-O" TODO with a first-pass status matrix. It is **not** closeout: 2026-07-05 continuations compiled the focused pass/GenValid tests and ran scaled dedicated-profile lanes. The first direct DAE aggregate smoke exposed invalid GC refinement templates; after fixing the final-super subtype bug, later red-first slices fixed direct `return_call` unused-parameter rewriting and exact `struct.new_default`-derived GC argument/result signatures. A later GenValid slice changed `dae-unused-returns` from the superseded two-result residual trigger to a generated single-result DAE-F positive leaf. The historical direct dedicated closeout `.tmp/pass-fuzz-dae-direct-closeout-10000-single-return` requested/compared `10000/10000`, normalized `3125`, cleanup-normalized `0`, and had `6875` raw mismatches with zero failures. After typed scratch-local localization made the `dae-localization` and `dae-effectful-args` singleton lanes green at `10000/10000` each, `.tmp/pass-fuzz-dae-direct-closeout-10000-after-scratch-localization-20260709` refreshed the aggregate to `5000/10000` normalized with `5000` raw mismatches and zero failures, now only `dae-unused-params=1875`, `dae-constant-args=1875`, `dae-convergence=625`, and `dae-tail-call-boundary=625`. A 2026-07-06 red-first simple function-signature reuse fix removes the convergence raw-positive caveat in post-fix mismatch replays (`dae-convergence` becomes `-2500` raw/canonical in the dedicated replay and `-240` raw/canonical in the random-all replay), but all saved mismatch cases remain raw mismatches. A later 2026-07-06 typed-block operand slice used a local Binaryen probe to confirm that `--dae` narrows a live broad base parameter when owned direct callers pass a non-null leaf typed-block operand; the red-first focused test now passes after Starshine accepts non-null block-result actuals, and replaying the saved direct random-all mismatches under `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-20260706` still left `996/996` raw mismatches with zero failures. A follow-up typeidx block operand probe `.tmp/dae-arg-typeidx-block-leaf-operand-refine.wat` showed Binaryen narrows the same broad parameter when the actual is a zero-param `(block (type $carrier))` whose type result is non-null leaf; red-first focused Starshine coverage failed while the parameter stayed `(ref null $base)`, then passed after DAE accepted zero-param type-indexed block actual candidates. A later typeidx if/loop operand probe `.tmp/dae-arg-typeidx-if-loop-operand-refine.wat` showed Binaryen also narrows through zero-param `(if (type $carrier) ...)` and `(loop (type $carrier) ...)` actuals whose type result is non-null leaf; red-first focused Starshine coverage failed while the parameter stayed broad, then passed after DAE accepts the value-producing control instruction at the end of an argument slice as the candidate source. No compare-pass lane was run for those focused slices, so direct DAE remains raw-red. A follow-up multi-result typeidx block/if/loop argument probe family showed Binaryen v130 narrows the same live broad parameter when a type-indexed control actual returns an earlier scalar lane plus a final non-null leaf reference consumed by the helper; red-first focused Starshine coverage failed for all three controls while the helper stayed broad, then passed after DAE accepted the last non-null concrete GC result lane of final `block`/`if`/`loop` actuals. A follow-up typed-block result slice probed a helper body shaped as `(block (result (ref $leaf)) (struct.new_default $leaf))`; Binaryen narrows the broad result to exact leaf and flattens the wrapper, and Starshine now matches the focused case by recognizing that wrapper body and rewriting it to the exact allocation expression when tightening the result signature. The replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-20260706` still left `996/996` saved direct random-all cases as raw mismatches with zero failures. A direct residual closeout replay with the documented DAE cleanup normalizers, `.tmp/pass-fuzz-dead-argument-elimination-random-all-mismatch-replay-after-typed-block-result-cleanup-normalized-20260706`, also compared `996/996`, normalized `0`, cleanup-normalized `0`, left all `996` mismatched, had zero validation/generator/property/command failures, and validated all `4980` input/raw/canonical wasm artifacts; measured aggregate deltas were Starshine-smaller across every residual profile (`-593155` raw / `-1294602` canonical / `-8900446` WAT total). This is still raw-red direct DAE evidence, not a green lane. A direct `dead-argument-elimination + remove-unused-module-elements` replay over the same saved residual set (`.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`) also stayed fully raw-red (`996/996` mismatches, zero failures, zero cleanup-normalized matches) while validating all `4980` input/raw/canonical artifacts; aggregate canonical/WAT sizes still favored Starshine (`-1166714` canonical / `-8016600` WAT), but `coverage-forced-portable` had `59` raw-positive cases in that context, so RUME does not close the plain direct residuals. A targeted follow-up on the largest raw-positive coverage case, `case-007094`, replayed `dead-argument-elimination + remove-unused-module-elements` with the documented cleanup normalizers into `.tmp/pass-fuzz-dead-argument-elimination-coverage-raw-positive-7094-rume-reduced-20260706` (`1/1` compared, `1` mismatch, zero failures, Binaryen cache `1/0`, no reduced-artifact metadata) and `.tmp/pass-fuzz-dead-argument-elimination-coverage-raw-positive-7094-rume-runtime-20260706` (`runtimeExecutionMatrix.outcome="blocked"`, `unsupported=1`, zero failures). Five replay artifacts validated, and section/size review showed the raw-positive caveat is not canonical-positive for the sample (`+1006` raw, `-4123` canonical, `-28236` WAT). A follow-up raw-positive subset scan under `.tmp/dae-raw-positive-analysis-20260706` quantified all `59` raw-positive coverage cases: Starshine raw is positive in aggregate (`+30761`, `+348..+1006` per case), but every canonical/WAT delta is negative (`-186785` canonical and `-1280032` WAT total); input, Binaryen raw, and Starshine raw WAT all have `0` scanned side-effecting keywords before the first terminal `br_table`, and the side-effecting coverage tail appears after that terminal. A follow-up source review ties the terminal shape to `gen_valid_append_core_control_ops()` inside the coverage prelude, after fixed scalar numeric coverage and before later effectful call/SIMD/atomic/memory/table/global templates, and notes that portable coverage keeps those effectful surfaces enabled while only refs/tags are disabled. A bounded `wasm-tools shrink` attempt on `case-007094` accepted the original raw-positive/canonical-negative input with strict and size-only predicates but found no smaller module in 200 attempts (`41526` bytes, `0.00%` reduction). This strengthens the pure-prefix/unreachable-tail cleanup hypothesis for the raw-positive coverage subset but is still sample/scan/source-shape/shrink-negative evidence, not full family acceptance or direct green signoff. The optimizing sibling `.tmp/pass-fuzz-dae-optimizing-closeout-10000-single-return` is normalized-green at `10000/10000` under the documented cleanup normalizers, and regular optimizing GenValid `.tmp/pass-fuzz-dae-optimizing-genvalid-100000-20260705` is green at `100000/100000`. Partial random-all attempts exposed a real optimizing-only post-cleanup convergence gap: after exact non-null argument refinement, nested cleanup folded `ref.is_null` to `0`, but DAE did not rerun to delete the now-dead parameter and pure `struct.new_default` actuals. A red-first focused test now covers and fixes that shape; replay of the 25 partial mismatches normalized the four sampled `dae-arg-type-refinement` cases. A later red-first focused unused-return cleanup test fixed the two sampled `dae-unused-returns` partial mismatches where dropped-result cleanup reduced a helper to `nop` but DAE did not rerun to delete now-dead params; post-fix replay `.tmp/pass-fuzz-dae-optimizing-random-all-1000-replay-post-unused-return-20260705` compared `25/25`, normalized `6`, left `19` mismatches, and had zero failures. The 19 residual partial mismatches are triaged as non-closeout cleanup/scheduling evidence: `coverage-forced-portable=11` broad pure/unreachable-debris cleanup, `dae-localization=4` previously classified effect-preserving temp cleanup, and `dae-effectful-args=4` previously classified producer-return cleanup. Paired context probes now include 10000-case optimizing-profile lanes in addition to the older direct DAE + `vacuum` 1000-case raw-red probe: direct+vacuum stays raw-red with the same direct residuals, optimizing+vacuum is green through 10000, optimizing+inlining alone remains raw-red through 10000 because Starshine retains private unreachable helpers/types until RUME, and adding RUME is green through 10000. The first explicit `dae-optimizing` wasm-smith lane exposed one Starshine metadata/liveness bug: `dae_prune_unused_simple_func_types(...)` pruned a function type referenced only by an imported tag. A red-first imported-tag fixture now covers the bug, DAE marks/import-rewrites `TagExternType` references while pruning simple function types, the original wasm-smith case replays for both direct DAE and `dae-optimizing`, and `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-post-import-tag-20260705` now has zero Starshine failures (`10000` requested, `9956` compared, `9955` normalized, `1` cleanup-normalized, `0` mismatches, `44` Binaryen/oracle command failures). A later 2026-07-06 argument-LUB follow-up probed a two-caller Binaryen v130 shape where distinct leaf structs share a non-final `$mid` subtype; `wasm-opt --dae` narrows the live broad helper parameter to non-null `(ref $mid)`. Red-first focused Starshine coverage failed while the parameter stayed `(ref null $base)`, then passed after DAE accumulated the nearest common supertype for non-null direct-call GC actual candidates instead of rejecting differing leaf candidates. A later returned-value LUB follow-up probed a terminal `if` helper body whose arms return distinct leaf allocations under a broad `(ref null $base)` result; Binaryen v130 narrows both the helper result and terminal `if` block type to non-null `(ref $mid)`, and Starshine now matches that focused shape after computing the returned-arm LUB and rewriting the terminal result instruction type. A subsequent explicit-return LUB follow-up probed a terminal void `if` whose arms each return a distinct leaf allocation and leave a trailing `unreachable`; Binaryen v130 narrows the broad helper result to non-null `(ref $mid)`, and Starshine now matches that focused shape after recognizing explicit return operands in all terminal `if` arms. A follow-up loop-carried explicit-return probe showed Binaryen narrows a broad helper result to exact leaf when a terminal void `loop` body creates a leaf allocation, returns it explicitly, and leaves trailing `unreachable`; Starshine now matches that focused shape by recognizing void loops whose body has explicit-return result evidence. A later typed-control explicit-return probe showed Binaryen narrows broad helper results for terminal typed `if`, typed `block`, and typed `loop` shapes whose values exit through `return`; Starshine now matches those focused shapes by accepting `ValTypeBlockType` explicit-return result evidence and rebuilding the terminal control as void while tightening the result signature. A subsequent flat multi-result probe showed Binaryen narrows a broad reference result within `(result i32 (ref null $base))` helpers when the flat result operands include `struct.new_default $leaf`, both for implicit fallthrough and explicit `return`; Starshine now matches those focused shapes by refining per-result flat operand candidates while preserving scalar results. A follow-up Binaryen probe showed the same narrowing when a trivially droppable prefix (`i64.const; drop`) precedes the flat multi-result operand tail; red-first Starshine coverage failed while it kept the broad result, then passed after DAE accepted trivially dropped prefixes before the flat result operands. A later Binaryen probe showed the same narrowing through simple zero-input multi-result block carriers, both with fallthrough and explicit `return`; red-first Starshine coverage failed while it kept the broad result, then passed after DAE flattened those block carriers when tightening per-result signatures. A follow-up type-indexed block probe showed Binaryen narrows broad single-result helpers through zero-param typeidx block bodies, both for fallthrough and explicit `return`; red-first Starshine coverage failed on the typeidx wrapper, then passed after DAE accepted zero-param typeidx block evidence and rewrote the carrier to a typed/void block as needed while tightening the function result. A later fallthrough-loop probe showed Binaryen narrows broad single-result helpers through zero-param typed and type-indexed terminal `loop` carriers; red-first Starshine coverage failed while the result stayed broad, then passed after DAE accepted zero-param single-result loops and rewrote the carrier type while tightening the function result. A follow-up multi-result loop probe showed Binaryen narrows the reference lane in a zero-param type-indexed terminal `loop` returning `(result i32 (ref null $base))`; red-first Starshine coverage failed while the reference lane stayed broad, then passed after DAE preserved the loop carrier and rewrote it to a new zero-param multi-result block type with the narrowed result lane. A subsequent local-result probe showed Binaryen narrows a broad nullable base helper result to nullable leaf when the returned value is `local.get` of a narrower local; red-first Starshine coverage failed while the result stayed broad, then passed after DAE admitted static `local.get` type evidence and allowed nullable GC result candidates when the old result is nullable. A later block-carrier multi-result `if`-arm probe showed Binaryen narrows `(result i32 (ref null $base))` helpers to `(result i32 (ref (exact $leaf)))` when each terminal `if` arm wraps the flat result operands in a zero-param block; red-first Starshine coverage failed while the reference lane stayed broad, then passed after DAE flattens branch-free/effect-free zero-param block-carrier arms for terminal multi-result `if` result refinement. A follow-up explicit-return block-carrier probe showed the same narrowing when each terminal void `if` arm returns a zero-param multi-result block carrier; red-first coverage failed while the reference lane stayed broad, then passed after DAE applies the same bounded flattening for explicit-return arms. Broader arbitrary returned-value/control/candidate unification beyond the focused static-local, typed-control, simple/nested block-carrier, simple loop-carrier, simple `if`-arm, simple/nested block-carrier `if`-arm, and simple/nested/void-block explicit-return block-carrier `if`-arm subsets remains open, as do local value-flow beyond static local type evidence, non-flat multi-result carriers beyond those bounded block carriers, richer typeidx block boundaries with params/control flow, broader no-DWARF ordered scheduling/integration, and closeout. Direct regular GenValid is green at closeout scale (`.tmp/pass-fuzz-dead-argument-elimination-genvalid-100000-20260705`: `100000/100000` normalized, zero failures), and the previous direct wasm-smith size-losing unreachable-control-debris gap is fixed by red-first raw dropped-unreachable cleanup (`case-009332` replay normalized `1/1`; `.tmp/pass-fuzz-dead-argument-elimination-wasm-smith-10000-post-debris-cleanup-20260705`: `9956/9956` normalized, zero mismatches, `44` Binaryen/oracle command failures). Direct random-all remains raw-red. The refreshed no-reduction lane `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-after-scratch-localization-20260709` requested/compared `10000/10000`, normalized `9207`, cleanup-normalized `0`, mismatched `793`, had zero failures, used Binaryen cache `10000/0`, and left only `coverage-forced-portable=259`, `dae-unused-params=214`, `dae-constant-args=197`, `dae-tail-call-boundary=63`, and `dae-convergence=60`. Historical evidence remains preserved: `.tmp/pass-fuzz-dead-argument-elimination-random-all-500-20260705` normalized `443/500` with `57` mismatches; post-fix 500 timed out without `result.json`; post-fix 100/200/300/400-case lanes normalized `87/100`, `176/200`, `262/300`, and `350/400`; and `.tmp/pass-fuzz-dead-argument-elimination-random-all-10000-post-debris-cleanup-no-reduce-20260705` normalized `9004/10000` with `996` mismatches, zero failures, Binaryen cache `4950/5050`, and all `4980` input/raw/canonical failure artifacts validated. A 2026-07-06 stratified replay `.tmp/pass-fuzz-dead-argument-elimination-random-all-stratified-replay-runtime-20260706` sampled one raw-red saved case from each residual class (`coverage-forced-portable`, `dae-unused-params`, `dae-constant-args`, `dae-effectful-args`, `dae-localization`, `dae-tail-call-boundary`, `dae-convergence`) with `--runtime-execution node`; the six import-free leaf samples had equal Node results, the coverage sample was import-blocked for runtime, and all seven had zero validation/generator/property/command failures while remaining raw mismatches. The sample strengthened the cleanup classification but did not turn direct DAE green. A follow-up red-first simple function-signature reuse test then failed before DAE reused an existing equivalent simple function type instead of appending a duplicate replacement type; focused `dae_optimizing_test.mbt` later passes `242/242`, `moon test src/passes` passes `4275/4275`, `moon info` passes with pre-existing warnings, native CLI rebuild passes with pre-existing warnings, and full `moon test` passes `7714/7714`; saved-mismatch replays over direct random-all (`996/996`) plus direct dedicated-profile (`6875/6875`) remove the convergence raw-positive size caveat while leaving all cases raw mismatches; the later typed-block operand/result replays and cleanup-normalized replay over the direct random-all mismatches also left all `996/996` raw mismatches with zero failures. The explicit-return, simple mixed return/fallthrough, loop-carried explicit-return, typed-control explicit-return LUB, flat/droppable-prefix/block-carrier/nested-block-carrier/loop-carrier/select-operand and block-condition typed-select multi-result, zero-param typeidx block body, zero-param typeidx terminal `if`, fallthrough-loop, and single/multiple flat-suffix single-result `if` operand slices did not rerun compare-pass and do not change direct raw-red status. Direct DAE remains raw-red overall, not green.

The 2026-07-10 original-result boundary-row slice removes a duplicated production indexing surface without changing policy. `DaeOriginalResultBoundaryContext` builds one `DaeOriginalResultPreservationRow` per definition, derives the result-removal prune row from its shared facts plus prune-only arrays, and emits preservation/prune evidence together. Unobserved cleanup, zero-call cleanup, zero-parameter dropped-result removal, current-parameter argument-side repair, and the parameterized self-side-chain gate now consume that projection. Red-first whitebox coverage passed after the helpers landed; focused/full Moon validation and native build passed; fresh direct aggregate, direct random-all, and normalized optimizing random-all counts remained exactly `5000/10000`, `9207/10000`, and `9538/10000` normalized with unchanged residual families and zero failures. This is boundary-evidence consolidation, not direct parity or a scheduler change.

The following 2026-07-10 escaped-result suffix slice removes two more avoidable original-module scanners. `DaeOriginalDeadSuffixEscapedResultCallFacts` records dropped and undropped escaped-result calls from one dead-suffix traversal per definition, and the main plus reverse exact-literal cores now receive that carrier instead of parallel arrays collected by separate module passes. Red-first whitebox coverage passed for mixed facts and no-code defaults; DAE whitebox passed `167/167`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4437/4437`, `moon info`, full `moon test` `7876/7876`, and native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all remained `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures and unchanged profile families. This is a shared module-boundary scan with distinct dropped/undropped production consumers, not a policy or scheduler change.

The next 2026-07-10 escaped-result operand slice removes two further whole-module scans and reuses the suffix carrier instead of rediscovering dropped escaped-result callees. `DaeOriginalEscapedResultOperandCallFacts` records escaped-caller dead-suffix operand target counts and private-result caller target counts from one caller traversal. The main core/test drivers receive one carrier; preservation/reverse setup consumes the dead-suffix projection, while unobserved and zero-call cleanup consume both projections. Red-first whitebox coverage passed after the combined collector landed; DAE whitebox passed `168/168`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4438/4438`, `moon info` with 9 warnings, full `moon test` `7877/7877`, and native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all remained `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures, unchanged profile families, reduction disabled, and Binaryen cache `10000/0`. This is shared original-boundary evidence collection, not a legality, mutation-order, cleanup, or scheduler change.

The following 2026-07-10 result-escape row slice removes the duplicated per-definition indexing that remained after collection was shared. `DaeOriginalResultEscapeRow` is attached to `DaeOriginalResultBoundaryRow` and combines two escape-only original arrays with six exact fields already projected by `DaeOriginalResultPreservationRow`. Unobserved-unreachable and zero-call/uncalled-result cleanup consume that row while retaining separate policy predicates; current direct-call counts and current body simplification stay outside the original row. Red-first whitebox coverage passed after the context and row landed, including dropped-versus-undropped suffix distinction and out-of-range defaults; DAE whitebox passed `168/168`, pass-manager whitebox `189/189`, public DAE `302/302`, `moon test src/passes` `4438/4438`, `moon info` with 9 warnings, full `moon test` `7877/7877`, and native build passed. Fresh direct aggregate, direct random-all, and normalized optimizing random-all remained `5000/10000`, `9207/10000`, and `9538/10000` normalized with zero failures and unchanged profile families. This is original-phase row ownership consolidation, not a result-policy, mutation-order, cleanup, scheduler, or raw-parity change.

The 2026-07-10 observability slice closes the only previously unclassified direct random-all family without changing raw compare policy. `DaeInstructionObservabilityEvidence` replaces duplicate recursive effect/branch scans and adds conservative trap/unknown evidence for unreachable-prefix application. A red-first public regression proves reachable zero-divisor traps are retained. Exhaustive `.tmp/dae-coverage-forced-portable-classification-20260710.json` analysis classifies all `coverage-forced-portable=259` residuals as a measured Starshine win: all `936` prefixes share one constant-safe numeric sequence with no preterminal observable effect, unproved trap, or escaping branch; every raw/canonical/WAT delta favors Starshine; all full runtime attempts are explicitly recorded as unsupported rather than semantic evidence; and a validating import-free reduced probe measures faster Node compile and caught-trap execution. Fresh direct aggregate/random-all and optimizing random-all counts remain `5000/10000`, `9207/10000`, and `9538/10000` normalized respectively with zero failures. Direct DAE is still raw-red, but every current direct residual family is now strongly classified. This does not close broader scanner consolidation, arbitrary type/control reconstruction, dropped-result legality, or ordered-neighborhood work.

| ID | Family | Current classification | Evidence / remaining work |
| --- | --- | --- | --- |
| DAE-A | Registry/scheduling | Active partial, focused-green | `optimize.mbt` registers `dead-argument-elimination`, `dae`, `dae-optimizing`, and `dead-argument-elimination-optimizing` as module passes; `pass_manager.mbt` dispatches plain names to `dae_run_module_pass(mod_)` and optimizing names to the wrapper. `dae_optimizing_test.mbt` focused registry/plain-pipeline tests now compile and pass as part of `moon test --package jtenner/starshine/passes --file dae_optimizing_test.mbt` (`214/214` after the typeidx block body result-refinement fix). Direct compare remains open. |
| DAE-B | Scanner/call graph collection | Source-covered, direct compare open | `dead_argument_elimination.mbt` collects imports/defined functions, type indices, exports/start refs, `ref.func`/element escapes, direct callers, dropped calls, tail-call-sensitive facts, current facts, and large original call summaries. Direct `return_call` targets are now counted as owned direct callsites for argument/signature repair rather than as a blanket tail-call bailout; `return_call_indirect`/references remain unseen/escaped boundaries. Existing optimizing tests exercise many call-summary edge families, but closeout compare lanes remain open. |
| DAE-C | Unused parameter detection | Source-covered for scalar/private direct boundaries, sampled Starshine-win residual | `dae_local_use_for_callee(...)`, `dae_run_core_once(...)`, and `dae_try_rewrite_candidate(...)` detect unread params, rewrite every known direct caller, update locals, and repair types. New plain `dead-argument-elimination` / `dae` unused-scalar tests and DAE GenValid template tests now compile. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`, all `19` `dae-unused-params` failure dirs share one diff shape: Binaryen keeps old parameter slots as unused locals after signature pruning, while Starshine omits them because `dae_build_local_map(...)` only preserves removed parameter locals when the old slot is written. This is classified as a sampled Starshine-win local-declaration cleanup (`-38` normalized/raw bytes), not as closeout for broader direct DAE. |
| DAE-D | Callsite operand/effect preservation | Source-covered for recognized stack slices; sampled localization/effectful leaves are Starshine-win cleanup | `dae_collect_callsite_shapes(...)` / `dae_rewrite_callsite_at(...)` preserve removed non-pure actual slices as evaluated values followed by `drop`, while pure actuals can disappear. Existing `dae-optimizing preserves side effects from removed actual operands` and the `dae-effectful-args` / `dae-localization` profiles cover this intent. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`, all six `dae-localization` residuals share one diff shape: the value-producing block around `call $2` is still evaluated before the live helper argument, Binaryen stores the dead result to an unused temp local, and Starshine emits `drop (call $2)` directly. All six input/raw output artifacts validate, the producer still performs the same `global.set`, and helper signatures/operands match after the unused first parameter is removed. All twelve `dae-effectful-args` residuals also share one diff shape: Starshine preserves the producer call before the live helper actual, then removes the producer's now-dropped private return; Binaryen instead receives the producer result into an unused local. All twelve input/raw output artifacts validate and a Node replay of exported `main` returned matching values for input/Binaryen/Starshine. These sampled leaves are classified as Starshine-win effect-preserving temp/transitive-return cleanup (`dae-localization`: `-48` normalized / `-12` raw bytes; `dae-effectful-args`: `-72` normalized/raw bytes). Broader nonlocal operand shapes remain open. |
| DAE-E | Same-constant argument application | Source-covered; sampled literal leaf is Starshine-win cleanup | Uniform-constant helpers and selected exact-literal revisit lanes feed `dae_try_rewrite_candidate(...)` with `const_params`; many focused tests cover literals, typed blocks, loops, try-table carriers, immutable globals, and high/low revisit budgets. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`, all `19` `dae-constant-args` residuals share one deterministic literal diff: Binaryen materializes the uniform `i32.const 77` through a retained temp local, while Starshine rewrites the live `local.get` directly to the same `i32.const` via `dae_rewrite_instruction_param_consts(...)`, removing the dead temp (`-114` normalized/raw bytes). This sampled literal leaf is classified as a Starshine-win cleanup; broader selected/artifact-shaped constant-carrier families remain parity-risk areas until separately compared. |
| DAE-F | Unused return removal | Source-covered and generated single-result leaf green; sampled transitive producer-return and superseded multi-result tuple/local residuals are Starshine-win | `dae_try_remove_dropped_results(...)` updates single-result callers, removes adjacent `call; drop`, adds callee `drop`, repairs dead suffixes, and updates function types; it intentionally returns unchanged for multi-result callees via the `results.length() != 1` guard. A large focused test family covers dropped result positives/negatives. A red-first GenValid slice changed `dae-unused-returns` from a two-result residual trigger to a single-result private helper whose direct callers drop the helper result; the focused test first failed on the old `[i32, i32]` result shape, then passed after the profile emitted `(i32, i32) -> i32`, and `.tmp/pass-fuzz-dae-unused-returns-single-smoke-100` compared `100/100` with `100` normalized matches, `0` mismatches/failures, and Binaryen cache `100/0`. In the `dae-effectful-args` samples, the argument-removal pass first exposes `call $producer; drop`, then `dae_try_remove_dropped_results(...)` rewrites the private producer from `(result i32)` to void and appends an in-callee `drop` for the former result expression. All twelve samples share this shape, validate, replay with matching `main` results, and are `-72` normalized/raw bytes versus Binaryen's unused temp-local receive, so this transitive producer-return subset is classified as Starshine-win cleanup. The superseded two-result `dae-unused-returns` leaf had fourteen reviewed dirs with one diff shape hash `21baa02654be`: Binaryen kept the `(i32, i32) -> (i32, i32)` helper signature but expanded each caller's `call; drop` into tuple-extract/local-set/local-tee spill traffic, while Starshine preserved the smaller valid stack form unchanged (`-280` normalized / `-336` raw bytes). All fourteen input/Binaryen/Starshine raw artifacts validated and Node replay of exported `main` matched; classify that sampled multi-result residual as Starshine-win tuple/local-spill avoidance, not active single-result DAE-F debt. |
| DAE-G | Argument type refinement | Focused exact-ref, typed-block, zero-param, parametric, and multi-result typeidx block/if/loop operand, typed-select operand including typed-call result operands, branch-free effectful call, and bounded branchful non-escaping `if` conditions, nullable `ref.null` select LUB and nullable common-LUB, and two-caller LUB subsets fixed; broader operand/control LUB open | The Binaryen plain-DAE contract includes live GC parameter type refinement, and the `dae-arg-type-refinement` GenValid leaf exists. [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt) now has direct-DAE fixtures where owned callers pass `struct.new_default $leaf` values to a live broad `(ref null $base)` helper parameter and expect Binaryen-shaped `(ref (exact $leaf))`. The broad-to-leaf fixture failed red on 2026-07-05 (`(ref null $base)` stayed unchanged), and the exactness expectation later failed red while Starshine emitted inexact `(ref $leaf)`. Both pass after adding a narrow owned-direct `struct.new_default` actual refinement path, `StructNewDefault` stack-effect support, and exact reference signatures for these allocation-derived values. A 2026-07-06 local Binaryen probe then showed `--dae` also narrows the same broad parameter when every owned direct caller passes `(block (result (ref $leaf)) (struct.new_default $leaf))`; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE accepted non-null typed block-result operands and preserved the operand candidate's nullability. A later typeidx-block operand probe showed Binaryen narrows the same broad parameter when the single actual instruction is a zero-param `(block (type $carrier))` whose function type returns `(ref $leaf)`; red-first focused coverage failed while Starshine stayed broad, then passed after `dae_direct_gc_actual_candidate_type(...)` resolved zero-param `TypeIdxBlockType` result types for callsite actual refinement. A follow-up typeidx if/loop operand probe showed Binaryen also narrows when the argument slice ends in zero-param `(if (type $carrier) ...)` or `(loop (type $carrier) ...)` actuals returning `(ref $leaf)`; red-first `*typeidx if operands*` failed while the parameter stayed broad, then passed after DAE looks through the final value-producing control instruction for multi-instruction argument slices. A later parametric typeidx control probe family showed Binaryen v130 narrows the same live broad parameter through `(block|if|loop) (type $carrier)` actuals whose function type consumes an `i32` block input and returns `(ref $leaf)`; red-first focused Starshine coverage failed for all three while the helper parameter stayed `(ref null $base)`, then passed after DAE accepted single-result typeidx control result candidates even when the control type has params and treated final value-producing `block`/`loop` instructions like `if` at the end of multi-instruction argument slices. A later 2026-07-06 Binaryen probe showed a `ref.null` operand paired with an exact leaf allocation through `select (result (ref null $leaf))` narrows the broad helper parameter to nullable exact leaf; red-first Starshine coverage failed while the parameter stayed `(ref null $base)`, then passed after DAE treats null select operands as nullable LUB refinements and rewrites the `ref.null` actual plus select result type to the tightened parameter type. A follow-up probe with two owned direct callers passing nullable typed-select actuals from distinct leaf allocations with common `$mid` showed Binaryen narrows the helper parameter to nullable `(ref null $mid)` rather than staying broad; red-first `*nullable typed-select actuals*` failed while Starshine kept `(ref null $base)`, then passed after the general GC LUB helper preserved nullability when combining nullable actual candidates. A later effectful typed-select condition probe `.tmp/dae-arg-select-call-condition-probe.wat` showed Binaryen narrows the same broad parameter to exact non-null `(ref (exact $leaf))` when the select condition is an ordinary direct call returning `i32`; red-first `*effectful typed-select conditions*` failed while Starshine kept `(ref null $base)`, then passed after DAE allowed branch-free condition slices to use function type metadata for stack effects while still rejecting loops, try_table, local set/tee, and escaping branches in condition reconstruction. A follow-up branchful condition probe `.tmp/dae-argument-select-branchful-condition-probe.wat` showed Binaryen narrows the same broad parameter to non-null `(ref $mid)` when a branchful but non-escaping `if (result i32)` supplies the typed-select condition; focused `*branchful typed-select*` passes through the same bounded stack-modeled `if` recognizer added for returned-value select conditions. A later typed-call select-operand probe `.tmp/dae-argument-select-call-operands-probe.wat` showed Binaryen narrows the same broad parameter to non-null `(ref $mid)` when two direct calls returning distinct leaf types feed `select (result (ref null $base))`; red-first focused `*typed-call select*` failed while Starshine stayed broad, then passed after direct `call` result types became GC actual candidates for select operands and the select result type was rewritten to the tightened LUB. A later 2026-07-06 Binaryen probe showed two distinct direct-call leaf allocation operands with common non-final `$mid` narrow the broad base parameter to non-null `(ref $mid)`; red-first Starshine coverage failed while it rejected the differing candidates, then passed after DAE accumulated nearest common supertypes for non-null type-indexed actual candidates. Validation for this slice includes focused `*typeidx block operands*`, `*typeidx if operands*`, and `*typeidx loop operands*` (each `1/1`, failed red before implementation for the zero-param if case), focused `*parametric typeidx*` (`3/3`, failed red before implementation for all three parametric controls), focused `*multi-result typeidx*` (`3/3`, failed red before implementation for all three multi-result controls), focused `*parameters*` (`18/18` after typed-call select-operand argument refinement; previously `17/17` after branchful typed-select argument-condition coverage), focused `*nullable typed-select actuals*` (`1/1`, failed red before implementation), focused `*common direct-call LUB*` (`1/1`), focused `*narrows live GC*` (`6/6` after the typed-control explicit-return fixture), plus latest full `dae_optimizing_test.mbt` (`276/276` after typed-call select-operand argument refinement) and `moon test src/passes` (`4309/4309`); `moon info`, full `moon test` (`7748/7748`), native CLI build, and `git diff --check` passed after the typed-call select-operand argument slice with only pre-existing warnings. The generator's base struct was fixed to be non-final after `wasm-tools` rejected final-super subtyping. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`, all six sampled `dae-arg-type-refinement` leaves now match; typed-block and LUB follow-ups do not make direct DAE green because saved direct random-all residuals are known cleanup families. Broader arbitrary operand/control LUB refinement remains open, including non-tail-lane multi-result parametric typeidx/control-flow boundaries, branchful/effect-bearing control reconstruction beyond ordinary branch-free call conditions, and stack localization beyond these single-result and last-lane multi-result typed control actuals. |
| DAE-H | Return type refinement | Focused exact-ref, static `local.get`, typed-block body, zero-param typeidx block body, terminal-`if` LUB, implicit/explicit typed `select` LUB, typed-call, zero-/ten-param-probed stack-sliced `call_ref`, and zero-/ten-param-probed stack-sliced `call_indirect` select returned-value LUB, block-condition, pure-condition, branch-free call-condition, branch-free call-plus-scalar-post-op, branch-free memory-load, and bounded branchful-if-condition typed-select LUB, multi-result typed-select operand including non-tail-lane plus block-condition and pure-condition typed-select operand LUB, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful and trapping-load parametric typeidx block/if/loop returned-value refinement, explicit-return `if`, simple mixed single- and multi-result return/fallthrough `if`, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, fallthrough typed/typeidx loop, zero-param multi-result fallthrough loop, and flat implicit/explicit plus droppable-prefix, simple block-carrier, nested simple block-carrier, nested explicit-return block-carrier, simple loop-carrier, simple `if`-arm, tail, leading, explicit-return middle, multiple flat-suffix single-result `if` operands, and interleaved droppable-debris single-result `if` operands, simple/nested block-carrier `if`-arm, and simple/nested/void-block explicit-return block-carrier `if`-arm multi-result subsets fixed; arbitrary returned-value/control LUB open | The `dae-return-type-refinement` GenValid leaf exists. [`src/passes/dae_optimizing_test.mbt`](../../../../../src/passes/dae_optimizing_test.mbt) now has a direct-DAE fixture where a private helper is declared as returning `(ref null $base)` but always produces `struct.new_default $leaf` and callers consume the result, with an exact `(ref (exact $leaf))` expected result signature. It failed red on 2026-07-05 (`(ref null $base)` stayed unchanged), then the exactness expectation failed red while Starshine emitted inexact `(ref $leaf)`. Both pass after adding a narrow helper-body `struct.new_default` result refinement path and exact reference signatures for allocation-derived results. A 2026-07-06 local Binaryen probe then showed `--dae` also narrows a broad helper result to exact leaf when the body is a typed block around `struct.new_default`; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE recognized the wrapper body and rewrote it to the exact allocation expression while tightening the result signature. The typed-block result replay over saved direct random-all mismatches still left `996/996` raw mismatches because those residuals are known cleanup families. A later 2026-07-06 Binaryen probe showed a terminal `if` with two leaf-allocation arms sharing a non-final `$mid` narrows the broad helper result and the `if` result type to non-null `(ref $mid)`; red-first focused Starshine coverage failed while it kept `(ref null $base)`, then passed after DAE computed a nearest common supertype for the returned arms and rewrote the terminal result instruction type. A follow-up Binaryen probe showed the same common-supertype narrowing when a terminal void `if` has `struct.new_default; return` in both arms plus trailing `unreachable`; red-first focused Starshine coverage failed while it kept `(ref null $base)`, then passed after DAE recognized explicit return operands in terminal void `if` arms. A later pair of Binaryen probes showed the same common-supertype narrowing for simple mixed return/fallthrough paths where one void `if` arm explicitly returns a leaf allocation and the function falls through to another leaf allocation; red-first Starshine coverage failed by narrowing from the fallthrough terminal alone and producing invalid output, then passed after DAE computes a bounded LUB across the explicit-return arm(s) and fallthrough terminal before tightening the function result. A subsequent multi-result probe, `.tmp/dae-mixed-multi-return-fallthrough-lub.wat`, showed Binaryen narrows the reference lane of `(result i32 (ref null $base))` to `(ref $mid)` for the analogous explicit-return/fallthrough shape; red-first Starshine coverage failed while the reference lane stayed broad, then passed after DAE computes per-result LUBs across recognized explicit-return `if` arms and the flat fallthrough result operands. A later Binaryen probe showed a terminal void `loop` whose body explicitly returns `struct.new_default $leaf` and then falls through to `unreachable` narrows the broad helper result to exact leaf; red-first focused Starshine coverage failed while it kept `(ref null $base)`, then passed after DAE recognized loop bodies with explicit-return result evidence. A follow-up typed-control probe showed Binaryen also narrows terminal typed `if`, typed `block`, and typed `loop` shapes whose produced values exit through `return`; red-first typed-if and typed-control focused tests failed while Starshine kept `(ref null $base)`, then passed after DAE accepted `ValTypeBlockType` explicit-return evidence and rebuilt typed terminal controls as void when tightening the result signature. A later flat multi-result probe showed Binaryen narrows the reference result in helpers returning `(result i32 (ref null $base))` when the flat result operands are `i32.const` plus `struct.new_default $leaf`, both with implicit fallthrough and with a trailing explicit `return`; red-first focused tests failed while Starshine kept the broad reference result, then passed after DAE refined per-result flat operand candidates and preserved the scalar result. A follow-up droppable-prefix probe and a later simple block-carrier probe showed Binaryen applies the same narrowing when the flat operand tail is preceded by `i64.const; drop`, and when the same result operands are wrapped in a zero-input multi-result block with fallthrough or explicit `return`; red-first focused tests now pass after DAE accepts those prefixes and flattens those block carriers while tightening per-result signatures. A later nested block-carrier probe `.tmp/dae-nested-block-multi-result-lub.wat` showed Binaryen also narrows through branch-free/effect-free nested zero-input multi-result blocks; red-first focused Starshine coverage failed while the reference lane stayed broad, then passed after DAE recursively unwraps branch-free/effect-free nested block carriers for the flat multi-result tail before tightening the result signature. A follow-up nested explicit-return block-carrier probe `.tmp/dae-nested-explicit-block-multi-result-lub.wat` showed Binaryen applies the same narrowing when the nested operands exit through a terminal `return`; red-first focused coverage failed while the reference lane stayed broad, then passed after DAE allows recursive unwrapping when the only branch is that terminal function `return` after branch-free/effect-free result operands. A follow-up typeidx-block probe showed Binaryen narrows a broad single-result helper through a zero-param `(block (type $carrier) ...)` wrapper for both fallthrough and explicit `return`; red-first focused tests now pass after DAE accepts zero-param typeidx block result evidence and rebuilds the wrapper as a narrowed single-result block or void explicit-return carrier. A later multi-result `if`-arm probe showed Binaryen narrows `(result i32 (ref null $base))` helpers to `(result i32 (ref $mid))` when simple fallthrough or explicit-return `if` arms produce distinct leaf allocations with a common non-final `$mid`; red-first focused tests now pass after DAE computes per-result LUBs across those arms, appends/reuses a zero-param block type for fallthrough `if` results, and recognizes explicit-return multi-result arm operands. A subsequent typeidx terminal `if` probe showed Binaryen narrows broad single-result helpers through zero-param type-indexed terminal `if` carriers for both fallthrough and explicit `return`; red-first focused tests now pass after DAE admits zero-param typeidx `if` block types in returned-value and explicit-return LUB recognition. A later fallthrough-loop probe showed Binaryen narrows broad single-result helpers through zero-param typed and type-indexed terminal `loop` carriers; red-first focused tests now pass after DAE admits zero-param single-result loops and rebuilds them as narrowed value loops. A follow-up multi-result loop probe showed Binaryen narrows a zero-param type-indexed terminal `loop` returning `(result i32 (ref null $base))`; red-first focused coverage failed while Starshine kept the broad reference result, then passed after DAE preserved the loop carrier and rewrote it through a new zero-param multi-result type with the narrowed lane. A subsequent local-result probe showed Binaryen narrows a broad nullable base result to nullable leaf when the returned value is a `local.get` whose static local type is narrower; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE threaded function-local type evidence into result-candidate recognition and allowed nullable GC candidates only when the old result is nullable. A later block-carrier multi-result `if`-arm probe showed Binaryen narrows the reference lane when each terminal multi-result `if` arm wraps the flat result operands in a zero-param block; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE flattens branch-free/effect-free zero-param block-carrier arms before computing per-result `if` arm LUBs. A follow-up explicit-return block-carrier probe showed the same narrowing when terminal void `if` arms return block-carried multi-result values; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE reuses the branch-free/effect-free flattening path for explicit-return arms. A later nested block-carrier `if`-arm probe `.tmp/dae-terminal-if-nested-block-arm-multi-result-lub.wat` showed Binaryen narrows the reference lane when each terminal multi-result `if` arm wraps flat result operands in nested branch-free/effect-free zero-input blocks; red-first focused coverage failed while Starshine kept `(ref null $base)`, then passed after DAE recursively unwraps branch-free/effect-free zero-param block-carrier arms before per-result arm LUB computation. A paired explicit-return probe `.tmp/dae-terminal-if-nested-block-arm-explicit-return-multi-result-lub.wat` showed Binaryen also narrows terminal void `if` arms that return nested block-carried multi-result values, and the focused Starshine fixture now passes through the same bounded unwrapping path. A later void-block explicit-return probe `.tmp/dae-if-arm-void-block-internal-return-multi-result-lub.wat` showed Binaryen narrows the same broad reference lane when each terminal void `if` arm contains a zero-output block whose body produces flat multi-result operands and performs the function `return`; red-first Starshine coverage failed while the result stayed broad, then passed after DAE recognizes zero-output block wrappers whose body has only a terminal function `return` after branch-free/effect-free result operands and flattens those arms before per-result explicit-return LUB computation. A follow-up tail single-result `if` operand probe `.tmp/dae-multi-result-single-if-lane.wat` showed Binaryen narrows the reference lane to non-null `(ref $mid)` when a flat multi-result body returns `i32.const` plus a single-result `if` whose arms allocate distinct leaves under `$mid`; red-first Starshine coverage failed while the result stayed broad, then passed after DAE accepted the bounded tail shape. A follow-up leading/non-tail probe `.tmp/dae-multi-result-head-if-lane.wat` showed the same narrowing when the single-result `if` supplies the first lane and a scalar operand follows it; red-first coverage failed while the leading reference lane stayed broad, then passed after DAE generalized the stack-safe recognizer to a single modeled `if` operand at any lane in the flat multi-result suffix, verifies all surrounding result/condition operands are stack-local single-value instructions, and rewrites the `if` result type while tightening the function result. A later explicit-return middle-lane probe `.tmp/dae-multi-result-explicit-return-mid-if-lane.wat` showed Binaryen applies the same narrowing when the modeled single-result `if` supplies the middle lane of a three-result flat suffix that exits through terminal `return`; red-first coverage failed while Starshine kept the broad middle lane, then passed after the same stack-safe recognizer accepted explicit-return mode. A follow-up multiple-operand probe `.tmp/dae-multi-result-two-if-operands.wat` showed Binaryen narrows both reference lanes of a flat three-result suffix when two separate stack-local conditions feed two single-result `if` operands around a scalar lane; red-first coverage failed while both lanes stayed broad, then passed after DAE parsed one or more bounded single-result `if` operands in the same flat suffix while still leaving arbitrary non-flat value-slice reconstruction, richer effect/branchful operands, and broader multi-control shapes open. A later interleaved-debris probe `.tmp/dae-multi-result-if-interleaved-debris.wat` showed Binaryen narrows the reference lane of a `(result i32 (ref null $base))` helper when a trivially droppable `i64.const; drop` pair sits between the scalar result and the stack-local condition/single-result `if` operand; red-first Starshine coverage failed while the reference lane stayed broad, then passed after the flat suffix recognizer scanned from the full operand suffix and skipped trivially droppable pairs between modeled result operands. A later typed-select probe `.tmp/dae-select-return-refine.wat` showed Binaryen narrows a broad single-result helper through a typed `select` whose two value operands allocate distinct leaves under `$mid`; the explicit-return sibling `.tmp/dae-select-explicit-return-refine.wat` showed the same narrowing with terminal `return`. Red-first focused Starshine coverage failed while the helper result stayed broad, then passed after DAE recognized a bounded branch-free select suffix with two GC candidate operands, a trivially droppable/simple condition operand, optional terminal `return`, and rewrote the `select` result type to the computed LUB. A follow-up block-condition typed-select probe pair, `.tmp/dae-select-block-condition-refine.wat` and `.tmp/dae-select-multi-result-block-condition-refine.wat`, showed Binaryen also narrows through a branch-free zero-input block condition that feeds the select, both in single-result and flat multi-result suffix helpers. Red-first focused Starshine coverage failed while the reference lane stayed broad, then passed after DAE allowed stack-local branch-free/effect-free one-result select conditions while still leaving effectful and broader control-local select-condition reconstruction open. A later pure-condition typed-select probe pair, `.tmp/dae-select-pure-condition-slice-refine.wat` and `.tmp/dae-select-multi-result-pure-condition-slice-refine.wat`, showed Binaryen narrows the same single-result and flat multi-result helpers when the condition is a branch-free/effect-free multi-instruction stack-local slice such as `i32.const; i32.eqz`; red-first focused coverage failed while Starshine stayed broad, then passed after DAE scanned bounded condition slices with net stack effect `0 -> 1`, rejected local mutation/control/effectful branches for this multi-instruction path, and rewrote the typed `select` result. A follow-up call-condition typed-select probe `.tmp/dae-result-select-call-arg-condition-probe.wat` showed Binaryen also narrows a broad single-result helper when the condition is a branch-free direct call with a scalar operand (`i32.const; call $cond`); red-first `*call-condition select GC results*` failed while Starshine kept `(ref null $base)`, then passed after the returned-value candidate path threaded function type indices into select-condition stack-effect modeling. A follow-up coverage probe `.tmp/dae-result-select-call-postop-condition-probe.wat` showed Binaryen preserves that narrowing when the direct-call condition result feeds a scalar post-op (`i32.const; call $cond; i32.eqz`); focused `*call-postop select GC results*` passes without another implementation change, confirming the branch-free stack-effect model covers simple scalar post-processing after a direct call. A later probe `.tmp/dae-result-select-memory-condition-probe.wat` showed Binaryen also narrows when the select condition is branch-free but potentially trapping memory code (`i32.const 0; i32.load; i32.eqz`); focused `*memory-condition select GC results*` passes through the same stack-effect model. A follow-up probe `.tmp/dae-result-select-branchful-condition-probe.wat` showed Binaryen narrows through a branchful but non-escaping `if (result i32)` condition; red-first focused `*branchful select-condition*` failed while Starshine kept the broad result, then passed after DAE allowed stack-modeled `if` instructions in select-condition slices while still rejecting escaping branch instructions, loops, try_table, and local set/tee there. Broader control-local select-condition reconstruction remains open. A subsequent non-tail-lane probe `.tmp/dae-result-select-non-tail-multivalue-probe.wat` showed Binaryen narrows the first lane of a `(result (ref null $base) i32)` helper when the typed select is followed by a scalar result; focused `*non-tail-lane multi-result select*` passes through the bounded flat multi-result select-operand recognizer. A later typed-call select returned-value probe `.tmp/dae-returned-select-call-operands-probe.wat` showed Binaryen narrows a broad helper result to non-null `(ref $mid)` when two direct calls returning distinct leaves feed `select (result (ref null $base))`; red-first focused `*typed-call select returned-value*` failed while Starshine stayed broad, then passed after DAE accepts direct `call` result types as returned-value GC candidates. A follow-up zero-arg `call_ref` probe `.tmp/dae-returned-select-call-ref-operands-probe.wat` showed Binaryen performs the same narrowing when the select operands are bounded `ref.func; call_ref` slices returning distinct leaf refs; red-first focused `*call_ref select returned-value*` failed while Starshine stayed broad, then passed after DAE recognized zero-param `call_ref` result slices as returned-value GC candidates. A later single-parameter probe `.tmp/dae-returned-select-call-ref-param-probe.wat` showed Binaryen also narrows when each bounded `call_ref` candidate has one stack-local pure argument before the direct `ref.func` target; red-first focused `*parameterized call_ref select returned-value*` failed while Starshine stayed broad, then passed after DAE admitted only the at-most-one-argument, side-effect-free, non-branching direct-target slice. A later `call_indirect` probe `.tmp/dae-returned-select-call-indirect-probe.wat` showed Binaryen narrows the same returned-value select through one-argument indirect calls; red-first focused `*call_indirect select returned-value*` failed while Starshine stayed broad, then passed after DAE admitted only the at-most-one-argument, side-effect-free, non-branching indirect-call slice with a stack-local table index. A follow-up two-parameter probe pair showed Binaryen also narrows direct-target `call_ref` and `call_indirect` select operands with two stack-local pure arguments; red-first focused two-parameter tests failed while Starshine stayed broad, then passed after DAE scanned four-instruction result slices and admitted at most two such arguments. A later three-parameter probe pair showed the same narrowing through three stack-local pure arguments; red-first focused three-parameter tests failed while Starshine stayed broad, then passed after DAE scanned five-instruction result slices and admitted at most three such arguments. A follow-up four-parameter probe pair showed Binaryen narrows the same direct-target `call_ref` and `call_indirect` returned-value select operands with four stack-local pure arguments; red-first focused four-parameter tests failed while Starshine stayed broad, then passed after DAE scanned six-instruction result slices and admitted at most four such arguments. Seven-plus-parameter `call_ref`/`call_indirect`, effectful arguments or table indices, table/indirect `call_ref` targets, and arbitrary target/value reconstruction remain open. A later parametric typeidx probe trio `.tmp/dae-result-param-typeidx-nontail-block-probe.wat`, `.tmp/dae-result-param-typeidx-nontail-if-probe.wat`, and `.tmp/dae-result-param-typeidx-nontail-loop-probe.wat` showed Binaryen narrows the first lane of `(result (ref null $base) i32)` to exact leaf when terminal type-indexed `block`/`if`/`loop` controls consume an `i32`, drop it in their body/arms, and return a GC lane followed by a scalar lane; red-first focused `*non-tail-lane parametric typeidx*` failed for all three controls while Starshine kept `(ref null $base)`, then passed after DAE added a bounded drop-only parametric typeidx terminal-control recognizer and rewrites the control typeidx with the same params plus narrowed results. A follow-up non-drop probe trio `.tmp/dae-result-param-typeidx-nondrop-block-probe.wat`, `.tmp/dae-result-param-typeidx-nondrop-if-probe.wat`, and `.tmp/dae-result-param-typeidx-nondrop-loop-probe.wat` showed Binaryen also narrows the same first lane when the consumed control param flows through a stack-safe nontrapping pure `i32.eqz; drop` prefix before the flat result operands. Red-first focused `*non-drop parametric typeidx*` failed for all three controls, then passed after DAE generalized the consumed-param prefix recognizer from direct drops to stack-safe nontrapping pure prefixes. A later effectful probe trio `.tmp/dae-result-param-typeidx-effectful-block-probe.wat`, `.tmp/dae-result-param-typeidx-effectful-if-probe.wat`, and `.tmp/dae-result-param-typeidx-effectful-loop-probe.wat` showed Binaryen narrows the same first lane when a direct call consumes the control param before the flat result operands. Red-first focused `*effectful parametric typeidx*` failed for all three controls, then passed after DAE allowed ordinary direct calls in this bounded stack-safe prefix recognizer while still requiring the prefix to consume all control params and leave zero stack values before the results. A follow-up trapping-load probe pair `.tmp/dae-result-param-typeidx-load-block-probe.wat` and `.tmp/dae-result-param-typeidx-load-if-loop-probe.wat` showed Binaryen also narrows when the consumed param feeds a potentially trapping numeric load followed by `drop`; red-first focused `*trapping-load parametric typeidx*` failed for all three controls, then passed after DAE allowed stack-modeled numeric loads in the same bounded prefix recognizer while leaving load effects/traps in place and still rejecting stores, local mutation, branchful control, indirect calls, and arbitrary stack localization. Validation for these slices: focused `*non-tail-lane parametric typeidx*` `3/3`; focused `*non-drop parametric typeidx*` `3/3`; focused `*effectful parametric typeidx*` `3/3`; focused `*trapping-load parametric typeidx*` `3/3`; focused `*branchful select-condition*` `1/1` after failing red; focused `*branchful typed-select*` `1/1` for the argument-side branchful condition coverage; focused `*typed-call select*` `1/1` after failing red for the typed-call select-operand argument slice; focused `*typed-call select returned-value*` `1/1` after failing red for the typed-call returned-value slice; focused `*call_ref select returned-value*` `5/5` after failing red for the zero-arg, single-parameter, two-parameter, three-parameter, and four-parameter call_ref returned-value slices; focused `*call_indirect select returned-value*` `4/4` after failing red for the one-, two-, three-, and four-argument indirect-call returned-value slices; focused `*select*` `68/68`; focused `*typeidx*` `25/25`; full `dae_optimizing_test.mbt` `284/284`; `moon test src/passes` `4317/4317`; `moon info`; full `moon test` `7756/7756`; and native CLI build passed with pre-existing warnings before the four-parameter follow-up. The four-parameter follow-up has focused `*call_ref select returned-value*` `5/5`, `*call_indirect select returned-value*` `4/4`, focused `*select*` `70/70`, full `dae_optimizing_test.mbt` `286/286`, `moon test src/passes` `4319/4319`, `moon info`, full `moon test` `7758/7758`, and native CLI build with pre-existing warnings. A follow-up multi-result typed-select probe pair, `.tmp/dae-select-multi-result-refine.wat` and `.tmp/dae-select-multi-result-explicit-return-refine.wat`, showed Binaryen also narrows the reference lane of `(result i32 (ref null $base))` helpers when a flat multi-result suffix contains a scalar lane plus a typed `select` operand that chooses between distinct leaves under `$mid`, both with fallthrough and explicit `return`. Red-first focused Starshine coverage failed while the reference lane stayed broad, then passed after DAE added a bounded flat multi-result select-operand recognizer that consumes two GC candidate operands, a trivially droppable condition, and the typed `select`, rewrites the select result type, and tightens only the matching result lane. The same non-final base generator fix applies here. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact`, all six sampled `dae-return-type-refinement` leaves now match; arbitrary returned-value inference/control/candidate LUB refinement beyond the focused terminal-`if` allocation, typed-select allocation, typed-call and zero-/nine-param call_ref typed-select result operands, block-condition/pure-condition/call-condition/call-post-op-condition/memory-condition/branchful-if-condition typed-select allocation, multi-result typed-select operand allocation including the non-tail-lane select-first/scalar-second shape, non-tail-lane drop-only, stack-safe non-drop, and ordinary direct-call effectful plus trapping-load parametric typeidx block/if/loop terminal controls, explicit-return allocation, simple mixed single- and multi-result return/fallthrough allocation, loop-carried explicit-return, typed-`if` explicit-return, typed block/loop explicit-return, static local.get evidence, typed/typeidx-block argument operands, zero-param typeidx block bodies, zero-param typeidx terminal `if` carriers, fallthrough typed/typeidx loop carriers, and flat/droppable-prefix/simple-block-carrier/nested-block-carrier/simple-loop-carrier/simple-if-arm/tail/leading/explicit-return-middle/multiple-flat-suffix/interleaved-debris-single-result-if-operand/simple-and-nested-block-carrier-if-arm/simple-and-nested-explicit-return-block-carrier-if-arm plus simple mixed return/fallthrough multi-result shapes remains open. |
| DAE-I | Convergence/staleness iteration | Deterministic generated unlock family classified as measured Starshine win; broader convergence open | `dae_run_core(...)` has an 8-iteration fixed loop plus reverse/low revisit lanes and selected-def retries; `dae-convergence` generates a wrapper/leaf unlock shape. The 2026-07-06 signature-reuse fix removed the earlier raw-positive type-duplication caveat. The exhaustive 2026-07-09 analysis `.tmp/dae-convergence-residual-classification-20260709.json` covers all `625` refreshed dedicated-profile and all `60` refreshed random-all convergence residuals. Each lane has one unique artifact shape: both tools prune the leaf first and wrapper later, update the same calls and one-parameter function type, and differ only because Binaryen keeps one unread `i32` replacement local in each rewritten function. Removing exactly those two declarations makes every canonical WAT pair equal. Source review explains the shape: Binaryen `ParamUtils::removeParameters(...)` generically adds a local for every removed parameter, while Starshine's shared removed-parameter projection retains a slot only for a surviving write or localization requirement. All cases are Starshine-smaller with no positive deltas (dedicated `-2500` raw/canonical, `-21250` WAT; random-all `-240` raw/canonical, `-2040` WAT). Node runtime replays check all `685` cases with equal results and zero failures/unsupported runtimes; an alternating output benchmark measures `4.713 ns/call` Starshine versus `4.735 ns/call` Binaryen median and is treated as runtime parity, not a material speed claim. This strongly classifies the generated convergence family as Starshine-win local-declaration cleanup while direct DAE remains raw-red and broader non-template convergence/staleness remains open. |
| DAE-J | Tail-call boundaries | Focused direct `return_call` and direct-tail result subsets fixed; indirect-tail result blocker open | The `dae-tail-call-boundary` profile showed Starshine's old blanket `ReturnCall` bailout was too conservative: in `.tmp/pass-fuzz-dae-direct-smoke-100-after-genfix`, all six tail-call leaves mismatched because Binaryen removed the unused helper parameter and repaired `return_call` sites while Starshine preserved the old signature (`+18` bytes aggregate). A red-first focused fixture in `dae_optimizing_test.mbt` then failed while params stayed at `2` and passed after `dae_scan_current_call_facts_instrs(...)`, `dae_scan_calls_instrs(...)`, and `dae_rewrite_calls_instrs(...)` treated direct `return_call` like direct `call` for argument pruning. In `.tmp/pass-fuzz-dae-direct-smoke-100-after-tailcall`, all six residual tail-leaf diffs are only Binaryen retaining an unused old-parameter local while Starshine omits it (`-12` aggregate bytes), classified as a Starshine-win local-declaration cleanup for this sampled unused-parameter leaf. A later random-all retry exposed `tail call result mismatch` command failures. Red-first tail-call result fixtures now cover and fix a reduced direct-tail dropped-result shape plus a follow-up unreachable-suffix tail-call result shape. Native CLI replays of `case-000433` and `case-000434` now pass, and compare replays no longer command-fail, both replay as sampled `coverage-forced-portable` pure/unreachable cleanup mismatches with deletion-only canonical WAT diffs and validating artifacts. Full random-all closeout still remains open. |
| DAE-K | Indirect/reference/table/export unseen-call barriers | Smoke-green for current leaf; broader open | Exports/start, `ref.func`, element segments, and current refs mark `escaped`; call-ref/call-indirect surfaces are treated as observable effects and non-owned boundaries. `dae-unseen-call-boundary` covers `ref.func` in an active element plus `call_indirect`, and `.tmp/pass-fuzz-dae-direct-smoke-100-after-genfix` matched all six sampled unseen-call leaves. Broader export/start/call_ref/table/ref surfaces and larger counts still need closeout evidence. |
| DAE-L | Localization fallback | Sampled effectful-block leaf is Starshine-win cleanup; broader open | Removed effectful operands are preserved by evaluating and dropping them, and many non-adjacent carrier tests cover constant materialization through local/block/loop/try shapes. The after-genfix localization leaf initially stayed unapproved because all six samples mismatched despite smaller Starshine output. A later `.tmp/pass-fuzz-dae-direct-smoke-100-after-exact` source/diff review checked all six `dae-localization` dirs (`case-000007`, `000023`, `000039`, `000055`, `000071`, `000087`): they have one deterministic diff shape, raw artifacts validate, and the only difference is Binaryen's unused temp `local.set` for the removed block result versus Starshine's direct `drop` of the same effectful producer call before the live helper argument. This is now agent-classified as sampled Starshine-win localization cleanup, not harness-proven semantic equivalence. Broader localization fallback shapes, especially non-template carrier/control forms, remain open. |
| DAE-M | `dae-optimizing` post-change optimization | Focused-green; dedicated, regular GenValid, wasm-smith zero-mismatch, and full random-all evidence; ordered neighborhoods still open | `run_hot_pipeline_apply_dae_nested_cleanup(...)` runs touched-function nested cleanup after productive core rewrites, with touched-count and size guards. Tests cover trace order and touched-only behavior. The after-refine, after-tailcall, after-exact, and single-return 2026-07-05 100-case `dae-optimizing` dedicated-profile smokes all normalized `100/100`; the 1000-case follow-up `.tmp/pass-fuzz-dae-optimizing-smoke-1000-single-return` requested/compared `1000/1000`, normalized `1000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `1000/0`; the closeout-scale dedicated profile `.tmp/pass-fuzz-dae-optimizing-closeout-10000-single-return` requested/compared `10000/10000`, normalized `10000`, cleanup-normalized `0`, mismatches/failures `0`, Binaryen cache `10000/0`, selected `dae-optimizing=10000`. Regular GenValid `.tmp/pass-fuzz-dae-optimizing-genvalid-100000-20260705` requested/compared `100000/100000`, normalized `100000`, failures `0`, Binaryen cache `314/99686`. Explicit wasm-smith `.tmp/pass-fuzz-dae-optimizing-wasm-smith-10000-post-import-tag-20260705` requested `10000`, compared `9956`, normalized `9955`, cleanup-normalized `1`, mismatches `0`, validation/generator/property failures `0`, command failures `44` all in Binaryen/oracle classes, cache `wasm-smith 10000/0`, `Binaryen 9955/1`, `Binaryen failures 44/0`. A partial random-all lane exposed the exact-non-null `ref.is_null` post-cleanup convergence gap; red-first focused coverage now passes, and replay `.tmp/pass-fuzz-dae-optimizing-random-all-1000-replay-20260705` normalized the four sampled arg-refinement mismatches. A later red-first focused unused-return/nop-body rerun test fixed the two sampled `dae-unused-returns` partial mismatches; replay `.tmp/pass-fuzz-dae-optimizing-random-all-1000-replay-post-unused-return-20260705` compared `25/25`, normalized `6`, cleanup-normalized `0`, mismatches `19`, failures `0`, Binaryen cache `25/0`. Residual partial mismatches are triaged as `coverage-forced-portable=11` broad pure/unreachable cleanup drift, `dae-localization=4` known effect-preserving temp cleanup, and `dae-effectful-args=4` known producer-return cleanup. A later full random-all retry `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-fixes-20260705` timed out without `result.json` after `1037` records (`1035` compared, `985` normalized, `50` known cleanup/scheduling mismatches, `2` Starshine tail-call result command failures). The direct-tail and unreachable-suffix tail-call subsets are fixed; `case-000433` and `case-000434` now replay without command failures, but they become ordinary mismatches. The completed bounded random-all lane `.tmp/pass-fuzz-dae-optimizing-random-all-500-post-tail2-20260705` requested/compared `500/500`, normalized `474`, mismatched `26`, and had zero validation/generator/property/command failures. Those mismatches stay within the known cleanup/scheduling classes (`coverage-forced-portable=17`, `dae-localization=5`, `dae-effectful-args=4`), with validating raw/canonical artifacts and aggregate Starshine size wins (`-9617` raw / `-57678` canonical / `-395397` WAT bytes for coverage; `0` raw / `-30` canonical for localization; `-28` raw/canonical for effectful args). The higher bounded lane `.tmp/pass-fuzz-dae-optimizing-random-all-1000-post-tail2-20260705` requested/compared `1000/1000`, normalized `952`, cleanup-normalized `0`, mismatched `48`, and had zero validation/generator/property/command failures; mismatches stayed in the same classes (`coverage-forced-portable=30`, `dae-effectful-args=9`, `dae-localization=9`) with aggregate Starshine deltas `-17057` raw / `-99010` canonical / `-679016` WAT bytes and all `240` input/raw/canonical failure artifacts validating. A 2000-case retry `.tmp/pass-fuzz-dae-optimizing-random-all-2000-post-tail2-20260705` timed out after 7200s without `result.json` after `1287` records (`1232` normalized, `55` mismatches, zero command failures); partial mismatches stayed in the same cleanup/scheduling classes with aggregate deltas `-16097` raw / `-104538` canonical / `-717104` WAT bytes and all `275` input/raw/canonical failure artifacts validating. The full no-reduction lane `.tmp/pass-fuzz-dae-optimizing-random-all-10000-post-tail2-no-reduce-20260705` later completed `10000/10000`, normalized `9538`, left `462` classified cleanup/scheduling mismatches, and had zero failures; 10000-case `dae-optimizing + vacuum`, `dae-optimizing + inlining-optimizing + remove-unused-module-elements`, and the broader full no-DWARF post-pass suffix context `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-10000-single-return-no-reduce-20260706` now normalize fully on the optimizing profile, while `dae-optimizing + inlining-optimizing` alone remains a size-losing scheduling-neighborhood cleanup requirement. Full pass closeout still needs non-profile ordered integration evidence and direct DAE raw-red follow-up. |
| DAE-N | Metadata repair | Focused imported-tag liveness fix; broader validation open | Candidate and result rewrites update function type indices through `dae_update_defined_func_type(...)`, drop name metadata with `.without_name_sec()`, and prune unused simple function types with `dae_prune_unused_simple_func_types(...)`. Existing tests cover unique type replacement and simple type pruning. The first explicit `dae-optimizing` wasm-smith lane found a Starshine command failure where a module with an imported tag as the only type user was pruned to an invalid empty type section (`error: final module validate: No type exists for TypeIdx`). Red-first `dead-argument-elimination preserves imported tag type references while pruning types` now covers this; DAE now marks imported `TagExternType` type indices live and rewrites imported tag type indices under type pruning. The original case replays for direct DAE and `dae-optimizing`, and the post-fix wasm-smith lane has zero Starshine failures. `.mbti`/API diff review and direct compare classification remain required before closeout. |
| DAE-O | Surrounding-pass integration | Partial; 10000-case paired optimizing probes filed | Public `optimize` / `shrink` presets keep `dae-optimizing`, not plain DAE; nested cleanup integrates multiple touched-function hot/module passes. Paired context probes are filed but not closeout: `dead-argument-elimination + vacuum` on the DAE profile stays raw-red at 1000 (`312` normalized, `688` mismatches, zero failures), matching the direct residual split; `dae-optimizing + vacuum` now normalizes through 10000 (`.tmp/pass-fuzz-dae-optimizing-vacuum-context-10000-single-return-no-reduce-20260705`, `10000/10000`, zero failures); `dae-optimizing + inlining-optimizing` mismatches through 10000 (`0/10000` normalized, `10000` mismatches, zero failures) because Starshine retains a private unreachable helper and extra function type that Binaryen deletes, with aggregate Starshine deltas `+100000` raw / `+100000` canonical / `+1100000` WAT bytes and all `50000` input/raw/canonical artifacts validating; adding `remove-unused-module-elements` normalizes through 10000 (`.tmp/pass-fuzz-dae-optimizing-inlining-rume-context-10000-single-return-no-reduce-20260705`, `10000/10000`, zero failures). A direct `dead-argument-elimination + remove-unused-module-elements` replay over saved direct random-all residuals (`.tmp/pass-fuzz-dead-argument-elimination-random-all-residual-rume-context-20260706`) stayed raw-red at `996/996` mismatches with zero failures, so RUME does not close the plain direct residuals; the follow-up `case-007094` raw-positive coverage replay confirms one largest raw-positive sample is runtime-import-blocked and canonical/WAT-smaller after unreachable-tail cleanup, while the 59-case scan now quantifies the subset as raw-positive but all canonical/WAT-negative with no scanned pre-terminal side-effecting keywords in input/Binaryen/Starshine raw WAT. A follow-up source review ties the terminal to `gen_valid_append_core_control_ops()` in the coverage prelude, and a bounded `wasm-tools shrink` attempt on `case-007094` accepted the original raw-positive/canonical-negative shape but found no smaller module in 200 attempts, so this supports (but does not prove) pure-prefix/unreachable-tail cleanup for the raw-positive coverage subset. The broader no-DWARF post-pass suffix `dae-optimizing -> inlining-optimizing -> duplicate-function-elimination -> duplicate-import-elimination -> simplify-globals-optimizing -> remove-unused-module-elements -> string-gathering -> reorder-globals -> directize` also normalizes through 10000 on the optimizing profile (`.tmp/pass-fuzz-dae-optimizing-full-postpass-context-10000-single-return-no-reduce-20260706`, `10000/10000`, zero failures, Binaryen cache `10000/0`, `reduceMismatches=false`). This supports the current scheduling-neighborhood cleanup requirement classification for the optimizing-profile slice. A first non-profile regular-GenValid full-suffix attempt is raw-red: `.tmp/pass-fuzz-dae-optimizing-full-postpass-context-regular-genvalid-10000-20260706` stopped at the mismatch cap after `2015/10000` compared, with `0` normalized, `2015` mismatches, zero validation/generator/property/command failures, and selected `binaryen-oracle-portable=2015`. The no-DAE suffix control `.tmp/pass-fuzz-postpass-context-regular-genvalid-no-dae-control-1000-20260706` also mismatched every case with zero failures, so this is currently broader non-profile post-pass/portable-oracle output-shape drift, not DAE-owned green evidence or a DAE-owned Starshine-win classification. No broad semantic drift is approved here. |

## Validation ladder

Use this ladder when implementing:

1. **Registry honesty**
   - Assert `dead-argument-elimination` and `dae` are module-pass entries.
   - Assert `dae-optimizing` and `dead-argument-elimination-optimizing` are module-pass entries.
   - Keep plain DAE out of `optimize` / `shrink` presets unless a dedicated preset slice says otherwise.
2. **Analyzer-only tests**
   - No output changes.
   - Candidate classifications for private direct calls, exports, imports, `ref.func`, call-reference escape, indirect calls, and tail calls.
3. **Minimal scalar rewrite tests**
   - One unused param, one private callee, one direct caller.
   - Multi-caller direct boundary.
   - Removed actual with side effect preserved.
   - Export/import/reference negatives.
4. **Binaryen oracle comparison**
   - Run focused `wasm-opt --dae -S` comparisons for each supported slice.
   - Normalize only known textual noise; do not normalize away missing side effects or signature differences.
5. **GC/refinement tests**
   - Port the `dae-gc*` families only after local type-section and validator behavior are stable.
6. **Dropped-result tests**
   - Cover all-results-dropped positives, tail-call negatives, and uninhabited-result repair.
7. **Sibling split tests**
   - Keep a fixture where plain `dae` leaves valid cleanup debris that the optimizing sibling would remove.

## Beginner checklist

If you are unsure whether a future local rewrite belongs in plain DAE, ask:

- Does it change a function boundary?
- Can every caller that observes that boundary be found and repaired?
- Are removed operands still evaluated when needed?
- Are tail-call and uninhabited-result rules preserved?
- Is the result still valid before running any cleanup pass?
- Would the expected cleanup only happen under `dae-optimizing` in Binaryen?

If the answer to the last question is yes, do not put that cleanup in plain DAE.

## Sources

- [`../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md`](../../../raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md)
- [`../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md`](../../../raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md)
- [`../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md`](../../../raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md)
- [`./binaryen-strategy.md`](./binaryen-strategy.md)
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md)
- [`./wat-shapes.md`](./wat-shapes.md)
- [`./starshine-strategy.md`](./starshine-strategy.md)
- [`../dae-optimizing/starshine-strategy.md`](../dae-optimizing/starshine-strategy.md)
