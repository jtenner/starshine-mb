---
kind: comparison
status: working
last_reviewed: 2026-06-11
sources:
  - ../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md
  - ../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md
  - ../../../raw/research/0555-2026-05-07-aud001-backlog-split-after-current-head-rerun.md
  - ../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md
  - ../../../raw/research/0240-2026-04-21-ssa-nomerge-starshine-strategy-followup.md
  - ../../../raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md
  - ../../../raw/research/0076-2026-04-10-ssa-nomerge-parity-investigation.md
related:
  - ./index.md
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./merge-shapes-and-canonical-slots.md
  - ./wat-shapes.md
  - ../../../../../src/ir/hot_lift.mbt
  - ../../../../../src/ir/hot_lift_test.mbt
  - ../../../../../src/passes/ssa_nomerge.mbt
  - ../../../../../src/passes/ssa_nomerge_test.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/cmd/cmd_wbtest.mbt
  - ../../../../../scripts/lib/pass-fuzz-compare-task.ts
---

# `ssa-nomerge` Binaryen Parity

This page is the current local signoff tracker.
Use the strategy and shape pages in this folder for the upstream Binaryen algorithm itself; use `[./implementation-structure-and-tests.md](./implementation-structure-and-tests.md)` for owner-file and proof-surface navigation. This page is about the repo's current evidence and remaining gaps.

## Durable Conclusions

- The June 2026 exceptional-edge corruption family is fixed in-tree by making HOT `ssa-nomerge` fail closed on `try_table` / throw-family flow, matching the documented SSA v1 normal-flow-only contract.
- The June 2026 default-local branchy-control drift family is fixed in the raw no-write path: reads of never-written body locals are materialized as explicit type defaults across block/loop/if/try_table bodies without invoking HOT SSA.
- The June 2026 final-lane dropped-unreachable-debris drift family is fixed in the same no-write raw path: `drop (unreachable)` / nested dropped-unreachable debris before a hard `unreachable` is collapsed using the existing shared cleanup helper.
- The April `2026-04-10` dead-param-write parity family is now fixed in-tree for `ssa-nomerge`.
- Current source-mode `ssa-nomerge` replay on the checked-in debug CLI artifact now completes, validates, and matches Binaryen on normalized WAT and canonical per-function output.
- Random compare fuzz is still useful, but it is not sufficient as the only signoff lane for `ssa-nomerge`.
- The 2026-05-07 current-head local-declaration shaping drift family is fixed in-tree: `ssa_destroy` now matches Binaryen temp-local preservation/reuse for unreachable dead `local.tee` value-if carriers without generic lowerer trimming.
- Fresh post-merge reruns on seed `0x51a` stayed clean in both the mixed-generator and `gen-valid` lanes.
- The reduced unreachable compare-carrier slice behind the traced `Func 523` family is now covered in-tree in both lift and pass tests.
- The old traced `Func 523` `writeback-validate:type mismatch` skip is now retired by the focused extracted-function CLI replay test.
- Exact raw-byte parity is still not claimed because Starshine's raw lowering path can still fail closed on artifact-only writeback families, and this follow-up did not rerun a fresh full-artifact traced skip census.

## Current In-Tree Status

- The implementation lives in `[../../../../../src/passes/ssa_nomerge.mbt](../../../../../src/passes/ssa_nomerge.mbt)`.
- The current raw-lowering fixes live in `[../../../../../src/passes/pass_manager.mbt](../../../../../src/passes/pass_manager.mbt)`: dead param writes spill through fresh locals, live straight-line and typed-if param flows stay on the canonical param slot, no-write default-local reads are rewritten through a dedicated recursive default materializer instead of falling back to HOT SSA, no-write dropped-unreachable debris is cleaned before hard `unreachable` tails, concrete `br_on_null` taken edges insert canonical alias copies before exiting over fallthrough overwrites, concrete `br_on_non_null` exits insert canonical alias copies on the non-null taken edge when needed, no-copy `br_on_cast` / `br_on_cast_fail` exits can stay on the raw path, copy-needing non-loop cast exits, including multi-result label targets, lower through scratch source refs plus inner inverse cast-branch blocks so canonical alias copies run only on the taken edge, single-reference-param loop-target `br_on_non_null` backedges use the same void-loop/proxy-value-block lowering as loop-target casts, and copy-needing single-reference-param loop-target cast backedges lower through inverse cast blocks that store the cast payload into the loop scratch and run branch-specific canonical alias copies before the void backedge.
- That rule is an in-tree inference from Binaryen `src/passes/SSAify.cpp` `createNewIndexes()` plus direct `wasm-opt --ssa-nomerge` micro-replays on reduced param-write cases.
- The same pass-manager path still rejects per-function writebacks that fail module-aware validation, in addition to the existing `invalid-escape-carrier` and `suspicious-escape-carrier` families.
- The `cmd` package contains a native debug-artifact replay test in `[../../../../../src/cmd/cmd_wbtest.mbt](../../../../../src/cmd/cmd_wbtest.mbt)`, and a focused `moon test src/cmd --target native --filter 'run_cmd_with_adapter validates ssa-nomerge on debug artifact'` run is currently green.
- The reduced `Func 523` follow-up now also lives in `[../../../../../src/ir/hot_lift.mbt](../../../../../src/ir/hot_lift.mbt)`, `[../../../../../src/ir/hot_lift_test.mbt](../../../../../src/ir/hot_lift_test.mbt)`, `[../../../../../src/passes/ssa_nomerge_test.mbt](../../../../../src/passes/ssa_nomerge_test.mbt)`, and the focused extracted-function CLI replay in `[../../../../../src/cmd/cmd_wbtest.mbt](../../../../../src/cmd/cmd_wbtest.mbt)`.
- The 2026-06-09 audit added focused `try_table` exceptional-exit, branchy default-local-read, dropped-unreachable-debris, nested param-slot, large structured raw coverage, needed-only branch/label copies, canonical body-local writes, and `Func 4302` carrier-narrowing regressions in `[../../../../../src/passes/ssa_nomerge_test.mbt](../../../../../src/passes/ssa_nomerge_test.mbt)`; details and command evidence live in `[../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md](../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md)`.

## Current Signoff State

- 2026-06-11 loop-target `br_on_non_null` follow-up: raw structured `ssa-nomerge` now admits the source-backed typed loop-param non-null branch subset: typed loops with exactly one non-null reference param and no results, direct current-loop `br_on_non_null` backedges, and both no-copy and branch-specific canonical alias-copy cases before the loop header observes carried locals. The existing void-loop/proxy-value-block lowering is reused: no-copy branches exit the inner value block with the non-null payload; copy-needing branches lower through `ref.is_null` / `ref.as_non_null`, copy canonical aliases only on the non-null taken edge, exit the proxy with the payload, store it into the loop scratch, and branch to the outer void loop. Other non-cast typed loop-param backedges, multi-param/result typed loops, nullable/null loop branches, and nested branchy loop bodies remain fail-closed until the raw loop model grows broader loop-param/header SSA placement. Red evidence: focused `*loop-target br_on_non_null*` initially failed `0/2` with HOT lift errors for `br_on_non_null`, while reduced probes in `.tmp/ssa-nomerge-loop-refbranch/` show Binaryen rewriting both no-copy and copy-needing single-param non-null loop-backedge cases. Green evidence: focused `*loop-target br_on_non_null*` passed `2/2`, focused `*br_on_non_null*` passed `4/4`, focused `*loop-target br_on*` passed `6/6`, `ssa_nomerge_test.mbt` passed `142/142`, `moon test src/passes` passed `2161/2161`, `moon fmt`, `moon info` passed with 3 pre-existing GenValid warnings, full `moon test` passed `5421/5421`, native `src/cmd` build passed with pre-existing pass-manager warnings, `git diff --check` passed, and `wasm-tools validate --features all` passed on `.tmp/ssa-nomerge-loop-refbranch/br_on_non_null_loop_param.starshine.after.wasm`, `.tmp/ssa-nomerge-loop-refbranch/br_on_non_null_loop_param_copy.starshine.wasm`, and `.tmp/self-ssa-nomerge-debug-wasi-loop-target-non-null-final/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-loop-target-non-null-10000-final` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-loop-target-non-null-genvalid-normalized-10000-final` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-loop-target-non-null-final` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.620 ms` versus Binaryen `386.829 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 loop-target cast copy follow-up: raw structured `ssa-nomerge` now admits the next narrow typed loop-param cast backedge subset: typed loops with exactly one reference param and no results, direct current-loop `br_on_cast` / `br_on_cast_fail` backedges, and branch-specific canonical alias copies required before the loop header observes carried locals. The lowering keeps the prior void-loop/inner-value-block proxy for no-copy backedges, and copy-needing cast backedges now lower through a scratch source ref plus an inner inverse cast-branch block; the taken edge writes the cast payload into the loop scratch, runs the needed canonical alias copies while the stack is empty, and branches directly to the outer void loop. This avoids common-exit copies, which would be unsound for multiple branch sites with different alias states. Multi-param/result typed loops, non-cast typed-loop backedges, and nested branchy loop bodies remain fail-closed until the raw loop model grows broader loop-param/header SSA placement. Red evidence: after implementation but before updating the old boundary test, focused `*copy-needing loop-target br_on_cast*` failed `0/1` because the old test still expected `changed=false`; reduced WAT probes in `.tmp/ssa-nomerge-loop-cast-copy/` show Binaryen rewrites both copy-needing `br_on_cast` and `br_on_cast_fail` loop-param cases, and current Starshine outputs validate. Green evidence: focused `*copy-needing loop-target br_on_cast*` passed `2/2`, focused `*loop-target br_on_cast*` passed `4/4`, focused `*br_on_cast*` passed `10/10`, `ssa_nomerge_test.mbt` passed `140/140`, `moon test src/passes` passed `2159/2159`, `moon fmt`, `moon info` passed with 3 pre-existing GenValid warnings, full `moon test` passed `5419/5419`, native `src/cmd` build passed with pre-existing pass-manager warnings, `git diff --check` passed, and `wasm-tools validate --features all` passed on `.tmp/ssa-nomerge-loop-cast-copy/br_on_cast_loop_param_copy.starshine.wasm`, `.tmp/ssa-nomerge-loop-cast-copy/br_on_cast_fail_loop_param_copy.starshine.wasm`, and `.tmp/self-ssa-nomerge-debug-wasi-loop-target-cast-copies/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-loop-target-cast-copies-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-loop-target-cast-copies-genvalid-normalized-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-loop-target-cast-copies` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.780 ms` versus Binaryen `394.829 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 loop-target cast reference-branch follow-up: raw structured `ssa-nomerge` now admits the narrow source-backed typed loop-param cast backedge subset where the loop type has exactly one reference param and no results, the loop body branches back only via direct `br_on_cast` / `br_on_cast_fail` to the current loop, and no branch-time canonical alias copy is needed. The lowering rewrites the typed loop as a void loop plus an inner value block: the current loop param is held in a scratch local, the cast branch targets the inner value block with its cast payload, and the payload is written back to the scratch local before the outer void backedge. Copy-needing loop-target cast branches remained fail-closed in that slice until the later loop-target cast copy follow-up; multi-param/result typed loops and nested branchy loop bodies remain fail-closed until the raw loop model grows broader loop-param/header SSA placement. Red evidence: the focused no-copy loop-target `br_on_cast` and `br_on_cast_fail` tests initially failed with `changed=false`, matching reduced probes in `.tmp/ssa-nomerge-loop-multi-cast/` where Binaryen rewrote those typed-loop-param cases and old Starshine left them unchanged. Green evidence: focused `*loop-target br_on_cast*` passed `3/3`, focused `*br_on_cast*` passed `9/9`, `ssa_nomerge_test.mbt` passed `139/139`, `moon test src/passes` passed `2158/2158`, `moon fmt`, `moon info` passed with 3 pre-existing GenValid warnings, full `moon test` passed `5418/5418`, native `src/cmd` build passed with pre-existing pass-manager warnings, `git diff --check` passed, and `wasm-tools validate --features all` passed on reduced loop-target outputs plus `.tmp/self-ssa-nomerge-debug-wasi-loop-target-cast/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-loop-target-cast-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-loop-target-cast-genvalid-normalized-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-loop-target-cast` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.131 ms` versus Binaryen `357.030 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 multi-result cast reference-branch copy follow-up: raw structured `ssa-nomerge` now widens copy-needing non-loop `br_on_cast` and `br_on_cast_fail` exits to multi-result block labels. Prefix label operands are spilled to defaultable scratch locals, the final cast payload is preserved through an inner inverse cast-branch block, canonical alias copies run only on the taken edge, and prefix operands are restored below the final payload on both the taken branch and fallthrough. Source-backed probes in `.tmp/ssa-nomerge-loop-multi-cast/` show Binaryen rewriting multi-result `br_on_cast` / `br_on_cast_fail` exits, while the old Starshine path stayed unchanged (`changed=false`); focused tests failed red under that old behavior and now pass. Loop-target cast branches remained fail-closed in that slice: reduced typed loop-param `br_on_cast` / `br_on_cast_fail` probes show Binaryen rewrites them, but Starshine left them unchanged then because the raw loop contract only admitted void loop labels/backedges and did not model typed loop-param/header SSA copies. Green evidence: focused `*multi-result br_on_cast*` passed `2/2`, focused `*br_on_cast*` passed `6/6`, `ssa_nomerge_test.mbt` passed `136/136`, `moon test src/passes` passed `2155/2155`, `moon fmt`, `moon info` passed with 3 pre-existing GenValid warnings, full `moon test` passed `5415/5415`, native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate --features all` passed on the reduced multi-result Starshine outputs plus `.tmp/self-ssa-nomerge-debug-wasi-multi-result-cast-ref-branches/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-multi-result-cast-ref-branches-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-multi-result-cast-ref-branches-genvalid-normalized-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-multi-result-cast-ref-branches` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.387 ms` versus Binaryen `378.143 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 cast reference-branch copy follow-up: raw structured `ssa-nomerge` now lowers copy-needing non-loop `br_on_cast` and `br_on_cast_fail` exits through a scratch source ref plus an inner inverse cast-branch block. For `br_on_cast`, the inner `br_on_cast_fail` preserves the failure/fallthrough payload while the success path inserts canonical alias copies before branching to the original target; `br_on_cast_fail` uses the symmetric inner `br_on_cast` shape so copies run only on the failure/taken edge. Loop targets and multi-result cast branch targets remained fail-closed in that slice. Red evidence: focused tests for `.tmp/ssa-nomerge-refbranch-audit/br_on_cast_divergent_exit.wat` and `br_on_cast_fail_divergent_exit.wat` failed under the old `changed=false` behavior, while Binaryen preserved the branch-taken divergent alias over the fallthrough overwrite. Green evidence: focused `*br_on_cast*` passed `4/4`, focused `*br_on*` passed `7/7`, `ssa_nomerge_test.mbt` passed `134/134`, `moon test src/passes` passed `2153/2153`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5413/5413`), native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate --features all` passed on reduced Starshine outputs plus `.tmp/self-ssa-nomerge-debug-wasi-cast-ref-branch-copies/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-cast-ref-branch-copies-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-cast-ref-branch-copies-genvalid-normalized-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-cast-ref-branch-copies` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `4.918 ms` versus Binaryen `368.846 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 reference-branch widening follow-up: raw structured `ssa-nomerge` now lowers copy-needing non-loop `br_on_non_null` exits through a scratch reference plus `ref.is_null` / `ref.as_non_null`, inserting canonical alias copies only on the non-null taken edge before branching with the preserved non-null payload. The same slice admits source-backed no-copy non-loop `br_on_cast` and `br_on_cast_fail` exits on the raw path so Binaryen-observed dead-write freshening is preserved instead of falling through the HOT path unchanged; copy-needing cast exits and loop targets remained fail-closed in that slice. Red evidence: `.tmp/ssa-nomerge-refbranch-audit/br_on_non_null_divergent_exit.wat` showed Binaryen preserving the taken non-null divergent alias over a fallthrough overwrite while old Starshine failed closed; `.tmp/ssa-nomerge-refbranch-audit/br_on_cast_dead_noread.wat` and `br_on_cast_fail_dead_noread.wat` showed Binaryen freshening dead local writes around cast reference branches while old Starshine left the functions unchanged. Green evidence: focused `*br_on_non_null*` passed `2/2`, focused `*br_on_cast*` passed `2/2`, focused `*br_on*` passed `5/5`, `ssa_nomerge_test.mbt` passed `132/132`, `moon test src/passes` passed `2151/2151`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5411/5411`), native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate` passed on the new reduced Starshine outputs and `.tmp/self-ssa-nomerge-debug-wasi-ref-branches/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-ref-branches-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); normalized GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-ref-branches-genvalid-normalized-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-ref-branches` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.151 ms` versus Binaryen `357.178 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 no-copy concrete `br_on_non_null` follow-up: raw structured `ssa-nomerge` now preserves non-loop `br_on_non_null` exits when no canonical alias copy is needed at the branch. This lets source-backed reference-branch functions with dead local writes use the raw structured rewrite instead of falling through an unsupported HOT lift/no-op path, while copy-needing `br_on_non_null` and `br_on_cast`/`br_on_cast_fail` remain fail-closed. Red evidence: `.tmp/ssa-nomerge-refbranch-audit/br_on_non_null_dead.wat` showed Binaryen freshening the dead local write around a `br_on_non_null` result block, while old Starshine left the function unchanged after `hot-lift-error`; the focused AST regression failed before implementation with that lift error. Green evidence: focused `*br_on_non_null*` passed, `ssa_nomerge_test.mbt` passed `129/129`, `moon test src/passes` passed `2148/2148`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5408/5408`), native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate` passed on `.tmp/ssa-nomerge-refbranch-audit/br_on_non_null_dead.starshine.after.wasm` and `.tmp/self-ssa-nomerge-debug-wasi-br-on-non-null/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-br-on-non-null-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-br-on-non-null-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-br-on-non-null` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.145 ms` versus Binaryen `343.754 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 concrete `br_on_null` taken-edge copy follow-up: raw structured `ssa-nomerge` now treats concrete `br_on_null` as a conditional label exit instead of a fallthrough-only instruction. When a taken null edge exits a block over a fallthrough overwrite after divergent aliases, Starshine lowers the branch through `ref.is_null` / `ref.as_non_null`, inserts the needed canonical alias copies only on the taken edge, and preserves the non-null fallthrough reference for the following `drop`. Unsupported `br_on_null` result/copy shapes, copy-needing `br_on_non_null`, and `br_on_cast`/`br_on_cast_fail` remain fail-closed from the raw structured path. Source-backed red evidence: `.tmp/ssa-nomerge-refbranch-audit/br_on_null_divergent_exit.wat` showed Binaryen preserving the branch-taken `7`/`8` value while old Starshine copied divergent aliases to a temp and let the null exit read the default canonical local; the focused AST regression failed before implementation because no taken-edge copy lowering was present. Green evidence: focused `*br_on_null*` passed, `ssa_nomerge_test.mbt` passed `128/128`, `moon test src/passes` passed `2147/2147`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5407/5407`), native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate` passed on `.tmp/ssa-nomerge-refbranch-audit/br_on_null_divergent_exit.starshine.after.wasm` and `.tmp/self-ssa-nomerge-debug-wasi-br-on-null/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-br-on-null-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-br-on-null-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-br-on-null` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.124 ms` versus Binaryen `346.501 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 no-copy loop-target `br_if` follow-up: raw structured `ssa-nomerge` now admits the source-backed void loop-target `br_if` subset when no branch-time canonical alias copy is required. This preserves Binaryen-observed dead-write freshening for no-copy conditional backedges and keeps loop-carried read-before-write locals on canonical slots without falling through HOT SSA. Copy-needing conditional loop backedges, typed loop-param/result shapes, and result-carrying loop targets remain fail-closed. Red evidence: the no-copy loop-target `br_if` fixture previously fell through HOT, emitted `pass[ssa-nomerge]:mutated`, and dropped the dead local write that Binaryen freshens; the loop-carried `br_if` fixture previously used HOT temp/copy lowering instead of the Binaryen-observed canonical-local shape. Green evidence: focused `*br_if backedge*` and `*loop-target br_if*` passed individually, `ssa_nomerge_test.mbt` passed `127/127`, `moon test src/passes` passed `2146/2146`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5406/5406`), native `src/cmd` build passed with pre-existing pass-manager warnings, `wasm-tools validate` passed on `.tmp/self-ssa-nomerge-debug-wasi-loop-brif/starshine.wasm`, and `git diff --check` passed. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-loop-brif-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-loop-brif-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-loop-brif` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.087 ms` versus Binaryen `347.543 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 void loop-backedge follow-up: raw structured `ssa-nomerge` now admits void `loop` bodies and void loop-target `br` / `br_table` backedges, while typed loop-param/result shapes and loop-target `br_if` cases that need branch-time alias copies remained fail-closed at that slice. Header read-before-write facts are used both to keep loop-carried locals canonical and to avoid materializing first-iteration defaults as constants when a later backedge can observe the canonical local; dead loop-local writes with no loop-carried read can still freshen to Binaryen-style temporaries. Red evidence: the no-copy loop-target `br_table` fixture previously went through HOT, emitted `pass[ssa-nomerge]:mutated`, and dropped the dead local write that Binaryen freshens; the loop-carried read-before-write `br_table` fixture previously hit the HOT SSA phi-alignment abort instead of completing. Green evidence: focused `*loop*br_table*` passed `2/2`, `ssa_nomerge_test.mbt` passed `126/126`, `moon test src/passes` passed `2145/2145`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5405/5405`), and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-loop-backedge-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures (`22` `binaryen-rec-group-zero`, `1` `binaryen-bad-section-size`); GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-loop-backedge-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-loop-backedge` validates but still differs at `defined=0 abs=27`, with pass-local Starshine `3.288 ms` versus Binaryen `348.756 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 no-throw `try_table` follow-up: raw structured `ssa-nomerge` now treats a `try_table` body with no throw-family instructions and no calls/return-calls as normal structured control, preserving Binaryen-observed local-write rewriting while keeping real exceptional edges fail-closed. Red evidence: the focused no-throw `try_table` fixture failed under the old fallback with only the original local / no local-write freshening while Binaryen `wasm-opt --all-features --ssa-nomerge` freshened the same reduced shape. Green evidence: focused `*no-throw try_table*` passed `1/1`, focused `*exceptional*` fail-closed tests passed `2/2`, `ssa_nomerge_test.mbt` passed `124/124`, `moon test src/passes` passed `2143/2143`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5403/5403`), and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-no-throw-trytable-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-no-throw-trytable-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-no-throw-trytable` still differs at `defined=0 abs=27`, with pass-local Starshine `16.298 ms` versus Binaryen `344.507 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 result-typed `br_table` copy follow-up: raw structured `ssa-nomerge` now preserves non-loop `br_table` exits even when the table targets carry block results. The branch-local alias copies inserted before dispatch are stack-neutral, so branch payload values and the selector remain below the copy sequence and the original `br_table` stays valid; loop targets still fail closed. Red evidence: the focused result-typed `br_table` fixture failed before implementation with `changed=false` / no raw structured rewrite, while Binaryen `wasm-opt --all-features --ssa-nomerge` rewrote the same reduced source-backed shape. Green evidence: focused `*result br_table*` passed `1/1`, `ssa_nomerge_test.mbt` passed `123/123`, `moon test src/passes` passed `2142/2142`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5402/5402`), native `src/cmd` build passed with pre-existing pass-manager warnings, and `wasm-tools validate` passed on `.tmp/self-ssa-nomerge-debug-wasi-result-brtable/starshine.wasm`. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-result-brtable-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity `.tmp/pass-fuzz-ssa-nomerge-parity-result-brtable-10000` compared `10000/10000` with `10000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-result-brtable` still differs at `defined=0 abs=27`, with pass-local Starshine `16.233 ms` versus Binaryen `349.014 ms`; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 GenValid copy-family widening: `src/validate/gen_valid_ssa.mbt` now records and emits two source-backed SSA coverage facts from the latest pass behavior: `ssa-enclosing-label-copy-before-exit` and `ssa-void-br-table-canonical-copies`. Coverage/stress include both; smoke includes the quick enclosing-label copy signal but keeps `br_table` copy dispatch out; parity remains conservative A–E only and explicitly excludes both labels. Red evidence: focused GenValid tests failed before the new fields existed. Green evidence: `gen_valid_ssa_tests.mbt` passed `35/35`, focused `gen_valid_tests.mbt` `*ssa-nomerge*` passed `5/5`, `moon test src/validate` passed `1600/1600`, `moon test src/fuzz` passed `627/627`, `moon test src/passes` passed `2141/2141`, `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5401/5401`), and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: direct mixed-generator `.tmp/pass-fuzz-ssa-nomerge-genvalid-widening-direct-10000-final` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; parity GenValid `.tmp/pass-fuzz-ssa-nomerge-parity-new-copy-facts-genvalid-normalized-1000-final` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches. Coverage `.tmp/pass-fuzz-ssa-nomerge-coverage-new-copy-facts-20-final`, stress `.tmp/pass-fuzz-ssa-nomerge-stress-new-copy-facts-20-final`, and smoke `.tmp/pass-fuzz-ssa-nomerge-smoke-enclosing-label-copy-20-final` each compared `20/20` with `0` validation/generator/command failures and expected dirty coverage-lane mismatches. Agent classification: coverage/smoke/stress mismatches are representation/skip-artifact drift from dense or structured smoke templates, not parity lanes and not validation/generator/tool failures. This is generator/profile/template widening only; exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 enclosing-label copy / void `br_table` copy follow-up: raw structured `ssa-nomerge` now treats enclosing non-loop label read-before-write needs as observable when divergent `if` arms merge before a later branch can exit over a fallthrough overwrite. The same slice extends raw `br_table` preservation from no-copy-only to void-target `br_table` exits by inserting the union of needed canonical alias copies before the table dispatch; result-typed `br_table` targets were still rejected by this slice and were handled by the later result-typed `br_table` follow-up, while loop targets remain fail-closed. Red/green evidence: focused `*enclosing label copies*` and `*canonical merge copies*` fixtures failed under the old merge/copy behavior (`3` locals instead of the required merged temp/canonical copy shape) and then passed. Validation: `ssa_nomerge_test.mbt` passed `122/122`; `moon test src/passes` passed `2141/2141`; `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5400/5400`), native `src/cmd` build passed with pre-existing pass-manager warnings, `wasm-tools validate` passed on `.tmp/self-ssa-nomerge-debug-wasi-enclosing-label-brtable-copies/starshine.wasm`, and `git diff --check` passed. Compare evidence: direct mixed-generator lane `.tmp/pass-fuzz-ssa-nomerge-enclosing-label-brtable-copies-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity lane `.tmp/pass-fuzz-ssa-nomerge-parity-enclosing-label-brtable-copies-genvalid-normalized-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-enclosing-label-brtable-copies` still differs at `defined=0 abs=27`, with pass-local Starshine `16.568 ms` versus Binaryen `345.979 ms` and Starshine size `3150187` versus Binaryen `3155990`, so exact artifact and O4z closeout remain unclaimed.

- 2026-06-11 high-threshold structured / no-copy `br_table` follow-up: raw structured `ssa-nomerge` now preserves no-copy `br_table` exits on the raw path when every table target is a non-loop label and no observable alias merge copy is required. This keeps the Binaryen-shaped fresh-temp local write for a reduced no-copy `br_table` fixture instead of falling back to HOT SSA and dropping the dead local write. The guarded direct raw structured instruction budgets are now `8192` for both branch-bearing and no-branch structured functions, admitting two more sampled debug-artifact skips (`Func 502`, `7132` instructions / `827` locals; `Func 5418`, `7925` / `894`) without widening the `1024` branchy local-count guard. Red/green evidence: the focused `*br_table*` fixture failed under the old HOT fallback and then passed; the focused `*high-threshold*` boundary fixtures failed under the old `5376` budgets and then passed. Validation: `ssa_nomerge_test.mbt` passed `120/120`; `moon test src/passes` passed `2139/2139`; `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5398/5398`), and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: direct mixed-generator lane `.tmp/pass-fuzz-ssa-nomerge-brtable-8192-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity lane `.tmp/pass-fuzz-ssa-nomerge-parity-brtable-8192-genvalid-normalized-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches. Full debug-artifact trace `.tmp/ssa-nomerge-structured-8192-brtable-trace` validates and reduces `large-structured-local-writes` from `11` to `9` with `0` `skip-invalid-lower`; direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-brtable-8192` still differs at `defined=0 abs=27`, with pass-local Starshine `15.972 ms` versus Binaryen `343.315 ms` and Starshine size `3150187` versus Binaryen `3155990`, so exact artifact and O4z closeout remain unclaimed.
- 2026-06-11 mid-threshold structured budget follow-up: the guarded direct raw structured `ssa-nomerge` instruction budgets are now `5376` for both branch-bearing and no-branch structured functions. This admits four additional sampled debug-artifact skips (`Func 2114`, `4900` instructions / `679` locals; `Func 3139`, `5101` / `789`; `Func 95`, `5183` / `822`; and no-branch `Func 3554`, `5282` / `705`) without widening the `1024` branchy local-count guard. Red/green synthetic boundary coverage in `ssa_nomerge_test.mbt` locks `5373`-instruction branchy and `5374`-instruction no-branch fixtures onto the raw structured path while keeping `5377` / `5378` fixtures classified as `large-structured-local-writes`. Evidence: focused test was red under the prior budgets, then `ssa_nomerge_test.mbt` passed `116/116`; `moon test src/passes` passed `2135/2135`; `moon fmt`, `moon info`, full `moon test` (`5393/5393`), and native `src/cmd` build passed with pre-existing warnings; sampled extracted outputs validate when rooted with `--extract-functions=<func> --strip-debug`; full debug-artifact trace `.tmp/ssa-nomerge-structured-5376-trace` validates and reduces `large-structured-local-writes` from `15` to `11` with `0` `skip-invalid-lower`; direct compare `.tmp/pass-fuzz-ssa-nomerge-structured-5376-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; `ssa-nomerge-parity` GenValid `.tmp/pass-fuzz-ssa-nomerge-parity-structured-5376-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-structured-5376` still differs at `defined=0 abs=27`; inspection of the saved function WAT classifies this first diff as local-allocation representation drift, where Starshine reuses fewer body locals while Binaryen preserves/adds separate temp locals for the same observable allocator-style flow. This is not exact artifact or O4z closeout.
- 2026-06-11 near-threshold structured-branch budget follow-up: the guarded direct raw structured `ssa-nomerge` branch budget is now `4608` instructions for branch-bearing functions, covering the two sampled threshold-only debug-artifact functions (`Func 3668`, `4509` instructions / `620` locals, and `Func 1265`, `4563` / `614`) without widening the local-count guard. A red/green focused synthetic threshold fixture in `ssa_nomerge_test.mbt` locks `4605`-instruction branchy functions onto the raw structured path and keeps `4609`-instruction functions classified as `large-structured-local-writes`. Evidence: focused test was red at `4096`, then `ssa_nomerge_test.mbt` passed `114/114`; `moon test src/passes` passed `2133/2133`; native `src/cmd` build passed with pre-existing pass-manager warnings; direct compare `.tmp/pass-fuzz-ssa-nomerge-structured-4608-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; `ssa-nomerge-parity` GenValid `.tmp/pass-fuzz-ssa-nomerge-parity-structured-4608-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches. Debug-artifact trace `.tmp/ssa-nomerge-structured-4608-trace` validates and reduces `large-structured-local-writes` from `17` to `15` with `0` `skip-invalid-lower`; direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-structured-4608` still differs at `defined=0 abs=27`, so this is a budget narrowing slice, not exact artifact closeout.
- 2026-06-11 deeper GenValid SSA coverage: dense `ssa-nomerge-coverage` / `ssa-nomerge-stress` now emit typed loop-param/backedge and `try_table`/`throw_ref` exceptional fail-closed specimens; feature-guided batch tests can require the full dense SSA fact set. Validation for the generator slice: focused GenValid SSA/profile tests passed, `moon test src/validate` passed `1598/1598`, `moon test src/fuzz` passed `627/627`, `moon test src/passes` passed `2132/2132`, full `moon test` passed `5390/5390`, `moon info` completed with pre-existing warnings, and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: `ssa-nomerge-parity` deeper 1000-case lane `.tmp/pass-fuzz-ssa-nomerge-parity-deeper-coverage-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches; `ssa-nomerge-smoke` `.tmp/pass-fuzz-ssa-nomerge-smoke-deeper-coverage-100` compared `100/100` with `100` expected representation mismatches; coverage/stress fail-closed-required signals `.tmp/pass-fuzz-ssa-nomerge-coverage-failclosed-deeper-20` and `.tmp/pass-fuzz-ssa-nomerge-stress-failclosed-deeper-20` each compared `20/20`, had `0` validation/generator/command failures, and stayed dirty (`20` mismatches) as coverage-lane artifacts, not parity lanes.
- 2026-06-11 local-tee and no-copy `br_if` narrowing: raw structured `ssa-nomerge` now keeps body-local `local.tee` writes on the canonical slot when a branch continuation or result branch reads the local before any overwrite, and preserves no-copy `br_if` terminators instead of lowering them to `if { br }` plus scratch locals when no alias merge copy is required. Red/green evidence: the focused `*tees canonical*` tests failed under the previous `local.tee` heuristic and then passed `2/2`; the focused `*no-copy*br_if*` tests failed under unconditional `br_if` lowering and then passed `2/2`. Validation: focused `ssa_nomerge_test.mbt` passed `119/119`; `moon test src/passes` passed `2138/2138`; `moon fmt`, `moon info` (3 pre-existing GenValid warnings), full `moon test` (`5397/5397`), and native `src/cmd` build passed with pre-existing pass-manager warnings. Compare evidence: direct mixed-generator lane `.tmp/pass-fuzz-ssa-nomerge-tee-brif-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; GenValid parity lane `.tmp/pass-fuzz-ssa-nomerge-parity-tee-brif-genvalid-normalized-1000` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches; GenValid smoke lane `.tmp/pass-fuzz-ssa-nomerge-smoke-tee-brif-genvalid-normalized-100` stayed intentionally dirty (`100/100` mismatches, no command failures) as a structured-allocation coverage artifact. Direct artifact replay `.tmp/self-ssa-nomerge-debug-wasi-tee-brif` still differs at `defined=0 abs=27`, with pass-local Starshine `23.089 ms` versus Binaryen `354.292 ms`, so exact artifact and O4z closeout remain unclaimed.
- 2026-06-11 raw structured GenValid family widening: `src/validate/gen_valid_ssa.mbt` now records four additional source-backed SSA facts/templates: `ssa-default-local-read-after-branchy-control` for branchy no-write default-local materialization, `ssa-dropped-unreachable-debris-before-hard-unreachable` for raw no-write unreachable-debris cleanup, `ssa-canonical-body-local-next-read` for canonical body-local writes whose next observable use is a read, and `ssa-immediate-block-exit-canonical-set-br` for immediate `local.set; br` block exits. Dense `ssa-nomerge-coverage` / `ssa-nomerge-stress` include all four; `ssa-nomerge-smoke` includes the three quick structured allocation signals and excludes dropped-unreachable debris; `ssa-nomerge-parity` remains A–E only. Validation: focused GenValid SSA tests passed `34/34`; `moon test src/validate` passed `1599/1599`; `moon test src/fuzz` passed `627/627`; native `src/cmd` build passed with pre-existing warnings. Compare evidence: parity profile `.tmp/pass-fuzz-ssa-nomerge-parity-genvalid-new-families-1000-final` compared `1000/1000` with `1000` compare-normalized matches and `0` mismatches; coverage `.tmp/pass-fuzz-ssa-nomerge-coverage-new-families-20-final` and smoke `.tmp/pass-fuzz-ssa-nomerge-smoke-new-families-20-final` required the new labels, compared `20/20`, had `0` validation/generator/command failures, and stayed dirty (`20` mismatches) as expected coverage-lane artifacts. This widens generated coverage only and is not an exact artifact or `ssa-nomerge` closeout claim.
- 2026-06-09 dropped-local-increment guard removal: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` now asserts HOT-path rewrite and reaching-local preservation for dropped `i32.add(local.get, i32.const 1)` debris alongside reversed/non-one boundary validation; the prior whole-function fail-closed guard was overconservative on reduced fixtures.
- 2026-06-10 scalar nested `drop(if (result t) ...)` support: single-result `i32`/`i64`/`f32`/`f64` shapes with straight-line arms, both arms present, and no local writes in the condition; `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` and `moon test --package jtenner/starshine/ir --file ssa_local_test.mbt` / `cfg_test.mbt` add positive fixtures per scalar type plus multi-value/missing-else boundary regressions; targeted GenValid `ssa-nomerge-parity` lane stayed green at `1000/1000` (`0x5eed`, `.tmp/pass-fuzz-ssa-nomerge-parity-after-scalar-drop-if-1000`).
- 2026-06-10 arm value-block nested `drop(if (result t) ...)` support: each then/else arm may be straight-line or a single `block (result t)` root with branch-free local traffic matching the parent `if` result type; branch/`br_if`/nested control inside arm blocks stays fail-closed; positive fixtures A–F and boundary G–K in `ssa_nomerge_test.mbt` / `ssa_local_test.mbt` / `cfg_test.mbt`; targeted GenValid `ssa-nomerge-parity` lane stayed green at `1000/1000` (`0x5eed`, `.tmp/pass-fuzz-ssa-nomerge-parity-after-arm-block-1000`).
- 2026-06-10 mixed arm-root nested `drop(if (result t) ...)` support: each then/else arm may be zero or more void straight-line prefix roots followed by exactly one simple `block (result t)` root matching the parent `if` result type; value-producing prefix roots, roots after the final value block, two value blocks, and nested control inside the final block stay fail-closed; positive fixtures A–F and boundary G–L in `ssa_nomerge_test.mbt` / `ssa_local_test.mbt` / `cfg_test.mbt`; targeted GenValid `ssa-nomerge-parity` lane stayed green at `1000/1000` (`0x5eed`, `.tmp/pass-fuzz-ssa-nomerge-parity-after-mixed-arm-block-1000`).
- 2026-06-11 immediate block-exit copy narrowing: raw structured `ssa-nomerge` now writes a `local.set` directly to the canonical label-merge slot when the very next instruction is an unconditional `br` to a non-loop block whose continuation needs that local. This removes the redundant fresh-temp plus canonical copy for the focused block-target `br` smoke shape without changing the guarded `br_if` lowering contract. Evidence: red/green focused fixture in `ssa_nomerge_test.mbt`; `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `113/113`; `moon test src/passes` passed `2132/2132`; `moon fmt` and native `src/cmd` build passed with pre-existing pass-manager warnings; direct mixed-generator compare `.tmp/pass-fuzz-ssa-nomerge-branch-copy-10000` requested `10000`, compared `9977`, had `9977` normalized matches, `0` mismatches, and `23` Binaryen/tool command failures; `ssa-nomerge-smoke` 100-case compare at `.tmp/pass-fuzz-ssa-nomerge-smoke-branch-copy-100` still has `100/100` representation mismatches and no validation/generator/command failures; paired `ssa-nomerge-parity` 100-case compare at `.tmp/pass-fuzz-ssa-nomerge-parity-branch-copy-100` stayed green with `100` compare-normalized matches and `0` mismatches.
- 2026-06-10 post-`bcf4b9199` audit (`HEAD` at `bcf4b9199`, GenValid nested result-if coverage templates O–W): `moon fmt` completed; `moon test --package jtenner/starshine/validate --file gen_valid_ssa_tests.mbt` passed `29/29`; `moon test src/validate` passed `1592/1592`; `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `111/111`; `moon test src/ir` passed `269/269`; `moon test src/passes` passed `2130/2130`; `moon test src/fuzz` passed `626/626`; `bun test scripts/lib/pass-fuzz-compare-task.test.ts` passed `27/27`.
- 2026-06-10 post-`bcf4b9199` minimum compare pair: `bun scripts/pass-fuzz-compare.ts --generator gen-valid --count 10000 --min-compared 10000 --seed 0x5eed --keep-going-after-command-failures --pass ssa-nomerge --out-dir .tmp/pass-fuzz-ssa-nomerge-genvalid-10000-post-bcf4 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe` compared `10000/10000`, with `10000` normalized matches, `0` mismatches, and `0` validation/generator/command failures.
- 2026-06-10 post-`bcf4b9199` nested result-if coverage smoke (not parity signoff): `bun scripts/pass-fuzz-compare.ts --generator gen-valid --gen-valid-profile ssa-nomerge-coverage --require-feature ssa-nested-result-if-supported-shape --count 100 --seed 0x5eed --pass ssa-nomerge --normalize ssa-local-allocation-debris --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-ssa-nomerge-nested-result-if-supported-100-post-bcf4 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures` compared `100/100` with `100` normalized mismatches and `0` command failures. All `100` cases had both Starshine and Binaryen raw outputs differ from input; every persisted `failure-metadata.json` also recorded `ssa-nested-cfg-fail-closed-shape` on the dense combined body. **Classification:** coverage-lane representation drift across the dense template (local allocation / combined-slice output shape), not an SSA semantic failure, validation failure, or Binaryen/tool oracle gap.
- 2026-06-10 post-`bcf4b9199` direct debug-artifact replay: `moon run src/cmd --target native -- --debug-serial-passes --tracing pass --ssa-nomerge --out .tmp/ssa-nomerge-post-bcf4-trace/starshine.wasm tests/node/dist/starshine-debug-wasi.wasm` exited `0`; `wasm-tools validate` succeeded on `.tmp/ssa-nomerge-post-bcf4-trace/starshine.wasm` and `.tmp/ssa-nomerge-debug-post-bcf4.wasm`.
- 2026-06-10 post-`bcf4b9199` skip census (`.tmp/ssa-nomerge-post-bcf4-trace/stderr.txt`) is **unchanged** from the June 2026 documented baseline: `2524` `structured-local-writes`, `1077` `no-local-writes`, `1020` `straight-line-local-writes`, `17` `large-structured-local-writes`, and `0` `skip-invalid-lower` lines. No `Func 523` or `Func 4302` suspicious skips reappeared. Largest remaining skips include `Func 3781` (`39283` instrs / `6634` locals), `Func 3885` (`26350` / `3879`), and `Func 5417` (`19507` / `3524`).
- 2026-06-10 post-`bcf4b9199` self-opt artifact replay: `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --out-dir .tmp/self-ssa-nomerge-debug-wasi-post-bcf4 --starshine-bin target/native/release/build/cmd/cmd.exe --ssa-nomerge` completed and validates, but still reports `Normalized WAT equal: no` and `Canonical function compare equal: no` (`defined=0 abs=27` first diff). Starshine pass-local time was `41.347 ms` versus Binaryen `382.718 ms`.
- 2026-06-09 nested CFG-sensitive control fail-closed hardening: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `46/46`, adding no-HOT-mutation regressions for nested value-control local traffic, branch/terminator-sensitive nested regions, and local preservation around nested result regions.
- 2026-06-09 typed-loop-param fail-closed hardening: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `42/42`, adding pipeline-trace no-HOT-mutation assertions for typed loop-param control, locals written before typed loops, backedge-vs-body-local separation, hot-path revision checks for the validated param-only typed-loop `br` repro, and tightening the existing typed-loop induction fixture. `moon test src/ir` passed `251/251` and `moon test src/passes` passed `2061/2061` after the follow-up.
- 2026-06-09 exceptional-edge fail-closed hardening: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` adds a pipeline-trace no-HOT-mutation assertion, default-local catch continuation, conditional throw, `rethrow`, and `delegate` regressions on top of the original `try_table` write-before-throw case.
- 2026-06-09 focused pass test after the exceptional-edge/default-local/debris fixes: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` passed `28/28`.
- 2026-06-09 artifact param-slot, large-structured, and needed-copy/canonical-local follow-ups: `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` now passes `33/33`, `moon test --package jtenner/starshine/passes` passes `2052/2052`, and the earlier full `moon test` passed `5247/5247` after adding nested param-write, large structured raw coverage, reduced `Func 4302` carrier, unneeded-copy, and canonical body-local regressions.
- 2026-06-09 SSA package coverage before the artifact param-slot follow-up: `moon test --package jtenner/starshine/ir` passed `251/251`, `moon test --package jtenner/starshine/passes` passed `2047/2047`, and full `moon test` passed `5245/5245`.
- 2026-06-09 direct compare: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass ssa-nomerge --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-ssa-nomerge-audit-after-10000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` mismatches, no validation/property/generator failures, and `23` Binaryen/tool command failures.
- Initial final lane before the dropped-unreachable-debris follow-up: `.tmp/pass-fuzz-ssa-nomerge-final-100000` compared `99751/100000`, normalized `99748`, with `3` agent-classified semantic-safe unreachable-debris mismatches and `249` Binaryen/tool command failures.
- Current final lane after the debris follow-up: `bun scripts/pass-fuzz-compare.ts --count 100000 --seed 0x5eed --pass ssa-nomerge --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-ssa-nomerge-final-100000-after-debris --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures` compared `99751/100000`, with `99751` normalized matches, `0` mismatches, no validation/property/generator failures, and `249` Binaryen/tool command failures (`219` `binaryen-rec-group-zero`, `12` `binaryen-bad-section-size`, `11` `binaryen-command-failed`, `6` `binaryen-table-index-out-of-range`, `1` `binaryen-invalid-tag-index`).
- 2026-06-09 large-structured / `Func 4302` follow-up direct compare: `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --pass ssa-nomerge --normalize local-cleanup-debris --out-dir .tmp/pass-fuzz-ssa-nomerge-high-threshold-narrow-ssa-10000 --jobs auto --starshine-bin target/native/release/build/cmd/cmd.exe --max-failures 2000 --keep-going-after-command-failures` compared `9977/10000`, with `9977` normalized matches, `0` mismatches, no validation/property/generator failures, and `23` Binaryen/tool command failures. The later needed-copy/canonical-local compare `.tmp/pass-fuzz-ssa-nomerge-canonical-local-10000` had the same compare/normalization counts; the earlier param-slot compare `.tmp/pass-fuzz-ssa-nomerge-param-canonical-10000` also had the same counts.
- `moon info` still reproduces the known Moon DB panic; `moon fmt` completed.
- `wasm-tools validate tests/node/dist/starshine-debug-wasi.wasm` succeeds.
- `moon test --package jtenner/starshine/ir --file ssa_destroy_test.mbt` is green with focused temp-local declaration and unreachable dead-tee regressions.
- `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt` is green with focused dead-param, live-param, unreachable-carrier, and value-if temp-local regressions.
- `bun scripts/pass-fuzz-compare.ts --generator gen-valid --count 500 --min-compared 500 --seed 0x5eed --keep-going-after-command-failures --pass ssa-nomerge --out-dir .tmp/recheck-ssa-nomerge-genvalid-500-if-order-fix` is green at `500 / 500` compared, `500` normalized matches, and `0` mismatches.
- `bun scripts/pass-fuzz-compare.ts --generator gen-valid --count 10000 --min-compared 10000 --seed 0x5eed --keep-going-after-command-failures --pass ssa-nomerge --out-dir .tmp/pass-fuzz-ssa-nomerge-genvalid-10000-if-order-fix-rerun` is green at `10000 / 10000` compared, `10000` normalized matches, `0` mismatches, and `0` validation/generator/command failures.
- Historical `bun scripts/pass-fuzz-compare.ts --generator gen-valid --count 10000 --min-compared 10000 --seed 0x5eed --max-failures 5 --out-dir /tmp/ssa-pass-fuzz-rebased-2026-04-10-signoff-gen-valid --pass ssa-nomerge` was green at `10000 / 10000` compared and `10000` normalized matches.
- `bun scripts/pass-fuzz-compare.ts --count 10000 --seed 0x5eed --max-failures 5 --out-dir /tmp/ssa-pass-fuzz-rebased-2026-04-10-signoff --pass ssa-nomerge` stays mismatch-free on comparable cases (`2380 / 10000` compared, `0` mismatches) and only stops on the Binaryen-only `binaryen-rec-group-zero` parser family.
- `bun scripts/pass-fuzz-compare.ts --count 2000 --seed 0x51a --max-failures 5 --out-dir /tmp/ssa-pass-fuzz-postcommit-mixed-seed51a --pass ssa-nomerge` stayed clean on all comparable cases (`1996 / 2000`, `0` mismatches, `4` Binaryen-only parser gaps).
- `bun scripts/pass-fuzz-compare.ts --generator gen-valid --count 10000 --min-compared 10000 --seed 0x51a --max-failures 5 --out-dir /tmp/ssa-pass-fuzz-postcommit-genvalid-seed51a --pass ssa-nomerge` is also green at `10000 / 10000` compared and `10000` normalized matches.
- `bun scripts/self-optimize-compare.ts tests/node/dist/starshine-debug-wasi.wasm --out-dir .tmp/self-ssa-nomerge-debug-wasi-canonical-local --starshine-bin target/native/release/build/cmd/cmd.exe --ssa-nomerge` completes and validates, but still reports `Normalized WAT equal: no` and `Canonical function compare equal: no` (`defined=0 abs=27` first diff). Starshine pass-local time was `26.731 ms` versus Binaryen `392.500 ms`, and final direct output size is now size-winning for Starshine (`3150432` bytes versus Binaryen `3155990`). `bun validate self-opt-smoke --wasm .tmp/self-ssa-nomerge-debug-wasi-canonical-local/starshine.wasm --limit 1` validated the artifact and completed with `0` failures.
- `moon test --package jtenner/starshine/ir --file hot_lift_test.mbt -F '*wired to typed block results*'` is green for the reduced unreachable compare-carrier lift slice.
- `moon test --package jtenner/starshine/passes --file ssa_nomerge_test.mbt -F '*func 523 shape*'` is green for the reduced pass-level `Func 523` follow-up.
- `moon run src/cmd --target native -- --debug-serial-passes --tracing pass --ssa-nomerge --out /tmp/ssa-nomerge-func523-followup.wasm tests/node/dist/starshine-debug-wasi.wasm` still exits zero, and `wasm-tools validate /tmp/ssa-nomerge-func523-followup.wasm` still succeeds.

## Coverage / Investigation Lanes

### 2026-06-09 join-allocation investigation (case `000001`)

Reduced block-join replay (`.tmp/ssa-block-join-minimal/`) proves **semantic-equivalent join allocation drift** when both tools actually run `ssa-nomerge`: Starshine’s raw structured path keeps canonical slots (`$x`/`$x`/`$x`) while Binaryen fresh-allocates per set (`$2`/`$3`/`$3`). Both outputs validate and observe the same post-join value.

The dense GenValid `ssa-nomerge` profile smoke lane (`0/100` normalized) is **not** primarily a normalizer gap. For case `000001`, `starshine.raw.wasm` is unchanged from input (`330` bytes, HOT `changed=false`, raw structured rewrite `None`), while Binaryen applies `--ssa-nomerge` (`271` bytes). The harness compares canonicalized **untransformed** Starshine output against Binaryen’s transformed output because the dense template combines fail-closed / raw-skip shapes with rewrite-expected slices in one function (`ssa_nested_cfg_fail_closed_shape`, dropped-increment guard shapes, and combined structured density).

**Classification:** coverage-lane artifact for the dense profile; `semantic-equivalent-join-allocation-drift` only for isolated fixtures where both sides transform.

**Normalizer:** opt-in `--normalize ssa-local-allocation-debris` stays narrow (straight-line islands and immediate pre-`if` carriers only). Do not broaden it across control-flow joins without proof and negative tests.

**Signoff baseline:** mixed-generator `100000` lane with `--normalize local-cleanup-debris` (`99751/99751` normalized matches) under **Current Signoff State** above.

### GenValid profile split (2026-06-09)

| Profile | Purpose | Templates included | Templates excluded |
|---|---|---|---|
| `ssa-nomerge-coverage` (`ssa-nomerge` alias) | Dense SSA body templates, all `ssa-*` coverage floors, fail-closed stress shapes, supported nested `drop(if (result t) ...)` family | straight-line multi-def, tee, param write, if-join, default merge, loop backedge, typed loop-param/backedge fail-closed, `try_table`/`throw_ref` exceptional fail-closed, block join, br_table, parallel-copy (+ cycle), unreachable carrier, dropped increment guarded/reversed, dropped-unreachable debris before hard unreachable, nested-CFG fail-closed, nested result-if straight-line/scalar/condition/arm/mixed templates, structured early-return, block `br`/`br_if`, structured-param merge, branch-local-not-read-later, default-local read after branchy control, canonical body-local next-read, immediate `local.set; br` block exit, enclosing-label copy-before-exit, void br_table canonical copies | — |
| `ssa-nomerge-smoke` | Quick coverage-signal profile for smoke/regression runs that should hit structured SSA branch shapes without dense stress slices | straight-line multi-def, tee, param write (when i32 param), if-join phi, one-arm default merge, structured early-return, block `br`/`br_if`, structured-param merge, branch-local-not-read-later, default-local read after branchy control, canonical body-local next-read, immediate `local.set; br` block exit, enclosing-label copy-before-exit | loop backedge, br_table (including void br_table copy dispatch), parallel-copy, unreachable carrier, dropped increment guarded/reversed, dropped-unreachable debris before hard unreachable, nested-CFG / typed-loop / exceptional fail-closed, dense nested result-if condition/value-block/scalar variants |
| `ssa-nomerge-parity` | Green compare candidate: rewrite shapes where both tools transform without fail-closed skip or structured branch allocation drift | straight-line multi-def, tee, param write (when i32 param), if-join phi, one-arm default merge | loop backedge, block join, br_table, parallel-copy, unreachable carrier, dropped increment guarded/reversed, dropped-unreachable debris, nested-CFG / typed-loop / exceptional fail-closed, nested result-if coverage templates, structured branch smoke shapes, default-local branchy-control, canonical-next-read, immediate set/br, enclosing-label copy-before-exit, and void br_table copy structured slices |
| `ssa-nomerge-stress` | Reserved pathological/fail-closed lane (currently aliases dense coverage templates) | same as coverage (including typed-loop-param, exceptional-flow, and nested result-if templates) | — |

Use `--gen-valid-profile ssa-nomerge-parity` with parity-appropriate `--require-feature` floors for targeted green GenValid parity-profile compare lanes. Use `--gen-valid-profile ssa-nomerge-smoke` when a quick smoke should hit structured branch SSA coverage and tolerate current local-allocation representation drift. Keep `--gen-valid-profile ssa-nomerge-coverage` (or legacy `ssa-nomerge`) for dense scanner/floor coverage only. The broad mixed-generator `100000` lane with `--normalize local-cleanup-debris` remains the primary signoff baseline; a green parity-profile lane is strong targeted evidence, not a replacement for that baseline.

2026-06-09 parity-profile compare (trimmed templates A–E only, `--normalize ssa-local-allocation-debris --normalize local-cleanup-debris`):

- `100/100` compared, `100` compare-normalized matches, `0` mismatches (`.tmp/pass-fuzz-ssa-nomerge-parity-profile-100-v2`)
- `1000/1000` compared, `1000` compare-normalized matches, `0` mismatches (`.tmp/pass-fuzz-ssa-nomerge-parity-profile-1000`)
- `10000/10000` compared, `10000` compare-normalized matches, `0` mismatches (`.tmp/pass-fuzz-ssa-nomerge-parity-profile-10000`)

These lanes are **targeted GenValid SSA parity-profile signoff** for the trimmed template set. They do not replace the broad mixed-generator `99751/100000` signoff baseline above.

The earlier dense-profile smoke (`ssa-nomerge-coverage` with loop/block/br_table/parallel-copy in one function) and the quick `ssa-nomerge-smoke` structured-branch lane remain coverage-lane artifacts, not parity-profile compare lanes, until the compare normalizer or pass output converges for structured control-flow local allocation. Current 2026-06-11 smoke evidence: `.tmp/pass-fuzz-ssa-nomerge-genvalid-smoke-profile-100` requested and compared `100/100`, with `0` validation/generator/command failures and `100` expected local-allocation representation mismatches. A later branch-copy narrowing run `.tmp/pass-fuzz-ssa-nomerge-smoke-branch-copy-100` still compares `100/100` with `100` representation mismatches and no validation/generator/command failures; case `000001` writes the unconditional block-exit `local.set; br` path directly to the canonical merge slot. The local-tee/no-copy-`br_if` slice preserves no-copy `br_if` in the focused fixtures and keeps the paired GenValid parity lane green (`.tmp/pass-fuzz-ssa-nomerge-parity-tee-brif-genvalid-normalized-1000`, `1000` compare-normalized matches, `0` mismatches), while `ssa-nomerge-smoke` remains dirty (`.tmp/pass-fuzz-ssa-nomerge-smoke-tee-brif-genvalid-normalized-100`, `100/100` mismatches) because broader structured allocation drift remains. The paired green parity checks `.tmp/pass-fuzz-ssa-nomerge-parity-profile-100-post-smoke` and `.tmp/pass-fuzz-ssa-nomerge-parity-branch-copy-100` each compared `100/100` with `100` compare-normalized matches and `0` mismatches.

2026-06-11 deeper GenValid SSA coverage adds positive coverage/stress specimens for the previously scanner-only fail-closed facts: `ssa-typed-loop-param-fail-closed-shape` now comes from a valid typed loop-param/backedge template, and `ssa-exceptional-fail-closed-shape` now comes from a valid `try_table` + `throw_ref` template with local writes. These remain coverage/stress-only and are intentionally excluded from `ssa-nomerge-parity` and `ssa-nomerge-smoke`; they prove generator/scanner/floor coverage, not pass closeout.

2026-06-11 raw structured GenValid widening adds four more facts from the recent pass-behavior slices: `ssa-default-local-read-after-branchy-control`, `ssa-dropped-unreachable-debris-before-hard-unreachable`, `ssa-canonical-body-local-next-read`, and `ssa-immediate-block-exit-canonical-set-br`. Coverage/stress include all four; smoke includes the three quick structured allocation signals and intentionally excludes dropped-unreachable debris; parity remains the conservative A–E set. A later same-day copy-family widening adds `ssa-enclosing-label-copy-before-exit` and `ssa-void-br-table-canonical-copies`; coverage/stress include both, smoke includes only the quick enclosing-label copy signal, and parity still excludes both. The coverage/stress/smoke required-feature compare lanes are intentionally dirty but validate and generate without command failures; the parity profile stayed green at `1000/1000` in `.tmp/pass-fuzz-ssa-nomerge-parity-new-copy-facts-genvalid-normalized-1000-final`.

2026-06-10 nested result-if GenValid coverage (templates O–W in `gen_valid_ssa.mbt`, feature facts `ssa-nested-result-if-*`):

- New GenValid facts distinguish supported `drop(if (result t) ...)` shapes from `ssa-nested-cfg-fail-closed-shape` (`drop(block (result t) ...)` with local writes).
- `ssa-nested-result-if-supported-shape` means the scanner matched the supported `ssa_simple_drop_result_if_allowed(...)` family: stack-boundary condition-window detection (not whole-function prefix), straight-line or single `block (result i32)` conditions, straight-line / single value-block / void-prefix + final value-block arms, both arms present, and scalar `i32`/`i64`/`f32`/`f64` results only. The scanner is conservative: false negatives are acceptable; false positives are not.
- Templates are wired into `ssa-nomerge-coverage` and `ssa-nomerge-stress` only; `ssa-nomerge-parity` stays on the conservative A–E template set.
- Targeted `--require-feature ssa-nested-result-if-supported-shape` lanes on the dense coverage profile are **coverage smoke**, not parity signoff: they combine rewrite-expected nested result-if slices with fail-closed / join-allocation drift shapes in one function body. A dirty 100-case lane is expected when allocation drift and fail-closed shapes share the same dense body. Post-`bcf4b9199` smoke at `.tmp/pass-fuzz-ssa-nomerge-nested-result-if-supported-100-post-bcf4` reproduced the expected dirty lane (`100/100` mismatches, `0` command failures) with both tools transforming every case.

### 2026-06-10 large-structured skip extraction (`Func 3668` / `Func 1265`)

Post-bcf4 trace census (`.tmp/ssa-nomerge-post-bcf4-trace/stderr.txt`) had `17` `large-structured-local-writes` skips and `0` `skip-invalid-lower` lines. The smallest skip by scanned instruction count was **`Func 3668`** (`instrs=4509`, `locals=620`); the next-smallest named target was **`Func 1265`** (`instrs=4563`, `locals=614`).

**Classification:** raw structured-path **instruction-count threshold only**. In `run_hot_pipeline_raw_ssa_nomerge` (`pass_manager.mbt`), branch-bearing structured functions skipped when `instr_count > structured_branch_raw_instr_limit` (`4096`) or when `base_local_count > structured_branch_raw_local_limit` (`1024`). Both `Func 3668` and `Func 1265` exceeded the instruction limit only (`4509` / `4563` > `4096`; locals stay well under `1024`). This was not `skip-invalid-lower`, not nested-CFG HOT fail-closed, not typed-loop-param control, and not exceptional flow.

The 2026-06-11 threshold slices raised the branch-bearing structured raw instruction budget to `4608` first, then both the branch-bearing and no-branch structured instruction budgets to `5376`, after synthetic red/green boundary coverage and extracted-artifact validation for sampled functions. Fresh full-artifact trace `.tmp/ssa-nomerge-structured-8192-brtable-trace/stderr.txt` now has `9` `large-structured-local-writes` skips and `0` `skip-invalid-lower` lines. The 2026-06-11 8192-budget follow-up additionally admitted `Func 502` (`7132` / `827`) and `Func 5418` (`7925` / `894`) after red/green synthetic boundary coverage, direct compare, and full-artifact trace validation. Remaining threshold/local-count owners are `Func 265` (`9664` / `1175`), `Func 3518` (`8965` / `1547`), `Func 3536` (`16195` / `2871`), `Func 3781` (`39283` / `6634`), `Func 3885` (`26350` / `3879`), `Func 4119` (`9458` / `1332`), `Func 4522` (`12067` / `976`), `Func 5417` (`19507` / `3524`), and `Func 5419` (`15300` / `2313`).

**Extraction replay:** `--extract-functions=3668` roots a large dependency closure (`.tmp/ssa-nomerge-large-structured-func3668/extracted.wasm`, validates). The rooted function replays as `Func 24` with the same `instrs=4509 locals=620` payload and `large-structured-local-writes` trace. Starshine leaves that function unchanged on the raw fast path; Binaryen `--ssa-nomerge` transforms the extracted module. Self-opt compare on the extracted module validates both sides but is not normalized/canonical equal (`defined=1 abs=25` first diff).

**Reduced fixture:** a programmatic WAT fixture with one `block ... br 0` prefix plus `1030` `if/else` pairs crosses the same threshold (`instrs=4125`, `large-structured-local-writes`); `1020` pairs stay on `structured-local-writes` (`instrs=4085`). Regression tests in `ssa_nomerge_test.mbt` lock that boundary without embedding the 1.5 MB extracted artifact.

**Next slice:** keep the 11 remaining artifact functions in backlog until measured raw-structured rewrite cost/validation evidence exists; do not raise the structured raw instruction/local budgets from a single extracted replay or without preserving direct compare and artifact validation evidence.

## Remaining Gap

- Dropped-local-increment debris no longer triggers a whole-function HOT fail-closed skip. HOT SSA rewrite preserves reaching locals for the exact `drop(i32.add(local.get, i32.const 1))` shape and `local.tee` variants on reduced HOT-path fixtures; reversed operands and non-`1` constants remain validated boundary shapes. Broader integer widths remain unclaimed until tested.
- A tiny nested-CFG subset is now supported on the HOT path: straight-line `drop(block (result t) ...)` value blocks with only local get/set/tee, constants, and simple numeric ops (`ssa_simple_drop_value_block_allowed(...)` plus inline rename in `ssa_local_visit_node`), and straight-line `drop(if (result t) ...)` with a single scalar numeric result (`i32`/`i64`/`f32`/`f64`), both arms present, and a straight-line terminator-free condition with local get/set/tee and simple numeric traffic, or one simple `block (result i32)` condition with the same straight-line local traffic (`ssa_simple_condition_value_block_allowed(...)` / `ssa_simple_result_if_condition_allowed(...)` / `ssa_simple_drop_result_if_allowed(...)` plus CFG-backed rename/use-def inline visits with per-root segment isolation and join phis on the post-`drop` continuation). Condition-local writes are allowed only because CFG keeps branch-free condition blocks in the predecessor block that dominates both arms and rename/use-def visit that block before the arm regions; branches and nested control in the condition stay fail-closed. The dropped result value is not SSA-modeled; arms still need CFG/SSA for local traffic. `v128`, reference/GC, and multi-value result-if remain unsupported. `ssa_nomerge_has_nested_cfg_sensitive_control(...)` still fail-closes all other nested value-control shapes.
- Nested CFG-sensitive behavior remains fail-closed for HOT SSA mutation beyond the supported subsets and is regression-covered: nested result-typed `if` inside dropped value blocks, nested `br` / `br_if` / `loop` / nested `if` inside supported operand shapes, missing-else or multi-value operand `if`, and broader nested control must not produce `pass[ssa-nomerge]:mutated` on the hot path and must leave the hot-path revision unchanged. Root-level value blocks and structured control bodies whose children are region-root slots remain outside this guard and may still rewrite through HOT SSA or the raw no-write path.
- Future work requires nested-region-aware liveness/use-def for unsupported shapes, exact local read discovery inside arbitrary nested value control, rename/destruction for nested `if`/loop/branch shapes, and lowering tests that prove local copies are inserted only at valid stack-neutral sites — not Binaryen parity claims for full nested CFG-sensitive SSA.
- Void loop backedges now have a narrow raw structured subset: void `loop` bodies and void loop-target `br` / `br_table` backedges can rewrite without entering HOT SSA, using loop-header read-before-write facts to preserve loop-carried canonical locals and to freshen only dead loop-local writes. This subset covers the source-backed no-copy loop-target `br_table` and loop-carried read-before-write `br_table` regressions; no-copy loop-target `br_if` is also admitted by the later conditional-backedge slice. Copy-needing conditional loop backedges, result-carrying loop targets, and broader loop-param ABI remain unsupported.
- Typed loop-param control has narrow raw structured exceptions for direct current-loop `br_on_cast` / `br_on_cast_fail` backedges when the loop type has exactly one reference param and no results, plus direct current-loop `br_on_non_null` backedges when the loop type has exactly one non-null reference param and no results. Both no-copy and copy-needing branch-specific alias-copy cases are now covered by the void-loop/proxy lowering for those families. Other typed loop-param control remains explicitly unsupported and intentionally fail-closed. HOT represents typed loop params as `BlockResultResolved` param prefixes on `HotOp::Loop` children and as branch operands on backedges, with `HotLabelInfo.branch_arity` recording the payload count. SSA v1's local overlay models only `LocalGet` / `LocalSet` / `LocalTee` reaching definitions; it does not treat loop-param stack flow as local defs, and destruction only inserts predecessor copies for overlay phis on normal CFG edges — not wasm branch operands that re-enter typed loop headers.
- `ssa-nomerge` is safe because typed-loop-param HOT mutation still skips via `ssa_nomerge_has_loop_param_control(...)`; the raw loop-target exceptions are separate and limited to direct single-reference-param cast/non-null backedges with stack-neutral or edge-local copy placement before the void backedge.
- Broader typed-loop-param behavior is fail-closed for HOT SSA mutation and regression-covered: pipeline traces must not contain `pass[ssa-nomerge]:mutated` for unsupported typed-loop-param fixtures, locals written before the loop stay preserved, backedge-carried param values stay separate from unrelated body locals, and the hot-path revision check stays unchanged for the validated param-only typed-loop `br` repro. A dedicated `br_if`-to-typed-loop WAT fixture is still deferred because the attempted operand+condition shapes fail module validation in the pass-test harness even though unconditional `br` repros lift and validate.
- Future work requires loop-param-aware liveness, use-def facts for branch operands, phi placement at typed loop headers, rename semantics that distinguish entry vs backedge param values, destruction copies before loop backedge terminators, lowering that preserves typed loop branch operands, and a validated `br_if` regression once the harness accepts that shape — not broad Binaryen parity claims for typed-loop SSA.
- Exceptional-edge SSA remains explicitly unimplemented and intentionally deferred. The CFG already records `ExceptionalEdge` successors for `try` / `try_table` headers and throw-family terminators, but SSA v1 liveness, dominators, phi placement, dominator-stack rename, and predecessor-copy destruction all follow the normal-flow-only contract.
- Catch continuations are not dominated by throwing blocks.
- The current rename algorithm cannot propagate reaching definitions across exceptional edges without a dedicated exceptional SSA mode.
- `ssa-nomerge` is safe because it skips exceptional-flow HOT mutation via `ssa_nomerge_has_exceptional_flow(...)`, not because the local SSA overlay models exceptional successors.
- Exceptional-flow behavior is fail-closed for HOT SSA mutation and regression-covered: `try_table` write-before-throw, default-local catch continuation, conditional throw with a preceding local write, legacy `rethrow` / `delegate` shapes, and a pipeline trace that must not contain `pass[ssa-nomerge]:mutated`. Some existing raw/no-write normalization may still preserve semantics, but `ssa-nomerge` must not enter HOT local-SSA rewriting for exceptional-flow functions. These tests prove preserved local traffic where writes exist; they do not enable exceptional SSA rewriting.
- Full exceptional SSA remains deferred. Future work requires exceptional-inclusive liveness, catch-block phi placement, exceptional phi inputs, rename semantics for catch blocks, SSA destruction copies before throw/rethrow/delegate terminators, and shape-by-shape guard narrowing with rewrite tests — not Binaryen parity claims for exceptional SSA.
- The O4z public preset still intentionally no-ops `ssa-nomerge`; the direct-pass corruption and debris fixes do not by themselves justify re-enabling the O4z slot.
- The 2026-06-09 direct final lane is normalized-green at `99751/100000` with `0` mismatches, and the param-slot / needed-copy follow-ups stayed green at `9977/10000`; artifact exact parity and O4z neighborhood replay remain open.
- The direct `[SSA]001` temp-local declaration drift is closed by focused tests plus the current `10000 / 10000` `gen-valid` lane.
- The focused extracted-function CLI replay now proves that the old `Func 523` `writeback-validate:type mismatch` skip is retired.
- A fresh direct traced skip census after the large-structured / `Func 4302` follow-up is available in `.tmp/ssa-nomerge-high-threshold-narrow-ssa-trace`, and the post-`bcf4b9199` rerun in `.tmp/ssa-nomerge-post-bcf4-trace` had raw fast/no-op skip families `2524` `structured-local-writes`, `1077` `no-local-writes`, `1020` `straight-line-local-writes`, and `17` `large-structured-local-writes`; there were `0` `skip-invalid-lower` lines. The 2026-06-11 `4608`-instruction budget slice updated the large census to `15`; the later `5376` structured budget slice updated it to `11`, and the high-threshold / no-copy `br_table` follow-up updates it to `9` `large-structured-local-writes` with still `0` `skip-invalid-lower` in `.tmp/ssa-nomerge-structured-8192-brtable-trace`; remaining large skips carry exact payloads such as `func=(Func 3781) instrs=39283 locals=6634`. The older `2026-04-11` count of `228` suspicious skips and the param-slot follow-up count of `1` `Func 4302` suspicious skip are both superseded.
- The same input still succeeds under Binaryen with `wasm-opt tests/node/dist/starshine-debug-wasi.wasm --all-features --ssa-nomerge`.
- So the current output-facing blocker for the reduced `Func 523` family is fixed, the `Func 4302` false-positive suspicious skip is narrowed away, most large-structured direct no-ops are now covered, and the high-threshold raw-structured artifact size regression is fixed by needed-only copies/canonical body-local reuse. Direct artifact replay remains mandatory because exact normalized/canonical function parity is not green and the remaining `9` huge structured functions are still skipped.

## Practical Rule

- Keep direct debug-artifact replay in the `ssa-nomerge` signoff loop.
- Treat the seeded `binaryen-rec-group-zero` wasm-smith failure as an oracle parser gap, not an SSA semantic mismatch.
- Treat current-source behavior parity as green for the fixed dead-param family, the reduced unreachable compare-carrier follow-up, the exceptional-edge fail-closed guard, the raw no-write default-local rewrite, the raw no-write dropped-unreachable-debris cleanup, and the raw structured needed-copy/canonical-local cleanup, but do not claim exact artifact parity until a fresh traced full-artifact replay is normalized/canonical-function equal or the remaining representation drift is intentionally accepted.
- Keep the direct artifact replay and the `10000 / 10000` `gen-valid` compare lane together as the minimum signoff pair; the current `0x5eed` `gen-valid` lane is green in `.tmp/pass-fuzz-ssa-nomerge-genvalid-10000-post-bcf4` (supersedes `.tmp/pass-fuzz-ssa-nomerge-genvalid-10000-if-order-fix-rerun`).

## Sources

- 2026-06-09 exceptional-edge/default-local audit: `[../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md](../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md)`
- Implementation/test-map refresh: `[../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md](../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md)`
- Research note for this refresh: `[../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md](../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md)`
- Archived research doc: `[../../../raw/research/0076-2026-04-10-ssa-nomerge-parity-investigation.md](../../../raw/research/0076-2026-04-10-ssa-nomerge-parity-investigation.md)`
- Pass implementation: `[../../../../../src/passes/ssa_nomerge.mbt](../../../../../src/passes/ssa_nomerge.mbt)`
- Pass manager guard: `[../../../../../src/passes/pass_manager.mbt](../../../../../src/passes/pass_manager.mbt)`
- CLI artifact test surface: `[../../../../../src/cmd/cmd_wbtest.mbt](../../../../../src/cmd/cmd_wbtest.mbt)`
- Random compare harness: `[../../../../../scripts/lib/pass-fuzz-compare-task.ts](../../../../../scripts/lib/pass-fuzz-compare-task.ts)`

