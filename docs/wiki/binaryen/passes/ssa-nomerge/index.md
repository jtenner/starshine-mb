---
kind: entity
status: supported
last_reviewed: 2026-06-14
sources:
  - ../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md
  - ../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md
  - ../../../raw/research/0555-2026-05-07-aud001-backlog-split-after-current-head-rerun.md
  - ../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md
  - ../../release-horizon-and-oracles.md
  - ../../../raw/research/0240-2026-04-21-ssa-nomerge-starshine-strategy-followup.md
  - ../../../raw/binaryen/2026-04-21-ssa-nomerge-primary-sources.md
  - ../../../raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md
  - ../../../../../src/passes/ssa_nomerge.mbt
  - ../../../../../src/passes/ssa_nomerge_test.mbt
  - ../../../../../src/passes/pass_manager.mbt
  - ../../../../../src/passes/optimize.mbt
  - ../../../../../agent-todo.md
  - ../../../raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/SSAify.cpp
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/local-graph.h
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/LocalGraph.cpp
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/ReFinalize.cpp
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/pass.cpp
  - https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/passes.h
  - https://github.com/WebAssembly/binaryen/blob/version_129/test/passes/ssa-nomerge_enable-simd.wast
  - https://github.com/WebAssembly/binaryen/blob/version_129/test/passes/ssa-nomerge_enable-simd.txt
  - https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/ssa.wast
  - https://github.com/WebAssembly/binaryen/blob/main/src/passes/SSAify.cpp
  - https://github.com/WebAssembly/binaryen/blob/main/src/ir/local-graph.h
  - https://github.com/WebAssembly/binaryen/blob/main/src/ir/LocalGraph.cpp
  - https://github.com/WebAssembly/binaryen/blob/main/src/ir/ReFinalize.cpp
  - https://github.com/WebAssembly/binaryen/blob/main/test/passes/ssa-nomerge_enable-simd.wast
  - https://github.com/WebAssembly/binaryen/blob/main/test/passes/ssa-nomerge_enable-simd.txt
related:
  - ./binaryen-strategy.md
  - ./implementation-structure-and-tests.md
  - ./starshine-hot-ir-strategy.md
  - ./merge-shapes-and-canonical-slots.md
  - ./wat-shapes.md
  - ./parity.md
  - ../../release-horizon-and-oracles.md
  - ../tracker.md
  - ../../no-dwarf-default-optimize-path.md
  - ../dead-code-elimination/index.md
  - ../coalesce-locals/index.md
---

# `ssa-nomerge`

## Role

- `ssa-nomerge` is an active implemented **hot pass** in Starshine.
- In upstream Binaryen `version_129`, `pass.cpp` describes it as:
  - ssa-ify variables so that they have a single assignment, ignoring merges

That description is correct, but it hides the most important part.

A better beginner summary is:

- Binaryen builds a whole-function LocalGraph of which local writes can reach which local reads,
- gives fresh local indexes only to writes whose later reads stay single-source,
- rewrites those reads to the fresh locals or explicit default values,
- and deliberately leaves merge points on the canonical original slot instead of inventing phi locals.

So this is **not** full SSA construction, but it is also **not** just straight-line renaming.

## Why this pass matters

- Earlier pass-wiki work had already made `ssa-nomerge` a strong upstream-facing dossier, but this run still needed to close the missing **Starshine strategy/code-map** gap so the folder matched the surrounding implemented-pass schema.
- In the canonical no-DWARF `-O` / `-Os` scheduler it is the first function pass:
  - `... -> global-struct-inference -> ssa-nomerge -> dead-code-elimination -> remove-unused-names -> ...`
- That placement is meaningful.
  - Binaryen wants simpler local traffic before the first cleanup wave.
  - The source comment in `pass.cpp` says untangling to semi-SSA form is helpful, but merges are ignored to avoid introducing new copies.
- In the saved generated-artifact `-O4z` audit, it appears at top-level slot `8` and already has strong semantic parity evidence:
  - exact wasm equal: `no`
  - normalized WAT equal: `yes`
  - Starshine wall/runtime: `10299.164 ms`
  - Binaryen wall/runtime: `496.466 ms`
  - Starshine in-pass time: `1341.831 ms`
  - Binaryen in-pass time: `286.388 ms`
  - input validates: `yes`
  - output validates: `yes`
- The saved `-O4z` debug log also shows repeated nested reruns of `ssa-nomerge`, not just the top-level slot.
- The 2026-06-09 audit found and fixed a true semantic corruption family: HOT `ssa-nomerge` ignored exceptional edges, so a `try_table` body local write followed by `throw` to a catch target could be dropped while a later `local.get` read the default value. Starshine now skips exceptional-flow HOT mutation for this pass until local SSA grows exceptional-edge semantics. `[SSANM-007a1]` / `[SSANM-007a2a]` refreshed that boundary on 2026-06-14: real throwing `try_table` catch and catch-all exits preserve their local writes and `throw` but stay off ordinary structured LocalGraph trace reasons. `[SSANM-007a2b]` / `[SSANM-007a2c]` then locked direct HOT `throw_ref`, legacy catch/rethrow, and delegate terminators as no-mutation boundaries and closed the throwing EH fail-closed subtrack. `[SSANM-007a3]` classifies retained no-throw `try_table` support as normal-flow work only when the body has no throw/call-bearing edge: ordinary local writes use the existing structured raw path, while scalar proxy and store-model rewrites remain typed-control helper ownership rather than real EH SSA support.
- The same audit fixed non-corrupting direct-compare drift in no-write functions: default body-local reads after branchy control now materialize explicit type defaults, and dropped-unreachable debris before a hard `unreachable` is cleaned in the raw path.
- Follow-ups on the debug-artifact direct trace reduced nested structured param/body-local shape drift, removed unneeded branch/label merge copies, removed the one current `Func 4302` false-positive suspicious-carrier skip, and expanded raw structured coverage for moderate large functions. The 2026-06-14 `[SSANM-008a]` refresh now tracks six remaining `large-structured-local-writes` functions for huge-function classification and replay work (`Func 3536`, `Func 3781`, `Func 3885`, `Func 4119`, `Func 5417`, and `Func 5419`); old anchors `Func 265`, `Func 3518`, and `Func 4522` drifted to non-huge boundary/no-op routes.
- `agent-todo.md` has a dedicated `[O4Z-AUDIT-SSA]` / `SSANM-*` backlog for the reopened audit. The direct explicit pass has strong normalized compare evidence; `[SSANM-003a]` / `[SSANM-003b]` now make the raw straight-line `local.set` and `local.tee` subsets consume the LocalGraph rewrite plan; `[SSANM-004a]` uses planned default-entry get materialization for straight-line defaults plus a narrow normal structured-control subset; `[SSANM-004b]` removes the duplicated no-local-write default-only rewriter by routing no-write defaults through the same LocalGraph materializer while preserving no-write unreachable-debris cleanup; `[SSANM-005a]` preserves simple missing-else one-arm body-local/default and parameter-entry merges through a canonical-only LocalGraph plan path; `[SSANM-005b1]` preserves simple both-arm and block/`br_if` multi-source body-local merges without fresh merge locals or predecessor copies; `[SSANM-005b2]` preserves simple void-loop direct `br_if 0` backedge merges through the same canonical-only LocalGraph plan before the legacy loop-carried HOT defer; `[SSANM-005c1]` / `[SSANM-005c2]` classify and implement a narrow mixed fresh/canonical normal `if`/`local.set` path where non-merge writes freshen while merge-feeding writes and merge reads stay canonical; `[SSANM-005c3a]` admits small mixed `local.tee` write sets through the same planned structured rewrite while preserving tee stack results; `[SSANM-005c3b]` / `[SSANM-005c3c]` classify mixed `br_if` early exits plus nested normal `block` / `if` as supported while keeping plain `br`, `br_table`, and loops fail-closed; `[SSANM-005b3]` separately classifies plain `br` / `br_table` multi-source merge regions as fail-closed from the canonical-only LocalGraph path until branch/table helpers are narrowed; and `[SSANM-006a2a]` adds ordinary normal `block` / `if` `local.set` freshening through `structured-localgraph-plan` for single-source no-merge traffic while leaving branches, returns, loops, EH, typed-control, `local.tee`, and trap boundaries on existing owners. `[SSANM-006a2b]` adds explicit merge-adjacent ordinary `local.set` coverage: freshenable writes before/after a canonical merge region use planned fresh locals while merge-feeding writes and merge reads stay on the original local. `[SSANM-006a2c]` narrows the remaining legacy `structured-local-writes` / `structured-local-writes-mutated` summaries to branch/table, typed-control, EH, huge-function, and explicit fail-closed owners. `[SSANM-006a3a]` records branch-exit helper ownership; `[SSANM-006a3b]` locks ordinary plain `br` plus non-mixed/non-backedge `br_if` local-source regions as fail-closed from planned structured LocalGraph paths; `[SSANM-006a3c]` does the same for ordinary no-value `br_table` local-source exits; `[SSANM-006a3d]` closes nested normal branch-target classification by keeping nested outer-target and loop-exit `br_if` shapes off planned LocalGraph reasons unless later branch/loop owners prove a narrower safe subset; `[SSANM-006b2a]` censuses the raw-dispatch paths that can still fall through to HOT SSA destruction, identifying non-simple loop-carried branch shapes and legacy structured-helper rejection paths as the remaining fallback-risk candidates; `[SSANM-006b2b]` adds public-pipeline trace locks proving straight-line, no-write, default-entry, and unreachable-debris families keep using named raw/no-write exits instead of lifted HOT fallback; `[SSANM-006b2c1]` / `[SSANM-006b2c2]` reroute branch-free decorated void-loop `br_if 0` canonical merge backedges to `structured-loop-backedge-merge-localgraph-plan` instead of lifted HOT predecessor-copy destruction; `[SSANM-006b2c3]` / `[SSANM-006b2c4]` close the retained structured fallback boundary by returning branch/table decorated loop-backedge survivors unchanged with `structured-loop-backedge-boundary-noop`, keeping invalid lowered carriers classified as lowering/writeback safety boundaries, and unblocking predecessor-copy wording/helper retirement; `[SSANM-006b2d]` classifies branch-alias, `br_table`, typed-control, ref/cast branch, and EH scratch/proxy helpers as explicit boundary owners rather than ordinary predecessor-copy work; `[SSANM-007a2]` locks the real throwing EH category fail-closed through direct/conditional throwing `try_table`, `throw_ref`, legacy catch/rethrow, and delegate evidence; `[SSANM-007a3]` classifies no-throw `try_table` local-write and typed-control helper subsets and adds a call-bearing fail-closed contrast, closing parent `[SSANM-007a]`; and `[SSANM-007b1]` inventories typed-control loop/result, branch-table, ref/null, and cast-branch raw helpers as ABI repair surfaces rather than ordinary LocalGraph local-source mutation. `[SSANM-007b2]` now adds trace/output locks for scalar/reference loop-param proxy helpers, no-copy typed-loop no-mutation boundaries, multi-param/single-result store-model helpers, typed-loop `br_table` helpers, and nested loop-target gates, keeping those scratches/proxies/stores classified as typed-control ABI repair rather than ordinary no-merge predecessor copies. `[SSANM-007b3a]` / `[SSANM-007b3b]` now lock value-carrying `br`, `br_if`, direct `br_table`, and mixed-target `br_table` operands as branch/table ABI boundaries that validate and preserve branch opcodes while staying off ordinary planned structured LocalGraph reasons. `[SSANM-007b3c]` now applies the same trace/validation boundary proof to representative no-copy, copy-needing/prefix, non-null raw-path, and local-tee tested-producer `br_on_null` / `br_on_non_null` exits; `[SSANM-007b3d]` applies it to no-copy, divergent-alias, multi-result, multi-param, no-copy loop-target, and copy-needing loop-target `br_on_cast` / `br_on_cast_fail` exits, closing the typed branch subtrack. `[SSANM-006b3]` closes predecessor-copy retirement by inventorying the retained HOT fallback, shared `ssa_destroy` helpers, raw branch/table/typed/EH helper surfaces, and stale summary/wiki wording; the public summary now says `Freshen single-source locals and materialize defaults while preserving merge traffic.` and a registry test locks that wording. `[SSANM-008a]` refreshes the huge-function artifact anchors: direct trace `.tmp/ssa-nomerge-huge-anchor-refresh-20260614` validates with six current huge guards and self-compare `.tmp/self-ssa-nomerge-huge-anchor-refresh-20260614` now first-diffs at `defined=108 abs=135`. `[SSANM-008b]` then classifies those six huge guards as size-threshold boundaries with no merge-feeding canonical writes or merge reads, while the three drifted old anchors remain non-huge branch/value-carrier boundaries. `[SSANM-009a]` refreshes the debug-WASI self-compare anchor to the same `defined=108 abs=135` first diff. `[SSANM-009b1]` fixes that first diff as a true straight-line stack-carried `local.tee` / default-read behavior-parity gap: the raw straight-line rewriter now keeps fresh aliases so a later read of a freshly teed stack value is not replaced by a body-local default. `[SSANM-009b2]` then fixes the `defined=215 abs=242` structured branch/value-carrier safety gap by rejecting legacy raw structured rewrites that read appended fresh locals with no write. `[SSANM-009b3]` fixes the next `defined=363 abs=390` structured branch-carrier safety gap by rejecting plain-branch rewrites that drop all writes to a pre-existing local while retaining reads of that local. `[SSANM-009b4]` then classifies/fixes `defined=501 abs=528` as a size-losing / parity-risky loop-carried plain-branch boundary by returning large structured functions unchanged with `loop-carried-plain-br-boundary-noop` instead of accepting legacy branch/else copy materialization; `[SSANM-009b5]` fixes/classifies the next `defined=538 abs=565` diff as the same boundary in the medium `dae_scan_local_use_ignoring_instrs` scanner family by lowering the fail-closed threshold to source-lowered scanner-sized functions (`base_local_count > 64`); `[SSANM-009b6]` fixes/classifies `defined=651 abs=678` as a size-losing / parity-risky typed result-loop plain-`br` carrier boundary in `dae_instr_is_unreachable_root` while preserving non-plain typed-control helper owners. `[SSANM-009b7]` fixes/classifies `defined=987 abs=1014` as a behavior/parity-risky typed loop-param local.set/plain-`br` carrier boundary in `gr_collect_func_global_sets`, narrowed so local.tee, br_if, br_table, nested, try_table, and reference/cast typed-control helper owners stay active. `[SSANM-009b8]` fixes `defined=1163 abs=1190` as a true branch result-carrier defaulting gap in `ar_rewrite_load_reinterpret_pair`: written continuation locals now stay canonical instead of being replaced by `i32.const 0` constructor operands. `[SSANM-009b9]` fixes/classifies `defined=1286 abs=1313` as a behavior/parity-risky nested typed loop-param plain-`br` boundary in `run_hot_pipeline_raw_simplify_locals_take_value_suffix`. `[SSANM-009b10]` then fixes the refreshed `defined=3 abs=30` `tlsf/addMemory` parameter-tee branch-carrier gap by preserving parameter `local.tee` writes that feed later not-taken-path reads while still freshening body-local allocator scratch. `[SSANM-009b11]` fixes the next `defined=154 abs=181` `parse__olevel__text` loop-header alias-copy gap by making future read/write scans descend through `loop` and `try_table`, preserving locals written before a loop when the loop header reads them before any loop-local write; `[SSANM-009b12]` fixes the next `defined=300 abs=327` `dae__try__unwrap__func313__suffix__staging__block__once` branch-exit store-suffix carrier gap by making the branch-exit canonical-alias scanner look through adjacent `local.set` suffixes before a plain `br`; `[SSANM-009b13]` classifies the next `defined=1413 abs=1440` / `defined=1431 abs=1458` candidates as a repeated multi-param typed result-loop store-model output-shape gap, split to `[SSANM-009b13a]` because Starshine initially spilled stack-carried branch operands through extra helper locals where Binaryen used a lower-local tuple/block carrier. `[SSANM-009b13a]` now closes that repeated family by keeping the source-shaped result-loop boundary and disabling the naive tuple/block carrier until it has canonical local-count proof: targeted current artifacts show Starshine final canonical locals lower than Binaryen at both `abs=1440` and `abs=1458`, while direct compare remains green. `[SSANM-009b14]` classifies the current full debug-WASI `defined=540 abs=567` first diff as a semantic-safe Starshine local-count win / no-regression representation difference: Starshine and Binaryen have identical canonical instruction/control skeletons, while Binaryen allocates 17 fresh dead one-shot temp locals that Starshine avoids; direct compare remains green. `[SSANM-009b15]` and `[SSANM-009b16]` classify the next repeated one-shot temp-local diffs at `defined=541 abs=568` and `defined=547 abs=574`: each differs only by one-use `local.tee` temp indexes, lowers Starshine local counts without function-body byte regression, and leaves control/call/memory/branch/table tokens identical. `[SSANM-009b17]` then classifies `defined=548 abs=575` as a semantic-safe neutral local-slot permutation: the local count, encoded body size, and control/dataflow skeleton are equal, and only branch-local temp indexes differ. `[SSANM-009b18]` classifies `defined=549 abs=576` as another semantic-safe one-shot temp-local local-count win: only 15 dead `local.tee` target indexes differ, Starshine has 59 body locals versus Binaryen 74, and both encode to the same 714-byte body. `[SSANM-009b19]` classifies `defined=552 abs=579` as a semantic-safe local-count win where Binaryen's seven extra one-use i32 tee temps shift the f64/f32/i64 carrier groups by seven indexes without changing dataflow, while both outputs encode to the same 832-byte body. `[SSANM-009b20]` classifies `defined=553 abs=580` as another one-shot temp-local win with only four dead `local.tee` target differences and equal 476-byte bodies. `[SSANM-009b21]` then classifies `defined=554 abs=581` as the same repeated one-shot temp-local local-count win with 15 dead `local.tee` target differences, Starshine 56 body i32 locals versus Binaryen 71, and equal 756-byte bodies. `[SSANM-009b22]` classifies `defined=557 abs=584` as a semantic-safe neutral local-slot permutation: the local count and 65-byte body are equal, and only same-type branch-local temp slots differ. `[SSANM-009b23]` classifies `defined=558 abs=585` as a semantic-safe Starshine local-count and encoded-body-size win: Starshine avoids Binaryen's 20 fresh one-use i32 tee temps, declares 119 body locals versus Binaryen 139, and encodes the body 14 bytes smaller. `[SSANM-009b24]` classifies `defined=560 abs=587` as a semantic-safe local-count win with i64 carrier group renumbering: masked body tokens are identical, Starshine declares `(42 i32, 2 i64)` body locals versus Binaryen `(56 i32, 3 i64)`, and the encoded body size is equal. `[SSANM-009b25]` classifies `defined=562 abs=589` as a semantic-safe Starshine local-count and encoded-body-size win: Binaryen allocates 37 fresh one-use i32 tee temps where Starshine reuses one-use locals, leaving masked body tokens identical while Starshine declares 151 body locals versus Binaryen 188 and encodes the body 37 bytes smaller. `[SSANM-009b26]` classifies `defined=566 abs=593` as another semantic-safe local-count win: only seven one-use `local.tee` target indexes differ, Starshine has 31 body i32 locals versus Binaryen 38, and both encode to the same 428-byte body. `[SSANM-009b27]` classifies `defined=567 abs=594` as a semantic-safe local-count and encoded-body-size win with i64 group renumbering: masked body tokens are identical, all 40 differing same-typed write targets are one-use, Starshine declares `(151 i32, 3 i64)` locals versus Binaryen `(188 i32, 6 i64)`, and Starshine encodes the body 34 bytes smaller. `[SSANM-009b28]` classifies `defined=568 abs=595` as another semantic-safe local-count win: the only token difference is one dead `local.tee` target, Starshine has 12 body i32 locals versus Binaryen 13, and both encode to the same 191-byte body. `[SSANM-009b29]` classifies `defined=570 abs=597` as a semantic-safe neutral local-slot permutation: masked body tokens, local operation counts, local declaration counts, and encoded body size are identical, and only same-type temp slots differ. `[SSANM-009b30]` classifies `defined=571 abs=598` as semantic-safe but Starshine-local-count-losing structured scratch-local drift: masked body tokens and encoded body bytes are identical, but Starshine appends locals `9`/`10` for two set/get pairs where Binaryen uses otherwise unused body locals `1`/`3`, leaving Starshine with 10 body i32 locals versus Binaryen 8. That gap is split to `[SSANM-009b30a]` rather than accepted as a win. `[SSANM-009b31]` classifies `defined=573 abs=600` as a semantic-safe local-count win with two one-use `local.tee` target differences: Starshine reuses locals `3` and `13`, while Binaryen allocates fresh locals `15` and `16`, with equal 217-byte bodies. `[SSANM-009b32]` classifies `defined=574 abs=601` as the same repeated one-shot temp-local win at larger scale: 20 one-use write targets differ, Starshine declares 72 body i32 locals versus Binaryen 92, and both encode to the same 999-byte body. `[SSANM-009b33]` classifies `defined=575 abs=602` as another repeated one-shot temp-local win: 27 one-use write targets differ, Starshine declares 96 body i32 locals versus Binaryen 123, and both encode to the same 1251-byte body. `[SSANM-009b34]` classifies `defined=576 abs=603` as the same family with 15 one-use `local.tee` target differences, Starshine 53 body i32 locals versus Binaryen 68, and equal 699-byte bodies. `[SSANM-009b35]` classifies `defined=577 abs=604` as a larger local-count and encoded-body-size win: 52 one-use write targets differ, Starshine declares `(169 i32, 5 i64)` locals versus Binaryen `(216 i32, 10 i64)`, and Starshine encodes the body 37 bytes smaller. `[SSANM-009b36]` through `[SSANM-009b38]` classify `defined=578..580 abs=605..607` as the same repeated one-shot temp-local win: masked body tokens are identical, only one-use `local.tee` targets differ, Starshine declares fewer i32 locals, and encoded bodies are equal. `abs=608` / `defined=581` matches after excluding the artifact-path log line. `[SSANM-009b39]` classifies `defined=582 abs=609` as another one-shot temp-local win with 12 one-use `local.tee` target differences, Starshine 59 body i32 locals versus Binaryen 71, and an equal 773-byte body; `abs=610` / `defined=583` matches exactly in targeted output. `[SSANM-009b40]` and `[SSANM-009b41]` classify `defined=584..585 abs=611..612` as the same repeated one-shot temp-local win: both have only 12 one-use `local.tee` target differences each, Starshine declares fewer i32 locals (`60` vs `72`, then `56` vs `68`), and encoded bodies are equal. `[SSANM-009b42]` and `[SSANM-009b43]` classify `defined=586..587 abs=613..614` as continued one-shot temp-local wins: masked bodies and encoded body sizes are equal, Starshine declares fewer body i32 locals (`49` vs `58`, then `42` vs `52`), and all differing locals are one-use `local.tee` targets. `[SSANM-009b44]` classifies `defined=588 abs=615` as a semantic-safe neutral local-slot permutation: masked bodies, local counts, and encoded body size are equal, with only a same-type permutation among one set/get pair and one-use tee targets. `[SSANM-009b45]` classifies `defined=589 abs=616` as a semantic-safe Starshine local-count and encoded-body-size win: masked bodies are identical, all 59 differing same-typed write targets are one-use, Starshine declares `(190 i32, 5 i64)` locals versus Binaryen `(244 i32, 10 i64)`, and Starshine encodes the body 40 bytes smaller. `[SSANM-009b46]` classifies `defined=590 abs=617` as another one-shot temp-local local-count win with eight one-use `local.tee` target differences, Starshine 26 body i32 locals versus Binaryen 34, and equal 341-byte bodies. `[SSANM-009b47]` classifies `defined=591 abs=618` as a semantic-safe neutral local-slot permutation with equal local counts and equal 329-byte bodies. `[SSANM-009b48]` classifies `defined=592 abs=619` as the same one-shot temp-local local-count win with 20 one-use `local.tee` target differences, Starshine 69 body i32 locals versus Binaryen 89, and equal 1089-byte bodies. `[SSANM-009b49]` through `[SSANM-009b51]` classify `defined=593..595 abs=620..622` as continued semantic-safe one-shot temp-local local-count wins: masked bodies are identical, only one-use `local.tee` target indexes differ, Starshine declares fewer body i32 locals (`58` vs `71`, `62` vs `77`, and `41` vs `46`), and encoded bodies are equal (`738`, `799`, and `542` bytes). Targeted `abs=623` output matches exactly. `[SSANM-009b52]` and `[SSANM-009b53]` classify `defined=597..598 abs=624..625` as the same semantic-safe local-count win family, with equal encoded bodies (`854` and `353` bytes) and fewer Starshine i32 locals (`68` vs `86`, then `30` vs `37`). `[SSANM-009b54]` classifies `defined=599 abs=626` as a larger structured-helper output-shape win: masked bodies are identical, 62 one-use same-typed write targets differ, Starshine declares `191` body i32 locals versus Binaryen `253`, and Starshine encodes the body smaller (`2932` vs `2974` bytes). `[SSANM-009b55]` through `[SSANM-009b58]` classify `defined=600..603 abs=627..630` as continued semantic-safe representation differences: `abs=627`, `629`, and `630` are local-count wins with only one-use `local.tee` target differences and equal encoded bodies (`423`, `733`, and `831` bytes), while `abs=628` is neutral local-slot drift with equal `26` body i32 locals and an equal `331` byte body. `[SSANM-009b59]` through `[SSANM-009b63]` extend that chain through `defined=604..608 abs=631..635`: `abs=631`, `632`, and `634` are local-count wins with equal encoded bodies (`673`, `615`, and `913` bytes), while `abs=633` and `635` are neutral local-slot permutations with equal body-local counts and equal encoded bodies (`342` and `279` bytes). `[SSANM-009b64]` through `[SSANM-009b68]` then classify `defined=609..613 abs=636..640`: `abs=636`, `638`, and `639` are local-count wins with equal encoded bodies (`1030`, `774`, and `565` bytes), `abs=637` is neutral local-slot drift with equal `26` body i32 locals and an equal `321` byte body, and `abs=640` is a local-count plus encoded-body-size win (`2602` vs Binaryen `2639` bytes). `[SSANM-009b69]` through `[SSANM-009b73]` continue the same representation-only chain through `defined=614..618 abs=641..645`: `abs=641` is a local-count and encoded-body-size win (`6063` vs Binaryen `6097` bytes), while `abs=642`, `643`, `644`, and `645` are local-count wins with equal encoded bodies (`629`, `615`, `399`, and `505` bytes). `[SSANM-009b74]` through `[SSANM-009b77]` continue through `defined=619..623 abs=646..650` after targeted `abs=647` matches exactly: `abs=646`, `648`, and `650` are local-count wins with equal encoded bodies (`689`, `341`, and `1086` bytes), while `abs=649` is neutral same-typed local-slot drift with an equal `329` byte body. `[SSANM-009b78]` through `[SSANM-009b82]` extend the representation-only chain through `defined=624..628 abs=651..655`: `abs=651`, `652`, `654`, and `655` are local-count wins with equal encoded bodies (`728`, `738`, `721`, and `431` bytes), while `abs=653` is neutral same-typed local-slot drift with equal `26` body i32 locals and an equal `313` byte body. `[SSANM-009b83]` through `[SSANM-009b87]` continue through `defined=629..634 abs=656..661` after targeted `abs=657` matches exactly: `abs=656`, `660`, and `661` are local-count wins with equal encoded bodies (`882`, `615`, and `1001` bytes), while `abs=658` and `659` are neutral same-typed local-slot drift with equal body-local counts and equal encoded bodies (`313` and `503` bytes). `[SSANM-009b88]` through `[SSANM-009b93]` continue after targeted `abs=662` / `defined=635` matches exactly: `abs=663`, `665`, `666`, and `668` are local-count wins with equal encoded bodies (`772`, `814`, `365`, and `622` bytes), `abs=664` is a local-count and encoded-body-size win (`1900` vs Binaryen `1930` bytes), and `abs=667` is neutral same-typed local-slot drift with equal `35` body i32 locals and an equal `320` byte body. `[SSANM-009b94]` through `[SSANM-009b98]` then continue after targeted `abs=670` / `defined=643` matches exactly: `abs=669`, `671`, `672`, and `674` are local-count wins with equal encoded bodies (`365`, `574`, `736`, and `557` bytes), while `abs=673` is neutral same-typed local-slot drift with equal `45` body i32 locals and an equal `465` byte body. `[SSANM-009b99]` through `[SSANM-009b104]` continue after targeted `abs=675` / `defined=648` matches exactly: `abs=676`, `677`, and `678` are local-count wins with equal encoded bodies (`805`, `1326`, and `173` bytes), while `abs=679`, `680`, and `682` are neutral same-typed local-slot drift with equal body-local counts and equal encoded bodies (`455`, `395`, and `335` bytes). Targeted `abs=681` / `defined=654` and `abs=683` / `defined=656` match exactly; the next tracked debug-WASI candidate is `defined=657 abs=684`, owned by `[SSANM-009b105]`. Huge-function admission/timing, remaining debug-WASI first-diff classification, and the currently no-op O4z slot decision remain open.

## Most important durable takeaways

- Upstream `ssa-nomerge` is **not** a separate algorithm from `ssa`.
  - both live in `SSAify.cpp`
  - the no-merge behavior is the shared algorithm with `allowMerges = false`
- The pass depends on whole-function LocalGraph CFG reasoning.
  - it is not just looking at adjacent `local.set` / `local.get` pairs
- The crucial no-merge decision is made **per set**, not per original local slot.
  - one original local can have some writes untangled and later merge-feeding writes left canonical
  - Starshine now consumes that LocalGraph decision table for straight-line raw `local.set` and `local.tee`, for simple missing-else one-arm entry/default merge preservation, for simple both-arm / block-`br_if` multi-source merge preservation, for simple void-loop direct `br_if 0` backedge canonical preservation, for narrow mixed fresh/canonical normal regions with `local.set`, small `local.tee` write sets, `br_if` early exits, and nested normal `block` / `if`, for ordinary normal `block` / `if` single-source `local.set` freshening, for explicit merge-adjacent ordinary `local.set` coverage, and for branch-free decorated void-loop `br_if 0` canonical merge backedges admitted by `[SSANM-006b2c1]` / `[SSANM-006b2c2]`; `[SSANM-006a1]` / `[SSANM-006a2c]` / `[SSANM-006a3a]` through `[SSANM-006a3c]` record the normal structured-control and legacy-helper boundary map, keeping plain `br`, ordinary non-mixed/non-backedge `br_if`, ordinary `br_table`, value-carrying branch operands, nested outer-target `br_if`, nested loop-exit `br_if`, EH, and typed-control branch operands fail-closed from ordinary planned LocalGraph mutation until their owning slices narrow the helper behavior; `[SSANM-006b2b]` adds regression locks that keep completed straight-line/default/no-write families away from lifted HOT SSA destruction, `[SSANM-006b2c3]` / `[SSANM-006b2c4]` now close retained structured fallback boundaries by keeping branch/table-decorated loop backedges off HOT SSA destruction and planned LocalGraph success reasons while leaving invalid lowered carriers as writeback safety boundaries, `[SSANM-006b2d]` keeps retained branch/table/typed/EH scratch helpers classified as boundary lowerings rather than ordinary no-merge predecessor-copy work, `[SSANM-007a2]` closes the throwing EH fail-closed locks while `[SSANM-007a3]` keeps no-throw `try_table` subsets classified as ordinary normal-flow or typed-control helper work only when call/throw-bearing edges are absent, and `[SSANM-007b1]` maps typed loop/result plus typed branch/cast helpers to `[SSANM-007b2]` / `[SSANM-007b3]` boundary locks instead of counting their scratches as ordinary no-merge predecessor copies; `[SSANM-007b2]` now locks scalar/reference loop-param proxy, no-copy typed-loop, multi-param/single-result store-model, typed-loop `br_table`, and nested loop-target behavior with focused trace/output assertions; `[SSANM-007b3a]` / `[SSANM-007b3b]` lock value `br`, `br_if`, and value `br_table` operands as branch/table ABI boundaries; `[SSANM-007b3c]` locks representative `br_on_null` / `br_on_non_null` no-copy, prefix/copy-needing, raw-path, and tested-producer exits as typed-control ABI boundaries; and `[SSANM-007b3d]` closes the typed branch subtrack by locking representative `br_on_cast` / `br_on_cast_fail` no-copy, divergent-alias, multi-result, multi-param, no-copy loop-target, and copy-needing loop-target exits as cast-branch ABI boundaries
- Reads with no explicit reaching set can be rewritten to explicit default values.
  - that includes scalar zeros, `v128` zero splats, and in shared `SSAify` helper paths also ref/tuple defaults
  - Starshine now consumes planned default-entry get decisions for straight-line functions, no-local-write functions, and a narrow normal structured-control subset with body-default reads and other writes
- Full phi-like merge materialization still exists in the same source file, but that is the `--ssa` path, not the `--ssa-nomerge` path. `[SSANM-007c]` split the remaining full-`ssa` materialization backlog into sibling `[O4Z-AUDIT-SSA-FULL]` / `[SSA-FULL-*]` tasks so no-merge closeout cannot silently absorb full-SSA work.
- A narrow 2026-04-20 source comparison found **no semantic post-`version_129` drift** in `SSAify.cpp`, `LocalGraph.cpp`, `local-graph.h`, `ReFinalize.cpp`, or the dedicated `ssa-nomerge` golden test file.

## Biggest beginner correction

The easy wrong mental model is:

- `ssa-nomerge` converts the whole function into SSA form, just without explicit phi syntax

The safer mental model is:

- Binaryen only untangles local writes whose influenced gets stay single-source,
- refuses to invent merge locals in no-merge mode,
- and keeps merged values on canonical original slots so later passes can benefit without immediate code-size growth.

That is why the pass is narrower than the name sounds, but more source-sensitive than a simple local peephole.

## What the pass sounds like versus what it actually does

What it sounds like:

- make all locals SSA-ish

What it actually is in `version_129`:

- a function-parallel LocalGraph-based pass,
- an already-SSA index detector,
- a per-set fresh-local allocator,
- a get rewriter for single-source and default-value reads,
- a deliberate no-op on merge gets,
- and a narrow refinalization repair step when default ref values sharpen types.

## Page map

- [`./binaryen-strategy.md`](./binaryen-strategy.md)
  - Deep dive into the actual `SSAify.cpp` structure, helper dependencies, scheduler placement, LocalGraph flow model, and the shared-`ssa` versus no-merge split.
- [`./implementation-structure-and-tests.md`](./implementation-structure-and-tests.md)
  - Source-owner and proof-surface map for upstream `SSAify.cpp`, `LocalGraph`, `ReFinalize`, pass registration, no-merge golden tests, and the exact current Starshine implementation/test files.
- [`./starshine-hot-ir-strategy.md`](./starshine-hot-ir-strategy.md)
  - Exact MoonBit code map for the current local pass, covering the LocalGraph-planned raw no-merge route, retained HOT SSA destruction fallback, branch/table/typed/EH boundary helpers, preset placement, and the main ways current Starshine differs from an AST clone of upstream Binaryen `SSAify(false)`.
- [`./merge-shapes-and-canonical-slots.md`](./merge-shapes-and-canonical-slots.md)
  - Focused guide to the hardest part to misunderstand on the upstream side: why the no-merge policy is per set, how default/param values count as merge inputs, and why some writes to one original local still get fresh locals while others stay canonical.
- [`./wat-shapes.md`](./wat-shapes.md)
  - Beginner-friendly shape catalog covering straight-line positives, dead-param and default-zero rewrites, merge bailouts, shared-helper ref/tuple defaults, and the full-`ssa` families that upstream `ssa-nomerge` intentionally declines.
- [`./parity.md`](./parity.md)
  - Current in-tree parity state, fixed direct-skip families, compare evidence, and the honest remaining artifact-level exact-parity / huge-function story.

## Freshness note

A 2026-06-13 `[SSANM-001a]` refresh rechecked the owning official source/test surfaces against the local Binaryen oracle, `wasm-opt version 130 (version_130)`.

- `src/passes/SSAify.cpp` is identical between `version_129` and `version_130`.
- `src/ir/local-graph.h` is identical between `version_129` and `version_130`.
- `src/ir/LocalGraph.cpp` is identical between `version_129` and `version_130`.
- `test/passes/ssa-nomerge_enable-simd.wast` and `.txt` are identical between `version_129` and `version_130`.
- `test/lit/passes/ssa.wast` and `test/gtest/local-graph.cpp` are identical between `version_129` and `version_130`.
- `src/ir/ReFinalize.cpp` only adds finalizers for new wide-int expression nodes; this does not change the no-merge default-ref repair contract.
- `src/passes/pass.cpp` and `src/passes/passes.h` only changed in unrelated registration/declaration surfaces plus a GC closed-world option spelling; `ssa-nomerge` registration and early optimizer scheduling are unchanged.

So the durable rule is:

- the `version_129` behavior dossier remains valid for local Binaryen `version_130` no-merge planning;
- keep the source-refresh note explicit and pass-surface-limited;
- do **not** silently upgrade that narrower claim into “all Binaryen optimizer behavior is unchanged.”

## Current maintenance rule

- Treat this folder as the canonical home for future `ssa-nomerge` parity and scheduler research.
- Keep the main beginner correction explicit:
  - upstream `ssa-nomerge` is LocalGraph-based single-source untangling with merges deliberately left canonical, not full phi SSA and not just straight-line renaming
- Keep the per-set no-merge rule, the default-value materialization behavior, the shared `ssa` helper boundary, the local HOT-SSA-destruction strategy page, the normal-flow-only SSA v1 exceptional-edge exclusion, and the current artifact-level fail-closed writeback caveat explicit whenever future docs or code changes touch this pass.

## Sources

- [`../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md`](../../../raw/research/0722-2026-06-09-ssa-nomerge-exceptional-edge-audit.md)
- [`../../../raw/binaryen/2026-06-13-ssa-nomerge-version-130-source-refresh.md`](../../../raw/binaryen/2026-06-13-ssa-nomerge-version-130-source-refresh.md)
- [`../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md`](../../../raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md)
- [`../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md`](../../../raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md)
- [`../../../raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md`](../../../raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md)
- [`../../../../../src/passes/ssa_nomerge.mbt`](../../../../../src/passes/ssa_nomerge.mbt)
- [`../../../../../src/passes/ssa_nomerge_test.mbt`](../../../../../src/passes/ssa_nomerge_test.mbt)
- [`../../../../../src/passes/pass_manager.mbt`](../../../../../src/passes/pass_manager.mbt)
- [`../../../../../src/passes/optimize.mbt`](../../../../../src/passes/optimize.mbt)
- [`../../../../../agent-todo.md`](../../../../../agent-todo.md)
- [`../../../raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md`](../../../raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md) preserves the saved generated-artifact `-O4z` slot, summary, and Binaryen debug-log facts; older `.artifacts` paths are replay identifiers, not durable wiki source links.
- Binaryen `version_129` sources:
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/SSAify.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/local-graph.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/LocalGraph.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/ir/ReFinalize.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/pass.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/src/passes/passes.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/passes/ssa-nomerge_enable-simd.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/passes/ssa-nomerge_enable-simd.txt>
  - <https://github.com/WebAssembly/binaryen/blob/version_129/test/lit/passes/ssa.wast>
- Binaryen `version_130` refresh surface:
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/passes/SSAify.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/ir/local-graph.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/ir/LocalGraph.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/ir/ReFinalize.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/passes/pass.cpp>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/src/passes/passes.h>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/test/passes/ssa-nomerge_enable-simd.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/test/passes/ssa-nomerge_enable-simd.txt>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/test/lit/passes/ssa.wast>
  - <https://github.com/WebAssembly/binaryen/blob/version_130/test/gtest/local-graph.cpp>
