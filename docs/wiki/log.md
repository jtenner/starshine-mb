# Wasm Knowledge Base Log

Append new entries; do not rewrite prior history except to fix obvious formatting mistakes or redact sensitive data.

## [2026-05-05] health | add `reorder-types` freshness layer and port-readiness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-reorder-types-current-main-recheck.md` plus `docs/wiki/raw/research/0492-2026-05-05-reorder-types-port-readiness-and-validation.md` after rechecking the official Binaryen `main` `ReorderTypes.cpp`, `pass.cpp`, and `reorder-types.wast` surfaces against the existing contract.
- Added `docs/wiki/binaryen/passes/reorder-types/starshine-port-readiness-and-validation.md` and refreshed the `reorder-types` dossier, the top-level wiki index, the pass-folder index, the tracker, and the reordered living page set so the 2026-05-05 freshness layer and the new validation bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same GC-only, `--closed-world`, private-type-only layout pass, and Starshine still keeps `reorder-types` boundary-only until a deliberate module-pass port exists.

## [2026-05-05] health | correct `local-cse` Starshine status and catalog wording

- Added `docs/wiki/raw/research/0491-2026-05-05-local-cse-starshine-active-direct-pass-correction.md` to record that the older raw upstream bridge is still valid for Binaryen, but its Starshine-status sentence is stale now that the direct Starshine `local-cse` pass has landed.
- Refreshed the `local-cse` implementation/test-map, Starshine strategy, Starshine readiness bridge, and both pass catalogs so they now distinguish the active direct pass from the still-gated ordered preset neighborhoods.
- Recorded unchanged upstream contract status: Binaryen `local-cse` still uses the same whole-tree, window-local scan/check/apply story; the remaining gap is exact ordered-neighborhood parity, not a missing direct transform.

## [2026-05-05] health | refresh `reorder-locals` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-reorder-locals-current-main-recheck.md` plus `docs/wiki/raw/research/0472-2026-05-05-reorder-locals-current-main-recheck.md` after rechecking current `main` `ReorderLocals.cpp`, `pass.cpp`, and the dedicated `reorder-locals*` lit surfaces against the existing contract.
- Refreshed the `reorder-locals` dossier, Binaryen strategy, implementation/test-map, parity bridge, Starshine readiness bridge, wiki index, pass catalog, tracker, and changelog so the 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same access-count plus first-use sort story, and Starshine still keeps the explicit module pass while preset scheduling remains gated on neighboring locals passes and ordered no-DWARF replay evidence.

## [2026-05-05] health | refresh `global-effects` line anchors and local code-map refs

- Added `docs/wiki/raw/binaryen/2026-05-05-global-effects-current-main-line-anchor-refresh.md` plus `docs/wiki/raw/research/0490-2026-05-05-global-effects-current-main-line-anchor-refresh.md` after rechecking the official Binaryen `main` `GlobalEffects.cpp`, `pass.cpp`, and `effects.h` anchors against the existing metadata-producing contract.
- Refreshed the `global-effects` overview, Binaryen strategy, implementation/test-map, Starshine strategy, and top-level wiki index so the 2026-05-05 line-anchor refresh is visible from the living dossier together with the exact local Starshine code-map anchors.
- Recorded unchanged contract status: upstream `generate-global-effects` still remains metadata-only and out of the default optimize sequence, and Starshine still keeps `global-effects` boundary-only until a real module-level metadata pass exists.

## [2026-05-05] health | refresh `simplify-locals-notee-nostructure` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-simplify-locals-notee-nostructure-current-main-recheck.md` plus `docs/wiki/raw/research/0489-2026-05-05-simplify-locals-notee-nostructure-current-main-recheck.md` after rechecking current `main` `SimplifyLocals.cpp`, `pass.cpp`, and the dedicated `simplify-locals-notee-nostructure` test pair against the existing contract.
- Refreshed the `simplify-locals-notee-nostructure` landing page, Binaryen strategy, implementation/test-map, variant-surface, WAT-shape catalog, Starshine strategy, Starshine readiness bridge, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same shared `SimplifyLocals<false, false, true>` engine plus the no-fresh-tee / no-structure aggressive-neighborhood split, and Starshine still keeps the pass active-direct but preset-deferred.

## [2026-05-05] health | refresh `global-struct-inference-desc-cast` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-global-struct-inference-desc-cast-current-main-recheck.md` plus `docs/wiki/raw/research/0488-2026-05-05-global-struct-inference-desc-cast-current-main-recheck.md` after rechecking current `main` `GlobalStructInference.cpp`, `pass.cpp`, `gsi-to-desc-cast.wast`, `gsi-desc.wast`, and `gsi.wast` against the corrected contract.
- Refreshed the `global-struct-inference-desc-cast` landing page, Binaryen strategy, implementation/test-map, singleton-gate guide, WAT-shape catalog, Starshine strategy, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same target-descriptor-singleton `ref.cast_desc_eq` gate, and Starshine still keeps the pass boundary-only until a deliberate port lands.

## [2026-05-05] health | refresh `dae-optimizing` source anchors and add port-readiness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-dae-optimizing-current-main-recheck.md` plus `docs/wiki/raw/research/0487-2026-05-05-dae-optimizing-current-main-recheck.md` after rechecking current `main` `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, and the optimizing DAE lit surface against the corrected contract.
- Added `docs/wiki/binaryen/passes/dae-optimizing/starshine-port-readiness-and-validation.md` and refreshed the `dae-optimizing` landing page, Binaryen strategy, implementation/test-map, signature / nested-rerun guide, WAT-shape catalog, Starshine strategy, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer and the new implementation-readiness bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same shared `DeadArgumentElimination.cpp` engine plus optimizing-only nested cleanup replay, and Starshine still keeps the pass unimplemented under the descriptive local spelling caveat.

## [2026-05-05] health | refresh `dae2` source anchors and raw bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-dae-optimizing-current-main-recheck.md` plus `docs/wiki/raw/research/0487-2026-05-05-dae-optimizing-current-main-recheck.md` after rechecking current `main` `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, and the optimizing DAE lit surface against the corrected contract.
- Added `docs/wiki/binaryen/passes/dae-optimizing/starshine-port-readiness-and-validation.md` and refreshed the `dae-optimizing` landing page, Binaryen strategy, implementation/test-map, signature / nested-rerun guide, WAT-shape catalog, Starshine strategy, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer and the new implementation-readiness bridge are visible from the living dossier.

- Added `docs/wiki/raw/research/0486-2026-05-05-dae2-source-anchor-refresh.md` to capture the exact upstream Binaryen file pages reviewed for `dae2` and keep the current-main recheck tied to the living dossier.
- Refreshed the `dae2` landing page, Binaryen strategy, implementation/test-map, pass catalog, and root wiki index so the folder now points readers at the source-anchor refresh note alongside the immutable manifest, current-main recheck, and the existing Starshine first-slice / validation bridge.
- Recorded unchanged contract status: current `main` still teaches the same backward fixed-point forwarded-parameter contract, and Starshine still treats `dae2` as unknown rather than boundary-only or removed.

## [2026-05-05] health | refresh `merge-locals` current-main bridge and add port-readiness page

- Added `docs/wiki/raw/binaryen/2026-05-05-merge-locals-current-main-recheck.md` plus `docs/wiki/raw/research/0485-2026-05-05-merge-locals-current-main-recheck.md` after rechecking current `main` `MergeLocals.cpp`, `pass.cpp`, and `merge-locals.wast` against the corrected copy-shape contract.
- Added `docs/wiki/binaryen/passes/merge-locals/starshine-port-readiness-and-validation.md` and refreshed the `merge-locals` landing page, Binaryen strategy, implementation/test-map, LocalGraph guide, WAT-shape catalog, Starshine strategy, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer and the new port-readiness bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same copy-shaped local.set/local.get / `LocalGraph` / orientation / rollback story, and Starshine still keeps `merge-locals` removed until a deliberate port lands.

## [2026-05-05] health | refresh `rereloop` current-main bridge and add readiness page

- Added `docs/wiki/raw/binaryen/2026-05-05-rereloop-current-main-recheck.md` plus `docs/wiki/raw/research/0484-2026-05-05-rereloop-current-main-recheck.md` after rechecking official Binaryen `main` `ReReloop.cpp`, `pass.cpp`, `Relooper.{h,cpp}`, `flat.h`, and the dedicated `flatten_rereloop` fixtures against the corrected contract.
- Added `docs/wiki/binaryen/passes/rereloop/starshine-port-readiness-and-validation.md` and refreshed the `rereloop` landing page, Binaryen strategy, implementation/test-map, flat-CFG boundary guide, WAT-shape catalog, Starshine strategy, and top-level wiki index so the 2026-05-05 freshness layer and the new port-readiness bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same flat-IR CFG-to-Relooper restructuring story, and Starshine still keeps `re-reloop` removed until a deliberate port lands.

## [2026-05-05] health | refresh `strip-target-features` freshness layer and current-main recheck

- Added `docs/wiki/raw/binaryen/2026-05-05-strip-target-features-current-main-recheck.md` plus `docs/wiki/raw/research/0483-2026-05-05-strip-target-features-current-main-recheck.md` after rechecking current `main` `StripTargetFeatures.cpp`, `pass.cpp`, `passes.h`, `pass.h`, and the custom-section spec context against the corrected module-metadata contract.
- Refreshed the `strip-target-features` overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, Starshine strategy, Starshine readiness bridge, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same shared-owner target-feature metadata toggle, and Starshine still keeps both names unimplemented with explicit registry rejection plus the opaque-custom-section / first-class-metadata choice left open.

## [2026-05-05] health | refresh `remove-relaxed-simd` freshness layer and current-main recheck

- Added `docs/wiki/raw/binaryen/2026-05-05-remove-relaxed-simd-current-main-recheck.md` plus `docs/wiki/raw/research/0482-2026-05-05-remove-relaxed-simd-current-main-recheck.md` after rechecking current `main` `RemoveRelaxedSIMD.cpp`, `pass.cpp`, `child-localizer.h`, and `remove-relaxed-simd.wast` against the existing contract.
- Refreshed the `remove-relaxed-simd` dossier pages so the 2026-05-05 freshness layer is visible in the landing page, Binaryen strategy, implementation/test map, WAT-shape catalog, Starshine strategy, and Starshine readiness bridge.
- Recorded unchanged contract status: current `main` still teaches the same trap-replacement `remove-relaxed-simd` contract, and Starshine still keeps the pass unimplemented with explicit registry rejection and a separate feature-metadata caveat.

## [2026-05-05] health | refresh `reorder-functions-by-name` source anchors and add port-readiness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-reorder-functions-by-name-current-main-recheck.md` plus `docs/wiki/raw/research/0481-2026-05-05-reorder-functions-by-name-current-main-recheck.md` after rechecking current `main` `ReorderFunctions.cpp`, `pass.cpp`, and `reorder-functions-by-name.wast` against the existing source-backed contract.
- Added `docs/wiki/binaryen/passes/reorder-functions-by-name/starshine-port-readiness-and-validation.md` and refreshed the `reorder-functions-by-name` dossier, `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the supporting strategy / implementation / proof / shape pages so the folder now has a dedicated implementation-readiness bridge plus a 2026-05-05 freshness layer.
- Recorded unchanged contract status: current `main` still teaches the same ascending-name declaration sort, and Starshine still keeps the pass boundary-only until a real module-pass permutation/remap implementation lands.

## [2026-05-05] health | refresh `global-effects` source anchors and raw manifest

- Updated the `global-effects` raw current-main recheck with exact 2026-05-05 upstream line anchors for `GlobalEffects.cpp`, `pass.cpp`, and `effects.h` so the source archive now names the reviewed current-main locations explicitly.
- Refreshed the living `global-effects` Binaryen strategy, implementation/test-map, metadata/consumer, WAT-shape, Starshine strategy, and Starshine readiness pages so their source lists now include the 2026-05-05 recheck alongside the earlier 2026-05-04 and 2026-04-27 manifests.
- Kept the contract unchanged: Binaryen still teaches the same metadata-producing `generate-global-effects` contract, and Starshine still keeps `global-effects` boundary-only until a real module-level analyzer exists.

## [2026-05-05] health | add `local-subtyping` port-readiness bridge

- Added `docs/wiki/binaryen/passes/local-subtyping/starshine-port-readiness-and-validation.md` to turn the existing `local-subtyping` dossier into a complete upstream-vs-future-port set: overview, implementation map, corrected Binaryen strategy, LUB/dominance guide, WAT-shape catalog, Starshine status, and implementation-readiness bridge.
- Kept the upstream contract unchanged: the existing 2026-05-05 raw current-main bridge still supports the same iterative set-fed LUB refinement with get-aware non-null dominance/type repair and repeated refinalization story.
- Refreshed the folder index, Starshine strategy, implementation/test-map, top-level wiki index, pass catalog, and tracker so the new bridge is discoverable from the shared catalogs.

## [2026-05-05] maintain | complete PRV003 match proof pilot

- Completed `[PRV]003` by adding `src/validate/match_proof.mbtp` with the Boolean symmetry postcondition behind `descriptor_compatible` and a focused helper in `match.mbt`.
- Added executable descriptor-match regressions for symmetric abstract refs plus equal and unequal exact-struct shapes, and updated the proof strategy to mark direct `src/validate/match.mbt` proving as an optional pilot rather than a required CI gate.

## [2026-05-05] health | refresh `dae2` current-main spotcheck and exact Starshine anchors

- Added an official GitHub web spotcheck to `docs/wiki/raw/binaryen/2026-05-05-dae2-current-main-recheck.md` so the current-main capture now names the `DeadArgumentElimination2.cpp`, `pass.cpp`, and `dae2.wast` source pages explicitly.
- Tightened the `dae2` living dossier and Starshine strategy page to point at exact local line ranges in `src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, and `src/cmd/cmd.mbt`, and refreshed the main wiki catalog plus Binaryen pass catalog entry to mention the spotcheck.
- Kept the upstream contract unchanged: `dae2` is still the same separate public experimental DAE reimplementation, and Starshine still treats it as unknown rather than boundary-only or removed.

## [2026-05-05] maintain | close PRV005 proof gate and trust ledger

- Completed `[PRV]005` by recording the current required proof gate policy: `moon prove src/validate_proof` is the committed proof target when Why3/Z3 are configured; root-level and direct `src/validate` proof gates remain deferred.
- Added a trust-surface ledger to `validation/moonbit-prove-strategy.md` covering the current absence of committed `proof_axiomatized` uses plus deferred direct-validator, recursive match, and typechecker proof debts.

## [2026-05-05] maintain | extend PRV002 proof kernel

- Completed `[PRV]002` by adding `rectype_suffix_member_index` to `src/validate_proof`, proving that a suffix-local rectype member maps to a bounded absolute type index.
- Extended the proof kernel regression tests and refreshed the validation proof strategy so the active committed proof boundary now records ten helper goals.

## [2026-05-05] maintain | close PRV001 proof documentation policy

- Updated `validation/moonbit-prove-strategy.md` with the explicit PRV001 proof policy: `src/validate_proof` is the current committed proof boundary and safe CI/local proof target, while direct `src/validate` proving remains an opt-in local investigation until the historical `jtenner/starshine/lib` WhyML lowering blocker is cleared.
- Kept broad root-level `moon prove` out of the required gate; `moon prove src/validate_proof` remains the current proof signoff command.

## [2026-05-05] health | refresh `type-generalizing` current-main bridge and exact local anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-type-generalizing-current-main-recheck.md` plus `docs/wiki/raw/research/0479-2026-05-05-type-generalizing-current-main-recheck.md` after rechecking Binaryen `main` `TypeGeneralizing.cpp`, `pass.cpp`, and `test/lit/passes/type-generalizing.wast` against the corrected contract.
- Refreshed the `type-generalizing` dossier, Starshine strategy and port-readiness pages, the superseded local-flow bridge page, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer and exact local code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still matches the same hidden not-yet-sound CFG/backward-analysis contract, and Starshine still keeps `type-generalizing` boundary-only until a deliberate module/type-graph implementation lands.

## [2026-05-05] health | refresh `de-nan` current-main bridge and stale anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-de-nan-current-main-recheck.md` plus `docs/wiki/raw/research/0478-2026-05-05-de-nan-current-main-recheck.md` after rechecking Binaryen `main` `DeNaN.cpp`, `pass.cpp`, `properties.h`, `names.h`, `wasm-builder.h`, `pass.h`, and `denan.wast` against the existing oracle.
- Refreshed the `de-nan` dossier, Starshine strategy and port-readiness pages, wiki index, pass catalog, and tracker so the 2026-05-05 freshness layer and corrected local code anchors are visible from the living dossier.

## [2026-05-05] fuzz | persist pass-fuzz failure metadata

- Completed `[FZG]029` by adding `failure-metadata.json` to every `pass-fuzz-compare` persisted failure directory.
- Recorded case index, generator, failure detail, copied artifacts, relative replay input, and pass flags so command failures, invalid bases, validation failures, and mismatches are easier to replay from the saved corpus.

## [2026-05-05] fuzz | retune gen-valid feature floors

- Completed `[FZG]028` by adding explicit smoke/ci/stress floors for every completed gen-valid widening family from `[FZG]002` through `[FZG]023`.
- Switched validate-valid profiles to coverage-forced gen-valid configs, keeping smoke cheap with minimum `1` per widened family while ci/stress require progressively higher nonzero floors (`10`/`100`).

## [2026-05-05] fuzz | widen WAST arbitrary surface

- Completed `[FZG]027` by adding a deterministic wide-surface prelude to `src/wast/arbitrary.mbt` for GC struct types, tags, `try_table`/`throw`, memory/table bulk ops, descriptor ref test/cast forms, tail-call/call-ref forms, inline exports, and representative SIMD text syntax.
- Updated `docs/wiki/fuzzing/wast-arbitrary-parity-plan.md` to record that this is parser/printer text-surface coverage, not a replacement for typed `gen_valid` feature-floor accounting.

## [2026-05-05] health | tighten `local-cse` starshine anchors and regression surfaces

- Corrected the `local-cse` Starshine dossier to stop pointing at a nonexistent `src/passes/pass_manager.mbt:8995-9001` range and instead cite the real local proof surfaces in `src/passes/optimize.mbt` and `src/passes/optimize_test.mbt`.
- Refreshed the `local-cse` implementation/test-map, Starshine strategy, and port-readiness pages so the removed-registry status, dispatcher absence, and the `flatten -> simplify-locals-notee-nostructure -> local-cse` neighborhood gate are all backed by exact current local line anchors.
- Kept the upstream source contract unchanged: Binaryen still teaches the same scan/check/apply temp-local reuse story, and Starshine still keeps `local-cse` removed until the missing local neighbors exist.

## [2026-05-05] health | refresh `remove-unused-types` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-remove-unused-types-current-main-recheck.md` plus `docs/wiki/raw/research/0477-2026-05-05-remove-unused-types-current-main-recheck.md` after rechecking current `main` `RemoveUnusedTypes.cpp`, `pass.cpp`, `type-updating.h`, `module-utils.h`, and `remove-unused-types.wast` against the existing corrected contract.
- Refreshed the `remove-unused-types` overview, Binaryen strategy, implementation/test-map, closed-world guide, WAT-shape catalog, Starshine strategy, Starshine port-readiness bridge, wiki index, pass index, tracker, and changelog so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same helper-owned GC/type rewrite story, and Starshine still keeps `remove-unused-types` boundary-only until shared closed-world type-graph infrastructure exists.

## [2026-05-05] fuzz | widen invalid AST memory64/shared strategy

- Added `shared-memory64-without-max`, `invalid-subtype-super-index`, and `invalid-subtype-super-shape` to the invalid-AST strategy registry for `[FZG]024`, reusing valid generated seeds and mutating them into invalid memory/type-section shapes.
- Recorded focused coverage in `docs/wiki/fuzzing/generator-coverage-ledger.md`; the new strategies reject through their expected validation families and participate in deterministic invalid-AST fuzz profiles.

## [2026-05-05] fuzz | document WAST arbitrary parity boundary

- Added `docs/wiki/fuzzing/wast-arbitrary-parity-plan.md` for `[FZG]026`, deciding that WAST arbitrary generation remains independent from `gen_valid` while sharing the FZG coverage vocabulary.
- Documented the stale-picker rule: duplicated text-shape opcode pickers in `src/wast/arbitrary.mbt` are allowed only when their lag from valid-generator coverage is explicit in the parity plan or generator coverage ledger.
- Added a source comment in `src/wast/arbitrary.mbt` pointing future generator edits at the parity boundary.

## [2026-05-05] health | refresh `directize` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-directize-current-main-recheck.md` plus `docs/wiki/raw/research/0476-2026-05-05-directize-current-main-recheck.md` after rechecking current `main` `Directize.cpp`, `pass.cpp`, `passes.h`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, and the dedicated `directize*` lit files against the existing `version_129` contract.
- Refreshed the `directize` overview, Binaryen strategy, implementation/test-map, table-info, WAT-shape, Starshine strategy, and Starshine port-readiness pages so the new 2026-05-05 freshness bridge and the exact local code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same table-facts-first indirect-call rewrite contract, and the new bridge keeps the `table.copy` mutation-barrier caveat explicit.

## [2026-05-05] fuzz | account import/export topology coverage

- Added `ImportExportTopology` gen-valid feature accounting and ledger docs so FZG020 no longer reports as a zero-count future row.
- Recorded the validation state: `moon info` / `moon fmt` are green, while `moon test --package jtenner/starshine/validate` remains blocked by the pre-existing coverage-forced gen-valid validation cluster before this slice's assertions can complete.

## [2026-05-05] health | tighten `optimize-casts` local code anchors

- Tightened the `optimize-casts` implementation/status map so the exact local registry anchor now points at the full removed-name block in `src/passes/optimize.mbt:143-149` instead of the narrower middle slice.
- Kept the existing current-main freshness layer and upstream contract pages intact; this was a stale-anchor cleanup only.

## [2026-05-05] health | refresh `constant-field-null-test-folding` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-constant-field-null-test-folding-current-main-recheck.md` plus `docs/wiki/raw/research/0475-2026-05-05-constant-field-null-test-folding-current-main-recheck.md` after rechecking current `main` `ConstantFieldPropagation.cpp`, `pass.cpp`, `cfp.wast`, and `cfp-reftest.wast` against the existing sibling contract.
- Refreshed the `constant-field-null-test-folding` dossier, the sibling `cfp-reftest` Binaryen strategy / implementation / shape / Starshine pages, the shared catalogs, and the wiki index so the sibling-specific freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same shared CFP engine plus narrow two-bucket `ref.test` rescue path, and Starshine still keeps the sibling boundary-only until a closed-world module analysis exists.

## [2026-05-05] health | tighten `tuple-optimization` freshness wording and source bridge

- Reworded the living `tuple-optimization` landing and Binaryen-strategy pages so their freshness note now matches the 2026-05-04 current-main bridge instead of the older 2026-04-22 check language.
- Kept the immutable 2026-04-22 raw primary-source manifest intact as the provenance anchor for `version_129`, and left the exact Starshine code-map pages unchanged because they were already current.
- No implementation work was needed; this was reference hygiene to keep the tuple dossier's current-main wording consistent with the captured source dates.

## [2026-05-05] health | refresh `constant-field-propagation` current-main bridge and port-readiness

- Added `docs/wiki/raw/binaryen/2026-05-05-constant-field-propagation-current-main-recheck.md` plus `docs/wiki/raw/research/0474-2026-05-05-constant-field-propagation-current-main-recheck.md` after rechecking current `main` `ConstantFieldPropagation.cpp`, `pass.cpp`, `cfp.wast`, and `cfp-reftest.wast` against the existing `version_129` contract.
- Added `docs/wiki/binaryen/passes/constant-field-propagation/starshine-port-readiness-and-validation.md` and refreshed the `constant-field-propagation` overview, Binaryen strategy, implementation/test-map, Starshine strategy, sibling Starshine status page, top-level wiki index, pass index, tracker, and changelog so the new 2026-05-05 freshness bridge and implementation-readiness bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same closed-world CFP engine, and Starshine still keeps both CFP-family names boundary-only until a real module/type-graph port exists.

## [2026-05-05] health | refresh `coalesce-locals` current-main bridge and exact Starshine code anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-coalesce-locals-current-main-recheck.md` plus `docs/wiki/raw/research/0473-2026-05-05-coalesce-locals-current-main-recheck.md` after rechecking current `main` `CoalesceLocals.cpp`, `pass.cpp`, `opt-utils.h`, `liveness-traversal.h`, `numbering.h`, `utils.h`, and `test/lit/passes/coalesce-locals.wast` against the existing `version_129` contract.
- Refreshed the `coalesce-locals` overview, Binaryen strategy, implementation/test-map, interference/order, WAT-shape, Starshine strategy, Starshine port-readiness/validation, top-level wiki index entry, and tracker entry so the new 2026-05-05 freshness bridge and exact Starshine code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same exact-type, value-aware slot-coalescing story, and Starshine still keeps the active direct pass honest about the remaining ordered-neighborhood and runtime follow-up.

## [2026-05-05] health | refresh `merge-blocks` current-main anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-merge-blocks-current-main-anchor-recheck.md` plus `docs/wiki/raw/research/0472-2026-05-05-merge-blocks-current-main-anchor-recheck.md` after rechecking current-main Binaryen `MergeBlocks.cpp`, `pass.cpp`, `merge-blocks.wast`, and the shared helper surfaces against the existing `version_129` contract.
- Refreshed the `merge-blocks` overview, Binaryen strategy, WAT-shape catalog, implementation/test-map, Starshine strategy, Starshine HOT code-map, index, pass catalog, tracker, and changelog so the exact Starshine code anchors now match the current `src/passes/*` line map.
- Recorded unchanged contract status: upstream `merge-blocks` still uses helper-driven block/loop-tail motion, and Starshine still stays HOT-region-root-only with the same live-label and typed-carrier rules.

## [2026-05-05] health | refresh `gufa` current-main bridge and add port-readiness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-gufa-current-main-recheck.md` plus `docs/wiki/raw/research/0471-2026-05-05-gufa-current-main-recheck.md` after rechecking current `main` `pass.cpp`, `GUFA.cpp`, `possible-contents.h`, and the three dedicated lit surfaces against the existing `version_129` contract.
- Added `docs/wiki/binaryen/passes/gufa/starshine-port-readiness-and-validation.md` and refreshed the `gufa` overview, Binaryen strategy, implementation/test-map, content-oracle boundaries, WAT-shape catalog, Starshine strategy, wiki index, pass index, and tracker entries so the new 2026-05-05 freshness layer and validation bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same closed-world `ContentOracle` family split, and Starshine still keeps plain `gufa` boundary-only until a real module-wide oracle owner exists.

## [2026-05-05] health | refresh `signature-pruning` current-main bridge and source hygiene

- Added `docs/wiki/raw/binaryen/2026-05-05-signature-pruning-current-main-recheck.md` plus `docs/wiki/raw/research/0470-2026-05-05-signature-pruning-current-main-recheck.md` after rechecking current `main` `SignaturePruning.cpp`, `pass.cpp`, and `test/lit/passes/signature-pruning.wast` against the existing `version_129` contract.
- Refreshed the `signature-pruning` overview, Binaryen strategy, implementation/test-map, constant-actuals/boundaries, WAT-shape, Starshine strategy, Starshine port-readiness bridge, wiki index, pass index, and tracker entries so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same closed-world heap-type-level pruning story, and Starshine still keeps `signature-pruning` boundary-only until a module/type rewrite port exists.

## [2026-05-05] health | refresh `optimize-casts` current-main bridge and freshness layer

- Added `docs/wiki/raw/binaryen/2026-05-05-optimize-casts-current-main-recheck.md` plus `docs/wiki/raw/research/0469-2026-05-05-optimize-casts-current-main-recheck.md` after rechecking current `main` `OptimizeCasts.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, `linear-execution.h`, `properties.h`, `effects.h`, `utils.h`, and `optimize-casts.wast` against the existing `version_129` contract.
- Refreshed the `optimize-casts` overview, Binaryen strategy, implementation/test-map, two-phase, WAT-shape, Starshine strategy, shared catalogs, and wiki index / tracker entries so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same GC-gated two-phase `ref.cast` / `ref.as_non_null` local-flow pass, and Starshine still keeps `optimize-casts` removed-registry only while the `OC` backlog caveat remains explicit.

## [2026-05-05] health | refresh `precompute` current-main bridge and freshness layer

- Added `docs/wiki/raw/binaryen/2026-05-05-precompute-current-main-recheck.md` plus `docs/wiki/raw/research/0468-2026-05-05-precompute-current-main-recheck.md` after rechecking current `main` `Precompute.cpp`, `pass.cpp`, `opt-utils.h`, and representative `precompute*.wast` surfaces against the existing `version_129` contract.
- Refreshed the `precompute` overview, Binaryen strategy, implementation/test-map, Starshine strategy, Starshine port-readiness bridge, shared catalogs, and wiki index / tracker entries so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same shared `Precompute.cpp` semantic-evaluator core and plain-vs-propagate split, and Starshine still keeps `precompute` as an active HOT fold-and-cleanup fixpoint rather than a full interpreter port.

## [2026-05-05] health | refresh `global-type-optimization` current-main bridge and add port-readiness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-global-type-optimization-current-main-recheck.md` plus `docs/wiki/raw/research/0467-2026-05-05-global-type-optimization-current-main-recheck.md` after rechecking current `main` `GlobalTypeOptimization.cpp`, `pass.cpp`, and `gto_and_cfp_in_O.wast` against the existing `version_129` contract.
- Refreshed the `global-type-optimization` overview, Binaryen strategy, implementation/test-map, field-removal, WAT-shape, Starshine strategy, new Starshine port-readiness bridge, shared catalogs, and wiki index / tracker entries so the new 2026-05-05 freshness layer and the missing local module/type-graph bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same closed-world `gto` struct-layout cleanup story, and Starshine still keeps `global-type-optimization` boundary-only while the future module/type-graph port remains explicit.

## [2026-05-05] health | refresh `signext-lowering` current-main bridge and source anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-signext-lowering-current-main-recheck.md` plus `docs/wiki/raw/research/0466-2026-05-05-signext-lowering-current-main-recheck.md` after rechecking current `main` `SignExtLowering.cpp`, `pass.cpp`, `passes.h`, and `signext-lowering.wast` against the existing `version_129` contract.
- Refreshed the `signext-lowering` overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, Starshine strategy, Starshine port-readiness bridge, shared catalogs, and wiki index / tracker entries so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same five-opcode shift-pair feature-lowering story, and Starshine still keeps `signext-lowering` removed from the registry while the feature-metadata decision remains explicit.

## [2026-05-05] health | refresh `optimize-added-constants` source anchors and Starshine code map

- Added `docs/wiki/raw/binaryen/2026-05-05-optimize-added-constants-current-main-recheck.md` plus `docs/wiki/raw/research/0465-2026-05-05-optimize-added-constants-current-main-recheck.md` after rechecking the shared `OptimizeAddedConstants.cpp` owner file, public registration, low-memory threshold, and plain-pass tests against the existing `version_129` contract.
- Refreshed the plain `optimize-added-constants` dossier, its low-memory / offset-merge companion page, implementation/test-map, WAT-shape catalog, Starshine strategy, Starshine port-readiness bridge, shared catalogs, and the pass tracker so the new 2026-05-05 source-anchor digest and exact Starshine code locations are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same direct memory-address offset-folding half of the shared pass family, and Starshine still keeps both `optimize-added-constants` spellings removed while the first mutating slice remains future work.

## [2026-05-05] health | add `local-cse` port-readiness bridge and validation layer

- Added `docs/wiki/raw/research/0464-2026-05-05-local-cse-port-readiness-and-validation.md` after reviewing the existing Binaryen `local-cse` source/test bridge and the nearest Starshine cleanup/rewrite surfaces.
- Added `docs/wiki/binaryen/passes/local-cse/starshine-port-readiness-and-validation.md` and refreshed the `local-cse` index, Starshine strategy, implementation/test-map, and shared catalogs so the dossier now has a dedicated implementation-readiness bridge in addition to the existing strategy and shape pages.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the new readiness bridge is discoverable from the shared catalogs.
- Recorded unchanged contract status: `local-cse` remains removed-registry only in Starshine, and the new bridge stays conservative about validation ordering until the missing local neighbors exist.

## [2026-05-05] health | refresh `rse` current-main bridge and exact local code map

- Added `docs/wiki/raw/binaryen/2026-05-05-rse-current-main-recheck.md` plus `docs/wiki/raw/research/0463-2026-05-05-rse-current-main-recheck.md` after rechecking current `main` `RedundantSetElimination.cpp`, `pass.cpp`, `rse_all-features.wast`, and `rse-gc.wast` against the corrected `version_129` contract.
- Refreshed the `rse` landing page, Binaryen strategy, implementation/test-map, CFG/value-tracking, WAT-shape, Starshine strategy, and Starshine port-readiness pages so the new 2026-05-05 freshness bridge and the exact local registry/dispatcher/owner/test anchors are visible from the living dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance and exact local code map are visible from the shared catalogs.
- Recorded unchanged contract status: current `main` stayed aligned with the same CFG/value-flow same-value-shell-removal story on the reviewed surfaces, and Starshine still keeps the direct active slice plus the remaining CFG/refined-get follow-up.

## [2026-05-05] health | refresh `type-merging` current-main bridge and add Starshine port-readiness page

- Added `docs/wiki/raw/binaryen/2026-05-05-type-merging-current-main-recheck.md` plus `docs/wiki/raw/research/0462-2026-05-05-type-merging-current-main-recheck.md` after rechecking current `main` `TypeMerging.cpp`, `pass.cpp`, and `type-merging.wast` against the existing `version_129` contract.
- Added `docs/wiki/binaryen/passes/type-merging/starshine-port-readiness-and-validation.md` and refreshed the `type-merging` landing page, Starshine strategy, shared catalogs, and wiki index so the new 2026-05-05 freshness layer and the missing port-readiness bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` stayed aligned with the same closed-world GC heap-type compaction story on the reviewed surfaces, and Starshine still keeps `type-merging` boundary-only with no owner file or active backlog slice.

## [2026-05-05] health | refresh `remove-unused-brs` current-main bridge and add Starshine strategy page

- Added `docs/wiki/raw/binaryen/2026-05-05-remove-unused-brs-current-main-recheck.md` plus `docs/wiki/raw/research/0461-2026-05-05-remove-unused-brs-current-main-recheck.md` after rechecking current `main` `RemoveUnusedBrs.cpp`, `pass.cpp`, and the representative `remove-unused-brs*` lit surfaces against the existing `version_129` contract.
- Added `docs/wiki/binaryen/passes/remove-unused-brs/starshine-strategy.md` and refreshed the landing page, Binaryen strategy, implementation/test-map, HOT-strategy companion, index, tracker, and shared catalogs so the 2026-05-05 freshness layer and the compact current-local strategy overview are visible from the living dossier.
- Recorded unchanged contract status: current `main` stayed aligned with the same staged structured-control cleanup story on the reviewed surfaces, and the already-tracked JumpThreader type-equality relaxation remains the only documented `main`-vs-`version_129` drift.

## [2026-05-05] health | refresh `discard-global-effects` current-main bridge and port-readiness

- Added `docs/wiki/raw/binaryen/2026-05-05-discard-global-effects-current-main-recheck.md` plus `docs/wiki/raw/research/0460-2026-05-05-discard-global-effects-current-main-recheck.md` after rechecking current `main` `GlobalEffects.cpp`, `pass.cpp`, and the Optimizer Cookbook against the existing cleanup-sibling contract.
- Added `docs/wiki/binaryen/passes/discard-global-effects/starshine-port-readiness-and-validation.md` and refreshed the discard-global-effects overview, Binaryen strategy, implementation/test-map, metadata-shape, Starshine strategy, wiki index, pass catalog, and tracker entries so the new 2026-05-05 freshness layer and missing cleanup bridge are visible from the living dossier.
- Recorded unchanged contract status: current `main` still clears the same function-level `Function.effects` summary state, while Starshine still has no registered cleanup sibling and only uses HOT revision-keyed effect-cache invalidation today.

## [2026-05-05] health | refresh `propagate-globals-globally` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-propagate-globals-globally-current-main-recheck.md` plus `docs/wiki/raw/research/0459-2026-05-05-propagate-globals-globally-current-main-recheck.md` after rechecking current `main` `SimplifyGlobals.cpp`, `pass.cpp`, and `propagate-globals-globally.wast` against the existing `version_129` contract.
- Refreshed the `propagate-globals-globally` overview, Binaryen strategy, implementation/test-map, startup-boundary guide, WAT-shape catalog, Starshine strategy, shared catalogs, and wiki index / tracker entries so the 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still exposes the same `PropagateGlobalsGlobally` subclass and startup-only propagation boundary, while Starshine remains boundary-only and unimplemented.

## [2026-05-05] health | refresh `remove-unused-non-function-elements` with current-main bridge and Binaryen strategy

- Added `docs/wiki/raw/binaryen/2026-05-05-remove-unused-non-function-elements-current-main-recheck.md` plus `docs/wiki/raw/research/0458-2026-05-05-remove-unused-non-function-elements-current-main-recheck.md` after rechecking current `main` `RemoveUnusedModuleElements.cpp`, `pass.cpp`, `passes.h`, `module-utils.h`, and the dedicated sibling fixture against the existing Binaryen `version_129` contract.
- Added a new `docs/wiki/binaryen/passes/remove-unused-non-function-elements/binaryen-strategy.md` page and refreshed the folder overview, implementation map, shared-engine boundary guide, module-shape catalog, Starshine strategy, Starshine port-readiness bridge, and shared catalogs so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still matches the same shared-engine sibling mode contract on the reviewed surfaces, while Starshine remains active module-pass sibling reuse with the same defined-vs-imported boundary.

## [2026-05-05] health | refresh `string-lifting` current-main bridge and freshness layer

- Added `docs/wiki/raw/binaryen/2026-05-05-string-lifting-current-main-recheck.md` plus `docs/wiki/raw/research/0457-2026-05-05-string-lifting-current-main-recheck.md` after rechecking current `main` `StringLifting.cpp`, `string-lifting.wast`, and `pass.cpp` against the existing Binaryen `version_129` source contract.
- Refreshed the `string-lifting` overview, Binaryen strategy, implementation/test-map, transformed-shape catalog, Starshine status page, Starshine port-readiness bridge, and shared catalogs so the new 2026-05-05 freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still matches the same import-lifting, helper-rewrite, refinalization, module-code, and Strings-enable contract, while Starshine remains upstream-only with the same missing lifted-op prerequisites.

## [2026-05-05] health | refresh `avoid-reinterprets` current-main bridge and direct-slice anchors

- Added `docs/wiki/raw/binaryen/2026-05-05-avoid-reinterprets-current-main-recheck.md` plus `docs/wiki/raw/research/0456-2026-05-05-avoid-reinterprets-current-main-recheck.md` after rechecking current `main` `AvoidReinterprets.cpp`, `pass.cpp`, `local-graph.h`, `properties.h`, and the two dedicated lit files against the existing Binaryen `version_129` source anchors.
- Refreshed the `avoid-reinterprets` overview, Binaryen strategy, implementation/test-map, single-load-chain guide, WAT-shape catalog, Starshine strategy, Starshine port-readiness page, and shared catalogs so the 2026-05-05 freshness layer plus exact landed direct-slice code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still matches the same narrow full-width load/reinterpret contract on the reviewed surfaces, while Starshine remains active-partial with the direct `reinterpret(load)` slice landed and the indirect helper-local family still future work.

## [2026-05-05] health | refresh `pick-load-signs` strategy and current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-pick-load-signs-current-main-recheck.md` plus `docs/wiki/raw/research/0455-2026-05-05-pick-load-signs-current-main-recheck.md` after rechecking current `main` `PickLoadSigns.cpp`, `pass.cpp`, `opt-utils.h`, `properties.h`, and `pick-load-signs_sign-ext.wast` against the existing Binaryen `version_129` source anchors.
- Added a dedicated `docs/wiki/binaryen/passes/pick-load-signs/starshine-strategy.md` page and refreshed the landing page, Binaryen strategy, implementation/test-map, WAT-shape, parity, and HOT-IR detail pages so the folder now separates upstream semantics from current Starshine status more cleanly.
- Recorded unchanged contract status: current `main` still matches the narrow upstream `version_129` contract on the reviewed surfaces, while Starshine keeps the broader i64 HOT implementation and its explicit parity watchpoint.

## [2026-05-05] health | refresh `code-pushing` current-main bridge and source cleanup

- Added `docs/wiki/raw/binaryen/2026-05-05-code-pushing-current-main-recheck.md` plus `docs/wiki/raw/research/0454-2026-05-05-code-pushing-current-main-recheck.md` after rechecking current `main` `CodePushing.cpp` and `pass.cpp` against the existing Binaryen `version_129` source anchors.
- Refreshed the `code-pushing` landing page, Binaryen strategy, implementation/test-map, Starshine strategy, Starshine port-readiness bridge, WAT-shape catalog, and shared catalogs so the new freshness layer and source manifest are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same `LocalAnalyzer` + `Pusher` SFA-local segment model and Starshine still keeps the explicit HOT subset narrower than upstream Binaryen.

## [2026-05-05] health | refresh `local-cse` current-main bridge and anchor cleanup

- Added `docs/wiki/raw/binaryen/2026-05-05-local-cse-current-main-recheck.md` plus `docs/wiki/raw/research/0453-2026-05-05-local-cse-current-main-recheck.md` after rechecking current `main` `LocalCSE.cpp`, `pass.cpp`, `opt-utils.h`, and `local-cse.wast`.
- Refreshed the `local-cse` landing page, Binaryen strategy, implementation/test-map, basic-block/window guide, WAT-shape catalog, Starshine strategy, and shared catalogs so the new freshness layer and current local code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same scan/check/apply temp-local reuse model and Starshine still keeps `local-cse` removed-registry only.

## [2026-05-05] health | refresh `dae2` current-main bridge and freshness cleanup

- Added `docs/wiki/raw/binaryen/2026-05-05-dae2-current-main-recheck.md` plus `docs/wiki/raw/research/0452-2026-05-05-dae2-current-main-recheck.md` after rechecking current `main` `DeadArgumentElimination2.cpp`, `pass.cpp`, `passes.h`, and `dae2.wast`.
- Refreshed the `dae2` landing page, Binaryen strategy, implementation/test-map, WAT-shape catalog, Starshine strategy, Starshine port-readiness bridge, and shared catalogs so the new freshness layer is visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same experimental fixed-point DAE model and Starshine still treats `dae2` as unknown, not boundary-only.

## [2026-05-05] health | tighten `merge-similar-functions` Starshine code anchors

- Refreshed `docs/wiki/binaryen/passes/merge-similar-functions/starshine-strategy.md` so the removed-registry, runtime rejection, and preset-omission claims now point at exact local anchors in `src/passes/optimize.mbt` (`pass_registry_entry_removed`, `pass_registry_removed_names`, `pass_registry_entries`, `run_hot_pipeline_expand_passes`, `optimize_preset_passes`, and `shrink_preset_passes`).
- Kept the existing current-main raw source bridge and upstream strategy pages intact; this pass still reads as Starshine removed-registry only, with no owner file and no active backlog slice.
- No new implementation work was added; this was a reference-hygiene update to make the current status easier to follow.

## [2026-05-05] health | refresh `signature-refining` current-main bridge and stale-anchor cleanup

- Added `docs/wiki/raw/binaryen/2026-05-05-signature-refining-current-main-recheck.md` plus `docs/wiki/raw/research/0451-2026-05-05-signature-refining-current-main-recheck.md` after rechecking current `main` `SignatureRefining.cpp`, `pass.cpp`, and the dedicated `signature-refining.wast` surface.
- Refreshed the `signature-refining` overview, Binaryen strategy, implementation/test-map, params/results/publicity guide, WAT-shape catalog, Starshine strategy, Starshine port-readiness page, and shared catalog entries so the fresh provenance and the corrected local code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same heap-type-wide param-LUB / result-LUB contract plus `call.without.effects` repair, and no teaching-relevant drift was found.

## [2026-05-05] health | refresh `minify-imports` current-main bridge and stale-anchor cleanup

- Added `docs/wiki/raw/binaryen/2026-05-05-minify-imports-current-main-recheck.md` plus `docs/wiki/raw/research/0450-2026-05-05-minify-imports-current-main-recheck.md` after rechecking current `main` `MinifyImportsAndExports.cpp`, `pass.cpp`, and the shared pass-family registration surface.
- Refreshed the `minify-imports` overview, Binaryen strategy, implementation/test-map, `env` / `wasi_` gate guide, WAT-shape catalog, Starshine strategy, Starshine port-readiness page, and shared catalog entries so the fresh provenance and the corrected local code anchors are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same ABI-visible import-base rewrite plus JSON map output contract and no teaching-relevant drift was found.

## [2026-05-05] health | refresh `dead-code-elimination` current-main bridge and Starshine strategy page

- Added `docs/wiki/raw/binaryen/2026-05-05-dead-code-elimination-current-main-recheck.md` plus `docs/wiki/raw/research/0449-2026-05-05-dead-code-elimination-current-main-recheck.md` after rechecking current `main` `DeadCodeElimination.cpp`, `pass.cpp`, and the dedicated `dce` lit surfaces.
- Refreshed the `dead-code-elimination` overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, Starshine strategy, Starshine HOT code-map, and shared catalog entries so the fresh provenance and the local HOT-vs-upstream split are visible from the living dossier.
- Recorded unchanged contract status: current `main` still teaches the same small `TypeUpdater`-centered unreachable-shape contract and no teaching-relevant drift was found.

## [2026-05-05] health | refresh `heap-store-optimization` code anchors and current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-heap-store-optimization-current-main-recheck.md` plus `docs/wiki/raw/research/0448-2026-05-05-heap-store-optimization-current-main-recheck.md` after rechecking current `main` `HeapStoreOptimization.cpp`, `pass.cpp`, and the dedicated lit test.
- Refreshed the `heap-store-optimization` overview, Binaryen strategy, implementation/test-map, swap-safety, WAT-shape, Starshine strategy, and shared catalog entries so the exact `optimize.mbt` / `pass_manager.mbt` line anchors now match the current tree.
- Recorded unchanged contract status: current `main` still teaches the same narrow constructor/store-folding contract and no teaching-relevant drift was found.

## [2026-05-05] health | refresh `local-subtyping` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-local-subtyping-current-main-recheck.md` plus `docs/wiki/raw/research/0447-2026-05-05-local-subtyping-current-main-recheck.md` after rechecking official Binaryen current-main `LocalSubtyping.cpp`, `pass.cpp`, and the dedicated `local-subtyping.wast` surface.
- Refreshed the `local-subtyping` overview, Binaryen strategy, implementation/test-map, LUB/dominance guide, WAT-shape catalog, Starshine strategy, shared catalogs, and wiki index so the new 2026-05-05 provenance is visible from the living dossier.
- Recorded unchanged local status: Starshine still keeps `local-subtyping` removed-registry only, with no dispatcher case and no implementation file.

## [2026-05-05] health | refresh `dataflow-optimization` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-dataflow-optimization-current-main-recheck.md` plus `docs/wiki/raw/research/0446-2026-05-05-dataflow-optimization-current-main-recheck.md` after rechecking official Binaryen current-main `DataFlowOpts.cpp`, `graph.h`, `node.h`, `users.h`, `utils.h`, `flat.h`, `pass.cpp`, and the combo `flatten_simplify-locals-nonesting_dfo_O3.wast` surface.
- Refreshed the `dataflow-optimization` overview, Binaryen strategy, implementation/test-map, Flat/DataFlow boundaries, WAT-shape catalog, Starshine strategy, and Starshine port-readiness pages so the new 2026-05-05 provenance is visible from the living dossier.
- Fixed stale local code-map anchors in the Starshine pages: the removed-pass guard now points at `src/passes/optimize.mbt#L522-L524`, and the removed-registry spelling anchor now points at `src/passes/optimize.mbt#L143-L146`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance is discoverable from the shared catalogs.

## [2026-05-05] health | refresh `unsubtyping` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-unsubtyping-current-main-recheck.md` plus `docs/wiki/raw/research/0444-2026-05-05-unsubtyping-current-main-recheck.md` after rechecking official Binaryen `main` `Unsubtyping.cpp`, `pass.cpp`, and the dedicated lit surfaces.
- Added `docs/wiki/binaryen/passes/unsubtyping/starshine-port-readiness-and-validation.md` and refreshed the `unsubtyping` landing page, Binaryen strategy, implementation/test-map, Starshine strategy, shared catalogs, and wiki index so the dossier now carries the missing port-readiness bridge and a 2026-05-05 freshness layer.
- Recorded unchanged local status: Starshine still keeps `unsubtyping` boundary-only, with no owner file, no dispatcher case, and no active backlog slice.

## [2026-05-05] health | refresh `asyncify` freshness bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-asyncify-current-main-recheck.md` plus `docs/wiki/raw/research/0445-2026-05-05-asyncify-current-main-recheck.md` after rechecking official Binaryen `main` `Asyncify.cpp`, `pass.cpp`, `asyncify.wast`, and the Emscripten Asyncify docs.
- Refreshed the `asyncify` landing page, Binaryen strategy, implementation/test-map, shape and boundary guides, Starshine strategy, port-readiness page, `docs/wiki/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the dossier now carries a 2026-05-05 freshness bridge and the living catalog points at the new raw sources.
- Recorded unchanged local status: Starshine still rejects `asyncify` as unknown, with no owner file, no dispatcher case, no preset role, and no active backlog slice.

## [2026-05-05] implementation | implement `coalesce-locals`

- Promoted `coalesce-locals` from removed-name tracking to an active direct module pass with registry, dispatcher, CLI, and pass-fuzz harness wiring.
- Added focused tests for active registration, non-overlap merging, interference negatives, redundant local copy cleanup, dead set/tee cleanup, and CLI adapter behavior.
- Recorded direct evidence: full `moon test` passes; `.tmp/pass-fuzz-cl-genvalid-10000-livecount` is `10000/10000` normalized matches; `.tmp/pass-fuzz-cl-mixed-1000-livecount` had `950/950` comparable mixed-generator matches with zero mismatches; `.tmp/self-opt-cl-debug-livecount` and `.tmp/self-opt-cl-optimized-livecount` are canonical-function equal against Binaryen 128, and the optimized-artifact direct pass timer is now faster than Binaryen after the live-count interference guard.

## [2026-05-05] health | refresh `merge-similar-functions` status and current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-merge-similar-functions-current-main-recheck.md` plus `docs/wiki/raw/research/0443-2026-05-05-merge-similar-functions-current-main-recheck.md` after rechecking official Binaryen `main` `MergeSimilarFunctions.cpp`, `pass.cpp`, helper headers, and dedicated lit tests.
- Added `docs/wiki/binaryen/passes/merge-similar-functions/starshine-port-readiness-and-validation.md` and refreshed the landing page, Binaryen strategy, implementation/test-map, mechanics, profitability, WAT-shape, Starshine strategy, shared index, tracker, and wiki catalog pages so the dossier now says the pass is removed-registry in Starshine rather than boundary-only.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the corrected status and new port-readiness bridge are discoverable from the shared catalogs.
- Recorded unchanged local status: Starshine still keeps `merge-similar-functions` unimplemented, with no owner file, no dispatcher case, and no active backlog slice.

## [2026-05-05] health | refresh `code-folding` current-main bridge

- Added `docs/wiki/raw/binaryen/2026-05-05-code-folding-current-main-recheck.md` plus `docs/wiki/raw/research/0442-2026-05-05-code-folding-current-main-recheck.md` after rechecking official Binaryen `main` `CodeFolding.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, and `code-folding.wast`.
- Refreshed the `code-folding` overview, Binaryen strategy, implementation/test-map, terminating-tail, WAT-shape, Starshine strategy, and Starshine port-readiness pages so the new freshness bridge is visible from the living dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the new current-main recheck is discoverable from the shared catalogs.

## [2026-05-05] implementation | tighten `vacuum` cleanup parity

- Extended Starshine's active `vacuum` hot pass beyond recursive `nop` cleanup with dropped nontrapping pure-result pruning and block-only `unreachable` unwrapping.
- Added focused pass tests for dropped pure arithmetic with preserved local writes and block-only `unreachable` cleanup.
- Recorded green direct evidence: `.tmp/pass-fuzz-vacuum-genvalid-10000-after-drop-block` is `10000/10000` normalized matches, and `.tmp/pass-fuzz-vacuum-1000-after-drop-block` has `131/131` comparable mixed-generator matches before Binaryen-side command failures hit the cap.

## [2026-05-04] health | correct `merge-locals` source contract

- Added `docs/wiki/raw/binaryen/2026-05-04-merge-locals-current-main-recheck.md` plus `docs/wiki/raw/research/0441-2026-05-04-merge-locals-current-main-recheck.md` after rechecking official Binaryen current-main `MergeLocals.cpp`, `pass.cpp`, `passes.h`, `local-graph.h`, and `merge-locals.wast`.
- Rewrote the `merge-locals` overview, Binaryen strategy, implementation/test-map, LocalGraph, WAT-shape, and Starshine strategy pages so the dossier now teaches the source-backed copy-shape local.set/local.get balancing contract with `LocalGraph` orientation and rollback, not the stale one-set-local / `EquivalentCopies` / `LocalStructuralDominance` overcorrection.
- Refreshed `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the corrected copy-balancing dossier is visible from the shared catalogs.

## [2026-05-04] health | deepen `precompute-propagate` with readiness bridge

- Added a 2026-05-04 current-main recheck raw manifest plus a matching research note for upstream Binaryen `precompute-propagate`.
- Added `docs/wiki/binaryen/passes/precompute-propagate/starshine-port-readiness-and-validation.md` so the removed-name status page now has a concrete first-slice / validation bridge.
- Refreshed the `precompute-propagate` landing page, Binaryen strategy, implementation/test map, local-worklist page, WAT-shape catalog, Starshine strategy, shared index, pass tracker, and catalog notes so the new freshness bridge is visible from the living dossier.

## [2026-05-04] implementation | activate `simplify-locals-notee-nostructure`

- Promoted Binaryen's exact `simplify-locals-notee-nostructure` spelling from saved-`-O4z` missing pass to an active direct Starshine HOT pass.
- Wired registry, dispatcher, pass-fuzz listing, and focused pass tests; the shared locals engine now has an `allowTee=false` mode so this sibling can sink single-use locals without creating fresh `local.tee`s or structure-result rewrites.
- Refreshed the pass dossier, Starshine strategy, port-readiness page, shared index, and tracker to record active direct status, explicit preset-neighborhood readiness gating, 10k fuzz parity evidence, repaired `gen-valid` parity evidence, Binaryen-128 self-opt parity evidence, and the still-deferred aggressive `flatten -> simplify-locals-notee-nostructure -> local-cse` preset neighborhood.

## [2026-05-04] health | sharpen `dead-argument-elimination` code anchors

- Tightened the plain `dead-argument-elimination` dossier with exact `src/passes/optimize.mbt` line anchors for the boundary-only registry list, boundary-only entry builder, and hot-pipeline rejection path.
- Refreshed the Starshine strategy and port-readiness pages, plus the shared wiki catalogs, so the exact local code-map anchors are visible from the living dossier.
- Recorded unchanged local status: Starshine still keeps plain DAE boundary-only, with no exact `dae` alias and no active preset role.

## [2026-05-04] health | refresh `reorder-functions` current-main bridge

- Rechecked `reorder-functions` against current `main` and found no teaching-relevant drift from the existing static-count declaration-order contract.
- Added `docs/wiki/raw/binaryen/2026-05-04-reorder-functions-current-main-recheck.md` and `docs/wiki/raw/research/0439-2026-05-04-reorder-functions-current-main-recheck.md`, then added `docs/wiki/binaryen/passes/reorder-functions/starshine-port-readiness-and-validation.md` and refreshed the landing page, Binaryen strategy, implementation/test map, module-shape catalog, Starshine strategy, `docs/wiki/index.md`, and `docs/wiki/binaryen/passes/index.md` so the new freshness bridge is visible from the living dossier and shared catalogs.
- Recorded unchanged local status: Starshine still treats `reorder-functions` as boundary-only with no owner file, no module dispatcher case, and no active backlog slice.

## [2026-05-04] health | refresh `reorder-types` current-main freshness

- Rechecked `reorder-types` against current `main` and found no teaching-relevant drift from the existing GC-only, `--closed-world`, private-type layout contract.
- Added `docs/wiki/raw/binaryen/2026-05-04-reorder-types-current-main-recheck.md` and `docs/wiki/raw/research/0438-2026-05-04-reorder-types-current-main-recheck.md`.
- Refreshed the landing page, Binaryen strategy, implementation/test map, cost-model/boundaries page, WAT-shape catalog, Starshine strategy, `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the current-main freshness layer is visible from the living dossier and shared catalogs.
- Recorded unchanged local status: Starshine still treats `reorder-types` as boundary-only with no owner file, no module dispatcher case, and no active backlog slice.

## [2026-05-04] health | refresh `global-effects` current-main bridge

- Revisited `docs/wiki/binaryen/passes/global-effects/` after a fresh current-main source recheck confirmed the existing metadata-only / `Function.effects` contract still matches current `main` on the reviewed surfaces.
- Added `docs/wiki/raw/binaryen/2026-05-04-global-effects-current-main-recheck.md` and `docs/wiki/raw/research/0438-2026-05-04-global-effects-current-main-recheck.md`, then refreshed the landing page, Binaryen strategy, implementation/test-map, metadata-naming guide, Starshine strategy, and Starshine port-readiness page so the new freshness bridge is visible from the whole dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the new current-main recheck is discoverable from the shared catalogs.
- Recorded unchanged contract status: Starshine still treats `global-effects` as boundary-only, and the follow-up port guidance remains analyzer-first summary production, separate read/write facts, SCC-friendly propagation, consumer sequencing, and explicit invalidation/discard planning.

## [2026-05-04] health | refresh `remove-relaxed-simd` current-main bridge

- Revisited `docs/wiki/binaryen/passes/remove-relaxed-simd/` after a fresh current-main source recheck confirmed the existing trap-replacement / child-effect-preservation contract still matches current `main`.
- Added `docs/wiki/raw/binaryen/2026-05-04-remove-relaxed-simd-current-main-recheck.md` and `docs/wiki/raw/research/0437-2026-05-04-remove-relaxed-simd-current-main-recheck.md`, then refreshed the landing page, Binaryen strategy, implementation/test-map, Starshine strategy, WAT-shape catalog, and Starshine port-readiness page so the freshness bridge is visible from the whole dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the new current-main recheck is discoverable from the shared catalogs.
- Recorded unchanged contract status: Starshine still has no registered `remove-relaxed-simd` pass, and the follow-up port guidance remains registry honesty, child-effect preservation, typed `v128` trap contexts, ordinary-SIMD preservation, and explicit dot-product spelling decisions.

## [2026-05-04] health | correct `merge-blocks` upstream contract and add Starshine strategy page

- Revisited `docs/wiki/binaryen/passes/merge-blocks/` after a fresh current-main source refresh showed the upstream pass is still the block-merging / loop-tail-merging family rather than the stale named-block deblocking model.
- Added `docs/wiki/raw/binaryen/2026-05-04-merge-blocks-current-main-refresh.md` and `docs/wiki/raw/research/0436-2026-05-04-merge-blocks-current-main-refresh.md`, then refreshed the landing page, Binaryen strategy, implementation/test-map, WAT-shape catalog, dedicated Starshine strategy page, and exact Starshine code-map page so the corrected contract and local code anchors are visible from the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed contract and new Starshine strategy page are discoverable from the shared catalogs.
- Recorded unchanged local status: Starshine `merge-blocks` remains an active HOT-region cleanup pass; the correction only changes the upstream Binaryen teaching frame.

## [2026-05-04] health | refresh `tuple-optimization` strategy and current-main bridge

- Revisited `docs/wiki/binaryen/passes/tuple-optimization/` as part of a wiki-maintenance pass: added a new 2026-05-04 current-main recheck raw manifest plus a matching research note, then refreshed the landing page, Binaryen strategy, implementation/test map, WAT-shape catalog, scheduler/gates page, parity page, reduced-repros page, implementation map, and the new Starshine strategy page so the direct-pass status, exact code anchors, and freshness bridge are all visible from the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and the tuple-opt living pages so the current-main bridge and the new Starshine strategy page are discoverable from the shared catalogs instead of only from the implementation map.
- Recorded unchanged contract status: Starshine still treats `tuple-optimization` as an active direct hot pass with explicit-pass oracle evidence, while public preset placement remains intentionally deferred until the exact Binaryen slot can be represented honestly.

## [2026-05-04] health | sync `simplify-locals-nostructure` catalogs after direct-pass activation

- Revisited `docs/wiki/binaryen/passes/simplify-locals-nostructure/` and the shared pass catalogs after the direct-pass status flipped active in the living dossier: refreshed the pass index / tracker source lists around the existing 2026-05-04 current-main recheck raw source so the active alias, direct parity evidence, and current-main freshness are visible from the catalog layer.
- Updated `docs/wiki/binaryen/passes/index.md` and `docs/wiki/binaryen/passes/tracker.md` so the old removed-name wording no longer lingers in the shared catalogs.
- Recorded unchanged contract status: Starshine treats `simplify-locals-nostructure` as an active direct hot pass with alias `simplify-locals-no-structure`, while public preset placement still remains conservative until the broader ordered local-neighborhood replay is claimed.

## [2026-05-04] health | refresh `dead-argument-elimination` current-main freshness

- Revisited `docs/wiki/binaryen/passes/dead-argument-elimination/` as part of a wiki-maintenance pass: added a fresh 2026-05-04 current-main recheck raw manifest plus a matching research note, then refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape, Starshine strategy, and Starshine validation bridge pages so the new freshness layer is discoverable from the living dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the newer current-main freshness layer is visible from the shared catalogs.
- Recorded unchanged contract status: plain `dead-argument-elimination` still shares `DeadArgumentElimination.cpp` with `dae-optimizing`, still treats `dae2.wast` as a separate sibling, and still has no teaching-relevant current-main drift.

## [2026-05-04] health | connect `de-nan` port-readiness bridge

- Revisited `docs/wiki/binaryen/passes/de-nan/` as part of a wiki-maintenance pass: added the missing `starshine-port-readiness-and-validation.md` bridge, then refreshed the landing, Binaryen strategy, implementation/test-map, helper-boundary, WAT-shape, and Starshine strategy pages so the validation ladder is discoverable from every maintained page in the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed `de-nan` provenance and the new port-readiness bridge stay visible from the shared catalogs.
- Recorded unchanged contract status: `de-nan` is still only a removed registry entry in Starshine, and the future port still needs module-owned instrumentation with helper-call effects, entry-param sanitization, result-fallthrough skipping, and nonfunction-context legality handled explicitly.

## [2026-05-04] health | retire stale `simplify-locals-nostructure` neighbor wording

- Revisited `docs/wiki/binaryen/passes/code-pushing/` and `docs/wiki/binaryen/passes/tuple-optimization/` after the direct `simplify-locals-nostructure` landing: removed the stale “missing neighbor” wording, clarified that the local no-structure pass is now active, and kept public preset omission framed as an ordered-neighborhood / exact-slot proof question instead of an availability gap.
- Updated the `code-pushing` and `tuple-optimization` living pages so their scheduler explanations now match the active `simplify-locals-nostructure` status and the remaining preset-parity work.

## [2026-05-04] health | add `simplify-locals-notee-nostructure` validation bridge

- Revisited `docs/wiki/binaryen/passes/simplify-locals-notee-nostructure/` as part of a wiki-maintenance pass: added a dedicated port-readiness / validation bridge, aligned the folder's page map and cross-links, and refreshed the shared catalogs so the missing companion page is now discoverable.
- Added `docs/wiki/binaryen/passes/simplify-locals-notee-nostructure/starshine-port-readiness-and-validation.md` using the existing immutable raw primary-source manifest and the current Starshine status / implementation pages as the source base.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `simplify-locals-notee-nostructure` landing / strategy / implementation pages so the validation bridge is cross-linked from the maintained dossier.
- Recorded the then-current contract status before implementation: `simplify-locals-notee-nostructure` was still unimplemented in Starshine and still used the local removed alias `simplify-locals-no-tee-no-structure`; the later same-day implementation entry supersedes that status.

## [2026-05-04] health | refresh `simplify-locals-nostructure` source anchors

- Revisited `docs/wiki/binaryen/passes/simplify-locals-nostructure/` as part of a wiki-maintenance pass: updated the overview, Binaryen strategy, implementation/test-map, variant-surface, WAT-shape, Starshine strategy, and catalog pages with a consistent `2026-05-04` `last_reviewed` stamp and a new current-main raw source recheck.
- Added `docs/wiki/raw/binaryen/2026-05-04-simplify-locals-nostructure-current-main-recheck.md` and `docs/wiki/raw/research/0433-2026-05-04-simplify-locals-nostructure-current-main-recheck.md` as the new current-main no-drift checkpoints for the dossier.
- Added `docs/wiki/binaryen/passes/simplify-locals-nostructure/starshine-port-readiness-and-validation.md` so the folder now has the usual implementation-readiness bridge alongside the strategy page.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance is discoverable from the shared catalogs.
- Recorded unchanged contract status: `simplify-locals-nostructure` still matches the source-backed `SimplifyLocals<true, false, true>` contract on the reviewed surfaces, and the 2026-05-04 recheck did not surface teaching-relevant drift.

## [2026-05-04] health | refresh `gufa-cast-all` source anchors

- Revisited `docs/wiki/binaryen/passes/gufa-cast-all/` as part of a wiki-maintenance pass: updated the overview, Binaryen strategy, implementation/test-map, cast-boundary page, WAT-shape, Starshine strategy, and catalog pages with a consistent `2026-05-04` `last_reviewed` stamp and a new current-main raw source recheck.
- Added `docs/wiki/raw/binaryen/2026-05-04-gufa-cast-all-current-main-recheck.md` and `docs/wiki/raw/research/0432-2026-05-04-gufa-cast-all-current-main-recheck.md` as the new current-main no-drift checkpoints for the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance is discoverable from the shared catalogs.
- Recorded unchanged contract status: `gufa-cast-all` still matches the source-backed `version_129` algorithm on the reviewed surfaces, and the 2026-05-04 recheck did not surface teaching-relevant drift.

## [2026-05-04] health | refresh `duplicate-function-elimination` source anchors

- Revisited `docs/wiki/binaryen/passes/duplicate-function-elimination/` as part of a wiki-maintenance pass: updated the overview, Binaryen strategy, implementation/test-map, WAT-shape, Starshine strategy, scheduler/validation bridge, parity page, and type-compaction split with a consistent `2026-05-04` `last_reviewed` stamp and a new current-main raw source recheck.
- Added `docs/wiki/raw/binaryen/2026-05-04-duplicate-function-elimination-current-main-recheck.md` as the new current-main no-drift checkpoint for the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance is discoverable from the shared catalogs.
- Recorded unchanged contract status: `duplicate-function-elimination` still matches the source-backed `version_129` algorithm on the reviewed surfaces, and the only checked current-main drift remains the non-semantic `std::set<Name>` -> `std::unordered_set<Name>` container change.

## [2026-05-04] health | refresh `duplicate-import-elimination` source anchors

- Revisited `docs/wiki/binaryen/passes/duplicate-import-elimination/` as part of a wiki-maintenance pass: updated the overview, Binaryen strategy, implementation/test-map, identity/rewrite-surface, WAT-shape, Starshine strategy, and catalog pages with a consistent `2026-05-04` `last_reviewed` stamp and the current raw source bridge.
- Added `docs/wiki/raw/binaryen/2026-05-04-duplicate-import-elimination-current-main-recheck.md` as the new current-main no-drift checkpoint for the dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the refreshed provenance is discoverable from the shared catalogs.
- Recorded unchanged contract status: `duplicate-import-elimination` still matches the function-import-only `version_129` contract on the reviewed surfaces, and no teaching-relevant current-main drift was found.

## [2026-05-04] health | refresh `string-gathering` source anchors

- Revisited `docs/wiki/binaryen/passes/string-gathering/` as part of a wiki-maintenance pass: updated the overview, Binaryen strategy, implementation/test-map, reuse/order, WAT-shape, Starshine strategy, and port-readiness pages with a consistent `2026-05-04` `last_reviewed` stamp and exact local code anchors.
- Added `docs/wiki/raw/binaryen/2026-05-04-string-gathering-current-main-recheck.md` and `docs/wiki/raw/research/0431-2026-05-04-string-gathering-current-main-recheck.md` as the new current-main no-drift / anchor-refresh checkpoints for the `string-gathering` dossier.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the refreshed provenance is discoverable from both catalogs.
- Recorded unchanged contract status: `string-gathering` remains the active direct pass, still separate from `reorder-globals`, and no teaching-relevant current-main drift was found.

## [2026-04-30] health | refresh `rereloop` wiki consistency

- Revisited `docs/wiki/binaryen/passes/rereloop/` as part of a wiki-maintenance pass: updated the pass's overview, Binaryen strategy, Starshine strategy, boundaries, and WAT-shape pages with a consistent `2026-04-30` `last_reviewed` stamp and a new immutable current-main source manifest.
- Added [`docs/wiki/raw/binaryen/2026-04-30-rereloop-current-main-refresh.md`](docs/wiki/raw/binaryen/2026-04-30-rereloop-current-main-refresh.md) as the primary-source refresh checkpoint for the `rereloop` dossier.
- Updated `docs/wiki/index.md` and the rereloop folder source lists so the new spot-check provenance is discoverable from all maintained entry points.
- Recorded unchanged local status: still removed-name tracking (`re-reloop`) with active request rejection, no owner file, and no active backlog slice.

## [2026-05-05] implementation | attach rich GC field-plan ledger coverage

- Completed `[FZG]019` by attaching `RichGcFieldPlans` to `GenValidFeatureStats` and the public validate feature ledger.
- The coverage-forced type-section scan now reports rich GC field plans from generated multi-field structs plus packed/ref struct and array field plans, with focused floor coverage in `gen_valid coverage-forced emits rich GC field plans`.
- Validation evidence: `moon fmt` and `moon info` passed with existing warnings; `moon test --package jtenner/starshine/validate` is currently blocked by existing fixed-seed coverage-forced generator validation failures (`No type exists for TypeIdx`, descriptor/describes agreement, and struct field-index issues) across pre-existing FZG tests.

## [2026-05-05] implementation | widen GC constructor/accessor gen-valid surface

- Completed `[FZG]011` by adding coverage-forced valid generation for non-default `struct.new`, non-default `array.new`, `array.new_fixed`, packed `struct.get_s`/`struct.get_u`, packed `array.get_s`/`array.get_u`, `array.fill`, and `array.copy` behind the existing `allow_ref_types` gate.
- Attached `GcConstructors` and `GcAccessors` to the public generator ledger and added focused validation/floor coverage for the widened GC constructor/accessor surface.
- Validation was blocked in this environment because `moon` was missing and the locally downloaded MoonBit toolchain could not load `moonbitlang/core`; follow-up validation should run `moon test --package jtenner/starshine/validate`, `moon info`, `moon fmt`, full `moon test`, and the standard `remove-unused-module-elements` gen-valid compare smoke.
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered rows.

## [2026-05-01] research | deepen `ssa-nomerge` implementation map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/ssa-nomerge/` folder, and `docs/wiki/raw/research/`, then chose active hot pass `ssa-nomerge` because it had strategy and shape coverage but lacked the standard implementation/test-map page.
- Added [`raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md`](raw/binaryen/2026-05-01-ssa-nomerge-implementation-primary-sources.md), citing official Binaryen `version_129` `SSAify.cpp`, `LocalGraph`, `ReFinalize`, pass registration, the dedicated no-merge golden pair, shared `ssa.wast`, and LocalGraph gtests plus exact local Starshine implementation/test surfaces.
- Added [`raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md`](raw/research/0431-2026-05-01-ssa-nomerge-implementation-structure.md) and [`binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md`](binaryen/passes/ssa-nomerge/implementation-structure-and-tests.md); refreshed the overview, Binaryen strategy, Starshine strategy, and parity page so upstream owner files, proof surfaces, local HOT-SSA destruction, raw fallbacks, CLI artifact tests, and the Starshine-vs-Binaryen representation split are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so `ssa-nomerge` is discoverable as a deep implemented hot-pass dossier with implementation/test-map coverage.

## [2026-05-01] implementation | widen i31/extern gen-valid surface

- Completed `[FZG]010` by adding coverage-forced valid generation for `ref.i31`, `i31.get_s`, `i31.get_u`, `any.convert_extern`, and `extern.convert_any` behind the existing `allow_ref_types` gate.
- Attached `I31ExternConversions` to the public generator ledger and added focused validation/floor coverage for the widened i31/extern conversion surface.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate`, full `moon test`, and `.tmp/pass-fuzz-genvalid-fzg010-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen basic ref gen-valid surface

- Completed `[FZG]009` by adding coverage-forced valid generation for `ref.is_null`, `ref.null none`, `ref.null nofunc`, `ref.null noextern`, nullable and nonnullable `ref.test`/`ref.cast`, and nullable/nonnullable-target `br_on_cast`/`br_on_cast_fail`.
- Attached `BasicRefInstructions` to the public generator ledger and added focused validation/floor coverage for the widened basic reference instruction surface.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate` and `.tmp/pass-fuzz-genvalid-fzg009-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen const-expression gen-valid surface

- Completed `[FZG]008` by adding valid const-expression variation to `gen-valid`: immutable imported and previously-defined globals feed `global.get`, `ref.func` appears in a global initializer, a safe struct default constructor appears in a global initializer, table initializers can use immutable ref globals, and active element/data offsets can use immutable `i32` globals.
- Added `allow_const_expr_variants`, attached `ConstExprVariants` to the public generator ledger, and added focused validation/floor coverage for the widened const-expression sites.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate`, full `moon test`, and `.tmp/pass-fuzz-genvalid-fzg008-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen table limit/initializer gen-valid surface

- Completed `[FZG]007` by adding valid zero-min and unbounded table limits, externref and GC/reference-typed tables, and table initializer expressions to `gen-valid`.
- Added `allow_table_limit_variants`, attached `TableLimitVariants` to the public generator ledger, and added focused validation/floor coverage plus a toggle-off regression.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate`, full `moon test`, and `.tmp/pass-fuzz-genvalid-fzg007-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen memory limit/proposal gen-valid surface

- Completed `[FZG]006` by adding valid zero-min, unbounded, shared, and memory64 memory shapes to `gen-valid`, guarded by explicit feature toggles.
- Attached `MemoryLimitVariants` to the public generator ledger and added focused validation/floor coverage for all four shapes plus a toggle-off regression.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate`, full `moon test`, and `.tmp/pass-fuzz-genvalid-fzg006-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen scalar memory gen-valid surface

- Completed `[FZG]005` by adding deterministic coverage-forced generation for every scalar load/store width/sign variant with varied valid nonzero memarg alignment or offset immediates.
- Attached `ScalarMemoryWidths` and `NonzeroMemarg` to the public generator ledger and added focused validation/floor coverage for those rows.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate` and `.tmp/pass-fuzz-genvalid-fzg005-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered rows and signoff evidence.

## [2026-05-01] implementation | widen tail-call gen-valid surface

- Completed `[FZG]004` by adding coverage-forced valid generation for `return_call`, `return_call_indirect`, and `return_call_ref` using callable signatures whose result lists exactly match the current function return type.
- Added `allow_tail_calls` to `GenValidFeatureToggles`, attached a `tail_calls` feature-stat counter to the public ledger, and added focused validation/floor coverage that proves all three tail-call forms appear in a validating coverage-forced module.
- Validated the widened corpus with `moon test --package jtenner/starshine/validate`, full `moon test`, and `.tmp/pass-fuzz-genvalid-fzg004-rume` (`remove-unused-module-elements` over 1000 `gen-valid` cases, `1000/1000` normalized matches).
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md), `agent-todo.md`, and `CHANGELOG.md` with the new covered row and signoff evidence.

## [2026-05-01] implementation | widen core control gen-valid surface

- Completed `[FZG]003` by wiring exact ledger/floor coverage for the deterministic coverage-forced core-control prelude covering `br_table`, standalone `unreachable`, `local.tee`, and typed `select` while keeping modules accepted by `validate_module`.
- Attached exact ledger counters for `BrTable`, `StandaloneUnreachable`, `LocalTee`, and `TypedSelect`, plus focused validator/floor coverage for those rows.
- Tightened the invalid-AST memory-name mutation to choose an index beyond imported plus defined memories after the widened fixed-seed corpus exposed the old defined-memory-only count.
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md) so the current covered rows distinguish `[FZG]002` scalar numeric coverage from `[FZG]003` core-control coverage.

## [2026-04-30] implementation | implement `string-gathering`

- Landed the active direct `string-gathering` module pass for the no-DWARF / O4z late-tail queue.
- Added `src/passes/string_gathering.mbt` plus focused behavior tests for registry status, no-op behavior, string literal hoisting, byte-level deduplication, imported/defined global remapping, function-before-global scan order, and nested structured bodies.
- Wired registry, dispatcher, CLI adapter coverage, generated pass API snapshot, and pass-fuzz harness support while keeping public preset scheduling deferred until the surrounding late tail can be replayed honestly.
- Updated `agent-todo.md`, `CHANGELOG.md`, and the `string-gathering` wiki pages/tracker with direct pass-fuzz and debug-artifact compare evidence.

## [2026-04-30] implementation | widen scalar numeric gen-valid surface

- Landed `[FZG]002` by adding a deterministic coverage-forced scalar numeric prelude for integer div/rem/shift/rotate, integer and float comparisons, float ceil/floor/trunc/nearest/sqrt/min/max/copysign, unsigned conversions, sign-extension, and saturating truncation families.
- Attached the first widened-surface ledger counter: `NumericFullOps` now reports nonzero coverage when the module scan sees expanded scalar numeric instructions, and a focused validator test proves a requested `NumericFullOps` floor passes on a validating coverage-forced module.
- Signoff included `moon info`, full `moon test`, and a 1k `gen-valid` compare smoke through `remove-unused-module-elements` with `1000/1000` normalized matches.
- Updated [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md) so later FZG slices distinguish still-zero future rows from the now-covered scalar numeric row.

## [2026-04-30] implementation | add generator coverage ledger

- Landed `[FZG]001` by adding a public validate-side feature ledger that reports stable rows for the current and planned generator-widening surfaces, including optional zero-count rows for future families and required-missing rows only when a profile explicitly asks for a floor.
- Added focused validator tests for optional missing FZG rows and future-row floor failures.
- Added [`fuzzing/generator-coverage-ledger.md`](fuzzing/generator-coverage-ledger.md) and indexed it so later FZG slices can attach exact counters and profile floors without rediscovering the reporting contract.

## [2026-04-27] health | refresh `reorder-locals` code pointers

- Ran a focused touched-area reference-hygiene check after adding the `reorder-locals` validation bridge.
- Updated the existing Starshine strategy code map so the registry, module dispatcher, registry-category test, and CLI adapter line pointers match the current in-tree locations already cited by the new validation bridge.

## [2026-04-27] research | deepen `reorder-locals` validation bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/reorder-locals/` folder, and `docs/wiki/raw/research/`, then chose active module pass `reorder-locals` because its source-correct dossier still lacked a compact explicit-pass-vs-preset validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-reorder-locals-validation-primary-sources.md`, rechecking official Binaryen current-main `ReorderLocals.cpp`, `pass.cpp`, and dedicated semantic / print-roundtrip fixtures plus exact local Starshine registry, dispatcher, implementation, focused tests, preset-exclusion test, registry-category test, and CLI adapter coverage.
- Added `docs/wiki/raw/research/0430-2026-04-27-reorder-locals-validation-bridge.md` and `docs/wiki/binaryen/passes/reorder-locals/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, names/roundtrip guide, Starshine strategy, and parity page so transformed local-declaration shapes, upstream strategy, exact local code/test surfaces, metadata repair, multivalue writer-boundary caveat, and preset-readiness gates are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `reorder-locals` classified as active implemented and deep while making the remaining scheduler/preset evidence requirement explicit.

## [2026-04-27] health | refresh `strip-target-features` tracker sources

- Ran a focused touched-area hygiene check after adding the `strip-target-features` port-readiness bridge.
- Updated the pass tracker frontmatter source list so the 2026-04-27 raw source manifest and research note are directly discoverable from the tracker, matching the refreshed row and tail maintenance note.

## [2026-04-27] research | deepen `strip-target-features` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/strip-target-features/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `strip-target-features` because its corrected dossier still lacked the standard Starshine port-readiness / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-strip-target-features-port-readiness-primary-sources.md`, rechecking official Binaryen `version_129` and current-main `StripTargetFeatures.cpp`, `pass.cpp`, `passes.h`, `pass.h`, the WebAssembly custom-section spec, and exact local Starshine pass-registry / custom-section decode / encode / validation surfaces.
- Added `docs/wiki/raw/research/0429-2026-04-27-strip-target-features-port-readiness.md` and `docs/wiki/binaryen/passes/strip-target-features/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape, and Starshine strategy so the shared `emit-target-features` / `strip-target-features` owner, inherited default-true `modifiesBinaryenIR()` behavior, target-feature metadata toggle, current local unknown-pass status, opaque-section landing zone, first-slice choices, and validation ladder are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `strip-target-features` classified as upstream-only and deep while retiring the remaining local-readiness gap and the too-literal 2026-04-26 owner-file override wording.

## [2026-04-27] health | refresh `const-hoisting` touched-area links

- Ran a focused touched-area hygiene check after adding the `const-hoisting` port-readiness bridge.
- Updated the existing Binaryen strategy, implementation/test-map, byte-size model, literal identity, and WAT-shape pages so the 2026-04-27 source recheck and [`starshine-port-readiness-and-validation.md`](binaryen/passes/const-hoisting/starshine-port-readiness-and-validation.md) are discoverable from every maintained `const-hoisting` page, not only from the overview and Starshine strategy pages.

## [2026-04-27] research | deepen `const-hoisting` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/const-hoisting/` folder, and `docs/wiki/raw/research/`, then chose removed `const-hoisting` because its source-correct dossier still lacked the newer first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-const-hoisting-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `ConstHoisting.cpp`, `pass.cpp`, `literal.h`, `wasm-binary.h`, `wasm-builder.h`, and `const-hoisting.wast` plus exact local Starshine registry, HOT scalar-constant, local builder, fresh-local, lower/lift, and signed-LEB encoding surfaces; no teaching-relevant upstream drift was found from the 2026-04-23 / 2026-04-25 contract.
- Added `docs/wiki/raw/research/0428-2026-04-27-const-hoisting-port-readiness.md` and `docs/wiki/binaryen/passes/const-hoisting/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed literal shapes, upstream strategy, exact local removed-name behavior, analyzer-first bucket discovery, byte-threshold tests, deterministic prelude insertion, required negatives, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `const-hoisting` from `dossier` to `deep` while keeping it explicitly removed and unimplemented in Starshine today.

## [2026-04-27] research | deepen `type-un-finalizing` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-un-finalizing/` folder, sibling `type-finalizing` port-readiness coverage, and `docs/wiki/raw/research/`, then chose boundary-only `type-un-finalizing` because its source-backed sibling dossier still lacked the newer first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-type-un-finalizing-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `TypeFinalizing.cpp`, `pass.cpp`, and `type-finalizing.wast` plus exact local Starshine registry, type model, WAT parser/lowerer/printer, validator, and binary roundtrip surfaces; no teaching-relevant upstream drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0427-2026-04-27-type-un-finalizing-port-readiness.md` and `docs/wiki/binaryen/passes/type-un-finalizing/starshine-port-readiness-and-validation.md`; refreshed every maintained `type-un-finalizing` page so transformed type-section shapes, upstream strategy, exact local boundary-only behavior, safe first slices, private-type visibility question, Binaryen oracle lanes, and local-vs-upstream spelling split are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `type-un-finalizing` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] health | refresh `type-finalizing` touched-area links

- Ran a focused touched-area hygiene check after adding the `type-finalizing` port-readiness bridge.
- Updated the existing Binaryen strategy, implementation/test-map, leaf-boundary, and WAT-shape pages so the 2026-04-27 current-main manifest and [`starshine-port-readiness-and-validation.md`](binaryen/passes/type-finalizing/starshine-port-readiness-and-validation.md) are discoverable from every maintained `type-finalizing` page, not only from the overview and Starshine strategy pages.

## [2026-04-27] research | deepen `type-finalizing` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-finalizing/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `type-finalizing` because its source-correct dossier still lacked the now-standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-type-finalizing-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `TypeFinalizing.cpp`, `pass.cpp`, and `type-finalizing.wast` plus exact local Starshine registry, type model, WAT parser/lowerer/printer, validator, and binary roundtrip surfaces; no teaching-relevant upstream drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0426-2026-04-27-type-finalizing-port-readiness.md` and `docs/wiki/binaryen/passes/type-finalizing/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed type-section shapes, upstream strategy, exact local boundary-only behavior, safe first slices, private-type visibility question, Binaryen oracle lanes, and sibling spelling split are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `type-finalizing` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] research | deepen `duplicate-function-elimination` validation bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/duplicate-function-elimination/` folder, and `docs/wiki/raw/research/`, then chose active module pass `duplicate-function-elimination` because its core dossier was source-correct but still lacked a single beginner-to-advanced scheduler/validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-duplicate-function-elimination-validation-primary-sources.md`, rechecking official Binaryen current-main DFE owner/scheduler/representative optimize-level test surfaces plus exact local registry, dispatcher, implementation, and focused-test code surfaces.
- Added `docs/wiki/raw/research/0425-2026-04-27-duplicate-function-elimination-validation-bridge.md` and `docs/wiki/binaryen/passes/duplicate-function-elimination/scheduler-validation-and-parity.md`; refreshed the overview, Starshine strategy, and parity pages so upstream repeat-budget / two-slot scheduling, current Starshine one-iteration explicit-pass behavior, local type/name/annotation cleanup, test surfaces, and preset-scheduling evidence requirements are connected without treating Starshine-local cleanup as Binaryen DFE proper.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `duplicate-function-elimination` classified as active implemented and deep while making the remaining scheduler/iteration parity gap explicit.

## [2026-04-27] research | deepen `minify-imports` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/minify-imports/` folder, sibling `minify-imports-and-exports` coverage, and `docs/wiki/raw/research/`, then chose upstream-only `minify-imports` because its source-corrected dossier still lacked the standard implementation-readiness / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-minify-imports-port-readiness-primary-sources.md`, rechecking official Binaryen `version_129` and current-main `MinifyImportsAndExports.cpp`, `pass.cpp`, `passes.h`, `name.h`, the sibling minify lit fixture, and exact local Starshine registry/dispatcher/import-section/binary/WAT code surfaces; no teaching-relevant upstream drift was found from the 2026-04-26 corrected contract.
- Added `docs/wiki/raw/research/0424-2026-04-27-minify-imports-port-readiness.md` and `docs/wiki/binaryen/passes/minify-imports/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine strategy so transformed import-section shapes, upstream strategy, exact local unknown-pass behavior, registry/reporting choice, safe plain import-base rewrite slice, sibling negatives, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `minify-imports` classified as upstream-only and deep while making the future validation ladder explicit.

## [2026-04-27] research | deepen `dataflow-optimization` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dataflow-optimization/` folder, and `docs/wiki/raw/research/`, then chose removed local `dataflow-optimization` / upstream `dfo` because its source-correct dossier still lacked the newer analyzer-first / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-dataflow-optimization-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `DataFlowOpts.cpp`, `src/dataflow/*`, `flat.h`, `pass.cpp`, and the combo `flatten_simplify-locals-nonesting_dfo_O3.wast` proof surface; no teaching-relevant drift was found from the 2026-04-25 contract.
- Added `docs/wiki/raw/research/0423-2026-04-27-dataflow-optimization-port-readiness.md` and `docs/wiki/binaryen/passes/dataflow-optimization/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, Flat/DataFlow boundary guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact removed-registry / request-rejection / no-substrate code surfaces, analyzer-only first slice, same-constant merge first mutation, supported all-constant expression folding, negative tests, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `dataflow-optimization` from `dossier` to `deep` while keeping it explicitly removed and unimplemented in Starshine today.

## [2026-04-27] research | deepen `flatten` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/flatten/` folder, and `docs/wiki/raw/research/`, then chose removed `flatten` because it had source-correct overview/strategy/shape/status coverage but still lacked the standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-flatten-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `Flatten.cpp`, `flat.h`, `pass.cpp`, `passes.h`, and the direct `flatten*` lit surfaces; no teaching-relevant drift was found from the 2026-04-25 contract.
- Added `docs/wiki/raw/research/0422-2026-04-27-flatten-port-readiness.md` and `docs/wiki/binaryen/passes/flatten/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, Flat IR/preludes guide, WAT-shape catalog, and Starshine strategy so analyzer-first Flat IR classification, narrow first mutating slices, tee/branch/EH follow-ups, unsupported-family policy, downstream validation lanes, exact removed-registry / CLI-spelling / dispatcher-gap surfaces, and removal-from-removed-registry gates are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `flatten` classified as removed and deep while making the future validation ladder explicit.

## [2026-04-27] research | re-correct `type-generalizing` strategy

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-generalizing/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `type-generalizing` because its dossier still carried a stale 2026-04-24 overcorrection.
- Added `docs/wiki/raw/binaryen/2026-04-27-type-generalizing-primary-source-correction.md`, rechecking official Binaryen `version_129` and current-main `TypeGeneralizing.cpp`, `pass.cpp`, and `type-generalizing.wast`; the source confirms the real hidden/not-yet-sound pass is CFG/backward-analysis plus `ContentOracle`/call/ref/GC constraints, not a tiny local-set/local-tee retagging pass.
- Added `docs/wiki/raw/research/0421-2026-04-27-type-generalizing-source-correction-and-port-readiness.md` and `docs/wiki/binaryen/passes/type-generalizing/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, shape catalog, and Starshine strategy, and marked the old local-flow page superseded in favor of `type-requirements-cfg-and-unsupported-families.md`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `type-generalizing` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] health | refresh `remove-unused` touched-area links

- Ran a focused touched-area hygiene check after adding the `remove-unused` port-readiness bridge.
- Updated the existing Binaryen strategy, implementation/test-map, historical-lineage, and module-shape pages so the 2026-04-27 source recheck and `starshine-port-readiness-and-validation.md` are discoverable from every maintained `remove-unused` page, not only from the landing and Starshine strategy pages.

## [2026-04-27] research | deepen `remove-unused` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/remove-unused/` folder, and `docs/wiki/raw/research/`, then chose local boundary-only `remove-unused` because its lineage dossier was source-correct but still lacked a decision/validation bridge for the ambiguous historical alias.
- Added `docs/wiki/raw/binaryen/2026-04-27-remove-unused-port-readiness-primary-sources.md`, rechecking official Binaryen current `main` `pass.cpp`, current `RemoveUnusedModuleElements.cpp`, and historical `RemoveUnusedFunctions.cpp`; no current-main resurrection of `remove-unused` / `remove-unused-functions` was found.
- Added `docs/wiki/raw/research/0420-2026-04-27-remove-unused-port-readiness.md` and `docs/wiki/binaryen/passes/remove-unused/starshine-port-readiness-and-validation.md`; refreshed the landing and Starshine strategy pages so old function-only shapes, modern RUME alias risk, exact local boundary-only / dispatcher / RUME code locations, and the four future choices (keep rejecting, remove/rename, implement literal old behavior, or intentionally alias to RUME) are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `remove-unused` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] research | deepen `type-refining` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-refining/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `type-refining` because its source-backed dossier still lacked the newer analyzer-first / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-type-refining-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `TypeRefining.cpp`, `pass.cpp`, and the dedicated `type-refining*` lit files; no teaching-relevant drift was found from the 2026-04-24 closed-world private-struct-field contract.
- Added `docs/wiki/raw/research/0419-2026-04-27-type-refining-port-readiness.md` and `docs/wiki/binaryen/passes/type-refining/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only code surfaces, no-rewrite analyzer sequencing, narrow closed-world private-struct first mutation, WAT `struct.set` fixture caveat, Binaryen oracle lanes, and the open `type-refining-gufa` registry question are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `type-refining` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] research | deepen `optimize-added-constants` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, the pass index and tracker, the existing `docs/wiki/binaryen/passes/optimize-added-constants/` folder, the sibling `optimize-added-constants-propagate` dossier, and `docs/wiki/raw/research/`, then chose removed plain `optimize-added-constants` because it was source-correct but still lacked the now-standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-optimize-added-constants-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged-source context for `OptimizeAddedConstants.cpp`, `pass.cpp`, `pass.h`, and the plain low-memory / memory64 / no-memory tests; no teaching-relevant drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0418-2026-04-27-optimize-added-constants-port-readiness.md` and `docs/wiki/binaryen/passes/optimize-added-constants/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, safety guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local removed-registry / option / HOT-memory code surfaces, analyzer/no-op sequencing, first mutating direct-fold scope, threshold negatives, constant-pointer overflow checks, Binaryen `--low-memory-unused` oracle lanes, and the open configurable-bound design question are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `optimize-added-constants` from `dossier` to `deep` while keeping it explicitly removed and unimplemented in Starshine today.

## [2026-04-27] health | refresh `global-effects` index hygiene

- Ran a focused reference-hygiene check after adding the `global-effects` port-readiness bridge.
- Updated the top-level wiki index's `global-effects` Binaryen-strategy summary so it no longer implies the 2026-04-24 source manifest is the only current anchor.
- Added maintenance-rule notes in the pass index and tracker so future wiki-health runs do not treat `global-effects` analyzer-first sequencing, summary-storage model, solver choice, paired consumer validation, or stale-summary risks as still-open dossier gaps.

## [2026-04-27] research | deepen `global-effects` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/global-effects/` folder, neighboring `discard-global-effects`, `simplify-locals`, and `vacuum` coverage, and `docs/wiki/raw/research/`, then chose boundary-only `global-effects` because its source-backed dossier still lacked a concrete implementation-readiness / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-27-global-effects-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `GlobalEffects.cpp`, `pass.cpp`, `effects.h`, `wasm.h`, the Optimizer Cookbook lifecycle context, and the paired consumer lit surfaces; no teaching-relevant upstream drift was found from the 2026-04-24 / 2026-04-25 contract.
- Added `docs/wiki/raw/research/0417-2026-04-27-global-effects-port-readiness.md` and `docs/wiki/binaryen/passes/global-effects/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, metadata/consumer guide, WAT-shape catalog, and Starshine strategy so transformed metadata shapes, upstream strategy, exact local boundary-only code surfaces, no-rewrite analyzer first slice, per-global read/write summary requirement, SCC/fixed-point choice, consumer sequencing, stale-summary risks, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `global-effects` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-27] implementation | activate `directize`

- Landed the active Starshine `directize` module pass for the default explicit Binaryen behavior, with module-wide table eligibility, imported/exported/mutated table bailouts, active element target collection, direct `call` / `return_call` rewrites, known-trap `unreachable` rewrites, subtype-compatible function checks, and narrow known-target `select` lowering with fresh locals.
- Wired registry, module-pass dispatcher, pass-fuzz list support, and focused tests for a constant known target, runtime mutation bailout, known-hole trap rewrite, and select lowering.
- Refreshed the `directize` wiki status, port-readiness page, tracker counts, and backlog with direct validation evidence: `moon info`, `moon fmt`, `moon test src/passes`, `moon test src/cmd`, full `moon test`, `.tmp/pass-fuzz-directize-genvalid-10000-final2`, `.tmp/pass-fuzz-directize-mixed-10000-final2`, and `.tmp/self-opt-directize-debug-final2`; preset scheduling and `directize-initial-contents-immutable` pass-arg support remain follow-up.

## [2026-04-26] implementation | activate `untee`

- Landed the active Starshine `untee` module pass for explicit `local.tee` desugaring into `local.set` plus `local.get`, including recursive structured-body traversal and direct flat `unreachable; local.tee` deletion.
- Wired registry, dispatcher, CLI adapter coverage, pass-fuzz support, public API snapshot, and focused tests for root tees, nested tees, unreachable input, and `if` bodies.
- Refreshed the `untee` wiki status, pass catalog, and tracker row from removed/future-port language to active direct-pass status; direct validation evidence is green on focused Moon tests, `moon test src/passes`, `moon test src/cmd`, full `moon test`, `.tmp/pass-fuzz-untee-genvalid-10000-staged`, `.tmp/pass-fuzz-untee-keepgoing-staged`, and `/tmp/starshine-self-optimize-compare-starshine-debug-wasi-866306` after fixing the neighboring `optimize-instructions` i64 compare-form regression.

## [2026-04-26] implementation | activate `remove-unused-nonfunction-module-elements`

- Landed the active Starshine `remove-unused-nonfunction-module-elements` module pass by reusing full RUME with a defined-function-rooting policy mode, while leaving imported functions as ordinary reachability candidates.
- Added focused tests for defined-function retention, imported-function removal, active global-offset segment retention, out-of-bounds active-data trap retention, empty in-bounds active-element pruning, and no-op start metadata cleanup; wired registry, dispatcher, and pass-fuzz support.
- Refreshed the `remove-unused-non-function-elements` wiki pages and tracker row from boundary-only/future-port language to active module-pass status; current validation evidence is `.tmp/pass-fuzz-runfe-genvalid-10000-final`, `.tmp/pass-fuzz-runfe-10000-after-oob`, and `.tmp/self-opt-runfe-debug-final`.

## [2026-04-26] implementation | activate `avoid-reinterprets` direct slice

- Landed the active Starshine `avoid-reinterprets` module pass for direct full-width load-plus-reinterpret pairs and wired registry, dispatcher, CLI-visible pass support, compare harness support, and focused tests.
- Updated the `avoid-reinterprets` wiki pages and tracker row to mark the pass active-partial: direct `reinterpret(load)` flips are implemented, while the indirect `reinterpret(local.get <- load)` helper-local family remains future work.
- Captured validation evidence in `.tmp/pass-fuzz-ar-genvalid-10000`, `.tmp/pass-fuzz-ar-mixed-10000`, and `.tmp/self-opt-ar-debug-native`.

## [2026-04-26] research | deepen `monomorphize` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/monomorphize/` folder, neighboring `monomorphize-always` coverage, and `docs/wiki/raw/research/`, then chose boundary-only `monomorphize` because its source-correct dossier still lacked a dedicated first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-monomorphize-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `Monomorphize.cpp`, `pass.cpp`, and dedicated lit surfaces; no teaching-relevant drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0416-2026-04-26-monomorphize-port-readiness.md` and `docs/wiki/binaryen/passes/monomorphize/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only / option-plumbing code surfaces, no-rewrite analyzer, scalar-literal clone first slice, dropped-result follow-up, effect-movement sequencing, `monomorphize-always` sharing, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `monomorphize` classified as boundary-only and deep while making the future validation ladder explicit.

## [2026-04-26] research | deepen `string-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/string-lowering/` folder, neighboring string pass coverage, and `docs/wiki/raw/research/`, then chose upstream-only `string-lowering` because the dossier had source-correct overview/strategy/shape/status coverage but still lacked the standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-string-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `StringLowering.cpp`, `pass.cpp`, and lit surfaces plus the official JS string builtins proposal context; no teaching-relevant Binaryen current-main drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0415-2026-04-26-string-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/string-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, JSON/magic-import guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream helper namespace, exact local unknown-pass status, `string.const` parser/encoder/validator/HOT code surfaces, missing helper-call source surface, registry-honesty first slice, default-JSON lowering, helper-import rewrite sequencing, magic-import/assert-mode checks, and feature-cleanup validation are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `string-lowering` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | deepen `llvm-memory-copy-fill-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/llvm-memory-copy-fill-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `llvm-memory-copy-fill-lowering` because its dossier still left Binaryen's exact helper-function ABI, fatal boundaries, and Starshine first-slice validation ladder too schematic.
- Added `docs/wiki/raw/binaryen/2026-04-26-llvm-memory-copy-fill-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `LLVMMemoryCopyFillLowering.cpp` plus local Starshine registry, HOT effect, and data-count validation surfaces; no current-main drift from the tagged contract was found, but the recheck corrected the teaching model from vague helper imports to local `__memory_copy` / `__memory_fill` functions.
- Added `docs/wiki/raw/research/0414-2026-04-26-llvm-memory-copy-fill-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/llvm-memory-copy-fill-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, helper-boundary guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream helper bodies, memory32 single-memory support, memory64/multi-memory/passive/table-bulk fatal boundaries, bulk-memory feature cleanup, exact local unknown-pass code surfaces, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `llvm-memory-copy-fill-lowering` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | correct and deepen `code-pushing` strategy

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/code-pushing/` folder, and `docs/wiki/raw/research/`, then chose active HOT `code-pushing` because its 2026-04-25 correction had become the stale reference needing cleanup.
- Added `docs/wiki/raw/binaryen/2026-04-26-code-pushing-current-main-port-readiness.md`, rechecking official Binaryen current-main and tagged `version_129` `CodePushing.cpp` plus the dedicated `code-pushing*` lit tests and exact Starshine `code_pushing.mbt`, `code_pushing_test.mbt`, and `optimize.mbt` code surfaces.
- Added `docs/wiki/raw/research/0413-2026-04-26-code-pushing-current-main-port-readiness.md` and `docs/wiki/binaryen/passes/code-pushing/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, movement-boundary, WAT-shape catalog, and Starshine strategy so the current upstream `LocalAnalyzer` / `Pusher` / segment-window model, one-consuming-arm `if` sinking, post-if unreachable-arm nuance, local subset, and analyzer-first Starshine validation ladder are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; explicitly superseded the 2026-04-25 miscorrection that had removed `Pusher` and segment terminology while preserving the warning against arbitrary two-live-arm duplication.
- Health-fixed the older `docs/wiki/raw/research/0115-2026-04-20-code-pushing-binaryen-research.md` and `docs/wiki/raw/research/0345-2026-04-25-code-pushing-source-correction-and-local-status.md` supersession notes so future readers are routed to the 2026-04-26 corrected contract instead of the overcorrected 2026-04-25 note.

## [2026-04-26] research | deepen `i64-to-i32-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/i64-to-i32-lowering/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `i64-to-i32-lowering` because its source-backed dossier still lacked the newer first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-i64-to-i32-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner/registration/helper/flatness/test sources plus exact Starshine registry, dispatcher, type, locals, WAT, binary, and validation code surfaces; no teaching-relevant upstream correction was found from the 2026-04-24 pair-lowering contract.
- Added `docs/wiki/raw/research/0412-2026-04-26-i64-to-i32-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/i64-to-i32-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, hard-boundaries guide, ABI/opcode ledger, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only code surfaces, analyzer-only first slice, scalar type/local first mutating slice, unsupported-family negatives, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `i64-to-i32-lowering` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-26] research | deepen `memory64-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/memory64-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `memory64-lowering` / `table64-lowering` because the corrected source dossier still lacked a concrete Starshine first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-memory64-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `Memory64Lowering.cpp`, `pass.cpp`, `memory64-lowering.wast`, `table64-lowering.wast`, and the official Emscripten `MEMORY64` setting; no teaching-relevant drift was found from the 2026-04-25 static-offset correction.
- Added `docs/wiki/raw/research/0411-2026-04-26-memory64-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/memory64-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, focused mechanics guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local unknown-pass code surfaces, registry-honesty/no-op-analyzer sequencing, memory declaration/data-offset first slice, grow sentinel repair, bulk/SIMD/atomic follow-ups, table64 typechecker prerequisite, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `memory64-lowering` / `table64-lowering` classified as upstream-only and deep while making the future validation ladder explicit.

## [2026-04-26] research | deepen `dae2` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dae2/` folder, neighboring DAE-family pages, and `docs/wiki/raw/research/`, then chose upstream-only `dae2` because the dossier was source-correct but still lacked the standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-dae2-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `DeadArgumentElimination2.cpp`, `pass.cpp`, `passes.h`, and `dae2.wast`; no teaching-relevant drift was found from the 2026-04-25 dossier.
- Added `docs/wiki/raw/research/0410-2026-04-26-dae2-port-readiness.md` and `docs/wiki/binaryen/passes/dae2/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, focused mechanics guide, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local unknown-pass code surfaces, registry-honesty first slice, no-rewrite analyzer, private direct-call deletion, fixed-point forwarding cycles, referenced type-tree follow-up, and Binaryen `--dae2 --closed-world` oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `dae2` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] health | refresh `type-ssa` touched-area links

- Ran a focused touched-area hygiene check after adding the `type-ssa` port-readiness bridge.
- Updated the focused exactness/interestingness guide so future-port sequencing and validation lanes point directly at `starshine-port-readiness-and-validation.md` instead of leaving the new page discoverable only through the overview and Starshine strategy page.
- Refreshed the Starshine strategy code-range pointers for the current registry arrays and active entry/preset span.

## [2026-04-26] research | deepen `type-ssa` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-ssa/` folder, neighboring type/SSA dossiers, and `docs/wiki/raw/research/`, then chose upstream-only `type-ssa` because the corrected allocation-subtype dossier still lacked a concrete Starshine first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-type-ssa-port-readiness-primary-sources.md`, rechecking official Binaryen `version_129` and current-main `TypeSSA.cpp`, `pass.cpp`, `passes.h`, `type-ssa.wast`, `ReFinalize.cpp`, and type-shape helpers; no teaching-relevant drift was found from the corrected allocation-subtype contract.
- Added `docs/wiki/raw/research/0409-2026-04-26-type-ssa-port-readiness.md` and `docs/wiki/binaryen/passes/type-ssa/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local unknown-pass status, GC-allocation / WAT-lowering / typechecker code surfaces, analyzer-first sequencing, fresh-subtype mutation prerequisites, and Binaryen `--type-ssa` oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `type-ssa` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] health | refresh `remove-unused-non-function-elements` touched-area links

- Ran a focused touched-area hygiene check after adding the `remove-unused-non-function-elements` port-readiness bridge.
- Updated the module-shape landing note and Binaryen strategy local-status note so future-port references point directly at `starshine-port-readiness-and-validation.md` instead of leaving the validation ladder discoverable only through the overview and Starshine strategy page.
- Refreshed the overview wording so the 2026-04-24 provenance update and 2026-04-26 port-readiness bridge are not conflated.

## [2026-04-26] research | deepen `remove-unused-non-function-elements` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/remove-unused-non-function-elements/` folder, neighboring full-RUME coverage, and `docs/wiki/raw/research/`, then chose boundary-only `remove-unused-non-function-elements` because its source dossier still lacked a concrete first-slice / validation bridge for a future Starshine sibling port.
- Added `docs/wiki/raw/binaryen/2026-04-26-remove-unused-non-function-elements-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `RemoveUnusedModuleElements.cpp`, `pass.cpp`, `passes.h`, and the dedicated `remove-unused-nonfunction-module-elements_all-features.wast` fixture; no teaching-relevant drift was found from the 2026-04-24 shared-RUME sibling contract.
- Added `docs/wiki/raw/research/0408-2026-04-26-remove-unused-non-function-elements-port-readiness.md` and `docs/wiki/binaryen/passes/remove-unused-non-function-elements/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, defined-vs-imported guide, module-shape catalog, and Starshine strategy so transformed module shapes, upstream strategy, exact local boundary-only code surfaces, policy-first liveness sequencing, required differential tests, Binaryen `--remove-unused-nonfunction-module-elements` oracle spelling, and validation ladder are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `remove-unused-non-function-elements` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-26] health | refresh `simplify-locals-nonesting` touched-area links

- Ran a focused touched-area hygiene check after adding the `simplify-locals-nonesting` port-readiness bridge.
- Updated the existing Binaryen strategy, implementation/test-map, flatness-boundary, and WAT-shape pages so future-port and validation references point directly at `starshine-port-readiness-and-validation.md` instead of leaving the new first-slice ladder discoverable only through the overview and Starshine status page.

## [2026-04-26] research | deepen `simplify-locals-nonesting` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/simplify-locals-nonesting/` folder, neighboring `simplify-locals*`, `flatten`, `dataflow-optimization`, and `souperify` coverage, and `docs/wiki/raw/research/`, then chose removed-name `simplify-locals-nonesting` because the dossier was source-correct but still lacked the standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-simplify-locals-nonesting-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner/registration/constructor/test/combo-lit surfaces plus exact Starshine removed-registry, CLI, dispatcher, and full-`simplify-locals` implementation surfaces; no teaching-relevant drift was found from the 2026-04-25 contract.
- Added `docs/wiki/raw/research/0407-2026-04-26-simplify-locals-nonesting-port-readiness.md` and `docs/wiki/binaryen/passes/simplify-locals-nonesting/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local removed-alias code surfaces, spelling-policy first step, no-rewrite skeleton, flat-copy first slice, disabled tee/structure/nesting negatives, late-cleanup follow-up, and Binaryen `--simplify-locals-nonesting` oracle lanes are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `simplify-locals-nonesting` classified as removed and deep while making the future validation ladder explicit.

## [2026-04-26] research | deepen plain `dead-argument-elimination` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dead-argument-elimination/` folder, neighboring `dae-optimizing` coverage, and `docs/wiki/raw/research/`, then chose boundary-only plain `dead-argument-elimination` because the dossier was source-correct but still lacked the standard first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-dead-argument-elimination-port-readiness-primary-sources.md`, rechecking official Binaryen current-main owner/helper/scheduler/test surfaces for `DeadArgumentElimination.cpp`, `pass.cpp`, `param-utils.*`, `lubs.h`, `return-utils.h`, `type-updating.h`, and the representative `dae_tnh` / `dae-gc*` lit files; no teaching-relevant drift was found from the tagged `version_129` contract.
- Added `docs/wiki/raw/research/0406-2026-04-26-dead-argument-elimination-port-readiness.md` and `docs/wiki/binaryen/passes/dead-argument-elimination/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only code surfaces, analyzer-only first slice, scalar direct-call deletion, follow-up constant/GC/result/localization families, Binaryen `--dae` oracle lanes, and the plain-vs-optimizing scheduler guardrail are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted plain `dead-argument-elimination` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-26] research | deepen `remove-unused-types` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/remove-unused-types/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `remove-unused-types` because its corrected source dossier still lacked a concrete first-slice / validation bridge for a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-26-remove-unused-types-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `RemoveUnusedTypes.cpp`, `pass.cpp`, `type-updating.h`, `module-utils.h`, and `remove-unused-types.wast`; no teaching-relevant drift was found from the corrected 2026-04-24 `GlobalTypeRewriter` contract.
- Added `docs/wiki/raw/research/0405-2026-04-26-remove-unused-types-port-readiness.md` and `docs/wiki/binaryen/passes/remove-unused-types/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed type-section shapes, upstream strategy, exact local boundary-only code surfaces, no-rewrite analyzer-first sequencing, private singleton deletion, old-rec-group member removal, descriptor/supertype retention, full type-use repair, and closed-world Binaryen-oracle validation are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `remove-unused-types` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-26] research | deepen `signature-pruning` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/signature-pruning/` folder, neighboring `signature-refining` and `dead-argument-elimination` coverage, and `docs/wiki/raw/research/`, then chose boundary-only `signature-pruning` because the dossier was source-correct but still lacked a dedicated first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-signature-pruning-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `SignaturePruning.cpp`, `signature-pruning.wast`, `pass.cpp`, and helper surfaces; no teaching-relevant drift was found from the 2026-04-24 `version_129` contract.
- Added `docs/wiki/raw/research/0404-2026-04-26-signature-pruning-port-readiness.md` and `docs/wiki/binaryen/passes/signature-pruning/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only code surfaces, direct-call first slice, `call_ref` / constant-actual / localization follow-up slices, validation ladder, and direct `call_ref` text-fixture caveat are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `signature-pruning` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.

## [2026-04-26] research | deepen `minify-imports-and-exports` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/minify-imports-and-exports/` folder, neighboring `minify-imports` coverage, and `docs/wiki/raw/research/`, then chose upstream-only `minify-imports-and-exports` because the dossier explained the shared Binaryen owner but still left first-slice sequencing, validation order, and JSON map shape hygiene implicit.
- Added `docs/wiki/raw/binaryen/2026-04-26-minify-imports-and-exports-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner/registration/test-directory sources; no teaching-relevant strategy drift was found, but the recheck confirmed the JSON map shape is row arrays and that no pass-named `minify-imports*.wast` fixture exists in the official pass-test directory.
- Added `docs/wiki/raw/research/0403-2026-04-26-minify-imports-and-exports-port-readiness.md` and `docs/wiki/binaryen/passes/minify-imports-and-exports/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, and Starshine status page so transformed import/export/module-name shapes, upstream strategy, exact local declaration-string code surfaces, registry-honesty choices, first mutating slice, sibling split, map-output checks, and Binaryen oracle lanes are connected.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md`; health-fixed a duplicate Binaryen source link in the overview and corrected the stale conceptual JSON object example to current row-array output.

## [2026-04-26] implementation | land direct `reorder-globals` port

- Used the existing `reorder-globals` dossier as the pass contract for an O4z no-DWARF late-tail pass that was still missing locally.
- Added `src/passes/reorder_globals.mbt` plus focused pass, registry, and CLI tests for the public `<128` cutoff, 129-global reorder, dependency preservation, export/name remapping, active module-pass classification, and explicit `--reorder-globals` acceptance.
- Wired the module pass through `src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/passes/pkg.generated.mbti`, and `scripts/lib/pass-fuzz-compare-task.ts`; left `reorder-globals-always` boundary-only and kept preset scheduling deferred until `string-gathering` and `directize` exist locally.
- Refreshed `docs/wiki/binaryen/passes/reorder-globals/` and `agent-todo.md` so the direct port, remaining late-tail/artifact evidence, and sibling deferral are explicit.

## [2026-04-26] health | refresh full `ssa` touched-area links

- Ran a focused touched-area hygiene check after adding the full `ssa` port-readiness bridge.
- Updated `docs/wiki/binaryen/passes/ssa/binaryen-strategy.md`, `implementation-structure-and-tests.md`, `merge-locals-entry-prepends-and-default-values.md`, and `wat-shapes.md` so future-port and shape references point directly at `starshine-port-readiness-and-validation.md` instead of leaving the new validation ladder discoverable only through the overview.

## [2026-04-26] research | deepen full `ssa` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/ssa/` folder, neighboring `ssa-nomerge` coverage, and `docs/wiki/raw/research/`, then chose upstream-only full `ssa` because its dossier was source-correct but still lacked the standard first-slice / validation bridge for a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-26-ssa-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `SSAify.cpp`, `pass.cpp`, `passes.h`, LocalGraph, `ReFinalize`, `ssa.wast`, and sibling `ssa-nomerge` proof surfaces; no teaching-relevant drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0402-2026-04-26-ssa-port-readiness.md` and `docs/wiki/binaryen/passes/ssa/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test map, WAT-shape, focused merge-local guide, and Starshine strategy so transformed shapes, upstream strategy, exact local code surfaces, registry honesty, source-classifier requirements, merge-local / incoming-`tee` rewrite order, `ssa-nomerge` sibling stability, and Binaryen `--ssa` validation are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `ssa` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | add `asyncify` port-readiness bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/asyncify/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `asyncify` because the dossier had overview/strategy/shape/source-map/status coverage but still lacked the now-standard first-slice / validation bridge for a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-26-asyncify-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `Asyncify.cpp`, `pass.cpp`, and `asyncify.wast` plus official Emscripten Asyncify docs; no teaching-relevant drift was found from the 2026-04-25 dossier.
- Added `docs/wiki/raw/research/0401-2026-04-26-asyncify-port-readiness.md` and `docs/wiki/binaryen/passes/asyncify/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local unknown-pass code surfaces, registry-honesty choices, direct-call scalar first slice, live-local save/restore, indirect-call policy, memory64 pointer traffic, EH/catch decisions, tail-call rejection, and dynamic host-harness validation are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `asyncify` classified as upstream-only and deep while making clear that Starshine still treats it as unknown rather than boundary-only or removed.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log, pass, source, and code files.

## [2026-04-26] research | add `precompute` port-readiness bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/precompute/` folder, neighboring `precompute-propagate` coverage, and `docs/wiki/raw/research/`, then chose active hot pass `precompute` because its dossier was deep but still lacked the newer campaign's dedicated implementation-readiness / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-precompute-current-main-port-readiness.md`, rechecking official Binaryen `Precompute.cpp`, `pass.cpp`, `opt-utils.h`, helper surfaces, and representative tests against the exact local Starshine owner / dispatcher / registry / preset / test locations.
- Added `docs/wiki/raw/research/0400-2026-04-26-precompute-port-readiness.md` and `docs/wiki/binaryen/passes/precompute/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy page so readers can distinguish Starshine's HOT fold-and-cleanup fixpoint from Binaryen's interpreter-driven semantic evaluator, follow safe next-slice ordering, and keep pass-manager writeback guards plus Binaryen-oracle comparison in scope.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` to include the new port-readiness bridge.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log, pass, source, and code files.

## [2026-04-26] research | clean up `duplicate-function-elimination` Starshine strategy naming

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/duplicate-function-elimination/`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose active module pass `duplicate-function-elimination` because its Starshine strategy page still carried a stale HOT-IR filename despite the page correctly teaching that DFE is module-scoped.
- Added `docs/wiki/raw/binaryen/2026-04-26-duplicate-function-elimination-current-main-and-starshine-strategy-health.md`, rechecking official Binaryen current-main DFE owner/helper/test surfaces and exact local Starshine registry/dispatcher/implementation/test locations.
- Added `docs/wiki/raw/research/0399-2026-04-26-duplicate-function-elimination-strategy-health.md`; renamed the living Starshine strategy page from `starshine-hot-ir-strategy.md` to `starshine-strategy.md`; refreshed DFE overview and strategy sources so Binaryen's hash/equality/rewrite loop, Starshine's module-pass implementation, local cleanup extras, and the one-iteration parity caveat remain connected.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` to use the renamed strategy page and to avoid teaching stale HOT-IR filename terminology for this module pass.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log, pass, source, and code files.

## [2026-04-26] research | deepen `signature-refining` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/signature-refining/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `signature-refining` because the dossier was source-correct but still lacked a dedicated first-slice / validation bridge for a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-26-signature-refining-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `SignatureRefining.cpp`, `signature-refining.wast`, and the official releases page; no teaching-relevant drift was found from the 2026-04-24 `version_129` contract.
- Added `docs/wiki/raw/research/0398-2026-04-26-signature-refining-port-readiness.md` and `docs/wiki/binaryen/passes/signature-refining/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local boundary-only code surfaces, no-rewrite analyzer, direct-call param first slice, shared-heap-type fanout, body-fixup/result/`call_ref`/`call.without.effects` follow-up slices, direct `call_ref` text-surface gap, missing local intrinsic surface, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `signature-refining` from `dossier` to `deep` while keeping it explicitly boundary-only and unimplemented in Starshine today.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log, pass, and source files.

## [2026-04-26] research | deepen `instrument-locals` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/instrument-locals/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `instrument-locals` because it had a source-backed dossier but still lacked the standard first-slice / validation bridge that `instrument-memory` already had.
- Added `docs/wiki/raw/binaryen/2026-04-26-instrument-locals-port-readiness-primary-sources.md`, rechecking official Binaryen current-main `InstrumentLocals.cpp` plus `instrument-locals_all-features_disable-gc.wast`, `instrument-locals_effects.wast`, and `instrument-locals-eh-legacy.wast`; no teaching-relevant drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0397-2026-04-26-instrument-locals-port-readiness.md` and `docs/wiki/binaryen/passes/instrument-locals/starshine-port-readiness-and-validation.md`; refreshed the overview and Starshine strategy so transformed shapes, upstream strategy, exact local code surfaces, unknown-pass status, scalar-first module-pass slice, helper-import synthesis requirement, imported-function-counter red herring, effect-invalidation checks, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `instrument-locals` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log, pass, and source files.

## [2026-04-26] research | deepen `signext-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/signext-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `signext-lowering` because the dossier was source-correct but still lacked a dedicated first-slice / validation bridge.
- Added `docs/wiki/raw/binaryen/2026-04-26-signext-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `SignExtLowering.cpp`, `pass.cpp`, `passes.h`, and `signext-lowering.wast`; no teaching-relevant drift was found from the 2026-04-25 contract.
- Added `docs/wiki/raw/research/0396-2026-04-26-signext-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/signext-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local code surfaces, unknown-pass status, instruction-only first slice, feature-metadata caveat, pretty-printer hygiene issue, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `signext-lowering` upstream-only and deep while making the future validation ladder explicit.
- Commit is intentionally withheld unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log and pass files.

## [2026-04-26] research | deepen `legalize-js-interface` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/legalize-js-interface/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `legalize-js-interface` because the dossier was source-correct but still left first-slice sequencing, function-index rewrite hazards, `ref.func` repair coverage, and validation order scattered across status and strategy pages.
- Added `docs/wiki/raw/binaryen/2026-04-26-legalize-js-interface-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner, registration, helper, and lit-test sources; no teaching-relevant drift was found from the 2026-04-24 `version_129` wrapper contract.
- Added `docs/wiki/raw/research/0395-2026-04-26-legalize-js-interface-port-readiness.md` and `docs/wiki/binaryen/passes/legalize-js-interface/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page so transformed shapes, upstream strategy, exact local code surfaces, registry honesty, export-stub-first sequencing, temp-ret helper lanes, direct-call and `ref.func` repair, deletion-last original-import cleanup, pass-argument lanes, and prune-sibling boundaries are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `legalize-js-interface` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.
- Commit is intentionally withheld in this run unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log and pass files.

## [2026-04-26] research | deepen `strip-toolchain-annotations` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/strip-toolchain-annotations/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `strip-toolchain-annotations` because the dossier was source-correct but still left the first safe Starshine slice, expression-annotation parity blocker, and validation ladder implicit.
- Added `docs/wiki/raw/binaryen/2026-04-26-strip-toolchain-annotations-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner, registration, lit-test, and changelog sources; no teaching-relevant drift was found from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0394-2026-04-26-strip-toolchain-annotations-port-readiness.md` and `docs/wiki/binaryen/passes/strip-toolchain-annotations/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local code locations, `FuncAnnotationSec` first slice, missing expression-annotation surface, local `binaryen.removable.if.unused` test gap, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `strip-toolchain-annotations` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.
- Commit is intentionally withheld in this run unless the pre-existing unrelated shared wiki/code changes can be isolated safely; the run started with many modified shared index/log and pass files.

## [2026-04-26] research | deepen `multi-memory-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/multi-memory-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `multi-memory-lowering` because the dossier explained Binaryen's many-memories-to-one transform but still left the first safe Starshine slice and validation ladder implicit.
- Added `docs/wiki/raw/binaryen/2026-04-26-multi-memory-lowering-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner, registration, factory, and lit-test sources plus the multi-memory proposal context; no teaching-relevant drift was found from the 2026-04-25 source dossier.
- Added `docs/wiki/raw/research/0393-2026-04-26-multi-memory-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/multi-memory-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine strategy so transformed shapes, upstream strategy, exact local code surfaces, unchecked two-memory first slice, helper sequencing, non-last-growth byte movement, checked-sibling trap caveat, and feature-cleanup uncertainty are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `multi-memory-lowering` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.
- Commit was intentionally withheld because this run started with many unrelated local changes already present in shared wiki index/log and source files; staging full shared paths would have mixed this run's edits with prior uncommitted work.

## [2026-04-26] research | deepen `remove-relaxed-simd` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/remove-relaxed-simd/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `remove-relaxed-simd` because the dossier was source-correct but still left the safe future Starshine slice and validation ladder scattered across overview, shape, and status pages.
- Added `docs/wiki/raw/binaryen/2026-04-26-remove-relaxed-simd-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` owner/test sources plus the relaxed SIMD proposal context; no teaching-relevant drift was found from the corrected trap-replacement contract.
- Added `docs/wiki/raw/research/0392-2026-04-26-remove-relaxed-simd-port-readiness.md` and `docs/wiki/binaryen/passes/remove-relaxed-simd/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page so transformed shapes, upstream strategy, exact local relaxed-SIMD code locations, registry honesty, child-effect preservation, typed `v128` trap contexts, ordinary-SIMD preservation, Binaryen oracle ordering, dot-product spelling decisions, and feature-metadata uncertainty are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `remove-relaxed-simd` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.
- Commit was intentionally withheld because this run started with many unrelated local changes already present in shared wiki index/log and source files; staging the shared paths would have mixed this run's edits with prior uncommitted work.

## [2026-04-26] research | deepen `inlining` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/inlining/` folder, neighboring `inlining-optimizing`, `inline-main`, `monomorphize`, and `duplicate-function-elimination` pages, and `docs/wiki/raw/research/`, then chose boundary-only `inlining` because the folder was source-backed but still lacked a dedicated first-slice / validation bridge for a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-26-inlining-current-main-port-readiness.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `Inlining.cpp`, `pass.cpp`, `pass.h`, `NoInline.cpp`, `module-utils.cpp`, and the main `inlining*` / `no-inline*` lit files; no teaching-relevant drift was found from the tagged plain-inlining contract.
- Added `docs/wiki/raw/research/0391-2026-04-26-inlining-port-readiness.md` and `docs/wiki/binaryen/passes/inlining/starshine-port-readiness-and-validation.md`; refreshed the overview, pass catalog, main wiki index, and Starshine strategy page so transformed shapes, upstream strategy, exact local boundary-only code locations, minimum direct-call copy/rewrite slice, root-survival negatives, no accidental optimizing suffix, and Binaryen oracle comparison order are connected.
- Commit was intentionally withheld because this run started with many unrelated local changes already present in shared wiki index/log and source files; staging full shared paths would have mixed this run's edits with prior uncommitted work.

## [2026-04-26] research | correct `strip-target-features` module-metadata contract

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/strip-target-features/`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `strip-target-features` because the folder looked complete but taught the wrong owner-file mechanics.
- Added `docs/wiki/raw/binaryen/2026-04-26-strip-target-features-source-correction.md`, rechecking official Binaryen `version_129` and current-main sources for `StripTargetFeatures.cpp`, `pass.cpp`, `passes.h`, and module `hasFeaturesSection` context; the corrected source-backed contract is `modifiesBinaryenIR() == true` plus `module->hasFeaturesSection = false`, not an option-only `emitTargetFeatures = false` toggle.
- Added `docs/wiki/raw/research/0390-2026-04-26-strip-target-features-source-correction.md`; refreshed the overview, Binaryen strategy, implementation/test-map, output-shape catalog, and Starshine status page so transformed metadata shape, upstream strategy, exact local custom-section code locations, and future Starshine port choices are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `strip-target-features` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | deepen `dealign` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dealign/` folder, neighboring `alignment-lowering` coverage, and `docs/wiki/raw/research/`, then chose upstream-only `dealign` because the dossier was source-correct but still lacked the standard first-slice / validation bridge and remained classified as only `dossier`.
- Added `docs/wiki/raw/binaryen/2026-04-26-dealign-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `DeAlign.cpp`, `pass.cpp`, and `dealign.wast`; no teaching-relevant drift was found from the corrected 2026-04-24 contract: function-parallel `Load` / `Store` / `SIMDLoad` alignment assignment to `1`, no `SIMDStore`, atomics, or bulk-memory visitor, and narrow scalar lit proof.
- Added `docs/wiki/raw/research/0389-2026-04-26-dealign-port-readiness.md` and `docs/wiki/binaryen/passes/dealign/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, align-one guide, WAT-shape catalog, and Starshine status page so transformed shapes, upstream strategy, exact local code anchors, registry honesty, memarg exponent `0` for text `align=1`, scalar-first implementation order, SIMD caveat, Binaryen oracle comparison, and negative-family tests are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `dealign` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | deepen `instrument-memory` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/instrument-memory/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `instrument-memory` because the dossier was source-correct but still made future implementers infer the safe first local slice, validation ladder, and distinction between local instruction support and actual helper-import instrumentation.
- Added `docs/wiki/raw/binaryen/2026-04-26-instrument-memory-current-main-port-readiness.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `InstrumentMemory.cpp`, `pass.cpp`, `passes.h`, and the four dedicated `instrument-memory*` lit files; the focused recheck found no teaching-relevant drift from the 2026-04-24 contract.
- Added `docs/wiki/raw/research/0388-2026-04-26-instrument-memory-port-readiness.md` and `docs/wiki/binaryen/passes/instrument-memory/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, helper/filter guide, WAT-shape catalog, and Starshine status page so transformed shapes, upstream strategy, exact local code locations, scalar linear-memory/grow first slice, unsupported-family preservation tests, filters, memory64, GC hooks, Binaryen oracle comparison, and effect-composition checks are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `instrument-memory` from `dossier` to `deep` while keeping it explicitly upstream-only and unknown to Starshine's registry today.

## [2026-04-26] research | correct `minify-imports` plain-pass mutation contract

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/minify-imports/` and `docs/wiki/binaryen/passes/minify-imports-and-exports/` folders, and `docs/wiki/raw/research/`, then chose upstream-only `minify-imports` because the dossier still taught a stale non-mutating imported-function-only `old:new` map pass.
- Added `docs/wiki/raw/binaryen/2026-04-26-minify-imports-current-main-source-correction.md`, rechecking official Binaryen `version_129` and current-main `MinifyImportsAndExports.cpp`, `pass.cpp`, `passes.h`, and the available minification lit surface; the corrected contract is that `createMinifyImportsPass()` uses the shared owner, mutates qualifying import base names, gates plain mode to `env` / `wasi_` modules, walks all import kinds, emits JSON-shaped map output, and leaves export/module-name mutation to the siblings.
- Added `docs/wiki/raw/research/0387-2026-04-26-minify-imports-source-correction.md` and `docs/wiki/binaryen/passes/minify-imports/env-wasi-json-map-and-module-merge.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page so readers can follow transformed shapes, upstream strategy, exact local unknown-pass code locations, output-channel caveats, and the supersession of the older 2026-04-25 source-correction note.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the neighboring `minify-imports-and-exports` overview so the import/export minification family no longer repeats the retired non-mutating/plain-pass description.

## [2026-04-26] research | correct `type-ssa` allocation-subtype strategy

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/type-ssa/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `type-ssa` because the dossier had a serious stale strategy: it taught a `createdTypes` / local-global-control-flow / call-return retagging pass that official Binaryen `version_129` and current `main` do not implement.
- Added `docs/wiki/raw/binaryen/2026-04-26-type-ssa-source-correction-and-current-main.md`, rechecking official Binaryen `TypeSSA.cpp`, `pass.cpp`, `passes.h`, `type-ssa.wast`, `ReFinalize.cpp`, and type-shape helpers; the corrected source-backed contract is allocation-subtype specialization: selected `struct.new`, `array.new`, `array.new_data`, `array.new_elem`, and `array.new_fixed` sites receive fresh private exact subtypes when exact-observation blockers and interestingness rules allow, followed by function/module-code refinalization.
- Added `docs/wiki/raw/research/0386-2026-04-26-type-ssa-source-correction.md`; refreshed the overview, Binaryen strategy, implementation/test-map, focused mechanics page, WAT-shape catalog, and Starshine strategy page so readers can follow transformed shapes, upstream strategy, exact local registry omission, module/type-section prerequisites, and why the older 2026-04-23 created-type propagation model is superseded.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; kept `type-ssa` upstream-only and unimplemented in Starshine.

## [2026-04-26] research | deepen `string-lifting` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/string-lifting/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `string-lifting` because the dossier was source-correct but still made future implementers infer first-slice order, exact local prerequisites, and validation sequencing.
- Added `docs/wiki/raw/binaryen/2026-04-26-string-lifting-current-main-port-readiness.md`, rechecking official Binaryen current-main `StringLifting.cpp`, `string-lifting.wast`, and `pass.cpp`; no teaching-relevant drift was found from the tagged `version_129` contract, including magic and JSON constant lifting, exact helper signature checks, wrong-signature fatal behavior, module-code walk, custom-section removal, Strings feature enablement, and the cast-repair TODO.
- Added `docs/wiki/raw/research/0385-2026-04-26-string-lifting-port-readiness.md` and `docs/wiki/binaryen/passes/string-lifting/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, transformed-shape catalog, and Starshine status page so readers can follow the current unknown-pass local status, exact WAT/lib/binary/validator/HOT prerequisite code locations, missing lifted-op surface, magic-import first slice, JSON/custom-section second slice, helper-call third slice, cast-policy decision, and Binaryen oracle validation ladder.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; promoted `string-lifting` from `dossier` to `deep` while keeping it explicitly upstream-only and unimplemented in Starshine.

## [2026-04-26] implementation | land active `redundant-set-elimination`

- Implemented the first active Starshine `redundant-set-elimination` pass from the RSE wiki entry: same-value `local.set` shells become `drop(value)`, same-value `local.tee` shells become their value, different overwritten values stay intact, and the raw lowered-function path keeps large debug-artifact runs fast.
- Wired `src/passes/rse.mbt`, registry/dispatcher/CLI coverage, and `pass-fuzz-compare` Binaryen aliasing (`redundant-set-elimination` -> `--rse`).
- Filed durable status back into `docs/wiki/binaryen/passes/rse/`, `agent-todo.md`, and `CHANGELOG.md`; remaining work is refined strict-subtype `local.get` retargeting, full fixed-point CFG loop/diamond parity, and late preset-slot scheduling.
- Fresh evidence: `.tmp/pass-fuzz-rse-genvalid-10000-raw` (`10000/10000`, `0` mismatches), `.tmp/pass-fuzz-rse-10000-raw` (`6759` comparable matches, `0` mismatches, `20` Binaryen-side command failures), and `.tmp/self-opt-rse-native-20260426b` (canonical function equality and normalized WAT equality via fallback on the checked debug artifact).

## [2026-04-26] research | add `llvm-memory-copy-fill-lowering` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `llvm-memory-copy-fill-lowering` because the widened tracker had no obvious remaining `none` target but Starshine already carries `memory.copy` / `memory.fill` representation, validation, binary, HOT, and effect surfaces while no pass page explained Binaryen's helper-call compatibility lowering.
- Added `docs/wiki/raw/binaryen/2026-04-26-llvm-memory-copy-fill-lowering-primary-sources.md`, capturing official Binaryen `version_129` and current-main owner/registration/factory surfaces for `LLVMMemoryCopyFillLowering.cpp`, `pass.cpp`, and `passes.h`, plus a focused official lit-directory caveat: no pass-named `llvm-memory-copy-fill-lowering.wast` fixture was found in the checked directories.
- Added `docs/wiki/raw/research/0384-2026-04-26-llvm-memory-copy-fill-lowering-source-dossier.md` and a new living folder `docs/wiki/binaryen/passes/llvm-memory-copy-fill-lowering/` with overview, Binaryen strategy, implementation/test map, helper-call boundary guide, WAT-shape catalog, and Starshine status page. The dossier records `memory.copy` / `memory.fill` to helper-call replacement, source-backed boundaries with `memory-packing`, `multi-memory-lowering`, and `memory64-lowering`, current unknown-pass local status, and exact Starshine WAT/lib/binary/validator/HOT code surfaces.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked the new family as a justified upstream-only dossier rather than active or boundary-only Starshine support.

## [2026-04-26] health | refresh `discard-global-effects` drift wording

- Ran a focused touched-area hygiene pass after the `discard-global-effects` implementation/test-map commit and found the Binaryen strategy page still described only the older 2026-04-25 current-main spot check.
- Updated `docs/wiki/binaryen/passes/discard-global-effects/binaryen-strategy.md` to name the 2026-04-26 focused recheck and include the pass-runner invalidation path in the current-main no-drift sentence.

## [2026-04-26] research | deepen `discard-global-effects` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/discard-global-effects/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `discard-global-effects` because the dossier had correct metadata-only strategy coverage but still lacked the standard owner/helper/test-map page and under-taught Binaryen's automatic `addsEffects()` invalidation hook.
- Added `docs/wiki/raw/binaryen/2026-04-26-discard-global-effects-implementation-test-map.md`, rechecking official Binaryen `version_129` and current-main `GlobalEffects.cpp`, `pass.cpp`, `pass.h`, `wasm.h`, `effects.h`, and the Optimizer Cookbook; the focused recheck found no cleanup-sibling drift and records the public pass plus pass-runner invalidation lifecycle.
- Added `docs/wiki/raw/research/0383-2026-04-26-discard-global-effects-implementation-test-map.md` and `docs/wiki/binaryen/passes/discard-global-effects/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, metadata-shape catalog, and Starshine strategy so metadata shapes, upstream strategy, owner files, indirect producer/consumer test evidence, no-standalone-WAT-diff caveat, and exact local effect-cache/registry code surfaces are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `discard-global-effects` from `dossier` to `deep` while keeping it explicitly upstream-only and currently unknown to Starshine's registry.

## [2026-04-26] research | correct `rse` CFG strategy and port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/rse/` folder, and `docs/wiki/raw/research/`, then chose removed `rse` because the 2026-04-25 source correction had overcorrected the pass into a straight-line-only `PostWalker` and erased Binaryen's real CFG value-flow contract.
- Added `docs/wiki/raw/binaryen/2026-04-26-rse-cfg-source-correction.md`, rechecking official Binaryen `version_129` and current-main `RedundantSetElimination.cpp`, registration files, helper headers, and official tests; the focused recheck found no teaching-relevant current-main drift and restored the source-backed `CFGWalker`, block start/end value arrays, predecessor merge value numbers, same-value set/tee removal, strict-subtype local-get retargeting, and conditional refinalization model.
- Added `docs/wiki/raw/research/0382-2026-04-26-rse-cfg-source-correction-and-port-readiness.md` and `docs/wiki/binaryen/passes/rse/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, CFG guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local registry/dispatcher/use-def/type-context/backlog code locations, first-slice order, full parity slices, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `agent-todo.md`, and `CHANGELOG.md`; marked the 2026-04-25 RSE research note superseded for the straight-line-only claim, refreshed the older `0114` / `0259` supersession notices, and preserved the valid narrowing away from `LocalGraph`, liveness, memory/global/heap-field stores, and arbitrary overwritten-write deletion.

## [2026-04-26] research | deepen `avoid-reinterprets` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/avoid-reinterprets/` folder, and `docs/wiki/raw/research/`, then chose removed `avoid-reinterprets` because the dossier was source-correct but still left future implementers to infer the safe first Starshine slice, LocalGraph-equivalent proof decision, and validation ladder.
- Added `docs/wiki/raw/binaryen/2026-04-26-avoid-reinterprets-port-readiness-primary-sources.md`, rechecking official Binaryen `version_129` and current-main `AvoidReinterprets.cpp`, `pass.cpp`, `avoid-reinterprets.wast`, and `avoid-reinterprets64.wast`; the focused recheck found no teaching-relevant drift from the narrow full-width load/reinterpret contract.
- Added `docs/wiki/raw/research/0381-2026-04-26-avoid-reinterprets-port-readiness.md` and `docs/wiki/binaryen/passes/avoid-reinterprets/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, single-load-chain guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local registry/HOT/use-def/SSA/codegen code locations, direct first-slice order, indirect helper-local prerequisites, memory64 pointer-temp rule, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `avoid-reinterprets` from `dossier` to `deep` while keeping the pass explicitly removed and outside the current no-DWARF / saved-`-O4z` queues.

## [2026-04-26] research | deepen `directize` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/directize/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `directize` because the dossier had correct upstream strategy coverage but still left the future Starshine first slice and validation ladder scattered across registry, parser, IR, binary, HOT, validator, and backlog surfaces while the tracker still classified it as only `dossier`.
- Added `docs/wiki/raw/binaryen/2026-04-26-directize-port-readiness-primary-sources.md`, rechecking official Binaryen `version_129` and current-main sources for `Directize.cpp`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, `pass.cpp`, `passes.h`, and the three dedicated `directize*` lit files; the focused recheck found no teaching-relevant drift and recast the evidence as a port-readiness bridge.
- Added `docs/wiki/raw/research/0380-2026-04-26-directize-port-readiness.md` and `docs/wiki/binaryen/passes/directize/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, table-info guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local code locations, table-facts-first implementation order, `Known` / `Trap` / `Unknown` classifier seam, constant and `select` rewrite ladder, and Binaryen oracle comparison are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `directize` from `dossier` to `deep` while keeping the pass explicitly boundary-only and unimplemented.

## [2026-04-26] research | deepen `alignment-lowering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/alignment-lowering/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `alignment-lowering` because the dossier was source-correct but still left future implementers to infer the first Starshine slice, local memory-op code map, and validation ladder while the tracker still classified it as only `dossier`.
- Added `docs/wiki/raw/binaryen/2026-04-26-alignment-lowering-current-main-port-readiness.md`, rechecking official Binaryen `version_129` and current-main sources for `AlignmentLowering.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `bits.h`, and `alignment-lowering.wast`; the focused recheck found no teaching-relevant drift from the narrow scalar load/store chunk-lowering contract.
- Added `docs/wiki/raw/research/0379-2026-04-26-alignment-lowering-port-readiness.md` and `docs/wiki/binaryen/passes/alignment-lowering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, chunk-selection guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local WAT/lib/binary/HOT prerequisite code locations, first red-test order, direct Binaryen oracle comparison, and the unresolved HOT-vs-lib landing-zone decision are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `alignment-lowering` from `dossier` to `deep` while keeping the pass explicitly boundary-only and outside the current no-DWARF / saved-`-O4z` queue.

## [2026-04-25] research | deepen `loop-invariant-code-motion` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/loop-invariant-code-motion/` folder, and `docs/wiki/raw/research/`, then chose removed Batch 3 `loop-invariant-code-motion` / upstream `licm` because the dossier had the corrected Binaryen strategy but still lacked a dedicated first-slice / validation ladder and exact refreshed local line anchors.
- Added `docs/wiki/raw/binaryen/2026-04-25-loop-invariant-code-motion-current-main-port-readiness.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `LoopInvariantCodeMotion.cpp`, `pass.cpp`, `pass.h`, `effects.h`, `find_all.h`, `local-graph.h`, `wasm-builder.h`, and `licm.wast`; the focused recheck found no teaching-relevant drift from the none-typed unconditional loop-entry statement-motion contract.
- Added `docs/wiki/raw/research/0378-2026-04-25-loop-invariant-code-motion-port-readiness.md` and `docs/wiki/binaryen/passes/loop-invariant-code-motion/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, proof guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, alias-policy work, current removed-registry status, candidate safety proof, cleanup-safe rewrite, and Binaryen `--licm` validation ladder are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `loop-invariant-code-motion` from `dossier` to `deep` while keeping the pass explicitly unimplemented and outside the current no-DWARF / saved-`-O4z` queue.

## [2026-04-25] research | deepen `string-gathering` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/string-gathering/` folder, and `docs/wiki/raw/research/`, then chose unimplemented no-DWARF late-tail `string-gathering` because the dossier was useful but still had 2026-04-23 freshness wording, stale local line anchors, and no dedicated first-slice / validation ladder.
- Added `docs/wiki/raw/binaryen/2026-04-25-string-gathering-current-main-and-port-readiness.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `StringLowering.cpp`, `pass.cpp`, `passes.h`, `string-utils.*`, `module-utils.h`, `wasm-traversal.h`, `string-gathering.wast`, and neighboring `propagate-globals-globally.wast`; the focused recheck found no teaching-relevant drift from the tagged exact-`StringConst` scan, canonical immutable global, direct slot rewrite, and validity-first reorder contract.
- Added `docs/wiki/raw/research/0377-2026-04-25-string-gathering-port-readiness.md` and `docs/wiki/binaryen/passes/string-gathering/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, reuse/order guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local registry gap, current string literal / HOT prerequisite anchors, first registry-honesty slice, module-pass rewrite sequence, and Binaryen oracle comparison ladder are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; promoted `string-gathering` from `dossier` to `deep` while preserving the explicit implementation debt that the public pass spelling is still absent from Starshine's registry arrays.

## [2026-04-25] research | deepen `simplify-globals-optimizing` port readiness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/simplify-globals-optimizing/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `simplify-globals-optimizing` because the dossier had source/shape/status coverage but still left future implementers to infer the safe first Starshine slice and validation ladder.
- Added `docs/wiki/raw/binaryen/2026-04-25-simplify-globals-optimizing-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` `SimplifyGlobals.cpp`, `pass.cpp`, and `pass.h`; the focused recheck found no teaching-relevant drift from the existing contract and kept the shared-engine plus optimizing-specific changed-function default-rerun split explicit.
- Added `docs/wiki/raw/research/0376-2026-04-25-simplify-globals-optimizing-port-readiness.md` and `docs/wiki/binaryen/passes/simplify-globals-optimizing/starshine-port-readiness-and-validation.md`; refreshed the overview, Binaryen strategy, implementation/test-map, linear-trace guide, WAT-shape catalog, and Starshine strategy page so transformed shapes, upstream strategy, exact local boundary-only code locations, first-slice order, scheduler tests, oracle comparison, and late-tail replay sequencing are connected.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check promoted `simplify-globals-optimizing` from `dossier` to `deep` while keeping the pass explicitly unimplemented.

## [2026-04-25] research | refresh `precompute-propagate` current-main and code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/precompute-propagate/` folder, and `docs/wiki/raw/research/`, then chose removed `precompute-propagate` because the dossier was complete-looking but still had stale `working` status, a tracker `dossier` classification, and a Starshine status page that named functions without exact line ranges.
- Added `docs/wiki/raw/binaryen/2026-04-25-precompute-propagate-current-main-and-code-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `Precompute.cpp`, `pass.cpp`, `opt-utils.h`, `local-graph.h`, `properties.h`, `wasm-interpreter.h`, and the two dedicated `precompute-propagate*` lit files. The focused recheck found no teaching-relevant drift: the pass remains shared semantic precompute plus `LazyLocalGraph` local propagation, fallthrough-value set analysis, all-reaching-set get consensus, defaultable-entry handling, and one extra ordinary evaluator walk.
- Added `docs/wiki/raw/research/0375-2026-04-25-precompute-propagate-current-main-code-map.md`; refreshed the overview, Binaryen strategy, implementation/test-map, local-worklist guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, upstream strategy, and exact local removed-registry / active plain-precompute / dispatcher / preset line ranges.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; the touched-area health check promoted `precompute-propagate` from `dossier` to `deep` while keeping the local non-implementation and nested-rerun scheduler dependency explicit.

## [2026-04-25] research | correct `memory64-lowering` static-offset semantics

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/memory64-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `memory64-lowering` / `table64-lowering` because the dossier was complete-looking but had over-broad high-constant wording that could mislead a future Starshine port.
- Added `docs/wiki/raw/binaryen/2026-04-25-memory64-lowering-static-offset-correction.md`, rechecking official Binaryen current-main and tagged `version_129` `Memory64Lowering.cpp`, `memory64-lowering.wast`, and `table64-lowering.wast`; the correction narrows the high-offset trap family to static memory-access `offset=` immediates, keeps dynamic operand constants on the `i32.wrap_i64` path, keeps active offsets as expression-width repair, and teaches grow lowering as result-sentinel repair rather than high-constant-delta preclassification.
- Added `docs/wiki/raw/research/0374-2026-04-25-memory64-lowering-static-offset-correction.md` and `docs/wiki/binaryen/passes/memory64-lowering/static-offsets-dynamic-operands-and-grow-repair.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page so transformed shapes, Binaryen strategy, and exact local code surfaces remain internally consistent.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked the older research note `0340` partially superseded for high-constant wording while preserving its valid declaration, size/grow repair, mixed-width, max-limit, and local-status findings.

## [2026-04-25] research | add `code-folding` port-readiness bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/code-folding/` folder, and `docs/wiki/raw/research/`, then chose removed `code-folding` because the folder had the standard source/shape/status pages but still lacked a direct implementation-readiness bridge tying Binaryen's source-backed contract to concrete Starshine HOT prerequisites and first-slice validation.
- Added `docs/wiki/raw/binaryen/2026-04-25-code-folding-port-readiness-primary-sources.md`, rechecking official Binaryen current-main and tagged `version_129` sources for `CodeFolding.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, and `code-folding.wast`; no teaching-relevant current-main drift was found beyond the existing dossier contract.
- Added `docs/wiki/raw/research/0373-2026-04-25-code-folding-port-readiness.md` and `docs/wiki/binaryen/passes/code-folding/starshine-port-readiness-and-validation.md`; refreshed the landing, strategy, shape, terminating-tail, implementation/test-map, Starshine status, catalog, and tracker pages so the pass now has a source-backed minimum viable Starshine slice order, HOT builder / region / label / branch-verification code map, official-test-family-first validation ladder, neighborhood replay plan, and open design questions.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check promoted `code-folding` from `dossier` to `deep` in the tracker and closed the stale local-readiness gap while keeping the pass explicitly unimplemented.

## [2026-04-25] health | refresh `coalesce-locals` local code-map anchors

- Ran a focused touched-area reference-hygiene pass after the `coalesce-locals` port-readiness commit and found the existing implementation/test-map page still pointed to older `agent-todo.md` and `simplify_locals.mbt` line anchors.
- Updated `docs/wiki/binaryen/passes/coalesce-locals/implementation-structure-and-tests.md` so the current Starshine file map uses the live `CL` backlog range and the current simplify-locals cleanup phase anchors.

## [2026-04-25] research | deepen `coalesce-locals` port-readiness coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/coalesce-locals/` folder, and `docs/wiki/raw/research/`, then chose removed local `coalesce-locals` because it had the standard strategy/shape/source-map pages but still lacked one beginner-to-advanced implementation-readiness bridge and the tracker still marked it as `dossier` rather than `deep`.
- Added `docs/wiki/raw/research/0372-2026-04-25-coalesce-locals-port-readiness-health-check.md`, reusing the official Binaryen `version_129` and current-main source manifests already committed for `CoalesceLocals.cpp`, `pass.cpp`, and `coalesce-locals.wast`. The health check found no new teaching-level Binaryen drift; the remaining uncertainty is local architecture: direct module/function analysis versus HOT-assisted facts plus module-level declaration/name repair.
- Added `docs/wiki/binaryen/passes/coalesce-locals/starshine-port-readiness-and-validation.md`; refreshed the coalesce-locals landing page and Starshine strategy page so future implementers can follow current registry/dispatcher/preset/backlog state, reusable `reorder-locals` and `simplify-locals` substrates, minimum viable port shape, first required reduced tests, neighborhood replay, and parity signoff ladder.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check promoted `coalesce-locals` to a deep dossier and kept all edits isolated from unrelated local source changes already present in the worktree.

## [2026-04-25] research | refresh `asyncify` EH/options and source map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/asyncify/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `asyncify` because the dossier was useful but still under-taught exception/catch unwind options and sent readers toward a separate `AsyncifyLocals` owner that the reviewed source does not expose as a standalone navigation target.
- Added `docs/wiki/raw/binaryen/2026-04-25-asyncify-current-main-and-eh-options.md`, capturing official Binaryen current-main and tagged `version_129` `Asyncify.cpp`, `pass.cpp`, `passes.h`, `asyncify.wast`, and official Emscripten Asyncify documentation. The focused recheck found no teaching-level drift from the tagged contract, but made the EH/catch option boundary and `Asyncify.cpp` mini-pipeline source map explicit.
- Added `docs/wiki/raw/research/0371-2026-04-25-asyncify-current-main-and-eh-options.md` and `docs/wiki/binaryen/passes/asyncify/state-machine-memory-and-eh-boundaries.md`; refreshed the overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, state-machine/runtime helper behavior, memory/pointer-width requirements, indirect-call bookkeeping, EH/catch handling, tail-call rejection, and current local unknown-pass status.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check retired the stale `AsyncifyLocals` owner wording while preserving local save/restore as a core correctness requirement.

## [2026-04-25] research | add `multi-memory-lowering` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `multi-memory-lowering` because the widened tracker had no easy `none` target but Starshine already has memory-index representation, binary, validation, and memory-pass surfaces while no pass page explained Binaryen's many-memories-to-one compatibility lowering.
- Added `docs/wiki/raw/binaryen/2026-04-25-multi-memory-lowering-primary-sources.md`, capturing official Binaryen `version_129` and current-main source surfaces for `MultiMemoryLowering.cpp`, `pass.cpp`, `passes.h`, the checked and unchecked lit files, README `wasm-merge` context, and the WebAssembly multi-memory proposal overview. The source check found no teaching-relevant current-main drift: Binaryen still lowers many memories into one combined memory with offset globals, active-data repair, instruction address shifts, generated `memory.size` / `memory.grow` helpers, and an optional bounds-checking sibling.
- Added `docs/wiki/raw/research/0370-2026-04-25-multi-memory-lowering-source-dossier.md` and a new living folder `docs/wiki/binaryen/passes/multi-memory-lowering/` with overview, Binaryen strategy, implementation/test map, layout/bounds/growth guide, WAT-shape catalog, and Starshine status page. The Starshine page maps exact local registry absence plus memory representation, WAT lowering, binary codec, and typecheck surfaces while keeping clear that current requests fail as unknown and no lowering pass exists.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked the new family as a justified upstream-only dossier rather than an active or boundary-only Starshine pass.

## [2026-04-25] research | recheck `dataflow-optimization` current-main contract

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dataflow-optimization/` folder, and `docs/wiki/raw/research/`, then chose removed local registry pass `dataflow-optimization` / upstream `dfo` because the dossier was useful but still carried 2026-04-23 freshness wording and stale Starshine request-guard line anchors.
- Added `docs/wiki/raw/binaryen/2026-04-25-dataflow-optimization-current-main-recheck.md`, capturing official Binaryen current-main source surfaces for `DataFlowOpts.cpp`, `src/dataflow/*`, `flat.h`, `pass.cpp`, and the combo `flatten_simplify-locals-nonesting_dfo_O3.wast` test. The recheck found no teaching-relevant drift: `dfo` remains a flat-input, integer-local DataFlow SSA side-graph simplifier with identical-constant-phi collapse and nested-`precompute` expression folding.
- Added `docs/wiki/raw/research/0369-2026-04-25-dataflow-optimization-current-main-recheck.md` and refreshed the living overview, Binaryen strategy, implementation/test-map, Flat/DataFlow boundary guide, WAT-shape catalog, and Starshine status page; the Starshine page now points to current `src/passes/optimize.mbt:468-472` removed-pass rejection anchors and keeps the absence of a local `src/dataflow/` or Flat IR substrate explicit.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; kept the pass as a removed, dossier-covered port-planning target rather than implying active local implementation.

## [2026-04-25] research | deepen `simplify-locals-nostructure` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/simplify-locals-nostructure/` folder, neighboring simplify-locals family pages, and `docs/wiki/raw/research/`, then chose removed `simplify-locals-nostructure` because its dossier still lacked the now-standard owner/helper/lit-test implementation map and exact refreshed Starshine dispatcher-gap/code-line map.
- Added `docs/wiki/raw/binaryen/2026-04-25-simplify-locals-nostructure-current-main-and-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `opt-utils.h`, helper headers, and the dedicated no-structure test pair. The focused recheck found no teaching-relevant drift: upstream remains the shared locals-family engine instantiated as `SimplifyLocals<true, false, true>`, with teeing and existing-consumer nesting enabled, structure-building disabled, late cleanup still active, and pass-runner local fixups still part of the validation contract.
- Added `docs/wiki/raw/research/0368-2026-04-25-simplify-locals-nostructure-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/simplify-locals-nostructure/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, variant guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, upstream owner/test surfaces, `pass.h` nondefaultable-local repair, exact removed-registry and tuple-slot guard line ranges, current dispatcher absence, and the future MoonBit `SLNS` landing zone.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked `simplify-locals-nostructure` as a deep removed-pass dossier now that the implementation/test-map and local-line-map gap is closed.

## [2026-04-25] research | deepen `reorder-globals` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/reorder-globals/` folder, and `docs/wiki/raw/research/`, then chose boundary-only `reorder-globals` because its dossier still lacked the now-standard owner/helper/lit-test implementation map and exact local numeric-`GlobalIdx` prerequisite map.
- Added `docs/wiki/raw/binaryen/2026-04-25-reorder-globals-current-main-and-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `wasm-traversal.h`, `topological_sort.h`, `wasm.h`, `GlobalStructInference.cpp`, and the `reorder-globals{,-real}.wast` lit proof surfaces. The focused recheck found no teaching-relevant drift from the existing `version_129` contract.
- Added `docs/wiki/raw/research/0367-2026-04-25-reorder-globals-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/reorder-globals/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, size/dependency guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, upstream owner/test surfaces, official test-family split, boundary-only local status, and exact Starshine module/binary/validator/HOT `GlobalIdx` remap prerequisites.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked `reorder-globals` as a deep boundary-only dossier now that the implementation/test-map gap is closed.

## [2026-04-25] research | deepen `dae-optimizing` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/dae-optimizing/` folder, the neighboring plain-DAE implementation/test-map page, and `docs/wiki/raw/research/`, then chose boundary-only `dae-optimizing` because its dossier still lacked the now-standard owner/helper/lit-test map page.
- Added `docs/wiki/raw/binaryen/2026-04-25-dae-optimizing-current-main-and-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `return-utils.h`, `lubs.h`, `type-updating.h`, helper `utils.h`, the distributed DAE lit family, and upstream PR `#8072` context for the unprofitable single-call-chain stop rule. The focused recheck found no teaching-relevant drift: `dae-optimizing` remains the shared DAE boundary engine plus the optimizing-only `optimizeAfterInlining(...)` nested cleanup suffix.
- Added `docs/wiki/raw/research/0366-2026-04-25-dae-optimizing-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/dae-optimizing/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, signature/rerun guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, upstream owner/test surfaces, current descriptive boundary-only local spelling, DAE backlog slices, and the still-missing module-boundary plus nested-rerun scheduler implementation.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked `dae-optimizing` as a deep unimplemented dossier now that the implementation/test-map gap is closed.

## [2026-04-25] research | deepen `heap2local` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/heap2local/` folder, and `docs/wiki/raw/research/`, then chose active `heap2local` because the dossier was useful but still lacked the now-standard implementation/test-map page and exact current local line ranges.
- Added `docs/wiki/raw/binaryen/2026-04-25-heap2local-current-main-and-code-map.md`, capturing official Binaryen current-main and tagged `version_129` source surfaces for `Heap2Local.cpp`, `heap2local.wast`, `pass.cpp`, `pass.h`, `type-updating.h`, and helper headers. The focused recheck found no new teaching-relevant drift beyond the earlier array/cmpxchg/unreachable-`ref.test` caveat: upstream remains conservative nonescaping-plus-exclusive GC scalarization with array-to-synthetic-struct lowering and validation repair.
- Added `docs/wiki/raw/research/0365-2026-04-25-heap2local-current-main-and-code-map.md` and `docs/wiki/binaryen/passes/heap2local/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, validation/fixup guide, WAT-shape catalog, Starshine HOT strategy, and parity page so readers can map transformed shapes, upstream owner/test surfaces, exact local registry/preset/dispatcher/candidate/rewrite/test line ranges, and the remaining nondefaultable-local/refinalization gap.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check fixed stale parity wording that had listed atomic array access as a generic upstream bailout even though Binaryen source has atomic/RMW/cmpxchg handling when nonescape and exclusivity are proven.

## [2026-04-25] research | deepen `optimize-casts` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/optimize-casts/` folder, and `docs/wiki/raw/research/`, then chose unimplemented `optimize-casts` because its dossier had strategy, shape, and Starshine pages but still lacked the now-standard owner/helper/lit-test implementation map and exact local prerequisite code map.
- Added `docs/wiki/raw/binaryen/2026-04-25-optimize-casts-current-main-and-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `OptimizeCasts.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, `linear-execution.h`, `properties.h`, `effects.h`, `utils.h`, and `optimize-casts.wast`. The recheck found no teaching-relevant drift: upstream remains a GC-gated two-phase `ref.cast` / `ref.as_non_null` local-flow pass, not a generic `ref.test`, descriptor-cast, extern-conversion, or CFG-wide cast optimizer.
- Added `docs/wiki/raw/research/0364-2026-04-25-optimize-casts-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/optimize-casts/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, two-phase dataflow guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, upstream owner/helper/test surfaces, current removed-registry behavior, the broader-than-upstream `OC` backlog caveat, and exact local parser/IR/binary/validator/HOT prerequisite code locations.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked `optimize-casts` as a deep unimplemented dossier and recorded that the former implementation/test-map gap is closed.

## [2026-04-25] research | correct `merge-locals` source strategy and test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/merge-locals/` folder, and `docs/wiki/raw/research/`, then chose unimplemented `merge-locals` because it looked complete but still lacked the standard implementation/test-map page and carried a stale 2026-04-23 `EquivalentCopies` / `LocalStructuralDominance` overcorrection.
- Added `docs/wiki/raw/binaryen/2026-04-25-merge-locals-current-main-source-correction.md`, capturing official Binaryen `version_129` and current-main sources for `MergeLocals.cpp`, `pass.cpp`, `passes.h`, `local-graph.h`, and `merge-locals.wast`. The recheck found no teaching-relevant current-main drift, but corrected the maintained contract back to one-set local merging with a local-name bailout, simple-value gate, graph-influenced-get proof, small source-local reuse, and fresh-temp fallback.
- Added `docs/wiki/raw/research/0363-2026-04-25-merge-locals-source-correction-and-test-map.md` and `docs/wiki/binaryen/passes/merge-locals/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, LocalGraph guide, WAT-shape catalog, and Starshine status page so transformed shapes, upstream owner/test surfaces, current removed-registry behavior, request rejection, and future port prerequisites are aligned.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked research notes `0128` and `0272` superseded for implementation details while preserving their queue/context provenance.

## [2026-04-25] research | correct and deepen `local-subtyping` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/local-subtyping/` folder, and `docs/wiki/raw/research/`, then chose unimplemented `local-subtyping` because the dossier still lacked the now-standard implementation/test-map page and the 2026-04-22 source correction had overcorrected the owner-file mechanics.
- Added `docs/wiki/raw/binaryen/2026-04-25-local-subtyping-implementation-test-map-source-correction.md`, capturing official Binaryen `version_129` and current-main sources for `LocalSubtyping.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, `lubs.*`, `local-structural-dominance.h`, and `local-subtyping.wast`. The recheck found no teaching-relevant current-main drift, but corrected the repo's prior reading: upstream records gets, iterates with `ReFinalize()`, scans params but rewrites only body locals, and uses set-fed LUBs plus get-aware non-null dominance/type repair rather than a set-only/no-refinalize pass.
- Added `docs/wiki/raw/research/0362-2026-04-25-local-subtyping-implementation-test-map-source-correction.md` and `docs/wiki/binaryen/passes/local-subtyping/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, LUB/dominance guide, WAT-shape catalog, and Starshine status page so transformed shapes, Binaryen strategy, source/test surfaces, and exact Starshine registry, dispatcher-gap, preset-honesty, backlog, type-model, validator, and HOT-local prerequisites are aligned.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked `docs/wiki/raw/research/0261-2026-04-22-local-subtyping-source-correction-and-starshine-followup.md` partially superseded for the corrected owner-file mechanics while preserving it as first-correction provenance.

## [2026-04-25] research | deepen `inlining-optimizing` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/inlining-optimizing/` folder, the neighboring `inlining` implementation/test-map page, and `docs/wiki/raw/research/`, then chose boundary-only `inlining-optimizing` because its dossier had overview, strategy, WAT shapes, and Starshine status coverage but still lacked the now-standard implementation/test-map page.
- Added `docs/wiki/raw/binaryen/2026-04-25-inlining-optimizing-current-main-implementation-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `Inlining.cpp`, `pass.cpp`, `opt-utils.h`, `pass.h`, `NoInline.cpp`, `module-utils.cpp`, and the shared `inlining*` / `no-inline*` / `inline-main` lit proof surfaces. The focused recheck found no teaching-relevant drift: `inlining-optimizing` remains the shared direct-call inliner with the optimizing `precompute-propagate` plus default-function-pipeline rerun suffix.
- Added `docs/wiki/raw/research/0361-2026-04-25-inlining-optimizing-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/inlining-optimizing/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, planning/rerun guide, WAT-shape catalog, and Starshine status page so readers can map transformed shapes, Binaryen owner/test surfaces, and exact Starshine boundary-only registry, request-guard, backlog, and scheduler locations.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check fixed stale Starshine line anchors in the `inlining-optimizing` strategy page and marked the folder as a deep dossier in the tracker.

## [2026-04-25] research | deepen `flatten` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/flatten/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `flatten` because the dossier had strategy, shapes, and Starshine status coverage but still lacked the now-standard implementation/test-map page and current-main owner/test proof split.
- Added `docs/wiki/raw/binaryen/2026-04-25-flatten-current-main-implementation-test-map.md`, capturing official Binaryen current-main and tagged `version_129` sources for `Flatten.cpp`, `flat.h`, `pass.cpp`, `passes.h`, helper surfaces, and the `flatten.wast` / `flatten_all-features.wast` / `flatten-eh-legacy.wast` proof lanes. The focused recheck found no teaching-relevant drift: upstream remains a function-parallel Flat-IR normalizer based on preludes, branch-target temps, value-carrying control erasure, tee removal, branch-payload routing, unsupported `BrOn*` / `TryTable` fatal boundaries, and EH nested-pop repair.
- Added `docs/wiki/raw/research/0360-2026-04-25-flatten-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/flatten/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, Flat-IR/prelude guide, WAT-shape catalog, and Starshine status page with exact local removed-registry, CLI-spelling, dispatcher-gap, Batch 2 planning, and no-active-backlog-slice locations.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; the touched-area health check marked `flatten` as a deep dossier and replaced stale generic proof wording with the smoke/all-features/EH official-test split.

## [2026-04-25] research | deepen `signext-lowering` implementation/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/signext-lowering/` folder, and `docs/wiki/raw/research/`, then chose upstream-only `signext-lowering` because its dossier was useful but still lacked the now-standard implementation/test-map page and carried an over-broad lit-proof wording around feature metadata.
- Added `docs/wiki/raw/binaryen/2026-04-25-signext-lowering-implementation-test-map-source-correction.md`, capturing official Binaryen `version_129` and current-main owner/registration/test sources. The source correction keeps the pass contract unchanged but narrows the evidence: `SignExtLowering.cpp` proves `FeatureSet::SignExt` clearing, while `test/lit/passes/signext-lowering.wast` directly proves the five instruction-shape rewrites rather than target-feature custom-section output.
- Added `docs/wiki/raw/research/0359-2026-04-25-signext-lowering-implementation-test-map.md` and `docs/wiki/binaryen/passes/signext-lowering/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, WAT-shape catalog, and Starshine status page with exact local parser, lib IR, encoder, validator, HOT-lift, `pick-load-signs`, unknown-registry, dispatcher-gap, and pretty-printer caveat line ranges.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; health-check edits in the touched folder replaced stale “lit proves feature removal” wording with source-proven feature-clearing wording and marked the dossier `deep` in the tracker.

## [2026-04-25] research | refresh `local-cse` current-main and test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/local-cse/` folder, and `docs/wiki/raw/research/`, then chose unimplemented `local-cse` because its dossier was useful but still lacked the now-standard implementation/test-map page and current local code-map precision.
- Added `docs/wiki/raw/binaryen/2026-04-25-local-cse-current-main-code-map.md`, capturing official Binaryen `version_129` and current-main sources for `LocalCSE.cpp`, `pass.cpp`, `opt-utils.h`, `linear-execution.h`, `effects.h`, `properties.*`, `intrinsics.h`, `cost.h`, and `local-cse.wast`. The recheck found no teaching-relevant drift: upstream remains a scan/check/apply temp-local reuse pass for repeated whole expression trees in limited linear windows.
- Added `docs/wiki/raw/research/0358-2026-04-25-local-cse-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/local-cse/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, window/barrier guide, WAT-shape catalog, and Starshine status page so the folder now maps transformed shapes, Binaryen owner/test surfaces, and exact local removed-registry, request-rejection, dispatcher-gap, local-cleanup, local-index rewrite, backlog, and scheduler code locations.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; the tracker now marks `local-cse` as a deep unimplemented dossier rather than a less-specific dossier-only entry. Focused touched-area hygiene found no stale 2026-04-22-only current-main wording left in the `local-cse` folder and tightened the folder maintenance rule so future findings update the strategy, implementation/test-map, and windows/barriers pages together.

## [2026-04-25] health | fix `merge-blocks` pass-manager wording

- Ran a focused reference-hygiene check after the `merge-blocks` source-correction commit and found the new implementation/test-map and Starshine strategy pages described `src/passes/pass_manager.mbt:7813` as trace integration.
- Corrected that wording to the actual code surface: HOT lower/writeback options for `merge-blocks`, with the dispatcher call still at `src/passes/pass_manager.mbt:8704`.

## [2026-04-25] research | correct `merge-blocks` source strategy and code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/merge-blocks/`, and `docs/wiki/raw/research/`, then chose active `merge-blocks` because its dossier was complete-looking but still taught the stale Binaryen model of tail-child-only unnamed/same-name block flattening and lacked the now-standard implementation/test-map page.
- Added `docs/wiki/raw/binaryen/2026-04-25-merge-blocks-current-main-source-correction.md`, capturing official Binaryen `version_129` and current-main `MergeBlocks.cpp`, `merge-blocks.wast`, `pass.cpp`, `branch-utils.h`, `effects.h`, and `wasm-traversal.h` URLs. The corrected source reading is that upstream `merge-blocks` is named-block deblocking with function-wide ambiguity prescan, recursive branch-user retargeting proof, effect barriers, block / `if` / terminal visitors, scope-name rewrites, and mandatory refinalization; the official lit file's `no-merge-nameless` coverage contradicts the older unnamed-tail-positive teaching.
- Added `docs/wiki/raw/research/0357-2026-04-25-merge-blocks-source-correction-and-code-map.md` and `docs/wiki/binaryen/passes/merge-blocks/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, WAT-shape catalog, and Starshine HOT strategy so the dossier now maps transformed shapes, corrected Binaryen strategy, and exact local registry, dispatcher, label-use, typed-carrier, unreachable-repair, preset, CLI, and test line ranges.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and marked older research notes `0111` and `0255` partially superseded for the Binaryen strategy overread while preserving them for scheduler and Starshine-status history.

## [2026-04-25] research | refresh `heap-store-optimization` current-main and code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/heap-store-optimization/`, and `docs/wiki/raw/research/`, then chose active `heap-store-optimization` because the folder was useful but still lacked the implementation/test-map page now standard for neighboring active passes and the Starshine strategy had mostly helper names rather than exact line ranges.
- Added `docs/wiki/raw/binaryen/2026-04-25-heap-store-optimization-current-main-code-map.md`, capturing official Binaryen current-main `HeapStoreOptimization.cpp`, `heap-store-optimization.wast`, `pass.cpp`, `local-graph.h`, and `effects.h` URLs plus exact local Starshine owner/test/dispatcher surfaces. The focused current-main recheck found no teaching-relevant drift: the pass remains narrow `struct.set`-into-fresh-`struct.new` folding, not generic heap dead-store elimination or load forwarding.
- Added `docs/wiki/raw/research/0356-2026-04-25-heap-store-optimization-current-main-code-map.md` and `docs/wiki/binaryen/passes/heap-store-optimization/implementation-structure-and-tests.md`; refreshed the overview, Binaryen strategy, and Starshine HOT strategy so the dossier now links the upstream owner/helper/test surfaces and exact MoonBit registry, preset, dispatcher, raw fast-skip, fold-helper, region-processing, focused-test, perf-test, and CLI-replay line ranges.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; health-check edits in the touched area replaced stale 2026-04-20 / 2026-04-22 freshness wording with the new 2026-04-25 source bridge while keeping the `version_129` oracle and current-main no-drift caveat explicit.

## [2026-04-25] research | correct `remove-relaxed-simd` feature-gate overread

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/remove-relaxed-simd/`, and `docs/wiki/raw/research/`, then chose upstream-only `remove-relaxed-simd` because its dossier was complete but still carried stale feature-gate / changed-function wording and an unresolved metadata caveat worth narrowing with current primary sources.
- Added `docs/wiki/raw/binaryen/2026-04-25-remove-relaxed-simd-current-main-source-correction.md` and `docs/wiki/raw/research/0355-2026-04-25-remove-relaxed-simd-current-main-source-correction.md`, capturing official Binaryen `version_129` and current-`main` `RemoveRelaxedSIMD.cpp`, `pass.cpp`, `passes.h`, `remove-relaxed-simd.wast`, and relaxed-SIMD proposal context.
- Refreshed the living `remove-relaxed-simd` overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status page. The corrected contract is trap replacement plus `ChildLocalizer` child-effect preservation and postwalk refinalization, with no reviewed owner-file feature gate or changed-flag-gated refinalization; the dossier now also records the Binaryen-vs-Starshine dot-product spelling caveat and keeps feature-section cleanup separate from instruction replacement.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0322` partially superseded for the feature-gate and changed-function overread while preserving it as the original dossier provenance.

## [2026-04-25] health | fix `const-hoisting` WAT-shape heading numbering

- Ran a focused reference-hygiene check after the `const-hoisting` source-bridge commit and found the WAT-shape page still had two `Bailout family 4` headings after the earlier `v128.const` heading correction.
- Updated `docs/wiki/binaryen/passes/const-hoisting/wat-shapes.md` so the cross-function repeat bailout is numbered `Bailout family 5`, keeping the shape catalog easier to scan and link.

## [2026-04-25] research | refresh `const-hoisting` current-main and Starshine code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose removed local pass `const-hoisting` because its dossier was complete but still had older freshness wording and a high-level Starshine page that did not point to the concrete HOT/local/binary primitives a future port would reuse.
- Added `docs/wiki/raw/binaryen/2026-04-25-const-hoisting-current-main-recheck.md`, capturing official Binaryen current-main sources for `ConstHoisting.cpp`, `const-hoisting.wast`, `pass.cpp`, `literal.h`, and `wasm-binary.h`; the check found no teaching-relevant drift from the `version_129` contract.
- Added `docs/wiki/raw/research/0354-2026-04-25-const-hoisting-current-main-code-map.md` and refreshed the living `const-hoisting` overview, Binaryen strategy, implementation/test-map, shape, size-model, literal-identity, and Starshine strategy pages. The Starshine page now links exact local prerequisites in `src/passes/optimize.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/ir/hot_builders.mbt`, `src/ir/hot_mutate.mbt`, and `src/binary/encode.mbt`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; fixed a nearby heading-numbering hygiene issue in the `const-hoisting` WAT-shape page while reviewing links.

## [2026-04-25] health | link `discard-global-effects` from global-effects source map

- Ran a focused reference-hygiene check after adding the `discard-global-effects` dossier and found the `global-effects` implementation/test-map page still mentioned the cleanup sibling as plain text with older 2026-04-24 source metadata.
- Updated `docs/wiki/binaryen/passes/global-effects/implementation-structure-and-tests.md` and one lingering sentence in `docs/wiki/binaryen/passes/global-effects/binaryen-strategy.md` so every source-map mention points at the new cleanup sibling dossier and cites the 2026-04-25 raw source manifest.

## [2026-04-25] research | add `discard-global-effects` lifecycle dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/global-effects/`, and `docs/wiki/raw/research/`, then chose upstream `discard-global-effects` because the producer-side `global-effects` dossier repeatedly mentioned the cleanup sibling but the pass still lacked its own canonical overview, metadata-shape, Binaryen strategy, and Starshine status pages.
- Added `docs/wiki/raw/binaryen/2026-04-25-discard-global-effects-primary-sources.md`, capturing official Binaryen `version_129` and current-main sources for `GlobalEffects.cpp` and `pass.cpp`, plus `wasm.h`, `effects.h`, and the Optimizer Cookbook lifecycle rule for effect-adding passes. The source check found no teaching-relevant cleanup-sibling drift: `discard-global-effects` remains a public metadata cleanup pass that clears per-function global-effect summaries rather than rewriting Wasm bodies.
- Added `docs/wiki/raw/research/0353-2026-04-25-discard-global-effects-source-dossier.md` plus `docs/wiki/binaryen/passes/discard-global-effects/`, covering purpose, invariants, metadata states, validation guidance, upstream implementation strategy, no-dedicated-lit-file caveat, and current Starshine status.
- Refreshed the neighboring `global-effects` overview, Binaryen strategy, naming/consumer guide, and Starshine strategy so the producer and cleanup sibling no longer duplicate each other. Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; grounded local status in `src/passes/optimize.mbt:128-142`, `src/passes/optimize.mbt:455-473`, `src/ir/effects.mbt:5-279`, `src/ir/analysis_cache.mbt:34`, `src/ir/analysis_cache.mbt:70-81`, `src/ir/analysis_cache.mbt:217-227`, `src/passes/pass_common.mbt:212-234`, and `src/passes/pass_manager.mbt:8717-8720`.

## [2026-04-25] research | refresh `coalesce-locals` source/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose removed local pass `coalesce-locals` because its dossier had overview, strategy, shapes, and Starshine status coverage but still lacked the source-confirmed implementation/test-map page now standard for neighboring pass folders.
- Added `docs/wiki/raw/binaryen/2026-04-25-coalesce-locals-current-main-recheck.md`, capturing official Binaryen current-main sources for `CoalesceLocals.cpp`, `pass.cpp`, `opt-utils.h`, helper headers, and `coalesce-locals.wast`, while retaining `version_129` as the tagged source oracle. The recheck found no teaching-relevant drift: `coalesce-locals` remains a late function-parallel local-slot coalescer built from liveness/value-number interference, exact-type greedy coloring, backedge copy priority, and rewrite cleanup/refinalization.
- Added `docs/wiki/raw/research/0352-2026-04-25-coalesce-locals-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/coalesce-locals/implementation-structure-and-tests.md`, mapping upstream owner/helper/test surfaces and exact local status points: `src/passes/optimize.mbt:144-151`, `src/passes/pass_manager.mbt`, `src/passes/reorder_locals.mbt:2`, `src/passes/reorder_locals.mbt:118`, `src/passes/reorder_locals.mbt:183`, `src/passes/reorder_locals.mbt:544`, `src/passes/simplify_locals.mbt:2`, `src/passes/simplify_locals.mbt:15`, `src/passes/simplify_locals.mbt:70`, `src/passes/simplify_locals.mbt:4132`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md:33`, and `agent-todo.md` backlog/order surfaces.
- Refreshed the living `coalesce-locals` overview, Binaryen strategy, interference/order guide, WAT-shape catalog, Starshine strategy, `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the pass dossier now maps transformed shapes, Binaryen strategy, Starshine status, and test/validation surfaces without duplicating the existing pages. `CHANGELOG.md` was left untouched because unrelated local edits pre-existed this run.

## [2026-04-25] research | refresh `code-folding` source/test map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose removed local pass `code-folding` because its dossier had overview, strategy, shapes, and Starshine status coverage but still lacked the source-confirmed implementation/test-map page now standard for neighboring pass folders.
- Added `docs/wiki/raw/binaryen/2026-04-25-code-folding-current-main-recheck.md`, capturing official Binaryen current-main sources for `CodeFolding.cpp`, `pass.cpp`, `opt-utils.h`, `passes.h`, and `code-folding.wast`, while retaining `version_129` as the tagged source oracle. The recheck found no teaching-relevant drift: `code-folding` remains a function-parallel duplicate-tail sharing pass split between expression exits and function-ending terminators, guarded by unsupported-branch poisoning, branch-target scope checks, EH movement barriers, profitability, and nested-pop repair.
- Added `docs/wiki/raw/research/0351-2026-04-25-code-folding-current-main-and-test-map.md` and `docs/wiki/binaryen/passes/code-folding/implementation-structure-and-tests.md`, mapping upstream owner/helper/test surfaces and exact local status points: `src/passes/optimize.mbt:144-151`, `src/passes/optimize.mbt:385-410`, `src/passes/optimize.mbt:469-471`, `src/cli/cli_test.mbt:159-165`, `src/cli/cli_test.mbt:297-309`, `src/passes/pass_manager.mbt`, `agent-todo.md:443-448`, and `docs/wiki/binaryen/no-dwarf-default-optimize-path.md:33`.
- Refreshed the living `code-folding` overview, Binaryen strategy, terminating-tail guide, WAT-shape catalog, Starshine strategy, `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the pass dossier now maps transformed shapes, Binaryen strategy, Starshine status, and test/validation surfaces without duplicating the existing pages.

## [2026-04-25] research | recheck `directize` current-main contract

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `directize` because the tracker has no obvious still-`none` target but this final no-DWARF tail pass still carried older 2026-04-22 current-main freshness wording around a table-analysis contract that is easy to over-widen.
- Added `docs/wiki/raw/binaryen/2026-04-25-directize-current-main-recheck.md`, capturing official Binaryen current-`main` sources for `Directize.cpp`, `pass.cpp`, `passes.h`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, and the three dedicated `directize*` lit files, with `version_129` retained as the tagged source oracle.
- Added `docs/wiki/raw/research/0350-2026-04-25-directize-current-main-recheck.md` and refreshed the living `directize` overview, Binaryen strategy, implementation/test-map, table-info guide, WAT-shape catalog, and Starshine strategy page. The recheck found no teaching-relevant drift: `directize` remains a table-info-gated `CallIndirect` rewrite pass with `Known` / `Trap` / `Unknown` target classification, narrow `select` lowering, side-effect-preserving trap rewrites, and post-edit type repair.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; sharpened the local code map to `src/passes/optimize.mbt:128-134`, `src/passes/optimize.mbt:462-468`, `agent-todo.md:687-691`, and `docs/wiki/binaryen/no-dwarf-default-optimize-path.md:34-35`, while keeping the source-backed-but-less-directly-lit `table.copy` mutation-barrier caveat explicit.

## [2026-04-25] research | add `signext-lowering` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `signext-lowering` because the tracker had no obvious `none` queue but Starshine already supports sign-extension instructions while neighboring `optimize-instructions` / `pick-load-signs` docs did not explain Binaryen's separate feature-lowering pass.
- Added `docs/wiki/raw/binaryen/2026-04-25-signext-lowering-primary-sources.md`, capturing Binaryen `version_129` `SignExtLowering.cpp`, `pass.cpp`, `passes.h`, the dedicated `signext-lowering.wast` lit test, and a focused current-main no-teaching-drift spot check.
- Added `docs/wiki/raw/research/0349-2026-04-25-signext-lowering-source-dossier.md` plus a new living dossier under `docs/wiki/binaryen/passes/signext-lowering/`, covering overview, Binaryen strategy, WAT shapes, and current Starshine status / future-port map.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no active/boundary-only/removed entry, `src/passes/pass_manager.mbt` has no dispatcher, and `src/wast/types.mbt`, `src/wast/keywords.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/lib/types.mbt`, `src/binary/encode.mbt`, `src/validate/typecheck.mbt`, `src/ir/hot_lift.mbt`, and `src/passes/pick_load_signs.mbt` are prerequisites rather than a lowering pass.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md`; recorded the local feature-metadata caveat that Starshine does not currently expose Binaryen's exact `FeatureSet::SignExt` model.

## [2026-04-25] research | correct `rse` strategy overread

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `rse` because the living dossier was complete-looking but still taught an over-broad LocalGraph/liveness/dataflow strategy for Binaryen `version_129`.
- Added `docs/wiki/raw/binaryen/2026-04-25-rse-source-correction.md` and `docs/wiki/raw/research/0348-2026-04-25-rse-source-correction-and-starshine-followup.md`, capturing the corrected primary-source contract: `RedundantSetElimination.cpp` is a small `PostWalker` with one value-number slot per local, same-value `local.set` / `local.tee` shell removal, `local.get` type refinement, conservative fact clearing, refinalization, and nested `vacuum`.
- Refreshed the `rse` overview, Binaryen strategy, value/barrier guide, WAT-shape catalog, and Starshine strategy; added `docs/wiki/binaryen/passes/rse/implementation-structure-and-tests.md`; and marked older notes `0114` and `0259` partially superseded for the over-broad strategy interpretation while preserving them as provenance.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `agent-todo.md` so future work plans no longer start from stale overwritten-write, globals, GC-field-write, liveness, or predecessor-merge expectations.

## [2026-04-25] research | recheck `untee` current-main freshness

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose removed local registry pass `untee` because the dossier was otherwise complete but still carried stale “2026-04-21 drift check” wording in pages reviewed on 2026-04-23.
- Added `docs/wiki/raw/binaryen/2026-04-25-untee-current-main-recheck.md` and `docs/wiki/raw/research/0347-2026-04-25-untee-current-main-recheck.md`, capturing a primary-source current-main recheck of Binaryen `Untee.cpp`, `pass.cpp`, `passes.h`, `SimplifyLocals.cpp`, and `untee.wast`.
- Confirmed no teaching-relevant drift from the `version_129` contract: `untee` remains a tiny `LocalSet` / `local.tee` postwalk, reachable tees still expand to `local.set` plus declared-type `local.get`, unreachable tees still delete the shell, the public registration and constructor declaration remain present, and the pass remains absent from the reviewed default function-optimization roster.
- Refreshed the `untee` overview, Binaryen strategy, implementation/test-map, boundary guide, WAT-shape catalog, and Starshine strategy page; updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the no-drift bridge and corrected freshness wording are discoverable.

## [2026-04-25] research | correct `string-lifting` wrong-signature behavior

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `string-lifting` because the dossier was complete but carried a stale behavior claim: recognized `wasm:js-string` helper names with wrong signatures were described as unchanged-call bailouts.
- Added `docs/wiki/raw/binaryen/2026-04-25-string-lifting-signature-fatal-source-correction.md` and `docs/wiki/raw/research/0346-2026-04-25-string-lifting-signature-fatal-source-correction.md`, capturing the source-backed correction that Binaryen treats recognized helper names with wrong expected types as fatal pass errors, while wrong modules remain unchanged and unknown helper names warn / remain unchanged.
- Refreshed the `string-lifting` overview, Binaryen strategy, import/call shape catalog, implementation/test-map, and Starshine strategy page so transformed shapes, validation guidance, and future local parity planning no longer teach the wrong-signature case as a bailout.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked research note `0327` partially superseded only for the wrong-signature behavior while preserving it as the original pass-dossier provenance.

## [2026-04-25] research | correct `code-pushing` source strategy and local status

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose active `code-pushing` because the dossier still contained stale `BranchSeeker` / `Pusher` / `benefit > cost` / two-live-arm duplication claims and the Starshine page still ended with obsolete “no transform yet” wording despite the active HOT pass.
- Added `docs/wiki/raw/binaryen/2026-04-25-code-pushing-source-correction-and-local-status.md` and `docs/wiki/raw/research/0345-2026-04-25-code-pushing-source-correction-and-local-status.md`, capturing the corrected official Binaryen `version_129` source map: `visitBlock`, one-unreachable-arm `optimizeIntoIf`, `canPushThrough`, `tryPush`, the dedicated `code-pushing*` lit roster, and a focused current-main no-teaching-drift spot check.
- Added `docs/wiki/binaryen/passes/code-pushing/implementation-structure-and-tests.md` and refreshed the overview, Binaryen strategy, barrier guide, WAT-shape catalog, and Starshine strategy page so the dossier now maps upstream transformed shapes to the current Starshine subset: const-like single-consuming-arm `local.set` sinking plus a separate local dead-block flattening helper in `src/passes/code_pushing.mbt`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `CHANGELOG.md`, and marked older research notes `0115` and `0258` superseded for current strategy and Starshine status while preserving them as historical provenance.

## [2026-04-25] health | refresh stale GSI freshness wording

- Ran a focused reference-hygiene check after the `global-struct-inference` source-map commit and found the overview still had one leftover 2026-04-20 freshness sentence even though the same page's Freshness note had been updated to the 2026-04-25 source bridge.
- Updated `docs/wiki/binaryen/passes/global-struct-inference/index.md` so the durable-takeaways section now uses the same 2026-04-25 no-teaching-drift wording as the rest of the refreshed dossier.

## [2026-04-25] research | add `global-struct-inference` source manifest and code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose active `global-struct-inference` because the living dossier already taught the core pass but still lacked an immutable plain-GSI raw primary-source manifest and the implementation/test-map page now standard for neighboring pass folders.
- Added `docs/wiki/raw/binaryen/2026-04-25-global-struct-inference-primary-sources.md`, capturing the official Binaryen `version_129` release, `GlobalStructInference.cpp`, `pass.cpp`, `passes.h`, helper headers for possible constants / subtypes / packed fields / module analysis / name generation, `ReorderGlobals.cpp`, the dedicated `gsi.wast` proof surface, and a focused current-main no-teaching-drift spot check.
- Added `docs/wiki/raw/research/0344-2026-04-25-global-struct-inference-primary-sources-and-code-map-followup.md` and `docs/wiki/binaryen/passes/global-struct-inference/implementation-structure-and-tests.md`, grounding the source-backed contract in exact Starshine code locations: `src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/passes/global_struct_inference.mbt`, and `src/passes/global_struct_inference_test.mbt`.
- Refreshed the `global-struct-inference` overview, Binaryen strategy, WAT-shape catalog, closed-world/un-nesting guide, Starshine strategy, and parity page so readers can follow the full chain from transformed shapes to Binaryen strategy to current Starshine's narrower closed-world direct-global subset.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older notes `0140` and `0234` superseded for raw-source provenance / code-map completeness while preserving them as the original Binaryen and Starshine strategy investigations.

## [2026-04-25] research | add `minify-imports` source correction dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `minify-imports` because the newly added import/export minification folder still hid a separate upstream public pass and carried a stale `WasmBinaryBuilder::getSymbolMap(...)` attribution.
- Added `docs/wiki/raw/binaryen/2026-04-25-minify-imports-family-source-correction.md`, capturing the official Binaryen `version_129` release, `MinifyImports.cpp`, `MinifyImportsAndExports.cpp`, `pass.cpp`, `passes.h`, `wasm-module-utils.h`, `support/name.h`, the sibling minify-imports-and-exports lit pair, and current-main spot checks. The capture records the corrected contract: `minify-imports` is non-mutating, reports `modifiesBinaryenIR() == false`, walks imported functions only, emits `old:new` mappings to stdout, and uses `Names::MinifiedNameGenerator`; the mutating import/export family also uses that generator plus used-name avoidance rather than `WasmBinaryBuilder::getSymbolMap(...)` in `version_129`.
- Added `docs/wiki/raw/research/0343-2026-04-25-minify-imports-source-correction.md` plus a new living dossier under `docs/wiki/binaryen/passes/minify-imports/`, including overview, Binaryen strategy, implementation/test-map, WAT-shape, and Starshine status pages.
- Refreshed `docs/wiki/binaryen/passes/minify-imports-and-exports/` to cross-link `minify-imports`, replace stale map-builder wording, keep the module-mutating sibling split explicit, and mark older research note `0342` superseded for the map-construction claim.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md` so the separate non-mutating map-emission pass and the corrected mutating family source strategy are discoverable.

## [2026-04-25] health | link minify dossier from pass-addition caveat

- Ran a focused reference-hygiene check after adding the `minify-imports-and-exports` dossier and found the pass-index tail note still mentioned only `strip-target-features` as source-confirmed in `version_129` without a clean changelog-addition line.
- Updated `docs/wiki/binaryen/passes/index.md` to link the new `minify-imports-and-exports` dossier from that caveat too, keeping the pass-addition chronology honest about source-confirmed-but-not-changelog-confirmed upstream names.

## [2026-04-25] research | add `minify-imports-and-exports` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `minify-imports-and-exports` because the main pass queues were dossier-covered but this public Binaryen module pass still had no Starshine wiki home even though it mutates ABI-visible import/export strings.
- Added `docs/wiki/raw/binaryen/2026-04-25-minify-imports-and-exports-primary-sources.md`, capturing the official Binaryen `version_129` release, `MinifyImportsAndExports.cpp`, `pass.cpp`, `passes.h`, `wasm-binary.h`, the sibling `minify-imports-and-exports-and-modules` WAT/expected-output pair, and current-main spot checks. The capture records the source-backed contract: `WasmBinaryBuilder::getSymbolMap(...)` computes maps, the plain pass rewrites import base names plus export names, the sibling also rewrites import module names, entity kinds and targets remain stable, and the official lit proof directly covers the sibling while the plain pass is source-confirmed through the constructor flag and registry help text.
- Added `docs/wiki/raw/research/0342-2026-04-25-minify-imports-and-exports-source-dossier.md` plus a new living dossier under `docs/wiki/binaryen/passes/minify-imports-and-exports/`, including overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no active, boundary-only, or removed minify pass entry; `src/passes/pass_manager.mbt` has no module dispatcher case; `src/lib/types.mbt` models import/export names directly; `src/binary/decode.mbt`, `src/binary/encode.mbt`, and `src/wast/lower_to_lib.mbt` provide prerequisite name-preserving surfaces; and future Starshine work must treat this as a module/ABI pass rather than a HOT peephole or harmless debug-name minifier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md` so the new upstream-only dossier is discoverable and future wiki runs do not rediscover external-name minification as an undocumented pass-family gap.

## [2026-04-25] research | recheck `de-nan` current-main contract

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose local removed `de-nan` / upstream `denan` because the dossier was complete but still worth a current-main source bridge around an effect-adding instrumentation pass whose helper-call strategy is easy to mis-teach as ordinary floating optimization.
- Added `docs/wiki/raw/binaryen/2026-04-25-de-nan-current-main-recheck.md`, capturing a focused primary-source recheck of Binaryen `version_129` and current `main` `DeNaN.cpp`, `pass.cpp`, `properties.h`, `names.h`, `wasm-builder.h`, `pass.h`, and `denan.wast`. The recheck found no teaching-relevant current-main drift: upstream still exposes `denan`, the implementation remains helper-call NaN-to-zero instrumentation with constant repair, entry-param sanitization, `local.get` / fallthrough skip rules, collision-safe helper names, post-walk helper insertion, and the scalar-lane SIMD helper strategy.
- Added `docs/wiki/raw/research/0341-2026-04-25-de-nan-current-main-recheck.md` and refreshed the living `de-nan` overview, Binaryen strategy, implementation/test-map, helper-boundary guide, WAT-shape catalog, and Starshine strategy pages. The Starshine page now states explicitly that the new source bridge does not change local status: `de-nan` remains a removed registry name, active requests are rejected, there is no owner file or backlog slice, and a faithful future port is still module-owned instrumentation rather than a HOT-only peephole.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md` so the fresh source bridge and unchanged local status are discoverable.

## [2026-04-25] research | recheck `memory64-lowering` out-of-range behavior

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `memory64-lowering` because the dossier was complete enough to be useful but still carried an explicit stale uncertainty around out-of-range constants, active offsets, grow deltas, and limit behavior.
- Added `docs/wiki/raw/binaryen/2026-04-25-memory64-lowering-current-main-recheck.md`, capturing a focused primary-source recheck of Binaryen `version_129` and current `main` `Memory64Lowering.cpp`, `memory64-lowering.wast`, and `table64-lowering.wast`. This entry's original high-constant wording was later narrowed by the `static-offset semantics` entry above: prefer that newer correction for the dynamic-operand versus static-`offset=` split. The still-valid findings are that dynamic address operands wrap with `i32.wrap_i64`, grow results need failure-sentinel repair, max limits clamp to the 32-bit maximum, and impossible min-limit behavior remains an internal-assertion caveat rather than a user-facing diagnostic contract.
- Added `docs/wiki/raw/research/0340-2026-04-25-memory64-lowering-out-of-range-recheck.md` and refreshed the living `memory64-lowering` overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine strategy pages. The Starshine page now points to exact registry, limit-model, memory/table typecheck, and validation code locations in `src/passes/optimize.mbt`, `src/lib/types.mbt`, `src/validate/typecheck.mbt`, and `src/validate/validate.mbt`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`, and marked older note `0315` superseded for out-of-range/grow/max/min policy while preserving its original source-bridge findings. Later note `0374` partially supersedes this entry's high-constant wording.

## [2026-04-25] research | recheck `duplicate-import-elimination` source and code map

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then continued with `duplicate-import-elimination` because the working tree already carried related local wiki updates and the dossier could be made healthier without inventing a duplicate pass page.
- Added `docs/wiki/raw/binaryen/2026-04-25-duplicate-import-elimination-current-main-recheck.md`, capturing a fresh primary-source recheck of Binaryen `main` for `DuplicateImportElimination.cpp`, `pass.cpp`, `opt-utils.h`, `import-utils.h`, and the dedicated WAT / expected-output pair. The recheck found no teaching-relevant drift from the reviewed `version_129` contract: imported functions only, `(module, base)` buckets, exact function-type equality, first-import-wins canonicalization, function-name replacement, duplicate imported-function removal, and continued non-function-import TODO.
- Refreshed the living `duplicate-import-elimination` pages to cite the new raw manifest, update `last_reviewed`, replace stale future-port wording with active-maintenance wording, and point the Starshine strategy to exact remap, rewrite, registry, dispatcher, focused-test, registry-test, and native-CMD code locations.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the current-main no-drift source bridge and exact local code map are discoverable. Health check fixed stale wording that still described an already-landed Starshine implementation as a future port and marked older note `0269` superseded for current local status while preserving its original provenance findings.

## [2026-04-25] implementation | sign off `duplicate-import-elimination`

- Read the existing local `duplicate-import-elimination` dossier and used the source-confirmed Binaryen `version_129` correction: implement only duplicate imported functions, keyed by `(module, base)`, with first-import-wins canonicalization and exact resolved function-type equality.
- Landed `src/passes/duplicate_import_elimination.mbt`, registry/dispatcher/CLI/compare-harness wiring, focused pass regressions, registry coverage, and native `run_cmd_with_adapter` coverage.
- Recorded direct evidence: `moon test src/passes`, `moon test src/cmd`, `.tmp/pass-fuzz-die-genvalid-10000` (`10000/10000`, `0` mismatches), `.tmp/pass-fuzz-die-10000-keep-going` (`9975/9975`, `0` mismatches, `25` Binaryen/parser command failures), and `/tmp/starshine-self-optimize-compare-starshine-debug-wasi-1962434` (canonical wasm and normalized WAT equal on the debug artifact).
- Refreshed `agent-todo.md`, `CHANGELOG.md`, and the living `duplicate-import-elimination` pages to mark the direct pass active while keeping full late-tail preset replay and future non-function-import drift as follow-up.

## [2026-04-25] research | add `remove-unused` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/remove-unused/` folder, then chose local boundary-only `remove-unused` because the dossier already had useful lineage, Binaryen strategy, implementation-map, and module-shape pages but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-remove-unused-primary-sources.md`, capturing historical upstream `RemoveUnusedFunctions.cpp`, historical registration/factory files, the `98e9e604...` supersession commit and early `RemoveUnusedModuleElements.cpp`, current `version_129` and current-`main` pass registry surfaces, current help-test oracles, and the Binaryen README context. The capture records the source-backed contract: old function-only start/export/table-rooted direct-call reachability, deletion of unreachable functions, supersession by broader RUME, modern absence of `remove-unused` / `remove-unused-functions`, and the caveat that the local alias relation remains an evidence-backed inference rather than a directly documented local rename.
- Added `docs/wiki/raw/research/0339-2026-04-25-remove-unused-source-bridge.md` plus `docs/wiki/binaryen/passes/remove-unused/starshine-strategy.md`, grounding local status in exact repository surfaces: `src/passes/optimize.mbt` keeps `remove-unused` boundary-only and rejects active requests during expansion, `src/passes/pass_manager.mbt` dispatches active `remove-unused-module-elements` but has no `remove-unused` case, `src/passes/remove_unused_module_elements.mbt` is the modern implementation to read instead, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md` still groups the alias in Batch 4, and `agent-todo.md` has no dedicated implementation slice.
- Refreshed the living `remove-unused` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0195` superseded for raw-source provenance and Starshine status while preserving it as the original folder-creation and lineage-research record.

## [2026-04-25] research | add `souperify` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `souperify` / `souperify-single-use` because the folder already had useful 2026-04-21 strategy, implementation-map, mechanics, and WAT-shape pages but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-souperify-primary-sources.md`, capturing the official Binaryen `version_129` release, `Souperify.cpp`, `pass.cpp`, `passes.h`, DataFlow helper files, flatness and local-graph helpers, the two official flatten-plus-cleanup Souper lit files, and current-main spot-check URLs. The capture records the source-backed contract: public trace-emission pass family, hard `Flat::verifyFlatness` precondition, DataFlow graph and `LocalGraph` extraction, bounded `Trace` growth with `Var` fallback, `if`-derived `pc` / `blockpc`, loop-carried `Var` cutoff, no standalone `souperify.wast`, and single-use child truncation rather than root filtering.
- Added `docs/wiki/raw/research/0338-2026-04-25-souperify-source-bridge.md` plus `docs/wiki/binaryen/passes/souperify/starshine-strategy.md`, grounding local status in exact repository surfaces: `src/passes/optimize.mbt` has no active/boundary-only/removed `souperify` entries, `src/passes/pass_manager.mbt` has no trace-emission dispatcher lane, `src/cmd/cmd.mbt` only reports unknown pass flags, no `src/passes/*souper*` owner exists, `src/ir/use_def.mbt`, `src/ir/ssa_local.mbt`, `src/ir/ssa_policy.mbt`, `src/ir/hot_lift.mbt`, `src/lib/types.mbt`, and `src/wast/` are prerequisites rather than an implementation, and `agent-todo.md` has no slice.
- Refreshed the living `souperify` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0219` superseded for raw-source provenance and Starshine status while preserving it as the original dossier-building research.

## [2026-04-25] research | add `dae2` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `dae2` because the folder already had useful 2026-04-21 strategy, implementation-map, mechanics, and WAT-shape pages but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-dae2-primary-sources.md`, capturing the official Binaryen `version_129` release, `DeadArgumentElimination2.cpp`, `pass.cpp`, `passes.h`, helper context, the dedicated `dae2.wast` lit file, and current-main spot-check URLs. The capture records the source-backed contract: public experimental `dae2`, separate owner from plain DAE, smallest-fixed-point backward used-parameter analysis, direct and root-function-type-tree forwarding, `LazyLocalGraph` incoming-value safety, conservative public/reference/intrinsic/control/effect blockers, `closedWorld && GC` referenced-function rewrite mode, replacement types, and current non-goals around result/constant/type propagation.
- Added `docs/wiki/raw/research/0337-2026-04-25-dae2-source-bridge.md` plus `docs/wiki/binaryen/passes/dae2/starshine-strategy.md`, grounding local status in exact repository surfaces: `src/passes/optimize.mbt` has no active/boundary-only/removed `dae2` registry entry, `src/passes/pass_manager.mbt` has no module dispatcher case, `src/cmd/cmd.mbt` has only prerequisite unknown-pass and `--closed-world` option plumbing, `src/lib/types.mbt` / `src/validate/*` / `src/wast/` expose prerequisite function-type, call, `call_ref`, `call_indirect`, and `ref.func` surfaces, no `src/passes/*argument*` owner exists, and `agent-todo.md` has no `dae2` slice.
- Refreshed the living `dae2` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0218` superseded for raw-source provenance and Starshine status while preserving it as the original dossier-building research.

## [2026-04-25] research | add `reorder-globals-always` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/reorder-globals-always/` folder, then chose `reorder-globals-always` because the dossier had accurate overview / Binaryen strategy / implementation-test map / proof / WAT-shape coverage but still lacked a sibling-specific immutable raw source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-reorder-globals-always-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `GlobalStructInference.cpp`, helper traversal/toposort surfaces, the dedicated `reorder-globals.wast` / `reorder-globals-real.wast` proof pair, and current-main spot-check URLs. The capture records the source-backed contract: separate public/test pass identity, one shared `ReorderGlobals` engine, removed `< 128` cutoff in always mode, exact smooth `1.0 + (i / 128.0)` scoring, unchanged imports/dependency/tie legality, nested GSI helper-global repair role, and narrow current-main no-teaching-drift result.
- Added `docs/wiki/raw/research/0336-2026-04-25-reorder-globals-always-source-bridge.md` plus `docs/wiki/binaryen/passes/reorder-globals-always/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` keeps the name boundary-only, active requests are rejected, `optimize` / `shrink` presets omit it, no `src/passes/reorder_globals_always.mbt` or shared `reorder_globals.mbt` owner exists, `agent-todo.md` has only the neighboring production `RG` backlog, and `src/lib/types.mbt` exposes numeric `GlobalIdx` users a future module pass must repair.
- Refreshed the living `reorder-globals-always` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older notes `0188` and `0214` superseded for raw-source provenance and Starshine status while preserving them as the original folder-creation and proof research.

## [2026-04-25] research | add `constant-field-null-test-folding` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose local boundary-only `constant-field-null-test-folding` / upstream `cfp-reftest` because the dossier already had useful strategy, matcher, implementation-map, and shape coverage but still lacked a sibling-specific immutable raw source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-constant-field-null-test-folding-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `ConstantFieldPropagation.cpp`, `pass.cpp`, helper files, the dedicated `cfp-reftest.wast` lit file, the parent `cfp` / `gto_and_cfp_in_O` tests, and current-`main` spot-check URLs. The capture records the source-backed contract: public upstream `cfp-reftest`, shared `ConstantFieldPropagation(true)` engine, closed-world + GC gates, ordinary CFP first, exact two-value rescue path, subtype classifier proof, nullable-base `ref.as_non_null` repair, final `select(ref.test(...))` output, and a narrow current-`main` no-teaching-drift result with mechanical subtype-iteration cleanup.
- Added `docs/wiki/raw/research/0335-2026-04-25-constant-field-null-test-folding-source-bridge.md` plus `docs/wiki/binaryen/passes/constant-field-null-test-folding/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` keeps the local alias boundary-only, active requests are rejected, active presets omit it, `src/passes/registry_test.mbt` guards preset categories, no `src/passes/*constant*` / `*field*` owner file exists, `agent-todo.md` has no `cfp-reftest` slice, and local GC/type/parser/validator/binary files are prerequisites rather than an implementation.
- Refreshed the touched living `constant-field-null-test-folding` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; softened stale wording that treated nonnullable-`ref.test` support as a source-confirmed fixed feature gate and reframed it as a validation-sensitive emitted-form boundary future Starshine ports must re-check.

## [2026-04-25] research | add `strip-target-features` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `strip-target-features` because the late-pass chronology already named it but the wiki still had no dedicated dossier, raw primary-source manifest, output-shape page, or Starshine status bridge.
- Added `docs/wiki/raw/binaryen/2026-04-25-strip-target-features-primary-sources.md`, capturing the official Binaryen `version_129` release, `StripTargetFeatures.cpp`, `pass.cpp`, `passes.h`, and the WebAssembly core custom-section context. The capture records the source-backed contract: public `strip-target-features`, `modifiesBinaryenIR() == false`, no in-memory module rewrite, one `runner->options.emitTargetFeatures = false` side effect, target-feature custom-section omission at output time, and an explicit caveat that the reviewed changelog pages did not provide a clean addition-provenance line.
- Added `docs/wiki/raw/research/0334-2026-04-25-strip-target-features-primary-sources-and-starshine-followup.md` plus a new living dossier under `docs/wiki/binaryen/passes/strip-target-features/`, including overview, Binaryen strategy, implementation/validation surface, output-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no active, boundary-only, or removed `strip-target-features` entry; `src/lib/types.mbt` models opaque `Module.custom_secs` / `CustomSec`; `src/binary/decode.mbt` preserves non-`name` custom sections opaquely; `src/binary/encode.mbt` writes stored custom sections without a Binaryen-like `emitTargetFeatures` option; and future Starshine work must choose between an output-option design and concrete `target_features` section deletion.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, the neighboring `strip-toolchain-annotations` living pages, and `CHANGELOG.md` so `strip-target-features` is no longer a catalog-only mention and is explicitly separated from `strip-toolchain-annotations`, `remove-relaxed-simd`, generic custom-section stripping, and feature lowering.

## [2026-04-25] research | add `simplify-locals-notee-nostructure` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream `simplify-locals-notee-nostructure` / local removed alias `simplify-locals-no-tee-no-structure` because the folder already had useful 0129-era overview, Binaryen strategy, variant-surface, and WAT-shape coverage but still lacked an immutable raw primary-source manifest, dedicated implementation/test-map page, and dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-simplify-locals-notee-nostructure-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, locals-family helper files, the dedicated `simplify-locals-notee-nostructure` WAT/expected-output pair, neighboring variant tests, and current-`main` spot-check URLs. The capture records the source-backed contract: one shared locals-family engine, public `simplify-locals-notee-nostructure`, exact `SimplifyLocals<false, false, true>` policy, no fresh tees, no structure synthesis, ordinary nesting still enabled, direct single-use sinking, late equivalent/dead cleanup, refinalization, and the aggressive `flatten -> sibling -> local-cse` slot.
- Added `docs/wiki/raw/research/0333-2026-04-25-simplify-locals-notee-nostructure-primary-sources-and-starshine-followup.md`, `docs/wiki/binaryen/passes/simplify-locals-notee-nostructure/implementation-structure-and-tests.md`, and `docs/wiki/binaryen/passes/simplify-locals-notee-nostructure/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` tracks only the local alias as removed, the exact upstream spelling is absent, presets omit the sibling, `src/cmd/cmd.mbt` accepts only active categories, `src/passes/pass_manager.mbt` dispatches only active full `simplify-locals`, `src/passes/simplify_locals.mbt` is a broader future landing zone without policy flags, and `agent-todo.md` has no dedicated sibling slice.
- Refreshed the living `simplify-locals-notee-nostructure` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0129` superseded for raw-source provenance and local status while preserving it as the original mechanics and saved-audit rationale.

## [2026-04-25] research | add `merge-similar-functions` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose local boundary-only `merge-similar-functions` because the dossier had useful overview, Binaryen strategy, implementation/test map, mechanics, profitability/type-barrier, and WAT-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-merge-similar-functions-primary-sources.md`, capturing the official Binaryen `version_129` release, `MergeSimilarFunctions.cpp`, `pass.cpp`, `pass.h`, helper files, the dedicated lit roster, and current-`main` spot-check URLs. The capture records the source-backed contract: late shrink-only module pass, hash-prefiltered equivalence classes, narrow literal/direct-callee differences, reusable diff vectors, helper cloning with non-param local-index shifting, original-name-preserving thunks, call-ref indirection gates, profitability, and the `255` synthetic-param boundary.
- Added `docs/wiki/raw/research/0332-2026-04-25-merge-similar-functions-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/merge-similar-functions/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` keeps the name boundary-only and rejects active requests, local `optimize` / `shrink` presets omit it, `src/cmd/cmd.mbt` has no implementation hook, `src/cli/cli.mbt` only collects pass-like flags, `src/passes/pass_manager.mbt` has no owner/dispatcher case, `src/lib/types.mbt` provides only prerequisite representation surfaces, and `agent-todo.md` has no active slice.
- Refreshed the living `merge-similar-functions` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older notes `0174` and `0201` superseded for raw-source provenance and Starshine status while preserving them as the original source-reading and mechanics research.

## [2026-04-25] research | add `simplify-locals-nonesting` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream `simplify-locals-nonesting` / local removed alias `simplify-locals-no-nesting` because the folder already had useful 0186-era overview, Binaryen strategy, implementation/test-map, flatness-boundary, and WAT-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-simplify-locals-nonesting-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, helper headers, the dedicated `simplify-locals-nonesting` WAT/expected-output pair, flatten-neighbor `dfo` / `souperify` combo lit files, neighboring variant tests, and current-`main` spot-check URLs. The capture records the source-backed contract: one shared locals-family engine, public `simplify-locals-nonesting`, `SimplifyLocals<false, false, false>`, no fresh tees, no structure synthesis, no ordinary new nesting, preserved flat copy-retargeting, late equivalent/dead cleanup, and the real flatness-preserving pipeline role.
- Added `docs/wiki/raw/research/0331-2026-04-25-simplify-locals-nonesting-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/simplify-locals-nonesting/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` tracks only local `simplify-locals-no-nesting` as removed, the upstream spelling is absent, `src/cmd/cmd.mbt` accepts only active hot/module/preset flags and hides removed names from help, `src/passes/pass_manager.mbt` dispatches only active full `simplify-locals`, and `src/passes/simplify_locals.mbt` is reusable future landing-zone code rather than a sibling-mode implementation today.
- Refreshed the living `simplify-locals-nonesting` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0186` superseded for raw-source provenance and local status while preserving it as the original mechanics and folder-creation rationale.

## [2026-04-25] research | add `optimize-added-constants-propagate` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `optimize-added-constants-propagate` because the folder already had overview / Binaryen strategy / implementation-test map / WAT-shape coverage but still lacked a sibling-specific immutable raw manifest and dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-25-optimize-added-constants-propagate-primary-sources.md`, capturing official Binaryen `version_129` and current-main URLs for `OptimizeAddedConstants.cpp`, `pass.cpp`, `pass.h`, `local-graph.h`, helper files, and the dedicated propagate WAT/expected-output pair. The capture records the source-backed contract: shared `MemoryAccessOptimizer`, public `optimize-added-constants-propagate`, `propagate = true`, `LazyLocalGraph` local-pair analysis, helper-local salvage, dead address-set cleanup, repeated iteration, and the same hard low-memory boundary as the plain sibling.
- Added `docs/wiki/raw/research/0330-2026-04-25-optimize-added-constants-propagate-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/optimize-added-constants-propagate/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` keeps the name removed and rejects active requests, presets omit it, no owner file / dispatcher case / backlog slice exists, and `src/cli/cli.mbt`, `src/cmd/cmd.mbt`, `src/lib/types.mbt`, `src/ir/hot_*`, `src/passes/remove_unused_names.mbt`, binary, WAT, and validation files are prerequisites rather than an implementation.
- Refreshed the living `optimize-added-constants-propagate` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older note `0165` superseded for raw-source provenance and local status while preserving it as the original teaching correction.

## [2026-04-24] research | add `simplify-locals-notee` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream `simplify-locals-notee` / local removed alias `simplify-locals-no-tee` because the folder had a useful 0166-era overview, Binaryen strategy, variant-boundary page, and WAT-shape catalog but still lacked an immutable raw primary-source manifest, a dedicated implementation/test-map page, and a Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-simplify-locals-notee-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, locals-family helper files, the dedicated `simplify-locals-notee` WAT/expected-output pair, neighboring variant tests, and current-`main` spot-check URLs. The capture records the source-backed contract: one shared locals-family engine, `createSimplifyLocalsNoTeePass()`, `SimplifyLocals<false, true>` / explicit `allowTee = false`, `allowStructure = true`, default `allowNesting = true`, preserved structure formation, preserved equivalent-copy and dead-set cleanup, and disabled fresh-tee multi-use sinking.
- Added `docs/wiki/raw/research/0329-2026-04-24-simplify-locals-notee-primary-sources-and-starshine-followup.md`, `docs/wiki/binaryen/passes/simplify-locals-notee/implementation-structure-and-tests.md`, and `docs/wiki/binaryen/passes/simplify-locals-notee/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` tracks only local `simplify-locals-no-tee` as removed, `src/cmd/cmd.mbt` accepts only active hot/module/preset pass flags, `src/passes/pass_manager.mbt` dispatches only active full `simplify-locals`, and `src/passes/simplify_locals.mbt` is the reusable HOT landing zone but has no no-fresh-tee sibling mode yet.
- Refreshed the living `simplify-locals-notee` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; recorded that the former direct-URL provenance / local-follow-along gap is now closed while keeping older `0166` as historical source-confirmation research.

## [2026-04-24] research | add `remove-unused-non-function-elements` source bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose local boundary-only `remove-unused-non-function-elements` / upstream `remove-unused-nonfunction-module-elements` because the folder had good 0194-era mechanics coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-remove-unused-non-function-elements-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `RemoveUnusedModuleElements.cpp`, `pass.cpp`, `passes.h`, `module-utils.h`, the dedicated all-features sibling test, inherited RUME lit surfaces, and current-`main` spot-check URLs. The capture records the source-backed contract: one shared RUME engine, a `rootAllFunctions = true` sibling constructor, defined-function rooting, imported-function removability, ordinary startup/export/imported-parent/trap roots, index repair, and function-type cleanup.
- Added `docs/wiki/raw/research/0328-2026-04-24-remove-unused-non-function-elements-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/remove-unused-non-function-elements/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` keeps the name boundary-only, `src/cmd/cmd.mbt` accepts only hot/module/preset categories and hides boundary-only names from help, `src/passes/pass_manager.mbt` dispatches full RUME but not this sibling, `src/passes/remove_unused_module_elements.mbt` owns reusable full-RUME liveness/rewrite machinery without a defined-function-rooting mode, and `src/passes/remove_unused_module_elements_test.mbt` has reusable full-RUME fixtures but no sibling-specific expectations yet.
- Refreshed the living `remove-unused-non-function-elements` pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; recorded that the former provenance / local-follow-along gap is now closed while keeping the older `0194` note as historical source-confirmation research.

## [2026-04-24] research | add `string-lifting` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream-only `string-lifting` because the string-family pages already mentioned it as Binaryen's reverse-side sibling of `string-lowering` but the wiki still had no canonical dossier, raw primary-source manifest, transformed-shape catalog, or Starshine status bridge.
- Added `docs/wiki/raw/binaryen/2026-04-24-string-lifting-primary-sources.md`, capturing the official Binaryen `version_129` release, `StringLifting.cpp`, `pass.cpp`, `passes.h`, `string-utils.h`, the dedicated `string-lifting.wast` lit file, and current-main spot-check URLs. The capture records the source-backed contract: magic and JSON `string.const` imported-global discovery, exact `wasm:js-string` helper import signature checks, `global.get` and helper-call lifting into wasm string instructions, per-function refinalization, module-code walking, Strings feature enablement, and the open upstream cast-repair TODO.
- Added `docs/wiki/raw/research/0327-2026-04-24-string-lifting-primary-sources-and-starshine-followup.md` plus a new living dossier under `docs/wiki/binaryen/passes/string-lifting/`, including overview, Binaryen strategy, implementation/test-map, import/call-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` does not track `string-lifting` as active, boundary-only, or removed; there is no `src/passes/string_lifting.mbt` owner file or backlog slice; and `src/lib/types.mbt`, `src/wast/`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/ir/hot_lift.mbt`, and `src/ir/hot_lower.mbt` provide only prerequisite string literal / partial string-op infrastructure.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, and the existing `string-gathering` / `string-lowering` pages so the bidirectional `string-lifting` / `string-lowering` split is now linked rather than a dangling sibling mention.

## [2026-04-24] research | add `global-struct-inference-desc-cast` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/` folder, then chose local `global-struct-inference-desc-cast` / upstream `gsi-desc-cast` because the dossier already had corrected Binaryen strategy, implementation/test-map, descriptor-singleton, and WAT-shape pages but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy bridge.
- Added `docs/wiki/raw/binaryen/2026-04-24-global-struct-inference-desc-cast-primary-sources.md`, capturing the official Binaryen `version_129` release, shared `GlobalStructInference.cpp`, `pass.cpp`, dedicated `gsi-to-desc-cast.wast`, neighboring `gsi-desc.wast` / `gsi.wast`, and a current-`main` spot check. The capture records the source-backed contract: one shared GSI engine, public `gsi-desc-cast` sibling, `optimizeToDescCasts` mode flag, hard GC gate, subtype legality check, effective closed-world `typeGlobals` dependency, exactly-one target descriptor global rule, direct `ref.cast_desc_eq` rewrite, and inherited descriptor un-nesting / `reorder-globals-always` repair.
- Added `docs/wiki/raw/research/0326-2026-04-24-global-struct-inference-desc-cast-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/starshine-strategy.md`, grounding the local status in exact repository surfaces: `src/passes/optimize.mbt` tracks the name as boundary-only and rejects active requests, `src/cmd/fuzz_harness_wbtest.mbt` only proves request failure plumbing, `src/passes/pass_manager.mbt` dispatches only active plain `global-struct-inference`, `src/passes/global_struct_inference.mbt` implements only the smaller direct-global field-fold subset, and `src/lib/types.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, and `src/binary/decode.mbt` provide prerequisite `ref.cast_desc_eq` infrastructure rather than a pass implementation.
- Refreshed the `global-struct-inference-desc-cast` living pages plus `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`; marked older notes `0170` and `0212` as superseded for raw-source provenance and local status while preserving `0212` as the source-correction note for the target-descriptor-singleton and dedicated-test fixes.

## [2026-04-24] research | add `reorder-functions-by-name` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/reorder-functions-by-name/` folder, then chose `reorder-functions-by-name` because the dossier already had accurate overview / Binaryen strategy / implementation map / lexical proof / module-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-reorder-functions-by-name-primary-sources.md`, capturing the official Binaryen `version_129` release, `ReorderFunctions.cpp`, `pass.cpp`, the dedicated `reorder-functions-by-name.wast` lit file, and a current-main spot check. The capture records the source-backed contract: separate public debugging-oriented pass identity, ascending internal-name ordering, declaration-only `requiresNonNullableLocalFixups() == false`, four lit-backed `$a/$b/$c` permutation proofs, and no teaching-relevant current-main drift on the reviewed surface.
- Added `docs/wiki/raw/research/0325-2026-04-24-reorder-functions-by-name-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/reorder-functions-by-name/starshine-strategy.md`, grounding local status in exact repository surfaces: `src/passes/optimize.mbt` tracks the name as boundary-only, rejects active requests, and omits it from presets; `src/lib/types.mbt` models the separate function/index-bearing module surfaces; `src/passes/remove_unused_module_elements.mbt` already has the closest function-index, name, and annotation remap examples; `src/binary/encode.mbt` encodes the module as provided; and `src/wast/lower_to_lib.mbt` resolves textual function names to numeric `FuncIdx` values before any future reorder pass would run.
- Refreshed `docs/wiki/binaryen/passes/reorder-functions-by-name/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `lexical-order-proof-and-boundaries.md`, and `module-shapes.md`, marked the older `0180` / `0213` research notes superseded for raw-source provenance and local status, then updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md`, so readers can follow the complete chain from transformed module-order shapes to Binaryen source strategy to exact Starshine non-implementation and future function-index remap requirements.

## [2026-04-24] research | add `strip-toolchain-annotations` source dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `strip-toolchain-annotations` because the late-pass chronology already named it as a newer upstream-only Binaryen pass but the wiki had no dedicated dossier, immutable primary-source manifest, transformed-shape catalog, or Starshine status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-strip-toolchain-annotations-primary-sources.md`, capturing the official Binaryen `version_129` release, `CHANGELOG.md`, `StripToolchainAnnotations.cpp`, `pass.cpp`, `passes.h`, the dedicated `strip-toolchain-annotations.wast` lit file, and current-`main` spot-check URLs. The capture records the source-backed contract: public `strip-toolchain-annotations`, function-parallel annotation cleanup, clearing `removableIfUnused`, `jsCalled`, and `idempotent`, empty-entry cleanup, and preservation of non-toolchain metadata such as `metadata.code.inline`.
- Added `docs/wiki/raw/research/0324-2026-04-24-strip-toolchain-annotations-primary-sources-and-starshine-followup.md` plus a new living dossier under `docs/wiki/binaryen/passes/strip-toolchain-annotations/`, including overview, Binaryen strategy, implementation/test-map, annotation-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no registry entry for `strip-toolchain-annotations`; `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, and `src/wast/module_wast_tests.mbt` already model a function-annotation subset; `src/passes/duplicate_function_elimination.mbt` remaps and hashes annotations; and the local binary custom-section roundtrip for this surface remains an explicit uncertainty.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, and `CHANGELOG.md` so the formerly only-mentioned upstream pass now has a stable home, while keeping `strip-target-features` separate as a still-only-catalog-level mention.

## [2026-04-24] health | mark `asyncify` tracker gap closed

- Ran a focused reference-hygiene check after adding the `asyncify` dossier and found the tracker's long “gap closed” maintenance section listed `asyncify` in the covered summary but lacked the matching explicit sentence used for neighboring recent dossier additions.
- Updated `docs/wiki/binaryen/passes/tracker.md` to state directly that `asyncify` now has an immutable raw primary-source manifest plus a dedicated Starshine status page, so future wiki runs do not rediscover it as an absent pass-wiki gap.

## [2026-04-24] research | add `asyncify` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose upstream `asyncify` because the widened pass wiki still had no dossier for Binaryen's public whole-module pause/resume transformation despite official Emscripten documentation and substantial Starshine prerequisite surfaces.
- Added `docs/wiki/raw/binaryen/2026-04-24-asyncify-primary-sources.md`, capturing the official Binaryen `version_129` release, `Asyncify.cpp`, `pass.cpp`, `passes.h`, `asyncify.wast`, current-`main` spot-check URLs, and Emscripten's official Asyncify docs. The capture records the source-backed contract: public `asyncify`, callgraph/import/indirect-call analysis, state/data globals, runtime helper exports including `asyncify_get_state`, staged flow and local instrumentation, Asyncify memory selection/creation, memory32-vs-memory64 pointer width, and the reviewed tail-call unsupported boundary.
- Added `docs/wiki/raw/research/0323-2026-04-24-asyncify-primary-sources-and-starshine-followup.md` plus a new living dossier under `docs/wiki/binaryen/passes/asyncify/`, including overview, Binaryen strategy, implementation/test-map, transformed-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no `asyncify` registry entry; `src/lib/types.mbt`, `src/wast/`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `src/validate/typecheck.mbt` already cover many prerequisite module, call, global, memory, and validation primitives, but Starshine has no Asyncify analysis, instrumentation, runtime API synthesis, or host integration harness today.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `CHANGELOG.md` so the formerly absent upstream-only pass now has a stable home and future runs do not treat it as an open dossier gap.

## [2026-04-24] health | link `precompute` relaxed-SIMD boundary to removal dossier

- Ran a focused touched-area reference check after adding the `remove-relaxed-simd` dossier and found the existing `precompute` relaxed-SIMD negative still described the nondeterminism boundary without linking to the now-canonical removal-pass page.
- Updated `docs/wiki/binaryen/passes/precompute/wat-shapes.md` and `docs/wiki/binaryen/passes/precompute/implementation-structure-and-tests.md` to cross-link `remove-relaxed-simd`, making the distinction explicit: `precompute` preserves relaxed SIMD to avoid erasing nondeterminism, while `remove-relaxed-simd` removes the feature by trapping at each relaxed operation.

## [2026-04-24] research | add `remove-relaxed-simd` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `remove-relaxed-simd` because the late-pass catalog already named it as a newer upstream-only Binaryen pass but the wiki had no dedicated dossier, immutable source manifest, transformed-shape page, or Starshine status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-remove-relaxed-simd-primary-sources.md`, capturing the official Binaryen `version_129` release/changelog surfaces, `RemoveRelaxedSIMD.cpp`, `pass.cpp`, `passes.h`, `child-localizer.h`, `remove-relaxed-simd.wast`, current-`main` spot-check URLs, and the official WebAssembly relaxed SIMD proposal overview. The capture records the source-backed contract: public `remove-relaxed-simd`, `version_126` changelog provenance, relaxed-SIMD-to-`unreachable` replacement, `ChildLocalizer` child-effect preservation, feature-gated function walks, refinalization, and ordinary-SIMD preservation.
- Added `docs/wiki/raw/research/0322-2026-04-24-remove-relaxed-simd-primary-sources-and-starshine-followup.md` plus a new living dossier under `docs/wiki/binaryen/passes/remove-relaxed-simd/`, including overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no registry entry for `remove-relaxed-simd`, while `src/wast/types.mbt`, `src/wast/keywords.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/lib/types.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/ir/hot_lift.mbt`, and `src/ir/hot_lower.mbt` already provide reusable relaxed-SIMD parser / binary / validation / HOT surfaces.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` so the formerly only-mentioned upstream pass now has a stable home, and recorded the remaining uncertainty that feature-metadata cleanup must be source-confirmed before any future Starshine port advertises more than expression removal.

## [2026-04-24] research | add `ssa` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/ssa/` folder, then chose full `ssa` because the dossier had overview / Binaryen strategy / implementation map / merge-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-ssa-primary-sources.md`, capturing the official Binaryen `version_129` release page, `SSAify.cpp`, `pass.cpp`, `passes.h`, `LocalGraph` sources, `ReFinalize.cpp`, `ssa.wast`, `local-graph.cpp`, sibling `ssa-nomerge_enable-simd.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: one shared `SSAify(bool allowMerges)` engine, separate public `ssa` and `ssa-nomerge` registrations, LocalGraph-driven fresh locals, full-`ssa` merge-local creation, incoming `local.tee` rewrites, parameter-entry prepends, default-entry no-prepend behavior, and narrow refinalization for reference default replacements.
- Added `docs/wiki/raw/research/0321-2026-04-24-ssa-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/ssa/starshine-strategy.md`, grounding local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/ssa_nomerge.mbt`, `src/passes/pass_common.mbt`, `src/ir/ssa_policy.mbt`, `src/ir/ssa_local.mbt`, `src/ir/ssa_destroy.mbt`, `src/ir/analysis_cache.mbt`, `src/passes/ssa_nomerge_test.mbt`, `src/passes/registry_test.mbt`, and `agent-todo.md`) and making explicit that current Starshine has no public `ssa` registry entry, only the active `ssa-nomerge` sibling plus reusable HOT SSA overlay/destruction infrastructure.
- Refreshed `docs/wiki/binaryen/passes/ssa/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `merge-locals-entry-prepends-and-default-values.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed full-SSA local/merge shapes to Binaryen source strategy to exact Starshine non-implementation and future local landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the older `0207` research note is now explicitly marked superseded for raw-source provenance and local status, and the touched pages now cite the committed raw manifest and new Starshine status page instead of relying only on direct online links and the older research note.

## [2026-04-24] research | correct `propagate-globals-globally` source contract and add Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/propagate-globals-globally/` folder, then chose `propagate-globals-globally` because it was a locally named boundary-only pass with no immutable raw primary-source manifest, no dedicated Starshine strategy page, and stale mechanics left over from earlier source correction.
- Added `docs/wiki/raw/binaryen/2026-04-24-propagate-globals-globally-primary-sources.md`, capturing the official Binaryen `version_129` release page, `SimplifyGlobals.cpp`, `pass.cpp`, helper surfaces, `propagate-globals-globally.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the corrected contract: public `propagate-globals-globally` registration, `PropagateGlobalsGlobally` subclass boundary, `propagateConstantsToGlobals()`-only behavior, declaration-order known-literal collection, global-initializer plus active data/elem offset rewrites, and no function-body propagation.
- Added `docs/wiki/raw/research/0320-2026-04-24-propagate-globals-globally-source-correction-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/propagate-globals-globally/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/lib/types.mbt`, `src/validate/validate.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps the pass boundary-only, rejects active requests, has no module dispatcher case, no owner file, no preset role, and no backlog slice.
- Refreshed `docs/wiki/binaryen/passes/propagate-globals-globally/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `shared-engine-and-startup-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can follow the complete chain from transformed startup-global/offset shapes to Binaryen source strategy to exact Starshine non-implementation and future module-pass landing zone.
- Health check while touching the folder fixed the stale-reference issues in place: older notes `0162` and `0196` are now explicitly marked superseded for the standalone-file, helper-name, reverse-scan, and `optimize=false` boundary claims, and the touched living pages now cite the committed raw manifest instead of relying only on older direct online links.

## [2026-04-24] research | add `inline-main` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/inline-main/` folder, then chose `inline-main` because the dossier had overview / Binaryen strategy / implementation map / boundary / WAT-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-inline-main-primary-sources.md`, capturing the official Binaryen `version_129` release page, `Inlining.cpp`, `pass.cpp`, helper surfaces, the dedicated `inline-main.wast` lit file, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: separate public pass identity, exact `main` / `__original_main` lookup, defined-endpoint requirement, direct-call-only scan, exact-one-call acceptance, shared `doInlining(...)` helper semantics, and no teaching-relevant chooser drift in the narrow current-main check.
- Added `docs/wiki/raw/research/0319-2026-04-24-inline-main-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/inline-main/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has only a boundary-only registry entry, active request rejection, no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/inline-main/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `special-case-contract-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from one-call wrapper WAT shapes to Binaryen source strategy to exact Starshine non-implementation and future module-level inline-helper landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older `0177` research note, and that older note is now explicitly marked superseded for raw-source provenance and local status.

## [2026-04-24] research | add `monomorphize-always` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/monomorphize-always/` folder, then chose `monomorphize-always` because the dossier had overview / Binaryen strategy / implementation map / policy-boundary / transformed-shape coverage but still lacked an immutable sibling-specific raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-monomorphize-always-primary-sources.md`, capturing the official Binaryen `version_129` release page, `Monomorphize.cpp`, `pass.cpp`, the direct `monomorphize-types.wast` sibling lit file, supporting `monomorphize-benefit.wast` / context / drop / limits files, and current-`main` spot-check URLs. The capture records the source-backed contract: one shared `Monomorphize.cpp` engine, `onlyWhenHelpful` policy split, separate public `monomorphize-always` registration, unchanged legality/triviality/limit gates, and the corrected direct-lit-test surface.
- Added `docs/wiki/raw/research/0318-2026-04-24-monomorphize-always-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/monomorphize-always/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/cli/cli.mbt`, `src/cmd/cmd.mbt`, `src/cmd/cmd_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has only a boundary-only registry entry, active request rejection, adjacent `monomorphize_min_benefit` option plumbing, no owner file, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/monomorphize-always/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `usefulness-gate-and-sibling-split.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from weak-benefit specialized WAT shapes to Binaryen source strategy to exact Starshine non-implementation and future shared module-pass landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older `0187` note, they no longer describe `monomorphize-benefit.wast` as direct `--monomorphize-always` evidence, and `docs/wiki/raw/research/0187-2026-04-21-monomorphize-always-binaryen-research.md` is now explicitly marked superseded for provenance/direct-lit attribution.

## [2026-04-24] pass-implementation | activate initial `code-pushing` hot pass

- Identified `code-pushing` as the next unimplemented pass in the canonical no-DWARF order after the active `precompute` surface.
- Added `src/passes/code_pushing.mbt` and focused tests for the initial conservative HOT rewrite: a const-like `local.set` immediately before a void `if` is replaced by a `nop` while a cloned `local.set` moves into the single consuming `if` arm, while later reads and trapping/non-const computations stay unchanged.
- Moved `code-pushing` from removed-name tracking to active hot-pass registry/dispatcher/CLI surfaces, updated `registry_test`, `cmd_wbtest`, and the public `.mbti` snapshot, and kept public presets unchanged until broader parity proof and `simplify-locals-nostructure` are available.
- Refreshed `agent-todo.md`, `CHANGELOG.md`, `docs/wiki/binaryen/passes/code-pushing/`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the living docs now distinguish the landed subset from the remaining Binaryen segment-sinking/profitability/artifact-parity work.

## [2026-04-24] research | correct `dealign` source contract and add Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/dealign/` folder, then chose `dealign` because the dossier had overview / Binaryen strategy / implementation map / shape coverage but still lacked an immutable raw primary-source manifest and dedicated Starshine status page, and fresh source review exposed stale mechanics.
- Added `docs/wiki/raw/binaryen/2026-04-24-dealign-primary-sources.md`, capturing the official Binaryen `version_129` release page, `DeAlign.cpp`, `pass.cpp`, `dealign.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the corrected contract: public `dealign`, function-parallel `WalkerPass<PostWalker<DeAlign>>`, direct `Load` / `Store` / `SIMDLoad` visitors, unconditional `align = 1` assignment, no reviewed `SIMDStore`, no explicit no-memory bailout, and a narrow scalar-`i32` lit proof surface.
- Added `docs/wiki/raw/research/0317-2026-04-24-dealign-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/dealign/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the no-DWARF path page) and making explicit that current Starshine does not track `dealign` in the registry at all, so explicit requests fall through to the unknown-pass error.
- Refreshed `docs/wiki/binaryen/passes/dealign/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `align-one-rewrite-surface-and-alignment-lowering-split.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can follow the corrected chain from transformed memory-access shapes to Binaryen source strategy to exact Starshine non-recognition and future registry/landing-zone questions.
- Health check while touching the folder fixed the main stale-reference issue in place: the older `0221` research note is now explicitly marked superseded for the no-memory-bailout / `SIMDStore` / helper-branch / broad-lit-coverage claims, and the touched living pages now cite the committed raw manifest instead of relying only on older direct online links.

## [2026-04-24] research | add `rereloop` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/rereloop/` folder, then chose `rereloop` because the dossier already had overview / Binaryen strategy / implementation map / CFG-boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-rereloop-primary-sources.md`, capturing the official Binaryen `version_129` release page, `ReReloop.cpp`, `pass.cpp`, `cfg/Relooper.*`, `ir/flat.h`, `flatten_rereloop.wast`, `opt_flatten.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: public upstream spelling `rereloop`, hard flatness precondition, task-based flat-IR CFG construction, generic `RelooperBuilder` rendering with a helper `i32` label local, EH hard rejection, dead-end terminator repair, result-typed `unreachable` repair, and final `ReFinalize`.
- Added `docs/wiki/raw/research/0316-2026-04-24-rereloop-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/rereloop/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cli/cli_test.mbt`, `src/cmd/cmd_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine tracks only the local `re-reloop` spelling as removed, parses the explicit CLI flag, rejects command execution without output, hides it from help, has no owner file, and has no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/rereloop/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flat-cfg-builder-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from flat WAT/CFG shapes to Binaryen source strategy to exact Starshine non-implementation and future flat-IR CFG/rendering landing questions.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0183 research note and direct online links.

## [2026-04-24] research | add `memory64-lowering` primary-source capture and Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/raw/research/`, then chose `memory64-lowering` / `table64-lowering` as a justified new upstream-only dossier because the tracker had no obvious remaining `none` target while Starshine already models memory64/table64 limits and neighboring memory/table pass docs repeatedly depend on address-width correctness.
- Added `docs/wiki/raw/binaryen/2026-04-24-memory64-lowering-primary-sources.md`, capturing the official Binaryen `version_129` release page, `Memory64Lowering.cpp`, `pass.cpp`, `passes.h`, `memory64-lowering.wast`, `table64-lowering.wast`, current-`main` spot-check URLs, and Emscripten's official `MEMORY64` setting reference. The capture records the source-backed contract: memory/table declaration lowering, `i32.wrap_i64` operand repair, `i64.extend_i32_u` size/grow result repair, active data/element offset rewrites, and mixed bulk memory/table width rules.
- Added `docs/wiki/raw/research/0315-2026-04-24-memory64-lowering-primary-sources-and-starshine-followup.md` plus the new living dossier under `docs/wiki/binaryen/passes/memory64-lowering/`, including overview, Binaryen strategy, implementation/test-map, WAT-shape catalog, and Starshine status/port-strategy pages.
- Grounded the local status in exact repo surfaces: `src/passes/optimize.mbt` has no `memory64-lowering` / `table64-lowering` registry entry; `src/lib/types.mbt` models `I32Limits` and `I64Limits`; `src/binary/decode.mbt` and `src/binary/encode.mbt` round-trip memory/table limit widths; `src/validate/validate.mbt` validates memory64 page and active-data-offset behavior; and `src/validate/typecheck.mbt` derives memory address/result widths while still leaving several table operations hard-coded to `i32`.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/binaryen/passes/tracker.md` so the new dossier is discoverable and the widened tracker remains honest about having no obvious remaining still-`none` queue target. `CHANGELOG.md` already had unrelated local edits when this writeback point was reached, so this wiki-only update did not add to or stage that file.

## [2026-04-24] pass-fix | keep nonempty imported-table nullref elem segments in RUME

- Ran the new `--keep-going-after-command-failures` RUME compare lane and found one fresh semantic mismatch at `.tmp/pass-fuzz-rume-keep-going-2026-04-24/failures/case-000186-wasm-smith`: Binaryen kept an imported `externref` table plus nonempty active expression elem segments whose initializers were all `ref.null`, while Starshine dropped the table and elem segments as effect-free.
- Updated `src/passes/remove_unused_module_elements_test.mbt` TDD-first by changing the imported nullref active elem regression to expect the imported table and nonempty active elem segments to remain; the focused package-file test failed before the implementation change.
- Updated `src/passes/remove_unused_module_elements.mbt` so expression-backed elem segments count as parent-retaining whenever their initializer list is nonempty, regardless of whether the expressions are all `ref.null`; empty expression lists and empty `funcs` lists still do not keep imported parents alive by themselves.
- Verified the fix with the focused RUME test file and a keep-going `wasm-smith` rerun at `.tmp/pass-fuzz-rume-keep-going-2026-04-24-fix`, which reached `298 / 300` compared, `298` normalized matches, `0` mismatches, `0` validation failures, `0` generator failures, and `2` command failures.

## [2026-04-24] research | add `type-un-finalizing` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/type-un-finalizing/` folder, then chose `type-un-finalizing` because the dossier already had overview / Binaryen strategy / implementation map / private-boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-type-un-finalizing-primary-sources.md`, capturing the official Binaryen release, shared `TypeFinalizing.cpp` owner file, `pass.cpp`, `module-utils.*`, `type-updating.*`, `subtypes.h`, the dedicated shared `type-finalizing.wast` lit file, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: public upstream spelling `type-unfinalizing`, local Starshine spelling `type-un-finalizing`, GC-only gate, private heap-type candidate set, no finalizing-mode leaf proof, `GlobalTypeRewriter` coherence, and `setOpen(true)` as the actual state toggle.
- Added `docs/wiki/raw/research/0314-2026-04-24-type-un-finalizing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-un-finalizing/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has only a boundary-only registry entry, active request rejection, no owner file, no preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/type-un-finalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `private-boundaries-sibling-split-and-no-leaf-rule.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed private-type-open WAT shapes to Binaryen source strategy to exact Starshine non-implementation and future shared type-finality landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the `type-finalizing` Starshine page no longer says the sibling still lacks a 2026-04-24 manifest, and the touched `type-un-finalizing` pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0193 research note and direct online links.

## [2026-04-24] pass-signoff | keep RUME compare coverage moving through known Binaryen parser failures

- Added an explicit `--keep-going-after-command-failures` mode to `scripts/lib/pass-fuzz-compare-task.ts` so RUME-style coverage sweeps can keep collecting comparable cases after known Binaryen parser/canonicalization command failures while still recording those failures and preserving replay artifacts.
- Covered the new mode in `scripts/test/pass-fuzz-compare-command.ts` with a fake Binaryen `invalid type index` run that would previously stop at the `--max-failures` cutoff, and tightened list coverage for the `binaryen-rec-group-zero` and `binaryen-invalid-wasm-type-neg64` classes that remain relevant to the RUME command-failure corpus.
- Updated `agent-todo.md` and the RUME parity page to recommend the explicit keep-going mode for future coverage-only RUME sweeps; the pass's semantic blocker classification remains unchanged.

## [2026-04-24] research | add `gufa` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/gufa/` folder, then chose plain `gufa` because the folder already had overview / Binaryen strategy / implementation map / oracle-boundary / transformed-shape coverage but still lacked the same immutable raw primary-source manifest and dedicated Starshine status/port-strategy page already added for `gufa-optimizing` and `gufa-cast-all`.
- Added `docs/wiki/raw/binaryen/2026-04-24-gufa-primary-sources.md`, capturing the official Binaryen release, shared `GUFA.cpp` owner file, `pass.cpp`, `possible-contents.h`, dedicated `gufa.wast`, sibling `gufa-optimizing.wast`, sibling `gufa-cast-all.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed base contract: shared `ContentOracle` engine, `optimizing = false`, `castAll = false`, materializable-value and impossible-site rewrites, `ref.eq` / `ref.test` / existing-`ref.cast` logic, refinalization, EH nested-pop repair, no sibling cleanup rerun, and no fresh-cast insertion.
- Added `docs/wiki/raw/research/0313-2026-04-24-gufa-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/gufa/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `src/passes/pass_manager.mbt`, `src/lib/types.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/ir/hot_flags.mbt`, `src/ir/effects.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, CLI rejection, lower-level not-implemented rejection, no owner file, no dispatch case, no active preset role, and no backlog slice.
- Refreshed `docs/wiki/binaryen/passes/gufa/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `content-oracle-variants-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed GUFA expression shapes to Binaryen source strategy to exact Starshine non-implementation and future whole-program-oracle landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0163 research note and direct online links. The family-level open follow-up from the `gufa-cast-all` refresh is now closed: plain `gufa`, `gufa-optimizing`, and `gufa-cast-all` all have raw manifests plus local status bridges.
- Follow-up health cleanup also marked the older `0311` / `0312` GUFA-family research-note open-follow-up sections as superseded and refreshed the tracker maintenance note so it no longer points future runs back at the now-closed plain-`gufa` provenance gap.

## [2026-04-24] pass-signoff | refresh RUME semantic blocker classification

- Re-read the pass implementation skill, repo docs schema, active `agent-todo.md` RUME section, and the existing `docs/wiki/binaryen/passes/remove-unused-module-elements/` dossier before choosing a docs-only signoff iteration.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-module-elements/parity.md` to carry the latest backlog evidence: the post-cleanup `.tmp/pass-fuzz-rume-live-nullfuncref-rerun` lane reached `165 / 165` comparable `wasm-smith` cases with `0` mismatches, Starshine contributed no command failures in that slice, and the remaining cutoff slots are Binaryen parser or canonicalization families rather than RUME semantic keep/drop or remap bugs.
- Updated `agent-todo.md` to point readers at the living RUME parity page and the moved archived optimize-path source note instead of the stale pre-wiki `docs/0066-...` path.
- No new wiki catalog page was needed because this was a freshness/signoff update to the existing RUME parity page.

## [2026-04-24] research | add `gufa-cast-all` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/gufa-cast-all/` folder, then chose `gufa-cast-all` because the dossier already had overview / Binaryen strategy / implementation map / cast-boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-gufa-cast-all-primary-sources.md`, capturing the official Binaryen release, shared `GUFA.cpp` owner file, `pass.cpp`, `possible-contents.h`, dedicated `gufa-cast-all.wast`, sibling `gufa.wast`, sibling `gufa-optimizing.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: shared `ContentOracle` engine, `optimizing = false`, `castAll = true`, first-phase plain-GUFA rewrites, post-refinalize `addNewCasts`, GC/castability/subtype/exactness gates, second refinalization after inserted casts, EH nested-pop repair, no nested `dce` / `vacuum`, and conservative imported/exported tag behavior.
- Added `docs/wiki/raw/research/0312-2026-04-24-gufa-cast-all-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/gufa-cast-all/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `src/passes/pass_manager.mbt`, `src/lib/types.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/ir/hot_flags.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, CLI rejection, lower-level not-implemented rejection, no owner file, no dispatch case, no active preset role, and no backlog slice.
- Refreshed `docs/wiki/binaryen/passes/gufa-cast-all/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `cast-insertion-exactness-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed cast-insertion WAT shapes to Binaryen source strategy to exact Starshine non-implementation and future whole-program-oracle-plus-cast-materialization landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0190 research note and direct online links. The tracker now records that `gufa-cast-all`'s former provenance / local-follow-along gap is closed and points a future GUFA-family refresh at plain `gufa` if uniformity remains useful.

## [2026-04-24] research | add `gufa-optimizing` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/gufa-optimizing/` folder, then chose `gufa-optimizing` because the dossier already had overview / Binaryen strategy / implementation map / cleanup contract / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-gufa-optimizing-primary-sources.md`, capturing the official Binaryen release, shared `GUFA.cpp` owner file, `pass.cpp`, `possible-contents.h`, dedicated `gufa-optimizing.wast`, sibling `gufa.wast`, sibling `gufa-cast-all.wast`, and current-`main` spot-check URLs reviewed in this run. The capture records the source-backed contract: shared `ContentOracle` engine, `optimizing = true`, `castAll = false`, refinalization before cleanup, EH nested-pop repair, changed-function nested `dce` then `vacuum`, no fresh-cast insertion, and no reviewed default no-DWARF scheduler role.
- Added `docs/wiki/raw/research/0311-2026-04-24-gufa-optimizing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/gufa-optimizing/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `src/passes/pass_manager.mbt`, `src/passes/dead_code_elimination.mbt`, `src/ir/effects.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, CLI rejection, lower-level not-implemented rejection, no owner file, no dispatch case, no active preset role, and no backlog slice.
- Refreshed `docs/wiki/binaryen/passes/gufa-optimizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `cleanup-rerun-contract.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from cleanup-residue WAT shapes to Binaryen source strategy to exact Starshine non-implementation and future module-oracle-plus-HOT-cleanup landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0189 research note and direct online links. The new follow-up also records that `gufa-cast-all` remains the more obvious GUFA-family sibling for the same future provenance-and-Starshine-bridge treatment.

## [2026-04-24] health | repair stale `type-generalizing` index summary

- Ran a focused stale-reference grep after the `type-finalizing` dossier commit and found one remaining stale summary in `docs/wiki/index.md` still describing `type-generalizing` as a closed-world `ContentOracle` / `call_ref` / optimizing-casts family.
- Updated that catalog entry to match the corrected 2026-04-24 source story: upstream `experimental-type-generalizing` is a hidden function-local local-set/local-tee evidence pass with subtype/LUB retagging, `local.get` drop-plus-zero fallback, defaultability barriers, and no `ContentOracle` / `call_ref` / optimizing-casts sibling surface.

## [2026-04-24] research | add `type-finalizing` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/type-finalizing/` folder, then chose `type-finalizing` because the dossier had overview / Binaryen strategy / implementation map / boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-type-finalizing-primary-sources.md`, capturing the official Binaryen release, `TypeFinalizing.cpp`, `pass.cpp`, helper surfaces, and `type-finalizing.wast` URLs reviewed in this run. The capture records the source-backed contract: GC-only gate, public `type-finalizing` plus upstream `type-unfinalizing`, private heap-type candidate set, finalizing-only immediate-subtype leaf proof, one `GlobalTypeRewriter` `setOpen(!finalize)` mutation, and a narrow current-`main` no-teaching-drift spot check.
- Added `docs/wiki/raw/research/0310-2026-04-24-type-finalizing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-finalizing/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, active request rejection, preset omission, no owner file, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/type-finalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `leaf-types-public-boundaries-and-sibling-split.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed final/open heap-type shapes to Binaryen source strategy to exact Starshine non-implementation and future module/type-section landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0192 research note and direct online links.

## [2026-04-24] research | add `reorder-types` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/reorder-types/` folder, then chose `reorder-types` because the dossier already had overview / Binaryen strategy / implementation map / cost-model / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-reorder-types-primary-sources.md`, capturing the official Binaryen release, `ReorderTypes.cpp`, `pass.cpp`, `type-updating.*`, `module-utils.*`, `wasm-type-ordering.h`, and dedicated `reorder-types.wast` URLs reviewed in this run. The capture records the source-backed contract: GC-only + hard `--closed-world` gates, public `reorder-types` plus hidden `reorder-types-for-testing`, private-supertype and private-described-type predecessor edges, 21 successor-weight factors, encoded-index cost scoring, `GlobalTypeRewriter` remap, and a narrow current-`main` no-teaching-drift spot check.
- Added `docs/wiki/raw/research/0309-2026-04-24-reorder-types-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/reorder-types/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/cli/cli.mbt`, `src/cmd/cmd.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, active request rejection, no module-dispatcher case, no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/reorder-types/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `ordering-cost-model-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed private-type layout shapes to Binaryen source strategy to exact Starshine non-implementation and future module/type-remap landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on older numbered research notes and direct online links.

## [2026-04-24] research | correct `type-generalizing` and add a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/type-generalizing/` folder, then chose `type-generalizing` because the dossier lacked an immutable raw primary-source manifest and Starshine strategy page, and fresh official source review exposed a major stale algorithm claim.
- Added `docs/wiki/raw/binaryen/2026-04-24-type-generalizing-primary-sources.md`, capturing the official Binaryen release, `TypeGeneralizing.cpp`, `pass.cpp`, `wasm-type.h`, and `type-generalizing.wast` URLs reviewed in this run. The capture records the correction: reviewed `version_129` registers one hidden/test `experimental-type-generalizing` pass, not a public two-sibling family, and implements function-local local-set/local-tee type evidence plus subtype/LUB retagging rather than a closed-world `ContentOracle` pass over `struct.get`, `struct.set`, `call_ref`, and `ref.cast`.
- Added `docs/wiki/raw/research/0308-2026-04-24-type-generalizing-source-correction-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-generalizing/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `agent-todo.md`) and making explicit that Starshine has only a boundary-only alias with active request rejection, no preset role, no owner file, and no backlog slice.
- Refreshed `docs/wiki/binaryen/passes/type-generalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `wat-shapes.md`, and renamed the stale `call-ref-casts-and-boundaries.md` page to `local-flow-type-floor-and-boundaries.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers now follow the corrected chain from local-flow shapes to Binaryen source strategy to exact Starshine non-implementation status.
- Health check while touching the folder fixed the main stale-reference issue in place: every touched living page now marks `docs/wiki/raw/research/0191-2026-04-21-type-generalizing-binaryen-research.md` as historical/superseded for mechanics instead of silently carrying forward unsupported `ContentOracle`, one-signature `call_ref`, impossible-target, and optimizing-casts claims. A repository-wide local markdown/frontmatter link scan across 918 wiki markdown files then passed with no missing local references.

## [2026-04-24] research | add `signature-refining` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/signature-refining/` folder, then chose `signature-refining` because the dossier already had the required overview / Binaryen strategy / implementation map / hard-boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-signature-refining-primary-sources.md`, capturing the official Binaryen release, `SignatureRefining.cpp`, registration source, helper headers/implementations, and dedicated `signature-refining.wast` URLs reviewed in this run. The capture records the source-backed contract: GC plus no-table gates, heap-type-level direct-call / `call_ref` / `call.without.effects` aggregation, param-LUB and result-LUB refinement, public/import/tag/subtyping blockers, params-only JS/continuation blockers, parameter-body repair, nominal signature rewriting, intrinsic import cloning, and final refinalization.
- Added `docs/wiki/raw/research/0307-2026-04-24-signature-refining-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/signature-refining/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only registry entry, active request rejection, active-preset omission, no owner file, no active backlog slice, no local `call.without.effects` surface, and a direct-`call_ref` WAT-lowering caveat.
- Refreshed `docs/wiki/binaryen/passes/signature-refining/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `params-results-publicity-and-intrinsics.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed signature/call/return shapes to Binaryen source strategy to exact Starshine non-implementation status and future module/type-graph landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0152 research note, and the local follow-along now records the missing intrinsic surface and direct-`call_ref` text-fixture caveat instead of hiding those future-port prerequisites.

## [2026-04-24] research | add `global-type-optimization` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/global-type-optimization/` folder, then chose `global-type-optimization` because the dossier already had the required overview / Binaryen strategy / implementation map / hard-boundary / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-global-type-optimization-primary-sources.md`, capturing the official Binaryen release, `GlobalTypeOptimization.cpp`, registration source, helper headers, and dedicated `gto*` lit-test URLs reviewed in this run. The capture records the source-backed contract: upstream `gto` spelling, hard GC + closed-world gates, runtime field traffic plus JS exposure evidence, hierarchy-aware immutability/removal decisions, instruction-before-type rewrite order, trap-preserving removed-field handling, field-name repair, and current-`main` no-teaching-drift spot-check scope.
- Added `docs/wiki/raw/research/0306-2026-04-24-global-type-optimization-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/global-type-optimization/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/typecheck.mbt`, `src/binary/decode.mbt`, `src/binary/encode.mbt`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only full-name registry entry, no `gto` alias, active request rejection, preset omission, no owner file, no backlog slice, and reusable but incomplete GC/type/parser/validator/binary surfaces.
- Refreshed `docs/wiki/binaryen/passes/global-type-optimization/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `field-removal-subtyping-js-interop-and-traps.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed private-struct field shapes to Binaryen source strategy to exact Starshine non-implementation and future closed-world module/type-graph landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older 0153 research note and direct online links; the new Starshine page also records the WAT `struct.set` fixture gap and the missing struct-atomic RMW/cmpxchg surface instead of hiding those local implementation prerequisites.

## [2026-04-24] research | add `global-effects` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/global-effects/` folder, then chose `global-effects` because the dossier already had overview / Binaryen strategy / implementation-test-map / metadata-consumer / shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-global-effects-primary-sources.md`, capturing the official Binaryen release, `GlobalEffects.cpp`, `pass.cpp`, `effects.h`, `wasm.h`, `vacuum-global-effects.wast`, and `global-effects_simplify-locals.wast` URLs reviewed in this run. The capture records the source-backed contract: metadata-only `Function.effects` production, shallow local effect scanning, direct-call/unknown-effect split, transitive static-call propagation, recursive-cycle conservatism, downstream consumer tests, the `discard-global-effects` lifecycle sibling, and current-`main` SCC propagation drift.
- Added `docs/wiki/raw/research/0305-2026-04-24-global-effects-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/global-effects/starshine-strategy.md`, grounding the local status in exact repository surfaces (`src/passes/optimize.mbt`, `src/cli/cli_test.mbt`, `src/ir/effects.mbt`, `src/ir/analysis_cache.mbt`, `src/passes/pass_common.mbt`, `src/passes/simplify_locals.mbt`, `src/passes/heap_store_optimization.mbt`, and `agent-todo.md`) and making explicit that current Starshine has boundary-only flag tracking plus function-local HOT effect masks, but no module-level interprocedural summary pass, owner file, preset slot, or backlog slice.
- Refreshed `docs/wiki/binaryen/passes/global-effects/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `metadata-naming-and-consumers.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from call/global metadata shapes to Binaryen source strategy to exact Starshine non-implementation and future module/call-graph landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on older online links and the 0168 research note, and the dossier now records the stale upstream `PassOptions` wording versus actual `Function.effects` storage contradiction explicitly.

## [2026-04-24] research | add `signature-pruning` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/signature-pruning/` folder, then chose `signature-pruning` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape / hard-boundary coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-signature-pruning-primary-sources.md`, capturing the official Binaryen release, `SignaturePruning.cpp`, registration source, helper headers, and dedicated `signature-pruning.wast` URLs reviewed in this run. The capture records the source-backed contract: GC + closed-world gates, whole-module table bailout, heap-type-level direct-call and `call_ref` aggregation, entry-liveness parameter usage, constant-actual promotion, public/import/tag/continuation/JS/subtyping blockers, atomic signature rewriting, and one delayed-localization rerun.
- Added `docs/wiki/raw/research/0304-2026-04-24-signature-pruning-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/signature-pruning/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine keeps `signature-pruning` as a boundary-only name with active request rejection, no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/signature-pruning/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `constant-actuals-localization-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed signature/call shapes to Binaryen source strategy to exact Starshine non-implementation status and future module/type-graph landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older numbered research note, and the local follow-along now records the direct `call_ref` WAT-lowering caveat instead of assuming text fixtures cover the entire local library/binary surface.

## [2026-04-24] research | add `type-refining` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/type-refining/` folder, then chose `type-refining` because the dossier already had the required overview / Binaryen strategy / implementation map / fixup-mechanics / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-type-refining-primary-sources.md`, capturing the official Binaryen release, `TypeRefining.cpp`, registration source, helper headers, dedicated `type-refining*` lit files, and current-`main` spot-check URLs reviewed in this run. The capture keeps the source-backed contract consumable: hard GC/closed-world gates, normal-vs-GUFA inference split, field-LUB aggregation, public-type freezing, hierarchy legality repair, explicit `struct.get` updates, module-wide type rewriting, post-rewrite write fixups, and the real `type-refining-gufa` sibling proof surface.
- Added `docs/wiki/raw/research/0303-2026-04-24-type-refining-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-refining/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps only the base `type-refining` spelling as a boundary-only name with active request rejection, no owner file, no active preset role, no active backlog slice, and no separate `type-refining-gufa` registry spelling today.
- Refreshed `docs/wiki/binaryen/passes/type-refining/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `normal-vs-gufa-and-fixups.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed struct-field shapes to Binaryen source strategy to exact Starshine non-implementation status and future closed-world module/type-graph landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on the older numbered research note, and the local follow-along now records the WAT `struct.set` fixture caveat instead of hiding it behind the broader struct-operation parser support story.
- Ran the focused local markdown/frontmatter link check over the 12 touched wiki files in this update and separately checked the new top `CHANGELOG.md` entry; no missing local references remained in the touched set. The check intentionally avoided full historical `CHANGELOG.md` scanning because older entries still contain known pre-existing `.tmp/` and absolute worktree link noise outside this update.

## [2026-04-24] health | repair local-reference findings after `monomorphize`

- Ran a repository-wide local markdown/frontmatter reference scan across `docs/wiki/` after the `monomorphize` provenance commit; the scan checked 900 wiki markdown files and reported 15 remaining local-reference misses.
- Fixed the durable misses by retargeting `alignment-lowering`, `duplicate-import-elimination`, `inlining-optimizing`, `reorder-globals`, and `type-ssa` Starshine status pages to the correct `../../no-dwarf-default-optimize-path.md` location, correcting the archived no-DWARF planning note link in `inlining-optimizing`, fixing the neighboring DAE research-note link in the `dae2` raw research note, and shortening two over-escaped repo-root paths in the `legalize-and-prune-js-interface` raw research note.
- Re-ran the same repository-wide scan and it passed with no missing local references across 900 wiki markdown files.

## [2026-04-24] research | add `monomorphize` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/monomorphize/` plus sibling `monomorphize-always/` folder, then chose `monomorphize` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape / clone-mechanics coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-monomorphize-primary-sources.md`, capturing the official Binaryen GitHub release, shared `Monomorphize.cpp` owner file, registration source, helper headers, and dedicated `monomorphize*` plus `no-inline-monomorphize-inlining` lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish timestamp **2026-04-01 14:31**, while a narrow current-`main` spot check did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0302-2026-04-24-monomorphize-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/monomorphize/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `src/cli/cli.mbt`, `src/cli/cli_test.mbt`, `src/cmd/cmd_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine tracks `monomorphize` / `monomorphize-always` as boundary-only names and carries `--monomorphize-min-benefit` option plumbing, but has no owner file, active transform, or dedicated backlog slice.
- Refreshed `docs/wiki/binaryen/passes/monomorphize/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `call-context-benefit-and-boundaries.md`, `clone-construction-signature-rebuild-and-dropped-call-rewrites.md`, `wat-shapes.md`, and the sibling `monomorphize-always/index.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the full chain from transformed call-context shapes to Binaryen source strategy to exact Starshine non-implementation plus future whole-module landing zone.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on older numbered research notes, and the sibling `monomorphize-always` landing page now points at the shared 2026-04-24 source manifest.

## [2026-04-24] research | add `constant-field-propagation` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/constant-field-propagation/` plus sibling `constant-field-null-test-folding/` folders, then chose `constant-field-propagation` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-constant-field-propagation-primary-sources.md`, capturing the official Binaryen release, shared `ConstantFieldPropagation.cpp` owner file, registration source, helper headers, parent and sibling lit files, and current-`main` spot-check URLs reviewed in this run. The capture keeps the source-backed contract consumable: hard GC/closed-world gates, struct write/default/copy/RMW scanning, tiny literal-or-immutable-global value lattice, exact-vs-inexact propagation, copy fixed points, read-only rewrite, null-trap / packed-field / atomic boundaries, and the real `cfp-reftest.wast` sibling proof surface.
- Added `docs/wiki/raw/research/0301-2026-04-24-constant-field-propagation-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/constant-field-propagation/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps both `constant-field-propagation` and `constant-field-null-test-folding` as boundary-only names with active request rejection, no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/constant-field-propagation/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `copies-subtypes-ref-tests-and-atomics.md`, and `wat-shapes.md`, lightly refreshed the sibling `constant-field-null-test-folding/index.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed struct-field read shapes to Binaryen source strategy to exact Starshine non-implementation and future closed-world module-pass landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the parent CFP implementation/test-map page now points at the real dedicated `cfp-reftest.wast` sibling proof surface instead of saying no dedicated variant file had been reviewed, and the touched pages now cite the committed raw manifest and Starshine status page rather than relying only on the older numbered research notes.

## [2026-04-24] health | repair local-reference findings from the wiki health check

- Ran a repository-wide local markdown/frontmatter reference scan across `docs/wiki/` after the `optimize-added-constants` dossier commit; the scan checked 894 wiki markdown files and reported 12 missing local references.
- Fixed the durable misses: two raw-research notes now point at `../binaryen/...` raw manifests, tooling pages now point at canonical `docs/README.md`, the `ssa` implementation/test map now links to the existing `ssa-nomerge` landing page instead of a nonexistent sibling implementation page, and the late-pipeline dispatch note now points at `../no-dwarf-default-optimize-path.md`.
- Removed missing uncommitted `.tmp/.../result.json` artifact paths from the `simplify-locals` frontmatter source lists while preserving the historical run summaries in prose, so frontmatter sources stay limited to committed durable files.

## [2026-04-24] research | add `optimize-added-constants` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/optimize-added-constants/` plus sibling `optimize-added-constants-propagate/` folders, then chose plain `optimize-added-constants` because the dossier already had overview / Binaryen strategy / implementation map / safety-matrix / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-optimize-added-constants-primary-sources.md`, capturing the official Binaryen release, shared `OptimizeAddedConstants.cpp` owner file, registration/options files, helper headers, plain and propagate low-memory test pairs, memory64 and no-memory lit files, plus a narrow current-`main` freshness note. The capture keeps the source-backed contract consumable: `Load` / `Store` memory-address canonicalization, hard `--low-memory-unused` gate, `LowMemoryBound = 1024`, strict `1023`-yes / `1024`-no cutoff, overflow-checked constant-pointer normalization, no-memory no-op, and the plain-vs-propagate split.
- Added `docs/wiki/raw/research/0300-2026-04-24-optimize-added-constants-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/optimize-added-constants/starshine-strategy.md`, grounding the follow-up in exact local status and future-port surfaces (`src/passes/optimize.mbt`, `src/cmd/cmd.mbt`, `src/cli/cli.mbt`, `src/lib/types.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_side_tables.mbt`, `src/ir/hot_builders.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/binary/decode.mbt`, `src/binary/encode.mbt`, `src/wast/lexer.mbt`, `src/wast/keywords.mbt`, `src/validate/typecheck.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine keeps the pass as a removed registry name with honest request rejection, no owner file, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/optimize-added-constants/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `low-memory-threshold-overflow-and-offset-merge-rules.md`, and `wat-shapes.md`, lightly refreshed the sibling `optimize-added-constants-propagate/index.md` to cite the shared manifest, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the full chain from transformed load/store address shapes to Binaryen source strategy to exact Starshine non-implementation and future direct-only HOT landing zones.
- Health check while touching the folder fixed the main stale-reference issue in place: the touched pages now cite the committed raw manifest and Starshine status page instead of relying only on older numbered research notes, and the tracker now marks the former provenance / local-follow-along gap as closed.

## [2026-04-24] research | add `i64-to-i32-lowering` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/i64-to-i32-lowering/` folder, then chose `i64-to-i32-lowering` because the dossier already had the required overview / Binaryen strategy / implementation map / flatness-boundary / ABI-opcode / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-i64-to-i32-lowering-primary-sources.md`, capturing the official Binaryen release, `I64ToI32Lowering.cpp`, registration/declaration files, helper headers, and the dedicated `flatten_i64-to-i32-lowering.wast` URLs reviewed in this run. The capture keeps the `version_129` flat-input pair-lowering contract, synthetic return-high global, wasm2js helper dependencies, and unsupported imported-global / return-call / atomic / hard-arithmetic boundaries consumable from one committed manifest.
- Added `docs/wiki/raw/research/0299-2026-04-24-i64-to-i32-lowering-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/i64-to-i32-lowering/starshine-strategy.md`, grounding the follow-up in exact local status and future-port surfaces (`src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/lib/util.mbt`, `src/wast/keywords.mbt`, `src/wast/lower_to_lib.mbt`, `src/binary/decode.mbt`, `src/binary/encode.mbt`, `src/validate/env.mbt`, `src/validate/typecheck.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine keeps `i64-to-i32-lowering` as a boundary-only name with no owner file, no module dispatcher case, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/i64-to-i32-lowering/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flatness-helpers-and-boundaries.md`, `abi-surface-and-opcode-coverage.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed i64 ABI/opcode shapes to Binaryen source strategy to exact Starshine non-implementation and future module-pass landing zones.
- Ran the focused local markdown/frontmatter link check over the 13 touched wiki files in this update and separately checked the new top `CHANGELOG.md` entry; no missing local references remained in the touched set. A broader historical `CHANGELOG.md` scan remains noisy from pre-existing `.tmp/` and absolute worktree links outside this update's touched entry.

## [2026-04-24] research | correct `remove-unused-types`, add primary-source capture, and file the Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/remove-unused-types/` folder, then chose `remove-unused-types` because the dossier had no immutable raw primary-source manifest or Starshine status/port page and a fresh source check exposed a teaching-significant stale algorithm overread.
- Added `docs/wiki/raw/binaryen/2026-04-24-remove-unused-types-primary-sources.md`, capturing the official Binaryen release, `RemoveUnusedTypes.cpp`, `pass.cpp`, `type-updating.h`, `module-utils.h`, and dedicated lit-test URLs reviewed in this run. The capture records that the corrected `version_129` owner file is a tiny GC/closed-world wrapper around `GlobalTypeRewriter(*module).update()`, not the older pass-local public/used scanner plus whole-old-rec-group builder story.
- Added `docs/wiki/raw/research/0298-2026-04-24-remove-unused-types-source-correction-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/remove-unused-types/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, `src/validate/env.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `remove-unused-types` as a boundary-only, actively rejected name with no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-types/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `closed-world-visibility-and-rec-group-rewrite.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers now follow the corrected chain from transformed private/public type-graph shapes to Binaryen's helper-owned strategy to exact Starshine non-implementation status without relying on the superseded 0149 phase map.
- Health check while touching the folder fixed the main stale-reference issue in place: every touched `remove-unused-types` page now marks the older 0149 pass-local scanner / whole-old-rec-group claim as historical and superseded, rather than silently smoothing over the contradiction.

## [2026-04-24] research | add `reorder-functions` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, the existing `docs/wiki/binaryen/passes/reorder-functions/` folder, and the sibling `reorder-functions-by-name` folder, then chose `reorder-functions` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-reorder-functions-primary-sources.md`, capturing the official Binaryen GitHub release, `ReorderFunctions.cpp`, `pass.cpp`, the sibling lit file, and pass-test directory listing URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner, registration, sibling-test, and directory-listing surfaces did not surface teaching-relevant drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0297-2026-04-24-reorder-functions-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/reorder-functions/starshine-strategy.md`, grounding the follow-up in exact local status and future-port surfaces (`src/passes/optimize.mbt`, `src/passes/pass_manager.mbt`, `src/lib/types.mbt`, `src/passes/duplicate_function_elimination.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `reorder-functions` as a boundary-only name with active request rejection, no module dispatcher case, no owner file, no active preset role, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/reorder-functions/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `count-surfaces-ordering-and-omissions.md`, and `module-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed module-order shapes to Binaryen source strategy to exact Starshine non-implementation and future numeric-`FuncIdx` remap requirements without relying on older research notes alone.
- Ran the focused local markdown-link check over the 12 touched wiki files in this update; no missing local wiki links remained. A broader `CHANGELOG.md` scan still reports the pre-existing historical `.tmp/` link noise already noted by earlier health passes, outside this update's touched entry.

## [2026-04-24] research | add `precompute-propagate` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/precompute-propagate/` folder, then chose `precompute-propagate` because the dossier had the required overview / Binaryen strategy / implementation map / transformed-shape / worklist coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-precompute-propagate-primary-sources.md`, capturing the official Binaryen GitHub release, shared `Precompute.cpp` owner file, registration and nested scheduler sources, helper headers, and dedicated propagate lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner/helper/test surfaces did not surface teaching-relevant sibling-contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0296-2026-04-24-precompute-propagate-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/precompute-propagate/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/precompute.mbt`, `src/passes/pass_manager.mbt`, `src/passes/registry_test.mbt`, `src/passes/optimize_test.mbt`, `src/passes/precompute_test.mbt`, `src/cmd/cmd_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine keeps `precompute-propagate` as a removed registry name while active plain `precompute` is only the nearest landing zone, not an implementation of the sibling `LazyLocalGraph` propagation mode.
- Refreshed `docs/wiki/binaryen/passes/precompute-propagate/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `local-worklist-fallthrough-and-merge-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from propagated local shapes to Binaryen source strategy to exact Starshine non-implementation / future-port status without treating `precompute-propagate` as a mere alias of local `precompute`.
- Ran the focused local markdown/frontmatter link check over the 12 touched wiki files in this update; no missing local references remained. A broader `CHANGELOG.md` link scan remains noisy from pre-existing historical `.tmp/` and absolute worktree links outside this follow-up's touched entry.

## [2026-04-24] research | add `abstract-type-refining` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/abstract-type-refining/` folder, then chose `abstract-type-refining` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-abstract-type-refining-primary-sources.md`, capturing the official Binaryen GitHub release, `AbstractTypeRefining.cpp`, registration source, helper headers, and dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0295-2026-04-24-abstract-type-refining-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/abstract-type-refining/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/ref_null_exact_surface_test.mbt`, `src/wast/exact_type_equivalence_test.mbt`, `src/validate/env.mbt`, `src/validate/match.mbt`, `src/validate/typecheck.mbt`, `src/binary/encode.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `abstract-type-refining` as a boundary-only, actively rejected pass name with no owner file, no active backlog slice, and no open-world no-DWARF preset role.
- Refreshed `docs/wiki/binaryen/passes/abstract-type-refining/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `traps-never-happen-exact-casts-and-descriptors.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed struct/descriptor/exact-cast shapes to Binaryen source strategy to exact Starshine non-implementation status without relying on the older research note alone.
- Ran a focused local markdown/frontmatter link check over the 12 touched wiki files in this update; no missing local references remained. A broader `CHANGELOG.md` link scan remains noisy from pre-existing historical `.tmp/` and absolute worktree links outside this follow-up's touched entry.

## [2026-04-24] research | add `type-merging` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/type-merging/` folder, then chose `type-merging` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-type-merging-primary-sources.md`, capturing the official Binaryen GitHub release, `TypeMerging.cpp`, registration source, helper headers, and dedicated lit-test URL reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit file did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0294-2026-04-24-type-merging-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-merging/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, `src/wast/ref_null_exact_surface_test.mbt`, `src/validate/env.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `type-merging` as a boundary-only, actively rejected pass name with no owner file, no active backlog slice, and reusable type-section / exact-ref / descriptor validation infrastructure but no pass implementation.
- Refreshed `docs/wiki/binaryen/passes/type-merging/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `dfa-partitions-casts-and-refinalization.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed type-graph shapes to Binaryen source strategy to exact Starshine non-implementation status without relying on the older research note alone.

## [2026-04-24] research | add `dead-argument-elimination` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/dead-argument-elimination/` folder, then chose plain `dead-argument-elimination` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-dead-argument-elimination-primary-sources.md`, capturing the official Binaryen GitHub release, shared `DeadArgumentElimination.cpp` owner file, registration source, helper headers, and dedicated plain / sibling lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit roster did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0293-2026-04-24-dead-argument-elimination-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/dead-argument-elimination/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/cmd/fuzz_harness_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `docs/wiki/binaryen/passes/dae-optimizing/starshine-strategy.md`, and `agent-todo.md`) and making explicit that current Starshine knows the descriptive boundary-only name `dead-argument-elimination` but not exact upstream alias `dae`.
- Refreshed `docs/wiki/binaryen/passes/dead-argument-elimination/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from plain DAE transformed shapes to Binaryen source strategy to exact Starshine non-implementation status without relying on the optimizing sibling dossier.

## [2026-04-24] research | add `legalize-and-prune-js-interface` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/legalize-and-prune-js-interface/` folder, then chose `legalize-and-prune-js-interface` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-legalize-and-prune-js-interface-primary-sources.md`, capturing the official Binaryen GitHub release, shared `LegalizeJSInterface.cpp` owner file, registration source, dedicated prune lit-test URL, and inherited plain-family lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, and dedicated prune lit file did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0292-2026-04-24-legalize-and-prune-js-interface-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/legalize-and-prune-js-interface/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/binary/tests.mbt`, `src/wast/keywords.mbt`, `src/wast/module_wast.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, and `agent-todo.md`) and making explicit that current Starshine treats `legalize-and-prune-js-interface` as an unknown upstream-only pass name rather than boundary-only, removed, or active.
- Refreshed `docs/wiki/binaryen/passes/legalize-and-prune-js-interface/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `prune-boundary-matrix.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from prune-mode import/export/global shapes to Binaryen source strategy to exact Starshine non-adoption status without relying on the older research note alone.

## [2026-04-24] research | add `legalize-js-interface` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/legalize-js-interface/` folder, then chose `legalize-js-interface` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-legalize-js-interface-primary-sources.md`, capturing the official Binaryen GitHub release, shared `LegalizeJSInterface.cpp` owner file, registration source, helper headers, and dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0291-2026-04-24-legalize-js-interface-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/legalize-js-interface/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/binary/tests.mbt`, `src/wast/keywords.mbt`, `src/wast/module_wast.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, and `agent-todo.md`) and making explicit that current Starshine treats `legalize-js-interface` as an unknown upstream-only pass name rather than boundary-only, removed, or active.
- Refreshed `docs/wiki/binaryen/passes/legalize-js-interface/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `temp-ret-helpers-and-pruning-split.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed JS-boundary shapes to Binaryen source strategy to exact Starshine non-adoption status without relying on the older research note alone.
- Ran the focused local markdown/frontmatter link check over the 12 touched wiki files in this update; no missing local references remained. A broader `CHANGELOG.md` link scan remains noisy from pre-existing historical `.tmp/` and absolute worktree links outside this follow-up's touched entry.

## [2026-04-24] research | add `minimize-rec-groups` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/minimize-rec-groups/` folder, then chose `minimize-rec-groups` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-minimize-rec-groups-primary-sources.md`, capturing the official Binaryen GitHub release, `MinimizeRecGroups.cpp`, public registration source, helper headers, and dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0290-2026-04-24-minimize-rec-groups-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/minimize-rec-groups/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/lib/types.mbt`, `src/wast/parser.mbt`, `src/wast/lower_to_lib.mbt`, `src/wast/module_wast_tests.mbt`, `src/wast/rec_group_typeuse_test.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `minimize-rec-groups` as a boundary-only, actively rejected pass name with active-preset omission, no owner file, and no active backlog slice.
- Refreshed `docs/wiki/binaryen/passes/minimize-rec-groups/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `permutations-brands-and-public-conflicts.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed type-section shapes to Binaryen source strategy to exact Starshine non-implementation status without relying on the older research note alone.
- Ran the focused local markdown/frontmatter link check over the 12 touched wiki files in this update; no missing local references remained. A broader `CHANGELOG.md` link scan remains noisy from pre-existing historical `.tmp/` and absolute worktree links outside this follow-up's touched entry.

## [2026-04-24] research | add `unsubtyping` primary-source capture and a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/unsubtyping/` folder, then chose `unsubtyping` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-unsubtyping-primary-sources.md`, capturing the official Binaryen GitHub release, `Unsubtyping.cpp`, public registration/scheduler sources, helper headers, and dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on the owner file, registration surface, helper headers, and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0289-2026-04-24-unsubtyping-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/unsubtyping/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/cmd/fuzz_harness_wbtest.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `src/lib/types.mbt`, `src/wast/module_wast_tests.mbt`, `src/wast/ref_null_exact_surface_test.mbt`, and `agent-todo.md`) and making explicit that current Starshine keeps `unsubtyping` as a boundary-only registry name, not an active HOT or module pass.
- Refreshed `docs/wiki/binaryen/passes/unsubtyping/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `descriptor-squares-casts-and-js-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so readers can now follow the complete chain from transformed shapes to Binaryen source strategy to exact Starshine non-implementation status without relying on the older research note alone.

## [2026-04-24] research | add `instrument-memory` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/instrument-memory/` folder, then chose `instrument-memory` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-instrument-memory-primary-sources.md`, capturing the official Binaryen GitHub release, `InstrumentMemory.cpp`, public registration/declaration files, and four dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0288-2026-04-24-instrument-memory-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/instrument-memory/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/ir/hot_core.mbt`, `src/ir/hot_lift.mbt`, `src/ir/hot_lower.mbt`, `src/lib/types.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, and `agent-todo.md`) and making explicit that current Starshine treats `instrument-memory` as an unknown pass name rather than a boundary-only or removed compatibility entry.
- Refreshed `docs/wiki/binaryen/passes/instrument-memory/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `helper-import-roster-filters-and-unsupported-types.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/index.md`, and `CHANGELOG.md`, so readers can now follow the complete chain from transformed shapes to Binaryen source strategy to exact Starshine non-adoption status without relying on the older research note alone.

## [2026-04-24] research | add `instrument-locals` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/instrument-locals/` folder, then chose `instrument-locals` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and a dedicated Starshine status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-instrument-locals-primary-sources.md`, capturing the official Binaryen GitHub release, `InstrumentLocals.cpp`, public registration/declaration files, and three dedicated lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on the owner file and dedicated lit files did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0287-2026-04-24-instrument-locals-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/instrument-locals/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `docs/wiki/binaryen/passes/tracker.md`, and `agent-todo.md`) and making explicit that current Starshine treats `instrument-locals` as an unknown pass name rather than a boundary-only or removed compatibility entry.
- Refreshed `docs/wiki/binaryen/passes/instrument-locals/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `unsupported-types-effects-and-import-roster.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so readers can now follow the complete chain from transformed shapes to Binaryen source strategy to exact Starshine non-adoption status without relying on the older research note alone.

## [2026-04-24] research | add `simplify-globals-optimizing` primary-source capture and a Starshine port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/raw/research/`, and the existing `docs/wiki/binaryen/passes/simplify-globals-optimizing/` folder, then chose `simplify-globals-optimizing` because the broader tracker has no obvious `none` target left but this no-DWARF / saved-`-O4z` boundary pass still lacked an immutable raw primary-source manifest, source-confirmed implementation/test-map page, and dedicated Starshine strategy/status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-simplify-globals-optimizing-primary-sources.md`, capturing the official Binaryen GitHub release, shared `SimplifyGlobals.cpp` engine, registration / nested-runner sources, helper headers, and clustered `simplify-globals-*` plus `propagate-globals-globally.wast` lit-test URLs reviewed in this run. The capture records that on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check did not surface teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0286-2026-04-24-simplify-globals-optimizing-primary-sources-and-starshine-followup.md`, `docs/wiki/binaryen/passes/simplify-globals-optimizing/implementation-structure-and-tests.md`, and `docs/wiki/binaryen/passes/simplify-globals-optimizing/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/cmd/fuzz_harness_wbtest.mbt`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `docs/wiki/binaryen/passes/tracker.md`, and `agent-todo.md`) and making explicit that current Starshine keeps `simplify-globals-optimizing` as a boundary-only, honestly rejected pass with `SGO` backlog slices but no owner file, global fact table, or nested-rerun scheduler yet.
- Refreshed `docs/wiki/binaryen/passes/simplify-globals-optimizing/index.md`, `binaryen-strategy.md`, `linear-traces-read-only-to-write-and-reruns.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so readers can now follow the complete chain from transformed shapes to Binaryen source strategy to Starshine status and future port plan without relying on neighboring plain-`simplify-globals` pages for provenance.

## [2026-04-24] research | add `dae-optimizing` primary-source capture and a Starshine naming/status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/raw/research/`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/dae-optimizing/` folder, then chose `dae-optimizing` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status/port-strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-dae-optimizing-primary-sources.md`, capturing the official Binaryen GitHub release, shared DAE engine source, optimizing helper, supporting helper headers, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, the helper headers, and the dedicated DAE lit roster did not surface a new teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0285-2026-04-24-dae-optimizing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/dae-optimizing/starshine-strategy.md`, grounding the follow-up in exact local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`, `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, and `agent-todo.md`) and making explicit that current Starshine has a boundary-only descriptive registry entry `dead-argument-elimination-optimizing`, not an exact `dae-optimizing` alias.
- Refreshed `docs/wiki/binaryen/passes/dae-optimizing/index.md`, `binaryen-strategy.md`, `signature-updates-and-nested-reruns.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and local status bridge while correcting the stale exact-registry-spelling implication.

## [2026-04-24] research | add `string-lowering` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/string-lowering/` folder, then chose `string-lowering` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine status page.
- Added `docs/wiki/raw/binaryen/2026-04-24-string-lowering-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01 14:31**, while a narrow current-`main` spot check on `StringLowering.cpp` did not surface teaching-relevant contract drift beyond the existing dossier claims.
- Added `docs/wiki/raw/research/0284-2026-04-24-string-lowering-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/string-lowering/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/binary/encode.mbt`, `src/binary/decode.mbt`, `src/binary/tests.mbt`, `src/wast/{keywords,parser,lower_to_lib}.mbt`, `src/validate/typecheck.mbt`, `src/ir/{hot_lift,hot_lower}.mbt`, `docs/wiki/strings/string-const-surface.md`, and `agent-todo.md`) and making explicit that current Starshine has no `string-lowering` registry spelling, owner file, or active backlog slice, even though the repo has real reusable `string.const` infrastructure.
- Refreshed `docs/wiki/binaryen/passes/string-lowering/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `json-and-magic-imports.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and local status bridge instead of leaving `string-lowering` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-24] research | add `de-nan` primary-source capture and a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/de-nan/` folder, then chose `de-nan` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-de-nan-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `DeNaN.cpp`, `pass.cpp`, `properties.h`, `names.h`, and `denan.wast` did not surface a new teaching-relevant contract drift beyond the existing dossier claims.
- Added `docs/wiki/raw/research/0283-2026-04-24-de-nan-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/de-nan/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine preserves the pass only as a removed registry entry with category and request-rejection tests, no owner file, no dedicated backlog slice, and an unresolved future landing question around module-owned instrumentation versus HOT expression rewriting.
- Refreshed `docs/wiki/binaryen/passes/de-nan/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `helper-functions-fallthrough-and-boundaries.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port bridge instead of leaving `de-nan` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-24] research | correct `loop-invariant-code-motion`, add primary-source capture, and file the Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/loop-invariant-code-motion/` folder, and the relevant local status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/0065-2026-03-24-ir2-execution-plan.md`, and `agent-todo.md`), then chose `loop-invariant-code-motion` because the dossier had the required overview / Binaryen strategy / shape coverage but lacked an immutable raw primary-source manifest and dedicated Starshine status page, while a source recheck exposed a teaching-significant stale overread.
- Added `docs/wiki/raw/binaryen/2026-04-24-loop-invariant-code-motion-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `LoopInvariantCodeMotion.cpp`, `pass.cpp`, and `licm.wast` did not surface a new teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0282-2026-04-24-loop-invariant-code-motion-primary-sources-and-source-correction-followup.md` plus `docs/wiki/binaryen/passes/loop-invariant-code-motion/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/0065-2026-03-24-ir2-execution-plan.md`, and `agent-todo.md`) and making explicit that current Starshine preserves the pass only as a removed registry entry with honest request rejection, no owner file, no dedicated backlog slice, and an unresolved future proof question around a `LazyLocalGraph`-equivalent local-dependency analysis.
- Refreshed `docs/wiki/binaryen/passes/loop-invariant-code-motion/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `effects-loops-and-hoisting-rules.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, correcting the older false temp-local-hoisting story: reviewed Binaryen `version_129` LICM moves eligible none-typed unconditional-loop-entry statements as whole statements, replaces old loop slots with `nop`, and emits a pre-loop block, rather than synthesizing fresh helper locals for arbitrary invariant value expressions.

## [2026-04-24] health | repair stale `simplify-locals-nonesting` links in the pass-folder catalog

- Ran a touched-area markdown link check after the `avoid-reinterprets` wiki-development commit and found four pre-existing stale relative links in `docs/wiki/binaryen/passes/index.md` that incorrectly included the `binaryen/passes/` prefix while already living inside that directory.
- Repaired the `simplify-locals-nonesting` Binaryen strategy, implementation/test map, flatness-boundary, and WAT-shape catalog links to use folder-local paths, restoring link validity without changing the page descriptions or pass semantics.
- Re-ran the focused link check for `docs/wiki/binaryen/passes/index.md` and the touched `avoid-reinterprets` pages; no local markdown-link failures remained in that checked set.

## [2026-04-24] research | add `avoid-reinterprets` primary-source capture and a Starshine status bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/avoid-reinterprets/` folder, then chose `avoid-reinterprets` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-24-avoid-reinterprets-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-24 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `AvoidReinterprets.cpp`, `pass.cpp`, `local-graph.h`, `properties.h`, and the dedicated `avoid-reinterprets*.wast` lit files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0281-2026-04-24-avoid-reinterprets-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/avoid-reinterprets/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, `src/ir/hot_builders.mbt`, `src/ir/hot_mutate.mbt`, `src/ir/use_def.mbt`, `src/ir/ssa_local.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`) and making explicit that current Starshine preserves the pass only as a removed registry entry with honest request rejection, no owner file, no dedicated backlog slice, and an unresolved future proof question around LocalGraph-equivalent single-load provenance.
- Refreshed the existing `avoid-reinterprets` landing, Binaryen strategy, implementation/test map, proof-boundary, and WAT-shape pages, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and local status bridge instead of leaving the folder as an upstream-only dossier with no exact Starshine follow-along path.

## [2026-04-23] health | refresh remaining `string-gathering` support pages to cite the new primary-source manifest and status page

- Ran a focused touched-area health check after the main `string-gathering` wiki-development commit, specifically looking for support pages inside the folder that still cited only the older research note and did not yet point readers at the new raw primary-source manifest or the new Starshine status/code-map bridge.
- Refreshed `docs/wiki/binaryen/passes/string-gathering/reuse-naming-and-ordering.md` and `docs/wiki/binaryen/passes/string-gathering/wat-shapes.md` so they now cite the committed 2026-04-23 raw manifest, point readers at `starshine-strategy.md`, and no longer look like pre-provenance holdouts inside an otherwise refreshed folder.
- Left older `string-gathering` history untouched because this log is append-only except for obvious formatting fixes, and this health pass only needed forward-looking touched-area cleanup.

## [2026-04-23] research | add `string-gathering` primary-source capture and a Starshine status/code-map bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/string-gathering/` folder, then chose `string-gathering` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that tied the mature upstream story back to the exact current local code and backlog surfaces.
- Added `docs/wiki/raw/binaryen/2026-04-23-string-gathering-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `StringLowering.cpp` did not surface a new teaching-relevant contract drift beyond the dossier's refreshed claims.
- Added `docs/wiki/raw/research/0280-2026-04-23-string-gathering-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/string-gathering/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/binary/{encode,decode}.mbt`, `src/binary/tests.mbt`, `src/wast/lower_to_lib.mbt`, `docs/wiki/strings/string-const-surface.md`, `docs/wiki/raw/research/0066-2026-03-24-binaryen-no-dwarf-default-optimize-path.md`, and `agent-todo.md`) and making explicit that current Starshine already has real string-literal plumbing and active backlog slices for the pass but still lacks both the transform itself and even the public pass-registry spelling.
- Refreshed `docs/wiki/binaryen/passes/string-gathering/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the exact local code-map bridge instead of leaving `string-gathering` split between upstream-only pass pages, literal-plumbing docs, backlog slices, and an isolated registry-bookkeeping caveat.

## [2026-04-23] health | refresh remaining `untee` support pages to cite the new primary-source manifest

- Ran a focused touched-area health check after the main `untee` wiki-development commit, specifically looking for stale provenance, missing backlinks, and folder-internal inconsistencies across the remaining support pages.
- Refreshed `docs/wiki/binaryen/passes/untee/flattening-code-pushing-and-tee-boundaries.md` and `docs/wiki/binaryen/passes/untee/wat-shapes.md` so they now cite the committed raw primary-source manifest, point readers at the new `starshine-strategy.md` page, and no longer look like pre-manifest holdouts inside an otherwise refreshed folder.
- Left older duplicated historical `docs/wiki/log.md` `untee` entries untouched because the log is append-only except for obvious formatting fixes, and this run's health pass did not justify rewriting prior audit history.

## [2026-04-23] research | add `untee` primary-source capture and a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/untee/` folder, then chose `untee` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-23-untee-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Untee.cpp`, `pass.cpp`, `passes.h`, `SimplifyLocals.cpp`, and `untee.wast` did not surface a new teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0279-2026-04-23-untee-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/untee/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `src/passes/simplify_locals.mbt`, `src/passes/pass_manager_wbtest.mbt`, and `agent-todo.md`) and making explicit that current Starshine intentionally still has no `untee` transform yet but does preserve a real removed-name registry entry, active request guard, no dedicated backlog slice, and a practical future landing zone beside rather than inside the current `simplify-locals` family.
- Refreshed `docs/wiki/binaryen/passes/untee/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port bridge instead of leaving the local side scattered across registry and neighboring locals docs.

## [2026-04-23] research | add `dataflow-optimization` primary-source capture and a Starshine status/planning bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/dataflow-optimization/` folder, then chose `dataflow-optimization` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page, while the current local planning story was also still scattered across registry, batch, and execution-plan docs.
- Added `docs/wiki/raw/binaryen/2026-04-23-dataflow-optimization-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and combo lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `DataFlowOpts.cpp`, `pass.cpp`, `src/dataflow/{graph,node,users,utils}.h`, `src/ir/flat.h`, and `flatten_simplify-locals-nonesting_dfo_O3.wast` did not surface a new teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0278-2026-04-23-dataflow-optimization-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/dataflow-optimization/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/0065-2026-03-24-ir2-execution-plan.md`, `src/passes/precompute.mbt`, `src/passes/simplify_locals.mbt`, and `agent-todo.md`) and making explicit that current Starshine preserves the pass name only as a removed registry entry with honest request rejection, no active implementation, no dedicated backlog slice, and a real planning gap between the older Batch 1 map and the newer preferred execution order.
- Refreshed `docs/wiki/binaryen/passes/dataflow-optimization/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the exact local status/planning bridge instead of leaving the local side scattered across registry, batch, and neighboring pass docs.

## [2026-04-23] research | add `type-ssa` primary-source capture and a Starshine status/non-adoption bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/type-ssa/` folder, then chose `type-ssa` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page.
- Added `docs/wiki/raw/binaryen/2026-04-23-type-ssa-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `TypeSSA.cpp`, `pass.cpp`, `ReFinalize.cpp`, and `type-ssa.wast` did not surface a new teaching-relevant contract drift beyond the dossier's updated claims.
- Added `docs/wiki/raw/research/0277-2026-04-23-type-ssa-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/type-ssa/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `ssa`, `type-merging`, `type-refining`, and `type-generalizing` dossiers) and making explicit that current Starshine intentionally still has no `type-ssa` transform, no registry compatibility name, no no-DWARF parity role, and no dedicated backlog slice today.
- Refreshed `docs/wiki/binaryen/passes/type-ssa/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/non-adoption bridge instead of leaving `type-ssa` as an upstream-only folder with no exact Starshine follow-along path.

## [2026-04-23] research | add `const-hoisting` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/const-hoisting/` folder, then chose `const-hoisting` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the exact current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-23-const-hoisting-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `ConstHoisting.cpp`, `literal.h`, `pass.cpp`, `insert_ordered.h`, `wasm-binary.h`, `wasm-builder.h`, and `const-hoisting.wast` did not surface a new teaching-relevant contract drift beyond the refreshed dossier's claims.
- Added `docs/wiki/raw/research/0276-2026-04-23-const-hoisting-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/const-hoisting/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `precompute` / `optimize-added-constants` / `merge-similar-functions` dossiers) and making explicit that current Starshine intentionally has no `const-hoisting` transform yet but does preserve a real removed-name registry entry, active request rejection, a small-HOT-pass future landing shape, and one important planning gap that the older folder left scattered: the repo still has no dedicated `const-hoisting` backlog slice today.
- Refreshed `docs/wiki/binaryen/passes/const-hoisting/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving `const-hoisting` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] research | add `simplify-globals` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/simplify-globals/` folder, then chose plain `simplify-globals` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the exact current local status and planning gap.
- Added `docs/wiki/raw/binaryen/2026-04-23-simplify-globals-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `SimplifyGlobals.cpp`, `pass.cpp`, and the key `simplify-globals-read_only_to_write` plus `propagate-globals-globally` tests did not surface a new teaching-relevant contract drift beyond the dossier's updated claims.
- Added `docs/wiki/raw/research/0275-2026-04-23-simplify-globals-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/simplify-globals/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `simplify-globals-optimizing` / `propagate-globals-globally` dossiers) and making explicit that current Starshine intentionally still has no plain `simplify-globals` transform yet but does keep a real boundary-only registry entry, active request guard, clear shared-engine neighborhood, and one important planning gap that the older folder left scattered: the repo still has no dedicated plain-`simplify-globals` backlog slice today.
- Refreshed `docs/wiki/binaryen/passes/simplify-globals/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving plain `simplify-globals` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] research | add `inlining` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/inlining/` folder, then chose plain `inlining` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the exact current local status and planning gap.
- Added `docs/wiki/raw/binaryen/2026-04-23-inlining-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Inlining.cpp`, `pass.cpp`, `NoInline.cpp`, `opt-utils.h`, `pass.h`, `wasm.h`, `wasm.cpp`, `wasm-binary.cpp`, `contexts.h`, `module-utils.cpp`, and the dedicated `inlining*` / `no-inline*` / `inline-main` / inline-hint lit roster did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0274-2026-04-23-inlining-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/inlining/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `inlining-optimizing` / `inline-main` / `duplicate-function-elimination` / `monomorphize` dossiers) and making explicit that current Starshine intentionally still has no plain `inlining` transform yet but does keep a real boundary-only registry entry, active request guard, clear shared-engine neighborhood, and one important planning gap that the older folder left scattered: the repo still has no dedicated plain-`inlining` backlog slice today.
- Refreshed `docs/wiki/binaryen/passes/inlining/index.md` and `binaryen-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving plain `inlining` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] research | add `alignment-lowering` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/alignment-lowering/` folder, and the relevant local planning/code surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `dealign` / `avoid-reinterprets` / `i64-to-i32-lowering` dossiers), then chose `alignment-lowering` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and still-open landing-zone question.
- Added `docs/wiki/raw/binaryen/2026-04-23-alignment-lowering-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `AlignmentLowering.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `bits.h`, and the dedicated `alignment-lowering.wast` file did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0273-2026-04-23-alignment-lowering-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/alignment-lowering/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `dealign` / `avoid-reinterprets` / `i64-to-i32-lowering` dossiers) and making explicit that current Starshine intentionally still has no `alignment-lowering` transform yet but does preserve a real boundary-only registry entry, active request guard, broader planning-bucket presence, and one important open question that the earlier folder left scattered: the repo still has no dedicated backlog slice or chosen landing zone for a future parity port.
- Refreshed `docs/wiki/binaryen/passes/alignment-lowering/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `chunk-selection-and-unreachable-semantics.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving `alignment-lowering` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] research | add `merge-locals` primary-source capture, repair the stale Binaryen story, and add a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/merge-locals/` folder, then chose `merge-locals` because the folder still lacked the required immutable raw primary-source manifest and dedicated Starshine strategy page, while a 2026-04-23 source recheck also exposed a teaching-significant stale overread in the living Binaryen pages.
- Added `docs/wiki/raw/binaryen/2026-04-23-merge-locals-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `MergeLocals.cpp`, `pass.cpp`, `pass.h`, `local-graph.h`, `LocalStructuralDominance.h`, `pass-utils.h`, and `merge-locals.wast` did not surface a new teaching-relevant contract drift beyond the refreshed dossier claims.
- Added `docs/wiki/raw/research/0272-2026-04-23-merge-locals-primary-sources-and-source-correction-followup.md` plus `docs/wiki/binaryen/passes/merge-locals/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and the neighboring `optimize-casts` / `local-subtyping` / `coalesce-locals` dossiers) and making explicit that current Starshine intentionally still has no `merge-locals` transform yet but does keep a real removed-name registry entry, explicit rejection path, historical batch intent, and practical local-cleanup landing zone.
- Refreshed `docs/wiki/binaryen/passes/merge-locals/index.md`, `binaryen-strategy.md`, `local-graph-and-copy-influences.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the local status/port-planning bridge while also correcting the older Binaryen overread: reviewed `version_129` invalidates DWARF, starts from simple root sets plus equivalent copy wrappers, chooses one existing winner local, and does not match the stale one-set/fresh-temp/local-name-bailout story.

## [2026-04-23] research | add `inlining-optimizing` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/inlining-optimizing/` folder, then chose `inlining-optimizing` because the dossier already had the required overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page, while the older 2026-04-20 living wording also still overread reviewed `version_129` by teaching some `call_ref` sites as chosen inline-planner inputs instead of keeping the stricter direct-call-based planner surface explicit.
- Added `docs/wiki/raw/binaryen/2026-04-23-inlining-optimizing-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Inlining.cpp`, `pass.cpp`, `opt-utils.h`, `pass.h`, `NoInline.cpp`, `module-utils.cpp`, and the dedicated `inlining*` / `no-inline*` / `inline-main` lit roster did not surface a new teaching-relevant contract drift beyond the refreshed dossier's claims.
- Added `docs/wiki/raw/research/0271-2026-04-23-inlining-optimizing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/inlining-optimizing/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `dae-optimizing` / `precompute-propagate` / `duplicate-function-elimination` / plain `inlining` dossiers) and making explicit that current Starshine intentionally has no `inlining-optimizing` transform yet but does have a real boundary-only registry entry, active request guard, backlog slice `INL`, canonical late-tail slot, shared post-inlining rerun dependency story, and a clear reason this remains boundary/module work rather than a HOT peephole.
- Refreshed `docs/wiki/binaryen/passes/inlining-optimizing/index.md`, `binaryen-strategy.md`, `planning-partial-inlining-and-reruns.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and clean local status/port-planning bridge while also keeping the reviewed direct-call chosen-action surface explicit instead of silently preserving the older broader `call_ref` planner reading.

## [2026-04-23] research | add `reorder-globals` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/reorder-globals/` folder, and the relevant local planning/code surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `string-gathering` / `reorder-globals-always` / `directize` dossiers), then chose `reorder-globals` because the folder already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-23-reorder-globals-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `wasm-traversal.h`, `topological_sort.h`, `wasm.h`, `GlobalStructInference.cpp`, and the dedicated `reorder-globals{,-real}.wast` pair did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0270-2026-04-23-reorder-globals-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/reorder-globals/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `string-gathering` / `reorder-globals-always` / `directize` dossiers) and making explicit that current Starshine intentionally has no `reorder-globals` transform yet but does have a real boundary-only registry entry, active request guard, backlog slice `RG`, canonical late-tail slot, and one important local implementation shape to keep centered: this is a reindexing-and-declaration-remap module pass, not a HOT peephole and not just a `string-gathering` fixup.
- Refreshed `docs/wiki/binaryen/passes/reorder-globals/index.md`, `binaryen-strategy.md`, `size-model-and-dependency-order.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving `reorder-globals` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] research | add `duplicate-import-elimination` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/duplicate-import-elimination/` folder, then chose `duplicate-import-elimination` because the dossier already had the required overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-23-duplicate-import-elimination-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `DuplicateImportElimination.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, `import-utils.h`, and the dedicated test pair did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0269-2026-04-23-duplicate-import-elimination-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/duplicate-import-elimination/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `duplicate-function-elimination` / `simplify-globals-optimizing` / `remove-unused-module-elements` dossiers) and making explicit that current Starshine intentionally has no `duplicate-import-elimination` transform yet but does have a real boundary-only registry entry, active request guard, backlog slice `DIE`, canonical late-tail slot, and one important local planning mismatch to keep honest: the current backlog wording is broader than the reviewed Binaryen `version_129` function-import-only contract.
- Refreshed `docs/wiki/binaryen/passes/duplicate-import-elimination/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and clean local status/port-planning bridge instead of leaving `duplicate-import-elimination` as corrected-upstream-only research with no exact Starshine follow-along path.

## [2026-04-23] docs | compact `AGENTS.md`, split `docs/README.md` into high-signal rules plus reference details, and refresh schema catalog wording

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, and this log, then treated the task as a repo-operations schema cleanup rather than a code or pass change.
- Rewrote `AGENTS.md` into a shorter operational contract centered on repo essentials, always-follow workflow rules, and task-specific guidance, removing low-frequency inventories from the top-level contract while keeping the behavior-shaping constraints intact.
- Reorganized `docs/README.md` so the mirrored repo rules now live under a compact `High-Signal Repo Rules` section that is easier to reuse in agent runtime context, while lower-frequency package/script/workflow inventories moved into a separate `Reference Details` section instead of competing with the operational rules.
- Updated `docs/wiki/index.md` so the schema catalog descriptions for `AGENTS.md` and `docs/README.md` match the new compact-versus-reference split.

## [2026-04-23] research | retire the rooted slot-43 `precompute` continuation blocker and file the HOT-lower follow-up

- Re-read `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, the existing `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` and `docs/wiki/binaryen/passes/precompute/` pages, the rooted artifact tree under `.tmp/o4z-post-5d2fd48/current-chain/`, and the touched lowerer/tests in `src/ir/hot_lower.mbt` plus `src/ir/hot_lower_test.mbt`, then treated this thread as verification/closure work rather than reopening the already-retired slot-42 or early slot-19 stories.
- Added `docs/wiki/raw/research/0268-2026-04-23-generated-o4z-precompute-slot43-retired-by-hot-lower-prefix-label-guard.md`, recording that the remaining rooted slot-43 blocker was not a new `precompute` algorithm bug but a HOT-lower wrapped carried-local / branch-depth corruption in `hot_lower_impl_stackify_wrapped_struct_set_prefixes(...)`, specifically the missing guard for doubly nested child exits that still target the carried-prefix block's own label even when the source local is not rewritten.
- Revalidated the fixed rooted witnesses and continued-chain outputs with `wasm-tools validate`, confirming that extracted `func 2754` remains green, extracted `func 3867` is now green, the fixed slot-43 continued replay validates, and downstream implemented slots `44`, `45`, `47`, `50`, and `53` also validate successfully from the same rooted continuation chain.
- Refreshed `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/precompute/index.md`, and `docs/wiki/binaryen/passes/precompute/starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md` and `docs/wiki/index.md`, so the living wiki now points readers directly to the new rooted-continuation retirement note and keeps the durable ownership story honest: the later slot-43 blocker was retired by neighboring HOT-lower carried-prefix label guarding, and no new live implemented-slot blocker was exposed through the continued chain's green slot-53 endpoint.

## [2026-04-23] research | add `flatten` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, the existing `docs/wiki/binaryen/passes/flatten/` folder, and the relevant local planning/code surfaces (`src/passes/optimize.mbt`, `src/cli/cli_test.mbt`, `docs/0065-2026-03-24-ir2-execution-plan.md`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `agent-todo.md`), then chose `flatten` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page, while the landing page also still carried stale campaign wording from the older wiki-status-`none` phase.
- Added `docs/wiki/raw/binaryen/2026-04-23-flatten-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-23 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Flatten.cpp`, `flat.h`, `pass.cpp`, and the dedicated `flatten*` lit files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0267-2026-04-23-flatten-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/flatten/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/cli/cli_test.mbt`, `docs/0065-2026-03-24-ir2-execution-plan.md`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, `agent-todo.md`, and the neighboring `simplify-locals-notee-nostructure` / `local-cse` / `rereloop` / `i64-to-i32-lowering` / `simplify-locals-nonesting` / `souperify` dossiers) and making explicit that current Starshine intentionally has no `flatten` transform yet but does have a real removed-name registry entry, preserved `--flatten` CLI spelling, Batch 2 planning story, and one important planning gap to keep honest: the active backlog still has no dedicated `flatten` slice today.
- Refreshed `docs/wiki/binaryen/passes/flatten/index.md`, `binaryen-strategy.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and the clean local status/port-planning bridge instead of leaving the local side scattered across older planning docs or stale campaign phrasing.

## [2026-04-22] research | add `directize` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/directize/` folder, then chose `directize` because the dossier already had the required upstream overview / Binaryen strategy / implementation map / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-directize-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Directize.cpp`, `pass.cpp`, `call-utils.h`, `table-utils.{h,cpp}`, and the dedicated `directize*` lit files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0265-2026-04-22-directize-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/directize/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `duplicate-import-elimination` / `simplify-globals-optimizing` / `remove-unused-module-elements` / `string-gathering` / `reorder-globals` dossiers) and making explicit that current Starshine intentionally has no `directize` transform yet but does have a real boundary-only registry entry, request guard, backlog slice `DIR`, canonical late-tail slot, and practical late-boundary landing zone that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/directize/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `table-info-and-immutability.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and clean local status/port-planning bridge instead of leaving `directize` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-22] research | add `coalesce-locals` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/coalesce-locals/` folder, then chose `coalesce-locals` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-coalesce-locals-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `CoalesceLocals.cpp`, `pass.cpp`, `opt-utils.h`, and `coalesce-locals.wast` did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0264-2026-04-22-coalesce-locals-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/coalesce-locals/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `src/passes/reorder_locals.mbt`, `src/passes/reorder_locals_test.mbt`, `src/passes/simplify_locals.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `local-subtyping` / `local-cse` / `reorder-locals` / `simplify-locals` dossiers) and making explicit that current Starshine intentionally has no `coalesce-locals` transform yet but does have a real removed-name registry entry, backlog slice `CL`, honest dual-slot scheduler story, preset-honesty proof, and practical MoonBit landing zone that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/coalesce-locals/index.md`, `binaryen-strategy.md`, `interference-and-ordering.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `coalesce-locals` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-22] research | add `simplify-locals-nostructure` primary-source capture and a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/simplify-locals-nostructure/` folder, then chose `simplify-locals-nostructure` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-simplify-locals-nostructure-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, and the dedicated no-structure test files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0263-2026-04-22-simplify-locals-nostructure-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/simplify-locals-nostructure/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `src/passes/simplify_locals.mbt`, `src/passes/reorder_locals.mbt`, `src/passes/pass_manager_wbtest.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that current Starshine intentionally has no `simplify-locals-nostructure` transform yet but does have a real removed-name registry entry, tuple exact-slot blocker helper plus regression, backlog slice `SLNS`, scheduler story, and practical MoonBit landing zone that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/simplify-locals-nostructure/index.md`, `binaryen-strategy.md`, `variant-surface.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and clean local status/port-planning bridge instead of leaving `simplify-locals-nostructure` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-22] research | add `local-cse` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/local-cse/` folder, then chose `local-cse` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-local-cse-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `LocalCSE.cpp`, `pass.cpp`, `opt-utils.h`, and `local-cse.wast` did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0262-2026-04-22-local-cse-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/local-cse/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `src/passes/simplify_locals.mbt`, `src/passes/reorder_locals.mbt`, `src/passes/pass_manager_wbtest.mbt`, `src/cmd/cmd_wbtest.mbt`, and the neighboring `flatten` / `simplify-locals-notee-nostructure` / `coalesce-locals` / `simplify-locals` / `reorder-locals` dossiers) and making explicit that current Starshine intentionally has no `local-cse` transform yet but does have a real removed-name registry entry, backlog slice, honest blocked scheduler story, and concrete local MoonBit landing zone that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/local-cse/index.md`, `binaryen-strategy.md`, `basic-block-windows-and-barriers.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `local-cse` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-22] research | add `local-subtyping` primary-source capture, repair the Binaryen source story, and add a Starshine status/port bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/local-subtyping/` folder, then chose `local-subtyping` because the folder already had the required overview / Binaryen strategy / shape pages but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page, while a source recheck also uncovered a teaching-significant overread in the existing Binaryen explanation.
- Added `docs/wiki/raw/binaryen/2026-04-22-local-subtyping-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, and the reviewed pass source corrected the touched dossier's key local overstatement by confirming that `local-subtyping` is a declaration-seeded, set-driven LUB pass with direct declaration/get/tee retagging and dominance-gated non-nullability, not the older `local.get` / `ref.as_non_null` plus `LocalUpdater` story.
- Added `docs/wiki/raw/research/0261-2026-04-22-local-subtyping-source-correction-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/local-subtyping/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `optimize-casts` / `coalesce-locals` / `local-cse` / `reorder-locals` dossiers) and making explicit that current Starshine still has no local transform but does have a real removed-name registry entry, backlog slice, scheduler story, and one important scope mismatch to keep honest: the current `LS` backlog wording is broader than the reviewed upstream `version_129` contract.
- Refreshed `docs/wiki/binaryen/passes/local-subtyping/index.md`, `binaryen-strategy.md`, `lubs-and-dominance.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and exact local status/port-planning bridge while also keeping the source correction explicit instead of silently preserving the older over-broad Binaryen summary.

## [2026-04-22] research | add `optimize-casts` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/optimize-casts/` folder, then chose `optimize-casts` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-optimize-casts-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `OptimizeCasts.cpp`, `pass.cpp`, `opt-utils.h`, and `optimize-casts.wast` did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0260-2026-04-22-optimize-casts-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/optimize-casts/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `heap2local` / `local-subtyping` / `coalesce-locals` / `local-cse` dossiers) and making explicit that current Starshine intentionally has no `optimize-casts` transform yet but does have a real removed-name registry entry, backlog slice, scheduler story, and one important scope contradiction to keep honest: Binaryen `version_129` handles `ref.cast` plus `ref.as_non_null`, while the local backlog still mentions `ref.test`.
- Refreshed `docs/wiki/binaryen/passes/optimize-casts/index.md`, `binaryen-strategy.md`, `two-phase-dataflow.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `optimize-casts` as upstream-only research with no exact Starshine follow-along path.

## [2026-04-22] research | add `rse` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/rse/` folder, then chose `rse` because the dossier already had the required upstream overview / Binaryen strategy / transformed-shape coverage but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-rse-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `RedundantSetElimination.cpp`, `pass.cpp`, `opt-utils.h`, and the dedicated `rse*` test files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0259-2026-04-22-rse-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/rse/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `simplify-locals` / `heap-store-optimization` / `merge-blocks` / `vacuum` dossiers) and making explicit that current Starshine intentionally has no `rse` transform yet but does have a real removed-name registry entry, backlog slice, scheduler story, and neighboring implementation map that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/rse/index.md`, `binaryen-strategy.md`, `cfg-and-value-tracking.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `rse` as an upstream-only research island with no exact Starshine follow-along path.

## [2026-04-22] research | add `code-pushing` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/code-pushing/` folder, then chose `code-pushing` because the dossier already had the required upstream overview / strategy / barrier / shape pages but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-code-pushing-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `CodePushing.cpp`, `pass.cpp`, `opt-utils.h`, and the dedicated `code-pushing*` lit files did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0258-2026-04-22-code-pushing-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/code-pushing/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `precompute` / `tuple-optimization` / `simplify-locals-nostructure` dossiers) and making explicit that current Starshine intentionally has no `code-pushing` transform yet but does have a real removed-name registry, tuple exact-slot gate, backlog slice, and scheduler story that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/code-pushing/index.md`, `binaryen-strategy.md`, `segment-selection-and-barriers.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `code-pushing` as an upstream-only research island with no exact Starshine follow-along path.

## [2026-04-22] research | add `code-folding` primary-source capture and a Starshine status/port-strategy bridge

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/code-folding/` folder, then chose `code-folding` because the dossier already had the required upstream overview / strategy / terminating-tail / shape pages but still lacked an immutable raw primary-source manifest and the required dedicated Starshine strategy page that mapped the current local status and future landing zone.
- Added `docs/wiki/raw/binaryen/2026-04-22-code-folding-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `CodeFolding.cpp`, `pass.cpp`, `opt-utils.h`, and `code-folding.wast` did not surface a new teaching-relevant contract drift beyond the dossier's existing Binaryen claims.
- Added `docs/wiki/raw/research/0257-2026-04-22-code-folding-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/code-folding/starshine-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-repo Starshine status surfaces (`src/passes/optimize.mbt`, `src/cli/cli_test.mbt`, `agent-todo.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and the neighboring `merge-blocks` / `remove-unused-brs` / `remove-unused-names` / `rse` dossiers) and making explicit that current Starshine intentionally has no `code-folding` transform yet but does have a real removed-name registry, CLI, backlog, and scheduler story that future port work can follow.
- Refreshed `docs/wiki/binaryen/passes/code-folding/index.md`, `binaryen-strategy.md`, `terminating-tails.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and a clean local status/port-planning bridge instead of leaving `code-folding` as an upstream-only research island with no exact Starshine follow-along path.

## [2026-04-22] research | add `once-reduction` primary-source capture and refresh provenance across the full dossier

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/once-reduction/` folder, then chose `once-reduction` because the dossier already had the required living overview / Binaryen strategy / shape / Starshine strategy / parity pages but still lacked an immutable raw primary-source manifest and still spread its freshness story across older notes instead of one reviewed official release/source/test capture.
- Added `docs/wiki/raw/binaryen/2026-04-22-once-reduction-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `OnceReduction.cpp` and `once-reduction.wast` did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0256-2026-04-22-once-reduction-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/once_reduction.mbt`, `src/passes/once_reduction_test.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining work was provenance and freshness alignment, not a missing pass-coverage structure.
- Refreshed `docs/wiki/binaryen/passes/once-reduction/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `dominance-propagation-and-cycle-safety.md`, `wat-shapes.md`, `starshine-hot-ir-strategy.md`, and `parity.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest and keeps the reviewed release-page provenance explicit instead of leaving the dossier's freshness story scattered across older local notes.

## [2026-04-22] research | add `merge-blocks` primary-source capture and repair the stale Starshine status story

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/merge-blocks/` folder, then chose `merge-blocks` because the folder still lacked the required Starshine strategy/code-map page and immutable raw primary-source manifest, while several catalogs were now stale enough to keep describing the pass as unimplemented even though the in-tree registry, presets, tests, and CLI surface already ship it as an active hot pass.
- Added `docs/wiki/raw/binaryen/2026-04-22-merge-blocks-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `MergeBlocks.cpp`, `pass.cpp`, and `merge-blocks.wast` did not surface a new teaching-relevant Binaryen contract drift beyond the dossier's existing upstream claims.
- Added `docs/wiki/raw/research/0255-2026-04-22-merge-blocks-primary-sources-and-starshine-followup.md` plus `docs/wiki/binaryen/passes/merge-blocks/starshine-hot-ir-strategy.md`, grounding the follow-up in the new raw-source capture and the exact in-tree MoonBit implementation / proof surface (`src/passes/merge_blocks.mbt`, `src/passes/merge_blocks_test.mbt`, `src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `src/passes/registry_test.mbt`, `src/cmd/cmd_wbtest.mbt`, and `src/ir/hot_lower_test.mbt`) and making explicit that current Starshine is a HOT-region-root flattening pass with label-use gating and typed-carrier guards rather than a direct AST-level `ProblemFinder` port.
- Refreshed `docs/wiki/binaryen/passes/merge-blocks/index.md`, `binaryen-strategy.md`, and `wat-shapes.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the new raw primary-source manifest and exact MoonBit code map and no longer falsely files `merge-blocks` under the unimplemented parity queue.

## [2026-04-22] research | add `tuple-optimization` primary-source capture and refresh tuple provenance anchors

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/tuple-optimization/` folder, then chose `tuple-optimization` because the dossier already had the required living overview / Binaryen strategy / shape / Starshine strategy pages but still lacked an immutable raw primary-source manifest and a compact 2026-04-22 provenance anchor tying the reviewed official Binaryen release surface directly to the current Starshine code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-tuple-optimization-primary-sources.md`, capturing the official Binaryen GitHub release, source, and dedicated lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `TupleOptimization.cpp`, the relevant `pass.cpp` / `OptimizeInstructions.cpp` tuple surfaces, and the dedicated `tuple-optimization.wast` file did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0254-2026-04-22-tuple-optimization-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/tuple_optimization.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/tuple_optimization_wbtest.mbt`, `src/passes/registry_test.mbt`, `src/cmd/cmd_wbtest.mbt`, and `src/cmd/cmd_native_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus read-along navigation, not a missing overview / strategy / shapes / Starshine page.
- Refreshed `docs/wiki/binaryen/passes/tuple-optimization/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest, keeps the reviewed release-page provenance explicit, and no longer leaves the folder's latest freshness anchor implicit.

## [2026-04-22] research | add `reorder-locals` primary-source capture and Binaryen owner/test-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/reorder-locals/` folder, then chose `reorder-locals` because the dossier already covered the core rule, shapes, and local Starshine module-pass adaptation well but still lacked an immutable raw primary-source manifest and a compact Binaryen owner/test-map page that tied the tiny upstream implementation directly to the broader roundtrip and parity notes.
- Added `docs/wiki/raw/binaryen/2026-04-22-reorder-locals-primary-sources.md`, capturing the official Binaryen GitHub release, source, supporting boundary-file, and dedicated test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `ReorderLocals.cpp`, `pass.cpp`, and the dedicated pass tests did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0253-2026-04-22-reorder-locals-primary-sources-and-code-map-followup.md` plus `docs/wiki/binaryen/passes/reorder-locals/implementation-structure-and-tests.md`, grounding the follow-up in the new raw-source capture and the in-repo MoonBit implementation (`src/passes/reorder_locals.mbt`, `src/passes/reorder_locals_test.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus a compact Binaryen owner/proof map, not missing overview, shape, or Starshine strategy pages.
- Refreshed `docs/wiki/binaryen/passes/reorder-locals/index.md`, `binaryen-strategy.md`, `names-roundtrip-and-porting.md`, `starshine-hot-ir-strategy.md`, and `parity.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest, the exact tiny `ReorderLocals.cpp` owner surface, the dedicated print-roundtrip test role, and the exact MoonBit module-pass code map instead of leaving those bridges implicit.

## [2026-04-22] research | add `memory-packing` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/memory-packing/` folder, then chose `memory-packing` because the dossier already covered upstream strategy and transformed shapes well but still lacked an immutable raw primary-source manifest and the local Starshine page still stopped at a truthful high-level subset summary instead of pointing readers to the exact MoonBit legality / range / dispatch / test code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-memory-packing-primary-sources.md`, capturing the official Binaryen GitHub release, source, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `MemoryPacking.cpp` and `pass.cpp` did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0252-2026-04-22-memory-packing-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/memory_packing.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/memory_packing_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy or transformed-shape coverage.
- Refreshed `docs/wiki/binaryen/passes/memory-packing/index.md`, `implementation-structure-and-tests.md`, and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the new raw primary-source manifest and the exact MoonBit legality/range/dispatch/test code map instead of leaving the Starshine side at only high-level local-gap framing.

## [2026-04-22] lint | align `memory-packing` freshness metadata with the new source capture

- During the touched-area health check after the `memory-packing` primary-source/code-map commit, re-scanned the folder and found that `binaryen-strategy.md` and `parity.md` still carried older `2026-04-20` freshness wording even though this run had just added a new raw primary-source manifest and re-checked the same release/source surfaces.
- Updated `docs/wiki/binaryen/passes/memory-packing/binaryen-strategy.md` and `parity.md` so the folder now cites the new raw primary-source capture directly, records the reviewed official `version_129` release-page date on **2026-04-22**, and keeps the touched `memory-packing` freshness metadata internally consistent instead of mixing the new provenance with older local wording.

## [2026-04-22] research | add `precompute` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/precompute/` folder, then chose `precompute` because the dossier was already strong on upstream strategy and transformed-shape teaching but still lacked an immutable raw primary-source manifest and the local Starshine page still stopped at a truthful high-level subset summary instead of pointing readers to the exact MoonBit descriptor / fold-helper / cleanup / writeback-guard / preset / test code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-precompute-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper-header, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Precompute.cpp` and `pass.cpp` did not surface a new teaching-relevant contract drift beyond the dossier's already-recorded newer-than-`version_129` notes.
- Added `docs/wiki/raw/research/0251-2026-04-22-precompute-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/precompute.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/precompute_test.mbt`, `src/passes/optimize_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy or transformed-shape coverage.
- Refreshed `docs/wiki/binaryen/passes/precompute/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest, the exact MoonBit descriptor/fold-helper/cleanup/writeback-guard/test code map, and the reviewed release-page provenance instead of leaving the Starshine side at only a high-level subset summary.

## [2026-04-22] research | add `dead-code-elimination` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/dead-code-elimination/` folder, then chose `dead-code-elimination` because the dossier already had the required living pass pages and the important upstream strategy correction, but it still lacked an immutable raw primary-source manifest and the local Starshine page still stopped at a high-level HOT description instead of giving readers an exact MoonBit owner-file / raw-skip / writeback-guard / test-lane code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-dead-code-elimination-primary-sources.md`, capturing the official Binaryen GitHub release, source, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `DeadCodeElimination.cpp`, `pass.cpp`, and representative ordinary / EH tests did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0250-2026-04-22-dead-code-elimination-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/dead_code_elimination.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/dead_code_elimination_test.mbt`, `src/passes/dead_code_elimination_live_repro_test.mbt`, `src/passes/perf_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy or transformed-shape coverage.
- Refreshed `docs/wiki/binaryen/passes/dead-code-elimination/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `typed-control-voidification-and-eh.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the raw primary-source manifest, the exact MoonBit owner-file / raw-skip / writeback-guard / focused-test code map, and the reviewed release-page provenance instead of leaving the Starshine side at only high-level HOT framing.

## [2026-04-22] research | add `vacuum` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/vacuum/` folder, then chose `vacuum` because the dossier was already strong on upstream strategy and test-map coverage but still lacked an immutable raw primary-source manifest and the local Starshine page still stopped at a truthful high-level subset summary instead of pointing readers to the exact MoonBit registry / dispatch / helper / validation-guard / trace / perf / CLI replay code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-vacuum-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper-header, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on `Vacuum.cpp`, `pass.cpp`, and representative function/TNH tests did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0249-2026-04-22-vacuum-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation and proof surface (`src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/optimize_test.mbt`, `src/passes/trace_golden_test.mbt`, `src/passes/perf_test.mbt`, `src/passes/pass_manager_wbtest.mbt`, `src/cmd/cmd.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy or WAT-shape coverage.
- Refreshed `docs/wiki/binaryen/passes/vacuum/index.md` and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatch/helper/validation-guard/trace/perf/CLI replay code map and keeps the reviewed release-page provenance explicit instead of leaving the Starshine side at only a high-level subset summary.

## [2026-04-22] research | add `optimize-instructions` primary-source capture and exact implementation/code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/optimize-instructions/` folder, then chose `optimize-instructions` because the dossier already covered the algorithm and shape families well but still lacked an immutable raw primary-source manifest, a compact upstream implementation/test-map page, and an exact MoonBit registry / dispatch / helper / proof-lane code map on the Starshine side.
- Added `docs/wiki/raw/binaryen/2026-04-22-optimize-instructions-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper-header, and representative lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while a narrow current-`main` spot check on the pass file, registration file, and representative default/GC test files did not surface a new teaching-relevant contract drift beyond the dossier's current claims.
- Added `docs/wiki/raw/research/0248-2026-04-22-optimize-instructions-primary-sources-and-implementation-followup.md` plus `docs/wiki/binaryen/passes/optimize-instructions/implementation-structure-and-tests.md`, grounding the follow-up in the new raw-source capture and the in-repo MoonBit implementation (`src/passes/optimize_instructions.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/optimize_instructions_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and closing the folder's practical owner/test-map gap rather than redoing the already-solid algorithm page.
- Refreshed `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `binaryen-strategy.md`, and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatch/helper/test code locations and to the new upstream implementation/test-map page instead of leaving the folder at only high-level algorithm coverage.

## [2026-04-22] lint | align stale `remove-unused-brs` implementation-map freshness metadata with the new source capture

- During the touched-area health check after the `remove-unused-brs` primary-source/code-map commit, re-scanned the folder and found that `implementation-structure-and-tests.md` still carried the older `2026-04-20` freshness wording even though this run had just re-checked the same current-`main` pass/test surfaces while building the new raw source manifest.
- Updated `docs/wiki/binaryen/passes/remove-unused-brs/implementation-structure-and-tests.md` so the page now cites the new raw primary-source capture directly and says the narrow current-main freshness check was re-confirmed on **2026-04-22**, keeping the touched folder internally consistent instead of mixing the new provenance with stale local freshness metadata.

## [2026-04-22] research | add `remove-unused-brs` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/remove-unused-brs/` folder, then chose `remove-unused-brs` because the dossier was already strong on upstream strategy and local shape families but still lacked an immutable raw primary-source manifest and the local Starshine strategy page still stopped at high-level HOT framing instead of pointing readers to the exact MoonBit registry / preset / raw-dispatch / helper-cluster / test map.
- Added `docs/wiki/raw/binaryen/2026-04-22-remove-unused-brs-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper-header, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while the checked current-`main` pass file and dedicated lit file still matched the already-documented teaching-relevant contract aside from the folder's existing narrow jump-threader drift note.
- Added `docs/wiki/raw/research/0247-2026-04-22-remove-unused-brs-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/remove_unused_brs.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/remove_unused_brs_test.mbt`, `src/passes/perf_test.mbt`, `src/passes/optimize_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing upstream strategy or WAT-shape coverage.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-brs/index.md` and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/preset/raw-dispatch/helper-cluster/test code map and keeps the reviewed release-page provenance explicit instead of leaving the folder at only high-level HOT framing.

## [2026-04-22] research | add `heap-store-optimization` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/heap-store-optimization/` folder, then chose `heap-store-optimization` because the dossier was already strong on the upstream algorithm but still lacked an immutable raw primary-source manifest and the local Starshine strategy page still stopped short of an exact MoonBit registry / dispatcher / helper-cluster / reduced-test / CLI-replay code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-heap-store-optimization-primary-sources.md`, capturing the official Binaryen GitHub release, source, helper-header, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while the checked current-`main` pass file and dedicated lit file still matched the already-documented teaching-relevant contract.
- Added `docs/wiki/raw/research/0246-2026-04-22-heap-store-optimization-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/heap_store_optimization.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/heap_store_optimization_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy coverage.
- Refreshed `docs/wiki/binaryen/passes/heap-store-optimization/index.md` and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatcher/helper-cluster/reduced-test/CLI-replay code map and keeps the reviewed release-page provenance explicit instead of leaving the folder at only high-level HOT framing.

## [2026-04-22] research | add `heap2local` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/heap2local/` folder, then chose `heap2local` because the Binaryen dossier was already solid but the folder still lacked an immutable raw primary-source manifest and the Starshine strategy page still stopped at high-level HOT/use-def framing instead of pointing readers to the exact MoonBit registry / dispatcher / candidate-analysis / rewrite / test code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-heap2local-primary-sources.md`, capturing the official Binaryen GitHub release, source, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, while the checked current-`main` source still differed only on the already-noted narrow array / cmpxchg / unreachable helper surfaces.
- Added `docs/wiki/raw/research/0245-2026-04-22-heap2local-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/heap2local.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/heap2local_test.mbt`, `src/passes/heap2local_primary_test.mbt`, `src/passes/optimize_test.mbt`, and `src/passes/registry_test.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing Binaryen strategy coverage.
- Refreshed `docs/wiki/binaryen/passes/heap2local/index.md` and `starshine-hot-ir-strategy.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatcher/candidate-analysis/rewrite/test code locations and keeps one touched-area hygiene clarification explicit: the dossier now says the local proof surface spans pass, primary, preset, and registry tests, but there is still no dedicated `heap2local` CLI replay lane in `src/cmd/cmd_wbtest.mbt`.

## [2026-04-22] research | add `pick-load-signs` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/pick-load-signs/` folder, then chose `pick-load-signs` because the dossier was already deep on the upstream algorithm but still lacked an immutable raw primary-source manifest and the local Starshine strategy page still stopped short of an exact MoonBit registry / dispatcher / raw-skip / rewrite / test map.
- Added `docs/wiki/raw/binaryen/2026-04-22-pick-load-signs-primary-sources.md`, capturing the official Binaryen GitHub release, source, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, and the checked current-`main` `PickLoadSigns.cpp` plus dedicated lit file still matched the tagged teaching-relevant mechanics.
- Added `docs/wiki/raw/research/0244-2026-04-22-pick-load-signs-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/pick_load_signs.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/pick_load_signs_test.mbt`, `src/passes/perf_test.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance plus exact local navigation, not missing upstream algorithm coverage.
- Refreshed `docs/wiki/binaryen/passes/pick-load-signs/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `starshine-hot-ir-strategy.md`, and `parity.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatcher/raw-skip/rewrite/test code map and keeps one touched-area health correction explicit: the code supports broader i64 families, but the focused local tests still do not isolate dedicated i64 positive rewrite cases.

## [2026-04-22] research | add `remove-unused-module-elements` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/remove-unused-module-elements/` folder, then chose `remove-unused-module-elements` because the upstream dossier was already solid but the folder still lacked an immutable raw primary-source manifest and the Starshine strategy page still stopped at high-level module-pass framing instead of pointing readers to the exact MoonBit registry / dispatcher / root-policy / liveness / rewrite / type-cleanup code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-remove-unused-module-elements-primary-sources.md`, capturing the official Binaryen GitHub release, source, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, and the checked `main` `RemoveUnusedModuleElements.cpp` surfaces still matched the tagged release on the reviewed teaching-relevant mechanics.
- Added `docs/wiki/raw/research/0243-2026-04-22-remove-unused-module-elements-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/remove_unused_module_elements.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/remove_unused_module_elements_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was provenance and exact local navigation, not missing upstream algorithm coverage.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-module-elements/index.md`, `starshine-hot-ir-strategy.md`, and `retention-and-index-rewrites.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatcher/root-policy/liveness/remap/type-cleanup/test code locations and keeps the local dead-type-cleanup story explicit instead of leaving the Starshine side at only high-level module-pass framing.

## [2026-04-22] research | add `duplicate-function-elimination` primary-source capture and exact Starshine code-map coverage

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/duplicate-function-elimination/` folder, then chose `duplicate-function-elimination` because the upstream dossier was already solid but the folder still lacked an immutable raw primary-source manifest and the Starshine strategy page still stopped at high-level module-pass framing instead of pointing readers to the exact MoonBit registry / dispatcher / core-iteration / rewrite / cleanup code map.
- Added `docs/wiki/raw/binaryen/2026-04-22-duplicate-function-elimination-primary-sources.md`, capturing the official Binaryen GitHub release, source, and lit-test URLs reviewed in this run and recording the checked release provenance explicitly: on 2026-04-22 the reviewed official `version_129` release page showed publish date **2026-04-01**, and the current-`main` pass file still differed only by the already-noted tiny duplicate-name container change.
- Added `docs/wiki/raw/research/0242-2026-04-22-duplicate-function-elimination-primary-sources-and-code-map-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/duplicate_function_elimination.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/duplicate_function_elimination_test.mbt`, and `src/cmd/cmd_wbtest.mbt`) and making explicit that the remaining documentation gap was local provenance and code-map clarity, not missing upstream algorithm coverage.
- Refreshed `docs/wiki/binaryen/passes/duplicate-function-elimination/index.md`, `starshine-hot-ir-strategy.md`, and `type-compaction-and-metadata.md`, then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now points readers directly to the exact local registry/dispatcher/core-rewrite/type-compaction/metadata code locations and to concrete local before/after shape families instead of leaving the Starshine side at only high-level module-pass framing.

## [2026-04-21] research | add `simplify-locals` primary-source capture and a structure-result/carrier bridge page

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/simplify-locals/` folder, then chose `simplify-locals` as a worthwhile already-deep follow-up because the folder was strong on strategy but still lacked an immutable primary-source manifest and one compact page that mapped the key block / if / loop carrier families directly onto current Starshine helpers and tests.
- Added `docs/wiki/raw/binaryen/2026-04-21-simplify-locals-primary-sources.md`, capturing the official Binaryen GitHub release/source/test URLs reviewed in this run and recording the dated release check explicitly: on 2026-04-21 GitHub showed Binaryen `version_129` as the latest release page with publish date **2026-04-01**, so the simplify-locals dossier can now use `version_129` as the released oracle without the older nearby release-order uncertainty wording.
- Added `docs/wiki/raw/research/0241-2026-04-21-simplify-locals-primary-sources-and-structure-followup.md`, grounding the follow-up in the new raw-source capture plus the in-tree MoonBit implementation (`src/passes/simplify_locals.mbt`, `src/passes/pass_manager.mbt`, `src/passes/simplify_locals_test.mbt`, `src/passes/pass_manager_wbtest.mbt`, and `src/passes_perf_long/simplify_locals_multivalue_perf_test.mbt`) and making explicit that the remaining teaching gap was the bridge from upstream structure-result shapes to local helper/test ownership, not a missing pass dossier.
- Added `docs/wiki/binaryen/passes/simplify-locals/structure-result-lifting-and-carrier-cleanup.md`, and refreshed `docs/wiki/binaryen/passes/simplify-locals/index.md`, `starshine-hot-ir-strategy.md`, `implementation-map.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now gives readers a clean path from official primary sources to the exact local block-result / if-result / one-armed-`if` / loop-tail / wrapper-forwarder helper clusters and regression lanes.

## [2026-04-21] lint | fix stale `ssa-nomerge` parity wording after the new strategy page landed

- During the touched-area health check, re-scanned the refreshed `docs/wiki/binaryen/passes/ssa-nomerge/` folder and found that `parity.md` plus the matching top-level `docs/wiki/index.md` parity blurb still repeated the older `2026-04-11` wording as if the traced `Func 523` `writeback-validate:type mismatch` skip were still current.
- Reworded the affected parity notes so the touched docs now say the focused extracted-function CLI replay retired that specific skip, while still keeping the broader raw-lowering signoff gap explicit and avoiding an unsupported fresh full-artifact traced-count claim.

## [2026-04-21] research | add Starshine `ssa-nomerge` strategy/code-map coverage and ingest a primary-source capture

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/ssa-nomerge/` folder, then chose `ssa-nomerge` because it was an active implemented hot-pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/binaryen/2026-04-21-ssa-nomerge-primary-sources.md`, capturing the exact official Binaryen release/source/test URLs reviewed in this run and preserving the release-numbering uncertainty explicitly: the official GitHub releases index visible on 2026-04-21 showed both `version_129` and a newer-by-date `version_125` page, so the touched dossier now treats `version_129` only as the reviewed tagged oracle for the exact `ssa-nomerge` surfaces, not as an asserted latest global Binaryen release.
- Added `docs/wiki/raw/research/0240-2026-04-21-ssa-nomerge-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/ssa_nomerge.mbt`, `src/passes/pass_manager.mbt`, `src/ir/ssa_destroy.mbt`, `src/passes/ssa_nomerge_test.mbt`, `src/cmd/cmd_wbtest.mbt`, and `src/passes/optimize.mbt`) plus the official Binaryen release / source / test surfaces captured in the new raw-source manifest, and making explicit that current Starshine is a HOT-SSA roundtrip plus raw-fallback family rather than a direct no-merge LocalGraph port.
- Added `docs/wiki/binaryen/passes/ssa-nomerge/starshine-hot-ir-strategy.md`, refreshed `docs/wiki/binaryen/passes/ssa-nomerge/index.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/Starshine/shapes/parity dossier shape instead of leaving `ssa-nomerge` as the remaining implemented hot-pass exception.

## [2026-04-21] research | add Starshine `tuple-optimization` code-map coverage and align touched-folder metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/tuple-optimization/` folder, then chose `tuple-optimization` because the dossier was already strong on the upstream side but still lacked a dedicated Starshine implementation-map page and an exact code-location-oriented local strategy page.
- Added `docs/wiki/raw/research/0239-2026-04-21-tuple-optimization-starshine-code-map-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/tuple_optimization.mbt`, `tuple_optimization_wbtest.mbt`, `pass_manager.mbt`, `optimize.mbt`, `registry_test.mbt`, `cmd_wbtest.mbt`, and `cmd_native_wbtest.mbt`) plus official Binaryen primary sources (`TupleOptimization.cpp`, `pass.cpp`, and `tuple-optimization.wast`) and making explicit that the main missing teaching gap was local code ownership, not upstream algorithm coverage.
- Added `docs/wiki/binaryen/passes/tuple-optimization/implementation-map.md`, and refreshed `starshine-hot-ir-strategy.md` so the folder now points readers directly to exact MoonBit code locations for the registry/dispatch/preset surface, seed-group and copy-group analysis, forwarded-use classification, rewrite-mask policy, root-slot placement, tuple-carrier builders, cleanup cluster, and the focused wbtest / CLI / native-oracle lanes.
- Refreshed `docs/wiki/binaryen/passes/tuple-optimization/index.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now advertises the stronger Binaryen-plus-Starshine dossier shape instead of leaving `tuple-optimization` with a comparatively thin local code-map surface.

## [2026-04-21] research | add Starshine `once-reduction` strategy/code-map coverage and align touched-area dossier metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/once-reduction/` folder, then chose `once-reduction` because it was an active implemented module-pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/research/0238-2026-04-21-once-reduction-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/once_reduction.mbt`, `once_reduction_test.mbt`, `pass_manager.mbt`, `optimize.mbt`, `registry_test.mbt`, and `cmd_wbtest.mbt`) plus official Binaryen primary sources (`OnceReduction.cpp`, `pass.cpp`, `intrinsics.h`, and `once-reduction.wast`) and making explicit that current Starshine is a narrower recursive boundary-array once-bit subset rather than the full upstream CFG/dominator plus idempotent-annotation engine.
- Added `docs/wiki/binaryen/passes/once-reduction/starshine-hot-ir-strategy.md`, giving the dossier its missing local-strategy page with exact code locations for once-wrapper matching, candidate-global scanning, recursive summary propagation, recursive rewrite, once-body cleanup, dispatch, preset placement, registry coverage, and CLI coverage.
- Refreshed `docs/wiki/binaryen/passes/once-reduction/index.md` and `parity.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/shapes/Starshine/parity dossier shape instead of leaving `once-reduction` as an implemented-pass exception.

## [2026-04-21] research | add Starshine `reorder-locals` strategy/code-map coverage and align touched-area dossier metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/reorder-locals/` folder, then chose `reorder-locals` because it was an active implemented module-pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/research/0237-2026-04-21-reorder-locals-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/reorder_locals.mbt`, `reorder_locals_test.mbt`, `pass_manager.mbt`, `optimize.mbt`, `optimize_test.mbt`, `registry_test.mbt`, and `cmd_wbtest.mbt`) plus official Binaryen primary sources (`ReorderLocals.cpp`, `pass.cpp`, and the dedicated `reorder-locals*` tests) and making explicit that current Starshine matches the core Binaryen ordering rule but keeps the pass module-scoped because parameter lookup and name/raw-payload repair cross module metadata boundaries in this repo.
- Added `docs/wiki/binaryen/passes/reorder-locals/starshine-hot-ir-strategy.md`, giving the dossier its missing local-strategy page with exact code locations for access scanning, used-body-local sorting, parameter caching, grouped-local-run rebuilding, local-name rewriting, module rebuild/name-section invalidation, dispatch, registry placement, preset exclusion, and CLI coverage.
- Refreshed `docs/wiki/binaryen/passes/reorder-locals/index.md` and `parity.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/shapes/Starshine/parity dossier shape instead of leaving `reorder-locals` as one of the remaining implemented-pass exceptions.

## [2026-04-21] lint | fix stale `global-refining` landing-page follow-up wording after the new Starshine strategy page landed

- During the post-commit health check, re-scanned the touched `docs/wiki/binaryen/passes/global-refining/` folder for stale references and found that `index.md` still described the latest follow-up as if it were about the earlier implementation/test-map gap rather than the newly completed Starshine strategy/code-map gap.
- Reworded the affected landing-page bullet so the folder now consistently says this run closed the missing Starshine strategy/code-map page, not the already-closed owner/test-map gap from the earlier `0208-2026-04-21-global-refining-source-confirmation-followup.md` change.

## [2026-04-21] research | add Starshine `global-refining` strategy/code-map coverage and align touched-area dossier metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/global-refining/` folder, then chose `global-refining` because it was an active implemented module-pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/research/0236-2026-04-21-global-refining-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/global_refining.mbt`, `global_refining_test.mbt`, `pass_manager.mbt`, and `optimize.mbt`) plus official Binaryen primary sources (`GlobalRefining.cpp`, `pass.cpp`, and `global-refining.wast`) and making explicit that current Starshine is a narrower private-global declaration-tightening subset rather than the full upstream exported-boundary plus `global.get`-retagging contract.
- Added `docs/wiki/binaryen/passes/global-refining/starshine-hot-ir-strategy.md`, giving the dossier its missing local-strategy page with exact code locations for initializer typing, local ref-type joining, cheap candidate-set scanning, HOT-assisted `global.set` collection, declaration rewriting, dispatch, preset placement, and the focused local tests.
- Refreshed `docs/wiki/binaryen/passes/global-refining/index.md` and `parity.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/shapes/Starshine/parity dossier shape instead of leaving `global-refining` as an implemented-pass exception.

## [2026-04-21] research | add Starshine `remove-unused-names` strategy/code-map coverage and align touched-area dossier metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/remove-unused-names/` folder, then chose `remove-unused-names` because it was an active implemented hot-pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/research/0235-2026-04-21-remove-unused-names-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/remove_unused_names.mbt`, `remove_unused_names_test.mbt`, `pass_manager.mbt`, and `optimize.mbt`) plus official Binaryen primary sources (`RemoveUnusedNames.cpp`, `pass.cpp`, and `remove-unused-names.wast`) and making explicit that current Starshine is a narrower same-typed-block-peel plus dead-label-loop-demotion subset of Binaryen's fuller generic control-label cleanup pass.
- Added `docs/wiki/binaryen/passes/remove-unused-names/starshine-hot-ir-strategy.md`, giving the dossier its missing local-strategy page with exact code locations for label-use collection, same-typed block peeling, loop demotion, raw fast-skip gating, dispatch, preset placement, and the focused local tests.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-names/index.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/shapes/Starshine dossier shape instead of leaving `remove-unused-names` as an implemented-pass exception.

## [2026-04-21] research | add Starshine `global-struct-inference` strategy/code-map coverage and align touched-area dossier metadata

- Re-read `AGENTS.md`, `docs/README.md`, `docs/wiki/index.md`, `docs/wiki/log.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the existing `docs/wiki/binaryen/passes/global-struct-inference/` folder, then chose `global-struct-inference` because it was an active implemented pass dossier that still lacked the dedicated Starshine strategy/code-map page the surrounding implemented-pass schema already expected.
- Added `docs/wiki/raw/research/0234-2026-04-21-global-struct-inference-starshine-strategy-followup.md`, grounding the follow-up in the in-tree MoonBit implementation (`src/passes/global_struct_inference.mbt`, `global_struct_inference_test.mbt`, `pass_manager.mbt`, and `optimize.mbt`) plus official Binaryen primary sources (`GlobalStructInference.cpp`, `pass.cpp`, and `gsi.wast`) and making explicit that current Starshine is a closed-world direct-global fold, not the full upstream origin-analysis pass.
- Added `docs/wiki/binaryen/passes/global-struct-inference/starshine-hot-ir-strategy.md`, giving the dossier its missing local-strategy page with exact code locations for candidate-global harvesting, packed-field repair, direct `global.get` + `struct.get*` rewriting, null-trap preservation, dispatch, preset placement, and the focused local tests.
- Refreshed `docs/wiki/binaryen/passes/global-struct-inference/index.md` and `parity.md`, and updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`, so the touched area now consistently advertises the full landing/Binaryen/shapes/Starshine/parity dossier shape instead of leaving `global-struct-inference` as one of the remaining implemented-pass exceptions.

## [2026-04-21] research | deepen Binaryen `monomorphize` with exact clone / signature / dropped-call rewrite mechanics

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/monomorphize/` plus sibling `monomorphize-always/`, `inlining/`, and `inline-main/` folders, then chose `monomorphize` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-good dossier still lacked one compact source-confirmed page for the exact clone-construction bridge future ports are most likely to misremember.
- Added `docs/wiki/raw/research/0233-2026-04-21-monomorphize-clone-and-rewrite-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`Monomorphize.cpp`, `pass.cpp`, `find_all.h`, `manipulation.h`, `module-utils.h`, `names.h`, `return-utils.h`, and the dedicated `monomorphize-context` / `drop` / `consts` / `limits` lit files, plus a narrow current-`main` no-drift spot check) and making explicit that the real contract is not just “clone a specialized callee,” but signature derivation from surviving `LocalGet`s in transformed context operands, old-param-to-local conversion, old-var shifting, prelude `local.set` reconstruction, dropped-result `Type::none` plus `removeReturns(...)`, and caller-side `(drop (call ...))` repair.
- Added `docs/wiki/binaryen/passes/monomorphize/clone-construction-signature-rebuild-and-dropped-call-rewrites.md`, and refreshed `docs/wiki/binaryen/passes/monomorphize/index.md`, `binaryen-strategy.md`, and `implementation-structure-and-tests.md`, so the living dossier now has a canonical focused home for the exact `makeMonoFunctionWithContext(...)` mechanics instead of leaving them spread across the broader strategy and source-map pages.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `monomorphize` folder's former clone/rewrite mechanics gap is closed, that the dossier should now be treated as deep, and that future recursive campaign threads should not spend another pass on the same missing explanation.

## [2026-04-21] research | deepen Binaryen `optimize-added-constants` with the exact low-memory and overflow safety matrix

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/optimize-added-constants/` plus sibling `optimize-added-constants-propagate/` folders, then chose plain `optimize-added-constants` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-covered folder still lacked one compact source-confirmed page for the exact safety math future ports are most likely to misread.
- Added `docs/wiki/raw/research/0232-2026-04-21-optimize-added-constants-safety-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`OptimizeAddedConstants.cpp`, `pass.cpp`, `pass.h`, the plain low-memory oracle pair, the shared `memory64` / `nomemory` lit files, plus a current-`main` no-drift check) and making explicit that the real plain-pass contract is not just “small constants go into offsets,” but strict `1023`-yes / `1024`-no low-memory checks on both the new constant and merged total offset, plus separate unsigned-overflow-safe constant-pointer normalization rules for memory32 vs memory64.
- Added `docs/wiki/binaryen/passes/optimize-added-constants/low-memory-threshold-overflow-and-offset-merge-rules.md`, and refreshed `docs/wiki/binaryen/passes/optimize-added-constants/index.md`, so the living dossier now has a canonical focused home for the hard `--low-memory-unused` gate, the merged-offset rule, the exact constant-pointer normalization split, and the reminder that memory64 changes the overflow proof width rather than the low-memory threshold itself.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `optimize-added-constants` folder's former low-memory / overflow teaching gap is closed and future recursive campaign threads should not spend another pass on the same safety-matrix question.

## [2026-04-21] research | add Binaryen `instrument-memory` dossier for memory/heap-access instrumentation and filter-aware helper imports

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `instrument-locals` dossier, then chose `instrument-memory` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the new `instrument-locals` docs already depended on keeping the sibling split explicit and Binaryen's own public help text still understates the real surface as only loads/stores.
- Added `docs/wiki/raw/research/0231-2026-04-21-instrument-memory-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`InstrumentMemory.cpp`, `pass.cpp`, and the dedicated `instrument-memory*.wast` lit family, plus a current-`main` no-drift spot check) and making explicit that the real contract is a compact effectful postwalk over `load` / `store` / `memory.grow` plus GC `struct.get` / `struct.set` / `array.get` / `array.set`, with exact filter strings, unconditional-vs-GC helper import injection, scalar-only payload support, and non-uniform shared ID numbering rather than a vague generic memory tracer.
- Added `docs/wiki/binaryen/passes/instrument-memory/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `helper-import-roster-filters-and-unsupported-types.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the sibling split from `instrument-locals`, the broader-than-help-text rewrite surface, the exact helper API roster, the filter contract, the memory64 address-width behavior, and the downstream effect-analysis interaction.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `instrument-memory` is now a justified upstream-only tracker expansion rather than an uncataloged neighboring-pass mention.

## [2026-04-21] research | deepen Binaryen `dead-argument-elimination` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/dead-argument-elimination/` plus neighboring `dae-optimizing/` and `dae2/` folders, then chose plain `dead-argument-elimination` as an explicitly justified major-gap follow-up because the existing dossier still had an obvious missing-page hole: its landing page already promised `implementation-structure-and-tests.md`, but that source-confirmed owner/test map did not exist yet.
- Added `docs/wiki/raw/research/0230-2026-04-21-dead-argument-elimination-implementation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `return-utils.h`, `lubs.h`, `type-updating.h`, and the `dae_tnh` / `dae-gc*` / neighboring `dae-optimizing*` lit files) and making explicit that plain `dae` is the shared `DeadArgumentElimination.cpp` engine in non-optimizing mode, that `opt-utils.h` only owns the optimizing rerun, and that the real plain-pass proof surface is the distributed `dae_tnh` + `dae-gc*` family rather than one dedicated `dae.wast` file.
- Added `docs/wiki/binaryen/passes/dead-argument-elimination/implementation-structure-and-tests.md`, and refreshed `docs/wiki/binaryen/passes/dead-argument-elimination/index.md` plus `binaryen-strategy.md`, so the living dossier now has a canonical compact home for the shared owner split, the optimizing-only helper boundary, and the explicit `dae2`-is-separate test-attribution rule.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `dead-argument-elimination` folder's missing implementation/test-map gap is closed and future recursive campaign threads should not spend another pass on the same owner-attribution question.

## [2026-04-21] research | deepen Binaryen `precompute` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/precompute/` plus neighboring `precompute-propagate/` folders, then chose plain `precompute` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-deep dossier still lacked one compact source-confirmed page that answered the practical owner/test-attribution question instead of forcing readers to reconstruct it from the broad strategy page.
- Added `docs/wiki/raw/research/0229-2026-04-21-precompute-implementation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`Precompute.cpp`, `pass.cpp`, `opt-utils.h`, `wasm-interpreter.h`, `local-graph.h`, `properties.h`, and the broad `precompute*.wast` lit roster, with the neighboring `precompute-propagate*` files kept explicit as sibling-boundary proof) and making explicit that plain `precompute` is the shared semantic-evaluator core in non-propagating public mode rather than a tiny standalone arithmetic file.
- Added `docs/wiki/binaryen/passes/precompute/implementation-structure-and-tests.md`, and refreshed `docs/wiki/binaryen/passes/precompute/index.md` plus `binaryen-strategy.md`, so the living dossier now has a canonical compact home for the shared `Precompute.cpp` owner story, the public and nested-rerun split from `precompute-propagate`, and the fact that the real plain-pass proof surface is the broad `precompute*.wast` family rather than one tiny dedicated lit file.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `precompute` folder's compact implementation/test-attribution gap is closed and future recursive campaign threads should not spend another pass on the same missing explanation.

## [2026-04-21] research | deepen Binaryen `pick-load-signs` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/pick-load-signs/` folder, then chose `pick-load-signs` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-deep dossier still lacked one compact page that made the tiny upstream owner graph and proof surface easy to teach instead of forcing readers to reconstruct them from the strategy page.
- Added `docs/wiki/raw/research/0228-2026-04-21-pick-load-signs-implementation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`PickLoadSigns.cpp`, `pass.cpp`, `opt-utils.h`, `properties.h`, the dedicated `pick-load-signs_sign-ext.wast` lit file, and the neighboring `optimize-instructions-sign_ext.wast` boundary proof, plus a current-`main` no-drift spot check) and making explicit that the real contract is concentrated in `PickLoadSigns.cpp` with helper semantics in `properties.h`, not in a larger hidden analysis subsystem.
- Added `docs/wiki/binaryen/passes/pick-load-signs/implementation-structure-and-tests.md`, and refreshed `index.md`, `binaryen-strategy.md`, and `parity.md`, so the living dossier now has a canonical compact home for the pass owner split, the dedicated-vs-neighboring test split, and the reminder that broader i64 sign-extension cleanup should not be silently attributed to upstream `pick-load-signs`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `pick-load-signs` folder's implementation/test-map gap is closed and that future recursive campaign threads should not spend another pass on the same missing explanation.

## [2026-04-21] research | add Binaryen `instrument-locals` dossier for local-access instrumentation and effect invalidation

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then chose `instrument-locals` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets, the current thread was forbidden from reusing the recent candidate set, and this real public Binaryen pass still lacked a canonical home even though its effect-invalidating instrumentation contract is easy to mis-teach as either locals optimization or harmless logging.
- Added `docs/wiki/raw/research/0227-2026-04-21-instrument-locals-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`InstrumentLocals.cpp`, `pass.cpp`, `passes.h`, and the dedicated `instrument-locals_all-features_disable-gc.wast`, `instrument-locals_effects.wast`, and `instrument-locals-eh-legacy.wast` proof files, plus a current-`main` no-drift spot check) and making explicit that the real contract is a tiny effectful postwalk that wraps supported `local.get` / `local.set` traffic in imported helper calls, injects helper imports, still skips ordinary `i64` traffic, and deliberately invalidates existing global-effects knowledge.
- Added `docs/wiki/binaryen/passes/instrument-locals/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `unsupported-types-effects-and-import-roster.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the postwalk rewrite shape, shared call-id numbering, unconditional scalar helper-import roster, current `i64` / general-ref / typed-funcref / `pop` boundaries, and the neighboring `generate-global-effects` / `vacuum` interaction.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `instrument-locals` is now a justified upstream-only tracker expansion instead of an uncataloged public Binaryen pass.

## [2026-04-21] research | deepen Binaryen `inlining` with a source-confirmed split between compilation hints, `no-inline*` flags, and clone survival

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/inlining/` folder, then chose plain `inlining` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-good but not-yet-deep dossier still lacked one compact source-confirmed page for the practical split between preserved `@metadata.code.inline` bytes, the real `no-inline` / `no-full-inline` / `no-partial-inline` function flags, and why those flags survive function cloning.
- Added `docs/wiki/raw/research/0226-2026-04-21-inlining-inline-hints-and-no-inline-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`Inlining.cpp`, `NoInline.cpp`, `pass.cpp`, `pass.h`, `wasm.h`, `wasm/wasm.cpp`, `wasm/wasm-binary.cpp`, `parser/contexts.h`, `module-utils.cpp`, and the dedicated `inline-hints*` / `no-inline*` lit files, plus a narrow current-`main` no-drift check) and making explicit that plain `inlining` consults `Function::noFullInline` / `noPartialInline` rather than `CodeAnnotation::inline_`, while `ModuleUtils::copyFunction` preserves those booleans on clones.
- Added `docs/wiki/binaryen/passes/inlining/compilation-hints-vs-no-inline-flags-and-clone-survival.md`, and refreshed `docs/wiki/binaryen/passes/inlining/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `heuristics-splitting-and-plain-vs-optimizing.md`, so the living dossier now has a canonical compact home for compilation-hints roundtripping, the wildcard `no-inline*` pass family, full-vs-partial suppression, and the monomorphized-clone survival story.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `inlining` folder's metadata/policy source-confirmation gap is closed and that the dossier should now be treated as deep instead of merely a basic upstream-only folder.

## [2026-04-21] research | deepen Binaryen `const-hoisting` with exact `Literal` float-bucket semantics and a lit-comment correction note

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/const-hoisting/` folder, then chose `const-hoisting` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this already-good dossier still lacked one compact source-confirmed page for the exact float grouping key and the tiny misleading comment in the upstream lit file.
- Added `docs/wiki/raw/research/0225-2026-04-21-const-hoisting-literal-identity-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`ConstHoisting.cpp`, `literal.h`, `pass.cpp`, and `const-hoisting.wast`, plus a current-`main` no-drift spot check) and making explicit that the real grouping key is exact `Literal` type-plus-bits identity, so `+0.0` / `-0.0` and distinct NaN payloads are separate hoist buckets even though the source still has a future-looking zero TODO.
- Added `docs/wiki/binaryen/passes/const-hoisting/literal-bit-identity-zero-signs-and-nan-payloads.md`, and refreshed `index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `size-model-and-boundaries.md`, and `wat-shapes.md`, so the living dossier now has a canonical focused home for float bit-identity grouping, the `Literal`-equality-versus-`isZero()` distinction, the `+0.0` / `-0.0` and NaN-payload WAT families, and the note that the final official `f64` example's inline comment is stale while the checked output and implementation still prove the real `8`-byte / `2`-use threshold.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `const-hoisting` folder's float-corner-case teaching gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | add Binaryen `legalize-and-prune-js-interface` dossier for prune-mode JS-boundary sanitization

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the new neighboring `legalize-js-interface` dossier, then chose `legalize-and-prune-js-interface` as an explicitly justified upstream-only expansion because the tracker still had no obvious remaining `none` targets while the plain-family docs now depended on keeping their prune-mode sibling explicit instead of teaching it only indirectly.
- Added `docs/wiki/raw/research/0224-2026-04-21-legalize-and-prune-js-interface-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`LegalizeJSInterface.cpp`, `pass.cpp`, and the dedicated `legalize-and-prune-js-interface.wast` proof file, with a current-`main` no-drift spot check) and making explicit that the real contract is plain `i64` JS-boundary legalization plus result-sensitive import stubbing, export removal, and post-prune refinalization for still-illegal SIMD / multivalue-result / EH / stack-switching surfaces rather than a generic feature-lowering pass.
- Added `docs/wiki/binaryen/passes/legalize-and-prune-js-interface/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `prune-boundary-matrix.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the shared-file subclass structure, the import/export/global prune matrix, the `nop` vs zero/default vs `unreachable` stub split, the imported-and-exported combined case, and the exact relation to plain `legalize-js-interface`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `legalize-and-prune-js-interface` is now a justified upstream-only tracker expansion instead of an uncataloged sibling hidden behind the plain dossier.

## [2026-04-21] research | add Binaryen `legalize-js-interface` dossier for JS-boundary `i64` ABI legalization

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `i64-to-i32-lowering` dossier, then chose `legalize-js-interface` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the existing whole-module `i64`-lowering docs already depended on teaching where earlier JS-boundary legalization happens without giving it a canonical home.
- Added `docs/wiki/raw/research/0223-2026-04-21-legalize-js-interface-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`LegalizeJSInterface.cpp`, `pass.cpp`, and the dedicated `legalize-js-interface*` plus `legalize-and-prune-js-interface` lit files, with a current-`main` no-drift spot check) and making explicit that the real contract is wrapper-based import/export `i64` legalization with temp-ret helpers, import-use repair, optional `export-originals` preservation, and a separate prune-only sibling rather than whole-module integer lowering.
- Added `docs/wiki/binaryen/passes/legalize-js-interface/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `temp-ret-helpers-and-pruning-split.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the `legalstub$` / `legalimport$` / `legalfunc$` split, the temp-ret helper policy, the `export-originals` and `exported-helpers` pass arguments, the `call` / `ref.func` repair surface, and the exact split from both `i64-to-i32-lowering` and `legalize-and-prune-js-interface`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `legalize-js-interface` is now a justified upstream-only tracker expansion instead of an uncataloged neighboring-pass dependency.

## [2026-04-21] research | deepen Binaryen `simplify-globals` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/simplify-globals/` folder, then chose plain `simplify-globals` as an explicitly justified already-dossier follow-up because the tracker had no obvious remaining `none` targets while this late-global dossier still lacked one compact source-confirmed owner/test-map page.
- Added `docs/wiki/raw/research/0222-2026-04-21-simplify-globals-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`SimplifyGlobals.cpp`, `pass.cpp`, `pass.h`, the main helper headers, and the dedicated `simplify-globals*` plus `propagate-globals-globally` lit roster) and making explicit that the real contract is one shared `SimplifyGlobals.cpp` family with distinct public registrations rather than separate standalone implementation files for each sibling.
- Added `docs/wiki/binaryen/passes/simplify-globals/implementation-structure-and-tests.md`, and refreshed `index.md` plus `binaryen-strategy.md`, so the living dossier now has a canonical compact home for the real owner split, the public plain / optimizing / startup-only registration boundary, the pass-runner split from the optimizing sibling, and the clustered lit proof surface.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `simplify-globals` folder's compact implementation/test-map gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | add Binaryen `dealign` dossier for the upstream align-to-1 metadata pass

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `alignment-lowering` dossier, then chose `dealign` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the refreshed alignment docs already depended on teaching the opposite-direction sibling precisely without giving it a canonical home.
- Added `docs/wiki/raw/research/0221-2026-04-21-dealign-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`DeAlign.cpp`, `pass.cpp`, and `dealign.wast`, plus a narrow current-`main` spot check) and making explicit that the real contract is a tiny module pass over a no-memory bailout, defined-function traversal, and the literal `if (align > 1) align = 1` rewrite for `Load` / `Store` / `SIMDLoad` / `SIMDStore`, not chunk-lowering legalization or generic memory optimization.
- Added `docs/wiki/binaryen/passes/dealign/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `align-one-rewrite-surface-and-alignment-lowering-split.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the exact rewrite surface, the scalar-vs-SIMD proof split, the no-memory bailout, and the explicit distinction from `alignment-lowering`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `dealign` is now a justified upstream-only tracker expansion instead of an uncataloged neighboring-pass mention.

## [2026-04-21] research | deepen Binaryen `remove-unused-names` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, the existing `docs/wiki/binaryen/passes/remove-unused-names/` folder, and the local `src/passes/remove_unused_names*.mbt` surfaces, then chose `remove-unused-names` as an explicitly justified already-deep follow-up because the tracker had no obvious remaining `none` targets while this implemented dossier still lacked one compact source-confirmed owner/test-map page.
- Added `docs/wiki/raw/research/0220-2026-04-21-remove-unused-names-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`RemoveUnusedNames.cpp`, `pass.cpp`, `branch-utils.h`, `shared-constants.h`, the dedicated `remove-unused-names` pair, and the neighboring combo files) and making explicit that the real contract is concentrated in a tiny `branchesSeen` postwalk plus generic scope-target helpers and caller-delegate sentinel cleanup rather than a vague generic block flattener.
- Added `docs/wiki/binaryen/passes/remove-unused-names/implementation-structure-and-tests.md`, and refreshed `index.md`, `binaryen-strategy.md`, and `wat-shapes.md`, so the living dossier now has a canonical compact home for the real owner split, the three top-level no-DWARF slots, the direct-vs-combo test surface, and the explicit note that delegate behavior is source-confirmed more from code ownership than from a dedicated standalone lit filename.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `remove-unused-names` folder's compact implementation/test-map gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | add Binaryen `souperify` dossier for the upstream flat-IR Souper trace-emission family

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `flatten` / `simplify-locals-nonesting` / `dataflow-optimization` docs, then chose `souperify` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while those neighboring dossiers already depended on placing Souper extraction precisely without giving it a canonical home.
- Added `docs/wiki/raw/research/0219-2026-04-21-souperify-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`Souperify.cpp`, `pass.cpp`, `dataflow/graph.h`, `dataflow/node.h`, `dataflow/utils.h`, `flat.h`, `local-graph.h`, and the two official flatten-plus-`simplify-locals-nonesting` lit files) and making explicit that the real pass family is a flat-IR-only DataFlow-graph plus `LocalGraph` extractor over bounded unary/binary/`select` slices rather than an ordinary optimizer.
- Added `docs/wiki/binaryen/passes/souperify/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flat-dataflow-traces-and-single-use-boundaries.md`, and `wat-shapes.md`, so the living wiki now has a dedicated home for the hard `Flat::verifyFlatness` precondition, the DataFlow node vocabulary, the bounded trace-growth plus `Var` replacement rules, the `if`-only `pc` / `blockpc` surface, the loop-phi avoidance policy, and the exact sibling split between plain and single-use extraction.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `souperify` is now a justified upstream-only tracker expansion and no longer just an uncataloged neighboring-pass mention.

## [2026-04-21] research | add Binaryen `dae2` dossier for the upstream fixed-point dead-argument reimplementation

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring DAE-family docs, then chose `dae2` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the existing plain-`dead-argument-elimination` dossier already had to keep warning that `dae2.wast` belongs to a different pass without giving it a canonical home.
- Added `docs/wiki/raw/research/0218-2026-04-21-dae2-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`DeadArgumentElimination2.cpp`, `pass.cpp`, `dae2.wast`, plus the supporting `local-graph`, `module-utils`, `type-updating`, `intrinsics`, `effects`, and related helper surfaces) and making explicit that the real pass is a backward fixed-point used-parameter analysis over forwarded params and function-type trees, not a minor scheduling variant of plain `dae`.
- Added `docs/wiki/binaryen/passes/dae2/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `fixed-point-forwarding-type-trees-and-expression-removal.md`, and `wat-shapes.md`, so the living wiki now has a dedicated home for the used-vs-forwarded parameter lattice, reverse-graph propagation, the `--closed-world` + GC referenced-function rewrite gate, replacement types for unreferenced siblings, and the effect-preserving expression-removal boundaries.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `dae2` is now a justified upstream-only tracker expansion and no longer just an uncataloged neighboring-pass footnote.

## [2026-04-21] research | add Binaryen `type-ssa` dossier for the upstream created-exact-type GC precision pass

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing neighboring GC/type dossiers, then chose `type-ssa` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the existing `type-merging` docs already depended on contrasting against `type-ssa` without giving it a canonical home of its own.
- Added `docs/wiki/raw/research/0217-2026-04-21-type-ssa-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`TypeSSA.cpp`, `pass.cpp`, `type-ssa.wast`, and `ReFinalize.cpp`, plus a narrow current-`main` spot check) and making explicit that the real pass is a small GC function pass over a `createdTypes` map, not general SSA construction or whole-program type inference.
- Added `docs/wiki/binaryen/passes/type-ssa/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `created-exact-types-control-values-and-signature-rewrites.md`, and `wat-shapes.md`, so the living wiki now has a dedicated home for the constructor/cast seed surface, the exact `block` / `if` / `try` value-carry rules, the explicit loop exclusion, the local/global propagation story, and the direct call/return retagging surface.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `type-ssa` is now a justified upstream-only tracker expansion and no longer an uncovered neighboring pass mention.

## [2026-04-21] research | deepen Binaryen `constant-field-null-test-folding` with exact matcher and nonnullable-`ref.test` source confirmation

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/constant-field-null-test-folding/` folder, then chose `constant-field-null-test-folding` / upstream `cfp-reftest` as an explicitly justified already-dossier follow-up because the tracker had no obvious remaining `none` targets while this closed-world registry dossier still lacked one compact source-confirmed page for the exact two-bucket matcher, subtype partition proof, nullable-base repair, and feature gate that make the pass much narrower than its local name suggests.
- Added `docs/wiki/raw/research/0216-2026-04-21-constant-field-null-test-folding-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`ConstantFieldPropagation.cpp`, `pass.cpp`, `possible-constant.h`, `struct-utils.h`, `cfp-reftest.wast`, `cfp.wast`, and `gto_and_cfp_in_O.wast`) and making explicit that the real matcher requires `values.size() == 2`, one classifier bucket per side, one subtype discriminator, optional `ref.as_non_null` repair, and a feature-sensitive nonnullable-`ref.test` bailout before emitting `select(ref.test(...))`.
- Added `docs/wiki/binaryen/passes/constant-field-null-test-folding/two-bucket-subtype-partitions-and-nonnullable-ref-test-gates.md`, and refreshed `index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md`, so the living dossier now has a canonical focused home for the exact matcher mechanics, the nullable-base/nonnullable-test split, the tiny dedicated `cfp-reftest.wast` proof surface, and the stronger warning that this is not a generic null-test optimizer.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `constant-field-null-test-folding` folder's source-confirmation gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | add Binaryen `string-lowering` dossier for the upstream externref/import/custom-section lowering family

- Added the raw note `docs/wiki/raw/research/0215-2026-04-21-string-lowering-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`StringLowering.cpp`, `pass.cpp`, `passes.h`, `string-utils.h`, `string-gathering.wast`, `string-lowering.wast`, and `string-lowering.js`) and distilling the real contract: upstream `string-lowering` is a whole-module pass that runs the full `StringGathering` prefix first, rewrites `string` heap types to `extern`, lowers defining globals into imports plus either `string.consts` JSON metadata or magic imports, rewrites a narrow current helper/op subset, refinalizes, and disables the Strings feature.
- Added `docs/wiki/binaryen/passes/string-lowering/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `wat-shapes.md`, and `json-and-magic-imports.md`, so the living wiki now has a dedicated home for the split between plain `string-gathering` and full lowering, the ABI-visible type rewrite, the helper-import surface, the JSON-vs-magic-import modes, and the explicit upstream TODO boundaries for unsupported `string.new*` / `string.encode*` variants.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future threads can see that this was an intentional upstream-only tracker expansion justified by the neighboring string dossier dependency, not a random detour from the main no-DWARF / saved-`-O4z` queue.

## [2026-04-21] research | deepen Binaryen `reorder-functions-by-name` with a source-confirmed lexical-order proof page

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/reorder-functions-by-name/` folder, then chose `reorder-functions-by-name` as an explicitly justified already-dossier follow-up because the tracker had no obvious remaining `none` targets while this upstream-only registry folder still relied too much on source-inferred module-order examples instead of one compact source-confirmed page for the exact comparator, declaration-only boundary, and official lit proof surface.
- Added `docs/wiki/raw/research/0213-2026-04-21-reorder-functions-by-name-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`ReorderFunctions.cpp`, `pass.cpp`, and `reorder-functions-by-name.wast`, plus a current-`main` no-drift check) and making explicit that the real contract is exactly `std::sort(module->functions, ...)` with `a->name < b->name`, declaration-only `requiresNonNullableLocalFixups() == false`, and four lit-backed `$a/$b/$c` permutation proofs.
- Added `docs/wiki/binaryen/passes/reorder-functions-by-name/lexical-order-proof-and-boundaries.md`, and refreshed `index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `module-shapes.md`, so the living dossier now has a canonical focused home for the exact comparator, the declaration-only boundary, the direct lit-backed positive families, and the current-`main` no-drift result instead of leaving those facts scattered or partly inference-framed.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `reorder-functions-by-name` folder's source-confirmation gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | correct Binaryen `global-struct-inference-desc-cast` to the real singleton-descriptor-gate contract

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/` folder, then chose `global-struct-inference-desc-cast` / upstream `gsi-desc-cast` as an explicitly justified already-dossier follow-up because the tracker had no obvious remaining `none` targets while this upstream-only registry pass still had a real correctness gap in its living docs.
- Added `docs/wiki/raw/research/0212-2026-04-21-global-struct-inference-desc-cast-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`GlobalStructInference.cpp`, `pass.cpp`, `gsi-desc.wast`, `gsi-to-desc-cast.wast`, and `gsi.wast`, plus a current-`main` no-drift check) and making explicit that the real desc-cast rewrite is a narrow target-descriptor-singleton rule, not the richer cast-input-origin analysis the older dossier implied.
- Refreshed `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md`, and added `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/descriptor-singleton-gate-and-dedicated-tests.md`, so the living dossier now source-confirms the exact `visitRefCast` gates, the effectively closed-world-dependent desc-cast surface, the exact-vs-strict-subtype split, and the dedicated `gsi-to-desc-cast.wast` plus neighboring `gsi-desc.wast` proof map instead of citing only the broader `gsi.wast` family.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `global-struct-inference-desc-cast` folder's source-confirmation gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | deepen Binaryen `reorder-functions` with a source-confirmed count/ordering boundary page

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/reorder-functions/` folder, then chose `reorder-functions` as an explicitly justified already-dossier follow-up because the tracker had no obvious remaining `none` targets while this upstream-only folder still lacked one compact source-confirmed page that made the real count surfaces, serial-vs-parallel assembly split, tie-break rule, and omission boundaries easy to see at a glance.
- Added `docs/wiki/raw/research/0211-2026-04-21-reorder-functions-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`ReorderFunctions.cpp`, `pass.cpp`, and the sibling lit file, plus a current-`main` no-drift check) and making explicit that the real contract is a tiny pre-seeded count map plus function-parallel direct-`call` scan, serial start/export/element bumps, descending-name tie breaks, explicit `ref.func` / declaration TODO omissions, a declaration-only `requiresNonNullableLocalFixups() == false` boundary, and a raw-size-vs-gzip tradeoff note.
- Added `docs/wiki/binaryen/passes/reorder-functions/count-surfaces-ordering-and-omissions.md`, and refreshed the landing page plus strategy / implementation pages so the living dossier now has a canonical focused home for the exact count surface, the declaration-only boundary, and the still-open source TODOs instead of leaving those facts spread across broader prose.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `reorder-functions` folder's source-confirmation gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | deepen Binaryen `vacuum` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/vacuum/` folder, then chose `vacuum` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this deep dossier still lacked one compact source-confirmed page for the real owner-file split and shipped `vacuum-*` test surface.
- Added `docs/wiki/raw/research/0210-2026-04-21-vacuum-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`Vacuum.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, `branch-hints.h`, `drop.h`, and the shipped `vacuum-*` lit roster, plus the existing current-`main` freshness correction) and making explicit that the pass's real contract is concentrated in `Vacuum.cpp`, with visible helper ownership mainly in branch-hint repair, dropped-children rebuilding, public registration, and nested-rerun context.
- Added `docs/wiki/binaryen/passes/vacuum/implementation-structure-and-tests.md`, and refreshed the landing page plus strategy page so the living dossier now has a canonical compact owner/test-map page instead of leaving the file/test surface implied across broader prose.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `vacuum` folder's implementation/test-map gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | deepen Binaryen `global-refining` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/global-refining/` folder, then chose `global-refining` as an explicitly justified major-gap follow-up because the tracker had no obvious remaining `none` targets while this deep dossier still lacked one compact source-confirmed page for the real owner-file map, helper ownership, and shipped test surface.
- Added `docs/wiki/raw/research/0208-2026-04-21-global-refining-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`GlobalRefining.cpp`, `pass.cpp`, `lubs.h`, `public-type-validator.h`, and `global-refining.wast`, plus a current-`main` no-drift check) and making explicit that the real contract is a tiny GC-gated module rewrite over `global.set` collection, initializer-plus-write LUB aggregation, export legality filtering, and `global.get` retagging in both functions and module code.
- Added `docs/wiki/binaryen/passes/global-refining/implementation-structure-and-tests.md`, and refreshed the landing page plus strategy page so the living dossier now has a canonical compact owner/test-map page instead of leaving the file/test surface implied across broader prose.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `global-refining` folder's implementation/test-map gap is closed and future recursive campaign threads can see that it is no longer a good follow-up target for the same reason.

## [2026-04-21] research | add Binaryen `ssa` dossier for the full merge-owning sibling of `ssa-nomerge`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/ssa-nomerge/` folder, then chose `ssa` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the deep local `ssa-nomerge` dossier repeatedly depended on explaining the full-`ssa` sibling without giving it a canonical home of its own.
- Added `docs/wiki/raw/research/0207-2026-04-21-ssa-binaryen-research.md`, grounding the new dossier in official Binaryen `version_129` sources (`SSAify.cpp`, `pass.cpp`, `passes.h`, `local-graph.h`, `LocalGraph.cpp`, `ReFinalize.cpp`, `test/lit/passes/ssa.wast`, and `test/gtest/local-graph.cpp`, plus a current-`main` no-drift check) and making explicit that full `ssa` is the same shared `SSAify.cpp` engine as `ssa-nomerge`, but with merge-local materialization, incoming `local.tee` rewrites, and parameter-entry prepends enabled.
- Added `docs/wiki/binaryen/passes/ssa/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `merge-locals-entry-prepends-and-default-values.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the full-`ssa` sibling split, the direct-vs-source-derived test coverage distinction, the LocalGraph-driven merge-local algorithm, and the key beginner-facing difference from `ssa-nomerge`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record the justified upstream-only `ssa` expansion and future recursive campaign threads can see that this sibling is no longer an uncovered target.

## [2026-04-21] research | deepen Binaryen `string-gathering` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/string-gathering/` folder, then chose `string-gathering` as an eligible still-underdocumented pass because it was not on the thread's exclusion list, it still sat at dossier level in the tracker, and the folder still lacked one compact source-confirmed page for the real owner-file map, exact implementation phases, and shipped test surface.
- Added `docs/wiki/raw/research/0206-2026-04-21-string-gathering-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`StringLowering.cpp`, `pass.cpp`, `passes.h`, `string-utils.*`, `module-utils.h`, `wasm-traversal.h`, and `test/lit/passes/string-gathering.wast`, plus a current-`main` spot check) and making explicit that the real pass is a small late `StringConst`-slot scan + canonical immutable-global + validity-first reorder + `global.get` rewrite pass rather than a vague generic string optimizer.
- Added `docs/wiki/binaryen/passes/string-gathering/implementation-structure-and-tests.md`, and refreshed the landing page plus strategy page so the living dossier now points readers at the shared `StringLowering.cpp` owner-file structure, the exact-slot `StringConst` scan, the split between directly tested global-initializer/reuse/reorder families and broader source-derived `walkModuleCode(...)` coverage, and the dedicated lit proof surface.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `string-gathering` folder's owner/test-map gap is closed and future recursive campaign threads can see that this dossier is no longer missing that clarification.

## [2026-04-21] research | correct Binaryen `duplicate-import-elimination` to the real `version_129` function-import-only pass

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/duplicate-import-elimination/` folder, then chose `duplicate-import-elimination` as an eligible still-underdocumented pass because it was not on the thread's exclusion list, it still had only a dossier-level unimplemented folder, and the existing living pages were teaching a materially over-broad all-import contract that did not match Binaryen `version_129` source.
- Added `docs/wiki/raw/research/0205-2026-04-21-duplicate-import-elimination-source-confirmation-followup.md`, grounding the correction in official Binaryen `version_129` sources (`DuplicateImportElimination.cpp`, `pass.cpp`, `opt-utils.h`, `import-utils.h`, and the shipped `test/passes/duplicate-import-elimination.{wast,txt}` pair, plus a current-`main` no-drift check) and making explicit that the real pass is a tiny late duplicate imported-function cleanup keyed by `(module, base)` plus exact function-type equality, not a broad function/global/table/memory import deduplicator.
- Added `docs/wiki/binaryen/passes/duplicate-import-elimination/implementation-structure-and-tests.md`, and rewrote the landing page, strategy page, identity/rewrite page, and WAT-shape page so the living dossier now points readers at the real owner-file/test map, the real function-only rewrite surface (`call`, `ref.func`, module-code function refs, start, and function exports), and the explicit non-goals around globals, tables, memories, and tags.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that this dossier's earlier all-import misconception is closed and future recursive campaign threads can see that `duplicate-import-elimination` is no longer a good candidate for the same source-confirmation correction.

## [2026-04-21] research | deepen Binaryen `memory-packing` with a source-confirmed implementation/test map

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/memory-packing/` folder, then chose `memory-packing` as an explicitly justified major-gap follow-up because the tracker no longer had obvious `none` targets while this implemented dossier still lacked one compact page for the real owner-phase split and shipped lit coverage in Binaryen `version_129`.
- Added `docs/wiki/raw/research/0204-2026-04-21-memory-packing-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`MemoryPacking.cpp`, `pass.cpp`, and the shipped `memory-packing_all-features`, `memory-packing_traps`, `memory-packing_zero-filled-memory`, `memory-packing_zero-filled-memory64`, `memory-packing_memory64-high-addr`, and `memory-packing-gc` lit files, plus a current-`main` no-drift check) and making explicit that the pass is a small module pipeline over legality gating, early segment-op cleanup, referrer discovery, dead passive-segment cleanup, split-range analysis, split emission, and passive replacement planning.
- Added `docs/wiki/binaryen/passes/memory-packing/implementation-structure-and-tests.md`, and refreshed the landing page, strategy page, segment-op/trap page, and WAT-shape page so the living dossier now points future readers at a compact source-confirmed owner map instead of leaving the implementation structure spread across broad prose.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `memory-packing` folder's former implementation/test-map gap is closed and future recursive campaign threads can see that this pass is no longer a good default major-gap fallback for the same reason.

## [2026-04-21] research | correct Binaryen `dead-code-elimination` to the real `version_129` `TypeUpdater`-centered unreachable-shape pass

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/dead-code-elimination/` folder, then chose `dead-code-elimination` as an explicitly justified major-gap follow-up because the tracker had no obvious `none` targets while this deep dossier still lacked a source-confirmed implementation/test-map page and, worse, still taught an over-broad local story that did not match Binaryen `version_129` source.
- Added `docs/wiki/raw/research/0203-2026-04-21-dead-code-elimination-source-confirmation-followup.md`, grounding the correction in official Binaryen `version_129` sources (`DeadCodeElimination.cpp`, `pass.cpp`, and the shipped `dce_all-features`, `dce_vacuum_remove-unused-names`, `dce-eh`, `dce-eh-legacy`, and `dce-stack-switching` lit files, plus a current-`main` no-diff check) and making explicit that the real pass is a small function-parallel postwalk over `TypeUpdater` state, dead-suffix trimming, control-type-to-`unreachable` rewrites, and the exact `hasPop && addedBlock` EH-pop repair trigger.
- Added `docs/wiki/binaryen/passes/dead-code-elimination/implementation-structure-and-tests.md`, and rewrote the landing page, strategy page, typed-control/EH page, and WAT-shape page so the living dossier now corrects the old non-source-backed claims about helper walkers, `EffectAnalyzer`, broad dead-result `drop` simplification, flattening, refinalization, and nondefaultable-local repair.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `dead-code-elimination` folder's implementation/test-map gap is closed and, more importantly, that the earlier over-broad local description should no longer send future recursive campaign threads back into the same misconception.

## [2026-04-21] research | deepen Binaryen `once-reduction` with a source-confirmed implementation/test map and exact phase ownership

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/once-reduction/` folder, then chose `once-reduction` as an explicitly justified major-gap follow-up because the tracker had no obvious `none` targets while this deep dossier still lacked one compact source-confirmed page for the exact implementation/test map and the real ownership split between `Scanner`, the top-level fixed-point driver, `Optimizer`, and `optimizeOnceBodies(...)`.
- Added `docs/wiki/raw/research/0202-2026-04-21-once-reduction-implementation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`OnceReduction.cpp`, `pass.cpp`, `intrinsics.h`, and `once-reduction.wast`, plus a current-`main` no-diff check) and making explicit that the pass's core contract is not just “scan and remove repeated calls,” but a four-owner module pass over shared `OptInfo` state, immediate-dominator propagation, summary-cardinality fixed-point convergence, fake-global idempotent support, and deterministic wrapper-cycle cleanup.
- Added `docs/wiki/binaryen/passes/once-reduction/implementation-structure-and-tests.md`, and refreshed the folder landing page, strategy page, and WAT-shape page so the living dossier now records the exact file/test ownership, the entry-block-only function-summary rule, the shared direct-call-plus-`global.set` redundancy helper, the source-backed but easy-to-miss `@binaryen.idempotent` extension, and the dedicated lit file's extra try/catch CFG robustness surface.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `once-reduction` folder's implementation-structure/test-map gap is closed and future recursive campaign threads can see that this pass no longer needs the same follow-up.

## [2026-04-21] research | deepen Binaryen `merge-similar-functions` with exact equivalence-class, diff-vector, and thunk-rewrite mechanics

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/merge-similar-functions/` folder, then chose `merge-similar-functions` as an explicitly justified major-gap follow-up because the tracker had no obvious `none` targets while this dossier still lacked one source-confirmed page for the exact core mechanics a future port could easily mis-implement: same-hash versus same-class, lockstep diff derivation, local-index shifting, and thunk replacement.
- Added `docs/wiki/raw/research/0201-2026-04-21-merge-similar-functions-mechanics-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`MergeSimilarFunctions.cpp`, `pass.cpp`, and the shipped lit roster, plus a current-`main` no-diff check) and making explicit that the pass is not just “helper plus thunks,” but a narrow hash-then-classify-then-lockstep-diff-derive-then-clone-and-shift-locals algorithm.
- Added `docs/wiki/binaryen/passes/merge-similar-functions/equivalence-classes-param-derivation-and-thunk-rewrites.md`, and refreshed the folder landing page, strategy page, WAT-shape page, and supporting pages so the living dossier now source-confirms the exact `DeepValueIterator` walk, exact diff-vector reuse keyed by full per-function vectors, `call_ref` / `return_call_ref` helper reconstruction, old-local reindexing after synthetic params are appended, and original-name-preserving thunk replacement.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `merge-similar-functions` folder's remaining mechanics gap is closed and future recursive campaign threads can see that this dossier is no longer missing the same algorithm-level clarification.

## [2026-04-21] research | deepen Binaryen `reorder-types` by replacing the old working-contract guesswork with source-confirmed `version_129` behavior

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/reorder-types/` folder, then chose `reorder-types` as an explicitly justified major-gap fallback because the tracker no longer had obvious `none` targets while this dossier still labeled itself a working contract, still lacked a dedicated implementation/test map, and still left the real ordering-unit / public-private / cost-model questions open.
- Added `docs/wiki/raw/research/0199-2026-04-21-reorder-types-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`ReorderTypes.cpp`, `pass.cpp`, `type-updating.*`, `module-utils.*`, `wasm-type-ordering.h`, and `reorder-types.wast`) and making explicit that the pass is a small GC-only, `--closed-world`, private-types-only module rewrite using exact predecessor edges from private supertypes and described types plus a 21-factor successor-weight search over encoded type-index cost.
- Rewrote `docs/wiki/binaryen/passes/reorder-types/index.md`, `binaryen-strategy.md`, `ordering-cost-model-and-boundaries.md`, and `wat-shapes.md`, and added `implementation-structure-and-tests.md`, so the living dossier now records the real `GlobalTypeRewriter` contract: private/public candidate split, single-large-private-rec-group rebuild, full declaration/code/metadata remap surface, the hidden `reorder-types-for-testing` sibling, and the shipped lit coverage for successor-weight tie breaks plus the used-IR-vs-binary-type regression.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `reorder-types` folder's old source-confirmation gap is closed and future recursive campaign threads can see that it is no longer a good major-gap fallback for the same reason.

## [2026-04-21] research | deepen Binaryen `precompute-propagate` with the real local-worklist / fallthrough / merge-consensus contract

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, the existing `docs/wiki/binaryen/passes/precompute-propagate/` folder, and official Binaryen `version_129` sources (`Precompute.cpp`, `pass.cpp`, `opt-utils.h`, `local-graph.h`, `precompute-propagate-partial.wast`, and `precompute-propagate_all-features.wast`), then chose `precompute-propagate` as an explicitly justified major-gap fallback because the tracker no longer had obvious `none` targets while this dossier still compressed the real extra phase into a vague `LazyLocalGraph` note and still lacked one focused page for the actual `propagateLocals(...)` algorithm.
- Added `docs/wiki/raw/research/0198-2026-04-21-precompute-propagate-worklist-followup.md`, grounding the follow-up in official Binaryen `version_129` and making explicit that the pass is neither a generic SCCP engine nor a mere scheduler alias, but a shared semantic-precompute core plus a small set/get influence worklist: set fallthrough-value analysis, subtype filtering, all-reaching-sets consensus for gets, defaultable-local entry zeros, param and nondefaultable-local bailouts, and exactly one extra evaluator rerun.
- Added `docs/wiki/binaryen/passes/precompute-propagate/local-worklist-fallthrough-and-merge-boundaries.md`, and refreshed the folder landing page, strategy page, and implementation/test map so the living dossier now records the exact local-worklist topology, the stop-after-one-extra-walk rule, the temp heap-cache boundary behind `precompute-propagate-partial.wast`, and the broader behavior coverage proven by `precompute-propagate_all-features.wast`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that this folder's former vague-extra-phase teaching gap is closed and future recursive campaign threads can see that `precompute-propagate` is no longer a good major-gap fallback for the same reason.

## [2026-04-21] research | deepen Binaryen `i64-to-i32-lowering` with a dedicated ABI/opcode coverage ledger

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, the existing `docs/wiki/binaryen/passes/i64-to-i32-lowering/` folder, and official Binaryen `version_129` sources (`I64ToI32Lowering.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `abi/js.h`, `shared-constants.h`, helper headers, and `flatten_i64-to-i32-lowering.wast`), then chose `i64-to-i32-lowering` as an eligible upstream-only dossier with a real remaining teaching gap: the folder already explained the big picture, but it still lacked one compact source-backed page answering exactly which ABI surfaces and opcode families are lowered directly, helper-backed, fallback-only, or still explicitly unsupported.
- Added `docs/wiki/raw/research/0197-2026-04-21-i64-to-i32-lowering-abi-and-coverage-followup.md`, grounding the follow-up in official Binaryen `version_129` plus a current-`main` spot check and making explicit that this pass is neither a tiny arithmetic rewrite nor a universal arbitrary-i64 legalizer, but a flat-input whole-module ABI-and-expression rewrite with several helper-backed and assumed-gone families.
- Added `docs/wiki/binaryen/passes/i64-to-i32-lowering/abi-surface-and-opcode-coverage.md`, and refreshed the folder landing page plus strategy page, so the living dossier now has a canonical compact ledger for defined-vs-imported globals, call/ref.func signature rewriting, the synthetic high-bits return global, direct pairwise opcode coverage, wasm2js-helper-backed families, and the explicit unsupported boundaries like imported i64 globals, i64 `return_call`, atomic cmpxchg, and the “should already be removed by now” ops.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that the `i64-to-i32-lowering` folder's ABI/opcode coverage gap is closed and future recursive campaign threads can see that this pass no longer needs the same clarification follow-up.

## [2026-04-21] research | deepen Binaryen `propagate-globals-globally` with the real shared `SimplifyGlobals.cpp` engine and startup-boundary contract

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/propagate-globals-globally/` folder, then chose `propagate-globals-globally` as an explicitly justified major-gap fallback because the tracker no longer had clear `none` targets while this dossier still taught the wrong upstream source layout and still lacked a focused page for the real shared-engine/startup-boundary split.
- Added `docs/wiki/raw/research/0196-2026-04-21-propagate-globals-globally-shared-engine-research.md`, grounding the follow-up in official Binaryen `version_129` sources (`SimplifyGlobals.cpp`, `pass.cpp`, and `propagate-globals-globally.wast`, plus a current-`main` spot check) and correcting the old folder's biggest mistake: the public pass does not live in a standalone `PropagateGlobals.cpp`, but in the shared `PropagateGlobals` engine inside `SimplifyGlobals.cpp`, where `propagate-globals-globally` is the `optimize = false` startup-only sibling of `simplify-globals*`.
- Rewrote `docs/wiki/binaryen/passes/propagate-globals-globally/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md`, and added `shared-engine-and-startup-boundaries.md`, so the living dossier now documents the real family structure, the startup-safe expression subset, the expression-shaped constant map, the active data/elem offset rewrite surface, and the preserved function-body boundary in beginner-friendly language.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the shared catalogs now record that this folder's earlier source-layout gap is closed and future recursive campaign threads can see that `propagate-globals-globally` is no longer a good "major-gap fallback" candidate for the same reason.

## [2026-04-21] research | add Binaryen local `remove-unused` dossier for the historical `remove-unused-functions` lineage and its modern supersession

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the main parity queues plus the first registry-expansion wave were already dossier-covered while the short local boundary-only name `remove-unused` still had no dedicated living folder and no source-backed explanation of what upstream Binaryen surface it actually referred to.
- Added `docs/wiki/raw/research/0195-2026-04-21-remove-unused-binaryen-research.md`, grounding the local alias in official Binaryen history and current sources: older upstream Binaryen publicly exposed `remove-unused-functions` with a tiny start/export/table-rooted direct-call reachability implementation in `RemoveUnusedFunctions.cpp`, Binaryen replaced that pass in 2016 with `remove-unused-module-elements`, and current `version_129` exposes no public `remove-unused` or `remove-unused-functions` spelling at all.
- Added `docs/wiki/binaryen/passes/remove-unused/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `historical-lineage-and-modern-supersession.md`, and `module-shapes.md` so the living wiki now has a canonical home for the local alias, the historical upstream implementation contract, the supersession commit that introduced modern RUME, and the beginner-friendly module-shape comparison between the old function-only pass and the broader modern replacement.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the upstream-only registry section now includes `remove-unused`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this legacy-alias gap is no longer undocumented.

## [2026-04-21] research | add Binaryen `remove-unused-non-function-elements` / upstream `remove-unused-nonfunction-module-elements` dossier for the defined-functions-rooted RUME sibling

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and the existing `remove-unused-module-elements` dossier, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `remove-unused-non-function-elements` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's sibling pass that roots all defined functions before running the shared module-element pruner.
- Added `docs/wiki/raw/research/0194-2026-04-21-remove-unused-non-function-elements-binaryen-research.md`, grounding the sibling in official Binaryen `version_129` sources (`RemoveUnusedModuleElements.cpp`, `pass.cpp`, `passes.h`, the dedicated `remove-unused-nonfunction-module-elements_all-features.wast` file, and the neighboring shared RUME lit surfaces) and distilling the real contract: the upstream sibling is the same whole-module reachability engine as `remove-unused-module-elements`, but with `rootAllFunctions = true`, which force-keeps defined functions while still allowing imported-function, function-type, start-metadata, and non-function cleanup.
- Added `docs/wiki/binaryen/passes/remove-unused-non-function-elements/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `shared-engine-rooting-and-defined-vs-imported-functions.md`, and `module-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the shared-engine story, the defined-vs-imported function boundary, the dedicated all-features test surface, and the beginner-friendly module-shape catalog.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `remove-unused-non-function-elements`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `type-un-finalizing` / upstream `type-unfinalizing` dossier for the private type-reopening sibling

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and the neighboring `type-finalizing` plus GC/type dossiers, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `type-un-finalizing` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's separate private type-reopening sibling.
- Added `docs/wiki/raw/research/0193-2026-04-21-type-un-finalizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`TypeFinalizing.cpp`, `pass.cpp`, and the shared `type-finalizing.wast` lit file, plus neighboring helper contracts already documented from official sources in the type dossiers) and distilling the real contract: upstream `type-unfinalizing` is the same GC-only module rewrite engine as `type-finalizing`, but it reopens all private heap types through one `GlobalTypeRewriter` `setOpen(true)` toggle without the leaf-only restriction.
- Added `docs/wiki/binaryen/passes/type-un-finalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `private-boundaries-sibling-split-and-no-leaf-rule.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the private-only boundary, the absence of a leaf-only rule, the shared-engine story, and the beginner-friendly positive/preserved/bailout module-shape catalog.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `type-un-finalizing`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `type-finalizing` dossier for the upstream GC type-state cleanup pass

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and neighboring GC/type dossiers, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `type-finalizing` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's tiny final/open heap-type cleanup family.
- Added `docs/wiki/raw/research/0192-2026-04-21-type-finalizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`TypeFinalizing.cpp`, `pass.cpp`, and `type-finalizing.wast`, plus shared helper contracts already documented from official sources in the neighboring type dossiers) and distilling the real contract: upstream `type-finalizing` is a GC-only module pass that toggles only private leaf heap types to final through one `GlobalTypeRewriter` `setOpen(...)` rewrite, while the shared sibling engine behind local `type-un-finalizing` / upstream `type-unfinalizing` reopens private types instead.
- Added `docs/wiki/binaryen/passes/type-finalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `leaf-types-public-boundaries-and-sibling-split.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the public-vs-private boundary, the leaf-only finalization rule, the sibling naming split, the absence of a closed-world gate or AST refinalization walk, and the beginner-friendly positive/preserved/bailout module-shape catalog.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `type-finalizing`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this pass is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `type-generalizing` dossier for the upstream experimental closed-world type-precision family

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the neighboring GC/type dossiers, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `type-generalizing` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's experimental `type-generalizing` family.
- Added `docs/wiki/raw/research/0191-2026-04-21-type-generalizing-binaryen-research.md`, grounding the family in official Binaryen `version_129` sources (`TypeGeneralizing.cpp`, `pass.cpp`, `possible-contents.h`, `lubs.h`, and the shipped `type-generalizing*.wast` lit files) and distilling the real contract: the local registry alias `type-generalizing` maps to the upstream experimental pair `experimental-type-generalizing` and `experimental-type-generalizing-with-optimizing-casts`, whose real `version_129` behavior is a tiny closed-world `ContentOracle` consumer over `struct.get`, `struct.set`, `call_ref`, and optional cast-target tightening, followed by refinalization.
- Added `docs/wiki/binaryen/passes/type-generalizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `call-ref-casts-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the exact two-sibling family identity, the one-signature `call_ref` rule, the impossible-target `unreachable` rewrite, the cast-tightening-only sibling split, and the beginner-friendly positive/preserved/bailout shape catalog.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `type-generalizing`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this family is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `gufa-cast-all` dossier for the upstream cast-materializing GUFA sibling

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the broader `gufa` / `gufa-optimizing` folders, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `gufa-cast-all` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's separate cast-owning GUFA sibling.
- Added `docs/wiki/raw/research/0190-2026-04-21-gufa-cast-all-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`GUFA.cpp`, `pass.cpp`, `possible-contents.h`, and `gufa-cast-all.wast`, plus the neighboring `gufa` / `gufa-optimizing` lit surfaces) and distilling the real contract: upstream `gufa-cast-all` is the same whole-program `ContentOracle` rewrite engine as plain `gufa`, but with `optimizing = false, castAll = true`, which means Binaryen runs a post-refinalize `addNewCasts` walk that inserts fresh `ref.cast`s where the oracle knows a narrower castable type instead of running the optimizing sibling's nested cleanup rerun.
- Added `docs/wiki/binaryen/passes/gufa-cast-all/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `cast-insertion-exactness-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the separate public-pass identity, the shared `GUFA.cpp` engine, the exact cast-owning sibling split, the feature-sensitive exactness rules, and the beginner-friendly positive/preserved/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `gufa-cast-all`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer just an incidental note inside the broader `gufa` dossier.

## [2026-04-21] research | add Binaryen `gufa-optimizing` dossier for an upstream cleanup-owning GUFA sibling

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the broader `gufa` folder, then explicitly widened the tracker again because the main parity queues plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `gufa-optimizing` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's separate cleanup-owning sibling of `gufa`.
- Added `docs/wiki/raw/research/0189-2026-04-21-gufa-optimizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`GUFA.cpp`, `pass.cpp`, `possible-contents.h`, `gufa-optimizing.wast`, plus the neighboring `gufa` / `gufa-cast-all` lit surfaces) and distilling the real contract: upstream `gufa-optimizing` is the same whole-program `ContentOracle` rewrite engine as plain `gufa`, but with `optimizing = true`, which means Binaryen runs nested `dce` and `vacuum` on functions the GUFA rewrite phase actually changed.
- Added `docs/wiki/binaryen/passes/gufa-optimizing/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `cleanup-rerun-contract.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the separate public-pass identity, the shared `GUFA.cpp` engine, the exact changed-functions-only cleanup rerun contract, the split from `gufa-cast-all`, and the beginner-friendly positive/preserved/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `gufa-optimizing`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer just an incidental note inside the broader `gufa` dossier.

## [2026-04-21] research | add Binaryen `reorder-globals-always` dossier for an upstream small-module/test/internal-fixup sibling

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the neighboring `reorder-globals` dossier and raw note, the local registry in `src/passes/optimize.mbt`, and confirmed that `agent-todo.md` still has no dedicated `reorder-globals-always` slice, then chose `reorder-globals-always` as a justified tracker expansion because the main parity queues are already dossier-covered while this real local-registry sibling still lacked its own landing folder.
- Added the raw note `docs/wiki/raw/research/0188-2026-04-21-reorder-globals-always-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `wasm-traversal.h`, `topological_sort.h`, `wasm.h`, `GlobalStructInference.cpp`, and the shipped `reorder-globals{,-real}.wast` tests) and distilling the real contract: `reorder-globals-always` is the same global-ordering engine as `reorder-globals`, but with the public `< 128` bailout removed and a smooth synthetic scoring model, making it both the main small-module teaching surface and the real nested fixup helper after `GlobalStructInference` creates fresh globals.
- Added `docs/wiki/binaryen/passes/reorder-globals-always/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the sibling's separate pass identity, shared `ReorderGlobals.cpp` engine, exact small-module and scoring-model split, and beginner-friendly module-shape catalog.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `reorder-globals-always`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer just an incidental note inside the broader `reorder-globals` dossier.

## [2026-04-21] research | source-confirm Binaryen `reorder-globals-always` cutoff, smooth scoring, and proof surface

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the existing `docs/wiki/binaryen/passes/reorder-globals-always/` folder, then chose `reorder-globals-always` as a justified major-gap follow-up because the tracker no longer had an obvious remaining `none` target while this dossier still lacked one compact page proving the exact `< 128` cutoff removal, exact `1.0 + (i / 128.0)` formula, strongest lit-backed proof families, explicit nested `GlobalStructInference` caller, and current-`main` no-drift result.
- Added the raw note `docs/wiki/raw/research/0214-2026-04-21-reorder-globals-always-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources plus a current-`main` freshness check and distilling the exact contract: `reorder-globals-always` is still the same shared `ReorderGlobals.cpp` engine, but it only differs from production mode by skipping the `globals.size() < 128 && !always` bailout and using the exact smooth cost factor `1.0 + (i / 128.0)` while preserving the same imports-first, dependency-safe, original-index-tie ordering rules.
- Added `docs/wiki/binaryen/passes/reorder-globals-always/small-module-threshold-scoring-and-proof.md` and refreshed `docs/wiki/binaryen/passes/reorder-globals-always/{index.md,binaryen-strategy.md,implementation-structure-and-tests.md}` so the living dossier now teaches the exact cutoff split, exact formula, strongest `reorder-globals.wast` / `reorder-globals-real.wast` proof split, explicit nested `GlobalStructInference` caller, and current-`main` no-drift result in one canonical place.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future campaign threads can see that this source-confirmation gap inside `reorder-globals-always` is now closed.

## [2026-04-21] research | add Binaryen `monomorphize-always` as a source-backed upstream-only boundary-only sibling dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `monomorphize-always` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's separate usefulness-disabled sibling of `monomorphize`.
- Added the raw note `docs/wiki/raw/research/0187-2026-04-21-monomorphize-always-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Monomorphize.cpp`, `pass.cpp`, the real helper headers around cost/effects/return handling, and the `monomorphize-benefit` / `context` / `drop` / `types` lit surfaces) and distilling the real contract: upstream `monomorphize-always` is the same direct-call contextual-specialization engine as `monomorphize`, but with the usefulness rejection removed rather than with the legality or triviality checks removed.
- Added `docs/wiki/binaryen/passes/monomorphize-always/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `usefulness-gate-and-sibling-split.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the separate public-pass identity, the shared-engine story, the exact usefulness-gate split, and the beginner-friendly weak-benefit positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `monomorphize-always`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer just an incidental note inside the broader `monomorphize` dossier.

## [2026-04-21] research | add Binaryen `simplify-locals-nonesting` / local `simplify-locals-no-nesting` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `simplify-locals-nonesting` or `simplify-locals-no-nesting` slice, and the local removed registry still exposed a real documentation gap around Binaryen's flatness-preserving public locals-family variant.
- Added the raw note `docs/wiki/raw/research/0186-2026-04-21-simplify-locals-nonesting-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, the dedicated `simplify-locals-nonesting` pass pair, and the `flatten_simplify-locals-nonesting_*` combo files) and distilling the real contract: upstream `simplify-locals-nonesting` is the exact `SimplifyLocals<false, false, false>` sibling tracked locally as `simplify-locals-no-nesting`, so it still performs flat copy cleanup plus late equivalent/dead-set cleanup while forbidding new tees, new structure, and new nesting.
- Added `docs/wiki/binaryen/passes/simplify-locals-nonesting/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flatness-variant-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the upstream-vs-local naming split, the exact family identity, the `preserves flatness` contract, the dedicated and combo test surfaces, the contrast against `simplify-locals-notee-nostructure`, and the beginner-friendly positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `simplify-locals-nonesting`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `untee` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `untee` slice, and the local removed registry still exposed a real documentation gap around Binaryen's standalone tee-desugaring pass.
- Added the raw note `docs/wiki/raw/research/0185-2026-04-21-untee-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Untee.cpp`, `pass.cpp`, `passes.h`, `SimplifyLocals.cpp`, and the dedicated `untee.wast` lit file) and distilling the real contract: upstream `untee` is a tiny tee-only postwalk that expands reachable `local.tee` nodes into explicit `local.set` plus `local.get`, deletes unreachable tees entirely, and sits beside rather than inside the broader `simplify-locals*` family.
- Added `docs/wiki/binaryen/passes/untee/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flattening-code-pushing-and-tee-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the tee-only rewrite surface, the postorder nested-expansion rule, the `code-pushing` interaction story, the `simplify-locals-notee` contrast, and the beginner-friendly positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `untee`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `de-nan` / upstream `denan` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `de-nan` / `denan` slice, and the local removed registry still exposed a real documentation gap around Binaryen's NaN-determinization instrumentation pass.
- Added the raw note `docs/wiki/raw/research/0184-2026-04-21-de-nan-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DeNaN.cpp`, `pass.cpp`, `properties.h`, `names.h`, and the dedicated `denan.wast` lit file) and distilling the real contract: upstream `denan` is a runtime NaN-to-zero instrumentation pass tracked locally as `de-nan`, with constant-only global repair, function-entry param sanitization, selective producer wrapping, `local.get` / result-fallthrough skip rules, and a subtle SIMD helper strategy rather than ordinary floating optimization.
- Added `docs/wiki/binaryen/passes/de-nan/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `helper-functions-fallthrough-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the helper-function contract, the nested `merge-blocks` cleanup, the global-context and imported-function boundaries, and the main beginner-friendly positive/preserved/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `de-nan` / `denan`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `rereloop` / local `re-reloop` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `rereloop` / `re-reloop` slice, and the local removed registry still exposed a real documentation gap around Binaryen's post-`flatten` control-flow restructuring pass.
- Added the raw note `docs/wiki/raw/research/0183-2026-04-21-rereloop-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ReReloop.cpp`, `pass.cpp`, `cfg/Relooper.h`, `cfg/Relooper.cpp`, `ir/flat.h`, `flatten_rereloop.wast`, and `opt_flatten.wast`) and distilling the real contract: upstream `rereloop` is a Flat-IR-only CFG rebuild plus generic `Relooper` rendering pass, tracked locally under the alias `re-reloop`, not a vague “reverse flatten” cleanup or a default no-DWARF preset slot.
- Added `docs/wiki/binaryen/passes/rereloop/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flat-cfg-builder-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the hard flatness precondition, the task-based CFG builder, the helper-label-local render contract, the explicit EH unsupported boundary, the dead-end terminator repair, and the main beginner-friendly positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `rereloop` / `re-reloop`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `const-hoisting` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `const-hoisting` slice, and the local removed registry still exposed a real documentation gap around Binaryen's small repeated-literal size pass.
- Added the raw note `docs/wiki/raw/research/0182-2026-04-21-const-hoisting-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ConstHoisting.cpp`, `pass.cpp`, `insert_ordered.h`, `wasm-binary.h`, `wasm-builder.h`, and the dedicated `const-hoisting.wast` lit file) and distilling the real contract: upstream `const-hoisting` is byte-model-driven temp-local hoisting of repeated `Const` literals inside one function rather than generic constant propagation, CSE, or runtime optimization.
- Added `docs/wiki/binaryen/passes/const-hoisting/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `size-model-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the exact profitability formula and thresholds, the fresh-local entry-prelude rewrite shape, the unsupported `v128` boundary, the raw-size-versus-gzip-size warning, and the main beginner-friendly positive/preserved/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `const-hoisting`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `type-merging` as a source-backed upstream-only boundary-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `type-merging` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's late closed-world GC type-compaction pass.
- Added the raw note `docs/wiki/raw/research/0181-2026-04-21-type-merging-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`TypeMerging.cpp`, `pass.cpp`, `wasm-type-ordering.h`, `dfa_minimization.h`, `module-utils.h`, `type-updating.h`, and the dedicated `type-merging.wast` lit file) and distilling the real contract: upstream `type-merging` is a cast-aware supertype-then-sibling partition-refinement merge pass over live private heap types, with descriptor-chain coupling and post-merge refinalization, not dead-type cleanup or simple declaration deduplication.
- Added `docs/wiki/binaryen/passes/type-merging/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `dfa-partitions-casts-and-refinalization.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the hard closed-world+GC gate, the private-type candidate rule, the DFA partition model, the cast/exactness and public-visibility boundaries, the descriptor-chain rewrite contract, and the main beginner-friendly positive/bailout module shapes.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `type-merging`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `reorder-functions-by-name` as a source-backed upstream-only boundary-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the freshly landed `reorder-functions` dossier, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `reorder-functions-by-name` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's separate lexical/debugging sibling pass.
- Added the raw note `docs/wiki/raw/research/0180-2026-04-21-reorder-functions-by-name-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ReorderFunctions.cpp`, `pass.cpp`, and the dedicated `reorder-functions-by-name.wast` lit file) and distilling the real contract: upstream `reorder-functions-by-name` is a separate public debugging-oriented pass that just sorts `module->functions` by ascending internal name and leaves function bodies alone.
- Added `docs/wiki/binaryen/passes/reorder-functions-by-name/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `module-shapes.md` so the living wiki now has a canonical home for the pure ascending-name comparator, the dedicated lit coverage, the declaration-order-only contract, and the explicit split from count-based `reorder-functions`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `reorder-functions-by-name`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this sibling is no longer just an incidental mention inside the `reorder-functions` dossier.

## [2026-04-21] research | add Binaryen `reorder-functions` as a source-backed upstream-only boundary-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `reorder-functions` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's tiny function-section layout pass.
- Added the raw note `docs/wiki/raw/research/0179-2026-04-21-reorder-functions-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ReorderFunctions.cpp`, `pass.cpp`, the shipped `reorder-functions-by-name.wast` lit file, and a reviewed `test/lit/passes` directory listing) and distilling the real contract: upstream `reorder-functions` is a static-use-count declaration sorter over direct calls, start/export roots, and element-segment function names with descending-name tie breaks, not profile-guided hotness or body optimization.
- Added `docs/wiki/binaryen/passes/reorder-functions/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `module-shapes.md` so the living wiki now has a canonical home for the module-ordering contract, the sibling split from `reorder-functions-by-name`, the explicit `ref.func` / declaration TODO boundaries, and the beginner-friendly positive/surprising module-shape families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `reorder-functions`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `dataflow-optimization` / `dfo` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `dataflow-optimization` or `dfo` slice, and the local removed registry still exposed a real documentation gap around Binaryen's flat-IR-only DataFlow SSA side-graph pass.
- Added the raw note `docs/wiki/raw/research/0178-2026-04-21-dataflow-optimization-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DataFlowOpts.cpp`, `pass.cpp`, `dataflow/graph.h`, `dataflow/node.h`, `dataflow/users.h`, `dataflow/utils.h`, `ir/flat.h`, and the combo lit file `flatten_simplify-locals-nonesting_dfo_O3.wast`) and distilling the real contract: upstream `dfo` is a tiny flat-input integer-local DataFlow-IR worklist that collapses identical constant phis and all-constant expressions via nested `precompute`, not a broad generic optimizer despite the local name `dataflow-optimization`.
- Added `docs/wiki/binaryen/passes/dataflow-optimization/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flat-ir-dataflow-ir-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the hard flatness precondition, the side-IR node model, the loop-precision cutoff, the narrow actual rewrite surface, and the main positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `dataflow-optimization` / `dfo`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `inline-main` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `inline-main` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's tiny but public `main` / `__original_main` wrapper-cleanup pass.
- Added `docs/wiki/raw/research/0177-2026-04-21-inline-main-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Inlining.cpp`, `pass.cpp`, the small shared helper/dependency surface around `FindAllPointers`, branch/name repair, and local/type fixups, plus the dedicated `inline-main.wast` lit file) and distilling the real contract: `inline-main` is a standalone exact-one-direct-call wrapper inliner that reuses the shared inline-body helper rather than the ordinary heuristic planner.
- Added `docs/wiki/binaryen/passes/inline-main/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `special-case-contract-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the exact `main` / `__original_main` scope, the imported-endpoint and multi-call bailouts, the shared-helper return/label/local repair story, and the main positive/preserved WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `inline-main`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `monomorphize` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `monomorphize` slice, and the local boundary-only registry still exposed a real documentation gap around Binaryen's whole-module callsite-context specialization pass.
- Added `docs/wiki/raw/research/0176-2026-04-21-monomorphize-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Monomorphize.cpp`, `pass.cpp`, the real helper headers around cost/effects/module-copy/return handling, and the dedicated `monomorphize*` plus `no-inline-monomorphize-inlining` lit roster) and distilling the real contract: `monomorphize` is empirical reverse-inlined callee cloning based on movable callsite context, dropped-call result context, and nested usefulness measurement rather than ordinary inlining or type-only monomorphization.
- Added `docs/wiki/binaryen/passes/monomorphize/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `call-context-benefit-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the direct-call-only scope, effect-safe context builder, nested `-O3` usefulness gate, `MaxParams = 20` hard limit, and the main positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `monomorphize`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | deepen Binaryen `alignment-lowering` with exact chunk-matrix and unreachable-contract notes

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `alignment-lowering` folder, then kept the same pass because the earlier dossier still lacked one major porting-grade teaching surface: the exact helper byte-width/alignment matrix and operand-preserving unreachable rules implemented in upstream `AlignmentLowering.cpp`.
- Added `docs/wiki/raw/research/0200-2026-04-21-alignment-lowering-chunk-matrix-followup.md`, grounding the follow-up in official Binaryen `version_129` sources plus a fresh current-main comparison and distilling the new durable conclusion: Binaryen uses a very small exact integer helper matrix, chooses the largest naturally aligned chunk still allowed by the declared weaker alignment, and treats unreachable load/store rewrites as operand-preserving fast paths rather than generic cleanup.
- Added `docs/wiki/binaryen/passes/alignment-lowering/chunk-selection-and-unreachable-semantics.md` so the living dossier now has a dedicated mechanics page for the exact `2`/`4`-byte helper cases, the wrapper relation of `f32`/`i64`/`f64` to those helpers, the single-evaluation builder scaffolding, and the literal unreachable-load/store replacement rules.
- Updated `docs/wiki/binaryen/passes/alignment-lowering/index.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the shared catalogs now advertise that the folder covers the exact chunk matrix and unreachable contract, not just the broad scalar-misalignment summary.

## [2026-04-21] research | add Binaryen `alignment-lowering` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `alignment-lowering` slice, and the local boundary-only registry still exposed a real beginner-facing gap around Binaryen's standalone scalar misalignment-lowering pass.
- Added `docs/wiki/raw/research/0171-2026-04-21-alignment-lowering-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`AlignmentLowering.cpp`, `pass.cpp`, `pass.h`, `bits.h`, `passes.h`, and the dedicated `alignment-lowering.wast` lit file) and distilling the real contract: `alignment-lowering` is a narrow `Load` / `Store` legalization pass that preserves single evaluation with fresh temporaries, lowers scalar misaligned accesses into smaller aligned chunk operations, and handles signed, float, 64-bit, and unreachable cases with explicit helper logic instead of generic memory optimization.
- Added `docs/wiki/binaryen/passes/alignment-lowering/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the pass's scheduler status, narrow scalar scope, current-main-equals-`version_129` implementation check, exact chunked rewrite families, and the beginner-friendly positive/negative/bailout WAT shapes.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `alignment-lowering`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `global-struct-inference-desc-cast` / `gsi-desc-cast` dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the existing `global-struct-inference` dossier, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first tracker-expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `global-struct-inference-desc-cast` or `gsi-desc-cast` slice, and the local boundary-only registry still exposed a real documentation gap around the descriptor-cast sibling that had only been mentioned inside the broader `gsi` folder.
- Added `docs/wiki/raw/research/0170-2026-04-21-global-struct-inference-desc-cast-binaryen-research.md` to capture a source-backed tracker expansion for the local boundary-only pass name `global-struct-inference-desc-cast`, making explicit that the official upstream public pass is `gsi-desc-cast`, that the implementation is the shared `GlobalStructInference.cpp` engine rather than a separate cast pass, and that the real contract is a narrow descriptor-type / strict-subtype / trusted-global `ref.cast` rewrite surface rather than generic cast optimization.
- Added `docs/wiki/binaryen/passes/global-struct-inference-desc-cast/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the shared-GSI-engine identity, the piggyback `gsi.wast` test surface, and the beginner-friendly positive/bailout cast shapes around descriptor-driven specialization.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `global-struct-inference-desc-cast` / `gsi-desc-cast`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `constant-field-null-test-folding` / `cfp-reftest` dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the existing `constant-field-propagation` dossier, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `constant-field-null-test-folding` or `cfp-reftest` slice, and the local boundary-only registry still exposed a real documentation gap around the local-vs-upstream naming split for the narrow `cfp-reftest` variant.
- Added `docs/wiki/raw/research/0169-2026-04-21-constant-field-null-test-folding-binaryen-research.md` to capture a source-backed tracker expansion for the local boundary-only pass name `constant-field-null-test-folding`, making explicit that the official upstream public pass is `cfp-reftest`, that the implementation is the shared `ConstantFieldPropagation.cpp` engine with one extra `optimizeUsingRefTest(...)` mode, and that the real contract is exact-two-bucket subtype discrimination rather than generic null-test folding.
- Added `docs/wiki/binaryen/passes/constant-field-null-test-folding/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the shared-CFP-engine contract, the dedicated `cfp-reftest.wast` test surface, and the beginner-friendly positive/bailout field-read shapes around `select(ref.test(...))` synthesis.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `constant-field-null-test-folding` / `cfp-reftest`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `global-effects` / `generate-global-effects` dossier

- Added `docs/wiki/raw/research/0168-2026-04-21-global-effects-binaryen-research.md` to capture a source-backed tracker expansion for the local boundary-only pass name `global-effects`, making explicit that the official upstream public pass is `generate-global-effects`, that `agent-todo.md` currently has no dedicated slice for it, and that it matters mainly through downstream consumers rather than direct WAT rewrites.
- Added `docs/wiki/binaryen/passes/global-effects/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `metadata-naming-and-consumers.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the pass's non-default scheduler placement, shallow-plus-fixed-point call-graph algorithm, `Function.effects` metadata contract, and neighboring `simplify-locals` / `vacuum` consumer story.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the expanded upstream-only registry now includes `global-effects`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

# Wasm Knowledge Base Log

Append new entries; do not rewrite prior history except to fix obvious formatting mistakes or redact sensitive data.

## [2026-04-21] research | add Binaryen `legalize-js-interface` dossier for JS-boundary `i64` ABI legalization

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the neighboring `i64-to-i32-lowering` dossier, then chose `legalize-js-interface` as an explicitly justified upstream-only expansion because the tracker had no obvious remaining `none` targets while the existing whole-module `i64`-lowering docs already depended on teaching where earlier JS-boundary legalization happens without giving it a canonical home.
- Added `docs/wiki/raw/research/0223-2026-04-21-legalize-js-interface-binaryen-research.md`, grounding the dossier in official Binaryen `version_129` sources (`LegalizeJSInterface.cpp`, `pass.cpp`, and the dedicated `legalize-js-interface*` plus `legalize-and-prune-js-interface` lit files, with a current-`main` no-drift spot check) and making explicit that the real contract is wrapper-based import/export `i64` legalization with temp-ret helpers, import-use repair, optional `export-originals` preservation, and a separate prune-only sibling rather than whole-module integer lowering.
- Added `docs/wiki/binaryen/passes/legalize-js-interface/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `temp-ret-helpers-and-pruning-split.md`, and `wat-shapes.md`, so the living wiki now has a canonical home for the `legalstub$` / `legalimport$` / `legalfunc$` split, the temp-ret helper policy, the `export-originals` and `exported-helpers` pass arguments, the `call` / `ref.func` repair surface, and the exact split from both `i64-to-i32-lowering` and `legalize-and-prune-js-interface`.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so future recursive campaign threads can see that `legalize-js-interface` is now a justified upstream-only tracker expansion instead of an uncataloged neighboring-pass dependency.

## [2026-04-21] research | add Binaryen `untee` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first widened upstream-only wave were already dossier-covered, `agent-todo.md` still has no dedicated `untee` slice, and the local removed registry still exposed a real documentation gap around Binaryen's standalone tee-desugaring pass.
- Added the raw note `docs/wiki/raw/research/0185-2026-04-21-untee-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Untee.cpp`, `pass.cpp`, `passes.h`, `SimplifyLocals.cpp`, and the dedicated `untee.wast` lit file) and distilling the real contract: upstream `untee` is a tiny tee-only postwalk that expands reachable `local.tee` nodes into explicit `local.set` plus `local.get`, deletes unreachable tees entirely, and sits beside rather than inside the broader `simplify-locals*` family.
- Added `docs/wiki/binaryen/passes/untee/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flattening-code-pushing-and-tee-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the tee-only rewrite surface, the postorder nested-expansion rule, the `code-pushing` interaction story, the `simplify-locals-notee` contrast, and the beginner-friendly positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `untee`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `dataflow-optimization` / `dfo` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `dataflow-optimization` or `dfo` slice, and the local removed registry still exposed a real documentation gap around Binaryen's flat-IR-only DataFlow SSA side-graph pass.
- Added the raw note `docs/wiki/raw/research/0178-2026-04-21-dataflow-optimization-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DataFlowOpts.cpp`, `pass.cpp`, `dataflow/graph.h`, `dataflow/node.h`, `dataflow/users.h`, `dataflow/utils.h`, `ir/flat.h`, and the combo lit file `flatten_simplify-locals-nonesting_dfo_O3.wast`) and distilling the real contract: upstream `dfo` is a tiny flat-input integer-local DataFlow-IR worklist that collapses identical constant phis and all-constant expressions via nested `precompute`, not a broad generic optimizer despite the local name `dataflow-optimization`.
- Added `docs/wiki/binaryen/passes/dataflow-optimization/index.md`, `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flat-ir-dataflow-ir-and-boundaries.md`, and `wat-shapes.md` so the living wiki now has a canonical home for the local-vs-upstream naming split, the hard flatness precondition, the side-IR node model, the loop-precision cutoff, the narrow actual rewrite surface, and the main positive/bailout WAT families.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `dataflow-optimization` / `dfo`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `i64-to-i32-lowering` as a source-backed upstream-only boundary-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the earlier upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `i64-to-i32-lowering` slice, and the local boundary-only registry still exposed a real beginner-facing gap around Binaryen's whole-module i64 ABI/legalization pass.
- Added the raw note `docs/wiki/raw/research/0175-2026-04-21-i64-to-i32-lowering-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`I64ToI32Lowering.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `abi/js.h`, `asmjs/shared-constants.h`, `flat.h`, `iteration.h`, `memory-utils.h`, `module-utils.h`, `names.h`, and the dedicated `flatten_i64-to-i32-lowering.wast` lit file) and distilling the real contract: upstream `i64-to-i32-lowering` is a flat-input whole-module pair-lowering pass that splits locals and signatures, uses a hidden high-half temp side channel, lowers returns through a synthetic high-bits global, and relies on helper-based reinterpret and atomic support rather than acting like a small arithmetic rewrite.
- Created the new living folder `docs/wiki/binaryen/passes/i64-to-i32-lowering/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `flatness-helpers-and-boundaries.md`, and `wat-shapes.md` so the flatness precondition, the low-visible/high-hidden value model, the split-local/signature plus return-global ABI contract, the helper/runtime boundaries, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `i64-to-i32-lowering`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `merge-similar-functions` as a source-backed upstream-only boundary-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `merge-similar-functions` slice, and the local boundary-only registry plus Batch 4 map still exposed a real beginner-facing gap around Binaryen's late shrink-oriented near-duplicate function parameterization pass.
- Added the raw note `docs/wiki/raw/research/0174-2026-04-21-merge-similar-functions-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`MergeSimilarFunctions.cpp`, `pass.cpp`, `pass.h`, `hashed.h`, `manipulation.h`, `module-utils.h`, `names.h`, `wasm-limits.h`, and the dedicated `merge-similar-functions*` lit files) and distilling the real contract: upstream `merge-similar-functions` is a whole-module helper-plus-thunk size pass that hashes near-duplicate functions, derives reusable synthetic params for differing constants or eligible direct callees, rewrites shared bodies through cloning plus local-index repair, and rejects over-wide or unprofitable classes instead of acting like duplicate-function elimination.
- Created the new living folder `docs/wiki/binaryen/passes/merge-similar-functions/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `profitability-indirection-and-type-barriers.md`, and `wat-shapes.md` so the shrink-level scheduler slot, the hash-plus-equivalence-class algorithm, the feature-gated `call_ref` indirection path, the profitability plus `255`-param limit rules, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `merge-similar-functions`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `loop-invariant-code-motion` / `licm` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `loop-invariant-code-motion` or `licm` slice, and the local removed registry plus Batch 3 pass-port map still exposed a real beginner-facing gap around Binaryen's loop-header hoisting pass.
- Added the raw note `docs/wiki/raw/research/0173-2026-04-21-loop-invariant-code-motion-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`LoopInvariantCodeMotion.cpp`, `pass.cpp`, `pass.h`, `effects.h`, `find_all.h`, `parents.h`, and the dedicated `licm.wast` lit file) and distilling the real contract: upstream `licm` is a conservative fixed-point loop-header hoister published locally as `loop-invariant-code-motion`, and its real safety rule is loop invariance plus effect safety plus child hoistability rather than generic code motion.
- Created the new living folder `docs/wiki/binaryen/passes/loop-invariant-code-motion/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `effects-loops-and-hoisting-rules.md`, and `wat-shapes.md` so the local-vs-upstream naming split, the fixed-point temp-local hoisting strategy, the current-main spot-check, the effect/trap/child-hoistability proof, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `loop-invariant-code-motion` / `licm`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `avoid-reinterprets` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, then explicitly widened the tracker again because the original no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `avoid-reinterprets` slice, and the local removed registry plus Batch 1 pass-port map still exposed a real beginner-facing gap around Binaryen's load/reinterpret cleanup pass.
- Added the raw note `docs/wiki/raw/research/0172-2026-04-21-avoid-reinterprets-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`AvoidReinterprets.cpp`, `pass.cpp`, `local-graph.h`, `properties.h`, and the dedicated `avoid-reinterprets*.wast` lit files) and distilling the real contract: `avoid-reinterprets` is a narrow full-width load-plus-reinterpret rewrite pass that follows only single-load local chains, duplicates eligible loads through helper locals when needed, and preserves memory64 pointer typing rather than performing generic type canonicalization or load CSE.
- Created the new living folder `docs/wiki/binaryen/passes/avoid-reinterprets/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `single-load-chains-and-bailouts.md`, and `wat-shapes.md` so the scheduler status, current-main-equals-`version_129` implementation check, provenance and fallthrough proof, helper-local duplication strategy, and beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the widened upstream-only registry now includes `avoid-reinterprets`, the human-readable catalogs link the new folder pages, and future recursive campaign threads can see that this candidate is no longer an uncovered expansion target.

## [2026-04-21] research | add Binaryen `precompute-propagate` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `src/passes/optimize.mbt`, and the existing `precompute` dossier, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `precompute-propagate` slice, and the local removed-name registry plus neighboring nested-rerun docs still left a real beginner-facing gap around Binaryen's aggressive / after-inlining `precompute` sibling.
- Added the raw note `docs/wiki/raw/research/0167-2026-04-21-precompute-propagate-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`Precompute.cpp`, `pass.cpp`, `opt-utils.h`, `local-graph.h`, `wasm-interpreter.h`, and `precompute-propagate-partial.wast`) and distilling the real contract: `precompute-propagate` is a real public pass name with the same semantic precompute core as plain `precompute`, but it adds a `LazyLocalGraph`-based local-propagation phase plus one extra rerun of the main walk and therefore has different scheduler meaning and reachable fixed points.
- Created the new living folder `docs/wiki/binaryen/passes/precompute-propagate/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the public-vs-shared-core split from plain `precompute`, the aggressive / nested scheduler placement under `optimizeAfterInlining(...)`, the helper-dependency story, and the beginner-friendly positive/bailout local-carried rewrite families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so `precompute-propagate` now has a first-class dossier home, the widened registry section no longer silently leaves that local removed-registry pass undocumented, and the global wiki catalog now points future aggressive-scheduler research at the new canonical pages.

## [2026-04-21] research | add Binaryen `simplify-locals-notee` as a source-backed upstream-only removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first expansion wave were already dossier-covered, `agent-todo.md` still has no dedicated `simplify-locals-notee` or `simplify-locals-no-tee` slice, and the existing `simplify-locals` family docs still lacked a canonical landing page for the public upstream `-notee` variant itself.
- Added the raw note `docs/wiki/raw/research/0166-2026-04-21-simplify-locals-notee-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyLocals.cpp`, `pass.cpp`, the core linear/effects/equivalence helper headers, and the dedicated `simplify-locals-notee` test pair) and distilling the real contract: upstream `simplify-locals-notee` is `SimplifyLocals<false, true, true>`, so it still performs structured block / `if` / loop result formation, late equivalent-copy cleanup, and final dead-set cleanup while forbidding only the multi-use sink rewrite that would create a new `local.tee`.
- Created the new living folder `docs/wiki/binaryen/passes/simplify-locals-notee/` with a landing page plus `binaryen-strategy.md`, `variant-boundaries-and-registry-aliases.md`, and `wat-shapes.md` so the upstream-vs-local spelling split (`simplify-locals-notee` vs `simplify-locals-no-tee`), the exact variant identity, the contrast against `-nostructure` / `-notee-nostructure` / `-nonesting`, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so `simplify-locals-notee` now appears as a dedicated dossier-backed upstream-only registry candidate instead of an undocumented registry alias hiding inside broader `simplify-locals` family notes.

## [2026-04-21] research | add Binaryen `optimize-added-constants-propagate` dossier and correct the `optimize-added-constants` family contract

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion queue were already dossier-covered, `agent-todo.md` still has no dedicated `optimize-added-constants-propagate` slice, and the local removed-name registry still exposed a real pass-family gap around the plain and propagating load/store-offset pair in `OptimizeAddedConstants.cpp`.
- Added the raw note `docs/wiki/raw/research/0165-2026-04-21-optimize-added-constants-propagate-binaryen-research.md`, grounding the chosen pass in official Binaryen `version_129` sources (`OptimizeAddedConstants.cpp`, `pass.cpp`, `pass.h`, the shared `optimize-added-constants*_low-memory-unused` tests, and the `memory64` / `nomemory` lit files) and distilling the real contract: this family is about moving small added constants in memory addresses into explicit load/store offsets under `--low-memory-unused`, while the `propagate` sibling adds a real `LazyLocalGraph`-based local-pair propagation surface.
- Created the new living folder `docs/wiki/binaryen/passes/optimize-added-constants-propagate/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the local removed-registry status, the direct-vs-propagating sibling split, the SSA-vs-helper-local reuse rule, the `LowMemoryBound = 1024` threshold, the no-memory and memory64 overflow boundaries, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Corrected the neighboring `docs/wiki/binaryen/passes/optimize-added-constants/` living pages to match the same reviewed source: plain `optimize-added-constants` is now documented as the direct load/store-address offset-folding sibling rather than an arithmetic reassociation pass, and the folder now points readers at the new propagate dossier for the extra local-pair behavior instead of silently merging both contracts into one incorrect arithmetic story.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `optimize-added-constants-propagate` now has a first-class dossier home, the corrected plain-pass description no longer misstates the family contract, and the tracker now treats both removed-registry siblings as dossier-covered source-backed expansions.

## [2026-04-21] research | add Binaryen `optimize-added-constants` as a source-backed removed-registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and `src/passes/optimize.mbt`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the first upstream-only expansion queue were already dossier-covered, `agent-todo.md` still has no dedicated `optimize-added-constants` slice, and the local removed-name registry still exposed a real beginner-facing gap around Binaryen's small arithmetic-tree cleanup pass family.
- Added the raw note `docs/wiki/raw/research/0164-2026-04-21-optimize-added-constants-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`OptimizeAddedConstants.cpp`, `pass.cpp`, the dedicated `optimize-added-constants.wast` lit file, and the public README pass list) and distilling the real contract: upstream `optimize-added-constants` is a narrow integer add/sub reassociation pass rather than generic constant folding, while `optimize-added-constants-propagate` is the same core matcher with a more aggressive immediate-revisit behavior for longer ladders.
- Created the new living folder `docs/wiki/binaryen/passes/optimize-added-constants/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the local removed-registry status, the tiny helper-only implementation shape, the plain-vs-propagate split, the sign-correct mixed add/sub rules, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `optimize-added-constants` now has a first-class dossier home, the additional tracker-expansion section explicitly includes it as a deliberate source-backed removed-registry candidate, and the global catalog now points future arithmetic-canonicalization port work at the new canonical pages.
- Recorded four durable beginner-facing corrections in the new dossier: the pass is not part of the repo's current no-DWARF default optimize path, the real `version_129` surface is a tiny integer add/sub tree canonicalizer rather than a broad arithmetic optimizer, the propagate sibling is a real public contract rather than an undocumented footnote, and a future Starshine port must preserve the commutative-normalization plus sign-correct rebuild rules instead of treating the pass as generic constant propagation.

## [2026-04-21] research | add Binaryen `gufa` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue and the first upstream-only registry expansion queue were already dossier-covered, `agent-todo.md` still has no dedicated `gufa` slice, and the local boundary-only registry plus the existing `type-refining` GUFA notes still left a real beginner-facing gap around Binaryen's standalone whole-program contents-oracle pass family.
- Added the raw note `docs/wiki/raw/research/0163-2026-04-21-gufa-binaryen-research.md`, grounding the pass family in official Binaryen `version_129` sources (`GUFA.cpp`, `possible-contents.h`, `pass.cpp`, and the dedicated `gufa*` lit files) and distilling the real contract: upstream `gufa` is a closed-world whole-program contents-oracle pass with a narrow direct rewrite surface, while `gufa-optimizing` adds nested `dce` + `vacuum` reruns and `gufa-cast-all` adds explicit cast insertion.
- Created the new living folder `docs/wiki/binaryen/passes/gufa/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `content-oracle-variants-and-boundaries.md`, and `wat-shapes.md` so the `ContentOracle` data model, the plain-vs-optimizing-vs-cast-all split, the closed-world and materialization boundaries, the dedicated `ref.eq` / `ref.test` / `ref.cast` logic, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `gufa` now has a first-class dossier home, the tracker’s additional upstream-only registry table explicitly includes it as a deliberate source-backed expansion, and the pass-folder map plus global catalog now point future analysis-family port work at the new canonical pages.
- Recorded five durable beginner-facing corrections in the new dossier: GUFA is not just constant propagation, it is not part of the repo's current default no-DWARF optimize path, `gufa-optimizing` has a real nested-cleanup contract, `gufa-cast-all` has a real new-cast contract, and knowing a unique runtime value reaches a site does not always mean Binaryen can emit that value directly at the original static type.

## [2026-04-21] research | add Binaryen `propagate-globals-globally` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue and the first upstream-only expansion queue were already dossier-covered, `agent-todo.md` still has no dedicated `propagate-globals-globally` slice, and the local boundary-only registry plus the already-deep `simplify-globals*` / `string-gathering` folders still left a real beginner-facing gap around Binaryen's startup-only top-level propagation surface.
- Added the raw note `docs/wiki/raw/research/0162-2026-04-21-propagate-globals-globally-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`PropagateGlobals.cpp`, `pass.cpp`, and the dedicated `propagate-globals-globally.wast` lit file) and distilling the real contract: upstream `propagate-globals-globally` is a small module pass for startup-known global chains and startup-offset propagation, not a generic function-body global simplifier and not a synonym for full `simplify-globals`.
- Created the new living folder `docs/wiki/binaryen/passes/propagate-globals-globally/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the startup-global-chain and active-offset rewrite contract, the explicit split from full `simplify-globals*`, the lightweight module-pass dependency story, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `propagate-globals-globally` now has a first-class dossier home, the additional upstream-only registry table explicitly includes it as a deliberate source-backed expansion, and the global wiki catalog now points future late-global port work at the new canonical pages.
- Recorded four durable beginner-facing corrections in the new dossier: `globally` here means across module startup expressions rather than all code, the pass rewrites later globals and startup offsets but not ordinary function bodies, the pass should be taught as a separable startup-only neighbor of `simplify-globals*`, and a future Starshine port should stay module-shaped instead of silently becoming a hot function pass.

## [2026-04-21] research | add Binaryen `inlining` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue plus the earlier upstream-only expansion queue were already covered, `agent-todo.md` still has no dedicated plain `inlining` slice, and the local boundary-only registry plus the already-deep `inlining-optimizing` folder still left a real beginner-facing gap around the shared-engine but different-scheduler plain variant.
- Added the raw note `docs/wiki/raw/research/0161-2026-04-21-inlining-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Inlining.cpp`, `pass.cpp`, `opt-utils.h`, `pass.h`, the main branch/local/type helper headers, and the reviewed `inlining*` / `no-inline*` / `inline-main` / inline-hint lit roster) and distilling the real contract: upstream plain `inlining` is a whole-module direct-call inliner with layered tiny/one-caller/trivial heuristics, Pattern A / Pattern B partial splitting, structured callsite rewrite plus local/label/type repair, bounded iterative action choice, and dead-helper cleanup, while `inlining-optimizing` is that same engine plus a nested useful-pass rerun.
- Created the new living folder `docs/wiki/binaryen/passes/inlining/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `heuristics-splitting-and-plain-vs-optimizing.md`, and `wat-shapes.md` so the direct-call-only `version_129` planner surface, the layered heuristic classes, the Pattern A / Pattern B split rules, the no-inline controls and inline-hint context, the `inline-main` relation, the copied-local / return-call / unreachable repair story, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `inlining` now has a first-class dossier home, the tracker’s additional upstream-only registry table explicitly includes it as a deliberate source-backed expansion, the pass-folder map plus global catalog now point future boundary-pass work at the new canonical pages, and the older `inlining-optimizing` blurbs now reflect the stricter `version_129` direct-call planner surface instead of implying generic precise-`call_ref` planning.
- Recorded four durable beginner-facing corrections in the new dossier: plain `inlining` is not part of the repo's current default no-DWARF path, actual `version_129` action planning is direct-call based even though the updater repairs copied `return_call_ref` / `return_call_indirect` forms, partial inlining is limited to two narrow top-of-function `if` families rather than arbitrary CFG extraction, and the optimizing sibling's nested rerun is the main semantic split from the plain pass rather than optional polish.

## [2026-04-21] research | add Binaryen `simplify-globals` as a source-backed upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue, the first upstream-only expansion queue, and the just-finished `dead-argument-elimination` dossier were already covered, `agent-todo.md` still has no dedicated `simplify-globals` slice, and the local boundary-only registry plus the already-deep `simplify-globals-optimizing` folder still left a real beginner-facing gap around the shared-engine but different-scheduler plain variant.
- Added the raw note `docs/wiki/raw/research/0160-2026-04-21-simplify-globals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyGlobals.cpp`, `pass.cpp`, `pass.h`, `effects.h`, `find_all.h`, `linear-execution.h`, `properties.h`, `utils.h`, and the full reviewed `simplify-globals*` plus `propagate-globals-globally` lit roster) and distilling the real contract: upstream plain `simplify-globals` is a late whole-module global cleanup and propagation pass with practical-immutability discovery, single-use global-init folding, dead or `read-only-to-write` set removal, startup-vs-runtime propagation, immutable copy-chain canonicalization, and refinalization, while `simplify-globals-optimizing` is that same engine plus a nested default-function rerun.
- Created the new living folder `docs/wiki/binaryen/passes/simplify-globals/` with a landing page plus `binaryen-strategy.md`, `plain-vs-optimizing-and-safety.md`, and `wat-shapes.md` so the plain-vs-optimizing split, the startup-vs-runtime propagation distinction, the exact `read-only-to-write` safety rules, the actual-node versus effect-summary boundary, the `drop(value)` write-removal contract, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `simplify-globals` now has a first-class dossier home, the additional upstream-only registry section explicitly includes it as a deliberate source-backed expansion, and the pass-folder map plus global wiki catalog now point future global-pass work at the new canonical pages.
- Recorded four durable beginner-facing corrections in the new dossier: the plain pass is a real public contract distinct from `simplify-globals-optimizing`, startup-time propagation is broader than runtime current-trace propagation, `read-only-to-write` requires actual `global.get` / `global.set` ownership rather than only inferred effects, and removed global writes preserve operand evaluation as `drop(value)` instead of silently deleting the whole subtree.

## [2026-04-21] research | add Binaryen `dead-argument-elimination` / `dae` as a second-wave upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue, the first upstream-only expansion queue, and the just-finished `constant-field-propagation` dossier were already covered, `agent-todo.md` still has no dedicated `dead-argument-elimination` slice, and the existing `dae-optimizing` dossier already depended on understanding the plain pass as a separately registered local boundary-only name.
- Added the raw note `docs/wiki/raw/research/0159-2026-04-21-dead-argument-elimination-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `return-utils.h`, `lubs.h`, `type-updating.h`, and the reviewed `dae_*` lit files) and distilling the real contract: upstream plain `dae` is a whole-module direct-call boundary rewrite pass with param/result LUB refinement, constant actual materialization, helper-driven param removal, dropped-return cleanup, and iterative localization, while `dae-optimizing` is that same engine plus a nested cleanup rerun.
- Created the new living folder `docs/wiki/binaryen/passes/dead-argument-elimination/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, and `wat-shapes.md` so the upstream/local naming split (`dae` vs `dead-argument-elimination`), the direct-call ownership rule, the export and `ref.func` unseen-call bailouts, the tail-call and uninhabitable-return repair rules, the iterative localization story, and the beginner-friendly rewrite/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `dead-argument-elimination` now has a first-class dossier home, the additional upstream-only registry table now includes it as a deliberate source-backed second-wave expansion rather than a silent side mention, and the pass-folder map plus global catalog now point future boundary-pass work at the new canonical pages.
- Recorded four durable beginner-facing corrections in the new dossier: plain `dae` is broader than dead-param deletion, the absence of `optimizeAfterInlining(...)` is the key semantic split from `dae-optimizing`, exports and `ref.func` escapes are first-class signature-change bailouts, and dropped uninhabitable results must preserve `unreachable` information rather than collapsing to an ordinary none-typed call.

## [2026-04-21] research | add Binaryen `constant-field-propagation` / `cfp` as a newly widened upstream-only registry dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly widened the tracker again because the main no-DWARF / saved-`-O4z` queue and the first closed-world upstream-only expansion queue were already dossier-covered, `agent-todo.md` still has no dedicated `constant-field-propagation` slice, and the local boundary-only registry plus existing `global-type-optimization` / `type-refining` / `signature-refining` notes already treated `cfp` as a real scheduler neighbor.
- Added the raw note `docs/wiki/raw/research/0158-2026-04-21-constant-field-propagation-binaryen-research.md`, grounding the pass family in official Binaryen `version_129` sources (`ConstantFieldPropagation.cpp`, `pass.cpp`, `possible-constant.h`, `struct-utils.h`, `subtypes.h`, `module-utils.h`, `cfp.wast`, and `gto_and_cfp_in_O.wast`) and distilling the real contract: upstream `cfp` is a closed-world struct-field constant/global read replacer driven by write facts, exact-vs-inexact subtype propagation, and a copy fixed point, while `cfp-reftest` is the narrower two-value `ref.test` sibling variant.
- Created the new living folder `docs/wiki/binaryen/passes/constant-field-propagation/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `copies-subtypes-ref-tests-and-atomics.md`, and `wat-shapes.md` so the hard `--closed-world` requirement, the upstream/local naming split (`constant-field-propagation` vs `cfp` / `cfp-reftest`), the copy-fixed-point algorithm, the packed-field and subtype validity repairs, the atomic ordered-read bailout, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `constant-field-propagation` now has a first-class dossier home, the tracker’s additional upstream-only registry table now includes it as a deliberate source-backed expansion rather than an undocumented side mention, and the pass-folder map plus global wiki catalog now point future port work at the new canonical pages.
- Recorded four durable beginner-facing corrections in the new dossier: `cfp` is not generic constant propagation, the pass is intentionally type-level and escape-insensitive, ordered atomic reads stay untouched even when the field value is constant while known-trapping reads can still collapse to `unreachable`, and the exactly-two-value `ref.test` optimization belongs only to the `cfp-reftest` sibling variant rather than ordinary `cfp`.

## [2026-04-21] research | add Binaryen `reorder-types` as the last obvious tracker-`none` upstream-only dossier

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign into the remaining tracker-listed upstream-only `none` candidate because the main no-DWARF / saved-`-O4z` queue and the newer `remove-unused-types`, `type-refining`, `signature-pruning`, `signature-refining`, `global-type-optimization`, `abstract-type-refining`, `unsubtyping`, and `minimize-rec-groups` dossiers were already covered, and `agent-todo.md` still has no dedicated `reorder-types` slice.
- Added the raw note `docs/wiki/raw/research/0157-2026-04-21-reorder-types-binaryen-research.md`, grounding the pass in the local registry and release-horizon docs plus a clearly labeled upstream-source follow-up inventory (`ReorderTypes.cpp`, `pass.cpp`, `module-utils.*`, `type-updating.*`, `topological_sort.h`, `wasm-type-ordering.h`, and likely `reorder-types.wast`) and distilling the current working contract: `reorder-types` is a constrained type-index layout pass rather than dead-type cleanup or rec-group repartitioning, and the hard part is balancing profitability against graph-order legality plus full-module remapping.
- Created the new living folder `docs/wiki/binaryen/passes/reorder-types/` with a landing page plus `binaryen-strategy.md`, `ordering-cost-model-and-boundaries.md`, and `wat-shapes.md` so the explicit-nondefault scheduler placement, the type-layout-versus-graph-constraint story, the distinction from `remove-unused-types` and `minimize-rec-groups`, the likely metadata-rewrite surface, and the beginner-friendly positive/bailout module-shape families are now explicit in-tree even before a later web-enabled source-confirmation pass.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `reorder-types` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker now says the first obvious upstream-only `none` expansion queue is exhausted instead of still pointing at `reorder-types`.
- Recorded four durable beginner-facing corrections in the new dossier: `reorder-types` should be taught as a constrained module-wide type-index layout rewrite, not as dead-type deletion or rec-group minimization; legality constraints matter as much as the cost model; the pass is absent from the repo's current no-DWARF default optimize path; and a future port must keep the full-module type/name/index remap explicit instead of treating the rewrite as a cosmetic type-section sort.

## [2026-04-21] research | add Binaryen `minimize-rec-groups` as the next upstream-only registry dossier after `abstract-type-refining`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes plus the newer `remove-unused-types`, `type-refining`, `signature-pruning`, `signature-refining`, `global-type-optimization`, `abstract-type-refining`, and `unsubtyping` dossiers, and `agent-todo.md` still has no dedicated `minimize-rec-groups` slice.
- Added the raw note `docs/wiki/raw/research/0156-2026-04-21-minimize-rec-groups-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`MinimizeRecGroups.cpp`, `pass.cpp`, `module-utils.h`, `type-updating.*`, `strongly_connected_components.h`, `topological_sort.h`, `disjoint_sets.h`, `wasm-type-shape.*`, and the full `minimize-rec-groups*` lit roster) and distilling the real contract: upstream `minimize-rec-groups` is a GC-only explicit module pass whose real work is private-type SCC minimization plus identity preservation through feature-sensitive shape comparison, permutation search, and compact brand insertion.
- Created the new living folder `docs/wiki/binaryen/passes/minimize-rec-groups/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `permutations-brands-and-public-conflicts.md`, and `wat-shapes.md` so the explicit-nondefault scheduler placement, the private-vs-public group split, the supertype/descriptor order constraints, the lazy equivalence-class canonicalization, the automorphism-aware permutation story, the compact brand iterator, and the beginner-friendly positive/bailout module-shape families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `minimize-rec-groups` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion target now narrows from `minimize-rec-groups` plus `reorder-types` to just `reorder-types` unless a future thread can justify a major-gap fallback.
- Recorded four durable beginner-facing corrections in the new dossier: the pass is not part of the default no-DWARF or closed-world optimize presets in reviewed `version_129`, SCC splitting alone is insufficient because equal-shape groups would accidentally merge type identity, exactness only differentiates shapes when it survives feature lowering, and the pass can intentionally increase type count by inserting compact brand types.

## [2026-04-21] research | add Binaryen `abstract-type-refining` as the next upstream-only registry dossier after `unsubtyping`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes plus the newer `remove-unused-types`, `type-refining`, `signature-pruning`, `signature-refining`, `global-type-optimization`, and `unsubtyping` upstream-only dossiers, and `agent-todo.md` still has no dedicated `abstract-type-refining` slice.
- Added the raw note `docs/wiki/raw/research/0155-2026-04-21-abstract-type-refining-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`AbstractTypeRefining.cpp`, `pass.cpp`, `subtypes.h`, `type-updating.*`, `module-utils.h`, `localize.h`, `drop.h`, `wasm-type.h`, and the full `abstract-type-refining*` lit roster) and distilling the real contract: upstream `abstract-type-refining` is a closed-world, struct-only creation-evidence pass with always-on never-created-to-bottom rewriting, TNH-only singleton-child refinement, descriptor/exact-cast preoptimization, and a deliberate handoff of declared subtype-edge cleanup to later `unsubtyping`.
- Created the new living folder `docs/wiki/binaryen/passes/abstract-type-refining/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `traps-never-happen-exact-casts-and-descriptors.md`, and `wat-shapes.md` so the hard `--closed-world` gate, the `struct.new`-only created-type scan, the public-types-treated-as-created rule, the TNH-vs-non-TNH split, the exact/descriptor-cast repair story, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `abstract-type-refining` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion targets now move on to the remaining still-`none` upstream-only candidates instead of reopening `abstract-type-refining`'s first-dossier gap.
- Recorded four durable beginner-facing corrections in the new dossier: only struct types are optimized today, TNH is required only for parent-to-child refinement and not for bottomizing fully never-created families, exact casts often collapse to bottom/null checks instead of refining to live children, and visible type shrink in the shipped lit files often relies on pairing `abstract-type-refining` with `remove-unused-types`.

## [2026-04-21] research | add Binaryen `unsubtyping` as the next upstream-only registry dossier after `global-type-optimization`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes plus the new `remove-unused-types`, `type-refining`, `signature-pruning`, `signature-refining`, and `global-type-optimization` upstream-only dossiers, and `agent-todo.md` still has no dedicated `unsubtyping` slice.
- Added the raw note `docs/wiki/raw/research/0154-2026-04-21-unsubtyping-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Unsubtyping.cpp`, `pass.cpp`, `subtype-exprs.h`, `js-utils.h`, `type-updating.*`, `module-utils.h`, `localize.h`, `effects.h`, and the full `unsubtyping*` lit roster) and distilling the real contract: upstream `unsubtyping` is a closed-world subtype-plus-descriptor fixed-point minimization pass driven by validation constraints, cast-success preservation, descriptor-square completion, and JS-boundary keepalive rather than a simple declaration-edge trimmer.
- Created the new living folder `docs/wiki/binaryen/passes/unsubtyping/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `descriptor-squares-casts-and-js-boundaries.md`, and `wat-shapes.md` so the hard `--closed-world` gate, the minimal-relation worklist algorithm, the exact-vs-ordinary cast split, the descriptor-square and JS-prototype rules, the allocation trap-preservation story, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `unsubtyping` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion targets now move on to the remaining still-`none` upstream-only candidates instead of reopening `unsubtyping`'s first-dossier gap.
- Recorded four durable beginner-facing corrections in the new dossier: the pass owns descriptor cleanup as well as subtype edges, exact casts are narrower than ordinary casts, most official shrinkage is checked under `--unsubtyping --remove-unused-types`, and descriptor-bearing allocation sites may need explicit repair or synthetic globals even after the descriptor relation itself disappears.

## [2026-04-21] research | add Binaryen `global-type-optimization` as the next upstream-only registry dossier after `signature-refining`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes plus the new `remove-unused-types`, `type-refining`, `signature-pruning`, and `signature-refining` upstream-only dossiers, and `agent-todo.md` still has no dedicated `global-type-optimization` slice.
- Added the raw note `docs/wiki/raw/research/0153-2026-04-21-global-type-optimization-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`GlobalTypeOptimization.cpp`, `pass.cpp`, `struct-utils.h`, `type-updating.*`, `module-utils.h`, `js-utils.h`, `wasm-type-ordering.h`, the full `gto*` lit roster, and the neighboring `signature-refining_gto.wat`) and distilling the real contract: upstream `gto` is a closed-world private-struct mutability/removal pass with subtype-safe field reordering, JS-descriptor prototype keepalive rules, and trap-preserving field-rewrite order rather than generic field-type refinement.
- Created the new living folder `docs/wiki/binaryen/passes/global-type-optimization/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `field-removal-subtyping-js-interop-and-traps.md`, and `wat-shapes.md` so the upstream/local naming split (`gto` vs `global-type-optimization`), the hard `--closed-world` requirement, the mutability-vs-removal split, the subtype-layout constraints, the JS interop descriptor rules, the instruction-before-type rewrite contract, and the beginner-friendly positive/bailout WAT families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `global-type-optimization` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion targets now move on to the remaining still-`none` upstream-only candidates instead of reopening `global-type-optimization`'s first-dossier gap.
- Recorded three durable beginner-facing corrections in the new dossier: `gto` does not refine field value types at all, constructor traffic does not keep fields alive or mutable in the real decision phase, and removed field rewrites must preserve side-effect order plus both null traps and module-instantiation traps rather than simply deleting dead writes.

## [2026-04-21] research | add Binaryen `signature-refining` as the next upstream-only registry dossier after `signature-pruning`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes, `remove-unused-types`, `type-refining`, and `signature-pruning` were already dossier-covered, and `agent-todo.md` still has no dedicated `signature-refining` slice.
- Added the raw note `docs/wiki/raw/research/0152-2026-04-21-signature-refining-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SignatureRefining.cpp`, `pass.cpp`, `lubs.*`, `module-utils.*`, `type-updating.*`, `subtypes.h`, `intrinsics.*`, and `signature-refining.wast`) and distilling the real contract: upstream `signature-refining` is a heap-type-level signature subtype-tightening pass that refines params from caller operand LUBs and results from returned-value LUBs, then rewrites signature heap types atomically and repairs `call.without.effects` imports afterward.
- Created the new living folder `docs/wiki/binaryen/passes/signature-refining/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `params-results-publicity-and-intrinsics.md`, and `wat-shapes.md` so the param-vs-result split, the public-vs-params-only blocker matrix, the body-fixup-local story, the `call.without.effects` special handling, and the beginner-friendly positive/bailout call-and-return-shape families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `signature-refining` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion targets now move on to the remaining still-`none` upstream-only candidates instead of reopening `signature-refining`'s first-dossier gap.
- Recorded three durable beginner-facing corrections in the new dossier: the pass refines results as well as params, the default scheduler's closed-world placement is broader than the pass body's own GC-plus-no-table gates, and `call.without.effects` is part of the real algorithm on both sides because it contributes to param LUBs and can require cloned refined-result imports afterward.

## [2026-04-21] research | add Binaryen `signature-pruning` as the next upstream-only registry dossier after `type-refining`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes, `remove-unused-types` and `type-refining` were already dossier-covered, and `agent-todo.md` still has no dedicated `signature-pruning` slice.
- Added the raw note `docs/wiki/raw/research/0151-2026-04-21-signature-pruning-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SignaturePruning.cpp`, `pass.cpp`, `param-utils.*`, `module-utils.*`, `type-updating.h`, `subtypes.h`, `intrinsics.*`, `possible-constant.h`, `localize.h`, `eh-utils.h`, `liveness-traversal.h`, and `signature-pruning.wast`) and distilling the real contract: upstream `signature-pruning` is a closed-world, GC-gated, heap-type-level dead-argument-elimination pass for nominal function signatures with constant-actual promotion, atomic signature rewriting, and one delayed localization rerun.
- Created the new living folder `docs/wiki/binaryen/passes/signature-pruning/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `constant-actuals-localization-and-boundaries.md`, and `wat-shapes.md` so the heap-type aggregation model, the constant-actual phase, the delayed `ChildLocalizer` rerun, the public/tag/continuation/JS-called/subtyping blockers, and the beginner-friendly positive/bailout call-shape families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `signature-pruning` now has a first-class dossier home, the tracker no longer lists it as `none`, the pass-folder map now includes it in the upstream-only registry section, and the tracker's suggested next queue-expansion targets now move on to the remaining still-`none` upstream-only candidates instead of reopening `signature-pruning`'s first-dossier gap.
- Recorded three durable beginner-facing corrections in the new dossier: the pass prunes parameters only and does so per nominal heap type rather than per function, constant actual promotion and delayed operand localization are part of the real algorithm rather than optional polish, and exported/public-rec-group, tag, continuation, `call.without.effects`, table, and function-subtyping boundaries are all real source-backed no-op rules.

## [2026-04-21] research | add Binaryen `type-refining` as the next upstream-only registry dossier after `remove-unused-types`

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign again beyond the already dossier-covered no-DWARF / saved-`-O4z` queue because the prompt still excluded the refreshed parity passes and `agent-todo.md` still has no dedicated `type-refining` slice.
- Added the raw note `docs/wiki/raw/research/0150-2026-04-21-type-refining-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`TypeRefining.cpp`, `pass.cpp`, `struct-utils.h`, `lubs.h`, `possible-contents.h`, `type-updating.h`, `module-utils.h`, and the `type-refining*` lit family) and distilling the real contract: upstream `type-refining` is a closed-world, GC-only private-struct-field refinement pass with a normal direct-struct-traffic mode, a stronger `type-refining-gufa` whole-program oracle mode, public-type freezing, hierarchy legality repair, and mandatory read/write fixups.
- Created the new living folder `docs/wiki/binaryen/passes/type-refining/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `normal-vs-gufa-and-fixups.md`, and `wat-shapes.md` so the closed-world-only scheduler slot, the normal-vs-GUFA split, the tee-vs-block fallthrough boundary, the public/private legality rules, and the beginner-friendly positive/bailout field-shape families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `type-refining` now has a first-class dossier home, the additional upstream-only registry section now lists it as dossier-covered rather than `none`, and the tracker guidance now points future queue-expansion threads at the remaining still-`none` upstream-only candidates instead of reopening `type-refining`'s first-dossier gap.
- Recorded three durable beginner-facing corrections in the new dossier: ordinary reads do not constrain field inference in this pass, the normal and GUFA variants share a rewrite/fixup back end but do not share the same inference strength, and Binaryen's declaration rewrite is only half the job because the pass must also repair invalidated `struct.get`, `struct.new`, `struct.set`, and RMW/cmpxchg sites afterward.

## [2026-04-21] research | add Binaryen `remove-unused-types` as the first upstream-only registry dossier after the parity queue closed

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, and `agent-todo.md`, then explicitly expanded the campaign beyond the fully dossier-covered no-DWARF / saved-`-O4z` queue because the prompt now excludes the already-deepened parity passes and `agent-todo.md` still has no dedicated `remove-unused-types` slice.
- Added the raw note `docs/wiki/raw/research/0149-2026-04-21-remove-unused-types-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`RemoveUnusedTypes.cpp`, `pass.cpp`, `type-updating.h`, `module-utils.h`, and `remove-unused-types.wast`) and distilling the real contract: upstream `remove-unused-types` is a closed-world, GC-only private-heap-type cleanup pass with public-type seeding, declaration/code type-use scanning, rec-group-aware retention, and a `GlobalTypeRewriter` full-module heap-type rewrite.
- Created the new living folder `docs/wiki/binaryen/passes/remove-unused-types/` with a landing page plus `binaryen-strategy.md`, `implementation-structure-and-tests.md`, `closed-world-visibility-and-rec-group-rewrite.md`, and `wat-shapes.md` so the closed-world gate, public-vs-private split, helper structure, and beginner-friendly module-shape families are all explicit in-tree.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so `remove-unused-types` now has a first-class dossier home, and widened the tracker with a new upstream-only registry section that records further still-`none` queue-expansion candidates (`type-refining`, `signature-pruning`, `signature-refining`, `global-type-optimization`, `abstract-type-refining`, `unsubtyping`, `minimize-rec-groups`, and `reorder-types`) for future campaign threads.
- Recorded three durable beginner-facing corrections in the new dossier: `remove-unused-types` is not part of the current open-world no-DWARF path, public heap types stay for visibility reasons even when local code no longer mentions them, and a live private type keeps its whole rec group while the tiny pass file itself only orchestrates helper-driven scanning and rewriting.

## [2026-04-21] research | refresh Binaryen `simplify-locals` as a source-backed major-gap fallback dossier

- Re-read `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `SL` slice in `agent-todo.md`, the older archived `0076` simplify-locals note, the current in-tree simplify-locals docs, and the official Binaryen `version_129` sources before choosing `simplify-locals` as an explicitly justified already-`deep` fallback now that the `none` queue, implemented-landing queue, tuple-opt fallback, RUME fallback, RUB fallback, and DFE fallback are all closed.
- Added the raw note `docs/wiki/raw/research/0148-2026-04-21-simplify-locals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyLocals.cpp`, `pass.cpp`, `opt-utils.h`, `pass.h`, `linear-execution.h`, `effects.h`, `equivalent_sets.h`, `local-utils.h`, and the major `simplify-locals*` lit files) and distilling the real contract: upstream `simplify-locals` is a staged five-variant family with a first-cycle single-use sink phase, later tee-aware sinking, optional structure formation, late equivalent-copy cleanup, and pass-runner nondefaultable-local fixups, not just dead-local cleanup.
- Refreshed `docs/wiki/binaryen/passes/simplify-locals/` with a rewritten landing page plus refreshed `binaryen-strategy.md` and `wat-shapes.md`, and added the new living pages `implementation-structure-and-tests.md` and `variant-matrix-and-scheduler.md` so the upstream file map, official lit roster, public variant matrix, scheduler placement, and beginner-friendly positive/bailout WAT families are all explicit in the folder.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the simplify-locals dossier now advertises that new implementation/test-map plus public-variant/scheduler surface, and updated the tracker guidance to record that simplify-locals' former `version_129` source-structure / variant-surface gap is now closed too.
- Recorded three durable beginner-facing corrections in the refreshed dossier: Binaryen ships five public simplify-locals variants from one templated pass family rather than one vague locals pass, one-armed `if` result synthesis is explicitly speculative and blocked for nondefaultable locals, and a narrow 2026-04-21 current-main check found only tiny container cleanup while the checked dedicated lit files are unchanged from `version_129`.

## [2026-04-20] research | refresh Binaryen `duplicate-function-elimination` as a source-backed major-gap fallback dossier

- Re-read `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` surface, the older archived DFE note `0067`, the later health rerun note `0079`, and the current in-tree DFE docs before choosing `duplicate-function-elimination` as an explicitly justified already-`deep` fallback now that the `none` queue, implemented-landing queue, tuple-opt fallback, RUME fallback, and RUB fallback are all closed.
- Added the raw note `docs/wiki/raw/research/0147-2026-04-20-duplicate-function-elimination-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DuplicateFunctionElimination.cpp`, `pass.cpp`, `function-utils.h`, `hashed.h`, `opt-utils.h`, and the five dedicated `duplicate-function-elimination*` lit files) and distilling the real contract: upstream DFE is a small hash/equality/rewrite loop with an option-dependent iteration budget, not the broader type-compaction/name-strip cleanup bundle currently implemented locally.
- Refreshed `docs/wiki/binaryen/passes/duplicate-function-elimination/` with a rewritten landing page plus refreshed `binaryen-strategy.md`, `wat-shapes.md`, `starshine-hot-ir-strategy.md`, `type-compaction-and-metadata.md`, and `parity.md`, and added the new living page `implementation-structure-and-tests.md` so the upstream file map, helper dependency story, dedicated lit roster, early-plus-late scheduler placement, survivor-rule, and upstream-vs-local-extra split are all explicit in the folder.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the DFE dossier now advertises that new implementation/test-map plus source-contract split, and updated the tracker guidance to record that DFE's former upstream-vs-local-contract gap is now closed too.
- Recorded three durable beginner-facing corrections in the refreshed dossier: official Binaryen `version_129` DFE only rewrites function references and never performs the local type-compaction/name-strip extras by itself, non-semantic metadata like branch hints and debug locations do not block merges while semantics-altering function annotations do, and a narrow 2026-04-20 freshness check found only a tiny non-semantic current-main drift (`set` -> `unordered_set`) while the dedicated lit files remain unchanged.

## [2026-04-20] research | refresh Binaryen `remove-unused-brs` as a source-backed major-gap fallback dossier

- Re-read `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `RUB` backlog slice in `agent-todo.md`, the older archived Binaryen comparison notes `0070` / `0071`, and the current in-tree RUB docs before choosing `remove-unused-brs` as an explicitly justified already-`deep` fallback now that the `none` queue, implemented-landing queue, tuple-opt fallback, and RUME fallback are all closed.
- Added the raw note `docs/wiki/raw/research/0146-2026-04-20-remove-unused-brs-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`RemoveUnusedBrs.cpp`, `pass.cpp`, `branch-utils.h`, `branch-hints.h`, `cost.h`, `drop.h`, `effects.h`, `gc-type-utils.h`, `localize.h`, `properties.h`, and the full `remove-unused-brs*` lit roster) and distilling the real contract: upstream RUB is a staged function-parallel structured-control pass with flow cleanup, loop cleanup, block sinking, GC BrOn optimization, jump-threading, and a late final optimizer, not just tail-branch stripping.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-brs/` with a rewritten landing page plus a new source-backed `binaryen-strategy.md`, and added the new living pages `implementation-structure-and-tests.md` and `wat-shapes.md` so the upstream file map, helper dependency story, lit-family roster, EH/GC/branch-hints/cost-model surface, and beginner-friendly positive/bailout WAT shapes are all explicit in the folder.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the RUB dossier now advertises that new implementation/test-map plus WAT-shape surface; and updated the tracker guidance to record that RUB's former official-source / shape-catalog gap is now closed too.
- Recorded three durable corrections in the refreshed dossier: Binaryen RUB owns real EH and GC branch cleanup rather than only plain `br` / `return` cleanup, branch-hint propagation and `remove-unused-brs-never-unconditionalize` are part of the official contract, and a narrow 2026-04-20 source check found a small current-main jump-threader drift while the lit-family roster itself remains unchanged.

## [2026-04-20] research | refresh Binaryen `remove-unused-module-elements` as a source-backed major-gap fallback dossier

- Re-read `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `RUME` slice in `agent-todo.md`, the saved optimize log `.artifacts/o4z-wasm-opt-debug.log`, the focused local RUME follow-up notes `0090` and `0091`, and the current in-tree RUME docs before choosing `remove-unused-module-elements` as an explicitly justified already-`deep` fallback now that the `none` queue, implemented-landing queue, and old tuple-opt gap are all closed.
- Added the raw note `docs/wiki/raw/research/0145-2026-04-20-remove-unused-module-elements-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`RemoveUnusedModuleElements.cpp`, `pass.cpp`, `element-utils.h`, `gc-type-utils.h`, `module-utils.h`, `table-utils.h`, `FunctionTypeUtils.cpp`, and the main `remove-unused-module-elements*` lit files) plus the saved optimize log, and distilling the real contract: upstream RUME is a strong-vs-reference-only whole-module graph pass with helper-backed root discovery, queue-driven propagation, function-type cleanup, and kind-specific delete-versus-weaken behavior, not just dead-function removal.
- Refreshed `docs/wiki/binaryen/passes/remove-unused-module-elements/` with a rewritten landing page plus refreshed `binaryen-strategy.md` and `wat-shapes.md`, and added the new living pages `implementation-structure-and-tests.md` and `roots-reference-only-and-nullification.md` so the upstream file map, helper dependencies, sibling `remove-unused-nonfunction-module-elements` mode, official test surface, strong-vs-reference-only root split, active-segment parent retention rules, and non-function nullification story are all explicit in the folder.
- Updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the RUME dossier now advertises that new source-backed implementation/test-map plus roots/reference-only/nullification surface; and updated the tracker guidance to record that RUME's former major source-structure gap is now closed too.
- Recorded three durable beginner-facing corrections in the refreshed dossier: Binaryen RUME is a whole-module graph pass rather than a dead-function filter, `used` and `usedReferenced` are intentionally different states instead of one generic live bit, and some referenced-only non-function elements are weakened through nullification instead of being either fully kept or fully deleted.

## [2026-04-20] research | refresh Binaryen `tuple-optimization` as a source-backed major-gap fallback dossier

- Re-read `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` tuple slice, the older tuple port-plan note `docs/wiki/raw/research/0076-2026-04-01-tuple-optimization-binaryen-port-plan.md`, the newer `code-pushing` dossier that already recorded tuple-opt's exact scheduler neighborhood, the saved generated-artifact optimize log `.artifacts/o4z-wasm-opt-debug.log`, and the current in-tree Starshine tuple docs before choosing `tuple-optimization` as an explicitly justified already-`deep` fallback now that the `none` queue and implemented-landing queue are both clear.
- Added the raw note `docs/wiki/raw/research/0144-2026-04-20-tuple-optimization-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`TupleOptimization.cpp`, `pass.cpp`, the tuple-specific `OptimizeInstructions.cpp` peephole, `wasm.cpp`, `wasm-validator.cpp`, and the dedicated `tuple-optimization.wast` lit file) plus the saved optimize log, and distilling the real contract: upstream tuple-opt is a tiny function-parallel tuple-local splitter built on `uses` / `validUses`, a symmetric copy graph, badness propagation, contiguous scalar-local allocation, and tee-preserving rewrites, not a generic multivalue optimizer.
- Refreshed `docs/wiki/binaryen/passes/tuple-optimization/` with a rewritten landing page plus refreshed `binaryen-strategy.md` and `wat-shapes.md`, and added the new living page `implementation-structure-and-tests.md` to make the upstream file map, helper dependencies, validation/finalize neighbors, official positive and bailout test families, and current-main freshness note explicit; then updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md` so the tuple dossier now advertises that new source-backed implementation/test-map surface.
- Recorded three durable corrections in the refreshed tuple dossier: `tuple-optimization` is about tuple **locals** rather than tuple syntax generally, the direct `tuple.extract(tuple.make(...))` peephole belongs to `OptimizeInstructions.cpp` rather than tuple-opt itself, and a narrow 2026-04-20 freshness check found the tagged `version_129` core tuple pass plus dedicated lit file still matching current `main` on the relevant surfaces.

## [2026-04-20] research | deepen Binaryen `remove-unused-names` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` state (which still has no dedicated `RUN` slice beyond the shared post-SSA cleanup note), the in-tree Starshine implementation and tests in `src/passes/remove_unused_names.mbt`, `src/passes/remove_unused_names_test.mbt`, `src/passes/{optimize,registry_test}.mbt`, and `src/cmd/cmd_wbtest.mbt`, the saved generated-artifact `-O4z` audit summary in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.md`, and the official Binaryen `version_129` sources for `RemoveUnusedNames.cpp`, `pass.cpp`, `branch-utils.h`, `shared-constants.h`, `README.md`, the dedicated `remove-unused-names` golden pair, and the neighboring combo tests with `precompute`, `vacuum`, `remove-unused-brs`, `code-folding`, and `merge-blocks`, then chose `remove-unused-names` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0143-2026-04-20-remove-unused-names-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `remove-unused-names` is not wasm name-section cleanup, but a tiny function-parallel control-label pass built around child-first scope-name-use tracking, dead-label clearing, same-type parent/child block retarget-and-merge, dead-label loop demotion, and delegate-to-caller sentinel cleanup, with much of the visible structural simplification coming later from Binaryen's implicit-block emission rules and neighboring cleanup passes.
- Expanded `docs/wiki/binaryen/passes/remove-unused-names/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `control-names-implicit-blocks-and-delegates.md`, and `wat-shapes.md`; kept the existing `invalid-tag-index-parser-gap.md` page as part of the folder; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `remove-unused-names` from `landing` to `deep`; and updated the tracker guidance to record that the implemented-landing queue is now clear.
- Recorded three durable beginner-facing corrections in the new dossier: Binaryen control names are semantic branch targets rather than debug metadata, explicit same-type parent/child merge and later implicit-block disappearance are related but different mechanisms, and a narrow 2026-04-20 freshness check found no current-main drift in the core `RemoveUnusedNames.cpp` plus dedicated base test pair relative to `version_129`.

## [2026-04-20] research | deepen Binaryen `reorder-locals` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `RL` slice in `agent-todo.md`, the earlier reorder-locals research and scope notes in `docs/wiki/raw/research/0073-2026-04-02-reorder-locals-binaryen-comparison.md` and `0074-2026-04-02-binaryen-multivalue-call-local-disparity.md`, the current in-tree Starshine implementation and tests in `src/passes/{reorder_locals,pass_manager,optimize}.mbt`, `src/passes/reorder_locals_test.mbt`, `src/passes/{optimize_test,registry_test}.mbt`, and `src/cmd/cmd_wbtest.mbt`, and the official Binaryen `version_129` sources for `ReorderLocals.cpp`, `pass.cpp`, the dedicated `reorder-locals*` test pairs, plus the multivalue boundary files `wasm-ir-builder.cpp` and `wasm-stack.cpp`, then chose `reorder-locals` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0142-2026-04-20-reorder-locals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `reorder-locals` is a tiny function-parallel frequency sorter that keeps params fixed, sorts body locals by descending access count plus first-use tie breaks, drops only the zero-count body-local suffix, rewrites local-name maps, and relies on no CFG/liveness/effects/refinalize machinery; the remaining multivalue-call instability belongs to Binaryen's tuple packaging and binary writeback layers, not to `ReorderLocals.cpp` itself.
- Expanded `docs/wiki/binaryen/passes/reorder-locals/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `names-roundtrip-and-porting.md`, and `wat-shapes.md`; kept the existing parity and multivalue-scope pages as part of the folder; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `reorder-locals` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `reorder-locals` to `remove-unused-names`.
- Recorded three durable beginner-facing corrections in the new dossier: upstream `reorder-locals` is not `coalesce-locals` or dead-store elimination, the dedicated print-roundtrip tests mean emitted declaration order is part of the real contract, and current `main` still matches the tagged `version_129` pass file and dedicated tests exactly while the known multivalue-call local-growth issue still lives outside the pass in Binaryen's IR-builder and writer machinery.

## [2026-04-20] research | deepen Binaryen `ssa-nomerge` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current indirect `SSA` state in `agent-todo.md`, the in-tree Starshine implementation and tests in `src/passes/{ssa_nomerge,pass_manager,optimize}.mbt` and `src/passes/ssa_nomerge_test.mbt`, the current artifact-backed parity note in `docs/wiki/binaryen/passes/ssa-nomerge/parity.md`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{md,json}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `SSAify.cpp`, `local-graph.h`, `LocalGraph.cpp`, `ReFinalize.cpp`, `pass.cpp`, the dedicated `ssa-nomerge_enable-simd` golden pair, the shared `ssa.wast` helper surface, and the LocalGraph gtests, then chose `ssa-nomerge` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0141-2026-04-20-ssa-nomerge-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `ssa-nomerge` is not full phi SSA and not a straight-line peephole, but shared `SSAify` logic with merges disabled, driven by whole-function LocalGraph flow, per-set no-merge gating, default-value materialization, and a narrow refinalization step when shared helper rewrites sharpen ref types.
- Expanded `docs/wiki/binaryen/passes/ssa-nomerge/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `merge-shapes-and-canonical-slots.md`, and `wat-shapes.md`; refreshed `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `ssa-nomerge` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `ssa-nomerge` to `reorder-locals`, with `remove-unused-names` as the current backup.
- Recorded three durable beginner-facing corrections in the new dossier: the no-merge decision is made per set rather than per original local, entry param/default values count as real merge inputs, and a narrow 2026-04-20 source comparison found no post-`version_129` semantic drift because current `main` still matches `SSAify.cpp`, the LocalGraph helpers, and the dedicated `ssa-nomerge` golden test exactly.

## [2026-04-20] research | deepen Binaryen `global-struct-inference` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` state (which still has no dedicated `GSI` slice), the current in-tree Starshine implementation in `src/passes/global_struct_inference.mbt`, the focused local tests in `src/passes/global_struct_inference_test.mbt`, the scheduler and closed-world plumbing in `src/passes/{pass_manager,optimize}.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{md,json}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `GlobalStructInference.cpp`, `pass.cpp`, `possible-constant.h`, `subtypes.h`, and the dedicated `gsi.wast` lit file, then chose `global-struct-inference` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0140-2026-04-20-global-struct-inference-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `gsi` is not just a closed-world constant folder, but a layered GC pass with an always-on open-world direct immutable-global fast path, a richer closed-world type-to-global candidate analysis, an at-most-two-unique-values `ref.eq` / `select` rewrite rule, non-constant operand un-nesting into fresh immutable globals, packed/atomic read handling, and the nearby descriptor-facing helper surface.
- Expanded `docs/wiki/binaryen/passes/global-struct-inference/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `closed-world-analysis-and-unnesting.md`, `wat-shapes.md`, and a refreshed `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `global-struct-inference` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `global-struct-inference` to `ssa-nomerge`, with `reorder-locals` and `remove-unused-names` as the current backups.
- Recorded three durable beginner-facing corrections in the new dossier: official Binaryen `gsi` still has open-world optimization behavior even when closed-world type-global analysis is absent, the real profitability rule is about one or two unique values rather than raw global counts, and current `main` still matches the tagged `version_129` semantics aside from comment typo fixes while the dedicated `gsi.wast` file is unchanged.

## [2026-04-20] research | deepen Binaryen `global-refining` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` state (which still has no dedicated `GR` slice), the current in-tree Starshine implementation in `src/passes/global_refining.mbt`, the focused local tests in `src/passes/global_refining_test.mbt`, the scheduler and options coverage in `src/passes/{optimize,pass_manager}.mbt` and `src/passes/registry_test.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{json,md}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `GlobalRefining.cpp`, `pass.cpp`, `lubs.h`, `public-type-validator.h`, `pass.h`, and the dedicated `global-refining.wast` test, then chose `global-refining` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0139-2026-04-20-global-refining-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `global-refining` is a small initializer-plus-`global.set` LUB pass with subtle exported-global legality rules, not a broad control-flow-sensitive global optimizer; the official pass is GC-gated, flow-insensitive, skips all closed-world exports, still allows open-world immutable exported-public refinements, and must retag `global.get` users plus refinalize changed code after declaration rewrites.
- Expanded `docs/wiki/binaryen/passes/global-refining/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `exports-public-types-and-retagging.md`, `wat-shapes.md`, and `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `global-refining` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `global-refining` to `global-struct-inference`, with `ssa-nomerge` and `reorder-locals` as the current backups.
- Recorded three durable beginner-facing corrections in the new dossier: Binaryen's pass is basically initializer-plus-write LUB aggregation rather than whole-program global dataflow, the exported-global matrix is asymmetric in a surprising but source-backed way (`open-world immutable` can refine while current `closed-world` still skips all exports), and a narrow 2026-04-20 source comparison found no post-`version_129` drift because current `main` still matches both `GlobalRefining.cpp` and `global-refining.wast` exactly.

## [2026-04-20] research | deepen Binaryen `once-reduction` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the current `agent-todo.md` state (which still has no dedicated `OR` slice), the current in-tree Starshine implementation in `src/passes/once_reduction.mbt`, the focused local tests in `src/passes/once_reduction_test.mbt`, the scheduler coverage in `src/passes/optimize.mbt` and `src/passes/registry_test.mbt`, the repo's parser / lowering annotation support in `src/wast/{parser,lower_to_lib,module_wast_tests}.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{json,md}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `OnceReduction.cpp`, `pass.cpp`, `intrinsics.h`, and the dedicated `once-reduction.wast` test, then chose `once-reduction` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0138-2026-04-20-once-reduction-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: upstream `once-reduction` is a conservative once-bit plus direct-call module pass that scans for exact guarded once-function shapes, accepts only monotonic nonzero integer writes, propagates dominated guaranteed-set facts through a fixed point over direct-call summaries, supports a narrow `@binaryen.idempotent` fake-global path, and finishes with tiny wrapper / empty-body cleanup guarded against cycles.
- Expanded `docs/wiki/binaryen/passes/once-reduction/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `dominance-propagation-and-cycle-safety.md`, `wat-shapes.md`, and `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `once-reduction` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `once-reduction` to `global-refining`, with `global-struct-inference` as the current fallback.
- Recorded three durable beginner-facing corrections in the new dossier: upstream `version_129` does not actually require the once-global initializer to be zero, the pass's central proof is conservative dominated-path once-bit knowledge rather than “all once-callees have fully completed,” and current `main` matches the tagged `version_129` source and dedicated lit file exactly, so there is no post-tag drift story to paper over here.

## [2026-04-20] research | deepen Binaryen `memory-packing` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the relevant indirect `MP` references in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/memory_packing.mbt`, the focused local tests in `src/passes/memory_packing_test.mbt`, the scheduler coverage in `src/passes/optimize.mbt` and `src/passes/registry_test.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{json,md}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `MemoryPacking.cpp`, `pass.cpp`, and the dedicated `memory-packing_*` / `memory-packing-gc.wast` tests, then chose `memory-packing` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0137-2026-04-20-memory-packing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: `memory-packing` is not only an active-segment splitter, but a whole-module segment-plus-segment-op rewrite pass that checks memory/layout legality, trims profitable zero ranges, rewrites passive `memory.init` / `data.drop` users into `memory.init` plus `memory.fill` sequences, lazily adds drop-state globals when needed, preserves startup/runtime trap behavior, and stays conservative around imported memory, GC data users, and segment-count limits.
- Expanded `docs/wiki/binaryen/passes/memory-packing/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `segment-op-rewrites-and-traps.md`, `wat-shapes.md`, `starshine-hot-ir-strategy.md`, and `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `memory-packing` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `memory-packing` to `once-reduction`, with `global-refining` as the current fallback.
- Recorded three durable beginner-facing corrections in the new dossier: upstream `version_129` `memory-packing` is as much about passive-segment user rewrites as it is about active-segment trimming, the real correctness story depends on explicit trap/drop preservation rather than just zero-byte economics, and a narrow 2026-04-20 source comparison found no semantic post-`version_129` drift because current `main` differs only by comment typo fixes and the dedicated lit files are unchanged.

## [2026-04-20] research | deepen Binaryen `pick-load-signs` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the relevant indirect `pick-load-signs` references in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/pick_load_signs.mbt`, the focused and Binaryen-facing local tests in `src/passes/pick_load_signs_test.mbt`, `src/passes/pass_manager.mbt`, `src/passes/optimize.mbt`, `src/passes/registry_test.mbt`, and `src/cmd/cmd_wbtest.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.{json,md}` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `PickLoadSigns.cpp`, `pass.cpp`, `opt-utils.h`, `properties.h`, the dedicated `pick-load-signs_sign-ext.wast` test, and the neighboring `optimize-instructions-sign_ext.wast` test, then chose `pick-load-signs` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0136-2026-04-20-pick-load-signs-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: `pick-load-signs` is not a generic all-width load-sign canonicalizer, but a tiny function-parallel AST pass over exact non-tee `local.set(load ...)` candidates whose every `local.get` use must match a small `properties.h` sign/zero-extension surface before the pass flips a non-atomic load's `signed_` bit.
- Expanded `docs/wiki/binaryen/passes/pick-load-signs/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; refreshed `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `pick-load-signs` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `pick-load-signs` to `memory-packing`, with `once-reduction` as the current fallback.
- Recorded three durable beginner-facing corrections in the new dossier: upstream `version_129` `pick-load-signs` is effectively i32-only because the helper surface it relies on in `properties.h` only recognizes i32 sign/zero-extension shapes, the dedicated official lit file is intentionally tiny and the broader i64 sign-extension story lives mainly in `optimize-instructions` instead, and a narrow 2026-04-20 freshness check found no post-`version_129` drift because current `main` matches both `PickLoadSigns.cpp` and `pick-load-signs_sign-ext.wast` exactly.

## [2026-04-20] research | deepen Binaryen `heap2local` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `H2L` backlog slices in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/heap2local.mbt`, the focused and Binaryen-aligned suites in `src/passes/heap2local_test.mbt` and `src/passes/heap2local_primary_test.mbt`, the scheduler coverage in `src/passes/optimize_test.mbt` and `src/passes/registry_test.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.json` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `Heap2Local.cpp`, `pass.cpp`, `opt-utils.h`, `pass.h`, `type-updating.h`, and the dedicated `heap2local.wast` test, then chose `heap2local` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0135-2026-04-20-heap2local-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and distilling the real contract: `heap2local` is conservative GC scalarization for nonescaping exclusive allocations, not generic heap-to-stack conversion; it proves nonescape and exclusivity with `EscapeAnalyzer` plus `LazyLocalGraph` / `Parents` / `BranchTargets`, lowers small fixed arrays through a synthetic-struct stage, rewrites direct ref operations and some packed/atomic special cases, and relies on real type/EH/local fixups afterward.
- Expanded `docs/wiki/binaryen/passes/heap2local/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `validation-fixups-and-special-cases.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; refreshed `parity.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `heap2local` from `landing` to `deep`; and advanced the tracker's suggested next implemented-landing target from `heap2local` to `pick-load-signs`, with `memory-packing` as the current fallback.
- Recorded three durable beginner-facing corrections in the new dossier: upstream arrays are handled through an explicit array-to-synthetic-struct stage rather than direct element localization, Binaryen's real validation story is split between in-pass `ReFinalize` / EH-pop repair and generic pass-runner nondefaultable-local fixups, and a narrow 2026-04-20 source comparison found small but real post-`version_129` current-main drift around array/cmpxchg/unreachable corner cases even though the dedicated `heap2local.wast` file only changed by a typo fix.

## [2026-04-20] research | deepen Binaryen `dead-code-elimination` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the active `DCE` backlog slices in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/dead_code_elimination.mbt`, the reduced pass/live-repro/perf/cmd coverage in `src/passes/dead_code_elimination_test.mbt`, `src/passes/dead_code_elimination_live_repro_test.mbt`, `src/passes/perf_test.mbt`, and `src/cmd/cmd_wbtest.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.json` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `DeadCodeElimination.cpp`, `pass.cpp`, `opt-utils.h`, `effects.h`, `type-updating.h`, `eh-utils.h`, `flatten.h`, and the dedicated `dce*` test files, then chose `dead-code-elimination` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0134-2026-04-20-dead-code-elimination-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and shipped tests and distilling the real contract: Binaryen `dce` is not just unreachable-tail trimming and not generic dataflow dead-store analysis, but an early function-parallel cleanup pass that combines child-first unreachable pruning, effect-aware dead-result simplification, dead typed-control voidification, and mandatory type / EH / flatten / refinalize repair.
- Expanded `docs/wiki/binaryen/passes/dead-code-elimination/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `typed-control-voidification-and-eh.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `dead-code-elimination` from `landing` to `deep`; and advanced the tracker's suggested next target from `dead-code-elimination` to `heap2local` with `pick-load-signs` and `memory-packing` as the current fallback implemented-landing targets.
- Recorded three durable beginner-facing corrections in the new dossier: upstream `pass.cpp` understates `dce` by describing it only as unreachable-code removal, dead typed `block` / `if` / `loop` / EH wrappers are often voidified rather than deleted outright, and a narrow 2026-04-20 freshness check found no obvious post-`version_129` drift in the owning `DeadCodeElimination.cpp` / `dce*` test surfaces even though the local HOT implementation has accumulated much larger artifact-driven wrapper and payload-rewrite machinery.

## [2026-04-20] research | deepen Binaryen `heap-store-optimization` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the shared `heap-store-optimization` references in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/heap_store_optimization.mbt`, the reduced pass/cmd/perf coverage in `src/passes/heap_store_optimization_test.mbt`, `src/cmd/cmd_wbtest.mbt`, and `src/passes/perf_test.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.json` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `HeapStoreOptimization.cpp`, `pass.cpp`, `cfg-traversal.h`, `effects.h`, `local-graph.h`, and the dedicated `heap-store-optimization.wast` test, then chose `heap-store-optimization` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0133-2026-04-20-heap-store-optimization-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and shipped tests and distilling the real contract: `heap-store-optimization` is not generic GC dead-store elimination, but a narrow function-parallel pass that folds `struct.set` into nearby fresh `struct.new` families using CFG-local action collection, effect-order checks, `LazyLocalGraph` control-flow safety, default-field materialization, and side-effect-preserving field replacement.
- Expanded `docs/wiki/binaryen/passes/heap-store-optimization/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `swap-safety-and-control-flow.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `heap-store-optimization` from `landing` to `deep`; and advanced the tracker's suggested next target from `heap-store-optimization` to `dead-code-elimination` with `heap2local` as the current fallback implemented-landing target.
- Recorded two durable beginner-facing corrections in the new dossier: upstream `version_129` `heap-store-optimization` is still mainly `struct.set`-into-`struct.new` folding rather than generic heap-store cleanup, and a narrow 2026-04-20 freshness check found current `main` still identical to `version_129` for both `HeapStoreOptimization.cpp` and its dedicated lit test, so there is no post-tag drift story to paper over here.

## [2026-04-20] research | deepen Binaryen `precompute` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the relevant `PC` backlog slices in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/precompute.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.json` and `.artifacts/o4z-wasm-opt-debug.log`, the retired slot-19 raw-output witness notes `0096` and `0105`, and the official Binaryen `version_129` sources for `Precompute.cpp`, `pass.cpp`, `opt-utils.h`, `local-graph.h`, `properties.h`, `wasm-interpreter.h`, and `17` dedicated `precompute*` lit tests, then chose `precompute` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0132-2026-04-20-precompute-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and shipped tests and distilling the real contract: `precompute` is not just constant arithmetic folding, but a bounded compile-time execution pass with child-retention for local/global writes, a block-specific quadratic-work bailout, higher-level partial-select precompute, optional `LazyLocalGraph` local propagation in the `precompute-propagate` mode, GC identity caching, emitability boundaries for refs/strings, and final refinalization.
- Expanded `docs/wiki/binaryen/passes/precompute/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `propagation-partial-precompute-and-gc-identity.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `precompute` from `landing` to `deep`; and advanced the tracker's suggested next target from `precompute` to `heap-store-optimization` with `dead-code-elimination` and `heap2local` as the current fallback implemented-landing targets.
- Recorded three durable beginner-facing corrections in the new dossier: top-level no-DWARF `-O` / `-Os` in Binaryen `version_129` uses plain `precompute` rather than `precompute-propagate`, the propagate variant still matters heavily in aggressive and nested optimizing reruns, and the retired slot-19 generated-artifact failure was a local writeback-safety issue rather than evidence that upstream `precompute` itself is still an open structural corruption family.

## [2026-04-20] research | deepen Binaryen `optimize-instructions` into a full dossier

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the relevant shared hot-batch slices in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/optimize_instructions.mbt`, the saved generated-artifact `-O4z` audit and debug log in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/summary.json` and `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` sources for `OptimizeInstructions.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, and the major helper headers plus `27` dedicated optimize-instructions lit tests, then chose `optimize-instructions` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0131-2026-04-20-optimize-instructions-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and shipped tests and distilling the real contract: `optimize-instructions` is not just constant folding or arithmetic peepholes, but a broad function-parallel canonicalization-and-simplification pass with local bit/sign-ext scanning, boolean and ternary rewrites, memory and bulk-memory cleanup, `call_ref`, GC cast/null-trap logic, and deferred refinalization plus EH-pop repair.
- Expanded `docs/wiki/binaryen/passes/optimize-instructions/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `gc-casts-call_ref-and-trap-sensitive-rewrites.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `optimize-instructions` from `landing` to `deep`; and advanced the tracker's suggested next target from `optimize-instructions` to `precompute` with `heap-store-optimization` and `dead-code-elimination` as the next fallback implemented-landing targets.
- Recorded two durable beginner-facing corrections in the new dossier: upstream `version_129` `visitIf()` is mostly boolean / duplicated-arm cleanup rather than generic constant-if folding, and the retired slot-16 / slot-44 generated-artifact failures were HOT-lower / writeback issues rather than a still-open pass-local corruption family.

## [2026-04-20] research | deepen Binaryen `vacuum` into a full dossier and correct the earlier Chromium drift mix-up

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the local `VQ` backlog slice in `agent-todo.md`, the current in-tree Starshine implementation in `src/passes/{optimize,pass_manager}.mbt`, the saved generated-artifact `-O4z` audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,summary.md}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, and the official Binaryen `version_129` / narrow-`main` sources for `Vacuum.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, `branch-hints.h`, `drop.h`, and a broad `vacuum-*` lit-test sample, then chose `vacuum` as the strongest remaining implemented-landing target.
- Added the raw note `docs/wiki/raw/research/0130-2026-04-20-vacuum-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources and shipped tests and distilling the real contract: `vacuum` is not just `nop` trimming but repeated unused-result effect pruning plus structural cleanup, with special handling for blocks, `if`, `drop`, EH wrappers, TNH trap-path cleanup, and function-level explicit-`unreachable` preservation.
- Expanded `docs/wiki/binaryen/passes/vacuum/` into a real multi-page dossier with a rewritten landing page plus `binaryen-strategy.md`, `effect-pruning-and-traps-never-happen.md`, `wat-shapes.md`, and `starshine-hot-ir-strategy.md`; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; promoted `vacuum` from `landing` to `deep`; and advanced the tracker's suggested next target from `vacuum` to `optimize-instructions` with `precompute` and `dead-code-elimination` as the next fallback landing-page deepening targets.
- Corrected the live vacuum freshness story in the landing page and `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`: a fresh 2026-04-20 source check showed Chromium commit `f284d54...` is the actual 2026-02-27 `Vacuum.cpp` change preserving explicit `unreachable`, Chromium commit `9ee4a25...` is actually a `RemoveUnusedBrs` change, and the `vacuum` safeguard is already present in Binaryen `version_129` rather than being a newer post-tag drift fact.

## [2026-04-20] research | add Binaryen `simplify-locals-notee-nostructure` dossier for an unimplemented aggressive no-tee/no-structure locals pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the neighboring `SLNS` slice in `agent-todo.md`, the older removed-pass note in `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and the official Binaryen `version_129` / narrow-`main` sources for `SimplifyLocals.cpp`, `pass.cpp`, `passes.h`, `opt-utils.h`, and the relevant helper headers, then chose `simplify-locals-notee-nostructure` as the last still-eligible saved-audit `none` target.
- Added the raw note `docs/wiki/raw/research/0129-2026-04-20-simplify-locals-notee-nostructure-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources plus the shipped `simplify-locals-notee-nostructure` and sibling simplify-locals tests, and distilling the real contract: the aggressive `flatten -> simplify-locals-notee-nostructure -> local-cse` prelude uses `SimplifyLocals<false, false, true>` to remove easy single-use local carriers plus late equivalent-get / dead-set cleanup, while deliberately refusing both tee creation and new control-flow result structure and still allowing ordinary nesting into existing consumer positions.
- Added the living folder `docs/wiki/binaryen/passes/simplify-locals-notee-nostructure/` with a landing page plus strategy, variant-surface, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `simplify-locals-notee-nostructure` no longer shows wiki status `none`; and refreshed the suggested next-target list so the saved-audit `none` queue is now fully cleared and `vacuum` is the next highest-value landing-page deepening target.

## [2026-04-20] research | add Binaryen `merge-locals` dossier for an unimplemented higher-aggression alias-local pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the older Batch 1 removed-pass note in `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md`, and confirmed that `agent-todo.md` still had no dedicated `merge-locals` slice, then chose `merge-locals` as the top remaining still-eligible `none` target.
- Added the raw note `docs/wiki/raw/research/0128-2026-04-20-merge-locals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`MergeLocals.cpp`, `pass.cpp`, `passes.h`, `pass-utils.h`, `local-graph.h`, and the shipped `merge-locals` lit test) and distilling the real contract: a higher-aggression function pass that skips named-local functions, builds an eager `LocalGraph`, only accepts locals with exactly one sufficiently simple set whose influenced gets still trace back to that exact set, then normalizes the alias group by either reusing a tiny source local or creating one fresh canonical temp before graph-guided rewrite materialization.
- Added the living folder `docs/wiki/binaryen/passes/merge-locals/` with a landing page plus strategy, LocalGraph/influence, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `merge-locals` no longer shows wiki status `none`; and refreshed the suggested next-target list so `simplify-locals-notee-nostructure` is now the sole remaining `none` target in the current saved-audit queue.

## [2026-04-21] research | add source-confirmed `directize` owner/test map follow-up

- Re-read `docs/README.md`, `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, `agent-todo.md`, and the existing `docs/wiki/binaryen/passes/directize/` folder, then chose an explicitly justified `directize` follow-up because the tracker had no obvious `none` targets left while the dossier still lacked one compact source-confirmed owner/test-map page.
- Added the raw note `docs/wiki/raw/research/0209-2026-04-21-directize-source-confirmation-followup.md`, grounding the follow-up in official Binaryen `version_129` sources (`Directize.cpp`, `pass.cpp`, `passes.h`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, and the shipped `directize_all-features`, `directize-gc`, and `directize-wasm64` tests) and distilling the real contract: a late module pass that first asks shared table-utils code whether any table can optimize by entry, then rewrites only `CallIndirect` / tail-`CallIndirect` sites, classifies constant targets as known direct call vs known trap vs unknown, delegates the exact `select` case to shared call-utils lowering, and refinalizes edited functions afterward.
- Added `docs/wiki/binaryen/passes/directize/implementation-structure-and-tests.md`, refreshed the landing and strategy pages, and updated `docs/wiki/binaryen/passes/tracker.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the directize dossier now makes the owner-file split and shipped-test coverage explicit instead of leaving that map implicit across several prose pages.

## [2026-04-20] research | add Binaryen `flatten` dossier for an unimplemented aggressive flat-IR preparation pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the older batch-intent notes in `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md` plus `docs/0065-2026-03-24-ir2-execution-plan.md`, and confirmed that `agent-todo.md` still had no dedicated flatten slice, then chose `flatten` as the highest-priority still-eligible `none` target.
- Added the raw note `docs/wiki/raw/research/0127-2026-04-20-flatten-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Flatten.cpp`, `flat.h`, `pass.cpp`, `passes.h`, and the shipped `flatten`, `flatten_all-features`, `flatten-eh-legacy`, `opt_flatten`, `flatten_rereloop`, and `flatten_i64-to-i32-lowering` tests) and distilling the real contract: an aggressive `-O4` / `-O4z` flat-IR normalizer built around postorder `preludes` plus named-target temp locals, value-carrying control-flow elimination, tee removal, branch-payload routing, function-body return wrapping, EH pop fixups after inserted catch-local blocks, and the current `BrOn*` / `TryTable` hard-stop plus selective non-nullability caveats.
- Added the living folder `docs/wiki/binaryen/passes/flatten/` with a landing page plus strategy, flat-IR/prelude, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `flatten` no longer shows wiki status `none`; and refreshed the suggested next-target list so `merge-locals` now leads the remaining saved-audit-only `none` targets.

## [2026-04-20] research | add Binaryen `directize` dossier for an unimplemented late indirect-call cleanup pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the neighboring late-tail dossiers culminating in `reorder-globals`, and the relevant `agent-todo.md` slice, then chose `directize` as the highest-priority still-eligible shared late-tail target that remained `none`.
- Added the raw note `docs/wiki/raw/research/0126-2026-04-20-directize-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Directize.cpp`, `pass.cpp`, `passes.h`, `call-utils.h`, `table-utils.{h,cpp}`, `type-updating.h`, and the shipped `directize_all-features`, `directize-gc`, and `directize-wasm64` tests) and distilling the real contract: a final late tail pass that computes whole-module table facts first, only rewrites `call_indirect` / `return_call_indirect`, classifies constant targets as known call vs known trap vs unknown, lowers a narrow `select` target shape to `if` plus fresh locals, preserves side-effectful children when replacing traps with `unreachable`, and relies on the optional `directize-initial-contents-immutable` mode plus post-rewrite refinalization to keep tricky imported/mutable and GC-typed cases honest.
- Added the living folder `docs/wiki/binaryen/passes/directize/` with a landing page plus strategy, table-info/immutability, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `directize` no longer shows wiki status `none`; and refreshed the suggested next-target list so `flatten` is now the leading remaining `none` target.

## [2026-04-20] research | add Binaryen `reorder-globals` dossier for an unimplemented late global-layout pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the neighboring freshly-landed `string-gathering` dossier, and the relevant `agent-todo.md` slice, then chose `reorder-globals` as the highest-priority still-eligible shared late-tail target that remained `none`.
- Added the raw note `docs/wiki/raw/research/0125-2026-04-20-reorder-globals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`ReorderGlobals.cpp`, `pass.cpp`, `passes.h`, `pass.h`, `wasm-traversal.h`, `topological_sort.h`, `wasm.h`, `GlobalStructInference.cpp`, and the shipped `reorder-globals{,-real}.wast` tests) and distilling the real contract: a late count-plus-dependency-search global-layout pass that counts both `global.get` and `global.set` in functions and module code, builds an initializer dependency DAG, tries four candidate order heuristics, scores them with the true observed use counts, keeps imports before defined globals, intentionally no-ops in public mode below `128` globals, and relies on the separate `reorder-globals-always` helper for testing and small-module fixup callers such as `GlobalStructInference`.
- Added the living folder `docs/wiki/binaryen/passes/reorder-globals/` with a landing page plus strategy, size-model/dependency-order, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `reorder-globals` no longer shows wiki status `none`; and refreshed the suggested next-target list so `directize` is now the leading remaining shared late-tail `none` target.

## [2026-04-20] research | add Binaryen `string-gathering` dossier for an unimplemented late string-hoisting pass

- Audited `docs/README.md`, the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the pass-folder catalog in `docs/wiki/binaryen/passes/index.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, the string literal background page in `docs/wiki/strings/string-const-surface.md`, and the relevant `agent-todo.md` slice, then chose `string-gathering` as the highest-priority still-eligible `none` target after the same-day late-tail dossiers for `dae-optimizing`, `inlining-optimizing`, `simplify-globals-optimizing`, and `duplicate-import-elimination` had already landed.
- Added the raw note `docs/wiki/raw/research/0124-2026-04-20-string-gathering-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`StringLowering.cpp`, `pass.cpp`, `passes.h`, `string-utils.{h,cpp}`, `module-utils.h`, `wasm-traversal.h`, the shipped `string-gathering.wast` test, and the neighboring `propagate-globals-globally.wast` test) and distilling the real contract: a late module-wide `StringConst` scan plus canonical immutable-global rewrite that deduplicates by literal payload, reuses only existing direct immutable `(ref string)` globals, preserves the reused initializer slots to avoid self-trampling, performs only a validity-first defining-global reorder before `reorder-globals`, and notably does not lower string operations, simplify preexisting `global.get` users, or run nested cleanup passes.
- Added the living folder `docs/wiki/binaryen/passes/string-gathering/` with a landing page plus strategy, reuse/naming/ordering, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; advanced the tracker so `string-gathering` no longer shows wiki status `none`; and recorded the current local caveat that the tracker/backlog treat the pass as real late boundary work even though the literal name is still absent from `src/passes/optimize.mbt`’s boundary-only array.

## [2026-04-20] research | add Binaryen `duplicate-import-elimination` dossier for an unimplemented late import-alias pass

- Audited the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, and the relevant `agent-todo.md` slice, then chose `duplicate-import-elimination` as the highest-priority still-eligible `none` target after the earlier same-day late-boundary dossiers landed.
- Added the raw note `docs/wiki/raw/research/0123-2026-04-20-duplicate-import-elimination-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DuplicateImportElimination.cpp`, `pass.cpp`, `import-utils.{h,cpp}`, `opt-utils.h`, `wasm.h`, and the shipped `duplicate-import-elimination.wast` test) and distilling the real contract: a tiny late module alias-cleanup pass that keeps the first imported function/global/table/memory for an exact import-identity key, rewrites direct calls plus `ref.func`, exports, start, global ops, bulk table ops, bulk memory ops, and module-level init expressions to that canonical name, then removes the redundant import declarations immediately, while also calling out the current tag omission, the table-key nullability footnote, and the still-open active-segment target-name reading caveat.
- Added the living folder `docs/wiki/binaryen/passes/duplicate-import-elimination/` with a landing page plus strategy, identity/rewrite-surface, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `duplicate-import-elimination` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `simplify-globals-optimizing` dossier for an unimplemented late global pass

- Audited the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}`, the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, and the relevant `agent-todo.md` slice, then chose `simplify-globals-optimizing` as a still-eligible high-value late boundary/global cleanup target after the earlier same-day `dae-optimizing` and `inlining-optimizing` dossiers landed.
- Added the raw note `docs/wiki/raw/research/0122-2026-04-20-simplify-globals-optimizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyGlobals.cpp`, `pass.cpp`, `pass.h`, `effects.h`, `linear-execution.h`, `properties.h`, the `simplify-globals-*` tests, and `propagate-globals-globally.wast`) and distilling the real contract: a multi-phase late module/global pass that discovers practical immutability, folds one-time global initializers, removes dead or `read-only-to-write` global writes, canonicalizes immutable copy chains, distinguishes startup-time propagation into later globals and offsets from runtime propagation in linear code traces, and reruns only the default function optimization pipeline on touched functions without prepended `precompute-propagate`.
- Added the living folder `docs/wiki/binaryen/passes/simplify-globals-optimizing/` with a landing page plus strategy, linear-traces/read-only-to-write/reruns, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `simplify-globals-optimizing` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `inlining-optimizing` dossier for an unimplemented late boundary pass

- Audited the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, and the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, then chose `inlining-optimizing` as the highest-priority still-eligible target after the earlier same-day dossiers landed.
- Added the raw note `docs/wiki/raw/research/0121-2026-04-20-inlining-optimizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`Inlining.cpp`, `pass.cpp`, `opt-utils.h`, `FunctionSplitter.cpp`, `possible-contents.h`, `cost.h`, and the shipped `inlining.wast` lit test) and distilling the real contract: a late whole-module inliner with root/escape conservatism, size and single-use heuristics, precise `call_ref` target reasoning, structured partial inlining through splitting, conditional callee removal, and the nested `precompute-propagate` plus default-function rerun on touched functions.
- Added the living folder `docs/wiki/binaryen/passes/inlining-optimizing/` with a landing page plus strategy, planning/partial-inlining/reruns, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `inlining-optimizing` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `dae-optimizing` dossier for an unimplemented late boundary pass

- Audited the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the updated pass tracker in `docs/wiki/binaryen/passes/tracker.md`, the saved generated-artifact `-O4z` skipped-slot audit in `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, and the saved Binaryen debug log in `.artifacts/o4z-wasm-opt-debug.log`, then chose `dae-optimizing` as the highest-priority still-eligible target after the earlier same-day dossiers landed.
- Added the raw note `docs/wiki/raw/research/0120-2026-04-20-dae-optimizing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`DeadArgumentElimination.cpp`, `pass.cpp`, `opt-utils.h`, `param-utils.h`, `return-utils.h`, `lubs.h`, `type-updating.h`, and the shipped `dae-*` lit tests) and distilling the real contract: a closed-world direct-call boundary pass that can refine GC param/result types, materialize constant actuals into the callee, localize hard call operands before deletion, remove dropped returns with `call; unreachable` repair for uninhabitable old results, and then rerun `precompute-propagate` plus the default function optimization pipeline on touched functions.
- Added the living folder `docs/wiki/binaryen/passes/dae-optimizing/` with a landing page plus strategy, signature-updates-and-nested-reruns, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `dae-optimizing` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `local-cse` dossier for an unimplemented local tree-reuse pass

- Audited the canonical no-DWARF scheduler in `docs/wiki/binaryen/no-dwarf-default-optimize-path.md`, the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json`, and confirmed that upstream `local-cse` is still a real missing Starshine pass in both the ordinary late no-DWARF local-cleanup cluster and the extra aggressive `flatten -> simplify-locals-notee-nostructure -> local-cse` `-O4z` prelude, with saved top-level skipped slots `11` and `31` and `36` total `local-cse` executions visible in the saved full debug log because of nested optimizing reruns.
- Added the raw note `docs/wiki/raw/research/0119-2026-04-20-local-cse-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`LocalCSE.cpp`, `pass.cpp`, `opt-utils.h`, `linear-execution.h`, `effects.h`, `properties.{h,cpp}`, `intrinsics.h`, `cost.h`, and the shipped `local-cse` lit test) and distilling the real contract: a scan/check/apply temp-local reuse pass for repeated whole expression trees, with parent-over-child request cancellation, limited basic-block-plus-adjacent-window semantics, trap-ignoring but effect-respecting invalidation, a narrow idempotent-call carveout, shallow-generative GC/call exclusions, and explicit size/cost profitability gates.
- Added the living folder `docs/wiki/binaryen/passes/local-cse/` with a landing page plus strategy, basic-block/windows/barriers, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `local-cse` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `coalesce-locals` dossier for an unimplemented late local-slot pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}` and confirmed that upstream `coalesce-locals` is still a real missing top-level Starshine slot twice in the saved ordered replay, at slots `30` and `35`, while the same saved Binaryen debug log also shows many later reruns of the same local-cleanup neighborhood under the optimizing passes.
- Added the raw note `docs/wiki/raw/research/0118-2026-04-20-coalesce-locals-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`CoalesceLocals.cpp`, `pass.cpp`, `opt-utils.h`, `liveness-traversal.h`, `numbering.h`, `utils.h`, and the shipped `coalesce-locals` lit test) and distilling the real contract: a no-spilling register-allocation-like local coalescer with exact-type matching, value-aware interference instead of plain lifetime overlap, zero-init and param-entry rules, backedge copy weighting, greedy order sensitivity, an optional non-default learning variant, and dead-set cleanup plus possible refinalization after rewrite.
- Added the living folder `docs/wiki/binaryen/passes/coalesce-locals/` with a landing page plus strategy, interference/order, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `coalesce-locals` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `simplify-locals-nostructure` dossier for an unimplemented early locals pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}` and confirmed that upstream `simplify-locals-nostructure` is still a real missing top-level Starshine slot at slot `22`, while the same saved Binaryen debug log also shows many later reruns of the same early local-cleanup neighborhood under the wider optimize pipeline.
- Added the raw note `docs/wiki/raw/research/0117-2026-04-20-simplify-locals-nostructure-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`SimplifyLocals.cpp`, `pass.cpp`, `opt-utils.h`, `local-utils.h`, `effects.h`, `equivalent_sets.h`, `linear-execution.h`, `properties.h`, and the shipped `simplify-locals-*` tests) and distilling the exact `SimplifyLocals<true, false, true>` variant identity, the first-cycle single-use sink rule, the later tee behavior, the linear-trace invalidation model, the still-active late equivalent-get and dead-set cleanup phases, and the precise “no new block / if / loop returns yet” boundary.
- Added the living folder `docs/wiki/binaryen/passes/simplify-locals-nostructure/` with a landing page plus strategy, variant-surface, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `simplify-locals-nostructure` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `local-subtyping` dossier for an unimplemented GC/local pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}` and confirmed that upstream `local-subtyping` is still a real missing top-level Starshine slot at slot `29`, while the saved Binaryen debug log also shows repeated later reruns of the same `optimize-casts -> local-subtyping -> coalesce-locals` neighborhood in the wider optimize pipeline.
- Added the raw note `docs/wiki/raw/research/0116-2026-04-20-local-subtyping-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`LocalSubtyping.cpp`, `pass.cpp`, `opt-utils.h`, `lubs.h`, `type-updating.h`, `local-structural-dominance.h`, the README pass summary, and the shipped `local-subtyping` test) and distilling the tiny fact collector, the non-param / non-tuple scope limits, the least-upper-bound narrowing rule, the dominance-aware `LocalUpdater` rewrite contract, and the key named-block / catch / non-null / dead-code shape lessons.
- Added the living folder `docs/wiki/binaryen/passes/local-subtyping/` with a landing page plus strategy, LUB/dominance, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `local-subtyping` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `code-pushing` dossier for an unimplemented early control-flow pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}` and confirmed that upstream `code-pushing` is still a real missing top-level Starshine slot at slot `20`, while the saved Binaryen debug log also shows repeated nested `precompute-propagate -> code-pushing -> tuple-optimization` reruns later in the same full optimization run.
- Added the raw note `docs/wiki/raw/research/0115-2026-04-20-code-pushing-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`CodePushing.cpp`, `pass.cpp`, `opt-utils.h`, `effects.h`, and the shipped `code-pushing*` tests) and distilling the contiguous-suffix block scan, the `BranchSeeker` / `Pusher` split, the asymmetry between one-arm and two-arm `if` sinking, the local `benefit > cost` heuristic, the option-sensitive trap barrier story, and the scheduler interaction with `tuple-optimization` plus `simplify-locals-nostructure`.
- Added the living folder `docs/wiki/binaryen/passes/code-pushing/` with a landing page plus strategy, segment/barrier, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `code-pushing` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `rse` dossier for an unimplemented late local-cleanup pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.json,skipped-unimplemented-slots.json}` and confirmed that upstream `rse` is still a real missing top-level Starshine slot at slot `46`, while the saved Binaryen debug log also shows repeated nested `optimize-instructions -> heap-store-optimization -> rse -> vacuum` cleanup clusters later in the same full run.
- Added the raw note `docs/wiki/raw/research/0114-2026-04-20-rse-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`RedundantSetElimination.cpp`, `pass.cpp`, `opt-utils.h`, `local-graph`, `liveness`, `numbering`, `properties`, the README pass summary, and the shipped `rse` / `rse-gc` tests) and distilling the locals-only scope, the CFG/value-tracking plus influence-liveness algorithm, the same-block read rewrite rule, the deliberate loop conservatism, the GC refined-local substitution contract, and the main “name sounds broader than implementation” warning.
- Added the living folder `docs/wiki/binaryen/passes/rse/` with a landing page plus strategy, CFG/value-tracking, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `rse` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `optimize-casts` dossier for an unimplemented GC/local pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json` and confirmed that upstream `optimize-casts` is still a real missing top-level Starshine slot at slot `28`, while the saved Binaryen debug log also shows many later nested reruns of the same pass inside the optimizing cleanup pipeline.
- Added the raw note `docs/wiki/raw/research/0113-2026-04-20-optimize-casts-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`OptimizeCasts.cpp`, `pass.cpp`, `opt-utils.h`, `linear-execution`, `properties`, `effects`, `utils`, and the shipped `optimize-casts` tests) and distilling the two-phase earlier-motion vs later-reuse algorithm, the GC gate, the barrier model, the refinalization requirement, and the important “`ref.cast` plus `ref.as_non_null` only” scope limit.
- Added the living folder `docs/wiki/binaryen/passes/optimize-casts/` with a landing page plus strategy, two-phase-dataflow, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `optimize-casts` no longer shows wiki status `none`.

## [2026-04-20] maintain | add Binaryen pass wiki / implementation tracker

- Added `docs/wiki/binaryen/passes/tracker.md` as the canonical cross-pass status board, grounding implementation state in `src/passes/optimize.mbt`, missing-pass scope in the canonical no-DWARF path plus the saved generated-artifact `-O4z` audit, and wiki state in the presence or absence of dedicated living pass pages.
- The tracker now separates active implemented passes from the unique unimplemented pass set, records wiki depth (`deep`, `landing`, `dossier`, `none`), and makes the current gap concrete: all `19` active implemented passes now have a dedicated living page, while only `1` of the `19` unique unimplemented passes observed in the saved `-O4z` audit has a dedicated dossier so far.
- Updated `docs/wiki/binaryen/passes/index.md` and `docs/wiki/index.md` so future pass work has one obvious place to check before asking whether a pass is already implemented, already documented, or still missing both.

## [2026-04-20] research | add Binaryen `code-folding` dossier for an unimplemented late no-DWARF pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/skipped-unimplemented-slots.json` and confirmed that upstream `code-folding` is still a real missing top-level Starshine slot in the current ordered replay at slot `38`, with repeated nested reruns of the same late cluster also visible later in the saved Binaryen debug log.
- Added the raw note `docs/wiki/raw/research/0112-2026-04-20-code-folding-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`CodeFolding.cpp`, `pass.cpp`, `opt-utils.h`, `branch-utils`, `effects`, `eh-utils`, `label-utils`, `utils`, and the shipped `code-folding` tests) and distilling the durable algorithm split between expression-exit folding and function-ending tail folding, the movement-safety checks, the size heuristics, the EH fixups, and the main bailout families.
- Added the living folder `docs/wiki/binaryen/passes/code-folding/` with a landing page plus strategy, terminating-tail, and WAT-shape pages; updated `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and `docs/wiki/index.md`; and advanced the tracker so `code-folding` no longer shows wiki status `none`.

## [2026-04-20] research | add Binaryen `merge-blocks` dossier for an unimplemented `-O4z` pass

- Audited the saved generated-artifact `-O4z` evidence in `.artifacts/o4z-wasm-opt-debug.log` plus `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/{summary.md,skipped-unimplemented-slots.json}` and confirmed that upstream `merge-blocks` is still a real missing top-level Starshine slot twice in the current ordered replay, at slots `39` and `42`.
- Added the raw note `docs/wiki/raw/research/0111-2026-04-20-merge-blocks-binaryen-research.md`, grounding the pass in official Binaryen `version_129` sources (`MergeBlocks.cpp`, `pass.cpp`, `branch-utils`, `effects`, `wasm-traversal`, and the shipped merge-blocks tests) and distilling the key durable facts: tail-child-only matching, unnamed-or-same-name gating, the named-child effect barrier, the whole-function same-name shadow bailout, and mandatory post-splice refinalization.
- Added the living folder `docs/wiki/binaryen/passes/merge-blocks/` with a landing page plus beginner-friendly strategy and WAT-shape pages, then updated `docs/wiki/binaryen/passes/index.md` and `docs/wiki/index.md` so the wiki now exposes this dossier as explicit unimplemented-pass research instead of leaving it buried in backlog notes.

## [2026-04-19] maintain | record natural exact-result param/local return follow-up

- Continued the post-`1892cad` natural exact-ref widening in `src/validate/gen_valid.mbt` and `src/validate/validate.mbt`: exact-result functions now also recognize an already-available exact param/local route before injecting a fallback exact local, so descriptor-bearing returns can stay on the pre-existing real local path instead of forcing an extra seed local.
- Added a focused validate-package test that locks the deterministic exact-param return route and proves the resulting exact-ref module still validates without the extra local seeding.

## [2026-04-19] maintain | record natural exact-result callable return follow-up

- Continued the post-`a4be731` natural exact-ref widening in `src/validate/gen_valid.mbt` and `src/validate/validate.mbt`: exact-result functions now only seed a matching exact local when no existing real route is already available, and natural exact returns can now flow through matching direct-call or `ref.func` + `call_ref` chains instead of collapsing back to forced local seeding.
- Added focused validate-package tests that lock both deterministic natural exact-result callable routes: one imported-helper direct-call return path and one exported-helper `call_ref` return path, while keeping the earlier local-return regression guard intact.

## [2026-04-19] maintain | record natural exact-result local return follow-up

- Re-checked the post-`a4be731` natural exact-ref widening state in `src/validate/gen_valid.mbt`, `src/validate/validate.mbt`, and `docs/wiki/validate/fuzz-hardening.md`.
- Landed a narrower follow-up for exact descriptor result plumbing: natural `gen_valid` now seeds exact-result functions with matching exact locals and prefers `local.get` / existing exact globals / callable results before falling back, so exact concrete refs can return through a real natural path instead of only direct null synthesis.
- Added a focused validate-package test that builds a minimal descriptor-bearing module around `gen_valid_tfunc(...)` and locks the exact-result natural local-return route without yet tightening the broader stochastic exact-result assertions.


## [2026-04-19] maintain | record natural `gen-valid` exact/bulk-array/mixed-catch widening

- Reviewed the widened natural valid-generation follow-up in `src/validate/gen_valid.mbt` and `src/validate/validate.mbt` after the rec-group metadata stabilization landed.
- Recorded that natural mode now also synthesizes exact typed-null descriptor probe chains, direct `array.new_data` / `array.new_elem` plus `array.init_data` / `array.init_elem` routes for the GC helper arrays when passive data/elem prerequisites are present, branchier multi-catch `try_table` exception shapes instead of only the earlier single-catch exnref forms, and exact concrete descriptor refs through generated locals/globals using exact-aware null initializers.
- Updated `docs/wiki/validate/fuzz-hardening.md` so the living docs now describe the newly landed natural-mode breadth and narrow the remaining follow-up gap list again.

## [2026-04-19] fix | stabilize `gen-valid` recursive metadata and flat invalid type indexing

- Normalized appended `RecType` metadata to absolute type indices before storing subtypes in the long-lived validator environment, which closes the later code-body mismatch on descriptor-bearing rec groups after the type section has already validated.
- Taught `gen_valid_module_with_config(...)` to retry natural and coverage-forced candidates until they validate, then updated the validate-package widening checks to aggregate stochastic evidence across multiple generated modules instead of overfitting to one seed-local specimen.
- Updated `src/validate/invalid_fuzzer.mbt` to use flat subtype counts instead of raw `RecType` entry counts when appending or indexing function/tag/type mutations, restoring the expected invalid-family outcomes for start/datacount/name-section strategies after wider rec-group seeds became common.

## [2026-04-19] maintain | record natural-route `gen-valid` descriptor/string/exn widening

- Continued the post-prelude `gen-valid` widening in `src/validate/gen_valid.mbt` and `src/validate/validate.mbt`: natural mode can now occasionally allocate concrete struct/array/descriptor/string-helper rec types, seed descriptor-aware globals plus non-func typed tables, emit typed expr element segments for those tables, and synthesize non-prelude body values for descriptor-bearing structs, descriptor refs, string helper arrays, exnref `try_table` shapes, and label-compatible `br_on_non_null` / `br_on_cast` / `br_on_cast_fail` blocks.
- Added validate-package coverage that natural mode now reaches concrete typed tables and GC-typed element segments, and that coverage-forced mode keeps a real non-funcref typed-table plus typed-elem route instead of only the earlier prelude-heavy function-table surface.
- Updated `docs/wiki/validate/fuzz-hardening.md` so the living docs now describe the newly landed natural-mode route and narrow the remaining widening gaps.

## [2026-04-19] maintain | record coverage-forced `gen-valid` descriptor/string/exn widening

- Reviewed the widened valid-generation sources `src/validate/gen_valid.mbt` and `src/validate/validate.mbt` to capture the next route after the landed struct/array/ref-heavy rec-group work.
- Recorded that `coverage-forced` `gen-valid` now also emits descriptor-bearing struct pairs plus string-helper array types, widens natural ref pools into `stringref` and `exnref`, and deliberately covers `struct.new_default_desc`, `ref.get_desc`, `ref.test_desc`, `ref.cast_desc_eq`, `br_on_non_null`, `br_on_cast`, `br_on_cast_fail`, string helper instructions, `throw`, `throw_ref`, and `try_table` catch shapes while keeping modules valid.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the descriptor/string/cast/exception widening route and point future follow-up toward natural-mode synthesis beyond the current coverage-forced prelude.

## [2026-04-18] maintain | record coverage-forced `gen-valid` GC/ref widening

- Reviewed the landed valid-generation and invalid-seed plumbing in `src/validate/gen_valid.mbt`, `src/validate/gen_invalid.mbt`, `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and their wbtests to capture the next widening route after the earlier extern/segment/bulk-op expansion.
- Recorded that `coverage-forced` `gen-valid` now emits concrete struct/array type defs plus a rec-group shape, wider abstract GC-family refs in natural generation, typed ref-heavy globals/locals/tables, and deterministic GC/ref instruction coverage including `struct.new_default`, `struct.get`, `struct.set`, `array.new_default`, `array.get`, `array.set`, `array.len`, `ref.test`, `ref.cast`, `ref.eq`, `ref.as_non_null`, `call_ref`, and `br_on_null`.
- Also recorded the compatibility guard that the AST/binary invalid seed presets now use a slightly smaller coverage-forced config so curated invalid-family expectations stay stable instead of being perturbed by the newer concrete GC type graph.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the broadened GC/ref surface and the invalid-seed compatibility carveout.

## [2026-04-18] research | audit current Node package surface and validate-port priorities

- Audited the checked-in `node/` package against the active MoonBit package signatures in `src/*/pkg.generated.mbti`, the current Node build notes in `node/README.md`, and the disabled generation / checked-in-artifact rules in `scripts/lib/generate-node-package.mjs` plus `scripts/lib/build-node-package.mjs`.
- Added the raw note `docs/wiki/raw/research/0110-2026-04-18-node-package-api-audit.md` to capture the current state: the Node package is working for its smoke-tested surface on `Node v25.9.0`, but it is now a hand-maintained/frozen wrapper layer rather than a regenerated mirror, only `7` of the `16` active repo packages are exported to Node, `cmd` currently has real `.d.ts` versus runtime export drift, and `validate` is the largest exported-surface gap.
- Added the living summary page `docs/wiki/tooling/node-package-surface.md` and updated `docs/wiki/index.md` so the durable wiki now keeps the compact conclusions: the highest-value `validate` APIs to port next are tracing, failure-family classification, targeted defined-function validation, configured valid generation, and stable invalid-repro registry helpers, while low-level `tc_state_*` internals and raw invalid-generator plumbing stay low priority.
- Verification for this audit: `node --version`, `npm --version`, `moon version`, `cd node && node --test test/smoke.test.mjs`, and `cd node && node --test test/examples.test.mjs`.

## [2026-04-18] maintain | record widened `gen-valid` extern, segment, and bulk-op coverage

- Reviewed the already-modified valid-generation sources `src/validate/gen_valid.mbt` and `src/validate/validate.mbt` to capture the next breadth step after the recent `gen_invalid` widening: the `gen-valid` path now mixes non-function imports, broader count ranges, passive data plus passive/declarative element segments, expr-backed element kinds, randomized active offsets/data payloads, and bulk memory/table instruction families in both natural and coverage-forced generation.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the wider `gen-valid` surface instead of only the older function-import/active-segment core.

## [2026-04-18] research | retire generated O4z optimize-instructions slot44 replay

- Re-read the slot-44 capture in `docs/wiki/raw/research/0100-2026-04-18-generated-o4z-optimize-instructions-slot44-func1818-stack-underflow.md`, the generated-artifact audit summary in `docs/wiki/raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md`, the current `optimize-instructions` landing page, and the in-tree native cmd replay tests before checking whether the saved predecessor was still a live blocker.
- Added `docs/wiki/raw/research/0109-2026-04-18-generated-o4z-optimize-instructions-slot44-retired-by-replay-verification.md` to record that the exact saved slot-43 predecessor now replays successfully, emits `wasm-tools`-valid output, and matches Binaryen at the normalized-WAT and canonical-function level, while making it explicit that the likely root cause is the already-landed shared HOT-lower guard work from `0103` and `0104` rather than a new pass-local rewrite in this run.
- Updated `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/remove-unused-brs/parity.md`, and `docs/wiki/index.md` so the living wiki no longer treats slot `44` as open, now records that the full generated-artifact hard-corruption set is retired, and drops the stale slot-40-open wording that had survived after `0108`.

## [2026-04-18] maintain | record funcref-start and decode-corruption invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include imported-start `funcref`-parameter invalid modules, the binary lane now also covers malformed section-size ULEBs plus section-payload length overflows, and the inline text lane now also includes malformed `f64` bad-opcode, imported `funcref`-start, and incompatible memory-maximum fixtures.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the broader imported-start surface, the added decode-front-end corruption families, and thirteen-wide committed text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record externref-start and twelfth-stage invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: the AST and binary lanes now include imported-start `externref`-parameter invalid modules, the inline text lane now includes the matching invalid start fixture, and the committed spec-seed lane now advances to a twelfth malformed/invalid/unlinkable trio.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the wider imported-start surface and twelve-wide committed text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader imported-start and eleventh-stage invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include imported-start param+result, multi-param, and `f32`-param invalid modules, while the text lanes now also include start-index-out-of-range plus incompatible memory/table import-shape cases and the next stage of committed spec-seed picks.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe deeper imported-start breadth in AST/binary and eleven-wide text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record field-name and tenth-stage invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include field-name invalid modules, while the text lanes now include another malformed bad-opcode case, an imported multi-param start case, another incompatible global import type case, and the next stage of committed spec-seed picks.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe broader field-name AST/binary coverage and ten-wide text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record local/label name and ninth-stage invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include local-name and label-name invalid modules, while the text lanes now include another malformed bad-opcode case, an imported multi-param start case, an alternate global-type unlinkable case, and ninth-stage spec seeds.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe broader name-section AST/binary coverage and nine-wide text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] research | retire generated O4z RUB slot40 block-stack leak

- Re-read the slot-40 capture in `docs/wiki/raw/research/0099-2026-04-18-generated-o4z-rub-slot40-block-stack-leak.md`, the generated-artifact audit summary in `docs/wiki/raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md`, the current RUB parity page, and the in-tree `remove-unused-brs` implementation before reworking the later ordered replay.
- Added the repair note `docs/wiki/raw/research/0108-2026-04-18-generated-o4z-rub-slot40-retired-by-tail-value-if-rewrite-guard.md` to record that the unsafe `rewrite-tail-value-if-simple-payload-arm` family is now retired conservatively, along with the slot-37 predecessor replay evidence and the extracted `Func 3863` artifact-backed guard.
- Updated `docs/wiki/binaryen/passes/remove-unused-brs/parity.md` and `docs/wiki/index.md` so the living docs now describe slot-40 as retired, name the conservative guard, and point future work at a narrower oracle-backed re-enable slice instead of leaving the old corruption description live.

## [2026-04-18] maintain | record imported-start and eighth-stage invalid widening

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include imported-start signature failures in both parameter and result directions, while the text lanes now include one more malformed bad-opcode case, an imported param+result start case, a global mutability unlinkable case, and eighth-stage spec seeds.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe imported-start breadth in AST/binary and eight-wide text/spec stage families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record another three-front invalid widening pass

- Reviewed the already-modified validator and fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` to capture another widening step: AST and binary coverage now include exported-memory and imported-tag invalid modules, while the text lanes now include malformed `i64` hex-token, imported-result-bearing start, and incompatible tag import cases plus seventh-stage spec seeds.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST export/import surface, the binary export/import surface, and the text/spec fixture depth as broader than the earlier six-wide state.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record a sixth text/spec widening pass

- Reviewed the already-modified text invalid sources `src/fuzz/invalid_text.mbt` and `src/fuzz/invalid_text_wbtest.mbt` to capture another fixture-breadth step: the inline text lane now includes malformed `i32` hex-token, imported-start-param, and incompatible-global-import-type cases, and the spec-seed lane now carries sixth malformed/invalid assertions plus a fourth incompatible-import-type unlinkable assertion from the committed corpus.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the text/spec lanes as six-wide per stage family instead of five-wide.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader binary tag-name and datacount coverage

- Reviewed the already-modified binary invalid sources `src/fuzz/invalid_binary.mbt` and `src/fuzz/invalid_binary_wbtest.mbt` to capture another binary widening step: the curated lane now includes encoded invalid tag-name modules plus datacount mismatch modules in both too-small and too-large directions.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the binary name/datacount surface as broader than the earlier func/table/global/type/elem/data-name and datacount-without-data-only set.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader AST tag-name and datacount breadth

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture another AST widening step: the curated lane now includes out-of-range tag-name indices plus datacount mismatches in both too-small and too-large directions.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST datacount and tag-name surfaces as broader than the earlier memory.init/data.drop and non-tag-name-only set.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record another text/spec widening pass

- Reviewed the already-modified text invalid sources `src/fuzz/invalid_text.mbt` and `src/fuzz/invalid_text_wbtest.mbt` to capture another fixture-breadth step: the inline text lane now includes malformed `i64.const`, start-with-result, and typed incompatible-function-import cases, and the spec-seed lane now carries fifth malformed/invalid assertions plus a third incompatible-import-type unlinkable assertion from the committed corpus.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the text/spec lanes as five-wide per stage family instead of four-wide.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader binary export invalid coverage

- Reviewed the already-modified binary invalid sources `src/fuzz/invalid_binary.mbt` and `src/fuzz/invalid_binary_wbtest.mbt` to capture another binary widening step: the curated lane now includes encoded invalid export table/global/tag modules in addition to duplicate-export-name and the earlier decode/name/code families.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the binary export-invalid surface as broader than the earlier duplicate-export-name-only coverage.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader AST export invalid coverage

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture another AST widening step: the curated lane now includes out-of-range export table, global, and tag indices in addition to the earlier duplicate-export-name and invalid exported function index cases.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST export surface as broader than the earlier func-only out-of-range case.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record deeper inline/spec text fixture breadth

- Reviewed the already-modified text invalid sources `src/fuzz/invalid_text.mbt` and `src/fuzz/invalid_text_wbtest.mbt` to capture another fixture-breadth step: the inline text lane now includes extra malformed `f64.const`, duplicate-export-name, and unknown-table-import cases, and the spec-seed lane now carries fourth malformed/invalid assertions plus a second incompatible-import-type unlinkable assertion from the committed corpus.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the text/spec lanes as four-wide per stage family instead of three-wide.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record broader binary decode and name coverage

- Reviewed the already-modified binary invalid sources `src/fuzz/invalid_binary.mbt` and `src/fuzz/invalid_binary_wbtest.mbt` to capture another binary widening step: the curated lane now includes reserved-section-id and malformed custom-section-name UTF-8 decode failures plus encoded invalid type-name, elem-name, and data-name modules.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the binary surface as broader than the earlier header/order/duplication/core plus function/table/global-name set.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record deeper AST name-section breadth

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture another AST breadth step: the curated invalid lane now covers out-of-range type-name, elem-name, and data-name indices in addition to the earlier function/memory/table/global name-section cases.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST name-section surface as broader than the earlier function/memory/table/global-only set.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record another AST-binary-text invalid widening pass

- Reviewed the already-modified validator and fuzz invalid sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, `src/fuzz/invalid_binary_wbtest.mbt`, `src/fuzz/invalid_text.mbt`, and `src/fuzz/invalid_text_wbtest.mbt` to capture the next release-hardening step: the AST lane now adds more name/code breadth, the binary lane now adds both header-corruption cases and more encoded invalid-module families, and the text/spec lanes now add another malformed/invalid/unlinkable round instead of stopping at two examples per stage family.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now record the newer `codesec-without-funcsec`, table/global-name, bad-header, start-param, and third spec-seed fixture coverage.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record deeper AST invalid breadth inside covered families

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture the next breadth step after family completion: the AST invalid lane now has secondary strategies inside already-covered export, start, function-body, and name families instead of only one representative strategy in each.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now distinguish family completeness from intra-family breadth and record the newer out-of-range export/start/name plus `data.drop`-based missing-datacount coverage.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record wider text and spec-seed fixture coverage

- Reviewed the already-modified text invalid sources `src/fuzz/invalid_text.mbt` and `src/fuzz/invalid_text_wbtest.mbt` to capture the next fixture-breadth step: the inline text lane now has multiple malformed, invalid, and unlinkable fixtures instead of just one of each, and the spec-seed lane now carries second malformed/invalid/unlinkable assertions from the committed spec corpus.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the deeper malformed/invalid/unlinkable text fixture set rather than treating the text/spec lanes as single-example smoke probes.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record wider binary invalid strategy coverage

- Reviewed the already-modified binary invalid sources `src/fuzz/invalid_binary.mbt` and `src/fuzz/invalid_binary_wbtest.mbt` to capture the next release-hardening step: the binary invalid lane now includes validator-rejected encoded modules for start, export, code, datacount, and name failures in addition to the earlier decode-stage corruptions and function-section type-index corruption.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the binary lane as a mixed strategy set with both raw byte corruptions and encoded invalid-module fixtures, and they narrow the remaining binary gap to additional corruption families and more validator-rejected section families.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record full AST validator-family coverage

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture the latest widening step: the AST invalid lane now also reaches element-section, memory-section, tag-section, type-section, and datacount-section failures, and the checked-in registry test now locks that every current `ValidationIssueFamily` has at least one deterministic AST invalid strategy.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe AST invalid coverage as family-complete and shift the remaining work toward adding more breadth within covered families plus widening the binary invalid lane.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record import/table AST invalid strategies

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture the next widening step after the earlier function/code/global/data additions: the AST invalid lane now also reaches import-section and table-section failures through `invalid-imported-func-type-index` and `mutable-global-get-in-table-init`.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now treat import and table coverage as landed and narrow the remaining AST-invalid family gap to element/tag/type corners.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record wider AST invalid-family coverage

- Reviewed the already-modified validator-fuzz sources `src/validate/invalid_fuzzer.mbt`, `src/validate/gen_invalid.mbt`, and `src/validate/gen_invalid_tests.mbt` to capture the next release-hardening step: the AST invalid lane now reaches additional section families beyond export/start/body/name, specifically function-section, code-section, global-section, and data-section failures.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the widened AST strategy set and explicitly call out the remaining table/element/import/tag/type corners as the next widening target instead of treating AST invalid coverage as effectively complete.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record `--emit-invalid-repro` on the fuzz CLI

- Reviewed the already-modified CLI and repro files `src/fuzz/imports.mbt`, `src/fuzz/main.mbt`, and `src/fuzz/main_wbtest.mbt` to capture the next fuzz-ops improvement: the main fuzz CLI now exposes the shared invalid repro builder directly through `--emit-invalid-repro` instead of requiring callers to stitch together report building and persistence manually.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the new direct CLI path alongside the library-level repro builders and dispatcher.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record suite-dispatched invalid repro builder

- Reviewed the already-modified repro files `src/fuzz/invalid_repro.mbt` and `src/fuzz/invalid_repro_wbtest.mbt` to capture the latest public utility improvement: callers now have a generic `build_invalid_fuzz_failure_report_by_suite_and_stable_id(...)` entrypoint instead of needing to select one of four source-kind-specific builders themselves.
- Updated `docs/wiki/validate/fuzz-hardening.md` so the living fuzz-repro summary now states that the generic builder accepts both full suite ids and short source-kind aliases (`ast`, `binary`, `text`, `spec-seed`) before dispatching to the specific report builders.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record seed-profile metadata on invalid repro reports

- Reviewed the already-modified invalid-generation and repro files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, `src/fuzz/invalid_binary_wbtest.mbt`, `src/fuzz/invalid_repro.mbt`, and `src/fuzz/invalid_repro_wbtest.mbt` to capture the next hardening step: named AST/binary seed helpers now preserve semantic profile identity instead of collapsing purely to config equality, and persisted repro metadata now records `seed_profile`, `seed_mode`, and `seed_require_strategy_prereqs` for AST/binary cases.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the new profile-introspection helpers and the extra persisted metadata fields, which makes the invalid repro corpus more self-describing when a future reduction or replay result needs to be audited offline.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record shared strategy-prereq shapers for `gen_invalid`

- Reviewed the already-modified invalid-generation files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_binary_wbtest.mbt` to capture the next public-API refinement after the compact/repro seed work: validate now exports reusable strategy-prereq seed shapers, and binary invalid-generation params now carry an explicit `require_strategy_prereqs` toggle on every constructor.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the shared validate-side prerequisite shapers (`gen_invalid_require_memory_data_gen_valid_config(...)`, `gen_invalid_require_defined_func_gen_valid_config(...)`) and the new binary opt-out toggle instead of treating all seed widening as implicit package-internal behavior.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record named repro-seed helpers on `gen_invalid`

- Reviewed the already-modified invalid-generation and report-builder files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, `src/fuzz/invalid_binary_wbtest.mbt`, `src/fuzz/invalid_repro.mbt`, and `src/fuzz/invalid_repro_wbtest.mbt` to capture the latest public-API refinement: both the validate and fuzz package surfaces now expose a semantic `repro_seed(...)` constructor in addition to the lower-level `small_coverage_forced_seed(...)` helper.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST and binary `gen_invalid` surfaces in terms of six named seed families: natural, coverage-forced, small-natural, small-coverage-forced, repro, and minimal.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record compact default seeds for AST/binary report builders

- Reviewed the already-modified shared repro files `src/fuzz/invalid_repro.mbt` and `src/fuzz/invalid_repro_wbtest.mbt` to capture the next durable behavior refinement after adding the compact random seed helpers: the default AST and binary stable-id report builders now route through `small_coverage_forced_seed(...)` instead of the larger full coverage-forced constructors.
- Updated `docs/wiki/validate/fuzz-hardening.md` so the living validator-fuzz summary now states that persisted AST/binary seed artifacts are intentionally compact by default, while callers can still request the larger explicit `coverage_forced_seed(...)` path when they want it.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | correct Binaryen upstream-only pass chronology from official source files

- Rechecked Binaryen against stronger official primary sources than the older package-surface and release-summary wording this wiki had been leaning on: the official raw `CHANGELOG.md` plus the current `main` `src/passes/pass.cpp` registry on GitHub.
- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` to correct several stale chronology claims at once: `--minimize-rec-groups` is already called out by `version_117`; `version_119` adds `--remove-unused-types`, `--string-lowering`, and `TypeRefiningGUFA`; `version_125` adds `ReorderTypes` plus `--strip-target-features`; and `version_126` adds `--remove-relaxed-simd`. The older wiki wording that mentioned `string-lifting` or `--strip-toolchain-annotations`, or dated some of those additions later, has now been retired explicitly instead of being silently overwritten.
- Filed the current `main` pass-registry check back into the living late-pass docs as a stronger source-of-truth layer for present-day naming: the source-level registry still carries the wiki's late-pass names and the newer upstream-only names `minimize-rec-groups`, `remove-unused-types`, `reorder-types`, `remove-relaxed-simd`, `string-lowering`, `type-refining-gufa`, and `strip-target-features`.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the chronology repair.

## [2026-04-18] maintain | record compact random `gen_invalid` seed helpers

- Reviewed the already-modified invalid-generation files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_binary_wbtest.mbt` to capture the next durable API refinement after the named natural/coverage-forced/minimal helpers: both the validate and fuzz package surfaces now also expose `small_natural_seed(...)` and `small_coverage_forced_seed(...)` entrypoints for callers that still want random valid seeds but prefer smaller repro-oriented modules.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST and binary `gen_invalid` surfaces in terms of five named seed families: natural, coverage-forced, small-natural, small-coverage-forced, and minimal.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record named natural and coverage-forced `gen_invalid` seed helpers

- Reviewed the already-modified invalid-generation files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/invalid_binary.mbt`, `src/fuzz/invalid_binary_wbtest.mbt`, and the generated package signatures `src/validate/pkg.generated.mbti` plus `src/fuzz/pkg.generated.mbti` to capture one more durable public-API refinement: callers now have explicit `natural_seed(...)` and `coverage_forced_seed(...)` helpers in addition to the earlier `minimal_seed(...)` helper, instead of having to pass the matching `GenValidConfig` defaults manually.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now describe the AST and binary `gen_invalid` surfaces in terms of those three named seed families: natural, coverage-forced, and minimal.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record public invalid-fuzz report builders for all source kinds

- Reviewed the already-modified fuzz invalid-repro files `src/fuzz/invalid_repro.mbt`, `src/fuzz/invalid_repro_wbtest.mbt`, and `src/fuzz/pkg.generated.mbti` to capture the durable follow-up after the first `gen_invalid` API pass: the shared repro surface now exports report builders not only for AST and binary invalid cases but also for inline text and spec-seed cases, so downstream callers can construct checked `InvalidFuzzFailureReport` values from stable ids across all four source kinds.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now say explicitly that AST/binary reports preserve both the invalid artifact and the valid seed artifact, while text/spec-seed reports now also have first-class stable-id builders on the same shared repro surface.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record public `gen_invalid` seed/mutation surfaces

- Reviewed the already-modified validator fuzz files `src/validate/gen_invalid.mbt`, `src/validate/gen_invalid_tests.mbt`, `src/fuzz/gen_invalid_wbtest.mbt`, `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, `src/fuzz/invalid_binary_wbtest.mbt`, and the generated public interfaces `src/validate/pkg.generated.mbti` plus `src/fuzz/pkg.generated.mbti` to confirm one durable wiki-worthy change: the AST-invalid and binary-invalid helpers are now supported package surfaces, not only internal fuzz-runner implementation details.
- Updated `docs/wiki/validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living docs now say explicitly that downstream packages can call `gen_invalid_ast_seed_config`, `gen_invalid_ast_seed_module`, `gen_invalid_ast_generate`, `gen_invalid_binary_seed_config`, `gen_invalid_binary_seed_module`, and `gen_invalid_binary_generate` through exported params/generated structs, with checked-in coverage for stable-id lookup plus both random-valid-seed and minimal-valid-seed modes.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | turn late ordered-audit note IDs into direct wiki links

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` and `docs/wiki/binaryen/passes/remove-unused-brs/parity.md` so the living wiki now links the active ordered `-O4z` blocker notes (`0099`, `0100`) and same-day retirement notes (`0102` through `0107`) directly instead of mentioning those raw-research IDs only as plain text.
- This maintenance pass did not change the underlying blocker story; it tightened source-of-truth hygiene by making the exact raw evidence reachable from the living summaries without an extra manual search step.
- Reran a repo-local markdown-link and living-orphan health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the cross-link refresh.

## [2026-04-18] fix | retry successful-but-no-output `moon run` fuzz launcher churn

- Updated `docs/wiki/binaryen/passes/tuple-optimization/parity.md` and `docs/wiki/binaryen/passes/tuple-optimization/reduced-repros-and-evidence.md` so the historical `2124 / 10000` long-lane stop at `/tmp/pass-fuzz-tuple-gen-valid-10000-emptysummary-2026-04-10` is now documented as an old `pass-fuzz-compare` launcher artifact rather than a standing workflow requirement to prefer `--starshine-bin` for every very long lane.
- The living tuple docs now record the narrower outcome: the harness retries a successful default `moon run` invocation that fails to materialize `starshine.raw.wasm`, so the earlier failure remains useful evidence about launcher churn without implying a current tuple-opt semantic mismatch or a permanent native-binary-only workaround.
- Updated `agent-todo.md` to remove the retired long-lane launcher-workaround bullet from the tuple backlog.

## [2026-04-18] maintain | record docs.rs Binaryen pass-coverage contradiction

- Ran another Binaryen primary-source/source-of-truth sweep against the official GitHub release horizon, the official GitHub `main` changelog, the Chromium mirror refs/changelog trail, and the current docs.rs `wasm_opt` pages before editing the wiki.
- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/heap-store-optimization/index.md`, and `docs/wiki/index.md` so the living docs now record one sharper package-surface caution explicitly: as of `2026-04-18`, the docs.rs crate overview says `Pass` represents or exposes all Binaryen optimization passes, but the linked enum page still omits `HeapStoreOptimization`, `MinimizeRecGroups`, and `StringLowering`. The wiki now treats docs.rs as self-contradictory completeness evidence and keeps official GitHub release pages plus the Debian manpage as the stronger terminology/release-horizon sources.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] fix | retire `[O4Z]005` generated slot-33 `vacuum` corruption

- Added `docs/wiki/raw/research/0107-2026-04-18-generated-o4z-vacuum-slot33-retired-by-validator-escape-fix.md` to capture the shifted symptom relative to `0098`: the saved slot-33 predecessor no longer died in Starshine's final validator, but it still emitted invalid typed-`if` output that `wasm-tools` rejected until the validator/typechecker escape fix and guarded `vacuum` writeback landed.
- Updated `docs/wiki/binaryen/passes/vacuum/index.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki no longer treats slot `33` as an active `vacuum` blocker; the ordered generated-artifact blocker set is now down to two slots overall (`remove-unused-brs` `40` and `optimize-instructions` `44`), and the durable explanation for the retired `vacuum` slot is the validator/writeback boundary rather than a new pass-local cleanup rule.
- Updated `agent-todo.md` so `[O4Z]005` is retired by `0107` and the remaining active generated `-O4z` blockers now start at `[O4Z]006`.

## [2026-04-18] maintain | repair stale precompute blocker wording in pass folder map

- Ran another Binaryen primary-source spot check against the official GitHub release surface plus the current `main` changelog before editing the wiki; this maintenance pass still did not find a documented `version_130` release page or a newer changelog-advertised optimization-pass addition beyond the already tracked horizon through `version_129`.
- Updated `docs/wiki/binaryen/passes/index.md` so the precompute catalog entry now matches the living `precompute` pass page and late-pipeline summary: slot `19` `func 108` is retired by `0105`, so the folder map no longer incorrectly describes it as an active blocker.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md`; there were `0` broken relative links after the wording repair.

## [2026-04-18] maintain | correct late-pass blocker-count wording after slot-19 and slot-23 retirements

- Rechecked the current Binaryen public-source hierarchy against the official GitHub `main` changelog and tagged release pages through `version_129`, plus the Chromium mirror commits already cited by the late-pass pages; this maintenance pass did not find a newer documented optimization-pass addition or rename affecting Starshine's current folder map.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so their summaries no longer say the late `-O4z` audit still has `five` remaining blockers; after the `0105` and `0106` retirements, the living late-pass summary correctly narrows the open generated-artifact blocker set to `three`: `vacuum` slot `33`, `remove-unused-brs` slot `40`, and `optimize-instructions` slot `44`.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md`; there were still `0` broken relative links after the wording correction.

## [2026-04-18] fix | retire `[O4Z]004` generated slot-23 `vacuum` corruption

- Added `docs/wiki/raw/research/0106-2026-04-18-generated-o4z-vacuum-slot23-retired-by-carrier-wrapper-guard.md` to record that the saved slot-23 predecessor from `0097` now replays cleanly on the current tree, the extracted `Func 652` replay also validates, and the Binaryen compare reports normalized-WAT plus canonical-function equality.
- Updated `docs/wiki/binaryen/passes/vacuum/index.md`, `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki no longer treats slot `23` as an active `vacuum` blocker; the remaining live `vacuum` blocker is slot `33`, and the durable inference is that slot `23` was fallout from the earlier HOT-lower carrier-wrapper fix in `0103`, not a pass-local `vacuum` mutation bug.
- Added cmd-level native regressions in `src/cmd/cmd_wbtest.mbt` for both the full saved slot-23 predecessor and the extracted `Func 652` replay, and updated `agent-todo.md` so the generated `-O4z` active blocker list now starts at `[O4Z]005`.

## [2026-04-18] fix | retire `[O4Z]003` generated slot-19 `precompute` corruption

- Added `docs/wiki/raw/research/0105-2026-04-18-generated-o4z-precompute-slot19-retired-by-writeback-guards.md` to record that the saved slot-19 predecessor from `0096` now replays cleanly on the current tree, stays `wasm-tools validate` clean, and matches Binaryen at normalized-WAT / canonical-function granularity.
- Updated `docs/wiki/binaryen/passes/precompute/index.md` and `docs/wiki/index.md` so the living wiki no longer describes slot `19` `func 108` raw-result loss as an active blocker; the durable frontier is back to parity/runtime work plus future lower-shape follow-ups, not this retired invalid-output witness.
- Added cmd-level native regressions in `src/cmd/cmd_wbtest.mbt` for both the full saved slot-19 predecessor and the extracted `Func 108` replay, and updated `agent-todo.md` so the generated `-O4z` active blocker list now starts at `[O4Z]004`.

## [2026-04-18] maintain | promote official GitHub release pages in Binaryen source-of-truth guidance

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/global-refining/index.md`, `docs/wiki/binaryen/passes/memory-packing/index.md`, `docs/wiki/binaryen/passes/once-reduction/index.md`, `docs/wiki/binaryen/passes/dead-code-elimination/index.md`, `docs/wiki/binaryen/passes/heap2local/index.md`, `docs/wiki/binaryen/passes/remove-unused-brs/index.md`, and `docs/wiki/index.md` after a fresh Binaryen source sweep.
- Tightened the public-source hierarchy again instead of changing the substantive pass claims: official GitHub release pages for `version_127` (`2026-03-10`), `version_128` (`2026-03-13`), and `version_129` (`2026-04-01`) are now recorded as the primary public release-horizon baseline, while the Chromium refs/release-note mirror remains corroborating evidence and the GitHub `main` changelog remains only a narrow post-tag drift watch.
- Replaced stale living-page wording that still said `non-GitHub` terminology checks on the stub landing pages and pass catalog, so the wiki now consistently reflects the maintenance lane's current rule: GitHub is allowed as a documentation source of truth for Binaryen pass names, release notes, and trunk drift, even though workflow operations still stay out of scope.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the refresh.

## [2026-04-18] maintain | switch Binaryen source-of-truth wording from non-GitHub-only to official-source hierarchy

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/heap-store-optimization/index.md`, and `docs/wiki/index.md` after a fresh Binaryen source sweep to remove stale wording that implied this maintenance lane had to avoid GitHub as a documentation source.
- Tightened the source-of-truth guidance instead of broadening claims: keep Chromium refs plus tagged release-note pages as the release-horizon baseline through `version_129`, keep Debian/docs.rs/README as incomplete package-surface corroboration, and use the official GitHub `main` changelog as the stronger current-trunk drift watch. The living wiki now records that Chromium and GitHub currently agree on the same `Current Trunk` lead item (`MemorySegment` -> `DataSegment` API renames) and still do not document a newer optimization-pass addition relevant to Starshine's pass catalog.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] maintain | tighten docs.rs pass-enum source-of-truth wording

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, `docs/wiki/binaryen/passes/heap-store-optimization/index.md`, and `docs/wiki/index.md` after a fresh non-GitHub Binaryen source sweep to record one useful docs.rs nuance explicitly: the published `wasm_opt::Pass` page says its exposed enum variants follow the command-line pass names with Rust capitalization conventions.
- Kept the conservative conclusion unchanged: that positive naming rule helps for entries the page does expose such as `RemoveUnusedBrs`, but the same page still omits `HeapStoreOptimization`, `MinimizeRecGroups`, and `StringLowering`, so those absences remain wrapper-surface lag evidence rather than proof of upstream renames or removals.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md`; there were still `0` broken relative links after the wording refresh.

## [2026-04-18] maintain | narrow the late-pass blocker roster after slot-14 and slot-16 retirements

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/precompute/index.md`, `docs/wiki/binaryen/passes/vacuum/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki no longer describes the 2026-04-18 generated `cmd.wasm` ordered `-O4z` fallout as only a broad failing-pass cluster.
- The refreshed living summary now names the exact five remaining blockers explicitly: `remove-unused-brs` slot `40`, `precompute` slot `19`, `vacuum` slots `23` and `33`, and `optimize-instructions` slot `44`, while also calling out that slot `14` (`RUB`) and slot `16` (`optimize-instructions`) are already retired by `0102`, `0103`, and `0104`.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | record README typo in Binaryen pass-name evidence ladder

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after another non-GitHub Binaryen source sweep because the bundled Binaryen README overview mirrored through `wasm-opt-sys` currently misspells `RemoveUnusedBrs` as `RemoveUnsedBrs`.
- Tightened the source-of-truth guidance accordingly: keep using the Debian `wasm-opt` manpage and Chromium-hosted release-note trail as stronger public spelling evidence, and treat the README overview as broad context rather than a canonical pass-name oracle.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] maintain | correct docs.rs heap-store terminology caveat

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/heap-store-optimization/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after a fresh non-GitHub source check because the published `wasm_opt::Pass` enum page does **not** currently expose `HeapStoreOptimization`, even though the Debian experimental `wasm-opt` `122` manpage still lists `--heap-store-optimization`.
- Tightened the source-of-truth guidance accordingly: for this pass name, treat the Debian CLI manpage as the stronger public terminology source and treat docs.rs as negative evidence of Rust-wrapper surface lag, not as evidence that upstream Binaryen renamed or dropped the pass.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the correction.

## [2026-04-18] fix | retire `[O4Z]002` slot-16 `optimize-instructions` after the `Func 1818` follow-up

- Added `docs/wiki/raw/research/0104-2026-04-18-generated-o4z-optimize-instructions-slot16-func1818-parent-exit-payload-guard.md` to capture the remaining slot-16 `Func 1818` root cause: `hot_lower_impl_try_pack_split_parent_exit_payload(...)` was inserting a new typed carrier around a split parent-exit payload even when nested child branches still targeted the wrapper label one level out.
- Updated `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `docs/wiki/binaryen/passes/vacuum/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki now says the complete slot-16 story accurately: `Func 652` was fixed by `0103`, `Func 1818` was fixed by `0104`, the only remaining `optimize-instructions` ordered blocker is the later slot-44 replay, and the `vacuum` landing page no longer treats the retired slot-16 failures as evidence of a still-shared live blocker family.
- Updated `agent-todo.md` so `[O4Z]002` is now checked off and the active generated-artifact corruption list starts at `[O4Z]003`.

## [2026-04-18] maintain | refresh current-trunk Binaryen changelog lead note

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after a fresh non-GitHub Binaryen source sweep because the Chromium-mirror `main` `CHANGELOG.md` no longer leads with the earlier worker-threading note; the current `Current Trunk` lead item is now the `MemorySegment` -> `DataSegment` API rename work.
- Kept the wiki's conservative conclusion unchanged: this is still only a freshness check, and the reachable `main` changelog still does not document a newer optimization-pass addition beyond the already tracked release-note horizon through `version_129`.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] maintain | anchor Binaryen release horizon with Chromium refs plus main changelog

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after another non-GitHub Binaryen source sweep so the living wiki now cites the Chromium refs listing directly as the anchor for the current reachable release horizon: `refs/heads/main` is present, and `version_129` is still the newest directly reachable release tag in that mirror listing.
- Recorded the matching current-trunk caution explicitly: the Chromium-mirror `main` `CHANGELOG.md` currently leads with a C API worker-threading note rather than a newer optimization-pass addition, which is useful as a freshness check but still not strong enough to replace the tagged release-note pages as the source-of-truth boundary for pass-addition claims.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] maintain | refine Binaryen package-surface lag notes

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after another non-GitHub Binaryen source sweep so the living wiki now says explicitly that the Debian `wasm-opt` manpage and the published `wasm_opt::Pass` enum are both incomplete in different directions: Debian already exposes some upstream-only passes such as `--minimize-rec-groups` / `--string-lowering`, while docs.rs still exposes older names like `RemoveUnusedTypes` without surfacing those newer additions.
- Tightened the source-of-truth guidance around late-pass upkeep: use the Debian manpage and docs.rs enum as terminology lower bounds, but keep the Chromium-hosted release-note trail through `version_129` as the safer public baseline when reconciling newer upstream-only pass additions or absences.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the wording refresh.

## [2026-04-18] maintain | extend non-GitHub Binaryen release-note coverage through version_129

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after another Chromium-hosted Binaryen check so the living wiki now says explicitly that the directly reachable non-GitHub release-note horizon extends through `version_129`, not just the earlier `version_126` lower bound.
- Recorded the precise conservative takeaway from those newer public changelog sections: the already tracked upstream-only pass additions are still the latest ones visible in reachable release notes (`version_119`, `version_124`, `version_125`, `version_126`), while `version_127` / `version_128` / `version_129` do not surface another optimization-pass addition relevant to Starshine's implemented folder map.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the catalog refresh.

## [2026-04-18] fix | retire the extracted slot-16 `Func 652` carrier-wrapper failure and surface the remaining blocker

- Added `docs/wiki/raw/research/0103-2026-04-18-generated-o4z-optimize-instructions-slot16-func652-carrier-guard.md` to capture the resolved part of `[O4Z]002`: the extracted slot-16 `Func 652` replay was failing because `hot_lower_impl_stackify_wrapped_struct_set_prefixes(...)` inserted a new `block (result i32)` in front of a wrapped `local.set` carrier even though child branches still targeted the parent exit label.
- Updated `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki now records the retired `Func 652` blocker explicitly and no longer describes slot 16 as only the old paired `Func 652` / `Func 1818` suspicion. The current landing-page truth is that the extracted `Func 652` witness is fixed, while the full slot-16 replay now advances to the still-open shared `Func 1818` family.
- Updated `agent-todo.md` so `[O4Z]002` stays active but now points at the narrowed state accurately: the old `Func 652` sub-bug is retired by `0103`, and the remaining direct replay failure for slot 16 is the newly surfaced `Func 1818` underflow.

## [2026-04-18] maintain | widen Binaryen upstream-only pass boundary notes again

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after a fresh non-GitHub Binaryen release-note pass so the living wiki now records one more upstream-only addition that was missing from the earlier lower-bound catalog: `ReorderTypes` is already called out by Chromium-hosted `version_125`, between the previously tracked `version_124` and `version_126` additions.
- Kept the wording conservative and explicit about evidence quality: the reachable non-GitHub sources still provide a lower-bound public catalog rather than a full historical Binaryen pass audit, and this update only records the newly confirmed `version_125` gap instead of implying that the full upstream pass history has now been exhaustively reconciled.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the catalog refresh.

## [2026-04-18] maintain | ingest ordered-audit notes for DCE and heap2local

- Updated `docs/wiki/binaryen/passes/dead-code-elimination/index.md` and `docs/wiki/binaryen/passes/heap2local/index.md` so the living pass stubs now absorb one durable takeaway from the 2026-04-18 generated `cmd.wasm` ordered `-O4z` audit: both passes are currently in the expensive-but-successful cluster rather than the hard-corruption cluster.
- Refreshed `docs/wiki/binaryen/passes/index.md` and `docs/wiki/index.md` so the human catalogs no longer make those two landing pages look like terminology-only stubs; they now also call out the ordered-audit status and, for `heap2local`, the current non-GitHub `Heap2Local` naming sanity check.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the ingest.

## [2026-04-18] maintain | widen Binaryen upstream-only pass boundary notes

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after another non-GitHub Binaryen release-note pass so the living wiki now records that upstream-only pass additions visible in reachable primary sources start earlier than the previously tracked `version_126` pair: `--minimize-rec-groups` is already present by `version_119`, `--string-lifting` and `TypeRefiningGUFA` are present by `version_124`, and `--remove-relaxed-simd` / `--strip-toolchain-annotations` remain the newer `version_126` additions.
- Kept the wording conservative and explicit about uncertainty: the repo still tracks Starshine's implemented pass subset, while this non-GitHub source sweep should be treated as a lower-bound public catalog rather than a full Binaryen pass-history audit.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the catalog refresh.

## [2026-04-18] maintain | record newer Precompute multibyte-array no-fold drift

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md`, `docs/wiki/binaryen/passes/precompute/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` after a fresh non-GitHub upstream check so the living wiki now records the 2026-03-26 Chromium-mirror `Precompute` follow-up for multibyte array access: current trunk intentionally keeps `array.load` in `NONCONSTANT_FLOW` instead of folding it like an ordinary constant read.
- Kept the earlier child-retention, GC-write, and GC-atomic drift notes intact and explicit, so the newer multibyte-array behavior is filed as an additional upstream drift item rather than silently replacing the older chronology.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the refresh.

## [2026-04-18] maintain | tighten Binaryen upstream-catalog boundary and precompute drift notes

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` after a fresh non-GitHub upstream check so the terminology section now records two additional source-of-truth facts: Binaryen `version_126` added the newer upstream-only passes `--remove-relaxed-simd` and `--strip-toolchain-annotations`, and `Precompute` had an extra 2026-03-23 GC-write/effects-model fix before the already-tracked 2026-03-25 GC-atomic no-fold change.
- Updated `docs/wiki/binaryen/passes/precompute/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki now distinguishes Starshine's implemented Binaryen pass subset from newer upstream-only additions instead of letting the older Debian `122` manpage lag read like an implicit deprecation signal.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still `0` broken relative links and `0` living orphan pages after the catalog refresh.


## [2026-04-18] maintain | refresh RUB wiki metadata and slot-14 ingest links

- Updated `docs/wiki/binaryen/passes/remove-unused-brs/index.md` so its frontmatter `last_reviewed` stamp and source list now match the already-ingested 2026-04-18 ordered-audit and slot-14 guard evidence, instead of implying the landing page stopped at the 2026-04-13 perf-only state.
- Updated `docs/wiki/binaryen/passes/remove-unused-brs/branch-exit-and-payload-rewrites.md` so the one-armed `if br` section now has matching 2026-04-18 review metadata plus direct links to the slot-14 guard note and the external-validation cmd wbtest coverage.
- Refreshed `docs/wiki/binaryen/passes/index.md` and `docs/wiki/index.md` so both human catalogs mention the retired slot-14 large-condition guard on the RUB landing page.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were still no broken relative links and no living orphan pages after the metadata refresh.

## [2026-04-18] research | retire `[O4Z]001` slot-14 `remove-unused-brs` corruption

- Added `docs/wiki/raw/research/0102-2026-04-18-generated-o4z-rub-slot14-if-br-large-condition-guard.md` to capture the resolved slot-14 path: the invalid raw output came from `remove_unused_brs_try_rewrite_if_br(...)` rewriting a large non-reorder-safe plain-`br` condition, while oracle Binaryen kept a valid block-plus-branch shape on the extracted `Func 1354` replay.
- Updated `docs/wiki/binaryen/passes/remove-unused-brs/branch-exit-and-payload-rewrites.md`, `docs/wiki/binaryen/passes/remove-unused-brs/parity.md`, and `docs/wiki/index.md` so the living wiki now records the landed large-condition guard, the retired slot-14 blocker, and the fact that slot `40` is now the remaining ordered RUB corruption.
- The checked-in cmd regression in `src/cmd/cmd_wbtest.mbt` now validates the extracted slot-14 replay with external `wasm-tools validate`, so the wiki points at a durable external-validity lock instead of the older in-process-only green surface.

## [2026-04-18] maintain | record newer Precompute GC heap-effect drift

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` and `docs/wiki/binaryen/passes/precompute/index.md` after a fresh non-GitHub upstream check to record the newer Chromium-mirror `2026-03-25` `Precompute` change that stops folding GC `struct` / `array` atomic RMW and `cmpxchg` ops because they read and write heap state, alongside the already-tracked `2025-08-27` child-retention rewrite.
- Refreshed `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so both human catalogs mention the broader `precompute` drift note instead of only the older child-retention change.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md`; there were still no broken relative links after the refresh.

## [2026-04-18] maintain | tighten Binaryen terminology evidence for late-pass stubs

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` so the 2026-04-18 non-GitHub terminology check now explicitly records direct Debian manpage confirmation for `--heap-store-optimization` and `--remove-unused-brs`, instead of implying that only the Rust bindings carried those names.
- Updated `docs/wiki/binaryen/passes/heap-store-optimization/index.md` so the landing page cites both the Debian experimental `wasm-opt` manpage and the current `wasm_opt::Pass` docs for the still-current `HeapStoreOptimization` name.
- Refreshed `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the human catalogs describe the stronger direct-source terminology evidence.
- Reran a repo-local markdown-link and orphan-page health check over `docs/wiki/**/*.md`; there were no broken relative links and no living orphan pages after the refresh.

## [2026-04-18] maintain | record newer Binaryen trunk drift without renaming pass pages

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` so the current non-GitHub Binaryen check now distinguishes stable pass names from newer trunk behavior drift, using Chromium-hosted mirror commits for the 2025-08-27 `Precompute` child-retention rewrite plus the 2026-02-27 `RemoveUnusedBrs` branches-to-traps and `Vacuum` unreachable-preservation changes.
- Updated the living pass pages for `precompute`, `vacuum`, and `remove-unused-brs` so they now say explicitly that the repo's older `version_129` Binaryen oracle is a tagged source baseline, not a claim that current Binaryen trunk internals are unchanged.
- Refreshed `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the human catalogs mention the new drift notes instead of implying the older summaries are the latest upstream behavior.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md` and confirmed there are still no broken relative links after the drift-note ingest.

## [2026-04-18] maintain | refresh late-pass stub terminology checks

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` so the current non-GitHub Binaryen terminology check now explicitly includes `HeapStoreOptimization` alongside the already-tracked `GlobalRefining`, `MemoryPacking`, `OnceReduction`, `OptimizeInstructions`, `Precompute`, `PrecomputePropagate`, and `Vacuum` surfaces.
- Refreshed the stub landing pages for `global-refining`, `memory-packing`, `once-reduction`, and `heap-store-optimization` so each page now records the current non-GitHub naming evidence and says explicitly that this maintenance run found no rename or deprecation signal in the available primary-ish sources.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so both catalogs describe the refreshed terminology-check coverage instead of the older generic stub wording.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md` and confirmed there are still no broken relative links after the refresh.

## [2026-04-18] research | narrow `[O4Z]001` to a native/source divergence blocker

- Added `docs/wiki/raw/research/0101-2026-04-18-generated-o4z-rub-slot14-native-source-divergence.md` to capture the current slot-14 blocker split: rebuilt native `cmd.exe` still emits invalid raw output on the saved predecessor from `0094`, while new in-process `run_cmd` / `run_cmd_with_adapter` wbtests on the same bytes stay green.
- Updated `docs/wiki/binaryen/passes/remove-unused-brs/parity.md`, `docs/wiki/index.md`, and `agent-todo.md` so the living docs now record the narrower blocker explicitly instead of treating slot 14 as an undifferentiated RUB corruption.
- Added focused cmd wbtests in `src/cmd/cmd_wbtest.mbt` for the saved slot-13 predecessor fixture so the repo now keeps the source-path behavior and the still-failing built-native behavior separate.

## [2026-04-18] maintain | record non-GitHub Binaryen source lag and paired late-pass blockers

- Updated `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` so the living wiki now says explicitly that the non-GitHub Binaryen discovery surfaces available to this maintenance run lag the repo's older GitHub-sourced `version_129` source oracle, and that the older Debian/docs.rs package versions should be treated as source-availability lag rather than as evidence of upstream pass-name removal or reversion.
- Updated `docs/wiki/binaryen/passes/optimize-instructions/index.md` and `docs/wiki/binaryen/passes/vacuum/index.md` so those landing pages now carry the durable ordered-audit conclusion that the current `Func 652` and `Func 1818` failures are paired `optimize-instructions` / `vacuum` ordered-prefix blockers, not yet isolated pass-only regressions.
- Reran a repo-local markdown-link health check over `docs/wiki/**/*.md` and confirmed there are still no broken relative links after the wording refresh.

## [2026-04-18] maintain | reconcile Binaryen pass terminology and repair wiki links

- Ran a non-GitHub Binaryen terminology check against the Debian experimental `wasm-opt` `122` manpage plus the current `wasm_opt` Rust docs/README mirrors, and recorded in `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` that the current upstream-facing names still align with the wiki's late-pass folder map.
- Updated `docs/wiki/binaryen/passes/precompute/index.md`, `docs/wiki/binaryen/passes/dead-code-elimination/index.md`, `docs/wiki/binaryen/passes/index.md`, and `docs/wiki/index.md` so the living wiki now records the explicit `Dce` and `precompute-propagate` alias boundaries instead of leaving those names implicit.
- Repaired broken relative links in `docs/wiki/binaryen/passes/late-pipeline-dispatch.md` and the archived RUME follow-up notes `0090` / `0091`, then reran a repo-local markdown-link scan to confirm `docs/wiki/` has no remaining broken relative links.

## [2026-04-18] maintain | tighten late-pass landing pages and cross-links

- Reworded the stub landing pages for `global-refining`, `memory-packing`, `once-reduction`, `heap-store-optimization`, `dead-code-elimination`, `optimize-instructions`, `precompute`, and `vacuum`, and shortened the root wiki catalogs in `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md`, so they point at `late-pipeline-dispatch.md` instead of repeating the generic “being authored” boilerplate.
- Kept the cross-links self-referential by adding the late-pipeline dispatch note to each page's `related` list.

## [2026-04-18] research | capture generated `cmd.wasm` ordered `-O4z` corruption blockers

- Added `docs/wiki/raw/research/0093-2026-04-18-generated-o4z-pass-audit-summary.md` plus the seven per-slot corruption notes `0094` through `0100` to capture the ordered self-opt audit on `_build/wasm/debug/build/cmd/cmd.wasm`, the saved `.artifacts/self-opt-pass-audit-o4z-generated-2026-04-18/` replay root, and the exact direct reproduce commands for every hard corruption slot.
- Updated `agent-todo.md` so the new `[O4Z]001` through `[O4Z]007` blockers sit at the top of the active backlog with saved predecessor inputs, direct replay commands, and raw-doc references for each corruption.
- Updated `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `docs/wiki/binaryen/passes/precompute/index.md`, `docs/wiki/binaryen/passes/vacuum/index.md`, `docs/wiki/binaryen/passes/remove-unused-brs/parity.md`, and `docs/wiki/index.md` so the living wiki now points at the generated-artifact ordered-prefix corruption follow-up instead of treating those passes as stub-only or purely parity-drift work.

## [2026-04-16] maintain | align fuzz runner wrapper and truth surfaces

- Updated `scripts/lib/fuzz-task.ts` and `scripts/test/task-family-commands.ts` so `bun fuzz run` now forwards the same discovery commands plus `--emit-gen-valid-batch --count <n> --seed <uint64> --out-dir <dir>` that `src/fuzz/main.mbt` already exposes, instead of carrying a narrower wrapper-only contract.
- Updated `src/fuzz/main.mbt` so the top-level `validate-valid` suite now reuses `validate_valid_run_config(...)` for the shared profile ladder and only layers the text-companion checks on top of `run_validate_valid_fuzz(...)` instead of keeping another copy of the profile normalization logic.
- Updated `tooling/fuzz-runner.md`, `validate/fuzz-hardening.md`, `docs/wiki/index.md`, `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, and `agent-todo.md` so the validator-fuzz handoff now records `[FUZ]010` as complete and no longer describes wrapper/docs alignment as unfinished fuzz-stack work.

## [2026-04-16] fix | persist validator invalid repros and replay helpers

- Added `src/fuzz/invalid_repro.mbt` plus `src/fuzz/invalid_repro_wbtest.mbt` with one shared invalid-failure report surface, stable `fuzz-corpus/invalid/<suite>/<strategy>/seed-<seed>-attempt-<attempt>/` persistence, metadata parse/load roundtrips, bounded shrink helpers, and direct replay helpers across AST, binary, text, and spec-seed artifacts.
- Updated `src/validate/invalid_fuzzer.mbt`, `src/fuzz/invalid_binary.mbt`, and `src/fuzz/invalid_text.mbt` so each current invalid source kind now exposes a deterministic minimal replay/reduction surface instead of requiring the original random run to reconstruct a failing case.
- Updated `validate/fuzz-hardening.md`, `tooling/fuzz-runner.md`, `docs/wiki/index.md`, `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, and `agent-todo.md` so the validator-fuzz handoff now records `[FUZ]009` as complete and points the next unfinished slice at `[FUZ]010` wrapper/docs alignment.

## [2026-04-16] fix | restore the validator text and spec-seed invalid fuzz lanes

- Added `src/fuzz/invalid_text.mbt` plus `src/fuzz/invalid_text_wbtest.mbt` with two deterministic stage-aware fuzz lanes: inline text invalidation and selected `tests/spec` invalid/malformed/unlinkable seed replay.
- Updated `src/wast/spec_harness.mbt` to export shared static-assertion evaluation helpers so the spec harness and the new fuzz runners now reuse one interpretation of `assert_malformed`, `assert_invalid`, and `assert_unlinkable`.
- Updated `tooling/fuzz-runner.md`, `validate/fuzz-hardening.md`, `docs/wiki/index.md`, `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, and `agent-todo.md` so the validator-fuzz handoff now records `[FUZ]008` as complete, the active suite inventory includes `validate-invalid-text` and `validate-invalid-spec-seed`, and the next unfinished slice is `[FUZ]009`.

## [2026-04-16] fix | restore the validator binary invalid fuzz lane

- Added `src/fuzz/invalid_binary.mbt` plus `src/fuzz/invalid_binary_wbtest.mbt` with a checked-in byte-corruption registry, deterministic smoke/ci/stress profile resolution, and stage-aware per-strategy stats for `attempted` / `applicable` / `mutated` / `decode_rejected` / `validate_rejected` / `rejected_expected` / `accepted`.
- Promoted `validate-invalid-binary` to a live `src/fuzz` suite and updated the fuzz runner truth surfaces in `tooling/fuzz-runner.md` so only `validate-invalid-text` and `validate-invalid-spec-seed` remain reserved.
- Updated `validate/fuzz-hardening.md`, `docs/wiki/index.md`, `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, and `agent-todo.md` so the validator-fuzz handoff now records `[FUZ]007` as complete and points the next unfinished slice at `[FUZ]008`.

## [2026-04-16] fix | restore the validator AST invalid fuzz lane

- Added `src/validate/invalid_fuzzer.mbt` with a checked-in AST invalid strategy registry, deterministic smoke/ci/stress profile resolution, expected `ValidationIssueFamily` accounting, and per-strategy `attempted` / `applicable` / `mutated` / `rejected` / `rejected_expected` stats.
- Restored `validate-invalid-ast` as a live `src/fuzz` suite and updated the fuzz runner truth surfaces in `tooling/fuzz-runner.md` so only the still-missing binary/text/spec-seed lanes remain reserved.
- Updated `validate/fuzz-hardening.md`, `docs/wiki/index.md`, `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, and `agent-todo.md` so the validator-fuzz handoff now records `[FUZ]006` as complete and points the next unfinished slice at `[FUZ]007`.

## [2026-04-16] fix | close the gen-valid-exposed RUME no-op start-section family

- Updated `src/passes/remove_unused_module_elements.mbt` so `RUME` now matches the proved Binaryen no-op `start` rule for defined single-`nop` start targets: it skips `start`-rooted liveness for that exact family, drops `start_sec` during rewrite, and still preserves the nearby empty-body negative boundary.
- Added focused regressions in `src/passes/remove_unused_module_elements_test.mbt` for exported/elem-linked single-`nop` start targets, the same family with locals still present, the empty-body negative boundary, and the start-only single-`nop` case that should disappear entirely once `start` stops rooting it.
- Updated `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, `agent-todo.md`, `validate/fuzz-hardening.md`, `docs/wiki/index.md`, and `raw/research/0091-2026-04-16-gen-valid-rume-start-section-parity-followup.md` so the validator-fuzz handoff now records `[FUZ]003B` as complete, the exact kept rule is documented, and the next unfinished fuzz slice is `[FUZ]004`.

## [2026-04-16] research | capture the next gen-valid-exposed RUME start-section family

- Added `raw/research/0091-2026-04-16-gen-valid-rume-start-section-parity-followup.md` to capture the two saved `gen-valid` `remove-unused-module-elements` mismatches that remain after the imported-function fix, including the exact `.tmp/pass-fuzz-fuz003a-genvalid-smoke/failures/case-000002-gen-valid/` and `case-000020-gen-valid/` repro folders plus the current uncertainty about Binaryen's precise no-op `start`-section pruning rule.
- Updated `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md` and `agent-todo.md` so the next unfinished validator-fuzz slice is now the explicit `[FUZ]003B` start-section follow-up instead of jumping straight to broader body widening.
- Updated `validate/fuzz-hardening.md` and `docs/wiki/index.md` so the living validator-fuzz summary now points at the newly documented `RUME` no-op `start`-section family.

## [2026-04-16] fix | close the gen-valid-exposed RUME imported-function family

- Updated `src/passes/remove_unused_module_elements.mbt` so module-pass `RUME` now drops unused function imports, remaps surviving function indices through the real used-function bitset, and compacts dead simple function types after imported-function removal.
- Added a focused imported-function regression in `src/passes/remove_unused_module_elements_test.mbt` that keeps a live exported/start/elem-defined function while requiring the unused imported function and its dead type to disappear.
- Refreshed `docs/0089-2026-04-15-fuzz-stack-hardening-execution-plan.md`, `agent-todo.md`, `validate/fuzz-hardening.md`, and research note `raw/research/0090-2026-04-16-gen-valid-rume-imported-function-parity-followup.md` so the validator-fuzz handoff now records the landed imported-function closure and the new distinct no-op start-section pruning follow-up revealed by the rerun.

## [2026-04-16] research | capture gen-valid-exposed RUME imported-function parity case

- Added `raw/research/0090-2026-04-16-gen-valid-rume-imported-function-parity-followup.md` to capture the exact `gen-valid`-seeded `remove-unused-module-elements` mismatch exposed after `[FUZ]003`, including the reproducer command, the `5/5` mismatch summary, and the saved `.tmp/pass-fuzz-fuz003-genvalid-smoke/failures/case-000001-gen-valid/` artifact paths.
- Updated `validate/fuzz-hardening.md` so the living validator-fuzz summary now records the active downstream parity follow-up exposed by widened `coverage-forced` generation instead of treating the topology widening as an isolated generator-only concern.
- Updated `docs/wiki/index.md` so the validator fuzz entry now calls out the active `gen-valid` / `RUME` imported-function follow-up.

## [2026-04-16] maintain | record fuzz runner suite inventory reconciliation

- Updated `tooling/fuzz-runner.md` so the living wiki now records the current active fuzz suites, the reserved future invalid-lane ids, and the `active` / `reserved` `--list-suites` output contract.
- Updated `validate/fuzz-hardening.md` so the validator-fuzz summary now reflects the current live valid-only surface, the reserved invalid suite ids, and the new hardening order instead of describing the removed old invalid lane as if it were still checked in.
- Updated `docs/wiki/index.md` so the catalog descriptions now mention the active-vs-reserved suite inventory boundary and the refreshed validator-fuzz hardening summary.

## [2026-04-15] maintain | record simplify-locals helper no-op raw skips

- Updated the living `simplify-locals` raw-lane and performance frontier pages so the new `branch-dense-structured-call-heavy-noop`, `block-rich-structured-call-heavy-noop`, `call-dense-structured-walker-noop`, and `low-local-decision-ladder-noop` contracts are part of the durable wiki instead of only the code and perf tests.
- Recorded that these families exist to retire unchanged helper-shaped hot work before lift, while the remaining open simplify-locals debt is still runtime budget and raw wasm/text equality versus Binaryen.

## [2026-04-13] maintain | record RUB call-heavy mixed-if mesh hot skip

- Added `raw/research/0087-2026-04-13-remove-unused-brs-call-heavy-mixed-if-mesh-hot-skip.md` to capture the newly retired lifted call-heavy mixed-if mesh family, the canonical extracted-function calibration, and the latest interleaved self-opt plus fuzz evidence.
- Updated the living `remove-unused-brs` hub, parity, HOT strategy, bailout notes, and pattern catalog so the new `call-heavy-mixed-if-mesh-noop` hot skip and its retired artifact funcs are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the later call-heavy mixed-if mesh retirement alongside the earlier medium-branchy lifted slice.

## [2026-04-13] maintain | record RUB medium branchy hot skip

- Added `raw/research/0086-2026-04-13-remove-unused-brs-medium-branchy-hot-skip.md` to capture the newly retired medium branchy lifted family, the extracted canonical-function calibration, and the latest self-opt plus fuzz evidence.
- Updated the living `remove-unused-brs` hub, parity, HOT strategy, bailout notes, and pattern catalog so the new `medium-branchy-block-ladder-noop` hot skip and its retired artifact funcs are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the later medium branchy lifted retirement alongside the earlier large `br_table`, tagged-prefix, and typed-encoder slices.

## [2026-04-10] maintain | calibrate RUB drop-heavy raw local-set floor

- Added `raw/research/0085-2026-04-10-remove-unused-brs-drop-heavy-local-set-floor.md` to capture the first reduced-only miss, the traced `Func 145` raw counts, the verified false HOT-only guards, and the landed `local_set >= 200` floor.
- Updated the living `remove-unused-brs` hub, parity, pattern-catalog, visit-order, and HOT-strategy pages so the new `large-drop-heavy-branch-ladder-noop` retirement, its artifact-calibrated floor, and the latest hotspot order are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the later drop-heavy `Func 145` raw retirement alongside the earlier `Func 828` and `Func 1482` raw slices.

## [2026-04-10] maintain | record RUB `br_table` one-arm payload parity guard

- Added `raw/research/0084-2026-04-10-remove-unused-brs-brtable-one-arm-payload-parity.md` to capture the reduced `Func 3771` parity failure, the Binaryen-side negative boundary for direct one-arm payload branches inside `br_table` functions, the rejected `hot_lower` detour, and the piggyback runtime recovery.
- Updated the living `remove-unused-brs` hub, parity, branch-exit/payload, visit-order, pattern-catalog, and HOT-strategy pages so the new direct one-arm payload `br_table` guard and the "piggyback broad guards onto an existing scan" lesson are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the new `Func 3771` parity boundary and the scan-reuse maintenance rule alongside the earlier `br_table` slices.

## [2026-04-10] maintain | record RUB large typed `br_table` encoder raw skip

- Added `raw/research/0083-2026-04-10-remove-unused-brs-large-typed-brtable-encoder-raw-skip.md` to capture the retired `Func 1482` family, the reduced perf lock, and the decoded-shell detector lesson from the first reduced-only draft that missed the real artifact body.
- Updated the living `remove-unused-brs` hub, parity, pattern-catalog, carried-guards/result-blocks, visit-order, and HOT-strategy pages so the new `large-typed-br-table-encoder-ladder-noop` raw skip, its `leading_any_block_chain_depth(...)` cheap prefilter, and the remaining `Func 1382` hotspot are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the typed `br_table` encoder raw skip and the current single-hotspot follow-up state.

## [2026-04-10] maintain | record RUB large tagged result-prefix hot skip

- Added `raw/research/0082-2026-04-10-remove-unused-brs-large-tagged-result-prefix-hot-skip.md` to capture the retired `Func 356` family, the reduced perf lock, the first detector-overhead draft, and the landed fastguard follow-up.
- Updated the living `remove-unused-brs` hub, parity, visit-order, pattern-catalog, carried-guards/result-blocks, and HOT-strategy pages so the new `large-tagged-result-prefix-ladder-noop` lifted skip and the detector-cost lesson are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the lifted tagged result-prefix slice and the new `Func 1382` / `Func 1482` hotspot order.

## [2026-04-10] maintain | record RUB large value-`if` / branch raw skip

- Added `raw/research/0081-2026-04-10-remove-unused-brs-large-value-if-branch-raw-skip.md` to capture the retired `Func 828` family, the reduced perf lock, and the updated fuzz plus artifact evidence.
- Updated the living `remove-unused-brs` hub, parity, visit-order, pattern-catalog, and HOT-strategy pages so the new `large-value-if-branch-ladder-noop` raw skip and the updated hotspot order are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the later raw value-`if` / branch skip alongside the earlier raw large-dispatch and lifted large-`br_table` slices.

## [2026-04-10] maintain | record RUB large `br_table` hot skip

- Added `raw/research/0080-2026-04-10-remove-unused-brs-large-brtable-hot-skip.md` to capture the new lifted no-op family, the reduced perf lock, and the updated artifact plus fuzz evidence.
- Updated the living `remove-unused-brs` hub, parity, visit-order, pattern-catalog, and HOT-strategy pages so the new `large-br-table-return-ladder-noop` skip and the retired `Func 1058` / `Func 1150` pair are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the later lifted `br_table` / return hot skip alongside the earlier raw large-dispatch and tee-floor slices.

## [2026-04-10] maintain | record RUB mid-band unique tee-floor correction

- Added `raw/research/0079-2026-04-10-remove-unused-brs-mid-unique-tee-floor.md` to capture the sixteen-tee unique-loop/select classifier change, the corrected `Func 1171` versus `Func 1150` artifact mapping, and the latest parity plus trace evidence.
- Updated the living `remove-unused-brs` hub, parity, visit-order, pattern-catalog, and HOT-strategy pages so the wider unique skip boundary and the corrected hotspot attribution are part of the durable wiki instead of a transient trace note.
- Updated `docs/wiki/index.md` so the RUB catalog entries describe the tee-floor correction as part of the current bailout and parity story.

## [2026-04-10] maintain | record RUB large result `br_table` raw skip

- Added `raw/research/0077-2026-04-10-remove-unused-brs-large-result-br-table-noop-skip.md` and `raw/research/0078-2026-04-10-remove-unused-brs-false-prefix-guard-raw-skip.md` to capture the large-dispatch no-op family, the false-prefix fix, and the kept perf evidence.
- Updated the living `remove-unused-brs` hub, parity, pattern-catalog, carried-guards/result-blocks, visit-order, and HOT-strategy pages so the large-dispatch no-op skip, the raw false-prefix guard, and the next artifact hotspot order are part of the durable wiki.
- Updated `docs/wiki/index.md` so the RUB catalog now describes the carried-wrapper plus large-dispatch artifact status instead of the stale early `Func 50` summary.

## [2026-04-09] bootstrap | initialize wasm knowledge base

- Added `docs/README.md` as the canonical docs and wiki schema.
- Added `docs/wiki/index.md` and `docs/wiki/log.md` as the initial catalog and audit trail.
- Added `docs/wiki/raw/README.md` to define committed raw-source handling.
- Updated `AGENTS.md` so wiki and knowledge-base work starts from `docs/README.md`.

## [2026-04-09] schema | tighten AGENTS wiki contract

- Rewrote `AGENTS.md` to split general work rules from docs and wiki rules.
- Made the numbered docs vs. living wiki distinction explicit.
- Added clear requirements for keeping `docs/wiki/index.md` and `docs/wiki/log.md` current on wiki schema and maintenance changes.
- Mirrored the same operational summary in `docs/README.md`.

## [2026-04-09] ingest | crystallize recent numbered docs into wiki pages

- Added `heap2local-binaryen-parity.md` from `0075` as the living summary of the current Binaryen transform surface, in-tree coverage, and remaining refinalization gap.
- Added `reorder-locals-binaryen-parity.md` from `0073` as the living summary of the exact ordering rule, module-pass scope, and current signoff boundary.
- Added `reorder-locals-multivalue-call-scope.md` from `0074` as the current scope decision for multivalue-call writeback parity.
- Added `binaryen-invalid-tag-index-parser-gap.md` from `0072` as the standing oracle parser-gap rule for `remove-unused-names` compare failures.
- Updated `docs/wiki/index.md` so the new decision and comparison pages are discoverable from the wiki catalog.

## [2026-04-09] organize | namespace Binaryen pass pages

- Moved the new pass-focused wiki pages under `docs/wiki/binaryen/passes/<pass>/...` so future Binaryen pass notes have one stable home.
- Kept `heap2local` parity under `binaryen/passes/heap2local/parity.md`.
- Kept `reorder-locals` parity and multivalue-call scope notes together under `binaryen/passes/reorder-locals/`.
- Moved the `remove-unused-names` parser-gap note under `binaryen/passes/remove-unused-names/invalid-tag-index-parser-gap.md`.
- Updated `docs/wiki/index.md` and intra-page links to point at the new namespace layout.

## [2026-04-09] ingest | crystallize four more recent pass docs

- Added `binaryen/passes/remove-unused-brs/parity.md` from `0070` as the living summary of Binaryen phase structure, current MoonBit coverage, and the remaining late-shape gap for `remove-unused-brs`.
- Added `binaryen/passes/remove-unused-brs/returned-ladder-hot-shapes.md` from `0071` as the standing HOT-shape note for returned-ladder families that do not look like their printed WAT form after lift.
- Added `binaryen/passes/pick-load-signs/parity.md` from `0069` as the living summary of the active rewrite surface, signoff state, and fast-skip behavior for `pick-load-signs`.
- Added `binaryen/passes/global-struct-inference/parity.md` from `0068` as the living summary of the current closed-world direct-global slice and its present scope limit.
- Updated `docs/wiki/index.md` so the new Binaryen pass pages are discoverable under concepts and comparisons.

## [2026-04-09] ingest | crystallize four more docs into Binaryen and IR2 wiki pages

- Added `binaryen/passes/duplicate-function-elimination/parity.md` from `0067` as the living summary of the module-wide merge contract, full `FuncIdx` rewrite surface, and the remaining direct artifact parity gap for `duplicate-function-elimination`.
- Added `binaryen/no-dwarf-default-optimize-path.md` from `0066` as the living summary of the real no-DWARF `-O` / `-Os` phase split, ordered pass path, and nested rerun rules for the MoonBit debug artifact.
- Added `ir2/execution-plan.md` from `0065` as the current IR2 handoff page covering the active registry surface, pipeline contract, and preferred next slice order.
- Added `ir2/test-matrix.md` from `0064` as the standing shared-helper and golden-fixture matrix for deterministic IR2 lift, analysis, lower, and pass-trace coverage.
- Updated `docs/wiki/index.md` so the new Binaryen and IR2 pages are discoverable under concepts and comparisons.

## [2026-04-09] ingest | crystallize four more IR2 handoff docs into wiki pages

- Added `ir2/registry-map.md` from `0063` as the living summary of the current registry categories, preset composition, and the now-partially-stale parts of the March batch map.
- Added `ir2/pass-porting-checklist.md` from `0062` as the standing helper and validation checklist for future IR2 pass ports.
- Added `ir2/local-ssa-policy.md` from `0061` as the current locals-only SSA policy page covering entry defs, overlay-only phis, rename policy, and predecessor-copy destruction.
- Added `ir2/cfg-contract.md` from `0060` as the normative CFG boundary and explicit-edge policy page for `HotFunc`.
- Updated `docs/wiki/index.md` so the new IR2 concept and decision pages are discoverable from the catalog.

## [2026-04-09] organize | reserve root docs for normative material

- Updated `AGENTS.md`, `docs/README.md`, and `docs/wiki/raw/README.md` so `docs/` is now the home for normative docs only, while numbered one-off investigations live under `docs/wiki/raw/research/`.
- Added `docs/wiki/raw/research/README.md` to define the absorbed-research archive rules.
- Moved the non-normative numbered docs out of root `docs/` into `docs/wiki/raw/research/`.
- Repointed `agent-todo.md`, `CHANGELOG.md`, and the Binaryen wiki pages so live references and archived sources still resolve after the move.
- Updated `docs/wiki/index.md` so the research archive rules are discoverable from the catalog.

## [2026-04-09] ingest | crystallize IR2 and validator tooling docs into wiki pages

- Added `ir2/architecture-rules.md` from `0059` as the living digest of the single-owned `HotFunc` contract, overlay model, and module-split rule.
- Added `tooling/fuzz-runner.md` from `0003` as the standing workflow page for keeping heavy randomized work in `src/fuzz` with reproducible suite, profile, and seed entrypoints.
- Added `tooling/tracing-playbook.md` from `0001` as the shared pass and validator trace contract for `key=value` logs, timing helpers, hotspot summaries, and indirect testing.
- Added `validate/trace-benchmark-baseline.md` from `0010` as the committed baseline page for validator trace corpora, phase totals, helper totals, and hotspot interpretation.
- Added `validate/fuzz-hardening.md` from `0058` as the current hardening plan for validator fuzz trust, generator breadth, diagnostic matching, and repro ergonomics.
- Updated `docs/wiki/index.md` so the new IR2, tooling, and validator pages are discoverable from the catalog.

## [2026-04-09] ingest | crystallize GC text, descriptor, and string docs into wiki pages

- Added `wast/gc-type-authoring.md` from `0018`, `0019`, `0020`, and `0026` as the standing higher-level WAST rule page for GC type defs, `rec` groups, descriptor metadata, and flat type indexing.
- Added `custom-descriptors/static-fixtures.md` from `0021` and `0032` as the harness-policy page for the native static `descriptors.wast` and `exact.wast` fixtures.
- Added `custom-descriptors/ref-get-desc-fixture-path.md` from `0022` through `0028` as the full-stack compatibility page for `ref.get_desc`, legacy GC aliases, exact `ref.null`, bottom-null operands, and the mixed-runtime fixture path.
- Added `custom-descriptors/exact-reference-equivalence.md` from `0029`, `0030`, and `0031` as the rule page for passive typed empty `elem` surface plus structural exact-reference matching for structs and functions.
- Added `strings/string-const-surface.md` from `0052` as the living page for the public `string.const` surface, binary string-literal section, constant-expression rule, and IR payload handling.
- Updated `docs/wiki/index.md` so the new WAST, custom-descriptor, and string pages are discoverable from the catalog.

## [2026-04-09] refresh | resync IR2 root contracts with current hot-IR code

- Updated `docs/0059-2026-03-24-ir2-architecture-rules.md` and `ir2/architecture-rules.md` so the architecture contract reflects the current split `src/ir` module map, the live post-dominance overlay, and `hot.mbt`'s current role as facade glue instead of an in-progress monolith.
- Updated `docs/0063-2026-03-24-pass-port-batches-and-registry-map.md` and `ir2/registry-map.md` so the canonical registry surface now matches code for active `precompute`, `heap2local`, `reorder-locals`, and the real preset expansion.
- Updated `docs/0065-2026-03-24-ir2-execution-plan.md` and `ir2/execution-plan.md` so the handoff plan matches the current active pass surface and the real `optimize` / `shrink` sequence.

## [2026-04-09] refresh | switch living Binaryen oracle references to version_129

- Updated the living Binaryen no-DWARF orientation page and active backlog to use Binaryen `version_129` as the upstream source oracle for new pass research.
- Confirmed from upstream `version_129` source that the open-world no-DWARF `-O` / `-Os` path for the MoonBit debug artifact still matches the archived `version_125` top-level shape used by the current wiki.
- Marked the saved `heap2local` and `reorder-locals` compare evidence as historical `version_125` command output until the local `wasm-opt` toolchain is upgraded.

## [2026-04-09] organize | scaffold pass-folder indexes and expand duplicate-function-elimination

- Added `docs/wiki/binaryen/passes/index.md` as the namespace catalog for active implemented pass folders.
- Added pass-folder landing pages for every currently implemented active pass so each pass now has a stable wiki home even where detailed subpages are still pending.
- Expanded `duplicate-function-elimination` into a multi-entry folder with `index.md`, `wat-shapes.md`, `binaryen-strategy.md`, `starshine-hot-ir-strategy.md`, and `type-compaction-and-metadata.md`.
- Refreshed `duplicate-function-elimination/parity.md` to distinguish direct explicit-pass behavior from the larger option-dependent iteration budget Binaryen uses inside `-O` / `-Os`.

## [2026-04-09] maintain | expand remove-unused-module-elements into a real pass folder

- Expanded `remove-unused-module-elements` from a stub landing page into a multi-entry folder with `index.md`, `wat-shapes.md`, `binaryen-strategy.md`, `starshine-hot-ir-strategy.md`, `retention-and-index-rewrites.md`, and `parity.md`.
- Documented RUME's imported-parent retention rules, explicit module-index rewrite surface, and the current in-tree coverage for empty active data, no-op active elem segments, and imported survivor remaps.
- Updated the root wiki catalog and pass-folder catalog so the expanded RUME pages are discoverable.

## [2026-04-10] investigate | record the current ssa-nomerge artifact parity blocker

- Added `binaryen/passes/ssa-nomerge/parity.md` plus raw research note `0076-2026-04-10-ssa-nomerge-parity-investigation.md` for the current `ssa-nomerge` evidence.
- Recorded that the checked-in debug CLI artifact now validates as input, Binaryen `--ssa-nomerge` succeeds on it, but direct Starshine `--ssa-nomerge` still fails final module validation at `Func 523`.
- Recorded that the seeded random compare rerun stayed semantically clean and only hit a Binaryen `binaryen-rec-group-zero` parser-gap case, so direct artifact replay remains mandatory for `ssa-nomerge` signoff.
- Updated the root wiki catalog and the Binaryen pass-folder catalog so the new parity page is discoverable.

## [2026-04-10] refresh | record the safe-fail ssa-nomerge replay fix

- Updated `binaryen/passes/ssa-nomerge/parity.md` and raw research note `0076-2026-04-10-ssa-nomerge-parity-investigation.md` after the current source build stopped letting the `Func 523` invalid writeback survive to final module validation.
- Recorded the new source-mode artifact replay evidence: `Func 523` now fails closed as `writeback-validate:type mismatch`, `Func 3773` is also visible as `writeback-validate:stack underflow`, and final module validation completes.

## [2026-04-10] fix | close the current ssa-nomerge dead-param parity family

- Updated `binaryen/passes/ssa-nomerge/parity.md`, `binaryen/passes/ssa-nomerge/index.md`, and raw research note `0076-2026-04-10-ssa-nomerge-parity-investigation.md` after current source `ssa-nomerge` was brought back into output parity with Binaryen for the dead-param write family.
- Recorded the new signoff evidence at `/tmp/ssa-pass-fuzz-rebased-2026-04-10-rerun3`, `/tmp/ssa-pass-fuzz-rebased-2026-04-10-signoff`, `/tmp/ssa-pass-fuzz-rebased-2026-04-10-signoff-gen-valid`, `/tmp/starshine-self-optimize-compare-starshine-debug-wasi-2`, and `/tmp/ssa-nomerge-final.log`.
- Updated the root wiki catalog and Binaryen pass-folder catalog so the parity page now points at the fixed output-facing state and the remaining trace-level raw-lowering caveat instead of the retired final-validation blocker.

## [2026-04-10] refresh | reprove ssa-nomerge parity after the tuple merge

- Updated `binaryen/passes/ssa-nomerge/parity.md`, `binaryen/passes/ssa-nomerge/index.md`, and raw research note `0076-2026-04-10-ssa-nomerge-parity-investigation.md` after rerunning `ssa-nomerge` parity lanes on the merged tree.
- Recorded the new post-merge evidence at `/tmp/ssa-pass-fuzz-postcommit-mixed-seed51a`, `/tmp/ssa-pass-fuzz-postcommit-genvalid-seed51a`, `/tmp/starshine-self-optimize-compare-starshine-debug-wasi-2`, and `/tmp/ssa-nomerge-postcommit.log`.
- Narrowed the traced remaining gap from the earlier two writeback-validate families down to `Func 523` plus `228` `suspicious-escape-carrier` skips, and updated the root wiki catalogs to match.
- Updated `agent-todo.md` so the shared post-SSA blocker note no longer claims `ssa-nomerge` safety without the newer per-function writeback-validation evidence.

## [2026-04-10] expand | turn tuple-optimization into a full pass folder

- Added `binaryen/passes/tuple-optimization/index.md` as the canonical landing page for tuple-opt documentation.
- Added `tuple-optimization/wat-shapes.md`, `binaryen-strategy.md`, and `starshine-hot-ir-strategy.md` to split the old monolithic `0076` note into concrete transform families, upstream strategy, and current HOT-native implementation strategy.
- Added `tuple-optimization/scheduler-and-gates.md` to separate explicit-pass correctness from still-pending preset-slot parity.
- Added `tuple-optimization/reduced-repros-and-evidence.md` and `tuple-optimization/parity.md` to capture the current direct native-compare status, the still-red exact-shape families, and the standing artifact/runtime gap.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the new tuple-opt folder is discoverable from the main wiki catalog and pass namespace catalog.

## [2026-04-10] maintain | narrow chained host-copy tail-live0 parity drift

- Updated `tuple-optimization/parity.md` and `tuple-optimization/reduced-repros-and-evidence.md` with the current anchored host-copy split: the middle group wants the specialized intermediate host-result carrier path again, while the terminal group is still the remaining exact-shape blocker.

## [2026-04-10] maintain | preserve terminal host tees through tuple cleanup and lowering

- Updated the tuple-opt backlog and wiki after confirming that one remaining `tail-live0` parity bug was not the rewrite root itself, but Starshine collapsing preserved `drop(local.tee ...)` host tails into `local.set`.
- Recorded the new guarded behavior in `src/passes/tuple_optimization.mbt` and `src/ir/hot_lower.mbt`: preserved host tees that still feed later non-drop reads now survive tuple cleanup and lowerer canonicalization.
- Narrowed the remaining `tail-live0` gap again: the final `local.tee 0` plus tail drops are now preserved, and the remaining deterministic diff is the anchored host-copy staging order and temp layout immediately before that tail.
- Updated `agent-todo.md` so the active tuple backlog now records the current deterministic tail-live0 blocker in that more specific form instead of treating the whole family as one undifferentiated drift.

## [2026-04-10] maintain | refresh tuple-opt parity and reduced performance evidence

## [2026-04-10] maintain | collapse tuple seed screening into one precise scan and cut full-artifact pass time

- Reworked `src/passes/tuple_optimization.mbt` so tuple-opt no longer runs a weak whole-function no-op screen in `run`, repeats that same weak screen in `analyze`, and then pays a second seed walk anyway. The pass now builds or reuses `use-def`, performs one precise seed-group scan, bails out immediately when that scan is empty, and reuses the collected groups for the rest of analysis.
- Tightened the seed, result-block, and scalar-forward collectors themselves to avoid the old per-candidate GC churn: they now use stamped local marks for duplicate-lane detection and write lane order directly instead of building temporary reverse arrays and calling `tuple_optimization_seen_local(...)` linearly on every lane.
- Added focused white-box invariants in `src/passes/tuple_optimization_wbtest.mbt` proving that duplicate lane locals in both seed groups and result-block copy groups are still rejected under the new stamped-mark collectors.
- Rechecked correctness on the kept tree: `moon test --package jtenner/starshine/passes --file tuple_optimization_wbtest.mbt` is `44 / 44`, `moon test --package jtenner/starshine/cmd --file cmd_native_wbtest.mbt --target native --filter '*tuple-optimization*'` is `15 / 15`, `moon test --package jtenner/starshine/cmd --file cmd_wbtest.mbt --filter '*tuple-optimization*'` is `7 / 7`, `/tmp/pass-fuzz-tuple-cleaned-genvalid-1000-2026-04-10` is `1000 / 1000`, and `/tmp/pass-fuzz-tuple-cleaned-smith-200-2026-04-10` is `199 / 200` with the same Binaryen-only `binaryen-rec-group-zero` command failure.
- The full-artifact runtime moved materially, not just the reduced repros. `/tmp/self-opt-tuple-full-candidatefilter-2026-04-10` and `/tmp/self-opt-tuple-full-candidatefilter-rerun-2026-04-10` are both canonically green (`Canonical function compare equal: yes`, `Normalized WAT equal: yes`) while Starshine tuple-pass time dropped into a `325.221-361.452 ms` band from the earlier `~966 ms` band.
- Fresh pass-trace evidence explains why the improvement is real: tuple-opt still visits `4462` functions but now changes only `18`, and total pass time on the cleaned kept tree is `277790 us` instead of `960971 us`, so the old unchanged-function hot quartet is gone and the remaining runtime debt is concentrated in `Func 1673` plus a smaller tail of candidate-heavy functions.
- The current kept-tree split is now sharper too: `Func 1673` (`_M0FP37jtenner9starshine4wast17wt__lower__module`) is still the main tuple-pass outlier at `101831 us`, followed by a much smaller tail (`148`, `2389`, `1905`, `3660`, `147`), while the biggest `analysis:use-def` costs are still in different functions (`3612`, `1553`, `1525`). That means the next tuple-runtime slice should target `Func 1673`-style candidate/query work, while any wall-time reduction beyond the reported tuple pass timer will need separate use-def or pipeline work.

## [2026-04-10] maintain | rerun full tuple self-opt compare and reframe the remaining work as unchanged-function runtime debt

- Reran the full tuple-only debug-artifact compare through the upgraded canonical fallback at `/tmp/self-opt-tuple-full-canonical-2026-04-10` and `/tmp/self-opt-tuple-full-smalllocals-2026-04-10`; raw normalized WAT text is still different, but canonical per-function comparison is green on both runs, so full tuple parity is now freshly re-proven on current head instead of inferred only from the saved `/tmp/self-opt-tuple-current` pair.
- Landed a low-risk tuple analysis cleanup in `src/passes/tuple_optimization.mbt`: alias resolution now reuses precomputed single-write locals plus stamped visit marks, and the seed / copy-group collectors no longer allocate `local_count`-sized duplicate-check arrays for every multi-value candidate lane walk.
- Rechecked parity on the kept pass body: `moon test --package jtenner/starshine/cmd --file cmd_native_wbtest.mbt --target native --filter '*tuple-optimization*'` is still `15 / 15`, `moon test --package jtenner/starshine/passes --file tuple_optimization_wbtest.mbt` is still `42 / 42`, `/tmp/pass-fuzz-tuple-genvalid-smalllocals-1000-2026-04-10` is `1000 / 1000`, and `/tmp/pass-fuzz-tuple-smith-smalllocals-2026-04-10` is `199 / 200` with the same Binaryen-only `binaryen-rec-group-zero` command failure.
- Added one more full-artifact performance diagnosis pass trace: tuple-opt currently visits `4462` functions and changes only `27`, with the runtime dominated by unchanged giant functions (`Func 3612`, `1553`, `1525`, `1673`) while the old parity focal point `Func 3660` is only a small fraction of the total. The pass therefore remains correctness-green, but this slice is effectively performance-neutral on the full artifact and the next real optimization target is unchanged-function candidate analysis rather than more local scratch cleanup.

- Updated the tuple-opt parity and reduced-repro pages after rerunning current-head coverage: direct native tuple compare is now `14 / 14`, black-box tuple cmd coverage is `7 / 7`, and the remaining in-tree red surface is down to six white-box exact-shape tests at `35 / 41`.
- Recorded fresh isolated fuzz evidence at `/tmp/pass-fuzz-tuple-gen-valid-2026-04-10` (`1000 / 1000` matches) and `/tmp/pass-fuzz-tuple-smith-2026-04-10` (`199 / 199` comparable matches plus one Binaryen `binaryen-rec-group-zero` parser failure).
- Added the fresh reduced self-opt timing baseline `/tmp/self-opt-tuple-tail-live0-2026-04-10`, which confirms that the reduced `tail-live0` parity lane is green while runtime and pass time are still materially slower than Binaryen.
- Rebased the tuple backlog from “red reduced native parity family” to the current open work: pre-lower `TupleMake` carrier debt in raw `tail-live0` HOT, six white-box exact-shape expectations, the full artifact gap at `func $3639`, and the remaining tuple-opt performance budget.

## [2026-04-10] maintain | retire the tuple `func $3639` blocker as a compare-surface bug

- Added one more reduced tuple regression family: a two-lane exact-copy chain past an unrelated multivalue block is now checked in both `src/passes/tuple_optimization_wbtest.mbt` and `src/cmd/cmd_native_wbtest.mbt`.
- That reduced probe was the key diagnostic: raw `wasm-opt -S --strip-debug` WAT still differs on the probe, but the white-box pass surface stays scalar and direct native Binaryen parity stays green, proving raw normalized WAT is too strict to serve as the only tuple oracle.
- Upgraded `scripts/lib/self-optimize-compare-task.ts` so self-opt compare now falls back to canonical per-function pretty output through `--print-func` when raw normalized WAT differs, ports the native tuple helper’s local alpha-normalization and scalar-ladder reordering into TypeScript, and records real mismatches as `func-definedN-absM.*`.
- Resolved the old saved `/tmp/self-opt-tuple-current` numbering confusion: the raw hunk at printed WAT `func $3639` is defined-function ordinal `3639`, which maps to absolute `Func[3660]` after the module’s `21` imported funcs; the canonical CLI function printer matches on that saved pair, so the old `func $3639` blocker is no longer treated as a proven tuple rewrite bug.

## [2026-04-10] maintain | retire stale tuple-opt white-box exact-shape reds

- Updated `src/passes/tuple_optimization_wbtest.mbt` so the six old white-box red cases now check stable scalarization and copyback invariants instead of temp-local numbering or transient carrier scaffolding; the full tuple white-box file is now `41 / 41`.
- Rechecked the surrounding tuple surface on the kept pass implementation: native direct Binaryen compare is still `14 / 14`, black-box tuple cmd coverage is still `7 / 7`, `/tmp/pass-fuzz-tuple-gen-valid-wbrefresh-2026-04-10` is `1000 / 1000`, and `/tmp/pass-fuzz-tuple-smith-wbrefresh-2026-04-10` is `199 / 200` with the same lone Binaryen `binaryen-rec-group-zero` parser failure at case `29`.
- Updated the tuple parity and reduced-repro wiki pages plus `agent-todo.md` so the docs now treat the white-box red surface as resolved expectation debt, not as an active parity blocker.
- Tried and rejected one more cleanup-path perf slice locally: the cleanup-query experiment did not produce a stable win on the reduced `tail-live0` repro, so the kept pass code stays on the earlier `0.511 ms` checkpointed implementation.

## [2026-04-10] maintain | prove stamped visit-buffer reuse is parity-safe but not yet a real tuple-opt speedup

- Updated the tuple-opt parity and reduced-repro pages after landing stamped visit-buffer reuse in forwarded-use analysis inside `src/passes/tuple_optimization.mbt`.
- Recorded fresh post-refactor compare-pass evidence at `/tmp/pass-fuzz-tuple-gen-valid-visitmarks-2026-04-10` (`1000 / 1000` matches) and `/tmp/pass-fuzz-tuple-smith-visitmarks-2026-04-10` (`199 / 199` comparable matches plus the same Binaryen-only `binaryen-rec-group-zero` parser failure at case `29`).
- Added the fresh reduced self-opt timing rerun `/tmp/self-opt-tuple-tail-live0-visitmarks-2026-04-10`, which shows the refactor kept parity but left runtime and pass time effectively unchanged on the reduced `tail-live0` repro.
- Updated the active tuple backlog so the next performance slice is explicitly about caching direct-use and forwarded-query answers, not just reusing traversal scratch arrays.

## [2026-04-10] maintain | keep tuple-opt parity green while adding cached forwarded-use answers

- Updated the tuple-opt parity and reduced-repro pages after landing per-local forwarded-use memoization and the no-group query-summary fast path in `src/passes/tuple_optimization.mbt`.
- Recorded fresh compare-pass evidence at `/tmp/pass-fuzz-tuple-gen-valid-forwardcache-2026-04-10`, `/tmp/pass-fuzz-tuple-smith-forwardcache-2026-04-10`, `/tmp/pass-fuzz-tuple-gen-valid-emptysummary-2026-04-10`, and `/tmp/pass-fuzz-tuple-smith-emptysummary-2026-04-10`; all comparable cases still match Binaryen, and the only continuing fuzz command failure is the same Binaryen `binaryen-rec-group-zero` parser limit at smith case `29`.
- Added the reduced timing ladder entries `/tmp/self-opt-tuple-tail-live0-querysummary-2026-04-10`, `/tmp/self-opt-tuple-tail-live0-forwardcache-2026-04-10`, and `/tmp/self-opt-tuple-tail-live0-emptysummary-2026-04-10` so the wiki captures the actual performance progression instead of only the best-looking point.
- Updated the tuple backlog to reflect the new conclusion: cached forwarded-use answers plus skipping summaries on no-group functions recovered the reduced-case regression and brought tuple-pass time down to `0.511 ms`, but candidate-heavy query-summary cost, six white-box exact-shape failures, and the full artifact gap at `func $3639` are still open.

## [2026-04-10] maintain | separate tuple-opt parity from launcher churn on long fuzz lanes

- Updated the tuple-opt parity and reduced-repro pages after rerunning the current tree through the direct native cmd binary and pushing the `gen-valid` lane back out to `10000` comparisons.
- Recorded fresh current-tree direct-binary fuzz evidence at `/tmp/pass-fuzz-tuple-gen-valid-bincurrent-2026-04-10` (`1000 / 1000`), `/tmp/pass-fuzz-tuple-smith-bincurrent-2026-04-10` (`199 / 199` comparable matches plus the same Binaryen-only `binaryen-rec-group-zero` parser failure), and `/tmp/pass-fuzz-tuple-gen-valid-10000-bin-sharedmarks-2026-04-10` (`10000 / 10000` clean).
- Recorded that the earlier `moon run`-backed long lane `/tmp/pass-fuzz-tuple-gen-valid-10000-emptysummary-2026-04-10` stopped after `2124` matches with repeated missing-output validation failures, while direct replay of the first recorded input still writes valid output, so the current evidence points at launcher churn rather than a tuple-opt semantic regression.
- Added the later reduced timing ladder entries `/tmp/self-opt-tuple-tail-live0-sharedmarks-2026-04-10` and `/tmp/self-opt-tuple-tail-live0-cleanupfast-2026-04-10`, which show that the newer shared traversal-mark reuse and cleanup fast-path experiments did not beat the earlier `0.511 ms` reduced pass checkpoint.

## [2026-04-10] ingest | crystallize simplify-locals research into the pass folder

- Moved the branch-local `simplify-locals` research note into `docs/wiki/raw/research/0076-2026-04-01-simplify-locals-binaryen-research-plan.md` because archive serial `0073` is already assigned in the shared wiki history.
- Expanded `binaryen/passes/simplify-locals/` with a real landing page plus `binaryen-strategy.md` and `starshine-hot-ir-strategy.md`.
- Recorded the staged Binaryen sink and cleanup model, the repo's no-structure-first HOT-IR port strategy, and the fact that the archived note is a historical source while living Binaryen pass pages now track the shared `version_129` oracle.
- Updated `docs/wiki/index.md` and `docs/wiki/binaryen/passes/index.md` so the new simplify-locals pages are discoverable.

## [2026-04-10] expand | turn simplify-locals into a full pass dossier

- Expanded `binaryen/passes/simplify-locals/` beyond the initial landing page into a full pass folder with `wat-shapes.md`, `implementation-map.md`, `effect-ordering-and-barriers.md`, `raw-lane-and-writeback.md`, `validation-and-signoff.md`, `performance-and-artifact-frontiers.md`, and `parity.md`.
- Rewrote `binaryen-strategy.md` and `starshine-hot-ir-strategy.md` into detailed maintenance pages instead of short orientation notes.
- Captured the exact WAT families, the actual helper ownership map in `simplify_locals.mbt` and `pass_manager.mbt`, the raw skip-reason taxonomy, and the dated artifact hotspot/frontier status that had previously only lived in backlog notes.
- Updated the pass-folder catalog and root wiki index so the full simplify-locals dossier is discoverable from the shared wiki entrypoints.

## [2026-04-10] research | map MoonBit formal proof rollout

- Added archived research note `docs/wiki/raw/research/0077-2026-04-10-moonbit-prove-strategy.md` covering the current official `moon prove` workflow, local CLI/toolchain evidence, proof-model constraints, and a Starshine target matrix.
- Added `docs/wiki/validation/moonbit-prove-strategy.md` as the living rollout plan for bootstrap, `src/validate`-first proof adoption, and deferred proof surfaces.
- Updated `docs/wiki/index.md` so the new validation page is discoverable from the wiki catalog.

## [2026-04-10] implement | land first proved helper and record current package-boundary blocker

- Added active proof package `src/validate_proof` with a proved `label_stack_storage_index` helper and package-local `moon prove src/validate_proof` entrypoint.
- Rewired `src/validate/env.mbt` to use the proved helper for `Env::get_label_types`, and added direct `LabelStack` plus helper regression tests beside the validator code.
- Updated the live proof strategy page and archived research note to record the current blocker on proving `src/validate` directly: the generated WhyML for `jtenner/starshine/lib` currently fails with `unbound type symbol 'name'`.

## [2026-04-11] implement | extend the proof kernel through code-body diagnostics and keep the wiki current

- Added proved `defined_body_func_index` to `src/validate_proof`, bringing the active sidecar helper kernel to `8` proved goals.
- Rewired the remaining code-body diagnostic paths in `src/validate/validate.mbt` to use proved body-to-function index helpers for bulk-memory data-count errors and `ref.func` declaration diagnostics.
- Added imported-prefix validator regressions so those diagnostics now explicitly pin `FuncIdx(1)` behavior when one imported function precedes the failing defined body.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md`, archived research note `0077-2026-04-10-moonbit-prove-strategy.md`, and `docs/wiki/index.md` so the living docs reflect the current helper inventory, usage surface, and `8`-goal proof-kernel state.

## [2026-04-11] implement | prove suffix-base recovery and document the virtual-group rule

- Added proved `suffix_start_index` to `src/validate_proof`, bringing the active sidecar helper kernel to `9` proved goals.
- Rewired `validate_codesec_diag` to recover the imported-function prefix through the proved suffix helper instead of open-coding `total - defined`.
- Rewired descriptor-metadata validation to use the same helper and documented the real rule: the current rectype group may already be appended to `env`, or it may still be a virtual suffix immediately after the existing type space during standalone `RecType` validation.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md`, archived research note `0077-2026-04-10-moonbit-prove-strategy.md`, and `docs/wiki/index.md` so the living docs reflect the new helper and the current `9`-goal kernel state.

## [2026-04-11] maintain | extend bounded-index reuse through remaining name-section checks

- Rewired the remaining name-section bounds checks in `src/validate/validate.mbt` so local, label, and struct-field name indices now all flow through the proved `bounded_index` helper instead of open-coded `idx >= total` comparisons.
- Kept the proof kernel shape unchanged at `9` goals while widening the validator surface that now relies on the existing helper inventory.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md` and archived research note `0077-2026-04-10-moonbit-prove-strategy.md` so the living proof docs reflect that broader `bounded_index` usage.

## [2026-04-11] fix | preserve branch-local `LabelStack` semantics while reusing proved reverse-index arithmetic

- Rewired `LabelStack::get` in `src/validate/env.mbt` to compute its reverse offset through the proved `label_stack_storage_index` helper while still traversing `head` / `parents`, instead of indexing the shared `values` backing array directly.
- Added a divergent-copy regression in `src/validate/env_tests.mbt` proving that `LabelStack.copy` branches can push different tails and still preserve each branch's logical declaration-order lookup.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md`, archived research note `0077-2026-04-10-moonbit-prove-strategy.md`, and `agent-todo.md` so the proof rollout docs now treat persistent branch semantics as an explicit invariant of the `Env` / `LabelStack` slice.

## [2026-04-11] maintain | widen proved flat-index reuse across the remaining name-section spaces

- Rewired the remaining flat name-section bounds checks in `src/validate/validate.mbt` so function, type, table, memory, global, elem, data, and tag name indices now all flow through the proved `bounded_index` helper instead of bespoke per-space lookups.
- Added focused validator regressions for out-of-range function-name and type-name entries so the widened helper reuse is locked by executable diagnostics, not just by the refactor itself.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md` and archived research note `0077-2026-04-10-moonbit-prove-strategy.md` so the living proof docs now describe `bounded_index` as the flat name-section index gate across all current validator spaces.

## [2026-04-11] maintain | widen latest-index proof-helper reuse into typecheck branch-label tails

- Rewired the `br_on_non_null`, `br_on_cast`, and `br_on_cast_fail` helper paths in `src/validate/typecheck.mbt` to recover the trailing label slot through the proved `latest_stack_index` helper instead of open-coding `label_ts.length() - 1`.
- Added focused typechecker regressions for the empty-label-payload error surface on those three branch helpers so the refactor stays pinned by exact observable errors.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md` and archived research note `0077-2026-04-10-moonbit-prove-strategy.md` so the living proof docs now treat `latest_stack_index` as active in the first typecheck helper slice as well as `env`.

## [2026-04-11] maintain | widen latest-index proof-helper reuse into core typecheck stack-top recovery

- Rewired `check_pop_types_from_top` in `src/validate/typecheck.mbt` to recover the current stack-top index through the proved `latest_stack_index` helper instead of open-coding `st.stack.length() - 1`.
- Added direct wrapper-level regressions for `tc_state_validate_end_stack` so reachable empty-stack underflow and unreachable virtual-bottom suffixes are both locked before wider `PRV004` helper extraction.
- Updated `docs/wiki/validation/moonbit-prove-strategy.md`, archived research note `0077-2026-04-10-moonbit-prove-strategy.md`, and `agent-todo.md` so the current proof rollout now records `latest_stack_index` as active in both branch-label tails and the core typecheck stack walk.

## [2026-04-11] maintain | reduce the `ssa-nomerge` `Func 523` follow-up into executable regressions

- Updated `binaryen/passes/ssa-nomerge/parity.md`, `binaryen/passes/ssa-nomerge/index.md`, archived research note `0076-2026-04-10-ssa-nomerge-parity-investigation.md`, `docs/wiki/index.md`, and `agent-todo.md` after reducing one unreachable compare-carrier slice from the traced `Func 523` family into checked-in lift and pass regressions.
- Recorded the new focused evidence in `src/ir/hot_lift_test.mbt` and `src/passes/ssa_nomerge_test.mbt`, plus the supporting concrete-pop-count follow-up in `src/ir/hot_lift.mbt`.
- Recorded that fresh direct artifact replay at `/tmp/ssa-nomerge-func523-followup.log` still exits zero and validates its output module, but still logs the same `skip-invalid-lower func=(Func 523) reason=writeback-validate:type mismatch` plus `228` `suspicious-escape-carrier` skips, so the reduced follow-up narrowed the family without yet closing the remaining artifact-only gap.

## [2026-04-11] health | refresh pass smoke evidence for `reorder-locals`, `heap2local`, and `remove-unused-module-elements`

- Added a new research node `docs/wiki/raw/research/0078-2026-04-11-parity-smoke-rerun.md` to archive the fresh 200-case smoke rerun matrix and mismatch classification.
- Updated `binaryen/no-dwarf-default-optimize-path.md` metadata to reflect current `version_129` toolchain parity context (`last_reviewed`, command-availability note).
- Reran compare health checks on current `version_129`:
  - `reorder-locals` (`both`): `199 / 200` compared, `198` normalized matches, `1` mismatch, `1` `binaryen-rec-group-zero` command failure (`case-000029-wasm-smith`).
  - `reorder-locals` (`gen-valid`): `199 / 200` compared, `199` normalized matches, `0` command failures, `1` mismatch (`case-000150-gen-valid`).
  - `heap2local`: `199 / 200` compared, `199` normalized matches, `0` mismatches, `1` `binaryen-rec-group-zero` command failure (`case-000029-wasm-smith`).
  - `remove-unused-module-elements`: `199 / 200` compared, `199` normalized matches, `0` mismatches, `1` `binaryen-rec-group-zero` command failure (`case-000029-wasm-smith`).
- Updated `reorder-locals`, `heap2local`, and `remove-unused-module-elements` parity pages to cite this health note and pin the command-failure classification to artifacts currently on disk.
- Verified the persistent `reorder-locals` mismatch remains behavioral-noise compatible by executing both failing outputs with `wasmtime --invoke main` (`exit 0` on both).

## [2026-04-11] health | rerun focused Binaryen health checks for DFE, pick-load-signs, RUB, tuple

- Added `docs/wiki/raw/research/0079-2026-04-11-pass-fuzz-health-round-two.md` to archive a second focused smoke batch for:
  - `duplicate-function-elimination`
  - `pick-load-signs`
  - `remove-unused-brs`
  - `tuple-optimization`
- Updated parity pages for all four passes to carry the fresh `2026-04-11` evidence and command-failure/mismatch classification.
- Notably, this rerun raised `remove-unused-brs` as the current open mismatch surface in this band:
  - mixed: `199 / 199` compared, `175` normalized matches, `24` mismatches
  - gen-valid: `114 / 114` compared, `84` matches, `30` mismatches, `maxFailuresHit: true`
- `duplicate-function-elimination`, `pick-load-signs`, and `tuple-optimization` remain clean in this 200-case smoke band (with one known `binaryen-rec-group-zero` command-failure class on mixed runs).

## [2026-04-11] maintain | expand wiki catalog coverage for active pass folder landing pages

- Updated `docs/wiki/index.md` so all currently active Binaryen pass landing pages under `docs/wiki/binaryen/passes/` are discoverable from the human-readable catalog (`binaryen/passes/index.md` remains the module namespace hub).
- The index lint now shows no missing non-raw, non-log living pages under `docs/wiki/`.
- This change does not alter pass semantics; it closes the knowledge-map maintenance gap for scaffolded and active pass folders alike.

## [2026-04-11] maintain | clarify proof rollout gates and active kernel slice list in moonbit proof docs

- Updated `validation/moonbit-prove-strategy.md` to reflect current `moonbit` proof gating (`src/validate` is not proof-enabled by default) and to make `moon prove src/validate*` commands explicitly conditional on enabling `"proof-enabled": true` in `src/validate/moon.pkg`.
- Added `src/validate_proof/suffix_index.mbt` to the staged proof-slice rollout and expanded source citations in the proof strategy page to include both `src/validate/moon.pkg` and `src/validate_proof/moon.pkg` package boundaries.
- Kept the archived `0077-2026-04-10-moonbit-prove-strategy.md` failure history as historical context while clarifying the present rollout sequence.

## [2026-04-11] maintain | audit late `-O4z` tail-pass wiring and stub-coverage wording

- Re-checked `-O4z` pathing and pass scheduling in `src/cmd/cli.mbt`, `src/cmd/cmd.mbt`, and `src/passes/optimize.mbt`:
  - `-O4z` maps to `optimize_level=4`, `shrink_level=4`; with no explicit pass flags this resolves to `shrink`.
  - `shrink` expands to the hot/module tail sequence including `global-refining`, `memory-packing`, `once-reduction`, `dead-code-elimination`, `optimize-instructions`, `heap-store-optimization`, `precompute`, `simplify-locals`, plus supporting module neighbors.
- Confirmed each inspected late pass is implemented/dispatched in-tree today:
  - module-path passes: `global-refining`, `memory-packing`, `once-reduction`
  - hot-path passes: `dead-code-elimination`, `optimize-instructions`, `heap-store-optimization`, `precompute`, `simplify-locals`
  - `vacuum` dispatch lives in `optimize.mbt` + `pass_manager.mbt` (hot path, no dedicated `src/passes/vacuum.mbt`).
- Updated `docs/wiki/binaryen/passes/index.md` and late-pass landing pages to reflect implemented status while keeping `status: stub` where parity/shape deep docs are still pending.
- Added `docs/wiki/raw/research/0080-2026-04-11-late-pipeline-pass-dispatch-audit.md` to preserve this pass-wiring and wiki-health audit.

## [2026-04-14] maintain | split slow simplify-locals multivalue perf stress out of the default package lane

- Moved the slowest synthetic `simplify-locals` multivalue perf witnesses into a dedicated package lane at `src/passes_perf_long/simplify_locals_multivalue_perf_test.mbt` so `moon test src/passes` stays a fast default edit-loop command.
- Trimmed the default `src/passes/perf_test.mbt` multivalue ladder sizes down to the smaller witnesses needed for the normal raw-skip / no-lift assertions and removed the irreducibly slow flat-dense stress case from that default package file.
- Updated the simplify-locals validation, implementation-map, and performance-frontier wiki pages so the docs now distinguish between the lean default package lane and the explicit opt-in command `moon test src/passes_perf_long`.

## [2026-04-14] validate | rerun simplify-locals native parity lanes after test cleanup

- Repaired the current simplify-locals branch surface until `moon test src/passes`, `moon test src/cmd`, and `moon test src/passes_perf_long` all passed again, including the simplify-locals raw-lane hookup, lowered nop preservation, dead control-owner rehoming, and the stale tuple / HSO / precompute expectation updates needed for the current kept outputs.
- Reran the native-binary simplify-locals compare-pass lane at `.tmp/pass-fuzz-sl-current-2026-04-14`; it finished at `10000/10000` compared cases with `10000` normalized matches and `0` mismatches.
- Reran the native-binary debug-artifact self-opt compare at `.tmp/self-opt-sl-current-2026-04-14`; it is now canonically green on the checked-in artifact (`normalizedWatEqual=true`, `canonicalFuncPrettyEqual=true`, no differing function indices) while still recording a large runtime gap and raw text / wasm inequality versus Binaryen.
- Updated the simplify-locals parity, validation, and performance-frontier pages so the wiki now treats the old `Func 71` first-diff story as historical context and the current live debt as runtime plus raw artifact canonicalization.

## [2026-04-18] maintain | compact wiki entrypoints and ingest current O4z plus CLI startup audits

- Added `binaryen/passes/late-pipeline-dispatch.md` from archived audits `0080` and `0093` as the compact current note for the `-O4z` / `shrink` tail roster, the module-vs-hot split, and the latest ordered `cmd.wasm` audit summary.
- Threaded the 2026-04-18 ordered-audit follow-up into `docs/wiki/binaryen/passes/remove-unused-brs/parity.md`, `docs/wiki/binaryen/passes/optimize-instructions/index.md`, `docs/wiki/binaryen/passes/precompute/index.md`, and `docs/wiki/binaryen/passes/vacuum/index.md`, plus the matching active slice in `agent-todo.md`, so the slot-specific corruption notes and cleanup checklist stay surfaced in the living docs.
- Added `tooling/cli-startup-path.md` from `0092` as the compact startup-path note, explicitly marking the older registry/help/config concerns as superseded while keeping path normalization and bucketed glob scanning as the live follow-up surface.
- Shortened the root wiki catalog entries in `docs/wiki/index.md` and the Binaryen pass-folder map in `docs/wiki/binaryen/passes/index.md` so the entrypoints point at the detailed pages instead of re-listing their full rosters.

## [2026-05-04] implement | land active simplify-locals-nostructure direct pass

- Moved `simplify-locals-nostructure` from removed/planned status to active direct hot-pass status, with `simplify-locals-no-structure` kept as a compatibility alias.
- Recorded the implementation shape: reuse the landed `simplify_locals.mbt` local-sink/dead-cleanup cycles while disabling structure-result rewrites, plus sharing existing raw simplify-locals artifact gates for runtime.
- Updated the pass tracker and Starshine strategy page with direct oracle evidence: `10000/10000` gen-valid matches, `9975/10000` mixed-generator comparable matches with no mismatches, and debug-artifact self-opt normalized/canonical-function parity.
- Left public `optimize` / `shrink` preset placement as ordered-neighborhood follow-up until the tuple/no-structure/vacuum/reorder-local slot is replayed end to end.

## [2026-05-05] maintain | refresh plain `simplify-globals` current-main bridge and port-readiness

- Added `docs/wiki/raw/binaryen/2026-05-05-simplify-globals-current-main-recheck.md` plus `docs/wiki/raw/research/0461-2026-05-05-simplify-globals-current-main-recheck.md` after rechecking official Binaryen current `main` `SimplifyGlobals.cpp`, `pass.cpp`, helper headers, and the clustered `simplify-globals*` / `propagate-globals-globally.wast` surfaces.
- Created `docs/wiki/binaryen/passes/simplify-globals/starshine-port-readiness-and-validation.md` and refreshed the plain dossier overview, Binaryen strategy, implementation/test map, plain-vs-optimizing safety guide, WAT-shape catalog, and Starshine strategy page so the current-main freshness layer, port-readiness ladder, and exact local boundary-only status are visible from the living dossier.
- Updated `docs/wiki/index.md`, `docs/wiki/binaryen/passes/tracker.md`, and the simplify-globals folder page maps so the new current-main bridge and port-readiness page are discoverable from the shared catalogs.
- Recorded unchanged contract status: current `main` still uses the same shared `SimplifyGlobals.cpp` family split, while Starshine still has no plain `simplify-globals` owner file or dedicated backlog slice and continues to reject the boundary-only pass honestly.
