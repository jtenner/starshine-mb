# IR2 Test Matrix

## Scope

- Define the shared fixture and golden-coverage layer for IR2 hot bodies, analyses, lowering, analysis caches, and pass traces.
- Keep future slices on one deterministic test harness instead of ad hoc per-module fixtures.
- Keep the living navigation surface in [`wiki/ir2/test-matrix.md`](./wiki/ir2/test-matrix.md) synchronized with this handoff doc when the matrix shape changes.

## Shared Helpers

- [`src/ir/test_helpers.mbt`](../src/ir/test_helpers.mbt):
  - `ir_test_build_hot_from_wat(...)` parses WAT, validates the module, lifts a selected defined function, and verifies the hot body.
  - `ir_test_assert_verify_and_lower(...)` verifies a mutated hot body, lowers it into the original module shell, and validates the result.
  - `ir_test_dump_cfg(...)`, `ir_test_dump_dom(...)`, `ir_test_dump_liveness(...)`, and `ir_test_dump_ssa(...)` produce deterministic cross-analysis text dumps.
- [`src/passes/pass_test_helpers.mbt`](../src/passes/pass_test_helpers.mbt):
  run the public pass pipeline from WAT fixtures, lift first-function fixtures through the shared checkpoint, and capture deterministic pass traces.

## Current Golden Fixtures

- CFG / dominance / liveness / SSA:
  a local-phi `if` fixture in [`src/ir/test_helpers_test.mbt`](../src/ir/test_helpers_test.mbt).
- Hot mutation and lower/validate roundtrip:
  a `nop` cleanup fixture plus a Binaryen-style tee/compare branch shape in [`src/ir/test_helpers_test.mbt`](../src/ir/test_helpers_test.mbt).
- Pass traces:
  a single-pass `vacuum` trace in [`src/passes/trace_golden_test.mbt`](../src/passes/trace_golden_test.mbt).

## Matrix

| Coverage area | Primary location | Expected proof |
| --- | --- | --- |
| Lift + verify | `src/ir/test_helpers.mbt`, `src/ir/test_helpers_test.mbt` | WAT fixtures validate before lift; lifted hot bodies pass core/control verification before analysis. |
| Lower + validate | `src/ir/test_helpers.mbt`, `src/ir/test_helpers_test.mbt` | Mutated hot bodies verify, lower into the original module shell, validate, and preserve important lowered shapes. |
| CFG shape | `src/ir/test_helpers_test.mbt`, `src/ir/cfg_test.mbt`, `src/ir/cfg_contract_test.mbt` | Stable entry/exit/synthetic blocks, region roots, predecessors, successors, branch/return/unreachable/exceptional edges. |
| CFG traversal order | `src/ir/cfg_order_test.mbt` | Preorder, reverse postorder, exceptional-inclusive RPO, worklist seed order, and region-local order stay deterministic. |
| Dominance | `src/ir/test_helpers_test.mbt`, `src/ir/dominators_test.mbt` | Stable immediate dominators, tree children, and dominance frontiers. |
| Post-dominance | `src/ir/postdominators_test.mbt` | Shared exits, separated return/exceptional exits, loop side-exits, and post-dominance frontiers are explicit. |
| Loop info | `src/ir/loop_info_test.mbt` | Natural-loop headers, bodies, latches, exits, depths, nested ownership, and early exits are covered. |
| Use-def | `src/ir/use_def_test.mbt` | Child/root use sites, block-local local traffic, unique read/write node lists, and lightweight node-use builds are covered. |
| Liveness | `src/ir/test_helpers_test.mbt`, `src/ir/liveness_test.mbt` | Live-in/live-out sets are stable across joins and use-def inputs; exceptional-edge policy is explicit. |
| Effects | `src/ir/effects_test.mbt` | Pure arithmetic, memory, calls, throws, traps, local state, region/block aggregation, and shared-DAG cache behavior are covered. |
| Local SSA build | `src/ir/test_helpers_test.mbt`, `src/ir/ssa_local_test.mbt` | Entry definitions, phi inputs, get/use mappings, and local-set/tee definitions are deterministic. |
| SSA destruction | `src/ir/ssa_destroy_test.mbt` | Phi lowering to predecessor copies, loop preheader/backedge copies, synthetic continuations, copy-cycle temps, dead-local cleanup, and temp-local retention choices are covered. |
| Analysis cache | `src/ir/analysis_cache_test.mbt` | Overlays are reused while the hot revision is unchanged and rebuilt or invalidated after mutation. |
| Pass execution | `src/passes/pass_test_helpers.mbt`, package pass tests | Pass fixtures run through the public optimizer path rather than pass-private harnesses unless the private helper is the unit under test. |
| Pass trace | `src/passes/trace_golden_test.mbt` | Trace capture uses the public optimizer trace callback and keeps stable pipeline/pass/function/mutation tokens. |

## Correctness Rules

- Golden output must stay deterministic across test runs.
- Shared helpers must use public lift/lower, analysis, cache, mutation, and pass APIs.
- Analysis overlays are revision-keyed caches, not owned IR; tests should prove cache reuse and rebuild where cache behavior is the subject.
- Trace goldens may assert revision numbers only when the mutation path is deterministic.
- Add new shared dump helpers only when multiple tests need the same durable cross-analysis output.

## Validation Plan

- Run `moon test src/ir` for IR helper, analysis, cache, or lowering changes.
- Run `moon test src/passes` for pass helper or trace changes.
- Before commit for behavior or API changes, run:
  - `moon info`
  - `moon fmt`
  - `bun validate readme-api-sync`
  - `moon test`

## Open Questions

- Whether later slices need shared text dumps for post-dominance, loop info, use-def, effects, or cache state in addition to the current focused tests.
- Whether trace goldens should eventually normalize revision numbers once mutation instrumentation grows.
